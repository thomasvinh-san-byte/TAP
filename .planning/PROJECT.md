# SaaS TAP Réunion

## What This Is

SaaS de régulation, optimisation, communication patient et pilotage pour
sociétés de Transport Assis Professionnalisé (TAP) et taxiteurs conventionnés
CGSS à La Réunion (974). L'outil sert au quotidien la régulatrice (utilisatrice
principale, 8 h/jour dans l'écran), les chauffeurs en tournée (PWA mobile), les
dirigeants (pilotage) et — en V1.5 — les donneurs d'ordres B2B (hôpitaux,
cliniques, EHPAD).

## Core Value

**La régulatrice doit avoir envie d'utiliser l'outil 8 heures par jour, 220 jours
par an, sans jamais le subir.** Tout le reste — moteur tarifaire, optimisation
de tournées, communication patient — sert cet objectif. Si la régulatrice
préfère son cahier papier, le produit a échoué.

## Requirements

### Validated

<!-- Shipped et validés. -->

- ✓ **FOND-01** : Monorepo Turborepo + pnpm workspaces opérationnel — Lot 0
- ✓ **FOND-02** : Multi-tenant Supabase via RLS forcée + `organization_id` — Lot 0
- ✓ **FOND-03** : Migrations Supabase 001 (foundations) et 002 (RLS) appliquées — Lot 0
- ✓ **FOND-04** : Tests pgTAP RLS automatisés en CI — Lot 0
- ✓ **FOND-05** : Packages `database` et `shared` initialisés — Lot 0
- ✓ **FOND-06** : ADR-001 (monorepo) et ADR-002 (RLS multi-tenant) actés — Lot 0
- ✓ **FOND-07** : CI/CD GitHub Actions (lint, types, tests, migrations) — Lot 0
- ✓ **FOND-08** : Comptes de démo seedés (dirigeant, régulateur, chauffeur) — Lot 0

### Active

<!-- Scope en cours. Référentiel détaillé dans REQUIREMENTS.md. -->

- [x] **REQ-patient-referentiel** : fiche patient complète avec NIR chiffré et préférences (Phase 1 livrée — 2026-05-06)
- [ ] **REQ-rgpd-compliance** : registre traitements + DPA + DPIA + portail droits patient *(Phase 1.5 — bloque first design partner)*
- [ ] **REQ-visible-progress** : Vercel preview + Supabase staging + seed démo 974 + comptes démo + showcase/ *(Phase 0.7 — Visible Progress Mandate CLAUDE.md § 13.5, débloque toute phase ≥ 2)*
- [ ] **REQ-saisie-express-course** : saisie d'une course en mode express < 30 s
- [ ] **REQ-moteur-tarification-cgss** : moteur tarifaire CGSS dans `packages/pricing`
- [ ] **REQ-courses-recurrentes** : génération de récurrences avec exceptions jours fériés 974
- [ ] **REQ-osrm-bootstrap** : `services/osrm` Docker + tuiles 974 + RPC distance/eta *(Phase 4.5 — débloque downstream)*
- [ ] **REQ-cockpit-regulateur** : cockpit temps réel par défaut, TTI < 2 s
- [ ] **REQ-planning-gantt** : planning drag-and-drop par chauffeur et par jour
- [ ] **REQ-gestion-imprevus** : workflows panne, patient absent, réaffectation
- [ ] **REQ-communication-sms-patient** : SMS sortants avec consentement actif
- [ ] **REQ-pwa-chauffeur** : PWA chauffeur terrain (hors-ligne, vocal, mode soleil)
- [ ] **REQ-geolocalisation-temps-reel** : capture position service uniquement + streaming cockpit + rétention 90j *(Phase 9.5)*
- [ ] **REQ-optimisation-tournees** : microservice Python OR-Tools + client TS
- [ ] **REQ-routing-gps-advanced** : geocoding inverse, alternatives, isochrones (Phase 11 — bootstrap déjà en Phase 4.5)
- [ ] **REQ-caisse-paiements-directs** : encaissements cash / CB / chèque rapprochés
- [ ] **REQ-mode-degrade** : continuité de service en panne réseau / Supabase / tiers
- [ ] **REQ-kpis-dirigeant** : CA, marge, mutualisation, productivité chauffeur *(Phase 14)*
- [ ] **REQ-conformite-reglementaire** : alertes 90/60/30j carte pro, CT, visite médicale, agrément ARS/CPAM *(Phase 15 — bloque mise en production commerciale)*
- [ ] **REQ-exports-comptables** : FEC annuel DGFiP, Lomaco CSV mensuel, PDF récap mensuel *(Phase 16)*
- [ ] **REQ-beta-terrain-chauffeur** : validation Hauts Réunion 35°C/3G dégradé/2-3 design partners *(Phase 17 — V1.5)*

### Out of Scope (v1)

<!-- Frontières explicites. -->

- **Application native iOS/Android** — la PWA chauffeur couvre les besoins terrain ; native repoussée si justifié
- **15 modules secondaires du CDC v2** non listés ici — à ingérer comme PRD une fois le `.docx` converti en `.md`
- **Portail B2B (apps/b2b)** — V1.5, après validation V1 par 2-3 design partners
- **Multi-langue** — français uniquement (cible 974, pas d'export hors France à court terme)
- **Application native chauffeur** — PWA suffit ; remplacement uniquement si limites techniques bloquantes
- **Analyse vidéo / dashcam** — hors scope produit
- **Facturation B2C complète** — la caisse couvre les paiements directs, pas un module compta
- **Outils marketing intégrés** — focus 100 % opérationnel, marketing en outils tiers

## Context

### Méthode produit
Le pilier 1 (UX qui donne envie) impose l'observation terrain. Toute journée
passée auprès d'une régulatrice, run en double avec un chauffeur, ou point avec
un dirigeant est consignée dans `docs/observations/AAAA-MM-JJ-societe-role.md`.
Anonymisation systématique (pas de NIR, pas de nom patient).

### État technique
- Lot 0 (fondations multi-tenant) **terminé** — commit `f68b1d2`.
- Lot 1 **en cours** : référentiels patients + saisie express.
- Branche active : `claude/setup-project-docs-ATeRo`.
- Repo : `thomasvinh-san-byte/tap`.
- Stack : Next.js 14 App Router, Supabase, OR-Tools, OSRM, MapLibre — figée (cf. décisions verrouillées).

### Document métier de référence
`docs/cahier_des_charges_saas_tap_v2.docx` — 24 modules fonctionnels. **9 modules
critiques** identifiés et adressés en V1 ; 15 modules secondaires à ingérer
ultérieurement (conversion `.docx` → `.md` requise pour parsing).

## Constraints

- **Tech stack** : Next.js 14 App Router, TypeScript strict, Tailwind + shadcn/ui, Supabase, MapLibre, FullCalendar/react-big-calendar, Python OR-Tools, OSRM auto-hébergé, Twilio ou OVH SMS Pro, Vercel, Sentry — **figée**, toute nouvelle dépendance majeure exige un ADR (cf. DEC-003).
- **Sécurité données de santé** : RGPD niveau santé + HDS pour la production commerciale. Supabase Cloud non HDS — bêta privée acceptable sous DPA, migration OVHcloud Postgres / Scaleway HDS anticipée. Architecture portable obligatoire (cf. CON-001).
- **Multi-tenant** : `organization_id` sur toute table métier + RLS forcée + tests pgTAP systématiques. `service_role` interdit côté client (audit CI bloquant) (cf. DEC-002).
- **Chiffrement applicatif** : NIR et notes médicales en AES-256-GCM, clé hors Supabase. TLS 1.3 minimum (cf. DEC-007).
- **Performance perçue** : saisie express < 30 s, feedback < 100 ms, action chauffeur < 1 s en 3G, TTI régulateur < 2 s (cf. DEC-005).
- **Accessibilité** : WCAG 2.1 AA, contraste 4.5:1, navigation clavier complète, `prefers-reduced-motion` respecté, mode contraste élevé + police +20 % côté chauffeur (cf. CON-005).
- **Design system** : spacing 4-8-12-16-24-32-48-64 px, polices Inter/Manrope/Geist Sans uniquement, icônes Lucide uniquement, mode jour ET nuit à parité, chiffres tabulaires obligatoires (cf. CON-006).
- **Localisation FR** : UI, erreurs, logs, commentaires, commits — **français uniquement**. Pas de jargon technique en UI (cf. DEC-011).
- **Limites code** : fichier ≤ 300 lignes, composant React ≤ 150 lignes, fonction ≤ 50 lignes, ≤ 3 niveaux d'imbrication (cf. CON-008).
- **Couverture tests** : `packages/pricing` et `packages/recurrence` à **100 % branches**, `packages/domain` ≥ 80 %, RLS tests pgTAP systématiques, E2E Playwright sur workflows imprévus (cf. DEC-013).
- **Architecture monorepo** : `apps/*` peuvent dépendre de `packages/*`, **jamais l'inverse** (cf. CON-012).
- **Sessions** : chauffeur 8 h max, régulateur 15 min inactivité, mode régulateur de garde mono-actif (cf. CON-014).
- **Géolocalisation** : capture uniquement pendant le service, rétention 90 j max, agrégation puis purge (cf. DEC-009).

## Key Decisions

<!-- Toutes verrouillées. ADRs sont sources d'autorité formelle ; les décisions DOC sont
élevées par autorité explicite du propriétaire projet sur CLAUDE.md. -->

<decisions locked="true">

| Décision | Source | Statut | Note |
|----------|--------|--------|------|
| **DEC-001** Monorepo Turborepo + pnpm workspaces | ADR-001 | LOCKED | `apps/*` ne dépendent QUE de `packages/*` |
| **DEC-002** Multi-tenant via RLS forcée + `organization_id` | ADR-002 | LOCKED | Tests pgTAP à chaque PR |
| **DEC-003** Stack technique figée (Next.js / Supabase / OR-Tools / OSRM) | CLAUDE.md § 3 | LOCKED | Nouvelle dépendance majeure = ADR |
| **DEC-004** 3 piliers non négociables (UX / design / sécurité HDS) | CLAUDE.md § 1 | LOCKED | Niveau qualité visé : Linear, Stripe, Notion |
| **DEC-005** Objectifs UX chiffrés (saisie < 30 s, TTI < 2 s, etc.) | CLAUDE.md § 1 | LOCKED | SLOs perçus, mesurés en E2E Playwright |
| **DEC-006** Authentification Supabase Auth PKCE + sessions | CLAUDE.md § 6 | LOCKED | Régulateur de garde mono-actif |
| **DEC-007** Chiffrement applicatif AES-256-GCM (NIR, notes médicales) | CLAUDE.md § 6 | LOCKED | Clé hors Supabase |
| **DEC-008** Consentement et règles SMS patient | CLAUDE.md § 6 | LOCKED | Pas d'envoi sans consentement actif horodaté |
| **DEC-009** Géolocalisation chauffeur (service uniquement, 90 j) | CLAUDE.md § 6 | LOCKED | Information préalable obligatoire |
| **DEC-010** Audit et traçabilité (`audit_logs` append-only) | CLAUDE.md § 6 | LOCKED | Index `(organization_id, ...)` |
| **DEC-011** Localisation FR + conventions de nommage | CLAUDE.md § 7 | LOCKED | Commits `type(scope): description` en français |
| **DEC-012** GitHub Flow adapté (`main` + `staging` + `feat/*`) | README.md | LOCKED | Migrations validées par CI avant merge |
| **DEC-013** Couverture tests (pricing 100 %, recurrence 100 %, domain ≥ 80 %) | CLAUDE.md § 9 | LOCKED | Vitest + Playwright + pgTAP + pytest |
| **DEC-014** Ergonomie chauffeur (boutons ≥ 56 px, 1 action / écran, swipe) | CLAUDE.md § 5 | LOCKED | Mode hors-ligne fonctionnel obligatoire |
| **DEC-015** Ergonomie régulatrice (cockpit accueil, `Cmd/Ctrl+Shift+K`, fuzzy 2 car) | CLAUDE.md § 5 | LOCKED | File d'attente brouillons + multi-saisies parallèles |
| **DEC-016** Localisation des règles métier en packages dédiés | CLAUDE.md § 11 | LOCKED | Tarification → `pricing`, récurrence → `recurrence`, SMS → `sms` |
| **DEC-017** Découpage Phase 04 monolithique | discuss 2026-05-13 | ABANDONNÉ | Remplacé par DEC-023 (refonte E2E). God-phase 17-25h jugée contre ADR-003. |
| **DEC-018** React Hook Form Phase 04+ pour nouveaux formulaires | discuss 2026-05-13 | LOCKED | Pas de migration rétroactive Phase 1/2. Premier usage = `/accept-invite` Phase 04. RHF + `zodResolver` standardise validation client/serveur. Server Actions conservées côté submit. |
| **DEC-019** Stack offline-first Serwist + Dexie 4.x | discuss 2026-05-13 | LOCKED | Recherche état de l'art 2026 (Next.js docs, LogRocket Jan 2026, Medium Apr 2026, PkgPulse Mar 2026). Successeur officiel `next-pwa` / `@ducanh2912/next-pwa`. Dexie 4.x choisi pour `useLiveQuery` réactif + migrations versionnées déclaratives. Détail Phase 04.9 CONTEXT. |
| **DEC-020** Transitions PWA Driver = fade-in `template.tsx` | discuss 2026-05-13 | LOCKED | Slide bidirectionnel iOS-style reporté Phase UI dédiée post-Passe 2. Cause : bug `vercel/next.js#42658` ouvert depuis 2022, `template.tsx` n'anime pas la sortie. View Transitions API Chrome only experimental. Verrou NFR-004 anti-`framer-motion` préservé. |
| **DEC-021** Pricing CGSS découpé en deux phases | discuss 2026-05-13 | LOCKED | Phase 04.7 livre UI mockup (stub DEMO + PricingBreakdown + Override + caisse). Phase 05.5 livre pricing réel (décision distance + grille dirigeant + calcul ±0,01 €). Cause : décision distance structurante (OSRM externe public non production-ready, choix entre auto-hébergé / Mapbox / Haversine) mérite discuss propre et validation dirigeant grille tarifaire 974. |
| **DEC-022** Persistence storage Dexie + warning UI > 7j inactivité | discuss 2026-05-13 | LOCKED | `navigator.storage.persist()` au mount PWA (WebKit accorde via heuristique home screen install). Tracking `lastUsedAt` + banner UI si > 7 j sans ouvrir. Cause : iOS purge IndexedDB après ~2 semaines inactivité PWA (WebKit script-writable storage cap), risque queue mutations + cache. Détail Phase 04.9 CONTEXT. |
| **DEC-023** Phase 04 god-phase abandonnée, refonte E2E logique | discuss 2026-05-13 | LOCKED | Découpage : 04 onboarding → 04.5 robustesse → 04.7 pricing mockup + caisse → 04.9 PWA enveloppe → 05 récurrences + cockpit + SMS + patient absent → 05.5 pricing réel → 06 HDS. Principe : logique métier web stable d'abord, mobilité PWA ensuite. PWA emballe un code mûr. Annule DEC-017. |
| **ADR-003** Pivot E2E par passes successives | `docs/adr/` 2026-05-11 | LOCKED | Inscription rétroactive dans bloc decisions PROJECT.md pour résoudre dette CONCERNS.md severity major. Méthode : chaque passe traverse les 6 maillons du parcours métier en améliorant le minimum partout, jamais un module en profondeur avant que tous existent. Critère de fin : design partner complète le parcours sans intervention dev. |
| **DEC-024** Workflow invitation chauffeur en 2 temps | discuss Phase 04 2026-05-13 | LOCKED | Bouton « Inviter » séparé de `createDriverAction`. Dirigeant crée d'abord la fiche métier `drivers` (peut rester sans email/compte connexion encore — cas hérité Phase 1, `profile_id` nullable déjà prévu), PUIS click « Inviter » quand prêt à rattacher un compte. Cohérent code existant. |
| **DEC-025** `driver_invitations` table séparée (PAS extension `drivers`) | discuss Phase 04 2026-05-13 | LOCKED | Évite duplication email (auth.users source de vérité après rattachement) et risque désynchronisation. La table `drivers` reste sans champ email. Pour affichage liste, JOIN informationnel avec `auth.users` via `profile_id`. |
| **DEC-026** `driverInvitationSchema` Zod séparé | discuss Phase 04 2026-05-13 | LOCKED | PAS d'extension `driverInputSchema`. Séparation des concerns : fiche métier vs compte connexion. Schéma : `{ email: z.string().email(), driverId: z.string().uuid() }`. |
| **DEC-027** Acceptation CGU obligatoire à `/accept-invite` | discuss Phase 04 2026-05-13 | LOCKED | Case à cocher obligatoire avec lien `/legal/cgu`. Trace `audit_logs` type `cgu_accepted_via_invitation`. Conforme Phase 1.5 RGPD. |
| **DEC-028** Pattern RHF sans wrapper `<Form>` shadcn pour formulaires simples | discuss Phase 04 2026-05-13 | LOCKED | `<Input>` `<Label>` `<Button>` shadcn directs pour formulaires ≤ 5 champs. Le wrapper `<Form>` shadcn ajoute des layers inutiles V1. Reste disponible pour formulaires complexes (caisse filters, override tarif Phase 04.7). |
| **DEC-029** Gestion chauffeurs élargie au régulateur (D1+D2+D3) | hotfix Phase 04 2026-05-13 | LOCKED | Phase 03.1 avait verrouillé CRUD + invitation au rôle `dirigeant`. Le métier réel taxi conventionné 974 = la régulatrice gère opérationnellement (embauche, invitation, modification). Sémantique affinée hotfix-bis 2026-05-13 en 4 actions distinctes : **Désactiver** (régulateur ou dirigeant, réversible facile, filet de sécurité) ; **Réactiver** (régulateur ou dirigeant, instantané) ; **Archiver** (DIRIGEANT UNIQUEMENT, confirmation renforcée motif 10..500 + saisie `ARCHIVER`) ; **Désarchiver** (régulateur ou dirigeant, confirmation simple). Trois lignes de défense : helpers `requireAdminOrRegulateur` / `requireDirigeant` au niveau Server Actions, RLS Postgres élargie, trigger column-level `drivers_archive_columns_guard` qui bloque l'archivage par non-dirigeant au niveau BDD. Sub-guard SSR `requireDirigeantPage` sur les autres pages admin (véhicules, legal/*) qui restent dirigeant-only. |
| **DEC-030** Conventions rédactionnelles FR user-facing | hotfix Phase 04 bis 2026-05-13 | LOCKED | Audit FR du repo en méthode C hybride (grep + revue manuelle par occurrence) sur les seuls textes user-facing (tsx UI, messages Zod, toasts, libellés ARIA, descriptions de Sheet/Dialog). Hors scope : commentaires code (// et /* */), JSDoc, docs .md, commentaires SQL, fichiers .test.ts. Règles appliquées (Option β) : (1) cadratin `—` en articulation/définition remplacé par `:` contextuel ; séparateurs de titre de page conservés (convention web FR Wikipédia/Stripe/Le Monde) ; (2) anglicismes verbes traduits — `assigner→affecter`, `assignation→affectation`, `modal→fenêtre`, `désassigner→désaffecter` ; (3) guillemets français `« »` privilégiés dans les messages d'erreur Zod. Dette future explicite : apostrophes typographiques `’`, espaces fines insécables U+202F. Toute nouvelle phase touchant à du texte user-facing doit respecter DEC-030 (audit ponctuel ré-effectué à chaque phase UI dédiée). |
| **DEC-031** Seed démo étendu pour UAT multi-chauffeurs | hotfix seed 2026-05-13 | LOCKED | Les 3 chauffeurs métier (Vergoz, Maillot, Boyer) sont désormais tous rattachés à un compte Auth distinct (`chauffeur@`, `chauffeur2@`, `chauffeur3@demo.tap`, mot de passe `demo1234!`). 12 courses fictives semées (5 historiques J-3 à J-1 statuts `terminee` / `annulee_regulateur` + 4 jour J0 mix `assignee` / `en_cours` / `validee` + 3 J+1 `assignee` / `validee`) pour peupler le cockpit régulateur dès le premier login UAT. `DemoCredentials` passe de 3 à 5 cards individuelles avec affichage du `type_permis` côté chauffeurs (taxi / taxi + TPMR). Réécrit l'hypothèse historique `seed.demo.sql` « un seul chauffeur lié auth » devenue obsolète post-Phase 04 (workflow d'invitation rattache dynamiquement). Seed adapté au schéma rides réel : `transport_mode` (pas `type_course`), `tarif_amount_eur` (pas `tarif_eur`), statuts `validee`/`assignee`/`en_cours`/`terminee`/`annulee_regulateur` (pas `planifiee`/`annulee` génériques). Aucune migration BDD impactée — données seulement. |
| **DEC-032** Politique migrations Supabase production via CD GitHub Actions | hotfix CD 2026-05-13 | LOCKED | `supabase db push` via le workflow `cd.yml` est le canal exclusif d'application des migrations sur Supabase prod/preview. L'application manuelle via Supabase MCP (`apply_migration`) reste autorisée en hotfix urgent uniquement, avec **obligation immédiate** de réconcilier `supabase_migrations.schema_migrations.version` sur le nommage repo après chaque application (sinon `db push` détecte le drift et refuse les pushes suivants). Incident 2026-05-13 : 3 migrations appliquées via MCP entre 10:52-10:53 UTC ont créé des versions auto-générées (timestamp courant `20260513105255/312/324`) au lieu des versions disque (`20260514000002`, `20260516000001`, `20260516000002`). 5 runs CD échoués jusqu'à correction par `UPDATE schema_migrations SET version = ...`. Playbook documenté en `CONCERNS.md`. |
| **DEC-033** Clés React listes de composants client incluant champ mutable | hotfix UAT 2026-05-13 | LOCKED | Quand une liste rend un composant **client** dont l'affichage dépend d'un champ mutable (status / actif / archive…), la clé React **DOIT** inclure ce champ : `key={\`${id}-${status}\`}`. Sinon `router.refresh()` repeuple les props serveur mais le composant client conserve son state interne et l'UI reste figée sur l'ancien rendu (bouton actif, badge inchangé, etc.). Précédent Phase 03.2 #4 (bouton Annuler course restait actif après annulation). Récidive UAT Phase 04 2026-05-13 (bouton « Démarrer la course » côté chauffeur restait visible après transition `assignee → en_cours`). Listes corrigées en hotfix : `(driver)/conduite/page.tsx` (status), `courses/rides-list.client.tsx` (status), `admin/chauffeurs/drivers-list.client.tsx` (actif + archive), `admin/vehicules/vehicles-list.client.tsx` (actif). Mécanisme préventif : audit `grep -rn "key={.*\\.id}"` à chaque PR qui ajoute une liste de composants client. |
| **DEC-034** Design system TAP codifié dans `.planning/UI-PATTERNS.md` | audit visuel post-Phase 04 2026-05-14 | LOCKED | Référence dense ops Linear / Stripe / Cal.com / `satnaing/shadcn-admin`. Layout pages admin : header sobre + toolbar (filtres pills + recherche + CTA `Nouveau`) + liste `divide-y rounded-md border` dense (~64-72 px/ligne) + Sheet latéral édition. Badges sémantiques 5 familles (type métier neutre, statut activité vert, neutre gris, warning amber, critique rouge) avec max 3 badges/ligne. Spacing scale 8 px strict (4/8/12/16/24/32/48/64). Color = signal (rouge réservé erreur ou destruction). Filtres pills segmented control iOS-like ≤ 4 options. Sidebar navigation `shadcn/ui Sidebar` (pas de tabs top page). Anti-patterns proscrits : cards individuelles ombrées en liste, avatars > 40 px hors header, modal pleine page édition, action destructive non séparée par `border-t`. Refactor progressif des pages admin existantes en Phase 04.5+ selon priorisation UAT dirigeant (`/admin/chauffeurs` aligner densité Linear, `/admin/legal/*` à auditer). |
| **DEC-035** Table `pois_metier` seedée pour recherche adresse métier 974 | Phase 04.5 plan 2026-05-15 | LOCKED | Nouvelle table `public.pois_metier` (organization_id, nom_court, nom_long, type_poi enum 11 valeurs, adresse, code_postal 974xx, ville, lat/lng, téléphone, notes_acces, actif) avec RLS same-org (`pois_metier_select_same_org`, `pois_metier_modify_admin_or_regulateur`), trigger audit_logs, index gin sur `to_tsvector('french', nom_court || nom_long)`. Seed `seed.demo.sql` : ~30 POI Réunion réels (CHU Félix Guyon, CHU Sud, Clinique Saint-Vincent, centres dialyse Nord/Sud/Ouest, EHPAD Les Lataniers/Mascareignes, cabinets kiné/ophtalmo, etc.). Migration appliquée via CD `supabase db push` (DEC-032). Permet à la régulatrice de saisir `« CHU »` et trouver instantanément le bon hôpital sans dépendre du score BAN. |
| **DEC-036** Masques de saisie stricts pour formulaire patient | Phase 04.5 plan 2026-05-15 | LOCKED | Tous champs sensibles RGPD du formulaire patient passent par des masques + validation : `DatePicker` FR `react-datepicker` format JJ/MM/AAAA (range 0-130 ans), NIR 15 chars + validation clé contrôle `97 − N mod 97` (gestion Corse 2A/2B → 19/18) avec indicateur visuel `CircleDashed`/`CheckCircle2`/`XCircle`, téléphone Réunion strict `0(262\|263\|692\|693)` ou `+262(62\|63\|92\|93)` (refus métropole 06/07), code postal préfixe `974` disabled + 2 chiffres saisissables, ville `z.enum(VILLES_974)` liste fermée 24 communes INSEE avec auto-complétion CP→Ville, regex nom/prénom `^[A-Za-zÀ-ÿ\s'-]+$`. Messages d'erreur Zod refondus FR (DEC-030) avec exemple format inline (« 1 76 05 25 974 001 12 »). Refus silencieux interdit. Fallback time-box : si algo NIR clé contrôle dépasse 1 h, mode dégradé `refine() => true` + Edge Function `nir-encrypt` valide côté serveur (dette CONCERNS.md). |
| **DEC-037** Composant `AddressOrPOIPicker` mixe POI métier + BAN | Phase 04.5 plan 2026-05-15 | LOCKED | Refonte `address-picker-field.client.tsx` → `address-or-poi-picker.client.tsx`. UI dropdown 2 sections séparées par `<Separator />` : « Lieux fréquents » (POI métier, prioritaires, icône `Hospital` Lucide) + « Adresses BAN » (icône `MapPin`). Sélection POI préremplit `adresse + code_postal + ville + notes_acces` ; sélection BAN préremplit `adresse + code_postal + ville` uniquement. ARIA pattern combobox complet : `aria-expanded`, `aria-controls`, `aria-activedescendant`. `aria-label` distinct par section pour annonce VoiceOver. Si BAN down : variant « POI seul » ; si pas de POI match : variant « BAN seul ». |
| **DEC-038** Modal d'assignation filtre par défaut chauffeurs compatibles avec véhicule | Phase 04.5 plan 2026-05-15 | LOCKED | Helper pur `isCompatible(driver, vehicle): boolean` dans `packages/shared/src/utils/driver-vehicle-compat.ts` (100 % branch coverage Vitest, 16 paires testées). Matrice : véhicule TPMR exige `tpmr ∈ type_permis`, VSL accepte `taxi`, taxi exige `taxi`, ambulance exige `ambulance`. UI `assign-modal.client.tsx` : toggle pills « Compatibles » (défaut) / « Afficher tous » (urgence), badge Compatible/Incompatible 1/ligne avec icône Lucide, warning explicite si incompatible sélectionné (`bg-warning/10` + `text-warning` icône + `text-destructive` texte). Clé React inclut `vehicle.type` mutable : `key={\`${driver.id}-${selectedVehicle?.type ?? 'none'}\`}` pour respecter DEC-033 (badge change selon véhicule). |
| **DEC-039** Seed démo `rides` idempotent + glissant via `ON CONFLICT DO UPDATE` | Phase 04.5 plan 2026-05-15 | LOCKED | `seed.demo.sql` patché : bloc INSERT rides démo `id LIKE '44444444-%'` passe de `ON CONFLICT (id) DO NOTHING` à `ON CONFLICT (id) DO UPDATE SET scheduled_at = EXCLUDED.scheduled_at, started_at = EXCLUDED.started_at, ended_at = EXCLUDED.ended_at, created_at = EXCLUDED.created_at`. Comme les valeurs source utilisent `date_trunc('day', now() - interval '3 days')` etc., chaque re-application CD fait glisser les dates de 1 jour. UAT peut être déroulé n'importe quel jour sans reset BDD manuel. Limitation au scope rides démo uniquement (préfixe ID 44444444). Autres seeds gardent `ON CONFLICT DO NOTHING` standard. |
| **DEC-041** Pattern Server Action Supabase avec row count check | Phase 04.5 amendement 2026-05-15 | LOCKED | Toute Server Action faisant UPDATE/DELETE sur une table avec RLS DOIT suivre le pattern : `const { data, error } = await supabase.from('table').update(payload).eq('id', id).select('id'); if (error) return { error: ... }; if (!data \|\| data.length === 0) return { error: 'Modification refusée — droits insuffisants.' };`. **Justification** : RLS Postgres rejette silencieusement les UPDATE qui ne match aucune ligne (0 rows affected, pas d'erreur SQL). Sans `.select()` + check `length`, l'application affiche un faux success. **Incident 2026-05-15** : `startRideAction` et `endRideAction` renvoyaient `{ success: true }` malgré RLS blocking → chauffeur croyait avoir démarré la course alors qu'elle restait `assignee` en BDD. **Scope Phase 04.5** : pattern appliqué UNIQUEMENT à `startRideAction` et `endRideAction` (mini-fix T1.4). L'audit complet de TOUTES les Server Actions avec application systématique du pattern est **reporté Phase 06 HDS** (conformité production-grade — sa place naturelle). Inscrit dans CONCERNS.md la liste des actions restant à auditer. |
| **DEC-042** Pricing stub Phase 04.7 = Haversine + fallback random | Phase 04.7 plan 2026-05-15 | LOCKED | `computeCgssShortTrip(input): PricingResult` dans `packages/pricing/` (nouveau workspace). **Algo** : si `pickup_lat/lng` ET `dropoff_lat/lng` sont fournis → calcul Haversine (formule sphérique terre, rayon 6371 km) puis tarif = `4.20 + dist_km × 2.10`. Sinon → `random(min, max)` dans plage cohérente avec `transport_mode` + `urgency` (`source: 'fallback_random'`). Toujours appliquer : majo nuit (+20 % entre 20 h et 7 h selon `scheduled_at`), supplément TPMR (+5 € si `transport_mode === 'tpmr'`), arrondi à 0.05 € le plus proche. Total clamp final `[10 €, 80 €]`. **Badge UI « DEMO »** obligatoire (Surface A `PricingBreakdown`) — rappelle que ce n'est PAS le calcul CGSS final (Phase 05.5 livrera l'algo réel). **Couverture Vitest** : 100 % branch sur `computeCgssShortTrip` (DEC-013 esprit — calcul critique data financière même stub). **Hors scope DEC-042** : grille tarifaire CGSS réelle (forfait/km/majo dim/férié 974), décision distance (OSRM / Mapbox / Haversine + override Hauts), validation grille dirigeant — tout cela en Phase 05.5 DEC-021. |
| **DEC-043** RLS caisse `/courses/caisse` exclusion chauffeur V1.5 | Phase 04.7 plan 2026-05-15 | LOCKED | Page `/courses/caisse` accessible **régulateur + dirigeant uniquement** (route guard `requireAdminOrRegulateur` côté Server Component). Chauffeur reçoit 403 explicite : « Cette page est réservée au régulateur. Vous pouvez consulter vos encaissements depuis l'écran de clôture de course. ». Server Actions `listRidesEncaisseesAction` + `exportCaisseCsvAction` requirent même check (defense in depth). RLS Postgres : queries scope `organization_id = current_organization_id()` (pas de filtre par chauffeur côté serveur — la régulatrice voit tout son org). **Justification** : la régulatrice gère 8 h/jour la vue Stripe Balance-like ; le chauffeur a déjà sa vue end-ride-modal pour ses propres encaissements. Vue chauffeur dédiée (`/conduite/mes-encaissements`) différée Phase 04.9 (PWA chauffeur). |
| **DEC-044** Géocoding persistance threadé BAN + POI dans `AddressOrPOIPicker.onSelect` | Phase 04.7 plan 2026-05-15 | LOCKED | Migration `<timestamp>_rides_geocoding.sql` ajoute 6 colonnes nullables sur `public.rides` : `pickup_lat numeric(10,7)`, `pickup_lng numeric(10,7)`, `pickup_citycode text`, idem `dropoff_*`. Index optionnel `rides_pickup_citycode_idx` partiel. Migration via CD push exclusif (DEC-032). **Threading UI** : `AddressOrPOIPicker.onSelect` signature élargie pour propager `lat?: number, lng?: number, citycode?: string` au parent (consommateurs `ride-express-modal.client.tsx` `createRideAction` + `editRideAction`). **Sources** : POI = `pois_metier.latitude/longitude` (déjà nullables PR #84), BAN = `f.geometry.coordinates[1]/[0]` + `f.properties.citycode`. Saisie libre = pas de coords (acceptable, fallback random pricing DEC-042). **Backfill courses existantes** : Server Action `backfillRideGeocodingAction` (dirigeant only) depuis `/admin/maintenance`, itère rides où `pickup_lat IS NULL`, appelle BAN gouv.fr, DO UPDATE idempotent, toast progress. Pas de cron, déclenchement manuel one-shot post-déploiement. |
| **DEC-045** Route Handlers explicites pour mutations driver PWA (Alt A vs Server Actions via fetch headers `Next-Action`) | Phase 04.9 discuss 2026-05-18 | LOCKED | Phase 04.9 PWA chauffeur enveloppe : mutations offline `start ride` / `end ride` passent par **Route Handlers REST classiques** `/api/driver/rides/[id]/start` + `/api/driver/rides/[id]/end` (Alt A). **Justification** : (1) visibilité explicite endpoints (curl-able pour debug device réel), (2) compatibilité Serwist offline cache standard `NetworkOnly` + queue, (3) cohérence pattern REST web, (4) Server Actions via `fetch(url, { headers: 'Next-Action' })` fonctionnent mais opaques pour debug offline + Serwist cache strategies plus complexes. **Auth** : cookies Supabase (middleware Next.js déjà en place, pas de JWT séparé). **Schéma payload** : JSON `{ idempotency_key: uuid, client_timestamp: iso8601, ...payload }` — `idempotency_key` UUID client-side dédupé server-side (colonne unique sur `ride_events` à valider plan-phase Wave 3). **Stratégie cross-platform Option A** : iOS Safari (apple-touch-startup-image 3 DPR + apple-touch-icon 180) + Android (manifest 192/512 maskable auto-généré) testés en parallèle Wave 6 sur 2 devices réels. |
| **DEC-046** Moteur récurrences = `rrule.js` standard RFC 5545 | Phase 05 discuss 2026-05-18 | LOCKED | Dépendance mineure (~30 KB), RFC iCalendar standard, bien maintenu, exceptions natives via `EXDATE`. **Justification** : pas de réinvention, compatible Vitest 100% branches DEC-013, timezone-aware (DST 974). Alternative écartée : custom léger (réinvention fragile, dette tests). |
| **DEC-047** Génération occurrences eager batch 3 mois + extension cron mensuel | Phase 05 discuss 2026-05-18 | LOCKED | À la création de récurrence : batch génération 3 mois d'occurrences. Cron Vercel mensuel (DEC-050) étend de 3 mois à chaque run. **Justification** : équilibre planning visibility (régulatrice voit 3 mois) + complexité modérée (pas génération à la volée fragile). Alternative écartée : lazy cron quotidien (perte visibility planning) ou eager 12 mois (volume BDD inutile). |
| **DEC-048** Modification récurrence active = modal confirm + regen futures non-démarrées | Phase 05 discuss 2026-05-18 | LOCKED | Régulatrice modifie jour/heure/durée d'une récurrence en cours : modal confirmation « X occurrences seront affectées (non-démarrées uniquement). Confirmer ? ». Regen toutes occurrences futures status `assignee` (et `non_assignee`). Préserve `en_cours` / `terminee` / `annulee_*`. `audit_logs` capture le diff (avant/après). **Justification** : protège l'intégrité métier (course commencée intouchable) + transparence régulatrice. |
| **DEC-049** Channel Realtime cockpit = global `cockpit:rides` MVP | Phase 05 discuss 2026-05-18 | LOCKED | Single channel Supabase Realtime `cockpit:rides` listen `postgres_changes` sur `rides` table UPDATE. **Scoping multi-tenant Phase 06+** (ADR HDS migration + Stack BDD long terme conjointe). MVP single-régie suffit V1.5 (1 design partner, 1 organization_id). **Justification** : simplicité MVP, scaling plus tard quand multi-régies. Alternative écartée : broadcast manuel via Edge Function (complexité, latence inutile). |
| **DEC-050** Cron jobs SMS = pg_cron Supabase + pg_net HTTP call → Route Handler Next.js | Phase 05 discuss 2026-05-18 / révisé UI-SPEC 2026-05-18 | LOCKED | **Révision UI-SPEC** : pg_cron 1.6.4 déjà activé sur projet Supabase (vérifié SQL). pg_net à activer Wave 1 (extension Supabase standard, gratuite, async HTTP/HTTPS depuis SQL, throughput 200 req/s). Coût 0€ supplémentaire (vs Vercel Cron Pro 20$/mois si Hobby actuel non confirmé). Timezone Réunion native PostgreSQL (vs UTC seul Vercel). **Cohérent CONCERNS architecture portable V1.5** (PR #119) : pg_cron part avec Postgres self-host Phase 06 HDS migration, zero reconfiguration. Pattern combo : (1) `cron.schedule('sms-j1', '0 18 * * *', $$ SELECT net.http_post(...) $$)` déclenche HTTP, (2) Route Handler Next.js `/api/cron/sms-reminders-j1` reçoit appel, valide `Authorization: Bearer ${CRON_APP_TOKEN}`, query rides + Twilio + tracking. **Auth token** stocké Supabase Vault (`vault.decrypted_secrets`) + env var miroir Vercel `CRON_APP_TOKEN`. **Observabilité** : `cron.job_run_details` + `net._http_response` SQL-queryable. **Logique métier TypeScript préservée** (testable Vitest, cohérent stack figée DEC-003). |
| **DEC-051** Templates SMS personnalisation = Mustache-like custom léger 5 variables | Phase 05 discuss 2026-05-18 | LOCKED | Implémentation custom ~20 lignes dans `packages/sms` : `interpolate(template, vars)` qui remplace `{{patient_prenom}}`, `{{patient_nom}}`, `{{heure}}`, `{{date}}`, `{{chauffeur_prenom}}`. **Justification** : pas de dépendance Handlebars (40+ KB, over-engineered), 5 variables suffisent V1.5, tests Vitest triviaux. Alternative écartée : Handlebars complet (dépendance majeure DEC-003), template literals JS (moins UX éditable régulatrice). |
| **DEC-052** Webhook Twilio delivery status = Route Handler HMAC vérifiée | Phase 05 discuss 2026-05-18 | LOCKED | Route Handler `POST /api/sms/webhook/twilio` reçoit les callbacks Twilio delivery status (`sent` → `delivered` → `failed` → `undelivered`). **Sécurité** : vérification `X-Twilio-Signature` HMAC SHA1 avec `TWILIO_AUTH_TOKEN` env var (standard Twilio). **Justification** : empêche injection callbacks falsifiés. Update `sms_message.status` + `audit_logs` à chaque transition. Pas de retry côté serveur (Twilio gère). |
| **DEC-053** Endpoint patient absent driver = Route Handler cohérent DEC-045 | Phase 05 discuss 2026-05-18 | LOCKED | `POST /api/driver/rides/[rideId]/no-show` avec payload `{ idempotency_key: uuid, motif?: string }`. **Pattern réutilisé DEC-045** : auth cookies Supabase (middleware), idempotency UUID dédupé (table `idempotency_keys` Phase 04.9 Wave 1), status guards (assignee → no_show_pending), row count check DEC-041, INSERT `ride_events`, audit_logs. Trigger Realtime alerte cockpit < 5s. Décision régulatrice (Reprog/Annul) via Server Action `/cockpit` (régulatrice online uniquement, pas besoin Route Handler). |
| **DEC-054** Cockpit landing page régulatrice | Phase 05 discuss 2026-05-18 | LOCKED | Redirect login régulatrice → `/cockpit` (au lieu de `/courses`). Cohérent COCK-01 écran d'accueil par défaut. `/courses` reste accessible via menu nav (link header). **Justification** : cockpit = vue principale 8h/jour régulatrice (CLAUDE.md § 1 pilier 1 UX). `/courses` = vue secondaire pour gestion non-temps-réel. Pas de breaking change pour dirigeant (qui reste sur `/patients` ou `/admin`) ni chauffeur (qui reste sur `/conduite`). **Amendée par DEC-071 (2026-05-21)** : la clause incidente sur le dirigeant est mise à jour — il atterrit désormais sur `/tableau-de-bord` ; le cœur (régulateur → /cockpit) est préservé. |
| **DEC-055** Notification famille patient absent → Phase 06 | Phase 05 discuss 2026-05-18 | LOCKED | Hors scope V1.5. SMS-06 respecté si famille a consentement actif horodaté distinct du patient. **Justification** : complexité légale RGPD (consentement tiers, contact urgence vs représentant légal, droit à l'information famille), pas de demande explicite design partner V1.5. Phase 06 si demande validée + ADR consentement tiers. |
| **DEC-056** Distance pricing = Haversine + facteur de correction routier | Phase 05.5 discuss 2026-05-19 | LOCKED | V1.5 : `distance_routiere = haversine × facteur_correction`. Facteur ~1,4 (relief 974, écart vol d'oiseau / routier élevé en zone montagneuse — cirques, routes sinueuses), **paramétrable BDD** (`tariff_grids.facteur_correction_routier`). OSRM auto-hébergé reporté Phase 06 (arrive avec la géolocalisation certifiée Assurance maladie obligatoire 1er janvier 2027). **Justification** : facturation CGSS réelle = Phase 06, donc V1.5 = tarif estimatif — la précision absolue est moins critique. Alternatives écartées : Haversine pur (sous-facturation Hauts), OSRM dès 05.5 (infra lourde), API externe Mapbox/Google (hors VISION + coût + dépendance DEC-003). |
| **DEC-057** Grille tarifaire = table BDD `tariff_grids` versionnée | Phase 05.5 discuss 2026-05-19 | LOCKED | Table `tariff_grids` : `id`, `organization_id`, `date_effet`, `forfait_eur` (13), `km_inclus` (4), `prix_km_eur` (1,22 défaut 974), `supplement_drom_eur` (3), `supplement_tpmr_eur` (30), `majoration_pct` (50), `facteur_correction_routier` (1,4), `arrondi`, `created_by`, `created_at`. `computeCgssShortTrip` prend la grille en **paramètre** → fonction pure, testable 100 % branches Vitest sans BDD (DEC-013, PRIC-03). **Justification** : tarif km 974 + supplément DROM jamais hardcodés — volatilité du conflit syndical local 974, négociation CGSS Réunion possible courant 2026 ; le dirigeant ajuste sans redéploiement + historique versionné. Alternative écartée : constantes dans le code (chaque changement = redéploiement). |
| **DEC-058** Périmètre pricing V1.5 = cas nominal monopatient | Phase 05.5 discuss 2026-05-19 | LOCKED | DANS V1.5 : forfait 13 € (4 km inclus) + km facturés au 5e km+ + supplément DROM 3 € + supplément TPMR 30 € + majoration nuit/we/férié 50 % non-cumulable. HORS Phase 06 : transport partagé + abattements multi-patients (-23/-35/-37 %) + retour à vide hospitalisation (+25/+50 %) + forfait Grande Ville (non applicable 974). **Justification** : le monopatient est le cas le plus fréquent ; le transport partagé est une feature à part entière (UI courses groupées + modélisation + abattements). |
| **DEC-059** Majoration nuit/we/férié = approximation heure de prise en charge | Phase 05.5 discuss 2026-05-19 | LOCKED | V1.5 : majoration 50 % si l'heure de `scheduled_at` est dans 20h-8h (nuit) OU samedi ≥ 12h / dimanche (week-end) OU date présente dans `holidays_974` (jour férié, table Phase 05). **Corrige le double bug du stub `isNuit`** : `getUTCHours` → `Indian/Reunion` (UTC+4) + prise en charge des cas we/férié. Une seule majoration 50 % maximum (non-cumulables entre elles). **Justification** : la règle exacte convention (« > 50 % du temps de transport en charge de nuit ») nécessite la durée routière → reportée Phase 06 avec OSRM. L'approximation heure de départ est simple et les données sont disponibles V1.5. |
| **DEC-060** Recalcul rétroactif pricing = Server Action dirigeant + garde-fous | Phase 05.5 discuss 2026-05-19 | LOCKED | Server Action one-shot `/admin/maintenance` (pattern `backfillRideGeocodingAction` DEC-044, dirigeant only). Recalcule uniquement les courses au tarif stub DEMO **non facturées / non encaissées**, **préserve les overrides manuels** (`source = 'manual_override'` Phase 04.7), `audit_logs` chaque recalcul (PRIC-04). **Justification** : protège les corrections dirigeant volontaires + ne touche pas aux courses déjà encaissées (intégrité comptable). |
| **DEC-061** Transition stub → réel = `PricingResult` enrichie + disclaimer estimatif | Phase 05.5 discuss 2026-05-19 | LOCKED | `PricingResult` enrichie : `forfait_eur`, `km_factures` (au-delà de 4), `km_total_eur`, `supplement_drom_eur`, `supplement_tpmr_eur`, `majoration_pct`, `majoration_eur`, `total_eur`, `source`, `distance_method`. Le badge « DEMO » disparaît (vrai calcul) mais un disclaimer sobre subsiste : « tarif estimatif, non contractuel jusqu'à facturation CGSS (Phase 06) ». `PricingBreakdown` (Phase 04.7) adapté pour la décomposition réelle. **Justification** : V1.5 n'a pas la télétransmission CGSS — le tarif reste estimatif jusqu'à Phase 06, le disclaimer évite toute ambiguïté contractuelle. |
| **DEC-062** Fournisseur SMS différé + adaptateur pluggable | ADR-004 2026-05-20 | LOCKED | Twilio écarté (société US, CLOUD Act — inadapté aux données de santé indirectes du transport sanitaire 974). Le choix du fournisseur SMS est différé : aucune intégration en V1.5. Les 2 cron jobs SMS Phase 05 sont mis en pause (migration `20260524000001_unschedule_sms_cron`, idempotente). `packages/sms` + 3 Route Handlers + UI `/admin/sms-templates` + tables `sms_*` conservés **dormants** (zéro coût, réutilisables). Cible privilégiée = API SMS française HDS (smsmode ISO 27001/HDS, Octopush, OVH SMS) cohérente avec la trajectoire HDS Phase 06 ; RaspiSMS auto-hébergé = plan B. Adaptateur **pluggable** : changer de fournisseur = réécrire le seul `packages/sms/src/twilio-adapter.ts`. **Justification** : pas de dépendance US pour des données de santé, décision reportée sans dette technique. Cf. ADR-004. **Précise DEC-003** (qui mentionnait déjà « Twilio ou OVH SMS Pro ») — ce n'est pas une révision. |
| **DEC-040** Guard `require*` partagé obligatoire sur les Server Actions de mutation admin/métier | candidate CONCERNS → promue Phase 06 PLAN-4 2026-05-21 | LOCKED | Toute Server Action modifiant des données admin/métier commence par un guard `require*` **partagé** (`@/lib/auth/require-dirigeant`, `require-admin-or-regulateur`). Pas de `requireX` local dupliqué (corrigé sur `vehicules/actions.ts`). Couplé à DEC-041 (row count check). Audit complet : `docs/security/SERVER-ACTIONS-AUDIT.md`. |
| **DEC-063** Phase 06 resserrée (E2E Passe 4 redécoupée) | discuss Phase 06 2026-05-20 | LOCKED | Phase 06 = Bloc A (facturation CGSS PDF) + Bloc E (audit RLS / Server Actions + advisors) + Bloc F (dettes CI D1/D2/D3). HDS et OR-Tools sortis en sous-phases dédiées 06.5 / 06.7 ; portail B2B et télétransmission B2/CNDA différés via ADR. Une phase qui livre un incrément cohérent et testable plutôt qu'une méga-phase de 19-32 h. |
| **DEC-064** Facturation CGSS V1.5 = PDF récapitulatif mensuel ; télétransmission B2/CNDA différée | discuss Phase 06 2026-05-20 | LOCKED | La facturation V1.5 est un PDF récapitulatif mensuel (`/admin/facturation`) consommant le moteur 05.5. La télétransmission B2/SEFi via logiciel certifié CNDA + formulaire 606b est différée : l'échéance du 31 mai 2026 pèse sur le taxi qui télétransmet, pas sur l'éditeur d'un SaaS en bêta sans client facturant. Cf. ADR-005. |
| **DEC-065** Migration HDS = Phase 06.5 dédiée | discuss Phase 06 2026-05-20 | LOCKED | La migration de l'hébergement vers une infra HDS-certifiée est une sous-phase dédiée (discuss propre + ADR pour le choix fournisseur Scaleway HDS / OVHcloud HDS / validation Supabase EU + DPA). En bêta sans client facturant, Supabase EU sous DPA reste acceptable ; HDS strict = prérequis du 1er client payant. CON-001 (architecture portable) maintenue. **Renumérotée 2026-05-22 : Phase 06.5 → Phase 09 — HDS repoussée en fin de parcours (DEC-077) ; la décision LOCKED reste inchangée sur le fond.** |
| **DEC-066** OR-Tools optimisation de tournées = Phase 06.7 dédiée | discuss Phase 06 2026-05-20 | LOCKED | Microservice Python OR-Tools (`services/optimizer`) + `packages/optimizer-client` en sous-phase dédiée. Distance V1 = Haversine × facteur de correction routier (DEC-056) ; OSRM auto-hébergé avec la géolocalisation certifiée Assurance maladie (1er janvier 2027). |
| **DEC-067** Portail B2B multi-tenant commercial différé | discuss Phase 06 2026-05-20 | LOCKED | Le portail B2B (signup Stripe, isolation tenant, onboarding donneur d'ordres) est différé jusqu'au 1er client payant validant le produit — anti-construction de multi-tenant commercial avant product-market fit. Réactivation = décision business. Cf. ADR-006. |
| **DEC-068** Pré-remplissage RGPD déclenché + revue-puis-insertion | Phase 06.6 2026-05-21 | LOCKED | Le pré-remplissage des pages RGPD est déclenché par un bouton (jamais d'auto-remplissage silencieux) et passe par un écran de revue : seules les entrées validées par le dirigeant sont insérées, jamais d'INSERT direct. Motif : `data_processing_register` est append-only (ni UPDATE ni DELETE) — une ligne erronée ne se supprime pas. Protège la responsabilité du dirigeant et de TAP. |
| **DEC-069** Périmètre du pré-remplissage de l'espace Légal | Phase 06.6 2026-05-21 | LOCKED | Pré-remplissage RÉEL : registre des traitements (6 entrées-types transport sanitaire) + fiches DPA des sous-traitants techniques (le dirigeant saisit la date de signature et la version) + trame squelette DPIA (`status='brouillon'`, sans verdict de risque). Aide contextuelle seule (textes, aucune donnée fictive) : breaches, requests, dpo — vides par nature. |
| **DEC-070** Non-certification juridique du pré-remplissage RGPD | Phase 06.6 2026-05-21 | LOCKED | Le contenu pré-rempli est une suggestion sourcée sur des références publiques (CNIL, Code de commerce) — TAP ne certifie pas la conformité. Disclaimer de non-certification obligatoire sur l'écran de revue, avant insertion. Aucune entrée validée n'est marquée « suggéré » (responsabilité transférée au dirigeant). Le SUMMARY recommande une relecture par un juriste ou le DPO. |
| **DEC-071** Redirection par rôle à la connexion | Phase 06.8 2026-05-21 | LOCKED | À la connexion (action de login) et à la racine `/` (`app/page.tsx`), chaque rôle est redirigé vers sa page d'accueil : dirigeant → `/tableau-de-bord`, régulateur → `/cockpit`, chauffeur → `/conduite`. Remplace le `redirect('/patients')` uniforme. **Amende DEC-054** : la clause incidente « le dirigeant reste sur /patients/admin » devient « dirigeant → /tableau-de-bord » ; le cœur de DEC-054 (régulateur → /cockpit) est préservé — les deux décisions partagent la même logique de landing par rôle. |
| **DEC-072** Tableau de bord dirigeant = pilotage, rendu au chargement | Phase 06.8 2026-05-21 | LOCKED | Le tableau de bord (`/tableau-de-bord`) est réservé au rôle `dirigeant` (`requireDirigeantPage`), rendu au chargement (Server Component, pas de temps réel — le live reste au Cockpit régulateur). Il agrège 6 KPIs en réutilisant les définitions de Caisse / Facturation (chiffres identiques) ; il n'est pas une duplication du Cockpit. |
| **DEC-073** Carte de statut de conformité = factuelle | Phase 06.8 2026-05-21 | LOCKED | La carte de conformité du tableau de bord affiche des compteurs documentaires factuels (registre, DPA, DPIA + dernière mise à jour + lien `/admin/legal`) — jamais un verdict, un feu vert, un score ou une mention « conforme RGPD ». Le RGPD (art. 5-2) impose de démontrer la conformité, pas de la revendiquer — cohérent avec la non-certification de DEC-070. |
| **DEC-074** Positionnement non-certifié — TAP s'interface | révision roadmap 2026-05-22 | CANDIDATE | TAP est un logiciel opérationnel non certifié. Il s'interface par export avec une solution certifiée tierce (facturation/géoloc certifiée Assurance maladie). La certification CNDA/SEFi et la géoloc certifiée obligatoire au 1er janvier 2027 sont **hors périmètre TAP** — elles incombent au taxi et à sa solution certifiée. Précise DEC-064 et ADR-005. Risque avant-vente : clarifier que TAP est complémentaire, pas un substitut. |
| **DEC-075** Géoloc opérationnelle post-HDS | révision roadmap 2026-05-22 | CANDIDATE | La géolocalisation opérationnelle temps réel (CDC §5.17) est la **Phase 08 (renumérotée Phase 10 le 2026-05-22), postérieure à la migration HDS (06.5, renumérotée Phase 09)**. Aucune position réelle de véhicule transportant des patients n'est stockée en prod avant HDS (donnée de santé indirecte ; RGPD géoloc salarié). Faisabilité liée à la Phase 07 (capture GPS arrière-plan non fiable en PWA). |
| **DEC-076** Modernisation Next.js 15 autonome, avant production | révision roadmap 2026-05-22 | CANDIDATE | Le socle Next.js est modernisé en deux temps : d'abord le patch de sécurité 14.2.35 (CVE déc. 2025), puis la montée 14 → 15 en **Phase 06.9 autonome**, faisable en bêta — elle ne dépend pas de HDS. La Phase 06.9 inclut l'audit de la rupture du cache `fetch()` (Next 15 ne met plus les requêtes en cache par défaut). React 18 est conservé ; React 19 et Next 16 sont différés (montée seulement quand elle devient nécessaire). Voir ADR-007. |
| **DEC-077** HDS repoussé en fin de parcours (Phase 09) | révision roadmap 2026-05-22 | CANDIDATE | Vu l'état bêta du projet, la migration HDS n'est plus un verrou proche : elle est renumérotée Phase 06.5 → **Phase 09** et devient le pré-requis de la seule mise en production commerciale (1er client payant). La géolocalisation opérationnelle (ex-Phase 08, renumérotée **Phase 10**) dépend de HDS (DEC-075) et suit donc la Phase 09. Précise DEC-065 sans la contredire : Supabase EU sous DPA reste acceptable en bêta. |
| **DEC-078** Accessibilité — posture prospective sans obligation | révision roadmap 2026-05-22 | CANDIDATE | Le RGAA 4.1.2 (transposition de WCAG 2.1 AA) reste la référence légale 2026. Un exploitant de taxis est sous les seuils d'assujettissement RGAA (chiffre d'affaires < 250 M€) comme EAA — European Accessibility Act (< 10 salariés ou < 2 M€ de bilan) : aucune obligation légale d'accessibilité ne pèse sur TAP à ce stade. TAP vise néanmoins volontairement WCAG 2.2 AA. Veille : le RGAA 5 est attendu fin 2026 — il intègrera WCAG 2.2 (cibles tactiles, focus non masqué). Aucune action immédiate requise. |
| **DEC-079** Hébergement `services/optimizer` = Vercel Python serverless | ADR-008 / Phase 06.7 Wave 3 2026-06-01 | LOCKED | Promotion de la CANDIDATE inscrite Wave 1 (2026-05-27). Réutilisation de la stack actuelle, zéro nouvelle dépense, zéro nouveau fournisseur : Vercel supporte nativement Python/FastAPI en serverless ; le projet paie déjà un plan Vercel pour `apps/web`. Discipline technique imposée : (a) `time_limit` solveur **plafonné à 3 s en production** (contre 5 s en local) pour conserver une marge contre la limite d'exécution serverless ; le researcher avait calé 2-5 s, on retient la borne basse en prod. (b) **Mesure obligatoire du cold start** au premier déploiement Wave 3 : si > 5 s sur 10 appels consécutifs après inactivité, déclencher le repli. (c) **Repli documenté : Clever Cloud** (opérateur FR avec offre HDS, cohérent avec la cible HDS Phase 09, DEC-077) ; le `Dockerfile` neutralisé en Wave 1bis (PR #186) demeure pour permettre une bascule sans rework. (d) Payload dé-identifié maintenu (D-08, CONTEXT 06.7). Alternative Supabase Edge Functions éliminée (Deno only, issues `supabase/supabase#22944` + `#41990`). Cf. ADR-008. |
| **DEC-080** Commits Wave-by-Wave plutôt que Task-by-Task quand `index.ts` réexporte | Phase 06.7 Wave 2 2026-06-01 | LOCKED | Le hook `guard-commit.sh` exige `pnpm typecheck` monorepo vert avant chaque commit. Quand un nouveau package est créé avec un `index.ts` qui réexporte des modules pas encore livrés (cas Wave 2 de Phase 06.7 — `packages/optimizer-client/src/index.ts` réexporte `./contract`, `./client`, `./transform`), les tâches intermédiaires (contrat sans client/transform) ne compilent pas → un commit atomique par tâche est impossible. Convention : pour ce cas précis (création de package neuf avec re-exports), **livrer la Wave en un seul commit `feat` + un commit `docs` de SUMMARY/STATE/ROADMAP**. Wave 2 de 06.7 (PR #188, commits `79322a1` + `5b1fd61`) sert de précédent. Pas d'impact qualité ; la traçabilité par tâche est assurée par le SUMMARY de plan. Si la limite gêne dans le futur (plans plus longs, debug git bisect ardu), évaluer `pnpm typecheck --filter` par tâche — modification du hook hors périmètre actuel. |
| **DEC-081** Indicateurs de la proposition d'optimisation = taux de mutualisation + km à vide, affichés « estimés » | Phase 06.7 Wave 3 2026-06-01 | LOCKED | Promotion de la CANDIDATE inscrite dans CONTEXT.md Phase 06.7 §"Décisions candidates". Deux indicateurs uniquement (D-19, arbitrage verrouillé) : (1) **taux de mutualisation** — part des courses regroupées dans le plan proposé ; (2) **km à vide estimés** — km sans patient entre deux courses, via Haversine. Les deux sont affichés avec le mot « estimé » visible (D-20). Aucun 3e indicateur ne peut être ajouté sans rouvrir cette décision. L'OTP terrain et le deadhead réel dépendent du GPS (Phase 10) — non représentés en V1. Refs : D-19/D-20, CONTEXT.md Phase 06.7. |
| **DEC-101** Direction artistique TAP gelée (doc fondateur 00-direction) | discuss design 2026-06-05 | LOCKED | Direction artistique TAP gelée (`docs/design-system/00-direction.md`). Personnalité sobre / confiant / situé ; bleu institutionnel dominant (`hsl(217 92% 32%)`, gardé « dans la famille » du Département sans calage pixel) + terracotta accent du moment-clé (`hsl(14 78% 55%)`, principe 60-30-10) ; **near-monochrome adossé à une STRUCTURE forte** (hiérarchie typo, espace 8 px, grille, profondeur) intra ET inter-écrans (architecture de l'information) ; échelle neutre 6-10 paliers ; iconographie Lucide unique ; grammaire d'animation sourcée Material / NN-g (desktop 150-200 ms, ease-out référence `cubic-bezier(0.0, 0.0, 0.2, 1)`, ≤ 2 effets / écran, `prefers-reduced-motion` respecté). Boussole d'inspiration : Linear (densité) tempéré Frappe « Espresso » (anti-distraction métier). Incarnation famille par famille à suivre (Régulation en premier). Faiblesses tracées à résoudre : hiérarchie typo écrasée (305 `text-sm` / 133 `text-xs`), `PageHeader` absent du cœur métier, `loading.tsx` / empty-states / skeletons inégaux, raccourcis localisés, rangement à arbitrer en contexte. Voir aussi `docs/design-system/08-horizon-open-source.md` (cadrage chromatique chiffré + comparaison OSS). Pas d'ADR (document de direction, pas d'architecture technique). |
| **DEC-107** Rangement Régulation audité — décision : pas de refactor (lot 5) | audit lot 5 incarnation 2026-06-05 | LOCKED | Rangement de la famille Régulation audité dans le cadre du programme d'incarnation DEC-101. **Le rangement est cohérent en pratique** : nav unifiée par rôle (`nav-config.ts`), surlignage de nav correct (logique « plus long préfixe gagne » dans `nav-tabs.client.tsx`, commentaire explicite « évite que Courses reste souligné en même temps que Caisse »), accès régulateur à `/admin/chauffeurs` LÉGITIME et géré par les gardes `(admin)/layout.tsx` (dirigeant OU régulateur). Les impuretés d'URL identifiées au friction log (caisse enfant de courses ; chauffeurs sous `/admin`) sont SANS impact utilisateur observable. **Déplacement physique des URLs VOLONTAIREMENT NON RÉALISÉ** : coût mesuré (18 refs `/admin/chauffeurs` + 7 refs `/courses/caisse` + redirections + mémoire musculaire cassée) > bénéfice (cosmétique). Direction §5ter : « le rangement ne se chamboule pas par goût ; tout changement d'URL = à peser ». **Question close.** Réévaluable si un vrai besoin émerge (SEO, refonte IA, demande explicite design partner). Pas d'ADR (décision de non-action). |
| **DEC-112** Module conformité réglementaire (CdC §5.21) lot 1 — fondation | Phase 06.33 2026-06-08 | LOCKED | Premier lot du module Conformité réglementaire métier (CdC §5.21, V1 §2.1 « Suivi des échéances réglementaires »). Pose le socle : **table dédiée `compliance_items`** (polymorphe driver/vehicle/organization, 8 types CHECK, multi-tenant RLS forcée + pgTAP 10 cas) ; **validators + libellés FR centralisés** dans `@tap/shared` (NFR-001, 0 hardcode UI) ; **helper pur `complianceStatus`** (90/60/30/7 j, 14 tests Vitest) — statut DÉRIVÉ à l'affichage, jamais stocké ; **saisie embarquée** dans `driver-form` + `vehicle-form` (`<ComplianceFieldset>`, mode édition) + page `/admin/conformite` (vue consolidée + section convention CGSS organisation) ; **`<ComplianceBadge>`** sémantique (icône + texte + jours, WCAG 1.4.1 critère couleur seule) dans drivers-list + vehicles-list. **Distinct du `ComplianceCard` RGPD documentaire** (registre/DPA/DPIA). Lot 2 (cron + alertes cockpit/email 90/60/30/7) et lot 3 (blocage paramétrable planification) à venir. Upload de scan (`document_url`) différé tant qu'aucun bucket Storage HDS prêt. Voir ADR-013 (table dédiée vs colonnes éparses). |
| **DEC-113** Module conformité réglementaire — lot 2 alertes in-app dérivées | Phase 06.34 2026-06-08 | LOCKED | Deuxième lot du module Conformité (CdC §5.21). **Q1 IN-APP uniquement** : cockpit (régulateur) + dashboard (dirigeant). Email/push = branchement d'infra REPOUSSÉ (principe : on construit la fonctionnalité, pas l'infra). La logique expose ses données pour qu'un canal externe futur les consomme sans refonte. **Q2 DÉRIVÉ** : statut/alerte calculés à la volée depuis `compliance_items` vs date du jour. PAS de table d'alertes stockées, PAS d'accusé de lecture (une échéance reste visible tant qu'elle n'est pas réglée). Validé par RETEX secteur. **Cron pg_cron : NON** au lot 2 (affichage dérivé suffit ; le cron servira au futur canal externe). **Helper pur `selectComplianceAlerts` + `countComplianceAlerts`** dans `@tap/shared` (9 tests Vitest verts) : filtre `soon`/`expired`, tri par urgence (expired du plus passé en haut puis soon ascendant). **Query serveur `getComplianceAlerts`** (`'server-only'`) partagée cockpit + dashboard, hydrate labels d'entité applicativement (pas de FK polymorphe SQL, cf. ADR-013). **`<ComplianceAlertsPanel>`** variants `panel` (cockpit, dense, limit 4, sous AlertsPanel courses) et `card` (dashboard, bordée, grid 2 cols à côté du `ComplianceCard` RGPD, limit 5). Statut accessible (icône + texte + jours, WCAG 1.4.1). **Pas de terracotta** (pas un moment-clé d'action ; les alertes sont une vigilance, l'action concrète vit dans `/admin/conformite`). 0 migration BDD, 0 cron, 0 dépendance. Lot 3 (blocage paramétrable planification) à venir. Pas d'ADR (pas de nouveau modèle, cohérent ADR-013). |
| **DEC-114** Module conformité réglementaire — lot 3 contrôle planification souple/paramétrable | Phase 06.35 2026-06-08 | LOCKED | Troisième et **dernier** lot du module Conformité (CdC §5.21). **Q4 souple-par-défaut** : avertissement visible, la régulatrice garde la main. Paramétrable vers dur sur décision dirigeant. RETEX secteur/FOSS : visibilité prime sur verrou. **Q5 optimiseur INCLUS** (sinon contournement). **Q6 granularité GLOBALE** : réglage org unique (pas par type d'échéance V1). **Migration** `compliance_blocking_mode text not null default 'warn' check (in 'warn','block')` sur `organizations`. **Helper pur `entityComplianceState` + `isPlanningBlocking`** (`@tap/shared`, 8 tests Vitest) — agrège plusieurs échéances d'une entité. **Assign-modal** : badge « Non conforme » (icône+texte WCAG 1.4.1) ; mode `warn` (panneau + toast warning, submit OK) ; mode `block` (ligne désactivée, submit refusé client + **revérif serveur dans `assignRideAction`** — defense in depth). **Optimiseur (route `/api/optimizer`)** : warn = tous au solveur + signalement `complianceWarnings` ; block = véhicules non conformes filtrés du pool + `complianceWarnings.blocked=true`. UI `optimization-shell` affiche panneau dédié. Extension rétro-compatible `OptimizationProposal.complianceWarnings?`. **`<BlockingModeControl>` SegmentedControl Avertir/Bloquer** dans `/admin/conformite` (dirigeant) + `updateComplianceBlockingModeAction` (DEC-041 row-count check). **Module Conformité §5.21 COMPLET (lots 1+2+3)**. Pas d'ADR (colonne simple, cohérent ADR-013). |
| **DEC-115** Incarnation du header / navigation principale (app + admin) | Phase 06.36 2026-06-08 | LOCKED | Incarnation du header global selon direction §3 (« bleu profond = la structure de confiance : navigation active, liens, en-têtes de section, identité »). **Angle mort** des lots 06.24 → 06.32 — le header restait générique. **D-01** : `NavTabs` onglet actif `text-foreground` → **`text-primary font-medium`** + soulignement `bg-primary` 2px → **3px pleine opacité** (`rounded-t-sm`). Double signal (couleur + poids + soulignement, WCAG 1.4.1). **D-02** : logo « **TAP** » en `text-primary text-base font-semibold` (présence de marque) ; « Régulation » reste muted. **D-03** : `shadow-sm` ajouté au header (border-b + shadow-sm) pour la séparation avec le contenu sans alourdir. Sticky + backdrop-blur conservés. **D-04** : factorisation `components/app-header.tsx` (Server Component avec slot `extras` pour `DraftQueue` côté `(app)`), les deux layouts passent de ~30 lignes de header dupliqué à 1 ligne. **Contraste WCAG vérifié** : `text-primary` jour 9.42:1 (AAA), nuit 5.21:1 (AA). **Header chauffeur PWA hors périmètre** (grammaire dédiée DEC-014). 0 migration BDD, 0 dépendance, 0 régression. Pas d'ADR (activation de pattern + factorisation rétro-compatible). |
| **DEC-116** Exports courses CSV + statistiques + PDF récap chauffeur (§5.23 partiel) | Phase 06.37 2026-06-08 | LOCKED | Comble une partie du creux CdC §5.23 avec les briques **déjà en place** : zéro dépendance, zéro achat. **NB** : numéroté 06.37/DEC-116 (le prompt cadrait 06.36/DEC-115 mais ces numéros ont été utilisés par l'incarnation header — collision évitée). **D-01** : `exportRidesCsvAction` (pattern `exportCaisseCsvAction`) — zod `from/to/driverId?/status?/transportMode?`, guard `requireAdminOrRegulateur`, query RLS scoppée + hydratation labels patient/chauffeur, colonnes claires (date RDV, patient, départ, destination, mode, statut, chauffeur, tarif). Tarif lu de `tarif_amount_eur` déjà calculé (0 recalcul de pricing). UI `<ExportCsvButton>` dans la toolbar `RidesList`. **D-02** : `exportStatsCsvAction` (guard dirigeant) — **réutilise `getDashboardData()`**, 0 duplication d'agrégat. Sections volume/incidents/chauffeurs/CA + comparatif N-1. UI `<ExportStatsButton>` dans le slot `actions` du PageHeader tableau de bord. **D-03** : Route `/api/admin/chauffeurs/recap/pdf` (runtime nodejs, renderToStream) — params `driver/from/to`, guard dirigeant, **audit log AVANT** le rendu (`chauffeur.recap.exported_pdf`), réutilise `PdfDocument` + `PdfSection` + `pdfStyles` (charte commune avec facturation CGSS). UI `<RecapPdfButton>` dans `DriverRowActions` (visible dirigeant uniquement, dialog mini-form from/to). **Lomaco** (format externe) et **FEC normé** (à délibérer) **RESTENT REPOUSSÉS** (registre des travaux). 0 migration BDD, 0 nouvelle dépendance, 0 régression. RGPD minimisation respectée (pas de données médicales exportées). Pas d'ADR (réutilisation de patterns existants). |
| **DEC-117** Header — regroupement de la navigation dirigeant (Flotte / Gestion / Légal) | Phase 06.38 2026-06-08 | LOCKED | Désencombrement du header dirigeant. Recherche secteur : **top-bar 5-7 entrées primaires max** ; TAP dirigeant était à **11 liens à plat**. Décision dirigeant : regroupement (pas sidebar — repoussée si besoin persiste). **D-01** : dirigeant = **5 liens primaires** (Tableau de bord, Cockpit, Patients, Courses, Caisse — flux quotidien) + **3 menus déroulants** : **Flotte ▾** (Chauffeurs, Véhicules, Conformité, Maintenance), **Gestion ▾** (Tarifs, Facturation), **Légal ▾** inchangé. **D-02** : régulateur conservé à 5 entrées plates (sous le seuil, son flux quotidien reste 1 clic). **D-03** : pattern `LegalNavMenu` (DropdownMenu shadcn) généralisé en **`<NavGroupMenu>`** (prop `group: NavGroup`). Signature bleue active DEC-115 préservée même dans un menu : `text-primary font-medium` sur le trigger + soulignement 3px pleine opacité quand un item du groupe est actif ; `aria-current="page"` sur trigger ET item actif du menu. `LegalNavMenu` réécrit pour suivre exactement le même style + items lus depuis `LEGAL_NAV_GROUP` (`nav-config`) — source unique. **D-04** : `nav-config` restructuré (`NavGroup`, `RoleNav`, `navForRole`) ; `tabsForRole` conservé en rétro-compat (renvoie `primary` seul). **D-05** : pas de sidebar, **pas de changement d'URL** (les 11 routes restent), pas le header chauffeur PWA. WCAG/RGAA : `DropdownMenu` shadcn (clavier OK : Espace, Entrée, flèches, Échap). 0 migration BDD, 0 nouvelle dépendance, 0 régression. Pas d'ADR (généralisation de pattern). |
| **DEC-118** SegmentedControl — ajustement de densité | Phase 06.39 2026-06-08 | LOCKED | Le `<SegmentedControl>` détonnait sur les écrans denses (patients, chauffeurs). Cause réelle : le segment portait `py-6`, hors du scale spacing custom (4/8/12/16/24/32/48/64) → fallback Tailwind par défaut 24px → hauteur ~76px, ~le double du champ de recherche voisin (`Input` h-10 = 40px). Correctif densité uniquement : segment `py-6` → **`py-4`** (4px) → hauteur ~36px (h-9), alignée sur la barre de filtres. Conteneur `p-4`/`gap-4`/`rounded-lg` conservés. **Fond plein actif + `font-medium` + `shadow-sm` CONSERVÉS** (double signal fond+graisse, contraste ≥ 4.5:1 jour ET nuit ; retirer le fond actif = anti-pattern a11y, surtout dark). Recherche secteur : accorder la densité visuelle à l'UI environnante. Composant partagé → profite aux 3 usages (patients, chauffeurs, assign-modal) + `BlockingModeControl`. Pas de changement d'API/logique/couleur. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-119** Hygiène .planning + resync tracking + docs de référence | Phase 06.40 2026-06-08 | LOCKED | Grand ménage du dossier de planification : **git rm de ~224 fichiers de processus consommé** sous `.planning/phases/` (PLAN/CONTEXT/SUMMARY/DISCUSSION-LOG/UI-SPEC/RESEARCH/UAT/VERIFICATION/STEPS des phases déjà livrées + mergées — git garde l'historique, pas d'archivage parallèle). **`10.0-prototype-geoloc/` préservé** intégralement. 3 `deferred-items.md` fusionnés dans le registre des travaux repoussés AVANT suppression (dette ESLint v10 flat config + test SIRET Luhn Carrefour). **STATE.md recompté** (compteur faux 34/38 corrigé → 50 phases livrées ; méthode de comptage documentée en commentaire frontmatter). **Registre DEC réconcilié** avec ROADMAP (entrées concises 082→111 ajoutées + DEC-118). **6 docs de référence durables** créés/relocalisés (registre-travaux-repousses + elements-disponibles à la racine ; cadrage+journal conformité sous `modules/conformite/` ; cadrage exports sous `modules/exports/` ; recherche UI sous `research/`). Planning/docs uniquement : 0 code applicatif, 0 migration. Pas d'ADR. |
| **DEC-120** Messagerie interne (§5.22) lot 1 — chat texte temps réel à la course | Phase 06.41 2026-06-08 | LOCKED | Premier lot (cœur) du module Messagerie, dérivé du cadrage GSD validé. Chat texte régulateur↔chauffeur **rattaché à une course**. **Table UNIQUE `internal_message(ride_id, …)`** — la course EST la conversation (pas de table conversation séparée → évite le bug Supabase #1721 : payloads Realtime mélangés quand 2 tables partagent un channel). **Mode = Postgres Changes** (persistant, cohérent cockpit `use-cockpit-rides`), PAS de Broadcast (sur-ingénierie à notre échelle). **RLS stricte forcée + indexée** (la RLS filtre aussi la réception Realtime → index sur `ride_id`/`organization_id`) : SELECT same_org (régul/dirigeant = org ; chauffeur = SES courses via `drivers.profile_id`, pattern `rides_update_chauffeur_own_rides`) ; INSERT anti-usurpation (`sender_profile_id = auth.uid()`, `sender_role = current_user_role()`, chauffeur seulement sur ses courses) ; **messages immuables** (pas d'UPDATE/DELETE — ni policy, ni grant). Publication Realtime ajoutée de façon idempotente. **14 pgTAP** (cross-tenant, isolation par course/chauffeur, anti-usurpation profil+rôle, anon, grants). Query `'server-only'` `getRideMessages` + Server Actions `getRideMessagesAction`/`sendRideMessageAction` (guard `getAuthContext` + zod 1..2000) + helper pur `groupMessagesByDay` (fuseau Indian/Reunion, 8 Vitest, dans `@tap/shared`). Hook `useRideMessages` (channel par course, reconnexion + refetch au resubscribe) + composant `<RideChat>` (aria-live, distinction d'auteur par libellé de rôle — jamais la couleur seule, Cmd/Ctrl+Entrée, auto-scroll `prefers-reduced-motion`, tokens jour+nuit). Greffé `ride-drawer` (régulateur) + `ride-detail` (chauffeur). **Photo (donnée santé → HDS), push web (VAPID), fil général, purge 1 an (pg_cron), email** : repoussés (registre / lots ultérieurs). 0 dépendance nouvelle. Pas d'ADR (modèle simple, conversation = course ; trace dans le journal de module `modules/messagerie/`). |
| **DEC-121** Fix overlays — combobox `createPortal` + dialog scrollable (cause racine) | Phase 06.42 2026-06-09 | LOCKED | Correction **cause-racine** de 2 bugs overlays de même famille (recherche `recherche-overlays-ui.md` : un élément `position:absolute` n'échappe **pas** au clipping/overflow ni aux stacking contexts d'un ancêtre ; z-index ne corrige rien de fiable — 3 systèmes : overflow, stacking contexts, containing blocks ; **le portail est la solution**). **D-01 `<Combobox>`** : la listbox est rendue détachée au `document.body` via **`createPortal`** (react-dom, **0 dépendance**, DEC-003 respecté), positionnée en `position: fixed` **ancrée au rect du champ** (mesure `getBoundingClientRect`, recalcul au scroll en capture + resize, cleanup ; **flip haut/bas** selon la place). z-`[60]` pour passer **au-dessus du `DialogContent`** (z-50, cas address-picker dans le modal course). ARIA APG + navigation clavier + Échap + clic-dehors **préservés** (`listboxRef` ajouté au test de clic-dehors car la listbox est hors wrapper) ; `onPointerDown` stoppé pour que le `DismissableLayer` de Radix ne ferme pas le `Dialog`. Les **8 tests combobox restent inchangés et verts**. **D-02 `DialogContent`** : `max-h-[85dvh]` + `overflow-y-auto` + `w-[calc(100%-2rem)]` + `min-w-0` sur les grilles internes `ride-fields` (mode/urgence + datetime) → un formulaire long **scrolle dans le modal** (bouton « Créer » atteignable), pas de débordement horizontal. **Composants partagés** → propage à tous les usages (vehicle-form, address-picker, modal course, etc.). Pas de `@radix-ui/react-popover` ni `floating-ui` (DEC-003). 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-122** Fix cible cliquable — suppression brouillons (WCAG 2.5.8, 44px) | Phase 06.43 2026-06-09 | LOCKED | La corbeille du popover brouillons (`draft-queue`) faisait **~12px** de cible (icône `Trash2 h-12` nue, sans padding ni min-size) → sous **WCAG 2.5.8 AA** (cible ≥ 24px ; 44px recommandé Apple, 48dp Material ; cibles < 44px = taux d'erreur ×3 — recherche `recherche-touch-targets.md`). Patron **icône discrète + cible large** : on agrandit la ZONE cliquable, pas le dessin. **D-01** : variant `size="icon"` de `<Button>` porté `h-10 w-10` (40px) → **`h-11 w-11` (44px)** (confort Apple/AAA, +4px marginal). **0 régression** : aucun autre usage de `size="icon"` dans le code à la date. **D-02** : la corbeille passe d'un `<button>` nu à **`<Button variant="ghost" size="icon">`** (cible 44px, **hover `bg-muted` + focus-visible ring** via le variant, jour+nuit par tokens) ; **icône discrète** `Trash2 h-16 w-16` ; `aria-label` « Supprimer ce brouillon » + `stopPropagation` (ne déclenche pas la reprise) conservés. **D-03** : `gap-8` ajouté à la ligne meta → **≥ 8px** entre l'horodatage et la corbeille. Comportement inchangé (suppression sans confirmation, hors périmètre). 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-123** Refonte page de connexion — bleu institutionnel (DEC-101) | Phase 06.44 2026-06-09 | SUPERSEDED par DEC-124 | _(Split-screen bleu — remplacé par le formulaire centré 06.45 ; ratio inversé jugé hors-norme.)_ La page login faisait peu professionnelle (panneau gauche `bg-muted` terne, logo affiché `h-12` = 12px tronqué, bouton terracotta criard). Recomposée selon recherche auth SaaS (`recherche-page-login.md`) + direction **DEC-101** : le **bleu institutionnel porte l'identité** ; le **terracotta est réservé aux moments-clés métier** — un login n'en est pas un. **D-01** : bouton « Se connecter » `variant="accent"` → **`variant="default"`** (bg-primary bleu). **`accept-invite` délibérément NON modifié** : son terracotta « Activer mon compte » est un moment-clé justifié et **LOCKED par DEC-111** (le toucher contredirait une décision verrouillée). **D-02** : `aside` `bg-muted` → **`bg-primary text-primary-foreground`**. Marque rendue **typographiquement** (pavé « T » `bg-primary-foreground text-primary` + mot « TAP `text-primary-foreground`, ~40-48px) plutôt que via `/logo-tap.svg` : le SVG a un « TAP » navy figé **illisible sur le bleu et cassant en nuit** (la paire `primary`/`primary-foreground` bascule : jour bleu profond/blanc → nuit bleu clair/navy). Le rendu typo par tokens garantit le contraste **AA jour ET nuit** sans asset. Baseline forte + **3 points de valeur** sobres (`Check`, `/80`), footer `/70`, bloc ancré vertical (`justify-center`). **D-04** : responsive `< lg` = **bandeau compact** (points de valeur + footer masqués, baseline `text-xl`). `Image` next/image retirée (inutilisée). **Aucun asset produit** (composition typographique). Artefact avant/après `docs/showcase/06.44-refonte-login/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-124** Refonte login — formulaire centré (supersede DEC-123) | Phase 06.45 2026-06-09 | LOCKED | **Supersede DEC-123** : le split-screen de 06.44 était hors-norme — ratio inversé (~75 % branding / < 25 % form), panneau de marque léger à moitié vide. Recherche (`recherche-page-login.md` + sources login centré) : le split vaut quand 2 éléments ont un **poids égal** (ratios 40:60 / 33:66, texte + image riche) ; en-dessous d'1/3 « c'est une sidebar, pas un split ». Le contenu de marque TAP (logo + baseline + puces) est léger → **formulaire CENTRÉ** (pattern Notion/Linear ; B2B = une seule colonne par défaut, chemin le plus court vers l'entrée). Décision dirigeant : option A (centré). **D-01** : `AuthShell` (Server Component, partagé /login + /accept-invite) split `lg:flex-row` → **centré une colonne** (`min-h-screen flex items-center justify-center`, fond `bg-muted` sobre) ; **aside/panneau bleu pleine hauteur supprimé** ; `ThemeToggle` repositionné en coin haut-droite (atteignable clavier). **D-02** : **carte centrée** `max-w-[400px] bg-background rounded-lg border shadow-sm p-32` ; en-tête = marque (pavé « T » `h-32` `bg-primary` + mot « TAP » `text-foreground`, par tokens — corrige le `h-12` tronqué ; **pas de SVG figé** car son « TAP » navy deviendrait illisible sur la carte sombre en nuit) + titre `{title}` ; `{children}` ; `rightSlot` + `footerHint` conservés ; **baseline courte sous la carte** ; **3 puces supprimées** (allègement, non essentielles à l'entrée) ; footer page discret. **D-03** : bouton bleu déjà en place (DEC-123). API props inchangée. Contraste **AA jour ET nuit** (tokens). **Aucun asset produit.** Artefact `docs/showcase/06.45-login-centre/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-125** Fix anneau de focus primary — `--ring` découplé de `--primary` | Phase 06.46 2026-06-09 | LOCKED | **Cause racine** d'un faux soulignement perçu sur les boutons à fond primary au focus clavier : dans les tokens, `--ring` était **strictement égal** à `--primary` (jour `217 92% 32%`, nuit `217 91% 60%`). Le base du `Button` applique `focus-visible:ring-2 ring-ring ring-offset-2` → sur un bouton `variant="default"` (`bg-primary`), l'anneau de focus avait la même couleur que le fond, se fondait, et l'offset dessinait un mince liseré perçu comme un soulignement de lien (affordance brouillée). Touche **tous** les boutons primary focalisés, pas seulement le login. **D-01** : `--ring` découplé de `--primary` **à la source** (`tokens.json` `color.action.focusRing` `217 92% 32%` → **`217 91% 60%`** ; `tokens.dark.json` `217 91% 60%` → **`213 94% 78%`**), puis régénération de `tokens.generated.css`/`.ts` via `pnpm tokens:build` (style-dictionary — convention DEC : ne pas éditer le généré à la main). Anneau désormais distinct : un bleu plus clair que primary → halo net sur le bouton primary, tout en gardant ≥3:1 sur fond clair (≈4:1 sur blanc, pour inputs/ghost/outline) ; en nuit, bleu ciel clair distinct du primary nuit, fort contraste sur fond sombre. **D-02** : focus visible et contrasté vérifié sur tous les variants (primary/outline/ghost/destructive) + inputs, jour ET nuit. **Anneau jamais retiré** (RGAA / WCAG 2.4.7). Fix de token = **global**. Pas de changement de `--primary`, ni des dimensions des boutons. Artefact avant/après `docs/showcase/06.46-focus-ring/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-126** Calibrage anneau de focus — `ring` → `outline` (icon-buttons + login) | Phase 06.47 2026-06-09 | LOCKED | Suite de DEC-125 : la **couleur** du ring était corrigée, mais `focus-visible:ring-2 ring-offset-2` (box-shadow plein) restait trop **épais** sur les petits boutons ronds (icônes header brouillons/thème, variant `ghost` 40px) → l'anneau bleu vif entourait si étroitement le bouton qu'il paraissait un **fond plein « ovale »** ; la même cause donnait au bouton login un aspect **pilule** au focus (une cause, deux symptômes ; le login reste `rounded-md`, pas d'ovale dans le code). **D-01** : `focus-visible` migré du **ring** (box-shadow qui peut se lire comme un glow/fond) vers l'**`outline`** (reco c) : `focus-visible:outline outline-2 outline-offset-2 outline-ring`. L'outline est un trait stroké qui ne remplit jamais et suit le `border-radius` → se lit comme un **contour net détaché**. **Harmonisé** sur le base `buttonVariants` (`button.tsx`) ET le `theme-toggle.client.tsx` (les deux définissaient le focus séparément et divergeaient). `ring-offset-background` retiré du base (inutile sans ring). `colors.ring` de `tailwind.config.ts` alimente `outline-ring` (même couleur que DEC-125, inchangée). **D-02/D-03** : forme et taille des boutons **inchangées** (login `rounded-md h-12 w-full ≥44px`) ; focus visible et contrasté **RGAA** conservé jour ET nuit ; anneau jamais retiré. Inputs hors périmètre (conservent leur ring, surfaces plus larges). Composant partagé → propage à tous les boutons + icon-buttons. Artefact avant/après `docs/showcase/06.47-focus-calibrage/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-127** Bouton de soumission auth aligné sur le gabarit des inputs | Phase 06.48 2026-06-09 | LOCKED | Sur les formulaires d'auth, les `<Input>` font `h-10` (40px) `rounded-md` mais le bouton de soumission faisait `h-12 w-full text-base` (48px) → plus haut que les champs, il « flottait » (désalignement de formulaire). **D-01** : bouton submit aligné sur les inputs — `h-12` → **`h-10`**, `text-base` retiré (`text-sm` par défaut du `Button`, comme l'input), `rounded-md` conservé (= radius des inputs). Résultat : barre rectangulaire de même gabarit que les champs (hauteur + largeur + radius), s'aligne au lieu de flotter. **D-02** : appliqué à **`login-form` ET `accept-invite-form`** ; `accept-invite` conserve son `variant="accent"` (terracotta, moment-clé justifié DEC-111 — hors périmètre couleur). **D-03** : cible reste large (pleine largeur ; `h-10`=40px légèrement sous 44px mais aligné aux inputs, et la surface horizontale d'un bouton pleine largeur est énorme — l'enjeu touch-target 44px vise les petites cibles). Couleur bleue (DEC-123) + focus outline (DEC-126) inchangés. Artefact avant/après `docs/showcase/06.48-bouton-auth-aligne/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-128** Tableau de bord — densité & alignement (anatomie de carte fixe) | Phase 06.49 2026-06-09 | LOCKED | Le Tableau de bord scrollait alors que son contenu tient sur ~430px : cause = **layout** (sections empilées, `space-y-24`, KPIs 4+2, cartes hautes, SLA en section pleine largeur), pas la quantité d'info. Refonte dérivée d'une **maquette throwaway validée par le dirigeant** + **doctrine d'alignement** (Cockpit référence). **D-01 KpiCard anatomie FIXE** : `[label]→[corps]→[action mt-auto]`, `h-full` + grilles `items-stretch` (hauteurs égales par rangée, valeurs sur la même ligne de base), valeur `text-3xl`→`text-2xl` ; les 4 variantes (simple/ventilation/multi/alerte) deviennent des **slots** d'une ossature unique, plus des compositions divergentes. **D-02 échelle d'espacement resserrée unique** : page `space-y-16`, titre→grille `space-y-8`, grilles `gap-12`, carte `p-16` (fini les `space-y-24`/`gap-16` ad hoc). **D-03 layout 3 rangées** tenant sur un viewport desktop sans scroll : À traiter `lg:grid-cols-3` (Courses à facturer · Alertes · Délais légaux SLA fusionné en 3e col) · Activité du mois `lg:grid-cols-6` (6 KPIs sur une rangée, replie `grid-cols-2 sm:grid-cols-3`) · Conformité & échéances `lg:grid-cols-2`. **D-04 couleurs sémantiques en tokens** : `text-green-700`/`text-amber-700` → `text-success`/`text-warning` (+ `bg-*`) dans `kpi-card` + `sla-badges`, jour+nuit, doublage couleur+texte conservé (WCAG 1.4.1). La ventilation par mode de paiement est condensée en `context` (info préservée, détail complet dans Caisse). `compliance-card` + `sla-badges` passées en `h-full`. **Données INCHANGÉES** (mêmes KPIs, valeurs, liens) — refonte présentation/densité uniquement. Première application de la **doctrine d'alignement** versée `.planning/doctrine-alignement-dashboard.md` (réutilisable Conformité, Caisse…). Artefact avant/après `docs/showcase/06.49-dashboard-densite/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-129** Conformité — densité & alignement (2e application de la doctrine) | Phase 06.50 2026-06-09 | LOCKED | **2e écran** aligné sur la doctrine (`.planning/doctrine-alignement-dashboard.md`) après le dashboard, dérivé d'une maquette validée. L'écran Conformité « flottait » : Chauffeurs/Véhicules occupaient chacun une **section pleine** pour un simple « aucune échéance saisie » (vide structurel + scroll), réglage Avertir/Bloquer en pleine largeur avec long paragraphe, résumé via un `SummaryCard` maison **incohérent** avec la `KpiCard` du dashboard. **D-01** : résumé via la **`KpiCard` partagée** (variant `simple`, importée `@/app/(app)/tableau-de-bord/_components/kpi-card`) — Expirées `state=alerte` si >0, Proches `attention` si >0, Total neutre ; `lg:grid-cols-3 items-stretch` ; `SummaryCard` maison supprimée (couleurs via states/tokens, fin de l'incohérence inter-écrans). **D-02** : `BlockingModeControl` compacté **sur une ligne** — titre + aide courte à gauche, `SegmentedControl` à droite (`flex justify-between` en `sm:`, aide `max-w-[58ch]`) ; `aria-labelledby` + fonctionnement inchangés ; le mode reste pleinement visible/utilisable. **D-03** : Chauffeurs + Véhicules **côte à côte** (`lg:grid-cols-2 items-stretch`) ; état **vide** = ligne discrète bordée muted + lien « Fiches → » (`EmptyEntity`) au lieu d'une section pleine ; état **rempli** = `ConformiteTable` existant (tableau structuré, cohérent Cockpit) ; Convention CGSS pleine largeur (`ComplianceFieldset` si vide / `ConformiteTable` sinon). **D-04** : échelle d'espacement doctrine (`space-y-16`/`space-y-8`, `gap-12`, `p-16`) + section-labels xs uppercase. **Données et logique INCHANGÉES** (mêmes counts, saisies, mode blocage, tableau) — présentation/densité uniquement. Tient sur un viewport desktop quand peu d'échéances. Artefact avant/après `docs/showcase/06.50-conformite-densite/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-130** Formulaire patient — 2 colonnes « tout visible » + patron de form partagé | Phase 06.51 2026-06-09 | LOCKED | **1re application de la doctrine FORMULAIRE** (`.planning/doctrine-formulaires.md`) + extraction d'un **patron de layout partagé** réutilisable. Le formulaire patient s'étirait en **colonne unique** → invisible d'un coup, difficile de relire/repérer une erreur. Recherche : la colonne unique vaut pour 90 % des formulaires, MAIS le **multi-colonnes est justifié pour la saisie de données dense** (back-office, la régulatrice saisit des patients à la chaîne, besoin de tout voir pour contrôler) ; anti-saut de champ Baymard = lecture **ligne par ligne** gauche→droite. Décision dirigeant : 2 colonnes « tout visible », souple. **D-01** : patron `components/form/form-layout.tsx` — `FormColumns` (`lg:grid-cols-2 gap-24`, 1 col < lg), `FormColumn` (`space-y-24`), `FormSection` (titre xs uppercase + `space-y-12`), `FormRow` (`grid-cols-2`, champs liés lus gauche→droite), `FormActions` (filet `border-t` + boutons à droite). Composants présentationnels server-safe. **D-02** : `patient-form` refondu en 2 colonnes (GAUCHE Identité + Préférences / DROITE Coordonnées + Note), `mx-auto max-w-[980px]` ; largeurs selon contenu (téléphone/canal ~240px, date/CP/sexe réduits) ; **champs homogènes h-10** : selects `genre`/`canal` en `<select>` **natifs** restylés au gabarit `Input` (le `ui/Select` Radix est contrôlé sans `name` → ne soumet pas dans un form natif ; conservé pour la ville avec hidden input), note → **`ui/Textarea`**, `BirthDateField` datepicker + ville Select `h-48`→`h-10` ; barre `FormActions` (Annuler → `/patients` + « Créer le patient » / « Enregistrer »). **D-03** : new + edit = même composant ; `patient-form-constraints` (même page édition) aligné `h-48`→`h-10`. **Données, validation zod, Server Actions, chiffrement NIR INCHANGÉS** (les hidden inputs canoniques préservent l'envoi). `h-48` restants = avatar/skeleton/recherche (tailles légitimes, hors champs de form). Doctrine versée (réutilisable véhicule/chauffeur — lot suivant). Artefact avant/après `docs/showcase/06.51-form-patient-patron/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-131** Formulaires véhicule & chauffeur — application du patron de form partagé | Phase 06.52 2026-06-09 | LOCKED | Application du patron de form (DEC-130) aux formulaires `vehicle-form` et `driver-form`, encore en style ad hoc (`space-y-16`, `grid-cols-2 gap-8/12`, labels/checkbox bricolés `border-input … px-12 py-8`). **D-01 véhicule** : `FormSection` Identification (immatriculation réduite + `FormRow` marque/modèle `Combobox`) · Caractéristiques (type `Select` + `FormRow` places assises/TPMR `NumberField`) · Disponibilité (checkbox propre `input`+`Label`) · Conformité (`ComplianceFieldset`) + `FormActions`. **D-02 chauffeur** : `FormSection` Identité & contact (nom + `FormRow` téléphone/licence) · Types de permis (grille de checkboxes propres ; l'ancien `<fieldset>/<legend>` remplacé par le titre de section + libellés liés) · Statut · Conformité + `FormActions`. **D-03** : champs homogènes (`Field`/`Combobox`/`Select`/`NumberField`), checkboxes en `input` + `Label` (fin des labels encadrés bricolés), espacement via le patron. **Layout souple 1 colonne** (`max-w-[640px]`) — les 2 forms sont rendus en `Sheet`, peu de champs : pas de 2 colonnes à moitié vides (la doctrine est souple). `SubmitButton` `w-full`→`h-10` dans `FormActions`. **Combobox marque/modèle + ComplianceFieldset + données/validation/Server Actions INCHANGÉS** fonctionnellement. Cohérence CRUD patient/véhicule/chauffeur. Artefact avant/après `docs/showcase/06.52-form-vehicule-chauffeur/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-132** Patron de liste unifié — toolbar + pagination (au-delà d'un seuil) + actions | Phase 06.53 2026-06-09 | LOCKED | Unification des écrans de liste sur `data-table`. Cohérents sur le tableau (tri/EmptyState/lignes cliquables) mais divergents au-dessus/en dessous : toolbars bricolées (alignements `items-center` vs `items-end`, actions tantôt à gauche tantôt à droite) ; paginations hétérogènes (`/courses` « Voir plus » cumulatif — offset additif serveur, recherche limitée à la fenêtre chargée ; drivers/vehicles/patients aucune). **D-01** : `ListToolbar` + `ListMeta` (`components/data-table/list-toolbar.tsx`, server-safe) — contenant à slots `search` (`flex-1` min 260px) · `filters` · `clear` · `actions` (`ml-auto`), hauteur/espacement uniques (`gap-12`) ; la toolbar ne porte que la **structure**, les **valeurs métier** (filtres/libellés/défauts) restent à chaque écran ; compteur en `ListMeta` (`text-muted-foreground text-sm tabular-nums`) SOUS la toolbar. **D-02** : `Pagination` (`components/data-table/pagination.tsx`, client) par PLAGE (« 1–50 sur 128 », `aria-live`) + Précédent/Suivant (grisés en bord, jamais cachés) + lignes par page optionnel ; pagination par PAGE (offset = `page*pageSize`), pas « voir plus » cumulatif ; indifférente au mode (client `slice` ou server-driven) ; `nav`+`aria-label` RGAA. **D-03 (règle clé — seuil)** : `if (total <= pageSize) return null` — aucune barre sous une liste courte (3 véhicules → rien ; 128 courses → plage). **D-04** : `<DataTable>` réutilisé tel quel, composants logés autour (data-table inchangé). **D-05 (migrés)** : courses (« Voir plus » → pagination par plage sur `FETCH_CAP=500` borné par la date du jour, ListToolbar+ListMeta, pageSize 50), patients (ListToolbar SegmentedControl+PatientSearch + Pagination pageSize 25 + ListMeta), véhicules (Pagination pageSize 50 seule — pas de recherche/filtre existant, démontre le seuil). **D-06 (préservé, CRITIQUE)** : statuts CGSS (Validée/Affectée/En cours…), **filtre date par défaut = aujourd'hui** (`/courses`), colonnes contextuelles, formats FR/974 (0262/0263/0692/0693, CP 974, dates FR, `tabular-nums`), pageSize par contexte, données/validation zod/Server Actions/RLS/NIR INCHANGÉS ; **pas de sélection multiple/bulk** (lot séparé). drivers (645 LOC, DropdownMenu 4 actions DEC-029)/caisse/tarifs/legal : mécaniques, à enchaîner (mêmes composants). Dette tracée : `rides-list.client.tsx` reste 356 LOC (> 300, pré-existant). Doctrine durable `.planning/audit-ui-pages.md`. Artefact `docs/showcase/06.53-listes-patron/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-133** Formulaires spécialisés — application respectueuse du patron de form | Phase 06.54 2026-06-09 | LOCKED | Application **respectueuse** du patron de form (DEC-130) aux formulaires spécialisés restés en style ad hoc (`space-y-12/16`, classes brutes, selects natifs `h-32`, textareas brutes sans anneau de focus). Achève l'harmonisation des formulaires CRUD (après patient/véhicule/chauffeur). Principe : ces formulaires sont hétérogènes/spécialisés → harmoniser l'**enveloppe** (espacement, sections, champs `ui/*`, barre d'action) SANS casser les mécaniques métier. **D-01 (écran par écran)** : `FormSection` pour les groupes, `FormRow` pour champs liés, `FormActions` pour la barre d'action ; champs homogènes via `ui/*` (`Textarea` à anneau de focus `focus-visible:ring`, selects natifs au gabarit `Input` `h-10` via `NATIVE_SELECT_CLASS`). **SMS** (`sms-templates/template-editor`) : enveloppe harmonisée (section Variables en kicker, `FormActions`), **insertion variables `{{…}}` (`textareaRef`/caret/`requestAnimationFrame`) + compteur `MAX_LENGTH=160` + aperçu `renderTemplate` INCHANGÉS** ; compteur annoncé `aria-live="polite"` + `aria-describedby` (amélioration a11y). **Tarifs** (`tarifs/tariff-edit-sheet`) : patron **dans** le `Sheet` (SheetHeader/SheetContent/SheetFooter conservés), `NumberField` prix/€ + simulateur (`tariff-simulator`, fichier séparé) intacts ; 2 `FormSection` (Période d'application / Paramètres tarifaires). **Legal** : `legal/dpia/dpia-form` (3 sections Identification / Analyse des risques / Suivi, `FormRow` reviewed_at/next_review_at), `legal/dpo/dpo-form` (`FormSection` Coordonnées + `FormActions`), `legal/breaches/breach-drawer` (4 sections Détection / Données touchées / Description et mesures / Notifications) + `breach-form-fields` (`SelectField` partagé `h-32`→`h-10`) — **libellés RGPD inchangés** (DPIA, périmètre, diagramme de flux Markdown, risques/mesures JSON, risque résiduel faible/moyen/élevé, consultation CNIL, Article 33 RGPD 72h, gravité/nature…). **D-02 (règle valeurs métier)** : les patrons portent la STRUCTURE, pas les valeurs — variables SMS (`{{prenom}}`/`{{heure}}`…), termes tarifaires, vocabulaire légal conservés tels quels ; aucun libellé métier remplacé par du générique. **D-03 (périmètre)** : logique/validation zod/Server Actions/mécaniques spéciales (insertion SMS, compteur, simulateur tarifs, Sheet) INCHANGÉES ; patron 06.51 réutilisé tel quel ; 0 nouvelle dépendance. `text-emerald-700`→`text-success` (token jour+nuit) sur le feedback SMS. Hors périmètre (lot suivant du plan d'audit) : `legal/requests`, `legal/registre` (selects `h-32` résiduels), cockpit/optimisation, chauffeur mobile, pages texte légales, utilitaires. Tous fichiers ≤ 300 LOC. Artefact avant/après `docs/showcase/06.54-form-restants/`. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-134** Parcours chauffeur mobile — debug masqué, géoloc en couches, bottom-sheets, zone pouce | Phase 06.55 2026-06-09 | LOCKED | Doctrine mobile chauffeur sourcée appliquée à la PWA terrain (nature DISTINCTE du back-office : soleil, batterie, une main, 3G). Captures : « PWA debug · état détecté : unsupported » visible en prod, notice géoloc verbeuse (5 lignes), modales centrées. **D-01 (debug masqué)** : bandeau de diagnostic PWA (`install-pwa-banner`) rendu DEV uniquement (`process.env.NODE_ENV !== 'production'` → inliné à la compilation, retiré du bundle prod) ; prompt d'install légitime (Android/Chrome, iOS Safari) conservé ; `unsupported`/`standalone` ne rendent rien. **D-02 (géoloc par couches — layered privacy notice)** : `geoloc-consent-banner` refondu — 1re couche toujours visible (quoi + pourquoi + rétention : « À chaque pointage, votre position est captée et liée à la course. Conservée 90 j max. »), bouton « En savoir plus » (`aria-expanded` + `aria-controls`) dépliant le détail (service uniquement jamais en continu, permission refusable — le pointage marche sans GPS, retrait/contact DPO) ; dismiss mémorisé `localStorage` (`geoloc:consent-ack`) ; pas de dark pattern (refus/dismiss aussi accessible que le reste) ; encadré discret conservé. **D-03 (bottom-sheets)** : composant partagé `components/ui/bottom-sheet.tsx` construit sur Radix Dialog → focus trap, `aria-modal`, fermeture Escape + backdrop GRATUITS (RGAA) + poignée de glissement + drag-to-dismiss tactile (seuil 96px) + `title` obligatoire rendu `h2` (ciblable E2E `getByRole('heading')`) ; `end-ride-modal` (Clôturer : tarif, grille paiement ≥44px, toggle, bouton bas) et `no-show-modal` (Patient absent : motif optionnel + compteur 0/200, « Confirmer absence » destructif distinct) migrés ; ancien hack Dialog responsive (bottom mobile / centré desktop) supprimé. **D-04 (carte + actions zone pouce)** : `ride-card`/`ride-actions`/`no-show-button` déjà conformes (liseré d'état 4px orange/vert, heure `text-2xl tabular-nums`, patient, meta mode·urgence, trajet `bg-muted/50` ; primaire Démarrer/Clôturer `h-14`/56px en bas, « Patient absent » `h-12` outline détaché par filet `mt-24 border-t pt-16` — anti mis-tap DEC-014) ; libellés métier conservés (Taxi conv., TPMR, VSL, Ambulance ; Programmée, Urgente, Immédiate) → laissés inchangés pour éviter régression. **D-05 (clavier numérique)** : tarif `inputMode="decimal"` CONSERVÉ (clavier chiffres ; jamais `type="number"`/alpha), motif = texte ; reste de l'app déjà sain. **D-06 (périmètre)** : logique métier (`endRideAction`, no-show, `captureCurrentPosition`, pointage, file offline Dexie `enqueue`) INCHANGÉE ; `(driver)` layout `max-w-[640px]` conservé (mobile assumé, vide desktop = artefact) ; `prefers-reduced-motion` respecté (règle globale `globals.css` neutralise animations + snap-back). E2E `driver-start-ride`/`driver-workflow-complete` compatibles (rôle dialog + heading h2 + labels préservés). Doctrine durable `.planning/doctrine-mobile-chauffeur.md` + maquette `.planning/mockups/conduite-mobile-maquette.html`. Artefact avant/après `docs/showcase/06.55-mobile-chauffeur/`. Tous fichiers ≤ 300 LOC. 0 migration, 0 dépendance. Pas d'ADR (le bottom-sheet suit le pattern Radix existant et est documenté dans la doctrine ; ADR jugé non nécessaire). |
| **DEC-135** Onboarding repreneur — méthode de travail versée au repo | Phase 06.56 2026-06-09 | LOCKED | Lot DOCUMENTAIRE pur (hors compte feature, comme 06.40). La méthode de travail « architecte-chat + Claude Code » (rôles, prompts autoporteurs, maquette-avant-code, recherche sourcée, préservation des valeurs métier, audit post-merge) vivait dans les sessions chat et des fichiers épars — un repreneur ne pouvait pas tenir le binôme. **D-01** : `.planning/METHODE-ARCHITECTE-CHAT.md` créé (3 rôles dirigeant/CC/architecte-chat ; règles d'or : prompts autoporteurs, trancher ici, recherche sourcée, état réel, audit post-merge ; maquette-avant-code ; préservation valeurs métier ; discipline GSD ; boucle de travail concrète ; limites — pas d'API GitHub, `git pull` ne voit que `main` ; onboarding repreneur). Renvoie à `CLAUDE.md` (quoi technique) + `.claude/README.md` (couche CC) sans les dupliquer. **D-02** : `.planning/PROMPT-MODELE.md` créé (gabarit de prompt autoporteur : titre, Pourquoi, État vérifié, Maquette, Décisions fermées, Garde-fous, Tâches, Validation, Cadrage planning, Commit/PR + rappels qualité). **D-03** : resync doctrines — règle transversale « préserver les valeurs métier du contexte (TAP/CGSS/974) » ajoutée à `doctrine-formulaires.md` ET `doctrine-alignement-dashboard.md` (un patron partagé = contenant paramétrable, lisser une spécificité = régression métier) ; `doctrine-mobile-chauffeur.md` portée au contenu canonique sourcé (M1-M6 + M3bis géoloc en couches + M6bis bottom-sheet). **D-04** : liens vers la méthode depuis `CLAUDE.md` (en-tête, près de la règle transverse), `README.md` (sous le renvoi CLAUDE.md) et `.claude/README.md` (note après l'intro) — sans réécrire ces fichiers. **D-05 (périmètre)** : documentaire pur — AUCUN fichier `apps/`/`packages/`/`services/`/`supabase/` modifié (vérifié `git status` vs tip 06.55) ; guard-commit vert (typecheck non impacté) ; NFR-001 respecté (la méthode cite le vocabulaire CGSS mais aucun nom propre du seed). 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-136** Optimisation — alignement léger sur la doctrine | Phase 06.57 2026-06-09 | LOCKED | Alignement cosmétique de l'écran `cockpit/optimisation` sur la doctrine d'alignement (cockpit = référence canonique R6). PAS une refonte : l'écran est fonctionnellement bon (PageHeader date FR, états idle/loading/empty/error, vue comparative « Plan actuel vs Plan proposé » 2 colonnes responsive, cibles ≥44px, dialog d'application). SEUL écart relevé à l'audit = cosmétique. **D-01 (échelle d'espacement unique)** : rythme homogène cohérent avec le cockpit — `comparative-view` marges sous-titres `mt-8`→`mt-12` (séparation kicker→contenu) ; `optimization-shell` séparation avant l'action majeure `gap-8 pt-8`→`gap-12 pt-16`. Pas de valeur hors échelle (4/8/12/16/24/32/48/64). **D-02 (titres de section harmonisés)** : titres « Plan actuel » / « Plan proposé » de la vue comparative passés de `text-base font-semibold` au **kicker** doctrine `text-muted-foreground text-xs font-semibold uppercase tracking-wide` (même style que `alerts-panel` du cockpit voisin et que `FormSection`) → le titre de page (PageHeader) redevient dominant. Empty-state « Aucun groupement proposé » laissé tel quel (titre d'état, pas label de section). **D-03 (rien d'autre)** : structure comparative 2 colonnes, logique d'optimisation (`useOptimization`), états, dialog d'application, pastilles véhicule, cibles 44px NON touchés ; pas de maquette (aucune structure nouvelle). **D-04 (périmètre)** : présentation (espacement + titres) de `optimization-shell.client.tsx` + `comparative-view.tsx` uniquement ; logique/données/états/mécanique INCHANGÉS ; `aria-labelledby` (plan-actuel-title/plan-propose-title) conservés ; valeurs métier (libellés tournées/véhicules/courses, date FR) INCHANGÉES ; 0 dépendance. Termine l'étape 5 du plan d'audit UI. Artefact avant/après `docs/showcase/06.57-optimisation-alignement/`. typecheck+lint(0 err)+build verts, 129 tests. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-137** Migration `drivers-list` sur le patron de liste + découpe LOC | Phase 06.58 2026-06-10 | LOCKED | Achève la migration des listes sur le patron 06.53 (DEC-132) — `drivers-list` était la dernière liste **riche** non migrée — ET résout la dette LOC tracée (645 LOC > 300, CLAUDE.md §11), dans le même lot (décision dirigeant). **D-01 (toolbar `ListToolbar`)** : `filters` = `ViewToggle` (SegmentedNav Actifs/Archivés), `actions` = bouton « Nouveau chauffeur » (`variant="accent"`, vue actifs uniquement). PAS de slot `search` (il n'y en avait pas — l'`Input` du fichier était le dialog d'invitation). **D-02 (compteur en `ListMeta`)** : `<p>` compteur sorti de la toolbar vers `ListMeta` SOUS la toolbar ; logique pluriel/archivé inchangée (« N chauffeur(s) [archivé(s)] »). **D-03 (pagination seuil)** : `useState(0)` + `slice(page*50, +50)` ; `Pagination pageSize=50 total=initialDrivers.length` sous le DataTable ; sous 50 chauffeurs → aucune barre (seuil intégré) ; pas de sélecteur lignes/page. **D-04 (découpe LOC, 0 changement de comportement)** : extraction en fichiers frères — `account-status-badge.client.tsx` (`AccountStatus` + `getAccountStatus` + badge), `driver-row-actions.client.tsx` (4 actions DEC-029 + RecapPdf), `drivers-empty-state.client.tsx` (export renommé `DriversEmptyState`), `drivers-columns.tsx` (factory `buildDriverColumns`), **+ `driver-invitation-dialogs.client.tsx`** (5e fichier, extension justifiée de D-04 : les 2 Dialogs invite/resend + leur état e-mail/soumission, nécessaire pour atteindre la cible <300). Orchestrateur **645 → 244 LOC**. **Garde-fous préservés** : `rowKey` DEC-033 `${id}-${actif}-${archive}` (slice n'altère pas le re-mount, clé = fonction de la ligne pas de l'index), valeurs métier DEC-132 D-06 (libellés permis `.toUpperCase()`, badges statut Archivé/Actif/Désactivé, AccountStatusBadge, ComplianceBadge, colonnes, `tabular-nums`, téléphone « non renseigné »), 4 actions DEC-029 + aria-label, Server Actions (invite/reactivate/resend), Sheet édition, dialogs archive/deactivate/unarchive, `stopPropagation` cellule actions. typecheck+lint(0 err, 8 warn)+build verts, 129 tests. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-138** Lot listes 2/2 — listes courtes (legal + tarifs) sur le patron de liste | Phase 06.59 2026-06-10 | LOCKED | Termine la généralisation du patron 06.53 (DEC-132) sur les listes **courtes** restantes (faible cardinalité : registres légaux, historique tarifs). Recherche UX : sur liste courte, la pagination se masque (seuil) ; le vrai gain est le **compteur discret au même endroit partout** (`ListMeta`). Migration **présentation seule** — aucune recherche/filtre ajouté (en ajouter = sur-ingénierie). **D-01** : `ListMeta` au-dessus de chaque DataTable, vocabulaire métier propre (DPIA « N analyse(s) d'impact », DPA « N contrat(s) sous-traitant », Violations « N violation(s) de données », Demandes « N demande(s) RGPD », Tarifs « N grille(s) ») ; affiché seulement si la liste n'est pas vide (pas de « 0 » à côté de l'EmptyState). **D-02** : `Pagination` à seuil pageSize 25 (`useState(0)` + `slice` + barre sous le DataTable) — ces listes étant courtes, la barre ne s'affiche quasiment jamais (voulu) ; pas de `pageSizeOptions`. **D-03** : `tariff-history-table` `total = rows.length` (après `.map` l.74, count inchangé) ; titre « Historique des grilles » + rowKey `${id}:${active}` conservés ; ListMeta sous le titre. **Migrés** : `dpia-list`, `dpa-list`, `breach-list`, `requests-list`, `tariff-history-table`. **Préservé DEC-132 D-06** : colonnes, libellés/statuts RGPD (Article 33, demandes), grilles CGSS, formats FR/974, `tabular-nums`, rowKey, EmptyState, données/Server Actions/drawers INCHANGÉS. **Caisse NON migrée** (décision sourcée : toolbar dédiée date+export déjà conforme, migration = abstraction prématurée ; registre travaux repoussés §4.4, 🔍 déblocage rule-of-three). Achève la migration des listes (seul écart assumé = caisse). typecheck+lint(0 err, 8 warn)+build verts, 129 tests. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-139** Primitive `ui/Checkbox` + refonte visuelle des drawers création (chauffeur + véhicule) | Phase 06.60 2026-06-10 | LOCKED | Inconfort visuel signalé (capture) sur le drawer création chauffeur. Audit : le pattern drawer est JUSTIFIÉ (sourcé : page si > ~10 champs, drawer si formulaire de base ; chauffeur 5 / véhicule 6 = drawer correct) — le problème est l'EXÉCUTION (5 défauts objectifs partagés par les deux drawers). **D-01 (primitive Checkbox)** : `components/ui/checkbox.tsx` créé — vrai `<input type=checkbox>` (clavier Tab+Espace, `name`/`value`/`defaultChecked` natifs), tokenisé (carré 18px `rounded-[5px]`, `border-input` décoché, `bg-primary` + coche blanche Lucide coché, focus ring `ring-ring`), cible tactile 24px (input overlay transparent, visuel + coche = frères pilotés `peer-checked`). 0 dépendance. **D-02 (permis en chips)** : chaque permis = `<label>` englobant (toute la pastille cliquable) `border-input` + `has-[:checked]:border-primary has-[:checked]:bg-primary/10 has-[:checked]:text-primary` ; grille 2 colonnes ; `name="type_permis"` inchangé. **D-03 (statut hors FormSection)** : « actif » (chauffeur) / « Disponibilité » (véhicule) → ligne checkbox discrète séparée par un filet (`border-t pt-12`), plus de titre-kicker ; libellés mot pour mot. **D-04 (footer ancré)** : drawers en 3 zones — `SheetHeader` figé (rendu par la liste) · corps `flex-1 overflow-y-auto` · `SheetFooter` figé (`border-t`, bouton primaire + **Annuler ghost** qui ferme le Sheet via `onCancel`) ; le `<form>` enveloppe corps + footer (`flex min-h-0 flex-1 flex-col`) ; `SheetContent` → `flex flex-col p-0` ; l'« Archiver » (édition) déplacé en bas du corps via `onArchive`. **D-05 (largeur)** : `max-w-[640px]` retiré des deux forms (épousent le drawer `w-full`) ; `max-w` internes (immat 220px, type 280px) conservés. **D-06 (bouton primaire bleu)** : submit en `variant` default (primary), distinct de l'`accent` terracotta (toolbar « Nouveau chauffeur » reste accent). **Préservé (DEC-132 D-06)** : champs/libellés/ordre/`name=`/`defaultChecked`/`defaultValue` inchangés ; logique `useFormState`/`useFormStatus`, `create/updateDriver/VehicleAction`, `onSuccess`, `ComplianceFieldset` édition, dépendance marque→modèle, `NumberField`, `Combobox`, `Select` type, `inputMode="tel"`, `uppercase tabular-nums` immat, `autoFocus` 1er champ. `tertiary` token absent → `border-input` utilisé (noté). Maquette `.planning/mockups/drawers-chauffeur-vehicule.html`. Doctrine formulaire enrichie (§8 page vs drawer, §9 checkbox primitive). Dette tracée (registre §5) : migration des ~14 autres checkboxes brutes → lot dédié, NON fait ici. typecheck+lint(0 err, 8 warn)+build verts, 129 tests. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-140** Brouillons de courses repositionnés du header vers le cockpit (CdC §5.13) | Phase 06.61 2026-06-10 | LOCKED | Le `DraftQueue` (file de brouillons) vivait dans le header haut-droite (icône `Inbox` + badge), la grammaire des notifications. Or le CdC tranche : §5.22 réserve cet emplacement aux notifications (push/email/messagerie), §5.13 liste « Brouillons de courses non finalisés » comme **indicateur du cockpit régulateur**. Le DraftQueue était un indicateur cockpit égaré dans le header, déguisé en notifications (rôle ambigu ; retex : « les compteurs doivent correspondre à ce que l'utilisateur voit en dessous »). **D-01** : `<DraftQueue />` retiré du slot `extras` de `(app)/layout.tsx` (header libéré pour l'échafaudage messagerie §5.22, lot 06.62) ; `(driver)/layout.tsx` ne le rendait pas (chauffeur sans brouillons de saisie régulateur). **D-02** : nouvel indicateur `cockpit/_components/drafts-indicator.client.tsx` — MÊME source (`useQuery(['ride-drafts'])` cache partagé, `listDraftsAction`, `deleteRideDraft`, `dispatch RESUME` → rouvre le modal exactement comme avant) ; présentation alignée sur les panneaux cockpit (réf `AlertsPanel` : kicker + compteur), pas la grammaire dropdown ; inséré dans l'aside du `cockpit-content` entre AlertsPanel et ComplianceAlertsPanel (famille « travail en attente ») ; **seuil discret** : « Aucun brouillon » plutôt que masqué (fidèle §5.13 indicateur permanent). Ancien `draft-queue.client.tsx` (header) **supprimé** (plus aucune référence active). Logique brouillons (Server Actions, table, RLS, `formatPreview`/`formatRelative`) INCHANGÉE (helpers déplacés tels quels). LIBÈRE le header pour l'échafaudage messagerie (lot 06.62, PR distincte). 0 migration, 0 dépendance. typecheck+lint(0 err, 8 warn)+build verts, 129 tests. Pas d'ADR. |
| **DEC-141** Échafaudage messagerie header (§5.22) derrière `MESSAGING_ENABLED` (OFF) | Phase 06.62 2026-06-10 | LOCKED | Le CdC §5.22 prévoit la messagerie interne (chat course OU **général**, photo, **push PWA**, email récap). On pose l'**échafaudage** (coquille UI) dans le header libéré par 06.61, derrière un **release toggle OFF par défaut** (pattern `GEOLOC_ENABLED`, sourcé Fowler). **Principe anti-piège (validé dirigeant)** : coquille « en attente d'infra », PAS un mock — on ne SIMULE ni conversations ni push ; aucune donnée santé/perso ne transite par un canal non construit. **État réel** : germe lot 1 existant (table `internal_message` `ride_id NOT NULL`, `ride-chat`/`lib/messaging` par-course) ; manquent fil général, push, accès header. **D-01 (flag)** `MESSAGING_ENABLED` déclaré `turbo.json` globalEnv + `.env.example` (vide = OFF) ; évalué en UN point haut **côté serveur** dans `(app)/layout.tsx` (`process.env.MESSAGING_ENABLED === 'true'` ; les env non-`NEXT_PUBLIC` ne sont pas exposées au client → le composant est MONTÉ seulement si ON, ce qui réalise « OFF → rien dans le header »). **D-02 (point d'accès)** `components/messaging/messaging-button.client.tsx` : icône `MessageSquare` (≠ `Inbox`), `DropdownMenu` ouvert au CLIC (a11y tactile), `aria-label`, focus ring. Panneau coquille : (a) « Conversations de course » → accès navigationnel `/courses` (le chat vit dans chaque course ; PAS de liste fabriquée = 0 logique data nouvelle), (b) « Fil général » en EmptyState honnête « Bientôt disponible ». **Pas de badge non-lus** : aucune notion de « non-lu » en base → aucun compteur inventé (le badge futur sera en sur-impression `absolute -top-1 -right-1`, corrige l'erreur `ml-4` de l'ancien DraftQueue — documenté). **D-03 (push)** NON construit (registre §1.3). **Garde-fous** : chat à la course (`ride-chat`/`internal_message`) INCHANGÉ (exposé, pas réécrit) ; flag OFF partout en prod (aucune route/fetch push/fil général actif) ; 0 table, 0 migration, 0 dépendance ; EmptyState honnête (pas de faux messages). Registre mis à jour (§1.3 push ; §1.4 messagerie — coquille posée, reste fil général temps réel + photo + push + non-lu). `.env.example` documente `MESSAGING_ENABLED` (OFF pré-§5.22, comme `GEOLOC_ENABLED` pré-HDS). Dépend de 06.61 (header libéré). typecheck+lint(0 err, 8 warn)+build verts, 129 tests. Pas d'ADR. |
| **DEC-142** Fix primitive `DialogContent` — grille `min-w-0` (fin du débordement des modals) | Fix `fix/ui-dialog-grid-overflow` 2026-06-10 | LOCKED | La modal « Nouvelle course » débordait à droite (libellés coupés « Char… »/« Créer la c… », adresse non tronquée, téléphones coupés). **Cause racine tranchée** : ce n'était PAS un bug des champs course (leurs `min-w-0`/`truncate`/`shrink-0` sont corrects) mais de la **primitive `components/ui/dialog.tsx`** : `DialogContent` est `display: grid` SANS `min-width: 0` sur ses items. Par spec CSS, un item de grille a `min-width: auto` (= aussi large que son contenu) → le `form` prend la largeur de sa plus longue chaîne insécable (adresse + bouton), pousse la largeur, `overflow-x-hidden` coupe le dépassement. Un seul ancêtre sans `min-w-0` neutralise tous les `truncate` enfants. Sourcé (CSS-Tricks « Preventing a Grid Blowout », W3C csswg, MDN). **D-01 (fix primitive)** : `[&>*]:min-w-0` ajouté à `DialogContent` → force `min-width: 0` sur tous les enfants directs de la grille ; corrige les **14 modals** d'un coup, neutre quand il n'y a pas de dépassement. **D-02** : `overflow-x-hidden` de la modal course conservé (filet de sécurité). **D-03 (complément)** : `title={value}`/`title={selectedLabel}` ajoutés sur les pastilles tronquées (`address-picker-field`, `ride-patient-picker`) → tooltip natif révélant le texte complet au survol. **Garde-fous** : logique champs/pickers/autosave/Server Actions/threadage coords (DEC-044) INCHANGÉE ; `min-w-0`/`truncate`/`shrink-0` enfants conservés (ils étaient corrects, juste neutralisés par le parent) ; revue de régression sur assign/cancel-ride/override-tarif + course. Note design-system ajoutée (doctrine-formulaires §10). typecheck+lint(0 err, 8 warn)+build verts, 129 tests. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-143** Échafaudage upload de documents conformité derrière `UPLOAD_DOCS_ENABLED` (OFF) | Phase 06.63 2026-06-10 | LOCKED | `compliance_items.document_url` existe en base (migration 20260608) et est lue par l'action, mais AUCUNE UI ne l'alimente. On pose la **coquille** d'upload, derrière un release toggle OFF (pattern `GEOLOC`/`MESSAGING`), en attendant le bucket HDS (registre §1.1, Phase 09). **Garde-fou santé (sourcé Fowler)** : tant que OFF, le chemin d'upload réel n'est PAS câblé — aucun octet vers un Storage non HDS ; on n'expose ni ne simule. **État réel** : Supabase Storage VIERGE dans tout le repo (aucun `.upload()`/`storage.from`) → échafaudage pur, pas de reprise. **D-01 (flag)** : `UPLOAD_DOCS_ENABLED` déclaré `turbo.json` globalEnv + `.env.example` (vide = OFF) ; évalué **côté serveur** dans les pages (`chauffeurs`, `vehicules`, `conformite`) et passé en prop `uploadEnabled` → threadé `page → DriversList/VehiclesList → DriverForm/VehicleForm → ComplianceFieldset` (jamais `process.env` côté client). **D-02 (coquille par slot)** : champ « Document justificatif » dans chaque slot du `ComplianceFieldset` — si `document_url` legacy : lien « Voir le document » ; sinon **OFF** = mention discrète désactivée « Téléversement bientôt disponible (hébergement sécurisé requis) » (AUCUN `<input type=file>` sur le chemin OFF) ; **ON** (dev) = zone de dépôt (`ComplianceDocUploadStub` : vrai `<input type=file>` + bouton) appelant `uploadComplianceDocumentAction` — **STUB explicite** retournant une erreur claire « hébergement sécurisé (HDS) non configuré » (PAS de faux succès, PAS d'URL inventée). **D-03 (pas de bucket)** : NI bucket Storage NI migration ; la colonne suffit ; bucket + RLS Storage = lot Phase 09. **D-04 (dissociation)** : NON fait (jugé non trivial — changerait le write path de `upsertComplianceItemAction` ; reporté). **Garde-fous** : logique conformité (slots/dates/référence/`conformite/actions.ts`/ComplianceBadge/planning) INCHANGÉE ; aucun `.upload()` réel ; pas d'URL factice ; flag OFF partout en prod. `.env.example` documente `UPLOAD_DOCS_ENABLED`. Registre §1.1/§4.3 mis à jour (coquille posée, reste bucket HDS + RLS + `.upload()` réel). ComplianceFieldset 289 LOC (< 300). typecheck+lint(0 err, 8 warn)+build verts, 129 tests. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-144** Échafaudage notifications email derrière `EMAIL_ENABLED` (OFF) | Phase 06.64 2026-06-10 | LOCKED | Le CdC §5.22 prévoit des notifications email (récap quotidien dirigeant, alertes échéances). RIEN n'existe (email VIERGE dans le repo : aucun provider, gabarit, point d'envoi). On pose la **coquille** — module d'envoi central + 1 point de déclenchement no-op — derrière un release toggle OFF (registre §1.2 « on construit la fonctionnalité, pas le branchement d'infra »). **Garde-fou (Fowler)** : OFF → aucun email, `send()` no-op loggé ; pas de provider/secret/dépendance ; on ne SIMULE pas d'envoi réussi ni de contenu factice ; périmètre MINIMAL (pas de templating). **D-01 (flag)** : `EMAIL_ENABLED` déclaré `turbo.json` globalEnv + `.env.example` (vide = OFF), évalué côté serveur. **D-02 (module central)** : `lib/email/send.ts` — `sendEmail({to, subject, body}): Promise<Result>` ne lève jamais. OFF → `{ skipped: true }` + log `[email:disabled]`. ON → no-op DISTINCT `{ sent: false, reason: 'no-provider' }` + log `[email:no-provider]` (pas de provider choisi ; c'est ici que le vrai branchement se grefferra). **D-03 (préférences)** : AUCUNE page réglages/profil n'existe dans l'app, AUCUNE colonne de préférences notif → conformément au prompt, pas de page créée, pas de table, pas de migration ; la persistance des préférences (+ les 2 toggles récap/alertes) est tracée comme reste-à-faire (registre §1.2). **D-04 (un point de déclenchement de démo)** : `maybeNotifyDeadline` branché dans `upsertComplianceItemAction` (événement existant) — après enregistrement d'une échéance à ≤ 30 j (ou dépassée), notifie le dirigeant (`auth.getUser().email`) via `sendEmail`. Best-effort (try/catch, ne bloque jamais la sauvegarde). No-op OFF → ne fait rien en prod (log seul). **Garde-fous** : alertes in-app/conformité/cockpit INCHANGÉS ; aucun provider installé (`package.json` inchangé) ; pas de faux envoi/contenu. Registre §1.2 mis à jour (coquille posée ; reste choix provider + `send` réel + persistance préférences + gabarits). `.env.example` documente `EMAIL_ENABLED`. typecheck+lint(0 err, 8 warn)+build verts, 129 tests. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-145** Harmonisation — 14 checkbox brutes migrées vers la primitive `ui/Checkbox` | Phase 06.65 2026-06-10 | LOCKED | La primitive `components/ui/checkbox.tsx` (06.60 : input accessible, focus ring + coche tokenisés, cible 24px) existait mais 14 fichiers (16 occurrences) utilisaient encore `<input type="checkbox">` brut (incohérence visuelle + a11y inégale ; dette tracée registre §5 depuis 06.60). Lot d'harmonisation **mécanique, iso-comportement**. **D-01** : remplacement 1:1 — `<input type="checkbox" …/>` → `<Checkbox …/>` en conservant À L'IDENTIQUE `name`/`value`/`checked`/`defaultChecked`/`onChange`/`id`/`required`/`aria-*`/`disabled`/`data-testid` ; suppression de `type="checkbox"` et des classes de style brut (`h-16 w-16 border-border rounded`, `accent-primary`, `mt-4 h-4 w-4`) désormais portées par la primitive. **D-02** : associations `label htmlFor` conservées. **D-03** : API `onChange` natif de la primitive utilisée telle quelle (`e.target.checked`) — pas d'`onCheckedChange` Radix supposé ; cas `react-hook-form register()` (accept-invite) et `form.setValue` (deactivate/unarchive) préservés (la primitive `forwardRef` relaie le `ref`). **Valeurs RGPD/CGU préservées (critique)** : cookie-banner (3 cases — techniques/analytics/marketing), accept-invite (CGU), legal dpia (CNIL)/breaches/registre (transfert international)/dpo, patient consentement SMS — `name`/`value`/états par défaut INCHANGÉS. **Fichiers** : cookie-banner, accept-invite-form, patient-form-sections, no-show-alert-modal, adjust-sheet, deactivate/unarchive-confirm-dialog, dpia-form, breach-form-fields, dpa-prefill-card, dpo-form, registre-fields, registre-prefill-card, tariff-simulator. Aucune exception : les 16 occurrences migrées (cookie-banner disabled+controlled → ajout `readOnly` pour cohérence React, iso-comportement car déjà `disabled`). `grep type="checkbox"` ne retourne plus que la primitive. Dette registre §5 RÉSOLUE. typecheck+lint(0 err, 8 warn)+build verts, 129 tests (prefill cards `data-testid` OK). 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-146** Primitive Tooltip (Radix) + adresse/patient tronqués révélés au survol/focus | Phase 06.66 2026-06-10 | LOCKED | Le `title` natif (posé en DEC-142 sur les pastilles tronquées) est insuffisant : non stylé, incohérent, peu fiable clavier/lecteur d'écran (sourcé amino.dev, TheWCAG). On le remplace par une vraie primitive Tooltip **ancrée au champ** (PAS « qui suit le curseur » — anti-pattern a11y), déclenchée au survol ET au focus clavier. **Choix Radix** (sourcé) : cohérent avec l'écosystème déjà présent (Dialog/Sheet/DropdownMenu) ; implémente la spec WAI-ARIA 1.2 (role tooltip, `aria-describedby`, focus Tab, Escape) + WCAG 1.4.13 (dismissable/hoverable/persistant) automatiquement. **D-01** : `@radix-ui/react-tooltip@^1.1.2` installé (SEULE dépendance ajoutée, résolu 1.2.9). **D-02** : `components/ui/tooltip.tsx` (style shadcn) — `TooltipProvider`/`Tooltip`/`TooltipTrigger`/`TooltipContent` ; Content tokenisé (`bg-foreground text-background`, `rounded-md px-12 py-8 text-xs`, ombre, `max-w-[280px]` wrap, `animate-in fade-in`), `sideOffset` 4, portalisé, placement top. **D-03** : `TooltipProvider delayDuration={400}` monté une fois dans `(app)/layout.tsx`. **D-04** : appliqué aux 2 pastilles tronquées (`address-picker-field`, `ride-patient-picker`) — `TooltipTrigger asChild` autour du `<span class="truncate">` (avec `tabIndex={0}` pour le focus clavier), `TooltipContent` = adresse/label complet. Survol ET focus (natif Radix). **D-05** : `title` natif retiré (pas de doublon). **Garde-fou a11y (sourcé Material UI / Sarah Higley)** : le tooltip n'est PAS le seul accès — le bouton « Changer » (filet, conservé) rouvre la recherche où l'adresse est lisible/éditable en entier. **Garde-fous** : logique pickers (sélection/autosave/threadage coords DEC-044/recherche BAN-POI) INCHANGÉE ; pas de tooltip suiveur de curseur ; pas de généralisation à toute l'app (seulement les 2 pastilles ; primitive réutilisable ensuite). Doctrine design-system mise à jour (texte tronqué porteur d'info → Tooltip, jamais `title` natif, jamais seul accès). `package.json` = +1 ligne. typecheck+lint(0 err, 8 warn)+build verts, 129 tests. 0 migration, 1 dépendance (Radix tooltip). Pas d'ADR. |
| **DEC-147** CI — suppression du sync-types redondant de `cd.yml` (fin de la dérive de re-merges) | Phase 06.67 (chore) 2026-06-10 | LOCKED | L'historique `main` a subi une dérive : mêmes branches re-mergées en boucle (06.64 en #309+#315, 06.65 en #310+#311+#314, 06.66 en #312+#313). Le **contenu final est sain** (Git déduplique — une seule primitive Tooltip/Checkbox, flags non dupliqués) mais l'historique était pollué et les branches en vol se périmaient sans cesse. **Cause racine** : DEUX mécanismes de sync de `packages/database/src/types.gen.ts` redondants après chaque CD. (1) `cd.yml` job `sync-types` régénérait depuis la prod puis **`git push origin HEAD:main`** (push DIRECT sans PR) → `main` avançait en continu → branches « behind » → re-merges. (2) `sync-types.yml` (`workflow_run: CD completed` + cron 3h + manuel) crée une **PR propre** (`peter-evans/create-pull-request@v6`, branch `chore/sync-supabase-types`, `delete-branch: true`). **Décision dirigeant — Chemin B** : garder `types.gen.ts` versionné (build Vercel autonome, OK Hobby) mais supprimer le mécanisme redondant. **D-01** : job `sync-types` retiré de `cd.yml` (aucun job ne le référençait en `needs` ; `deploy-migrations`/`deploy-edge-functions` intacts). **D-02** : `sync-types.yml` conservé tel quel (seul chemin de sync, par PR propre auto-supprimée). **D-03/D-04** : étapes manuelles dirigeant (hors CC) — activer « auto-delete head branches » dans Settings GitHub + fermer la PR sync orpheline si présente. **Garde-fous** : `types.gen.ts` reste versionné (pas de gitignore/rm --cached) ; `db:types` local conservé ; `sync-types.yml`/`ci.yml`/`preview-smoke.yml`/`setup-vercel.yml` non touchés ; 0 code applicatif, 0 migration, 0 dépendance. Doctrine CI : un seul chemin de sync pour un fichier généré versionné = PR via `sync-types.yml`, jamais de push direct sur `main` (chemin A — dé-committer + génération au build — envisageable plus tard si accès Supabase CI garanti). Sourcé consensus codegen. Pas d'ADR. |
| **DEC-148** Module donneurs d'ordres B2B — cœur (référentiel + rattachement course) | Phase 07.01 2026-06-10 | LOCKED | Le « Module donneurs d'ordres B2B (hôpitaux, cliniques, EHPAD) » est classé **Inclus V1** par le CdC (§2.1 l.74, détaillé §5.5). C'était le SEUL élément de la liste V1 absent de `main` (22 autres présents). On construit le CŒUR : référentiel (fiche + liste) + rattachement d'une course à un donneur d'ordres. **Distinction métier (CdC §5.5 l.186)** : le donneur d'ordres (entité B2B qui commande le transport pour son patient/résident, convention tarifaire propre + facturation centralisée) est DISTINCT du prescripteur (médecin émetteur du bon de transport) — non fusionnés. **D-01 (table)** `public.ordering_parties` (migration `20260610000001`) calquée EXACTEMENT sur `drivers` : `organization_id` cascade, `raison_sociale` (check 1-120), `siret` nullable (validation format/Luhn côté app, pas de check BDD bloquant), contact principal (nom/téléphone/email), `modalite_facturation` enum (`a_la_course`/`hebdomadaire`/`mensuelle`, défaut `a_la_course` — CdC l.190), `actif`/`archive`, audit trigger (`ordering_party.*`), index `(organization_id, actif)` partiel `archive=false`. **RLS calquée drivers** : `select_same_org` (le régulateur lit pour rattacher une course), `insert/update` dirigeant only ; pas de DELETE (archivage logique). pgTAP `ordering_parties_rls.sql` (13 assertions, miroir drivers). Contacts multiples par service (CdC l.188) = lot suivant (TODO en migration, cœur limité au contact principal). **D-02 (lien rides)** migration additive `20260610000002` : `rides.ordering_party_id uuid NULL references ordering_parties(id) on delete set null` (pattern colonnes futures) + index partiel `(organization_id, ordering_party_id)`. **NULLABLE non négociable** : cas nominal = transport prescrit individuel sans donneur d'ordres. **D-03 (référentiel front)** `(admin)/admin/donneurs-ordres/` (page RSC + actions CRUD + form drawer + liste DataTable/Pagination) répliquant `vehicules/` (patron `FormSection/FormRow`, `Field`/`Select`/`Checkbox`, `requireDirigeantPage`/`requireDirigeant`) ; entrée nav « Donneurs d'ordres » dans le menu dirigeant **Gestion** (avec Tarifs/Facturation, registre financier B2B). **D-04 (rattachement saisie)** champ OPTIONNEL « Donneur d'ordres » dans `ride-express-modal` (picker recherche client-side parmi les actifs, pattern PatientPicker pill+clear ; `listActiveOrderingPartiesAction`) ; `ordering_party_id` ajouté à `rideExpressInputSchema` (uuid nullable optional), threadé create/edit (spread `...input`) + prefill édition (`getRideByIdEnriched` joint la raison sociale). **Garde-fous** : SIRET via `siretSchema` existant ; vocabulaire FR/974 ; logique course existante INCHANGÉE (course sans donneur d'ordres = comportement d'avant). **Hors lot (tracés registre §6)** : demande groupée, grille tarifaire B2B (via moteur pricing à grille injectable DEC-057), récap PDF périodique, contacts multiples par service, portail self-service V1.5. Seed démo : 3 établissements 974 fictifs (CHU, clinique, EHPAD). typecheck+lint(0 err, 8 warn)+build verts, 124 tests shared. 2 migrations, 0 dépendance. Pas d'ADR. |
| **DEC-149** Page Réglages + préférences d'alertes cockpit (par utilisateur, effet immédiat) | Phase 06.68 2026-06-11 | LOCKED | L'app n'avait AUCUNE page réglages/paramètres utilisateur (lacune vérifiée). On la crée avec une section Notifications qui pilote les alertes in-app du cockpit déjà existantes — **valeur d'usage immédiate, pas de préférence dormante** (décision dirigeant « ne poser que ce qui sert maintenant »). **Périmètre tranché par recherche (normes SaaS)** : (a) portée par **UTILISATEUR** (le préférence store stocke par user ; chaque régulateur/dirigeant consomme les alertes différemment), PAS par organisation ; (b) **PAS de toggle email/push affiché** tant que le canal n'est pas branché (norme MVP : pas de commande inactive = fausse promesse). **D-01 (table)** `public.notification_preferences` (migration `20260611000001`) : PK `user_id` (1 ligne/user) + `organization_id` (multi-tenant), 3 booléens alertes cockpit défaut `true` (`alert_patient_no_show`/`alert_sms_failed`/`alert_ride_delayed` — alignés sur `CockpitAlertType`), `created_at`/`updated_at`. **RLS user-scoped stricte** : `select/update using (user_id = auth.uid())`, `insert with check (user_id = auth.uid() and organization_id = current_organization_id())` ; pas de DELETE. **Lazy** : aucune ligne = tous défauts `true` (rétro-compatible, pas de seed des users existants) ; ligne créée au 1er changement (upsert `onConflict user_id`). pgTAP `notification_preferences_rls.sql` (9 assertions, pattern ride_draft user-scoped). **D-02 (page)** `(app)/reglages/page.tsx` + `_components/notification-settings.client.tsx` : section Notifications → « Alertes du cockpit » avec 3 toggles (primitive `Checkbox` tokenisée) « Patient absent »/« Échec d'envoi SMS »/« Course en retard », **sauvegarde au changement** (optimiste + toast + restauration sur échec) via `updateNotificationPreferencesAction` (upsert). AUCUNE section email/push (commentaire « à ajouter avec EMAIL_ENABLED »). Guard via `(app)/layout.tsx`. **D-03 (effet immédiat cockpit)** `cockpit/page.tsx` charge les préférences côté serveur (`getCockpitAlertPreferences`, helper `lib/notifications/preferences.ts`, défaut tout-true best-effort) → passées à `CockpitContent` qui calcule `visibleAlerts = alerts.filter(prefs[event_type])` ; `AlertsPanel` ET la détection no-show modal consomment `visibleAlerts`. **Calcul des alertes (`useCockpitAlerts`, realtime, `event_type`) INCHANGÉ** — on filtre l'AFFICHAGE, pas la détection. **D-04 (nav)** item « Réglages » (icône engrenage) activé dans le `user-menu` (avatar) — remplace l'ancien « Paramètres » désactivé (V2) ; emplacement le moins intrusif (pas dans la barre principale). **Garde-fous** : défaut = tout activé (un user sans ligne voit tout comme avant) ; RLS user-scoped `auth.uid()` ; pas de toggle inactif ; pas de moteur de notification sur-dimensionné (3 toggles, point). Registre §1.2 : la persistance des préférences (reste-à-faire email) est AMORCÉE (table extensible aux toggles email au branchement provider). typecheck+lint(0 err, 8 warn)+build verts. 1 migration, 0 dépendance. Pas d'ADR. |
| **DEC-150** Perf — parallélisation des fetchs Server Components (Promise.all) | Phase 08.01 (perf) 2026-06-11 | LOCKED | Pages lentes : les Server Components enchaînaient des requêtes Supabase en CASCADE séquentielle (`await`…`await`…), chacune attendant la précédente (waterfall). Sur ~20 pages avec fetch, seules 3 utilisaient `Promise.all`. **Sourcé (Next.js docs, CodeXOps, LogRocket 2026)** : « les await séquentiels créent un waterfall ; `Promise.all` pour les sources indépendantes est le changement à plus fort impact en App Router ». **Lot 1 = parallélisation pure, ZÉRO changement fonctionnel** (mêmes données/filtres/rendu), risque quasi nul. **D-01 (pilote cockpit)** `cockpit/page.tsx` : 5 sources indépendantes (`rides`, `ride_events`, `driver_positions`+labels, `getComplianceAlerts`, `getCockpitAlertPreferences`) extraites en fonctions async nommées (`getRidesToday`/`getCockpitRideEvents`/`getDriverPositionsWithLabels`) puis lancées en `Promise.all` ; `auth.getUser()`+`redirect` restent EN PRÉALABLE (jamais parallélisés — le redirect court-circuite) ; `driverLabels ← positions` (vraie dépendance) reste séquentiel À L'INTÉRIEUR de sa fonction. **D-02 (dégradation gracieuse CRITIQUE préservée)** chaque fonction faillible (tables `ride_events`/`driver_positions` parfois absentes) encapsule son propre try/catch + fallback `[]` AVANT d'entrer dans le `Promise.all` → le lot ne rejette jamais, comportement IDENTIQUE à avant (le cockpit ne casse pas si une table manque). **D-03 (pattern réutilisable)** fonctions async nommées, chacune gérant sa dégradation. **D-04 (extension pages lourdes)** parallélisé UNIQUEMENT les requêtes réellement indépendantes : `tableau-de-bord` (`getDashboardData` + `getComplianceAlerts`), `chauffeurs` (drivers + invitations + compliance, 3 indép.), `vehicules` (vehicles + compliance), `conformite` (blockingMode + items, puis driverLabels + vehicleLabels). **Laissées telles quelles** : `caisse`/`facturation` (déjà en `Promise.all`) ; `dpo` (orgRes ← profile.organization_id, chaîne DÉPENDANTE) ; `patients/[id]` (guard `getPatientById` préalable au notFound + déjà partiellement parallèle) ; pages legal mono-requête. **Garde-fous** : aucun changement fonctionnel ; dégradation gracieuse stricte ; requêtes dépendantes jamais parallélisées ; `force-dynamic` NON touché (= Lot 2 perf, décision de cache séparée) ; types/casts existants inchangés. typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance. Registre : Lot 2 (audit des 27 `force-dynamic` → static/revalidate, levier principal de la navigation lente) + Lot 3 (Suspense granulaire) ouverts. Pas d'ADR. |
| **DEC-151** Perf — cache de page (force-dynamic → ISR) : NON APPLICABLE en l'état, pivot vers cache data | Phase 08.02 (perf, abandonnée → 08.03) 2026-06-11 | LOCKED | Lot 08.02 visait l'ISR de page (référentiels en cache + revalidatePath ; légales statiques). **Bloqué sur deux réalités techniques vérifiées, donc NON LIVRÉ** : (1) **Référentiels** (chauffeurs/vehicules/donneurs-ordres/tarifs/sms-templates) lisent tous `cookies()` via `createClient()`/`getAuthContext` → en App Router une page qui lit `cookies()` est rendue dynamiquement par construction, le `revalidate` page-level est ignoré (pas d'ISR plein-route) ; et si Next la cachait, ce serait une **fuite inter-org** (rendu d'une société servi à une autre) — exactement le garde-fou « sécurité prime ». Donc laissées `force-dynamic`. (2) **Pages légales** : `MDXRemote`/`compileMDX` de `next-mdx-remote@6` compile le MDX en `ReactElement` au rendu → le prerender statique casse le build (« A React Element from an older version of React was rendered », cause documentée dans `load-legal.ts`). Tentative reproduite (build rouge) puis **revertée** ; D-B2 invoqué (« si non trivial, ne pas forcer »). **Conclusion** : le cache de PAGE n'est pas applicable à ce codebase (pages app toutes authentifiées → dynamiques ; seules pages publiques bloquées par MDX). Le vrai levier = cache de DONNÉE par org → DEC-152 (08.03). DEC-151 acte le constat + la non-livraison ; aucun code mergé sous ce numéro. Pas d'ADR. |
| **DEC-152** Perf — data-cache par organisation (pilote chauffeurs) ; service-role choke-point + revalidateTag | Phase 08.03 (perf) 2026-06-11 | LOCKED | Suite au constat DEC-151 (cache de page impossible : pages authentifiées = dynamiques via cookies), on cache la **DONNÉE** et non la page, avec une clé **par organisation** (sourcé Next.js `unstable_cache`/`revalidateTag`). La page reste dynamique (aucune fuite de rendu HTML), mais la requête Supabase — le coût réel — est servie depuis un cache propre à chaque société, purgé à l'écriture → navigation accélérée, fraîcheur immédiate. **⚠️ Trade-off sécurité assumé par le dirigeant** : `unstable_cache` interdit `cookies()` à l'intérieur → impossible d'y utiliser le client RLS. On passe par le **client service-role (CONTOURNE la RLS)** et on filtre EXPLICITEMENT `.eq('organization_id', orgId)` sur CHAQUE requête ; `orgId` vient de la clé de cache (dérivée de la session via `getAuthContext` côté page). La RLS ne protège plus DANS la fonction cachée — le filtre org explicite est l'unique barrière. **Livré en PILOTE sur chauffeurs uniquement** (validation isolation avant généralisation). **D-01** `chauffeurs/_lib/cached-queries.ts` : `getChauffeursPageData(orgId, vueArchives)` (service-role, 3 requêtes drivers/invitations/compliance — toutes 3 tables ont `organization_id NOT NULL`, vérifié — chacune `.eq('organization_id', orgId)`) ; `getCachedChauffeursPageData` = `unstable_cache` clé `['chauffeurs-page', orgId, vue]` + tag `chauffeurs:<orgId>` + `revalidate: 3600` (filet). SEUL point service-role en cache pour ce référentiel. **D-02** `chauffeurs/page.tsx` : `force-dynamic` retiré (page reste dynamique via cookies) ; appelle `getCachedChauffeursPageData(ctx.organizationId, vueArchives)` ; filet liste vide si pas de session (jamais de cache org sans orgId issu de session). **D-03** `revalidateTag(chauffeursTag(ctx.organizationId))` ajouté aux 8 Server Actions de mutation (create/update/archive/deactivate/reactivate/unarchive/invite/resend) après mutation réussie → purge le cache de l'org concernée uniquement. **Garde-fous** : `orgId` TOUJOURS dans clé + tag (étanchéité inter-org) ; filtre org explicite sur chaque requête ; service-role confiné à `cached-queries.ts`. **TEST D'ISOLATION 2-ORGS = GATE PREVIEW** (non exécutable en sandbox local, cf. CLAUDE.md §13.5) : à valider sur preview AVANT de généraliser aux 4 autres référentiels (vehicules/donneurs-ordres/tarifs/sms-templates) — généralisation tracée registre §7. Si l'isolation échoue → revert chauffeurs en `force-dynamic`. **Partie B (légales statiques) : NON faite** — incompatibilité MDX/prerender (DEC-151), D-B2 invoqué, légales restent dynamiques. typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-153** Perf — extension du data-cache par org aux référentiels restants | Phase 08.04 (perf) 2026-06-11 | LOCKED | Le pilote chauffeurs (DEC-152) ayant passé l'audit d'isolation, on RÉPLIQUE le pattern data-cache par organisation aux référentiels restants encore en `force-dynamic`. **Pattern identique (validé)** : `_lib/cached-queries.ts` par référentiel — fonction data pure (orgId en paramètre), service-role + `.eq('organization_id', orgId)` explicite sur CHAQUE requête, `unstable_cache` clé+tag incluant `organizationId` + `revalidate: 3600`, tag helper ; page = `requireDirigeantPage()` → `getCachedXxx(ctx.organizationId)` (plus de `force-dynamic`) ; `revalidateTag(<entité>Tag(ctx.organizationId))` dans chaque action de mutation. **Couvert (3/4)** : **vehicules** (`vehiculesTag`, 2 requêtes vehicles+compliance, 3 actions create/update/archive), **donneurs-ordres** (`donneursOrdresTag`, 1 requête, 3 actions), **tarifs** (`tarifsTag`, 1 requête tariff_grids, 1 action saveTariffGrid). Toutes ces tables ont `organization_id NOT NULL` (vérifié). **EXCLU : sms-templates** — la table `sms_templates` n'a **PAS** de colonne `organization_id` (PK `key`, table GLOBALE partagée entre toutes les orgs, ex. `rappel_j1`/`rappel_j2h`). Le pattern par-org est inapplicable (rien à filtrer ; un cache par-org créerait N entrées identiques) ; un cache GLOBAL serait possible mais le gain sur une poignée de templates est négligeable. **Laissée `force-dynamic`** (signalé). **Garde-fous (identiques 08.03)** : `orgId` TOUJOURS dans clé + tag ; filtre org explicite sur chaque requête ; `orgId` issu de la session (`requireDirigeantPage`), jamais d'un défaut ; service-role confiné aux `cached-queries.ts`. **TEST D'ISOLATION 2-ORGS = GATE PREVIEW** par référentiel (non exécutable en sandbox, CLAUDE.md §13.5) — à valider avant de considérer le chantier clos ; si échec sur un référentiel → revert celui-ci en `force-dynamic`. Chantier data-cache référentiels : **4/5 cachés** (chauffeurs+vehicules+donneurs-ordres+tarifs), sms-templates hors-pattern (global). Lot 3 (Suspense) DIFFÉRÉ (gain marginal V1 — pages déjà parallélisées + loading.tsx ; déclencheur de réévaluation = montée des volumes / `count exact` du tableau de bord). typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-154** Dette — retrait des `as never` sur `.from()`, restauration du typage Supabase | Phase 09.01 (dette) 2026-06-11 | LOCKED | 78 `.from('<table>' as never)` désactivaient le typage Supabase sur les requêtes BDD (aucune vérif des noms de colonnes/types de retour) — c'est ce qui avait laissé passer le bug `drivers(prenom, nom)` au cockpit (corrigé depuis). Or le client EST correctement typé (`createServerClient<Database>` / `createBrowserClient<Database>`, `Database` exporté de `types.gen.ts` via `types.ts`) → ces casts ne servaient à rien sinon masquer la sécurité de type. **D-01/D-02** : casts retirés PAR LOTS de tables, `pnpm typecheck` vert maintenu à chaque étape — **70 retirés** sur 27 fichiers (drivers 21, vehicles 9, compliance_items 9, ride_recurrences 7, driver_invitations 6, audit_logs 5, tariff_grids 2, ride_events 2, organizations 2, internal_message 2, idempotency_keys 2, driver_positions 2, pois_metier 1). **Résultat honnête : 0 nouveau bug de colonne révélé** — le typecheck est resté vert tout du long, car le client était déjà typé et les requêtes existantes utilisaient des colonnes valides (le seul bug connu, cockpit, était déjà corrigé). La VALEUR est la PROTECTION restaurée : tout futur mauvais nom de colonne/table échouera désormais au BUILD au lieu de la prod. **D-03 (8 casts conservés, tracés)** : `ordering_parties` (6) et `notification_preferences` (2) sont ABSENTES de `types.gen.ts` (tables récentes, types pas resync). Régénération locale impossible (`supabase` CLI absent du sandbox + `gen types --local` requiert Docker). `as never` CONSERVÉ sur ces 2 tables uniquement, **à retirer après resync types.gen.ts** (le workflow `sync-types.yml` régénère depuis la prod ; relancer ce micro-lot ensuite). Sites : `courses/_lib/queries-enriched.ts` (×2), `reglages/actions.ts`, `donneurs-ordres/_lib/cached-queries.ts`, `donneurs-ordres/actions.ts` (×3), `lib/notifications/preferences.ts`. **D-04 (hors périmètre)** : ~127 `as never` sur les PAYLOADS `.insert()/.update()` (≠ `.from()`) non touchés — masquent encore le typage des payloads ; follow-up possible (registre). **JAMAIS de `as any`/`@ts-ignore`** ; `types.gen.ts` non modifié à la main. typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-155** Dette — fin du typage `.from()` ; payloads `.insert/.update/.upsert` NON typables (méthodes d'écriture = `never`) | Phase 09.02 (dette) 2026-06-11 | LOCKED | Suite à 09.01. **Partie A — LIVRÉE** : le resync (#323) a ajouté `ordering_parties` + `notification_preferences` à `types.gen.ts` → les **8 derniers `.from('… as never)` retirés** (queries-enriched ×2, reglages, donneurs-ordres cached-queries + actions ×3, lib/notifications/preferences). **Bug réel révélé et corrigé** : `donneurs-ordres/_lib/cached-queries.ts` avait un `.select()` en concaténation `'…' + '…'` multi-lignes que postgrest n'infère pas (→ `GenericStringError[]`, cast vers `OrderingPartyRow[]` rejeté) ; passé en littéral mono-ligne (inférence OK). **Dette `.from()` désormais 100 % résolue** (0 `.from(… as never)` dans tout `apps/web/src`). `Tables`/`TablesInsert`/`TablesUpdate` ré-exportés de `@tap/database` (outillage pour le futur typage des payloads). **Partie B — NON FAISABLE (constat fort, vérifié)** : le prompt visait à typer ~28 (en réalité ~100) payloads d'écriture via `TablesInsert/TablesUpdate`. **Impossible** : dans ce montage `@supabase/ssr` (`createServerClient<Database>`), les méthodes `.insert()/.update()/.upsert()` résolvent leur PARAMÈTRE à **`never`** pour TOUTES les tables (vérifié sur rides, ride_draft, notification_preferences — 3 tables indépendantes). Le `.select()` (lecture) est correctement typé, mais l'écriture dégrade en `never` (skew de version `@supabase/ssr`/postgrest-js entre le `Database` généré et le contrat interne attendu). Conséquence : passer un payload typé (`satisfies TablesInsert<…>`) échoue (`Argument … not assignable to never`) → le `as never` sur les payloads est **REQUIS** pour appeler les méthodes d'écriture, ce n'est PAS du masquage paresseux. **Aucune correction possible dans le périmètre** (0 dépendance, ne pas éditer `types.gen.ts`). Les ~100 `as never` de payload restent (légitimes). **Déblocage réel = lot dédié** : aligner les versions `@supabase/ssr`/`@supabase/postgrest-js` (ou shim de type du client) pour restaurer le typage des écritures — alors seulement les payloads deviendront typables (l'outillage `TablesInsert/Update` est déjà en place). Garde-fous : aucun changement fonctionnel ; aucune des écritures modifiée ; pas de `as any`. typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-156** Perf/fiabilité — élimination du N+1 des crons de rappel SMS | Phase 09.03 (perf) 2026-06-11 | LOCKED | Les 2 crons de rappel (`sms-reminders-j1` veille, `sms-reminders-j2h` J-2h) bouclaient sur les courses avec **1 requête consentement PAR course** (`hasActiveSmsConsent` → `from('patients').eq('id', …)`) + (j2h) **1 requête idempotency PAR course** + inserts `sms_messages` et envois SMS **séquentiels**. **Risque réel** : aucun `maxDuration` → un cron sur gros volume pouvait être tué par le timeout Vercel AVANT d'avoir envoyé tous les rappels → patients non prévenus, silencieusement (fiabilité, pas cosmétique). **D-01 (N+1 consentement → 1 requête)** : règle de validité extraite en fonction PURE `isConsentValid(row)` (source unique, réutilisée par `hasActiveSmsConsent`) + helper batch `getActiveSmsConsentMap(supabase, ids)` (1 requête `.in('id', ids)` → Map) dans `@tap/sms`. **DEC-008 préservé** : lecture BDD fraîche à chaque run (pas de cache), même règle exacte. Fail-safe renforcé : si la lecture consentement échoue → cron 500 (aucun envoi dans le doute, et PAS de faux statut « révoqué » écrit en masse). **D-01bis (j2h)** : idempotency aussi en 1 requête (`.in('ride_id', ids)` → Set) au lieu de N — anti-double-envoi PRÉSERVÉ. **D-02 (envois parallélisés)** : par lots de 10 (`SEND_BATCH_SIZE`), chaque envoi encapsulé try/catch (équivalent `allSettled`) → un échec n'interrompt pas les autres et trace son erreur. **D-03 (inserts groupés)** : traces `sms_messages` accumulées puis insérées en UN insert groupé, avec **fallback ligne par ligne** si l'insert groupé échoue → ne JAMAIS perdre la trace d'un SMS déjà envoyé (garde-fou RGPD). **D-04** : `export const maxDuration = 60` sur les 2 crons. **Traçabilité INTÉGRALE préservée** : chaque course produit sa ligne avec le bon `delivery_status` (`skipped_consent_revoked`/`queued`/`failed`), j2h conserve `skipped_idempotency`. Test unitaire : `isConsentValid` (4 cas) + `getActiveSmsConsentMap` (4 cas : Map vide, mapping, patient introuvable, throw sur erreur) — 19 tests `@tap/sms` verts, 100 % stmts. **Gain** : sur N courses, requêtes consentement N→1 (et idempotency N→1 pour j2h) ; envois parallélisés ; risque de timeout levé. Garde-fous : `hasActiveSmsConsent` conservé pour les autres appelants ; consentement vérifié AVANT envoi ; logique de consentement INCHANGÉE. typecheck+lint(0 err, 8 warn)+build verts ; 19 tests sms. 0 migration, 0 dépendance. Pas d'ADR. |
| **DEC-157** Grille tarifaire B2B par donneur d'ordres (extension donneurs d'ordres, socle 1/3) | Phase 07.02 2026-06-12 | LOCKED | 1re des 3 extensions B2B (CdC §5.5 : grille → demande groupée → récap PDF). CdC l.189 : « convention tarifaire propre (grille indépendante des tarifs CGSS si non conventionné, OU complément) » → le modèle gère LES DEUX cas sans présupposer. **Faisabilité (DEC-057)** : `computeCgssFromDistance(params, grid)` prend déjà la grille en PARAMÈTRE → une grille B2B s'injecte SANS toucher au moteur. **D-01 (modèle, migration `20260612000001`)** : enum `ordering_party_tariff_mode` (`cgss_standard` défaut | `grille_propre`) + colonne `ordering_parties.tariff_mode` ; table `ordering_party_tariff_grids` (mêmes colonnes que `tariff_grids` : forfait/km/prix_km/suppléments/majoration/facteur/arrondi + `date_effet`, FK `ordering_party_id`, versionnée `unique(ordering_party_id, date_effet)`, RLS par org calquée tariff_grids : select same_org / insert dirigeant / pas d'update-delete ; pgTAP 9 assertions). **D-02 (injection)** : helper `getActiveTariffGridForOrderingParty(orderingPartyId)` (lib/pricing) → grille propre du donneur si `grille_propre`, sinon `null` (l'appelant retombe sur la grille org) ; le recompute DEC-060 (`recomputeTarifsAction`) traite désormais AUSSI les courses `b2b_auto` avec la grille du donneur (résolue par lot, N+1 évité — 2 requêtes), les `cgss_auto` gardent la grille org. Moteur INCHANGÉ. **D-03 (UI)** : fiche donneur — Select `tariff_mode` (sauvé avec le donneur) ; si `grille_propre` (en édition), bouton ouvrant une Sheet dédiée (`OrderingPartyTariffForm`, portalisée → pas de form imbriqué) avec l'éditeur de grille versionnée (pré-rempli depuis la grille active, mêmes champs que /admin/tarifs). **D-05 (`tarif_source` b2b_auto)** : valeur ajoutée à la contrainte `rides_tarif_source_check` + zod `payment.ts` + label override modal → distincte de `cgss_auto` pour que le recompute CGSS n'écrase PAS un tarif B2B (le filtre `tarif_source='cgss_auto'` les exclut déjà ; garde-fou doublé). **Garde-fous** : défaut `cgss_standard` = comportement INCHANGÉ (donneur sans grille propre ou course sans donneur → grille org) ; RLS par org sur la grille B2B ; distinction prescripteur ≠ donneur d'ordres maintenue. **Dette typage** : `tariff_mode` (nouvelle colonne) absente de types.gen.ts → `.from('ordering_parties' as never)` sur les 3 SELECT qui la lisent, à retirer au prochain resync (registre §5). typecheck+lint(0 err, 8 warn)+build verts ; 124 tests shared. 1 migration, 0 dépendance. Lots suivants ouverts (registre §6) : demande groupée (2/3), récap PDF périodique (3/3). Pas d'ADR. |
| **DEC-158** Demande groupée B2B (extension donneurs d'ordres 2/3) | Phase 07.03 2026-06-12 | LOCKED | 2e des 3 extensions B2B (CdC §5.5 l.191-193 : « un donneur d'ordres demande N transports d'un coup » + « workflow d'acceptation/refus par la régulation »). **D-01 (modèle, migration `20260612000002`)** : table parent `ride_groups` calquée sur `ride_recurrences` — enum `ride_group_status` (`en_attente` défaut | `acceptee` | `refusee`), FK `organization_id` + `ordering_party_id` (NOT NULL : une demande groupée est TOUJOURS B2B), `motif_refus` text (≤500), RLS par org FORCÉE : select same_org / insert+update régulateur+dirigeant / **pas de DELETE** (demande tranchée reste tracée) ; index partiel `(org, status) where status='en_attente'` (file). Colonne `rides.ride_group_id` **NULLABLE** (`on delete set null`, index partiel) — NULL = course individuelle = cas nominal INCHANGÉ. pgTAP 11 assertions. **D-02 (workflow porté par le GROUPE, pas par `ride_status`)** : création → groupe `en_attente` + N courses enfants `status='brouillon'` ; acceptation → groupe `acceptee` + courses brouillon→`validee` (deviennent fermes) ; refus → groupe `refusee` + `motif_refus` + courses→`annulee_regulateur`. Server Actions `groups.ts` (createRideGroupAction/acceptRideGroupAction/refuseRideGroupAction) : row-count check DEC-041 sur accept/refus (déjà traitée / hors droits), géocodage filet serveur (geocodeIfMissing) sur chaque ligne, best-effort si l'insert des courses échoue (groupe marqué refusé). **D-03 (garde-fou brouillon invisible)** : les courses `brouillon` (demandes en attente, pas fermes) sont EXCLUES du cockpit (`getRidesToday` `.neq('status','brouillon')`) ET de la liste courses (`listRidesEnriched` exclut brouillon sauf filtre statut explicite) ; l'optimizer (`.eq('status','validee')`) et la caisse (`.eq('status','terminee')`) les excluaient déjà. **D-04 (tarification)** : les courses portent `ordering_party_id` → tarifées via la grille B2B (DEC-157) sans recode pricing ; une course ne devient ferme qu'à l'acceptation. **D-05 (UI `/courses/demandes-groupees`)** : formulaire multi-lignes (réutilise PatientPickerField/DateTimeFields/AddressOrPOIPicker/ModeUrgencyFields de la saisie express par ligne, 1..50) + file d'acceptation/refus (motif obligatoire via Sheet). Schéma zod `@tap/shared` : `groupedRideLineSchema` (= express sans `ordering_party_id`, porté par le groupe), `rideGroupRequestSchema`, `rideGroupRefusalSchema`. Nav : entrée régulateur (`/courses/demandes-groupees`) + menu Gestion dirigeant. **Garde-fous** : `ride_group_id` nullable (course individuelle inchangée) ; brouillon hors cockpit/optimisation/caisse ; RLS par org ; prescripteur ≠ donneur d'ordres. **Dette typage** : `ride_groups`/`ride_group_id` absents de types.gen.ts → `.from('ride_groups' as never)` + `as never` sur les payloads, à retirer au resync (registre §5, DEC-155). typecheck+lint(0 err, 9 warn)+build verts. 1 migration, 0 dépendance. Résout registre §6.1. Reste : récap PDF périodique (3/3). Pas d'ADR. |
| **DEC-159** Récap PDF périodique B2B (extension donneurs d'ordres 3/3, dernière) | Phase 07.04 2026-06-12 | LOCKED | 3e et dernière extension B2B (CdC §5.5 l.195 « récapitulatif périodique automatique PDF pour la facturation » + l.186 « facturation centralisée »). Boucle la chaîne B2B : grille tarifaire (07.02) → demande groupée (07.03) → récap PDF [ce lot]. **D-01 (route `api/admin/donneurs-ordres/recap/pdf/route.tsx`)** calquée sur le recap chauffeur (DEC-116) : params `ordering_party`/`from`/`to` (validés AAAA-MM-JJ, from≤to), runtime nodejs, auth + guard rôle **dirigeant OU régulateur** (403 sinon), requête `rides` filtrée `.eq('ordering_party_id').eq('archive',false).gte/lte('scheduled_at')` (RLS scope l'org) + patients en `.in()` sur `patients_safe` (vue sans ciphertext, RGPD ; pas de N+1), `renderToStream(<RecapDonneurPdf/>)`, headers de téléchargement, audit log `ordering_party.recap.exported_pdf` AVANT le rendu. **Isolation** : le donneur est lu via le client RLS-scopé (`ordering_parties_select_same_org`) → cross-org introuvable → 404. **D-02 (composant `RecapDonneurPdf`)** calqué sur `RecapChauffeurPdf` : réutilise `PdfDocument`/`PdfSection`/`pdfStyles` (charte commune, AUCUNE charte divergente) ; en-tête raison sociale + SIRET, résumé (nb courses / dont terminées / total €), tableau date/patient/trajet/statut/tarif. Montant = `tarif_amount_eur` réellement stocké (reflète déjà la grille B2B 07.02 si `grille_propre` — **pas de recalcul**). **D-03 (UI)** : bouton « Récap PDF » dans la fiche donneur (édition uniquement, besoin de l'id), Dialog de période pré-remplie selon `modalite_facturation` (hebdomadaire → lundi de la semaine en cours ; mensuelle / a_la_course → 1er du mois en cours) → today ; ouvre la route dans un nouvel onglet (pattern recap chauffeur). **D-04 (« automatique » CdC l.195)** : livré À LA DEMANDE (bouton) ; la génération planifiée (cron hebdo/mensuel + envoi) dépend de l'email (`EMAIL_ENABLED` OFF) → TRACÉE comme lot futur (registre §6.3), PAS construite ici. Le récap à la demande couvre le besoin de facturation immédiat. **Garde-fous** : charte PDF commune réutilisée ; pas de N+1 ; accès dirigeant/régulateur + isolation org ; tarif non recalculé ; `patients_safe`. typecheck+lint(0 err, 8 warn)+build verts. **0 migration, 0 dépendance** (réutilise `@react-pdf/renderer`). **Chaîne d'extensions B2B COMPLÈTE** (cœur 07.01 + grille 07.02 + groupée 07.03 + récap 07.04). Pas d'ADR. |
| **DEC-160** Replanification dynamique — cœur (panne/indispo + réaffectation scorée validable) | Phase 10.01 2026-06-12 | LOCKED | Item V1.5 (CdG §11.3 l.903, §5.14 l.363-374) : quand un chauffeur tombe en panne ou devient indisponible, la régulation réaffecte ses courses restantes sans tout refaire à la main. **Périmètre cœur** (décision dirigeant) : signalement panne/indispo + proposition de réaffectation **validable manuellement**, score **Haversine V1 SANS géoloc HDS** (GEOLOC OFF). **D-01 (modèle, migration `20260612000003`)** : table `driver_incidents` (enum `driver_incident_type` panne_vehicule|indisponible, FK org + driver, `nature`/`lieu` text, `started_at`, `resolved_at` NULL = ouvert, RLS org FORCÉE : select same_org / insert régulateur+dirigeant OU chauffeur pour SON driver / update régulateur+dirigeant (résolution) / pas de DELETE ; index partiel ouverts ; pgTAP 12). Statut opérationnel TEMPORAIRE distinct de `drivers.actif` (administratif permanent — jamais modifié). **D-02 (signalement)** : panne côté chauffeur PWA (`reportBreakdownAction`, bouton ≥56px sur /conduite → nature+lieu) ; indisponibilité côté régulateur (`declareIncidentAction` sur /replanification). **D-03 (cœur — proposition scorée, module PUR `lib/replanning/reassign.ts` + Vitest 5)** : pour chaque course restante (statut validee/assignee, future — JAMAIS en_cours/terminee), candidats = chauffeurs actifs sans incident ouvert, compatibles (réutilise `isCompatible` @tap/shared DEC-038 via map mode→type véhicule, AUCUNE divergence). **Score Haversine** = distance pickup course ↔ point de référence candidat (sa course la plus proche dans le TEMPS ; à défaut dépôt org ; à défaut distance inconnue) + pénalité de charge (anti-surcharge, +5 km/course affectée). Meilleur candidat par course, triable. **D-04 (validation)** : page régulateur `/replanification` — par incident, liste des courses + proposition (Select pré-réglé sur le meilleur, modifiable), boutons « Réaffecter les courses » (lot, `reassignRidesBatchAction` : driver_id→nouveau, vehicle_id→null pour re-sélection, statut assignee, row-count check DEC-041, courses en_cours/terminee jamais touchées) + « Marquer résolu ». **D-05 (débordement, CdG l.373)** : course sans candidat compatible → badge « Non réaffectable » (jamais d'auto-annulation). **Alerte régulateur** : incidents ouverts remontés au cockpit comme alertes `driver_incident` (alimentation au chargement ; poussée temps réel = lot suivant). Nav régulateur : entrée Replanification. **Garde-fous** : pas de géoloc live ; `actif` ≠ incident ; RLS par org ; réutilise `isCompatible` ; courses en cours intouchables. **Hors scope (registre)** : SMS patients impactés (dépend flux SMS), réaffectation 100% auto, suivi atelier, recalcul ETA embouteillage, poussée realtime incidents. La précision du score montera avec la géoloc HDS. typecheck+lint(0 err, 8 warn)+build verts ; Vitest 5 ; pgTAP 12. 1 migration, 0 dépendance. Pas d'ADR. |
| **DEC-161** SMS patients impactés lors d'une réaffectation (complément replanification) | Phase 10.02 2026-06-12 | LOCKED | Complément direct du cœur 10.01 (CdG §5.14 l.368 : SMS automatiques aux patients impactés lors d'une réaffectation panne/indispo). Ferme le scénario panne complet (signalement → réaffectation → notification). **D-01 (template, migration `20260612000004`)** : seed additif idempotent (`on conflict do nothing`) `sms_templates` key `reaffectation`, body Mustache (`{{patient_prenom}}`/`{{date}}`/`{{heure}}`, ≤160 car), éditable via le module sms-templates (dirigeant). **D-02 (branchement dans `reassignRidesBatchAction`)** : APRÈS la réaffectation committée, pour les courses dont le chauffeur a EFFECTIVEMENT changé → helper best-effort `notifyReassignedPatients(rideIds, orgId)` (`_lib/notify-reaffectation.ts`, server-only). Réutilise le socle SMS 09.03 (DEC-156) SANS réécrire de règle : `getActiveSmsConsentMap` (consentements en 1 requête, anti-N+1) + `isConsentValid` (règle RGPD unique DEC-008) + `renderTemplate` + `sendSms` + trace `sms_messages` (queued / skipped_consent_revoked / failed) ; envois parallélisés par lots de 10. **Client service-role** (comme les crons) car `sms_messages` n'a pas de policy INSERT pour authenticated (SELECT only) → toutes les requêtes filtrées explicitement `organization_id` (defense in depth). **D-03 (best-effort)** : l'envoi SMS ne fait JAMAIS échouer ni rollback la réaffectation (déjà persistée) — toute erreur loggée, jamais propagée (try/catch global + per-envoi). **D-04 (ETA)** : pas d'ETA inventé sans géoloc HDS → on garde l'horaire programmé (`{{heure}}`, « horaire maintenu »). La précision ETA montera avec la géoloc HDS. **Idempotence** : SMS envoyé uniquement si `driver_id` a réellement changé (rejouer l'action avec le même chauffeur → 0 changement → 0 SMS en double). **Garde-fous** : consentement RGPD réutilisé (jamais réécrit) ; traçabilité `sms_messages` intégrale (sent/skipped/failed) ; best-effort ; pas de N+1 ; 0 dépendance. typecheck+lint(0 err, 8 warn)+build verts. 1 migration (template seed), 0 dépendance. Ferme registre §8.1. Pas d'ADR. |
| **DEC-162** Référentiel prescripteurs (CdG §5.4, préalable des prescriptions) | Phase 07.05 2026-06-12 | LOCKED | Module V1 manquant (CdG §5.4 [NOUVEAU], Inclus V1 l.63) : référentiel des médecins/établissements à l'origine des prescriptions. PRÉALABLE de la gestion des prescriptions (§5.3, lot suivant : une prescription sera rattachée à un prescripteur). **3 entités DISTINCTES** : prescripteur (qui PRESCRIT) ≠ donneur d'ordres (qui COMMANDE/paie, §5.5) ≠ patient (transporté) — NOUVELLE table `prescribers`, AUCUNE fusion avec `ordering_parties`. **D-01 (migration `20260612000005`)** : enum `prescriber_type` (medecin|etablissement) + table `prescribers` (nom NOT NULL, prenom null, type, rpps/adeli/finess null, specialite, contact_telephone/email, adresse, actif/archive, created_by) calquée sur ordering_parties ; RLS forcée par org : SELECT same_org / INSERT+UPDATE **dirigeant OU régulateur** (le régulateur saisit les prescriptions et peut ajouter un prescripteur au vol) / pas de DELETE (archivage logique) ; index `(org, actif)` partiel archive=false ; trigger updated_at + audit (`prescriber.*`) ; pgTAP 11. Validations format `@tap/shared` (créées sur le modèle de siretSchema) : RPPS 11 chiffres, ADELI/FINESS 9 chiffres — format seul (pas de Luhn), tous optionnels (nullable BDD). **D-02 (front `(admin)/admin/prescripteurs/`)** répliquant `donneurs-ordres/` : page (guard `requireAdminOrRegulateurPage`) + actions CRUD/archivage (`requireAdminOrRegulateur`, DEC-041 row count) + `_lib/cached-queries.ts` (data-cache par org `prescribers:${orgId}`, service-role + filtre `.eq('organization_id')` explicite, `revalidateTag` à l'écriture — même garde-fou sécurité 08.03/08.04) + `_components` (liste DataTable + form drawer, identifiants conditionnels au type : RPPS/ADELI médecin, FINESS établissement). Nav : entrée « Prescripteurs » dans le menu Gestion (à côté de donneurs-ordres). **D-03 (hors lot)** : volumétrie / top prescripteurs / patients suivis (CdG l.181-184) AGRÈGENT les prescriptions → dépendent de §5.3 (lot suivant), NON construits ici. **Garde-fous** : 3 entités distinctes ; RLS par org ; data-cache filtré org (pas de fuite inter-org) ; pas de lien course/prescription ici (c'est §5.3) ; pas de volumétrie ici. typecheck+lint(0 err, 8 warn)+build verts ; pgTAP 11. 1 migration, 0 dépendance. **Suite (registre §5.3)** : gestion des prescriptions. Pas d'ADR. |
| **DEC-163** Prescriptions / bons de transport — logique compteur + alertes (SANS scan) | Phase 07.06 2026-06-12 | LOCKED | Module V1 à fort enjeu remboursement (CdG §5.3) : toute course conventionnée justifiée par une prescription consommée course après course. Construit TOUTE la logique (saisie structurée + compteur idempotent + alertes) **SANS le scan/upload du bon** (décision dirigeant : scan + archivage checksum SCOR dépendent du bucket HDS → Phase 09). **D-01 (migration `20260612000006`)** : enum `prescription_status` (active|epuisee|expiree) + table `prescriptions` (patient_id NOT NULL, prescriber_id null FK 07.05, numero, date_prescription, finess/motif/type_transport, `trajets_autorises` check>0, `trajets_consommes` default 0, date_expiration, statut, `document_url` PRÉVU scan **laissé NULL**) ; RLS forcée org SELECT same_org / INSERT+UPDATE dirigeant+régulateur / pas de DELETE ; trigger updated_at + audit ; pgTAP 14 (RLS + compteur). **D-02** colonne **`rides.prescription_id` NULLABLE** (`on delete set null`) — course non conventionnée OK ; threadée dans `rideExpressInputSchema` + `createRideAction`/`updateRideAction` (spread `...input`) + prefill (RIDE_COLUMNS + RideRow). **D-03 (compteur — choix archi : TRIGGER Postgres `rides_prescription_counter` SECURITY DEFINER, idempotent)** : delta basé sur la transition de l'état CONSOMMATEUR d'une course (consomme = statut ni brouillon ni annulée). +1 quand une course devient consommatrice, -1 à l'annulation/réaffectation, recalcul `statut` (epuisee si consommes≥autorises, expiree si date<today). Rejouer une transition identique → 0 delta (idempotent, prouvé pgTAP). Trigger = source unique évitant de threader le compteur dans create/assignment/clôture/cancel. **D-04 (alertes, pur `derivePrescriptionAlerts` @tap/shared + Vitest 6)** : seuil 80 % / épuisée / expirée / renouvellement (patient récurrent + <15 j d'échéance), réutilise l'esprit de `selectComplianceAlerts` ; surfacées sur le cockpit (`PrescriptionAlertsPanel` dans l'aside, dérivé serveur `getPrescriptionAlerts`, dégradation gracieuse) + badges par bon sur la fiche patient. **D-05 (saisie + UI)** : section Prescriptions sur la fiche patient (`(app)/patients/[id]`, liste + form de saisie structurée, actions `listPrescriptionsForPatientAction`/`createPrescriptionAction` guard dirigeant+régulateur) + picker « Prescription » dans la saisie express (`PrescriptionPickerField`, prescriptions du patient sélectionné, **avertissement non bloquant** si bon épuisé/expiré — CdG « alerte avant saisie »). **D-06** `document_url` NULL, aucun appel Storage (scan = Phase 09). **Garde-fous** : pas de scan/upload ; `prescription_id` nullable (lien obligatoire géré par avertissement, pas contrainte BDD) ; compteur idempotent (donnée de remboursement) ; 3 entités distinctes ; RLS org ; RGPD `patients_safe` à l'affichage. typecheck+lint(0 err, 8 warn)+build verts ; Vitest 6 ; pgTAP 14. 1 migration, 0 dépendance. **Suite (registre §9.2/§5.20)** : volumétrie/top prescripteurs + KPIs conformité bons ; scan/archivage SCOR = Phase 09 HDS. Pas d'ADR. |
| **DEC-164** KPIs prescriptions (top prescripteurs + bons par statut) | Phase 07.07 2026-06-12 | LOCKED | Rend la chaîne prescriptions (07.05+07.06) exploitable pour le pilotage dirigeant : CdG §5.4/§5.20 volumétrie + Top 5 prescripteurs + bons en attente/expirés/épuisés. Agrégation PURE LECTURE (résout registre §9.2/§9.3). **D-01 (bloc `DashboardPrescriptions`)** ajouté à `DashboardData` (`tableau-de-bord/_lib/queries-dashboard.ts`, modèle `DashboardConformite`) : `bonsActifs` / `bonsProchesSeuil` (actifs ≥80 % consommation, aligné alerte 80 % 07.06) / `bonsEpuises` / `bonsExpires` + `topPrescripteurs[]` (Top 5 par nombre de prescriptions). **D-02 (efficace, anti-N+1)** : `getPrescriptions(supabase)` fait 1 select des prescriptions (statut, prescriber_id, trajets) → comptes + top calculés en mémoire, puis libellés des 5 en 1 `.in()` sur `prescribers`. Ajouté au `Promise.all` existant du dashboard (parallélisé, 0 cascade ajoutée). Fallback gracieux. **D-03 (card `prescriptions-card.tsx`)** modèle `compliance-card` : section bons par statut (épuisés+expirés = mise en évidence destructive, proches seuil = ambre) + section Top prescripteurs (5 avec nom + nb). Insérée en rangée dédiée « Prescriptions » du tableau de bord. **D-04** drill-down non construit (pas de page liste prescriptions filtrée par statut — les bons vivent sur la fiche patient ; non trivial, optionnel). **Garde-fous** : agrégation seulement (ne touche NI trigger NI statut 07.06) ; pas de N+1 ; RLS org (un dirigeant ne voit que son org) ; RGPD (top = prescripteurs, aucune donnée santé patient) ; périmètre limité (PAS les autres KPIs 5.20). typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance. **Suite (registre §9.4)** : KPIs 5.20 restants (top patients CA, top donneurs B2B, taux occupation véhicule, marge/coût km, encours impayés, litiges CGSS). Pas d'ADR. |
| **DEC-165** Tops commerciaux dirigeant (Top 10 patients CA + Top 5 donneurs B2B) | Phase 07.08 2026-06-12 | LOCKED | Complète les KPIs commerciaux du dirigeant (CdG §5.20 l.502/504), même pattern que le Top prescripteurs (07.07). **D-01 (bloc `DashboardCommercial`)** ajouté à `DashboardData` : `topPatients[]` (Top 10 : patient_id, label, ca_eur, count) + `topDonneurs[]` (Top 5 : ordering_party_id, label, ca_eur, count — uniquement courses avec `ordering_party_id`). **D-02 (1 requête + 2 `.in()`, anti-N+1)** : `getCommercialTops(supabase, moisStart, moisEnd)` fait 1 select courses → agrégation EN MÉMOIRE (somme `tarif_amount_eur` + count par patient_id → top 10 ; par ordering_party_id non null → top 5) → libellés en 2 `.in()` (`patients_safe` RGPD pour les 10, `ordering_parties` pour les 5). Ajouté au `Promise.all` existant du dashboard (parallélisé). Fallback gracieux. **D-03 (cohérence CA — choix)** : CA = MÊME définition que `getCaMois` (status='terminee' + payment_status='encaisse', bornes `ended_at`) → les tops PARTITIONNENT exactement le CA mensuel (somme des tops = `caMois.total_eur`, chaque top ≤ caMois), garantissant le garde-fou de cohérence. **D-04 (card `commercial-tops-card.tsx`)** : section Top patients (10) + Top donneurs (5), montants `Intl` €, états vides honnêtes (pas de ligne fantôme) ; insérée en rangée « Prescriptions & tops commerciaux » (2 colonnes avec la card prescriptions). **Garde-fous** : RGPD `patients_safe` UNIQUEMENT pour les noms patients ; cohérence CA (réutilise la définition `getCaMois`) ; pas de N+1 ; RLS org ; courses sans donneur exclues du top donneurs (comptent dans top patients) ; agrégation seulement (0 mutation). typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance. **Suite (registre §9.4)** : KPIs 5.20 restants (panier moyen, écart prévu/réalisé, récurrentes/ponctuelles ; KPIs économiques marge/coût km paramétrables ; occupation/productivité). Pas d'ADR. |
| **DEC-166** KPI panier moyen par course (dashboard dirigeant) | Phase 07.09 2026-06-12 | LOCKED | KPI commercial CdG §5.20 l.501 « panier moyen par course ». Trivial — DÉRIVÉ de deux valeurs déjà présentes dans `DashboardData`, **aucune nouvelle requête**. **D-01** `panierMoyen = caMois.count > 0 ? caMois.total_eur / caMois.count : 0` calculé dans le Server Component `tableau-de-bord/page.tsx`. **D-02 (cohérence du périmètre — le seul piège)** : numérateur ET dénominateur portent sur le MÊME ensemble = `caMois` (CA encaissé : status='terminee' + payment_status='encaisse'). `caMois.count` (déjà calculé par `getCaMois`) = nombre de courses ENCAISSÉES → panier = CA encaissé ÷ courses encaissées, pas CA encaissé ÷ toutes les courses (qui mentirait). Aucun count séparé à dériver. **D-03** KpiCard « Panier moyen / course » (`eur.format`) ajoutée à la rangée « Activité du mois » près des CA (grid 6→7 cols), contexte « sur N courses encaissées ». Division par zéro gérée (0 course → 0 €, pas NaN). typecheck+lint(0 err, 8 warn)+build verts. 0 migration, 0 dépendance, 0 requête. **Suite (registre §9.4)** : écart prévisionnel/réalisé (préalable : CA prévisionnel = tarification des courses futures) + répartition récurrentes/ponctuelles (préalable : activer `rides.ride_recurrence_id`). Pas d'ADR. |
| **DEC-167** Notifications push PWA chauffeur (trou V1, sans HDS) | Phase 06.69 2026-06-12 | LOCKED | Trou V1 (CdG l.71 « PWA avec notifications push ») — infra Web Push standard, AUCUNE dépendance HDS/géoloc ; les clés VAPID sont des SECRETS à générer (pas un provider/achat). **D-01 (migration `20260612000007`)** : table `push_subscriptions` (org, user_id, endpoint unique, p256dh, auth, user_agent, last_used_at) RLS user-scoped (un user ne gère QUE ses abonnements, multi-appareils) + colonne `notification_preferences.push_enabled` (défaut true) ; pgTAP 9. **D-02 (VAPID)** : `NEXT_PUBLIC_VAPID_PUBLIC_KEY` (exposée client) / `VAPID_PRIVATE_KEY` (SECRET serveur) / `VAPID_SUBJECT` ; turbo.json globalEnv + .env.example (génération `npx web-push generate-vapid-keys`, secrets Vercel). **D-03 (abonnement client)** : `EnablePushButton` sur /conduite — permission demandée AU CLIC (jamais à froid), `pushManager.subscribe` → `savePushSubscriptionAction` (upsert RLS own) ; désabonnement = `unsubscribe()` + `deletePushSubscriptionAction`. **D-04 (SW)** : listeners `push` + `notificationclick` AJOUTÉS au SW Serwist existant (precache/offline INCHANGÉS) — affiche la notif + ouvre/focus l'app sur la course. **D-05 (envoi serveur `lib/push/send.ts`)** : `web-push` (1 dépendance, lib standard) ; `sendPushToUser`/`sendPushToDriver` (résout drivers.profile_id) ; client service-role filtré user+org (push_subscriptions RLS user-scoped) ; respecte `push_enabled` ; nettoie les abonnements 404/410 (Gone). Sans clés VAPID → no-op gracieux. **D-06 (déclenchement best-effort)** : `assignRideAction` (course affectée → « Nouvelle course à HH:MM ») + `reassignRidesBatchAction` (réaffectation → push groupé par nouveau chauffeur). Un échec push ne rollback/n'échoue JAMAIS l'action métier (pattern 10.02). **Garde-fous** : `VAPID_PRIVATE_KEY` jamais exposée ; permission au bon moment ; RLS own + scope org ; nettoyage subs morts ; respect push_enabled ; SW offline non régressé ; best-effort. typecheck+lint(0 err)+build verts (SW bundlé) ; pgTAP 9. 1 migration, 1 dépendance (web-push). Comble registre §1.3. Pas d'ADR. |
| **DEC-168** Anti-TOCTOU sur les transitions de statut de course (optimistic locking) | Phase 09.04 (fix concurrence) 2026-06-12 | LOCKED | Trou de conception (sources Doyensec/EnterpriseDB/sqlfordevs) : l'affectation lisait `status` (`select`), vérifiait `validee`, PUIS écrivait `assignee` — read-then-write NON atomique. Entre SELECT et UPDATE, un autre régulateur peut affecter la même course → les 2 passent la vérif, le 2nd écrase silencieusement (TOCTOU / lost update). Le row count check DEC-041 (`.eq('id')`) n'attrape PAS ce cas (la ligne existe → UPDATE réussit). **Solution (compare-and-set / optimistic locking, conflits rares → pas de verrou pessimiste)** : inclure le statut attendu dans le WHERE de l'UPDATE → si le statut a bougé, 0 ligne → le row count check existant déclenche le refus. **D-01/D-02 (WHERE durcis)** : `assignRideAction` `.eq('status','validee')` ; `unassignRideAction` `.eq('status','assignee')` ; `startRideAction` (chauffeur) `.eq('status','assignee')` ; `endRideAction` `.eq('status','en_cours')`. **Déjà protégés (inchangés)** : `cancelRideAction` (`.in('status',['validee','assignee'])`), `reassignRidesBatchAction` (`.in('status',['validee','assignee'])` + idempotence driver-change préservée). **Non protégés volontairement (D-02, pas de sur-protection)** : `assignVehicleAction` / `override` / `payment` / `edit` = mises à jour de CHAMPS (vehicle, tarif, payment_status, notes), pas de transition du state machine `status` → pas de corruption d'état concurrente. **D-03 (message conflit explicite)** : 0 ligne sur statut changé → « Cette course vient d'être affectée par un autre régulateur. Actualisez. » (distinct de « droits insuffisants »), invitant à recharger. **D-04 (idempotence préservée)** : réaffectation inchangée. **Garde-fous** : pas de `FOR UPDATE`/transaction lourde ; règles métier des transitions inchangées (on AJOUTE la condition de statut au WHERE) ; row count check DEC-041 réutilisé. typecheck+lint(0 err)+build verts. 0 migration, 0 dépendance. Pas d'ADR. |

</decisions>

### Réconciliation registre — entrées concises (2026-06-08, DEC-119)

Les DEC ci-dessous étaient référencés dans ROADMAP / journal / STATE mais absents de ce
registre (gaps historiques). Ajoutés ici en 1 ligne ; détail complet en ROADMAP « Livré »
et dans `.planning/journal.md`.

| Décision | Phase | Note (1 ligne — détail en ROADMAP/journal) |
|---|---|---|
| **DEC-082** | 06.7 | Pré-filtrage des fenêtres temporelles remplace `AddDisjunction` OR-Tools (workaround `bad_alloc`) ; comportement observable identique. |
| **DEC-083** | 06.7 | `CONTRACT_VERSION='1'` figé (SolveRequest/Response) ; parité contrat TS ↔ Python. |
| **DEC-084** | 06.11 | Phase « Polish produit » cadrée (tableau dirigeant HVI 2026 + passe UX optimisation + finitions). |
| **DEC-085** | 06.11→06.12 | Réordonnancement/renumérotation des phases bêta ; réactivation du vrai solveur reportée (06.12 candidate, ex-06.11). |
| **DEC-088** | 06.13 | Doctrine accessibilité du design system (WCAG 2.1 AA + RGAA 4.1.2 + conventions visuelles). |
| **DEC-089** | 06.13 | Étoile polaire visuelle hybride Carbon + Atlassian + NHS. |
| **DEC-090** | 06.13 | Phase foundations design system 100 % documentaire (1 PR docs pure). |
| **DEC-091** | 06.13 | Chantier PDF reporté. |
| **DEC-092** | 2026-06-04 | Mobile natif (Phase 07) abandonné (la PWA 04.9 couvre le terrain ; coût natif injustifié). Réversible. |
| **DEC-093** | 06.12 | Solveur réimplémenté en heuristique TS native (cluster-first / route-second) ; OR-Tools/Python/mock abandonnés. ADR-010 supersede ADR-008+009. |
| **DEC-094** | 06.19 | Géocodage branché sur récurrences + filet serveur BAN idempotent non bloquant ; alimente `solveLocal`. 0 migration. |
| **DEC-095** | 06.9 | Next.js 14.2 → 15.5 + migration async complète des Request APIs ; `typedRoutes` ; React 19 différé. ADR-011. |
| **DEC-096** | 10.0 | Prototype géoloc sur données fictives : capture événementielle aux pointages, cockpit « dernière position connue + âge », flag `GEOLOC_ENABLED` OFF pré-HDS. ADR-012. |
| **DEC-097** | 06.20 | Observabilité Sentry (3 runtimes) ; zéro PII santé (scrub `beforeSend`) ; Session Replay OFF. |
| **DEC-098** | 06.21 | Couverture tests RLS étendue 13 → 24 tables (11 fichiers pgTAP) ; 0 policy modifiée. |
| **DEC-099** | 06.22 | Error boundaries par segment (gabarit `<SegmentError>`, capture Sentry, stack dev-only). |
| **DEC-100** | 06.23 | Audit DEC-041 complet (11 trous comblés) + tests métier ciblés ; clôt la dette DEC-041. |
| **DEC-102** | 06.24 | Incarnation DEC-101 Régulation lot 1 : `PageHeader` unifié + hiérarchie typographique par taille. |
| **DEC-103** | 06.24 | Activation/complétude de pattern (DEC-101 §5bis levier 2) ; aucun choix structurel nouveau. |
| **DEC-104** | 06.25 | Incarnation lot 2 : terracotta sur 5 CTA moments-clés ; token accent corrigé `hsl(14 78% 46%)` (WCAG AA). |
| **DEC-105** | 06.26 | Incarnation lot 3 : `loading.tsx` (skeletons épousant le layout) sur les écrans à fetch ; perception de vitesse. |
| **DEC-106** | 06.27 | Incarnation lot 4 : refactor structurel `ride-fields` (≤ 175 l.) + cohérence modales (Dialog vs Sheet). |
| **DEC-108** | 06.28 | Incarnation Chauffeur terrain : terracotta « Démarrer », mode contraste élevé (`useHighContrast` + `prefers-contrast`). |
| **DEC-109** | 06.29 | Typographie FR : ~50 cadratins en prose UI remplacés par la ponctuation idiomatique ; 20 séparateurs-vide préservés. |
| **DEC-110** | 06.30 | Incarnation Admin (16 écrans) : terracotta 13 moments-clés ; `EmptyState` variant accent ; 6 `loading.tsx`. |
| **DEC-110bis** | 06.31 | `<SegmentedControl>` / `<SegmentedNav>` factorisés (remplacent le toggle Actifs/Archivés dupliqué × 3). |
| **DEC-111** | 06.32 | Incarnation Auth + Public (lot léger) : terracotta 3 moments-clés ; prose RGPD patient promue `text-base`. |

---
*Last updated: 2026-06-12 — DEC-168 LOCKED (anti-TOCTOU transitions de statut de course : UPDATE conditionnel `.eq('status', <attendu>)` + row count check = optimistic locking ; assign/unassign/start/end durcis, cancel/reassign déjà protégés ; corrige l'écrasement silencieux d'une affectation concurrente ; message conflit explicite). Registre complet jusqu'à DEC-168.*
