# SaaS TAP Réunion

## What This Is

SaaS de régulation, optimisation, communication patient et pilotage pour
sociétés de Transport Assis Professionnalisé (TAP) et taxiteurs conventionnés
CGSS à La Réunion (974). L'outil sert au quotidien la régulatrice (utilisatrice
principale, 8 h/jour dans l'écran), les chauffeurs en tournée (PWA mobile), les
dirigeants (pilotage) et — en V1.5 — les donneurs d'ordres B2B (hôpitaux,
cliniques, EHPAD).

## Core Value

**La régulatrice doit avoir envie d'utiliser l'outil 8 heures par jour, 220 jours
par an, sans jamais le subir.** Tout le reste — moteur tarifaire, optimisation
de tournées, communication patient — sert cet objectif. Si la régulatrice
préfère son cahier papier, le produit a échoué.

## Requirements

### Validated

<!-- Shipped et validés. -->

- ✓ **FOND-01** : Monorepo Turborepo + pnpm workspaces opérationnel — Lot 0
- ✓ **FOND-02** : Multi-tenant Supabase via RLS forcée + `organization_id` — Lot 0
- ✓ **FOND-03** : Migrations Supabase 001 (foundations) et 002 (RLS) appliquées — Lot 0
- ✓ **FOND-04** : Tests pgTAP RLS automatisés en CI — Lot 0
- ✓ **FOND-05** : Packages `database` et `shared` initialisés — Lot 0
- ✓ **FOND-06** : ADR-001 (monorepo) et ADR-002 (RLS multi-tenant) actés — Lot 0
- ✓ **FOND-07** : CI/CD GitHub Actions (lint, types, tests, migrations) — Lot 0
- ✓ **FOND-08** : Comptes de démo seedés (dirigeant, régulateur, chauffeur) — Lot 0

### Active

<!-- Scope en cours. Référentiel détaillé dans REQUIREMENTS.md. -->

- [x] **REQ-patient-referentiel** : fiche patient complète avec NIR chiffré et préférences (Phase 1 livrée — 2026-05-06)
- [ ] **REQ-rgpd-compliance** : registre traitements + DPA + DPIA + portail droits patient *(Phase 1.5 — bloque first design partner)*
- [ ] **REQ-visible-progress** : Vercel preview + Supabase staging + seed démo 974 + comptes démo + showcase/ *(Phase 0.7 — Visible Progress Mandate CLAUDE.md § 13.5, débloque toute phase ≥ 2)*
- [ ] **REQ-saisie-express-course** : saisie d'une course en mode express < 30 s
- [ ] **REQ-moteur-tarification-cgss** : moteur tarifaire CGSS dans `packages/pricing`
- [ ] **REQ-courses-recurrentes** : génération de récurrences avec exceptions jours fériés 974
- [ ] **REQ-osrm-bootstrap** : `services/osrm` Docker + tuiles 974 + RPC distance/eta *(Phase 4.5 — débloque downstream)*
- [ ] **REQ-cockpit-regulateur** : cockpit temps réel par défaut, TTI < 2 s
- [ ] **REQ-planning-gantt** : planning drag-and-drop par chauffeur et par jour
- [ ] **REQ-gestion-imprevus** : workflows panne, patient absent, réaffectation
- [ ] **REQ-communication-sms-patient** : SMS sortants avec consentement actif
- [ ] **REQ-pwa-chauffeur** : PWA chauffeur terrain (hors-ligne, vocal, mode soleil)
- [ ] **REQ-geolocalisation-temps-reel** : capture position service uniquement + streaming cockpit + rétention 90j *(Phase 9.5)*
- [ ] **REQ-optimisation-tournees** : microservice Python OR-Tools + client TS
- [ ] **REQ-routing-gps-advanced** : geocoding inverse, alternatives, isochrones (Phase 11 — bootstrap déjà en Phase 4.5)
- [ ] **REQ-caisse-paiements-directs** : encaissements cash / CB / chèque rapprochés
- [ ] **REQ-mode-degrade** : continuité de service en panne réseau / Supabase / tiers
- [ ] **REQ-kpis-dirigeant** : CA, marge, mutualisation, productivité chauffeur *(Phase 14)*
- [ ] **REQ-conformite-reglementaire** : alertes 90/60/30j carte pro, CT, visite médicale, agrément ARS/CPAM *(Phase 15 — bloque mise en production commerciale)*
- [ ] **REQ-exports-comptables** : FEC annuel DGFiP, Lomaco CSV mensuel, PDF récap mensuel *(Phase 16)*
- [ ] **REQ-beta-terrain-chauffeur** : validation Hauts Réunion 35°C/3G dégradé/2-3 design partners *(Phase 17 — V1.5)*

### Out of Scope (v1)

<!-- Frontières explicites. -->

- **Application native iOS/Android** — la PWA chauffeur couvre les besoins terrain ; native repoussée si justifié
- **15 modules secondaires du CDC v2** non listés ici — à ingérer comme PRD une fois le `.docx` converti en `.md`
- **Portail B2B (apps/b2b)** — V1.5, après validation V1 par 2-3 design partners
- **Multi-langue** — français uniquement (cible 974, pas d'export hors France à court terme)
- **Application native chauffeur** — PWA suffit ; remplacement uniquement si limites techniques bloquantes
- **Analyse vidéo / dashcam** — hors scope produit
- **Facturation B2C complète** — la caisse couvre les paiements directs, pas un module compta
- **Outils marketing intégrés** — focus 100 % opérationnel, marketing en outils tiers

## Context

### Méthode produit
Le pilier 1 (UX qui donne envie) impose l'observation terrain. Toute journée
passée auprès d'une régulatrice, run en double avec un chauffeur, ou point avec
un dirigeant est consignée dans `docs/observations/AAAA-MM-JJ-societe-role.md`.
Anonymisation systématique (pas de NIR, pas de nom patient).

### État technique
- Lot 0 (fondations multi-tenant) **terminé** — commit `f68b1d2`.
- Lot 1 **en cours** : référentiels patients + saisie express.
- Branche active : `claude/setup-project-docs-ATeRo`.
- Repo : `thomasvinh-san-byte/tap`.
- Stack : Next.js 14 App Router, Supabase, OR-Tools, OSRM, MapLibre — figée (cf. décisions verrouillées).

### Document métier de référence
`docs/cahier_des_charges_saas_tap_v2.docx` — 24 modules fonctionnels. **9 modules
critiques** identifiés et adressés en V1 ; 15 modules secondaires à ingérer
ultérieurement (conversion `.docx` → `.md` requise pour parsing).

## Constraints

- **Tech stack** : Next.js 14 App Router, TypeScript strict, Tailwind + shadcn/ui, Supabase, MapLibre, FullCalendar/react-big-calendar, Python OR-Tools, OSRM auto-hébergé, Twilio ou OVH SMS Pro, Vercel, Sentry — **figée**, toute nouvelle dépendance majeure exige un ADR (cf. DEC-003).
- **Sécurité données de santé** : RGPD niveau santé + HDS pour la production commerciale. Supabase Cloud non HDS — bêta privée acceptable sous DPA, migration OVHcloud Postgres / Scaleway HDS anticipée. Architecture portable obligatoire (cf. CON-001).
- **Multi-tenant** : `organization_id` sur toute table métier + RLS forcée + tests pgTAP systématiques. `service_role` interdit côté client (audit CI bloquant) (cf. DEC-002).
- **Chiffrement applicatif** : NIR et notes médicales en AES-256-GCM, clé hors Supabase. TLS 1.3 minimum (cf. DEC-007).
- **Performance perçue** : saisie express < 30 s, feedback < 100 ms, action chauffeur < 1 s en 3G, TTI régulateur < 2 s (cf. DEC-005).
- **Accessibilité** : WCAG 2.1 AA, contraste 4.5:1, navigation clavier complète, `prefers-reduced-motion` respecté, mode contraste élevé + police +20 % côté chauffeur (cf. CON-005).
- **Design system** : spacing 4-8-12-16-24-32-48-64 px, polices Inter/Manrope/Geist Sans uniquement, icônes Lucide uniquement, mode jour ET nuit à parité, chiffres tabulaires obligatoires (cf. CON-006).
- **Localisation FR** : UI, erreurs, logs, commentaires, commits — **français uniquement**. Pas de jargon technique en UI (cf. DEC-011).
- **Limites code** : fichier ≤ 300 lignes, composant React ≤ 150 lignes, fonction ≤ 50 lignes, ≤ 3 niveaux d'imbrication (cf. CON-008).
- **Couverture tests** : `packages/pricing` et `packages/recurrence` à **100 % branches**, `packages/domain` ≥ 80 %, RLS tests pgTAP systématiques, E2E Playwright sur workflows imprévus (cf. DEC-013).
- **Architecture monorepo** : `apps/*` peuvent dépendre de `packages/*`, **jamais l'inverse** (cf. CON-012).
- **Sessions** : chauffeur 8 h max, régulateur 15 min inactivité, mode régulateur de garde mono-actif (cf. CON-014).
- **Géolocalisation** : capture uniquement pendant le service, rétention 90 j max, agrégation puis purge (cf. DEC-009).

## Key Decisions

<!-- Toutes verrouillées. ADRs sont sources d'autorité formelle ; les décisions DOC sont
élevées par autorité explicite du propriétaire projet sur CLAUDE.md. -->

<decisions locked="true">

| Décision | Source | Statut | Note |
|----------|--------|--------|------|
| **DEC-001** Monorepo Turborepo + pnpm workspaces | ADR-001 | LOCKED | `apps/*` ne dépendent QUE de `packages/*` |
| **DEC-002** Multi-tenant via RLS forcée + `organization_id` | ADR-002 | LOCKED | Tests pgTAP à chaque PR |
| **DEC-003** Stack technique figée (Next.js / Supabase / OR-Tools / OSRM) | CLAUDE.md § 3 | LOCKED | Nouvelle dépendance majeure = ADR |
| **DEC-004** 3 piliers non négociables (UX / design / sécurité HDS) | CLAUDE.md § 1 | LOCKED | Niveau qualité visé : Linear, Stripe, Notion |
| **DEC-005** Objectifs UX chiffrés (saisie < 30 s, TTI < 2 s, etc.) | CLAUDE.md § 1 | LOCKED | SLOs perçus, mesurés en E2E Playwright |
| **DEC-006** Authentification Supabase Auth PKCE + sessions | CLAUDE.md § 6 | LOCKED | Régulateur de garde mono-actif |
| **DEC-007** Chiffrement applicatif AES-256-GCM (NIR, notes médicales) | CLAUDE.md § 6 | LOCKED | Clé hors Supabase |
| **DEC-008** Consentement et règles SMS patient | CLAUDE.md § 6 | LOCKED | Pas d'envoi sans consentement actif horodaté |
| **DEC-009** Géolocalisation chauffeur (service uniquement, 90 j) | CLAUDE.md § 6 | LOCKED | Information préalable obligatoire |
| **DEC-010** Audit et traçabilité (`audit_logs` append-only) | CLAUDE.md § 6 | LOCKED | Index `(organization_id, ...)` |
| **DEC-011** Localisation FR + conventions de nommage | CLAUDE.md § 7 | LOCKED | Commits `type(scope): description` en français |
| **DEC-012** GitHub Flow adapté (`main` + `staging` + `feat/*`) | README.md | LOCKED | Migrations validées par CI avant merge |
| **DEC-013** Couverture tests (pricing 100 %, recurrence 100 %, domain ≥ 80 %) | CLAUDE.md § 9 | LOCKED | Vitest + Playwright + pgTAP + pytest |
| **DEC-014** Ergonomie chauffeur (boutons ≥ 56 px, 1 action / écran, swipe) | CLAUDE.md § 5 | LOCKED | Mode hors-ligne fonctionnel obligatoire |
| **DEC-015** Ergonomie régulatrice (cockpit accueil, `Cmd/Ctrl+Shift+K`, fuzzy 2 car) | CLAUDE.md § 5 | LOCKED | File d'attente brouillons + multi-saisies parallèles |
| **DEC-016** Localisation des règles métier en packages dédiés | CLAUDE.md § 11 | LOCKED | Tarification → `pricing`, récurrence → `recurrence`, SMS → `sms` |
| **DEC-017** Découpage Phase 04 monolithique | discuss 2026-05-13 | ABANDONNÉ | Remplacé par DEC-023 (refonte E2E). God-phase 17-25h jugée contre ADR-003. |
| **DEC-018** React Hook Form Phase 04+ pour nouveaux formulaires | discuss 2026-05-13 | LOCKED | Pas de migration rétroactive Phase 1/2. Premier usage = `/accept-invite` Phase 04. RHF + `zodResolver` standardise validation client/serveur. Server Actions conservées côté submit. |
| **DEC-019** Stack offline-first Serwist + Dexie 4.x | discuss 2026-05-13 | LOCKED | Recherche état de l'art 2026 (Next.js docs, LogRocket Jan 2026, Medium Apr 2026, PkgPulse Mar 2026). Successeur officiel `next-pwa` / `@ducanh2912/next-pwa`. Dexie 4.x choisi pour `useLiveQuery` réactif + migrations versionnées déclaratives. Détail Phase 04.9 CONTEXT. |
| **DEC-020** Transitions PWA Driver = fade-in `template.tsx` | discuss 2026-05-13 | LOCKED | Slide bidirectionnel iOS-style reporté Phase UI dédiée post-Passe 2. Cause : bug `vercel/next.js#42658` ouvert depuis 2022, `template.tsx` n'anime pas la sortie. View Transitions API Chrome only experimental. Verrou NFR-004 anti-`framer-motion` préservé. |
| **DEC-021** Pricing CGSS découpé en deux phases | discuss 2026-05-13 | LOCKED | Phase 04.7 livre UI mockup (stub DEMO + PricingBreakdown + Override + caisse). Phase 05.5 livre pricing réel (décision distance + grille dirigeant + calcul ±0,01 €). Cause : décision distance structurante (OSRM externe public non production-ready, choix entre auto-hébergé / Mapbox / Haversine) mérite discuss propre et validation dirigeant grille tarifaire 974. |
| **DEC-022** Persistence storage Dexie + warning UI > 7j inactivité | discuss 2026-05-13 | LOCKED | `navigator.storage.persist()` au mount PWA (WebKit accorde via heuristique home screen install). Tracking `lastUsedAt` + banner UI si > 7 j sans ouvrir. Cause : iOS purge IndexedDB après ~2 semaines inactivité PWA (WebKit script-writable storage cap), risque queue mutations + cache. Détail Phase 04.9 CONTEXT. |
| **DEC-023** Phase 04 god-phase abandonnée, refonte E2E logique | discuss 2026-05-13 | LOCKED | Découpage : 04 onboarding → 04.5 robustesse → 04.7 pricing mockup + caisse → 04.9 PWA enveloppe → 05 récurrences + cockpit + SMS + patient absent → 05.5 pricing réel → 06 HDS. Principe : logique métier web stable d'abord, mobilité PWA ensuite. PWA emballe un code mûr. Annule DEC-017. |
| **ADR-003** Pivot E2E par passes successives | `docs/adr/` 2026-05-11 | LOCKED | Inscription rétroactive dans bloc decisions PROJECT.md pour résoudre dette CONCERNS.md severity major. Méthode : chaque passe traverse les 6 maillons du parcours métier en améliorant le minimum partout, jamais un module en profondeur avant que tous existent. Critère de fin : design partner complète le parcours sans intervention dev. |
| **DEC-024** Workflow invitation chauffeur en 2 temps | discuss Phase 04 2026-05-13 | LOCKED | Bouton « Inviter » séparé de `createDriverAction`. Dirigeant crée d'abord la fiche métier `drivers` (peut rester sans email/compte connexion encore — cas hérité Phase 1, `profile_id` nullable déjà prévu), PUIS click « Inviter » quand prêt à rattacher un compte. Cohérent code existant. |
| **DEC-025** `driver_invitations` table séparée (PAS extension `drivers`) | discuss Phase 04 2026-05-13 | LOCKED | Évite duplication email (auth.users source de vérité après rattachement) et risque désynchronisation. La table `drivers` reste sans champ email. Pour affichage liste, JOIN informationnel avec `auth.users` via `profile_id`. |
| **DEC-026** `driverInvitationSchema` Zod séparé | discuss Phase 04 2026-05-13 | LOCKED | PAS d'extension `driverInputSchema`. Séparation des concerns : fiche métier vs compte connexion. Schéma : `{ email: z.string().email(), driverId: z.string().uuid() }`. |
| **DEC-027** Acceptation CGU obligatoire à `/accept-invite` | discuss Phase 04 2026-05-13 | LOCKED | Case à cocher obligatoire avec lien `/legal/cgu`. Trace `audit_logs` type `cgu_accepted_via_invitation`. Conforme Phase 1.5 RGPD. |
| **DEC-028** Pattern RHF sans wrapper `<Form>` shadcn pour formulaires simples | discuss Phase 04 2026-05-13 | LOCKED | `<Input>` `<Label>` `<Button>` shadcn directs pour formulaires ≤ 5 champs. Le wrapper `<Form>` shadcn ajoute des layers inutiles V1. Reste disponible pour formulaires complexes (caisse filters, override tarif Phase 04.7). |
| **DEC-029** Gestion chauffeurs élargie au régulateur (D1+D2+D3) | hotfix Phase 04 2026-05-13 | LOCKED | Phase 03.1 avait verrouillé CRUD + invitation au rôle `dirigeant`. Le métier réel taxi conventionné 974 = la régulatrice gère opérationnellement (embauche, invitation, modification). Sémantique affinée hotfix-bis 2026-05-13 en 4 actions distinctes : **Désactiver** (régulateur ou dirigeant, réversible facile, filet de sécurité) ; **Réactiver** (régulateur ou dirigeant, instantané) ; **Archiver** (DIRIGEANT UNIQUEMENT, confirmation renforcée motif 10..500 + saisie `ARCHIVER`) ; **Désarchiver** (régulateur ou dirigeant, confirmation simple). Trois lignes de défense : helpers `requireAdminOrRegulateur` / `requireDirigeant` au niveau Server Actions, RLS Postgres élargie, trigger column-level `drivers_archive_columns_guard` qui bloque l'archivage par non-dirigeant au niveau BDD. Sub-guard SSR `requireDirigeantPage` sur les autres pages admin (véhicules, legal/*) qui restent dirigeant-only. |
| **DEC-030** Conventions rédactionnelles FR user-facing | hotfix Phase 04 bis 2026-05-13 | LOCKED | Audit FR du repo en méthode C hybride (grep + revue manuelle par occurrence) sur les seuls textes user-facing (tsx UI, messages Zod, toasts, libellés ARIA, descriptions de Sheet/Dialog). Hors scope : commentaires code (// et /* */), JSDoc, docs .md, commentaires SQL, fichiers .test.ts. Règles appliquées (Option β) : (1) cadratin `—` en articulation/définition remplacé par `:` contextuel ; séparateurs de titre de page conservés (convention web FR Wikipédia/Stripe/Le Monde) ; (2) anglicismes verbes traduits — `assigner→affecter`, `assignation→affectation`, `modal→fenêtre`, `désassigner→désaffecter` ; (3) guillemets français `« »` privilégiés dans les messages d'erreur Zod. Dette future explicite : apostrophes typographiques `’`, espaces fines insécables U+202F. Toute nouvelle phase touchant à du texte user-facing doit respecter DEC-030 (audit ponctuel ré-effectué à chaque phase UI dédiée). |
| **DEC-031** Seed démo étendu pour UAT multi-chauffeurs | hotfix seed 2026-05-13 | LOCKED | Les 3 chauffeurs métier (Vergoz, Maillot, Boyer) sont désormais tous rattachés à un compte Auth distinct (`chauffeur@`, `chauffeur2@`, `chauffeur3@demo.tap`, mot de passe `demo1234!`). 12 courses fictives semées (5 historiques J-3 à J-1 statuts `terminee` / `annulee_regulateur` + 4 jour J0 mix `assignee` / `en_cours` / `validee` + 3 J+1 `assignee` / `validee`) pour peupler le cockpit régulateur dès le premier login UAT. `DemoCredentials` passe de 3 à 5 cards individuelles avec affichage du `type_permis` côté chauffeurs (taxi / taxi + TPMR). Réécrit l'hypothèse historique `seed.demo.sql` « un seul chauffeur lié auth » devenue obsolète post-Phase 04 (workflow d'invitation rattache dynamiquement). Seed adapté au schéma rides réel : `transport_mode` (pas `type_course`), `tarif_amount_eur` (pas `tarif_eur`), statuts `validee`/`assignee`/`en_cours`/`terminee`/`annulee_regulateur` (pas `planifiee`/`annulee` génériques). Aucune migration BDD impactée — données seulement. |
| **DEC-032** Politique migrations Supabase production via CD GitHub Actions | hotfix CD 2026-05-13 | LOCKED | `supabase db push` via le workflow `cd.yml` est le canal exclusif d'application des migrations sur Supabase prod/preview. L'application manuelle via Supabase MCP (`apply_migration`) reste autorisée en hotfix urgent uniquement, avec **obligation immédiate** de réconcilier `supabase_migrations.schema_migrations.version` sur le nommage repo après chaque application (sinon `db push` détecte le drift et refuse les pushes suivants). Incident 2026-05-13 : 3 migrations appliquées via MCP entre 10:52-10:53 UTC ont créé des versions auto-générées (timestamp courant `20260513105255/312/324`) au lieu des versions disque (`20260514000002`, `20260516000001`, `20260516000002`). 5 runs CD échoués jusqu'à correction par `UPDATE schema_migrations SET version = ...`. Playbook documenté en `CONCERNS.md`. |
| **DEC-033** Clés React listes de composants client incluant champ mutable | hotfix UAT 2026-05-13 | LOCKED | Quand une liste rend un composant **client** dont l'affichage dépend d'un champ mutable (status / actif / archive…), la clé React **DOIT** inclure ce champ : `key={\`${id}-${status}\`}`. Sinon `router.refresh()` repeuple les props serveur mais le composant client conserve son state interne et l'UI reste figée sur l'ancien rendu (bouton actif, badge inchangé, etc.). Précédent Phase 03.2 #4 (bouton Annuler course restait actif après annulation). Récidive UAT Phase 04 2026-05-13 (bouton « Démarrer la course » côté chauffeur restait visible après transition `assignee → en_cours`). Listes corrigées en hotfix : `(driver)/conduite/page.tsx` (status), `courses/rides-list.client.tsx` (status), `admin/chauffeurs/drivers-list.client.tsx` (actif + archive), `admin/vehicules/vehicles-list.client.tsx` (actif). Mécanisme préventif : audit `grep -rn "key={.*\\.id}"` à chaque PR qui ajoute une liste de composants client. |
| **DEC-034** Design system TAP codifié dans `.planning/UI-PATTERNS.md` | audit visuel post-Phase 04 2026-05-14 | LOCKED | Référence dense ops Linear / Stripe / Cal.com / `satnaing/shadcn-admin`. Layout pages admin : header sobre + toolbar (filtres pills + recherche + CTA `Nouveau`) + liste `divide-y rounded-md border` dense (~64-72 px/ligne) + Sheet latéral édition. Badges sémantiques 5 familles (type métier neutre, statut activité vert, neutre gris, warning amber, critique rouge) avec max 3 badges/ligne. Spacing scale 8 px strict (4/8/12/16/24/32/48/64). Color = signal (rouge réservé erreur ou destruction). Filtres pills segmented control iOS-like ≤ 4 options. Sidebar navigation `shadcn/ui Sidebar` (pas de tabs top page). Anti-patterns proscrits : cards individuelles ombrées en liste, avatars > 40 px hors header, modal pleine page édition, action destructive non séparée par `border-t`. Refactor progressif des pages admin existantes en Phase 04.5+ selon priorisation UAT dirigeant (`/admin/chauffeurs` aligner densité Linear, `/admin/legal/*` à auditer). |
| **DEC-035** Table `pois_metier` seedée pour recherche adresse métier 974 | Phase 04.5 plan 2026-05-15 | LOCKED | Nouvelle table `public.pois_metier` (organization_id, nom_court, nom_long, type_poi enum 11 valeurs, adresse, code_postal 974xx, ville, lat/lng, téléphone, notes_acces, actif) avec RLS same-org (`pois_metier_select_same_org`, `pois_metier_modify_admin_or_regulateur`), trigger audit_logs, index gin sur `to_tsvector('french', nom_court || nom_long)`. Seed `seed.demo.sql` : ~30 POI Réunion réels (CHU Félix Guyon, CHU Sud, Clinique Saint-Vincent, centres dialyse Nord/Sud/Ouest, EHPAD Les Lataniers/Mascareignes, cabinets kiné/ophtalmo, etc.). Migration appliquée via CD `supabase db push` (DEC-032). Permet à la régulatrice de saisir `« CHU »` et trouver instantanément le bon hôpital sans dépendre du score BAN. |
| **DEC-036** Masques de saisie stricts pour formulaire patient | Phase 04.5 plan 2026-05-15 | LOCKED | Tous champs sensibles RGPD du formulaire patient passent par des masques + validation : `DatePicker` FR `react-datepicker` format JJ/MM/AAAA (range 0-130 ans), NIR 15 chars + validation clé contrôle `97 − N mod 97` (gestion Corse 2A/2B → 19/18) avec indicateur visuel `CircleDashed`/`CheckCircle2`/`XCircle`, téléphone Réunion strict `0(262\|263\|692\|693)` ou `+262(62\|63\|92\|93)` (refus métropole 06/07), code postal préfixe `974` disabled + 2 chiffres saisissables, ville `z.enum(VILLES_974)` liste fermée 24 communes INSEE avec auto-complétion CP→Ville, regex nom/prénom `^[A-Za-zÀ-ÿ\s'-]+$`. Messages d'erreur Zod refondus FR (DEC-030) avec exemple format inline (« 1 76 05 25 974 001 12 »). Refus silencieux interdit. Fallback time-box : si algo NIR clé contrôle dépasse 1 h, mode dégradé `refine() => true` + Edge Function `nir-encrypt` valide côté serveur (dette CONCERNS.md). |
| **DEC-037** Composant `AddressOrPOIPicker` mixe POI métier + BAN | Phase 04.5 plan 2026-05-15 | LOCKED | Refonte `address-picker-field.client.tsx` → `address-or-poi-picker.client.tsx`. UI dropdown 2 sections séparées par `<Separator />` : « Lieux fréquents » (POI métier, prioritaires, icône `Hospital` Lucide) + « Adresses BAN » (icône `MapPin`). Sélection POI préremplit `adresse + code_postal + ville + notes_acces` ; sélection BAN préremplit `adresse + code_postal + ville` uniquement. ARIA pattern combobox complet : `aria-expanded`, `aria-controls`, `aria-activedescendant`. `aria-label` distinct par section pour annonce VoiceOver. Si BAN down : variant « POI seul » ; si pas de POI match : variant « BAN seul ». |
| **DEC-038** Modal d'assignation filtre par défaut chauffeurs compatibles avec véhicule | Phase 04.5 plan 2026-05-15 | LOCKED | Helper pur `isCompatible(driver, vehicle): boolean` dans `packages/shared/src/utils/driver-vehicle-compat.ts` (100 % branch coverage Vitest, 16 paires testées). Matrice : véhicule TPMR exige `tpmr ∈ type_permis`, VSL accepte `taxi`, taxi exige `taxi`, ambulance exige `ambulance`. UI `assign-modal.client.tsx` : toggle pills « Compatibles » (défaut) / « Afficher tous » (urgence), badge Compatible/Incompatible 1/ligne avec icône Lucide, warning explicite si incompatible sélectionné (`bg-warning/10` + `text-warning` icône + `text-destructive` texte). Clé React inclut `vehicle.type` mutable : `key={\`${driver.id}-${selectedVehicle?.type ?? 'none'}\`}` pour respecter DEC-033 (badge change selon véhicule). |
| **DEC-039** Seed démo `rides` idempotent + glissant via `ON CONFLICT DO UPDATE` | Phase 04.5 plan 2026-05-15 | LOCKED | `seed.demo.sql` patché : bloc INSERT rides démo `id LIKE '44444444-%'` passe de `ON CONFLICT (id) DO NOTHING` à `ON CONFLICT (id) DO UPDATE SET scheduled_at = EXCLUDED.scheduled_at, started_at = EXCLUDED.started_at, ended_at = EXCLUDED.ended_at, created_at = EXCLUDED.created_at`. Comme les valeurs source utilisent `date_trunc('day', now() - interval '3 days')` etc., chaque re-application CD fait glisser les dates de 1 jour. UAT peut être déroulé n'importe quel jour sans reset BDD manuel. Limitation au scope rides démo uniquement (préfixe ID 44444444). Autres seeds gardent `ON CONFLICT DO NOTHING` standard. |
| **DEC-041** Pattern Server Action Supabase avec row count check | Phase 04.5 amendement 2026-05-15 | LOCKED | Toute Server Action faisant UPDATE/DELETE sur une table avec RLS DOIT suivre le pattern : `const { data, error } = await supabase.from('table').update(payload).eq('id', id).select('id'); if (error) return { error: ... }; if (!data \|\| data.length === 0) return { error: 'Modification refusée — droits insuffisants.' };`. **Justification** : RLS Postgres rejette silencieusement les UPDATE qui ne match aucune ligne (0 rows affected, pas d'erreur SQL). Sans `.select()` + check `length`, l'application affiche un faux success. **Incident 2026-05-15** : `startRideAction` et `endRideAction` renvoyaient `{ success: true }` malgré RLS blocking → chauffeur croyait avoir démarré la course alors qu'elle restait `assignee` en BDD. **Scope Phase 04.5** : pattern appliqué UNIQUEMENT à `startRideAction` et `endRideAction` (mini-fix T1.4). L'audit complet de TOUTES les Server Actions avec application systématique du pattern est **reporté Phase 06 HDS** (conformité production-grade — sa place naturelle). Inscrit dans CONCERNS.md la liste des actions restant à auditer. |
| **DEC-042** Pricing stub Phase 04.7 = Haversine + fallback random | Phase 04.7 plan 2026-05-15 | LOCKED | `computeCgssShortTrip(input): PricingResult` dans `packages/pricing/` (nouveau workspace). **Algo** : si `pickup_lat/lng` ET `dropoff_lat/lng` sont fournis → calcul Haversine (formule sphérique terre, rayon 6371 km) puis tarif = `4.20 + dist_km × 2.10`. Sinon → `random(min, max)` dans plage cohérente avec `transport_mode` + `urgency` (`source: 'fallback_random'`). Toujours appliquer : majo nuit (+20 % entre 20 h et 7 h selon `scheduled_at`), supplément TPMR (+5 € si `transport_mode === 'tpmr'`), arrondi à 0.05 € le plus proche. Total clamp final `[10 €, 80 €]`. **Badge UI « DEMO »** obligatoire (Surface A `PricingBreakdown`) — rappelle que ce n'est PAS le calcul CGSS final (Phase 05.5 livrera l'algo réel). **Couverture Vitest** : 100 % branch sur `computeCgssShortTrip` (DEC-013 esprit — calcul critique data financière même stub). **Hors scope DEC-042** : grille tarifaire CGSS réelle (forfait/km/majo dim/férié 974), décision distance (OSRM / Mapbox / Haversine + override Hauts), validation grille dirigeant — tout cela en Phase 05.5 DEC-021. |
| **DEC-043** RLS caisse `/courses/caisse` exclusion chauffeur V1.5 | Phase 04.7 plan 2026-05-15 | LOCKED | Page `/courses/caisse` accessible **régulateur + dirigeant uniquement** (route guard `requireAdminOrRegulateur` côté Server Component). Chauffeur reçoit 403 explicite : « Cette page est réservée au régulateur. Vous pouvez consulter vos encaissements depuis l'écran de clôture de course. ». Server Actions `listRidesEncaisseesAction` + `exportCaisseCsvAction` requirent même check (defense in depth). RLS Postgres : queries scope `organization_id = current_organization_id()` (pas de filtre par chauffeur côté serveur — la régulatrice voit tout son org). **Justification** : la régulatrice gère 8 h/jour la vue Stripe Balance-like ; le chauffeur a déjà sa vue end-ride-modal pour ses propres encaissements. Vue chauffeur dédiée (`/conduite/mes-encaissements`) différée Phase 04.9 (PWA chauffeur). |
| **DEC-044** Géocoding persistance threadé BAN + POI dans `AddressOrPOIPicker.onSelect` | Phase 04.7 plan 2026-05-15 | LOCKED | Migration `<timestamp>_rides_geocoding.sql` ajoute 6 colonnes nullables sur `public.rides` : `pickup_lat numeric(10,7)`, `pickup_lng numeric(10,7)`, `pickup_citycode text`, idem `dropoff_*`. Index optionnel `rides_pickup_citycode_idx` partiel. Migration via CD push exclusif (DEC-032). **Threading UI** : `AddressOrPOIPicker.onSelect` signature élargie pour propager `lat?: number, lng?: number, citycode?: string` au parent (consommateurs `ride-express-modal.client.tsx` `createRideAction` + `editRideAction`). **Sources** : POI = `pois_metier.latitude/longitude` (déjà nullables PR #84), BAN = `f.geometry.coordinates[1]/[0]` + `f.properties.citycode`. Saisie libre = pas de coords (acceptable, fallback random pricing DEC-042). **Backfill courses existantes** : Server Action `backfillRideGeocodingAction` (dirigeant only) depuis `/admin/maintenance`, itère rides où `pickup_lat IS NULL`, appelle BAN gouv.fr, DO UPDATE idempotent, toast progress. Pas de cron, déclenchement manuel one-shot post-déploiement. |
| **DEC-045** Route Handlers explicites pour mutations driver PWA (Alt A vs Server Actions via fetch headers `Next-Action`) | Phase 04.9 discuss 2026-05-18 | LOCKED | Phase 04.9 PWA chauffeur enveloppe : mutations offline `start ride` / `end ride` passent par **Route Handlers REST classiques** `/api/driver/rides/[id]/start` + `/api/driver/rides/[id]/end` (Alt A). **Justification** : (1) visibilité explicite endpoints (curl-able pour debug device réel), (2) compatibilité Serwist offline cache standard `NetworkOnly` + queue, (3) cohérence pattern REST web, (4) Server Actions via `fetch(url, { headers: 'Next-Action' })` fonctionnent mais opaques pour debug offline + Serwist cache strategies plus complexes. **Auth** : cookies Supabase (middleware Next.js déjà en place, pas de JWT séparé). **Schéma payload** : JSON `{ idempotency_key: uuid, client_timestamp: iso8601, ...payload }` — `idempotency_key` UUID client-side dédupé server-side (colonne unique sur `ride_events` à valider plan-phase Wave 3). **Stratégie cross-platform Option A** : iOS Safari (apple-touch-startup-image 3 DPR + apple-touch-icon 180) + Android (manifest 192/512 maskable auto-généré) testés en parallèle Wave 6 sur 2 devices réels. |
| **DEC-046** Moteur récurrences = `rrule.js` standard RFC 5545 | Phase 05 discuss 2026-05-18 | LOCKED | Dépendance mineure (~30 KB), RFC iCalendar standard, bien maintenu, exceptions natives via `EXDATE`. **Justification** : pas de réinvention, compatible Vitest 100% branches DEC-013, timezone-aware (DST 974). Alternative écartée : custom léger (réinvention fragile, dette tests). |
| **DEC-047** Génération occurrences eager batch 3 mois + extension cron mensuel | Phase 05 discuss 2026-05-18 | LOCKED | À la création de récurrence : batch génération 3 mois d'occurrences. Cron Vercel mensuel (DEC-050) étend de 3 mois à chaque run. **Justification** : équilibre planning visibility (régulatrice voit 3 mois) + complexité modérée (pas génération à la volée fragile). Alternative écartée : lazy cron quotidien (perte visibility planning) ou eager 12 mois (volume BDD inutile). |
| **DEC-048** Modification récurrence active = modal confirm + regen futures non-démarrées | Phase 05 discuss 2026-05-18 | LOCKED | Régulatrice modifie jour/heure/durée d'une récurrence en cours : modal confirmation « X occurrences seront affectées (non-démarrées uniquement). Confirmer ? ». Regen toutes occurrences futures status `assignee` (et `non_assignee`). Préserve `en_cours` / `terminee` / `annulee_*`. `audit_logs` capture le diff (avant/après). **Justification** : protège l'intégrité métier (course commencée intouchable) + transparence régulatrice. |
| **DEC-049** Channel Realtime cockpit = global `cockpit:rides` MVP | Phase 05 discuss 2026-05-18 | LOCKED | Single channel Supabase Realtime `cockpit:rides` listen `postgres_changes` sur `rides` table UPDATE. **Scoping multi-tenant Phase 06+** (ADR HDS migration + Stack BDD long terme conjointe). MVP single-régie suffit V1.5 (1 design partner, 1 organization_id). **Justification** : simplicité MVP, scaling plus tard quand multi-régies. Alternative écartée : broadcast manuel via Edge Function (complexité, latence inutile). |
| **DEC-050** Cron jobs SMS = pg_cron Supabase + pg_net HTTP call → Route Handler Next.js | Phase 05 discuss 2026-05-18 / révisé UI-SPEC 2026-05-18 | LOCKED | **Révision UI-SPEC** : pg_cron 1.6.4 déjà activé sur projet Supabase (vérifié SQL). pg_net à activer Wave 1 (extension Supabase standard, gratuite, async HTTP/HTTPS depuis SQL, throughput 200 req/s). Coût 0€ supplémentaire (vs Vercel Cron Pro 20$/mois si Hobby actuel non confirmé). Timezone Réunion native PostgreSQL (vs UTC seul Vercel). **Cohérent CONCERNS architecture portable V1.5** (PR #119) : pg_cron part avec Postgres self-host Phase 06 HDS migration, zero reconfiguration. Pattern combo : (1) `cron.schedule('sms-j1', '0 18 * * *', $$ SELECT net.http_post(...) $$)` déclenche HTTP, (2) Route Handler Next.js `/api/cron/sms-reminders-j1` reçoit appel, valide `Authorization: Bearer ${CRON_APP_TOKEN}`, query rides + Twilio + tracking. **Auth token** stocké Supabase Vault (`vault.decrypted_secrets`) + env var miroir Vercel `CRON_APP_TOKEN`. **Observabilité** : `cron.job_run_details` + `net._http_response` SQL-queryable. **Logique métier TypeScript préservée** (testable Vitest, cohérent stack figée DEC-003). |
| **DEC-051** Templates SMS personnalisation = Mustache-like custom léger 5 variables | Phase 05 discuss 2026-05-18 | LOCKED | Implémentation custom ~20 lignes dans `packages/sms` : `interpolate(template, vars)` qui remplace `{{patient_prenom}}`, `{{patient_nom}}`, `{{heure}}`, `{{date}}`, `{{chauffeur_prenom}}`. **Justification** : pas de dépendance Handlebars (40+ KB, over-engineered), 5 variables suffisent V1.5, tests Vitest triviaux. Alternative écartée : Handlebars complet (dépendance majeure DEC-003), template literals JS (moins UX éditable régulatrice). |
| **DEC-052** Webhook Twilio delivery status = Route Handler HMAC vérifiée | Phase 05 discuss 2026-05-18 | LOCKED | Route Handler `POST /api/sms/webhook/twilio` reçoit les callbacks Twilio delivery status (`sent` → `delivered` → `failed` → `undelivered`). **Sécurité** : vérification `X-Twilio-Signature` HMAC SHA1 avec `TWILIO_AUTH_TOKEN` env var (standard Twilio). **Justification** : empêche injection callbacks falsifiés. Update `sms_message.status` + `audit_logs` à chaque transition. Pas de retry côté serveur (Twilio gère). |
| **DEC-053** Endpoint patient absent driver = Route Handler cohérent DEC-045 | Phase 05 discuss 2026-05-18 | LOCKED | `POST /api/driver/rides/[rideId]/no-show` avec payload `{ idempotency_key: uuid, motif?: string }`. **Pattern réutilisé DEC-045** : auth cookies Supabase (middleware), idempotency UUID dédupé (table `idempotency_keys` Phase 04.9 Wave 1), status guards (assignee → no_show_pending), row count check DEC-041, INSERT `ride_events`, audit_logs. Trigger Realtime alerte cockpit < 5s. Décision régulatrice (Reprog/Annul) via Server Action `/cockpit` (régulatrice online uniquement, pas besoin Route Handler). |
| **DEC-054** Cockpit landing page régulatrice | Phase 05 discuss 2026-05-18 | LOCKED | Redirect login régulatrice → `/cockpit` (au lieu de `/courses`). Cohérent COCK-01 écran d'accueil par défaut. `/courses` reste accessible via menu nav (link header). **Justification** : cockpit = vue principale 8h/jour régulatrice (CLAUDE.md § 1 pilier 1 UX). `/courses` = vue secondaire pour gestion non-temps-réel. Pas de breaking change pour dirigeant (qui reste sur `/patients` ou `/admin`) ni chauffeur (qui reste sur `/conduite`). **Amendée par DEC-071 (2026-05-21)** : la clause incidente sur le dirigeant est mise à jour — il atterrit désormais sur `/tableau-de-bord` ; le cœur (régulateur → /cockpit) est préservé. |
| **DEC-055** Notification famille patient absent → Phase 06 | Phase 05 discuss 2026-05-18 | LOCKED | Hors scope V1.5. SMS-06 respecté si famille a consentement actif horodaté distinct du patient. **Justification** : complexité légale RGPD (consentement tiers, contact urgence vs représentant légal, droit à l'information famille), pas de demande explicite design partner V1.5. Phase 06 si demande validée + ADR consentement tiers. |
| **DEC-056** Distance pricing = Haversine + facteur de correction routier | Phase 05.5 discuss 2026-05-19 | LOCKED | V1.5 : `distance_routiere = haversine × facteur_correction`. Facteur ~1,4 (relief 974, écart vol d'oiseau / routier élevé en zone montagneuse — cirques, routes sinueuses), **paramétrable BDD** (`tariff_grids.facteur_correction_routier`). OSRM auto-hébergé reporté Phase 06 (arrive avec la géolocalisation certifiée Assurance maladie obligatoire 1er janvier 2027). **Justification** : facturation CGSS réelle = Phase 06, donc V1.5 = tarif estimatif — la précision absolue est moins critique. Alternatives écartées : Haversine pur (sous-facturation Hauts), OSRM dès 05.5 (infra lourde), API externe Mapbox/Google (hors VISION + coût + dépendance DEC-003). |
| **DEC-057** Grille tarifaire = table BDD `tariff_grids` versionnée | Phase 05.5 discuss 2026-05-19 | LOCKED | Table `tariff_grids` : `id`, `organization_id`, `date_effet`, `forfait_eur` (13), `km_inclus` (4), `prix_km_eur` (1,22 défaut 974), `supplement_drom_eur` (3), `supplement_tpmr_eur` (30), `majoration_pct` (50), `facteur_correction_routier` (1,4), `arrondi`, `created_by`, `created_at`. `computeCgssShortTrip` prend la grille en **paramètre** → fonction pure, testable 100 % branches Vitest sans BDD (DEC-013, PRIC-03). **Justification** : tarif km 974 + supplément DROM jamais hardcodés — volatilité du conflit syndical local 974, négociation CGSS Réunion possible courant 2026 ; le dirigeant ajuste sans redéploiement + historique versionné. Alternative écartée : constantes dans le code (chaque changement = redéploiement). |
| **DEC-058** Périmètre pricing V1.5 = cas nominal monopatient | Phase 05.5 discuss 2026-05-19 | LOCKED | DANS V1.5 : forfait 13 € (4 km inclus) + km facturés au 5e km+ + supplément DROM 3 € + supplément TPMR 30 € + majoration nuit/we/férié 50 % non-cumulable. HORS Phase 06 : transport partagé + abattements multi-patients (-23/-35/-37 %) + retour à vide hospitalisation (+25/+50 %) + forfait Grande Ville (non applicable 974). **Justification** : le monopatient est le cas le plus fréquent ; le transport partagé est une feature à part entière (UI courses groupées + modélisation + abattements). |
| **DEC-059** Majoration nuit/we/férié = approximation heure de prise en charge | Phase 05.5 discuss 2026-05-19 | LOCKED | V1.5 : majoration 50 % si l'heure de `scheduled_at` est dans 20h-8h (nuit) OU samedi ≥ 12h / dimanche (week-end) OU date présente dans `holidays_974` (jour férié, table Phase 05). **Corrige le double bug du stub `isNuit`** : `getUTCHours` → `Indian/Reunion` (UTC+4) + prise en charge des cas we/férié. Une seule majoration 50 % maximum (non-cumulables entre elles). **Justification** : la règle exacte convention (« > 50 % du temps de transport en charge de nuit ») nécessite la durée routière → reportée Phase 06 avec OSRM. L'approximation heure de départ est simple et les données sont disponibles V1.5. |
| **DEC-060** Recalcul rétroactif pricing = Server Action dirigeant + garde-fous | Phase 05.5 discuss 2026-05-19 | LOCKED | Server Action one-shot `/admin/maintenance` (pattern `backfillRideGeocodingAction` DEC-044, dirigeant only). Recalcule uniquement les courses au tarif stub DEMO **non facturées / non encaissées**, **préserve les overrides manuels** (`source = 'manual_override'` Phase 04.7), `audit_logs` chaque recalcul (PRIC-04). **Justification** : protège les corrections dirigeant volontaires + ne touche pas aux courses déjà encaissées (intégrité comptable). |
| **DEC-061** Transition stub → réel = `PricingResult` enrichie + disclaimer estimatif | Phase 05.5 discuss 2026-05-19 | LOCKED | `PricingResult` enrichie : `forfait_eur`, `km_factures` (au-delà de 4), `km_total_eur`, `supplement_drom_eur`, `supplement_tpmr_eur`, `majoration_pct`, `majoration_eur`, `total_eur`, `source`, `distance_method`. Le badge « DEMO » disparaît (vrai calcul) mais un disclaimer sobre subsiste : « tarif estimatif, non contractuel jusqu'à facturation CGSS (Phase 06) ». `PricingBreakdown` (Phase 04.7) adapté pour la décomposition réelle. **Justification** : V1.5 n'a pas la télétransmission CGSS — le tarif reste estimatif jusqu'à Phase 06, le disclaimer évite toute ambiguïté contractuelle. |
| **DEC-062** Fournisseur SMS différé + adaptateur pluggable | ADR-004 2026-05-20 | LOCKED | Twilio écarté (société US, CLOUD Act — inadapté aux données de santé indirectes du transport sanitaire 974). Le choix du fournisseur SMS est différé : aucune intégration en V1.5. Les 2 cron jobs SMS Phase 05 sont mis en pause (migration `20260524000001_unschedule_sms_cron`, idempotente). `packages/sms` + 3 Route Handlers + UI `/admin/sms-templates` + tables `sms_*` conservés **dormants** (zéro coût, réutilisables). Cible privilégiée = API SMS française HDS (smsmode ISO 27001/HDS, Octopush, OVH SMS) cohérente avec la trajectoire HDS Phase 06 ; RaspiSMS auto-hébergé = plan B. Adaptateur **pluggable** : changer de fournisseur = réécrire le seul `packages/sms/src/twilio-adapter.ts`. **Justification** : pas de dépendance US pour des données de santé, décision reportée sans dette technique. Cf. ADR-004. **Précise DEC-003** (qui mentionnait déjà « Twilio ou OVH SMS Pro ») — ce n'est pas une révision. |
| **DEC-040** Guard `require*` partagé obligatoire sur les Server Actions de mutation admin/métier | candidate CONCERNS → promue Phase 06 PLAN-4 2026-05-21 | LOCKED | Toute Server Action modifiant des données admin/métier commence par un guard `require*` **partagé** (`@/lib/auth/require-dirigeant`, `require-admin-or-regulateur`). Pas de `requireX` local dupliqué (corrigé sur `vehicules/actions.ts`). Couplé à DEC-041 (row count check). Audit complet : `docs/security/SERVER-ACTIONS-AUDIT.md`. |
| **DEC-063** Phase 06 resserrée (E2E Passe 4 redécoupée) | discuss Phase 06 2026-05-20 | LOCKED | Phase 06 = Bloc A (facturation CGSS PDF) + Bloc E (audit RLS / Server Actions + advisors) + Bloc F (dettes CI D1/D2/D3). HDS et OR-Tools sortis en sous-phases dédiées 06.5 / 06.7 ; portail B2B et télétransmission B2/CNDA différés via ADR. Une phase qui livre un incrément cohérent et testable plutôt qu'une méga-phase de 19-32 h. |
| **DEC-064** Facturation CGSS V1.5 = PDF récapitulatif mensuel ; télétransmission B2/CNDA différée | discuss Phase 06 2026-05-20 | LOCKED | La facturation V1.5 est un PDF récapitulatif mensuel (`/admin/facturation`) consommant le moteur 05.5. La télétransmission B2/SEFi via logiciel certifié CNDA + formulaire 606b est différée : l'échéance du 31 mai 2026 pèse sur le taxi qui télétransmet, pas sur l'éditeur d'un SaaS en bêta sans client facturant. Cf. ADR-005. |
| **DEC-065** Migration HDS = Phase 06.5 dédiée | discuss Phase 06 2026-05-20 | LOCKED | La migration de l'hébergement vers une infra HDS-certifiée est une sous-phase dédiée (discuss propre + ADR pour le choix fournisseur Scaleway HDS / OVHcloud HDS / validation Supabase EU + DPA). En bêta sans client facturant, Supabase EU sous DPA reste acceptable ; HDS strict = prérequis du 1er client payant. CON-001 (architecture portable) maintenue. **Renumérotée 2026-05-22 : Phase 06.5 → Phase 09 — HDS repoussée en fin de parcours (DEC-077) ; la décision LOCKED reste inchangée sur le fond.** |
| **DEC-066** OR-Tools optimisation de tournées = Phase 06.7 dédiée | discuss Phase 06 2026-05-20 | LOCKED | Microservice Python OR-Tools (`services/optimizer`) + `packages/optimizer-client` en sous-phase dédiée. Distance V1 = Haversine × facteur de correction routier (DEC-056) ; OSRM auto-hébergé avec la géolocalisation certifiée Assurance maladie (1er janvier 2027). |
| **DEC-067** Portail B2B multi-tenant commercial différé | discuss Phase 06 2026-05-20 | LOCKED | Le portail B2B (signup Stripe, isolation tenant, onboarding donneur d'ordres) est différé jusqu'au 1er client payant validant le produit — anti-construction de multi-tenant commercial avant product-market fit. Réactivation = décision business. Cf. ADR-006. |
| **DEC-068** Pré-remplissage RGPD déclenché + revue-puis-insertion | Phase 06.6 2026-05-21 | LOCKED | Le pré-remplissage des pages RGPD est déclenché par un bouton (jamais d'auto-remplissage silencieux) et passe par un écran de revue : seules les entrées validées par le dirigeant sont insérées, jamais d'INSERT direct. Motif : `data_processing_register` est append-only (ni UPDATE ni DELETE) — une ligne erronée ne se supprime pas. Protège la responsabilité du dirigeant et de TAP. |
| **DEC-069** Périmètre du pré-remplissage de l'espace Légal | Phase 06.6 2026-05-21 | LOCKED | Pré-remplissage RÉEL : registre des traitements (6 entrées-types transport sanitaire) + fiches DPA des sous-traitants techniques (le dirigeant saisit la date de signature et la version) + trame squelette DPIA (`status='brouillon'`, sans verdict de risque). Aide contextuelle seule (textes, aucune donnée fictive) : breaches, requests, dpo — vides par nature. |
| **DEC-070** Non-certification juridique du pré-remplissage RGPD | Phase 06.6 2026-05-21 | LOCKED | Le contenu pré-rempli est une suggestion sourcée sur des références publiques (CNIL, Code de commerce) — TAP ne certifie pas la conformité. Disclaimer de non-certification obligatoire sur l'écran de revue, avant insertion. Aucune entrée validée n'est marquée « suggéré » (responsabilité transférée au dirigeant). Le SUMMARY recommande une relecture par un juriste ou le DPO. |
| **DEC-071** Redirection par rôle à la connexion | Phase 06.8 2026-05-21 | LOCKED | À la connexion (action de login) et à la racine `/` (`app/page.tsx`), chaque rôle est redirigé vers sa page d'accueil : dirigeant → `/tableau-de-bord`, régulateur → `/cockpit`, chauffeur → `/conduite`. Remplace le `redirect('/patients')` uniforme. **Amende DEC-054** : la clause incidente « le dirigeant reste sur /patients/admin » devient « dirigeant → /tableau-de-bord » ; le cœur de DEC-054 (régulateur → /cockpit) est préservé — les deux décisions partagent la même logique de landing par rôle. |
| **DEC-072** Tableau de bord dirigeant = pilotage, rendu au chargement | Phase 06.8 2026-05-21 | LOCKED | Le tableau de bord (`/tableau-de-bord`) est réservé au rôle `dirigeant` (`requireDirigeantPage`), rendu au chargement (Server Component, pas de temps réel — le live reste au Cockpit régulateur). Il agrège 6 KPIs en réutilisant les définitions de Caisse / Facturation (chiffres identiques) ; il n'est pas une duplication du Cockpit. |
| **DEC-073** Carte de statut de conformité = factuelle | Phase 06.8 2026-05-21 | LOCKED | La carte de conformité du tableau de bord affiche des compteurs documentaires factuels (registre, DPA, DPIA + dernière mise à jour + lien `/admin/legal`) — jamais un verdict, un feu vert, un score ou une mention « conforme RGPD ». Le RGPD (art. 5-2) impose de démontrer la conformité, pas de la revendiquer — cohérent avec la non-certification de DEC-070. |
| **DEC-074** Positionnement non-certifié — TAP s'interface | révision roadmap 2026-05-22 | CANDIDATE | TAP est un logiciel opérationnel non certifié. Il s'interface par export avec une solution certifiée tierce (facturation/géoloc certifiée Assurance maladie). La certification CNDA/SEFi et la géoloc certifiée obligatoire au 1er janvier 2027 sont **hors périmètre TAP** — elles incombent au taxi et à sa solution certifiée. Précise DEC-064 et ADR-005. Risque avant-vente : clarifier que TAP est complémentaire, pas un substitut. |
| **DEC-075** Géoloc opérationnelle post-HDS | révision roadmap 2026-05-22 | CANDIDATE | La géolocalisation opérationnelle temps réel (CDC §5.17) est la **Phase 08 (renumérotée Phase 10 le 2026-05-22), postérieure à la migration HDS (06.5, renumérotée Phase 09)**. Aucune position réelle de véhicule transportant des patients n'est stockée en prod avant HDS (donnée de santé indirecte ; RGPD géoloc salarié). Faisabilité liée à la Phase 07 (capture GPS arrière-plan non fiable en PWA). |
| **DEC-076** Modernisation Next.js 15 autonome, avant production | révision roadmap 2026-05-22 | CANDIDATE | Le socle Next.js est modernisé en deux temps : d'abord le patch de sécurité 14.2.35 (CVE déc. 2025), puis la montée 14 → 15 en **Phase 06.9 autonome**, faisable en bêta — elle ne dépend pas de HDS. La Phase 06.9 inclut l'audit de la rupture du cache `fetch()` (Next 15 ne met plus les requêtes en cache par défaut). React 18 est conservé ; React 19 et Next 16 sont différés (montée seulement quand elle devient nécessaire). Voir ADR-007. |
| **DEC-077** HDS repoussé en fin de parcours (Phase 09) | révision roadmap 2026-05-22 | CANDIDATE | Vu l'état bêta du projet, la migration HDS n'est plus un verrou proche : elle est renumérotée Phase 06.5 → **Phase 09** et devient le pré-requis de la seule mise en production commerciale (1er client payant). La géolocalisation opérationnelle (ex-Phase 08, renumérotée **Phase 10**) dépend de HDS (DEC-075) et suit donc la Phase 09. Précise DEC-065 sans la contredire : Supabase EU sous DPA reste acceptable en bêta. |
| **DEC-078** Accessibilité — posture prospective sans obligation | révision roadmap 2026-05-22 | CANDIDATE | Le RGAA 4.1.2 (transposition de WCAG 2.1 AA) reste la référence légale 2026. Un exploitant de taxis est sous les seuils d'assujettissement RGAA (chiffre d'affaires < 250 M€) comme EAA — European Accessibility Act (< 10 salariés ou < 2 M€ de bilan) : aucune obligation légale d'accessibilité ne pèse sur TAP à ce stade. TAP vise néanmoins volontairement WCAG 2.2 AA. Veille : le RGAA 5 est attendu fin 2026 — il intègrera WCAG 2.2 (cibles tactiles, focus non masqué). Aucune action immédiate requise. |
| **DEC-079** Hébergement `services/optimizer` = Vercel Python serverless | ADR-008 / Phase 06.7 Wave 3 2026-06-01 | LOCKED | Promotion de la CANDIDATE inscrite Wave 1 (2026-05-27). Réutilisation de la stack actuelle, zéro nouvelle dépense, zéro nouveau fournisseur : Vercel supporte nativement Python/FastAPI en serverless ; le projet paie déjà un plan Vercel pour `apps/web`. Discipline technique imposée : (a) `time_limit` solveur **plafonné à 3 s en production** (contre 5 s en local) pour conserver une marge contre la limite d'exécution serverless ; le researcher avait calé 2-5 s, on retient la borne basse en prod. (b) **Mesure obligatoire du cold start** au premier déploiement Wave 3 : si > 5 s sur 10 appels consécutifs après inactivité, déclencher le repli. (c) **Repli documenté : Clever Cloud** (opérateur FR avec offre HDS, cohérent avec la cible HDS Phase 09, DEC-077) ; le `Dockerfile` neutralisé en Wave 1bis (PR #186) demeure pour permettre une bascule sans rework. (d) Payload dé-identifié maintenu (D-08, CONTEXT 06.7). Alternative Supabase Edge Functions éliminée (Deno only, issues `supabase/supabase#22944` + `#41990`). Cf. ADR-008. |
| **DEC-080** Commits Wave-by-Wave plutôt que Task-by-Task quand `index.ts` réexporte | Phase 06.7 Wave 2 2026-06-01 | LOCKED | Le hook `guard-commit.sh` exige `pnpm typecheck` monorepo vert avant chaque commit. Quand un nouveau package est créé avec un `index.ts` qui réexporte des modules pas encore livrés (cas Wave 2 de Phase 06.7 — `packages/optimizer-client/src/index.ts` réexporte `./contract`, `./client`, `./transform`), les tâches intermédiaires (contrat sans client/transform) ne compilent pas → un commit atomique par tâche est impossible. Convention : pour ce cas précis (création de package neuf avec re-exports), **livrer la Wave en un seul commit `feat` + un commit `docs` de SUMMARY/STATE/ROADMAP**. Wave 2 de 06.7 (PR #188, commits `79322a1` + `5b1fd61`) sert de précédent. Pas d'impact qualité ; la traçabilité par tâche est assurée par le SUMMARY de plan. Si la limite gêne dans le futur (plans plus longs, debug git bisect ardu), évaluer `pnpm typecheck --filter` par tâche — modification du hook hors périmètre actuel. |
| **DEC-081** Indicateurs de la proposition d'optimisation = taux de mutualisation + km à vide, affichés « estimés » | Phase 06.7 Wave 3 2026-06-01 | LOCKED | Promotion de la CANDIDATE inscrite dans CONTEXT.md Phase 06.7 §"Décisions candidates". Deux indicateurs uniquement (D-19, arbitrage verrouillé) : (1) **taux de mutualisation** — part des courses regroupées dans le plan proposé ; (2) **km à vide estimés** — km sans patient entre deux courses, via Haversine. Les deux sont affichés avec le mot « estimé » visible (D-20). Aucun 3e indicateur ne peut être ajouté sans rouvrir cette décision. L'OTP terrain et le deadhead réel dépendent du GPS (Phase 10) — non représentés en V1. Refs : D-19/D-20, CONTEXT.md Phase 06.7. |
| **DEC-101** Direction artistique TAP gelée (doc fondateur 00-direction) | discuss design 2026-06-05 | LOCKED | Direction artistique TAP gelée (`docs/design-system/00-direction.md`). Personnalité sobre / confiant / situé ; bleu institutionnel dominant (`hsl(217 92% 32%)`, gardé « dans la famille » du Département sans calage pixel) + terracotta accent du moment-clé (`hsl(14 78% 55%)`, principe 60-30-10) ; **near-monochrome adossé à une STRUCTURE forte** (hiérarchie typo, espace 8 px, grille, profondeur) intra ET inter-écrans (architecture de l'information) ; échelle neutre 6-10 paliers ; iconographie Lucide unique ; grammaire d'animation sourcée Material / NN-g (desktop 150-200 ms, ease-out référence `cubic-bezier(0.0, 0.0, 0.2, 1)`, ≤ 2 effets / écran, `prefers-reduced-motion` respecté). Boussole d'inspiration : Linear (densité) tempéré Frappe « Espresso » (anti-distraction métier). Incarnation famille par famille à suivre (Régulation en premier). Faiblesses tracées à résoudre : hiérarchie typo écrasée (305 `text-sm` / 133 `text-xs`), `PageHeader` absent du cœur métier, `loading.tsx` / empty-states / skeletons inégaux, raccourcis localisés, rangement à arbitrer en contexte. Voir aussi `docs/design-system/08-horizon-open-source.md` (cadrage chromatique chiffré + comparaison OSS). Pas d'ADR (document de direction, pas d'architecture technique). |
| **DEC-107** Rangement Régulation audité — décision : pas de refactor (lot 5) | audit lot 5 incarnation 2026-06-05 | LOCKED | Rangement de la famille Régulation audité dans le cadre du programme d'incarnation DEC-101. **Le rangement est cohérent en pratique** : nav unifiée par rôle (`nav-config.ts`), surlignage de nav correct (logique « plus long préfixe gagne » dans `nav-tabs.client.tsx`, commentaire explicite « évite que Courses reste souligné en même temps que Caisse »), accès régulateur à `/admin/chauffeurs` LÉGITIME et géré par les gardes `(admin)/layout.tsx` (dirigeant OU régulateur). Les impuretés d'URL identifiées au friction log (caisse enfant de courses ; chauffeurs sous `/admin`) sont SANS impact utilisateur observable. **Déplacement physique des URLs VOLONTAIREMENT NON RÉALISÉ** : coût mesuré (18 refs `/admin/chauffeurs` + 7 refs `/courses/caisse` + redirections + mémoire musculaire cassée) > bénéfice (cosmétique). Direction §5ter : « le rangement ne se chamboule pas par goût ; tout changement d'URL = à peser ». **Question close.** Réévaluable si un vrai besoin émerge (SEO, refonte IA, demande explicite design partner). Pas d'ADR (décision de non-action). |
| **DEC-112** Module conformité réglementaire (CdC §5.21) lot 1 — fondation | Phase 06.33 2026-06-08 | LOCKED | Premier lot du module Conformité réglementaire métier (CdC §5.21, V1 §2.1 « Suivi des échéances réglementaires »). Pose le socle : **table dédiée `compliance_items`** (polymorphe driver/vehicle/organization, 8 types CHECK, multi-tenant RLS forcée + pgTAP 10 cas) ; **validators + libellés FR centralisés** dans `@tap/shared` (NFR-001, 0 hardcode UI) ; **helper pur `complianceStatus`** (90/60/30/7 j, 14 tests Vitest) — statut DÉRIVÉ à l'affichage, jamais stocké ; **saisie embarquée** dans `driver-form` + `vehicle-form` (`<ComplianceFieldset>`, mode édition) + page `/admin/conformite` (vue consolidée + section convention CGSS organisation) ; **`<ComplianceBadge>`** sémantique (icône + texte + jours, WCAG 1.4.1 critère couleur seule) dans drivers-list + vehicles-list. **Distinct du `ComplianceCard` RGPD documentaire** (registre/DPA/DPIA). Lot 2 (cron + alertes cockpit/email 90/60/30/7) et lot 3 (blocage paramétrable planification) à venir. Upload de scan (`document_url`) différé tant qu'aucun bucket Storage HDS prêt. Voir ADR-013 (table dédiée vs colonnes éparses). |
| **DEC-113** Module conformité réglementaire — lot 2 alertes in-app dérivées | Phase 06.34 2026-06-08 | LOCKED | Deuxième lot du module Conformité (CdC §5.21). **Q1 IN-APP uniquement** : cockpit (régulateur) + dashboard (dirigeant). Email/push = branchement d'infra REPOUSSÉ (principe : on construit la fonctionnalité, pas l'infra). La logique expose ses données pour qu'un canal externe futur les consomme sans refonte. **Q2 DÉRIVÉ** : statut/alerte calculés à la volée depuis `compliance_items` vs date du jour. PAS de table d'alertes stockées, PAS d'accusé de lecture (une échéance reste visible tant qu'elle n'est pas réglée). Validé par RETEX secteur. **Cron pg_cron : NON** au lot 2 (affichage dérivé suffit ; le cron servira au futur canal externe). **Helper pur `selectComplianceAlerts` + `countComplianceAlerts`** dans `@tap/shared` (9 tests Vitest verts) : filtre `soon`/`expired`, tri par urgence (expired du plus passé en haut puis soon ascendant). **Query serveur `getComplianceAlerts`** (`'server-only'`) partagée cockpit + dashboard, hydrate labels d'entité applicativement (pas de FK polymorphe SQL, cf. ADR-013). **`<ComplianceAlertsPanel>`** variants `panel` (cockpit, dense, limit 4, sous AlertsPanel courses) et `card` (dashboard, bordée, grid 2 cols à côté du `ComplianceCard` RGPD, limit 5). Statut accessible (icône + texte + jours, WCAG 1.4.1). **Pas de terracotta** (pas un moment-clé d'action ; les alertes sont une vigilance, l'action concrète vit dans `/admin/conformite`). 0 migration BDD, 0 cron, 0 dépendance. Lot 3 (blocage paramétrable planification) à venir. Pas d'ADR (pas de nouveau modèle, cohérent ADR-013). |
| **DEC-114** Module conformité réglementaire — lot 3 contrôle planification souple/paramétrable | Phase 06.35 2026-06-08 | LOCKED | Troisième et **dernier** lot du module Conformité (CdC §5.21). **Q4 souple-par-défaut** : avertissement visible, la régulatrice garde la main. Paramétrable vers dur sur décision dirigeant. RETEX secteur/FOSS : visibilité prime sur verrou. **Q5 optimiseur INCLUS** (sinon contournement). **Q6 granularité GLOBALE** : réglage org unique (pas par type d'échéance V1). **Migration** `compliance_blocking_mode text not null default 'warn' check (in 'warn','block')` sur `organizations`. **Helper pur `entityComplianceState` + `isPlanningBlocking`** (`@tap/shared`, 8 tests Vitest) — agrège plusieurs échéances d'une entité. **Assign-modal** : badge « Non conforme » (icône+texte WCAG 1.4.1) ; mode `warn` (panneau + toast warning, submit OK) ; mode `block` (ligne désactivée, submit refusé client + **revérif serveur dans `assignRideAction`** — defense in depth). **Optimiseur (route `/api/optimizer`)** : warn = tous au solveur + signalement `complianceWarnings` ; block = véhicules non conformes filtrés du pool + `complianceWarnings.blocked=true`. UI `optimization-shell` affiche panneau dédié. Extension rétro-compatible `OptimizationProposal.complianceWarnings?`. **`<BlockingModeControl>` SegmentedControl Avertir/Bloquer** dans `/admin/conformite` (dirigeant) + `updateComplianceBlockingModeAction` (DEC-041 row-count check). **Module Conformité §5.21 COMPLET (lots 1+2+3)**. Pas d'ADR (colonne simple, cohérent ADR-013). |
| **DEC-115** Incarnation du header / navigation principale (app + admin) | Phase 06.36 2026-06-08 | LOCKED | Incarnation du header global selon direction §3 (« bleu profond = la structure de confiance : navigation active, liens, en-têtes de section, identité »). **Angle mort** des lots 06.24 → 06.32 — le header restait générique. **D-01** : `NavTabs` onglet actif `text-foreground` → **`text-primary font-medium`** + soulignement `bg-primary` 2px → **3px pleine opacité** (`rounded-t-sm`). Double signal (couleur + poids + soulignement, WCAG 1.4.1). **D-02** : logo « **TAP** » en `text-primary text-base font-semibold` (présence de marque) ; « Régulation » reste muted. **D-03** : `shadow-sm` ajouté au header (border-b + shadow-sm) pour la séparation avec le contenu sans alourdir. Sticky + backdrop-blur conservés. **D-04** : factorisation `components/app-header.tsx` (Server Component avec slot `extras` pour `DraftQueue` côté `(app)`), les deux layouts passent de ~30 lignes de header dupliqué à 1 ligne. **Contraste WCAG vérifié** : `text-primary` jour 9.42:1 (AAA), nuit 5.21:1 (AA). **Header chauffeur PWA hors périmètre** (grammaire dédiée DEC-014). 0 migration BDD, 0 dépendance, 0 régression. Pas d'ADR (activation de pattern + factorisation rétro-compatible). |
| **DEC-116** Exports courses CSV + statistiques + PDF récap chauffeur (§5.23 partiel) | Phase 06.37 2026-06-08 | LOCKED | Comble une partie du creux CdC §5.23 avec les briques **déjà en place** : zéro dépendance, zéro achat. **NB** : numéroté 06.37/DEC-116 (le prompt cadrait 06.36/DEC-115 mais ces numéros ont été utilisés par l'incarnation header — collision évitée). **D-01** : `exportRidesCsvAction` (pattern `exportCaisseCsvAction`) — zod `from/to/driverId?/status?/transportMode?`, guard `requireAdminOrRegulateur`, query RLS scoppée + hydratation labels patient/chauffeur, colonnes claires (date RDV, patient, départ, destination, mode, statut, chauffeur, tarif). Tarif lu de `tarif_amount_eur` déjà calculé (0 recalcul de pricing). UI `<ExportCsvButton>` dans la toolbar `RidesList`. **D-02** : `exportStatsCsvAction` (guard dirigeant) — **réutilise `getDashboardData()`**, 0 duplication d'agrégat. Sections volume/incidents/chauffeurs/CA + comparatif N-1. UI `<ExportStatsButton>` dans le slot `actions` du PageHeader tableau de bord. **D-03** : Route `/api/admin/chauffeurs/recap/pdf` (runtime nodejs, renderToStream) — params `driver/from/to`, guard dirigeant, **audit log AVANT** le rendu (`chauffeur.recap.exported_pdf`), réutilise `PdfDocument` + `PdfSection` + `pdfStyles` (charte commune avec facturation CGSS). UI `<RecapPdfButton>` dans `DriverRowActions` (visible dirigeant uniquement, dialog mini-form from/to). **Lomaco** (format externe) et **FEC normé** (à délibérer) **RESTENT REPOUSSÉS** (registre des travaux). 0 migration BDD, 0 nouvelle dépendance, 0 régression. RGPD minimisation respectée (pas de données médicales exportées). Pas d'ADR (réutilisation de patterns existants). |

</decisions>

---
*Last updated: 2026-06-08 — DEC-116 LOCKED (exports §5.23 partiel).*
