---
phase: 06.7-or-tools-optimisation-de-tourn-es
plan: 03
type: execute
wave: 3
depends_on: [06.7-02]
files_modified:
  - services/optimizer/vercel.json
  - services/optimizer/README.md
  - apps/web/src/app/api/optimizer/route.ts
  - apps/web/src/app/(app)/courses/actions/assignment.ts
  - apps/web/src/app/(app)/courses/actions/index.ts
  - apps/web/src/app/(app)/cockpit/optimisation/page.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/loading.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_lib/use-optimization.client.ts
  - apps/web/src/app/(app)/cockpit/optimisation/_lib/hauts-citycodes.ts
  - apps/web/src/app/(app)/cockpit/optimisation/_components/optimization-shell.client.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_components/comparative-view.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_components/proposed-group-card.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_components/adjust-sheet.client.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_components/indicators-band.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_components/excluded-rides-section.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_components/hauts-badge.tsx
  - apps/web/src/app/(app)/cockpit/optimisation/_components/apply-confirmation-dialog.client.tsx
  - apps/web/src/app/(app)/cockpit/_components/cockpit-content.client.tsx
  - apps/web/tests/e2e/optimizer-golden-path.spec.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - docs/showcase/06.7-or-tools-optimisation-de-tournees/README.md
autonomous: false
requirements: [OPTI-01, OPTI-02, OPTI-03, OPTI-04, OPTI-05]

must_haves:
  truths:
    - "Le Route Handler POST /api/optimizer est protégé : seul un régulateur ou un dirigeant authentifié peut l'appeler"
    - "Le Route Handler dé-identifie le payload (D-08) : aucun nom patient, NIR ou note médicale n'est lu depuis rides ni envoyé au solveur"
    - "L'écran /cockpit/optimisation affiche la vue comparative plan actuel / plan proposé, les 2 indicateurs estimés, la section courses exclues"
    - "La régulatrice accepte / rejette / ajuste chaque groupement au grain fin ; rien n'est écrit en base avant la validation finale"
    - "La validation finale écrit le véhicule suggéré via une Server Action (RLS + trigger audit_logs) — aucune écriture directe"
    - "Le cockpit a un bouton 'Optimiser la journée' qui ouvre l'écran d'optimisation pour la journée courante"
    - "Le golden path Playwright passe : lancer l'optimisation -> proposition rendue -> accepter un groupement -> appliquer"
    - "Décisions CONTEXT.md honorées par ce plan, traçabilité du gate de couverture : D-01, D-02, D-03, D-05, D-07, D-08, D-10, D-14, D-15, D-16, D-17, D-18, D-19, D-20"
  artifacts:
    - path: "apps/web/src/app/api/optimizer/route.ts"
      provides: "Route Handler : auth, lecture rides du jour, dé-identification, appel solveur"
      contains: "POST"
    - path: "apps/web/src/app/(app)/cockpit/optimisation/page.tsx"
      provides: "Page Server Component de l'écran d'optimisation"
      contains: "OptimizationShell"
    - path: "apps/web/src/app/(app)/cockpit/optimisation/_components/proposed-group-card.tsx"
      provides: "Carte de groupement avec 3 actions (accepter/rejeter/ajuster)"
      contains: "Accepter"
    - path: "apps/web/src/app/(app)/cockpit/optimisation/_lib/use-optimization.client.ts"
      provides: "Hook : appel au solveur, machine d'états, décisions par groupement"
      contains: "useOptimization"
    - path: "apps/web/tests/e2e/optimizer-golden-path.spec.ts"
      provides: "Test E2E golden path de l'optimisation"
      contains: "optimizer golden path"
  key_links:
    - from: "apps/web/src/app/api/optimizer/route.ts"
      to: "@tap/optimizer-client"
      via: "appel à solve() et utilisation de ridesToSolveRequest"
      pattern: "from '@tap/optimizer-client'"
    - from: "apps/web/src/app/(app)/cockpit/optimisation/_lib/use-optimization.client.ts"
      to: "/api/optimizer"
      via: "fetch POST vers le Route Handler"
      pattern: "fetch\\(.*api/optimizer"
    - from: "apps/web/src/app/(app)/cockpit/optimisation/_components/apply-confirmation-dialog.client.tsx"
      to: "apps/web/src/app/(app)/courses/actions/assignment.ts"
      via: "appel à la Server Action d'affectation de véhicule"
      pattern: "assignVehicle|VehicleAction"
    - from: "apps/web/src/app/(app)/cockpit/_components/cockpit-content.client.tsx"
      to: "/cockpit/optimisation"
      via: "lien du bouton Optimiser la journée"
      pattern: "cockpit/optimisation"
---

<objective>
Livrer l'écran d'optimisation de tournées dans le cockpit régulateur, le Route Handler qui orchestre l'appel au solveur avec dé-identification (D-08), la Server Action d'application du véhicule suggéré, le test E2E golden path, et la clôture documentaire de la Phase 06.7.

Purpose: C'est la Wave 3 — la couche visible (Visible Progress, CLAUDE.md §13.5). Elle consomme `@tap/optimizer-client` (Wave 2) qui consomme le service Python (Wave 1). L'écran applique le contrat UI-SPEC §7-§16 : propose/dispose au grain fin, aucune écriture avant validation, écriture finale via Server Action (audit auto).
Output: Écran `/cockpit/optimisation` fonctionnel sur la preview Vercel, golden path E2E vert, documents de phase à jour, showcase produit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-CONTEXT.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-UI-SPEC.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-PATTERNS.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-RESEARCH.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-02-PLAN.md

<notes>
- AUCUNE migration BDD dans cette phase. AUCUNE idempotence : l'appel d'optimisation est un calcul pur sans effet de bord en base (D-18, D-14). NE PAS importer `withIdempotency`, NE PAS toucher la table `idempotency_keys`, NE PAS ajouter `mutation_type = 'optimize_rides'`. La décision D-07 et la recommandation RESEARCH.md §"Idempotence" sont explicitement remplacées par l'arbitrage du brief de phase.
- IMPORTANT — Server Action d'application : la Server Action existante `assignRideAction` (`apps/web/src/app/(app)/courses/actions/assignment.ts`) EXIGE un `driverId`. Or D-03 met l'affectation chauffeur hors V1 — la proposition V1 suggère un VÉHICULE seulement. Il faut donc AJOUTER une nouvelle Server Action `assignVehicleAction` dans le même fichier, suivant exactement le même pattern (zod -> getAuthContext -> vérif statut -> UPDATE `vehicle_id` -> revalidatePath). Ce n'est PAS une migration : c'est une Server Action de plus dans un fichier existant, conforme à D-16 (Server Action + RLS + trigger audit_logs). Le trigger `rides_audit_trigger` capture déjà `vehicle_id`.
- La proposition est éphémère (D-18) : aucune persistance. La fermeture de l'écran jette l'état. L'écriture finale ne concerne que `vehicle_id`.
- Tous les composants <= 150 LOC, tous les fichiers <= 300 LOC, fonctions <= 50 LOC (CLAUDE.md §11). PATTERNS.md §"File Size Constraints" : si un composant dépasse, le découper.
- Test policy CLAUDE.md §9 / ADR-003 : 1 seul E2E Playwright golden path. PAS de test unitaire sur les composants React. Le smoke preview existant (`preview.spec.ts`) ne doit pas régresser.
- NFR-001 : aucun nom propre dans les mockups/seed. En production réelle, le nom patient s'affiche (donnée opérationnelle, pas un mockup) — l'exécuteur utilise les vrais champs. Mais le payload solveur reste dé-identifié (D-08).
</notes>

<interfaces>
<!-- Contrat consommé depuis @tap/optimizer-client (Plan 02). -->

Depuis @tap/optimizer-client :
  solve(request: SolveRequest, options: { baseUrl: string; timeoutMs: number }): Promise<SolveResponse>
  OptimizerError  (code: 'TIMEOUT' | 'UNAVAILABLE' | 'CONTRACT_MISMATCH' | 'VALIDATION')
  ridesToSolveRequest(rides: RideRow[], vehicles: VehicleRow[], opts): { payload, excluded }
  solveResponseToProposal(response, totalRides, excluded): OptimizationProposal
  Types : SolveRequest, SolveResponse, Groupement, OptimizationProposal, ExcludedRide, RideRow, VehicleRow

Server Action à AJOUTER (assignment.ts) :
  assignVehicleAction(args: { rideId: string; vehicleId: string }): Promise<ActionState>
    // pattern identique à assignRideAction mais n'écrit que vehicle_id, ne touche pas status ni driver_id

État du hook use-optimization :
  type OptimizationState =
    | { status: 'idle' }
    | { status: 'loading' }
    | { status: 'result'; proposal: OptimizationProposal }
    | { status: 'empty' }
    | { status: 'error'; message: string }
</interfaces>
</context>

<tasks>

<task type="manual_then_auto">
  <name>Task 0: Déploiement Vercel du service Python optimizer + URL preview</name>
  <files>services/optimizer/vercel.json (CRÉER), services/optimizer/README.md (METTRE À JOUR — section Déploiement)</files>
  <read_first>
    - docs/adr/ADR-008-hebergement-services-optimizer.md (décision Vercel Python serverless ; contraintes `time_limit` 3 s prod + mesure cold start + repli Clever Cloud)
    - .planning/PROJECT.md — DEC-079 LOCKED (discipline (a) `time_limit` 3 s en production, (b) mesure obligatoire du cold start, (c) repli Clever Cloud documenté, (d) payload dé-identifié maintenu D-08)
    - vercel.json racine actuel (`framework: nextjs` — ne déploie aujourd'hui que `apps/web`)
    - https://vercel.com/docs/functions/runtimes/python (runtime Python + détection d'entrypoint)
    - https://vercel.com/docs/services (option « Services » multi-runtimes dans un seul projet)
    - https://github.com/vercel/next.js/discussions/90367 (discussion option « deux projets Vercel séparés »)
  </read_first>
  <action>
    DEUX OPTIONS D'ARCHITECTURE DE DÉPLOIEMENT — TRANCHER AU LANCEMENT puis appliquer une seule.

    **Option A — un seul projet Vercel, deux Services** : ajouter un `services/optimizer/vercel.json` qui déclare le service Python (entrypoint `main.py`, framework FastAPI auto-détecté via `requirements.txt`), et configurer le projet Vercel racine pour exposer le service Python sous un sous-chemin URL (par exemple `/optimizer/*` route vers le service Python, le reste vers Next.js). Côté `apps/web`, `OPTIMIZER_SERVICE_URL` pointe vers `https://<deployment>.vercel.app/optimizer` (même domaine, pas de CORS à gérer).

    **Option B — deux projets Vercel séparés** : créer un projet Vercel distinct pour `services/optimizer` (root directory = `services/optimizer`, framework Python détecté). L'URL est sur un sous-domaine séparé (ex. `tap-optimizer-<hash>.vercel.app`). Côté `apps/web`, `OPTIMIZER_SERVICE_URL` pointe vers cette URL. Plus simple à configurer, deux pipelines de build distincts, deux quotas free-tier indépendants.

    Tracer le choix Option A vs B dans une section « Décision Task 0 » du SUMMARY de phase et en ajoutant un complément à `docs/adr/ADR-008-hebergement-services-optimizer.md` (la décision d'architecture est plus fine que ce que l'ADR initial pose).

    **Fichier `services/optimizer/vercel.json`** (quelle que soit l'option) — configurer :
    - `maxDuration: 10` (limite Hobby, en cohérence avec `time_limit` solveur plafonné à 3 s en production).
    - Variable d'environnement `OPTIMIZER_TIME_LIMIT_SECONDS=3` (consommée par le solveur en prod ; le défaut local reste 5 s).

    **`apps/web` côté Vercel** — déclarer `OPTIMIZER_SERVICE_URL` (preview + production) pointant vers l'URL finale du service Python. La variable n'a pas à être confidentielle (URL publique).

    **Mesure cold start (DEC-079 (c) — exigence)** — après le premier déploiement, exécuter 10 appels consécutifs au `/health` après > 5 min d'inactivité, mesurer p50/p95. Documenter le résultat dans le SUMMARY de phase. Si p95 > 5 s, déclencher le repli Clever Cloud (DEC-079 (c)) et bloquer Wave 3 sur ce point.

    **`services/optimizer/README.md`** — section Déploiement mise à jour : préciser que l'hébergement est Vercel Python serverless (DEC-079), citer la voie retenue (A ou B), documenter la procédure de mesure cold start, et noter que le `Dockerfile` est conservé en repli (Clever Cloud).
  </action>
  <verify>
    <automated>test -f services/optimizer/vercel.json && grep -q "maxDuration" services/optimizer/vercel.json && curl -sf "$OPTIMIZER_SERVICE_URL/health" | grep -q '"status":"ok"' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `services/optimizer/vercel.json` créé, déclare `maxDuration` et `OPTIMIZER_TIME_LIMIT_SECONDS=3`.
    - Choix Option A ou B documenté dans le SUMMARY de phase + complément ADR-008.
    - Le service Python répond `200 OK` sur `<OPTIMIZER_SERVICE_URL>/health` depuis la preview Vercel.
    - Mesure cold start réalisée (10 appels consécutifs après inactivité), p50/p95 documentés. Si p95 > 5 s, escalade vers Clever Cloud (repli DEC-079 (c)).
    - `OPTIMIZER_SERVICE_URL` configuré dans Vercel (preview + production).
    - Aucun nom de fournisseur autre que Vercel (ou Clever Cloud cité explicitement en repli) dans le code, la config ou la doc. NFR-001 respecté.
  </acceptance_criteria>
  <done>Le service Python est joignable depuis Vercel à l'URL configurée. La Task 1 peut consommer `process.env.OPTIMIZER_SERVICE_URL` sans surprise.</done>
</task>

<task type="auto">
  <name>Task 1: Route Handler /api/optimizer (dé-identification) et Server Action d'application véhicule</name>
  <files>apps/web/src/app/api/optimizer/route.ts, apps/web/src/app/(app)/courses/actions/assignment.ts, apps/web/src/app/(app)/courses/actions/index.ts</files>
  <read_first>
    - apps/web/src/app/(app)/courses/actions/assignment.ts (fichier à modifier — pattern Server Action à dupliquer pour assignVehicleAction)
    - apps/web/src/app/(app)/courses/actions/_shared.ts (REGULATEUR_OR_DIRIGEANT, ActionState)
    - apps/web/src/app/(app)/courses/actions/index.ts (barrel à mettre à jour)
    - apps/web/src/app/api/driver/rides/[rideId]/start/route.ts (analog exact du Route Handler : zod safeParse, auth, Supabase, error handling)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-PATTERNS.md (§"route.ts" lignes 321-391 ; §"Shared Patterns" lignes 784-832)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-RESEARCH.md (§"Pattern 6" lignes 595-653 ; §"Open Questions" Q2 facteur de correction)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-CONTEXT.md (D-08 dé-identification, D-16 écriture via Server Action, D-17 exclusion coords)
    - packages/optimizer-client/src/transform.ts (créé en Plan 02 — ridesToSolveRequest, types RideRow/VehicleRow)
    - CLAUDE.md (§6 sécurité ; §10 pattern mutation ; §11 fonctions <= 50 LOC)
  </read_first>
  <action>
    `assignment.ts` — AJOUTER `assignVehicleAction(args: { rideId: string; vehicleId: string }): Promise<ActionState>` : schéma zod `{ rideId: z.string().uuid(), vehicleId: z.string().uuid() }` ; `getAuthContext` ; garde `REGULATEUR_OR_DIRIGEANT` (message FR « Seul un régulateur ou un dirigeant peut affecter un véhicule. ») ; vérifier que la course existe et que son `status` autorise l'affectation (statut `validee` ou `assignee` — accepter les deux puisqu'on ne change que le véhicule, pas le chauffeur) ; `UPDATE rides SET vehicle_id = ..., updated_by = ctx.userId WHERE id = rideId` (NE PAS toucher `status` ni `driver_id`) ; `revalidatePath('/courses')` et `revalidatePath('/cockpit')` ; retourner `{ success: true, id: rideId }`. Le trigger `rides_audit_trigger` capture automatiquement le changement de `vehicle_id` (D-16). Fonction <= 50 LOC.
    `actions/index.ts` — exporter `assignVehicleAction` depuis le barrel.
    `route.ts` — créer le Route Handler `POST /api/optimizer`. Importer `solve`, `ridesToSolveRequest`, `SolveRequestSchema` depuis `@tap/optimizer-client` et `createClient` depuis `@/lib/supabase/server`.
    1. Body : `z.object({ date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/) })`, `safeParse` -> 400 « Payload invalide. » si échec.
    2. Auth : `createClient()` ; `supabase.auth.getUser()` -> 401 « Non authentifié. » si pas d'utilisateur. Vérifier le rôle applicatif (régulateur ou dirigeant) — réutiliser le helper d'auth disponible (`getAuthContext` ou équivalent) ; 403 « Accès non autorisé. » sinon.
    3. Lecture rides : `supabase.from('rides').select('id, scheduled_at, urgency, transport_mode, pickup_lat, pickup_lng, dropoff_lat, dropoff_lng, pickup_citycode, dropoff_citycode')` sur la journée `date` (`gte`/`lt` sur `scheduled_at`), statut `validee`. CRITIQUE D-08 : le `select` NE DOIT JAMAIS inclure `patient_id`, ni joindre la table `patients`, ni lire `nir`/`nom`/`prenom`/notes médicales.
    4. Lecture véhicules : `supabase.from('vehicles').select('id, type, places_assises, places_tpmr')` (véhicules actifs).
    5. Lire le facteur de correction : `tariff_grids.facteur_correction_routier` de la grille active (RESEARCH.md Q2) ; à défaut, constante `1.3` documentée.
    6. `ridesToSolveRequest(rides, vehicles, { date, depot, correctionFactor, avgSpeedKmh: 50, timeLimitSeconds: 3 })`. Si `payload.rides.length === 0` (toutes exclues / aucune course) -> retourner une réponse « vide » que l'UI rendra en état vide, SANS appeler le solveur.
    7. `solve(payload, { baseUrl: process.env.OPTIMIZER_SERVICE_URL!, timeoutMs: 5000 })`. Entourer d'un `try/catch` sur `OptimizerError` -> 503 avec message FR sobre, sans code HTTP brut.
    8. `solveResponseToProposal(response, totalRides, excluded)` et renvoyer le `OptimizationProposal` en JSON 200.
    Aucun log du contenu des courses (même dé-identifié). Fonctions <= 50 LOC — extraire la lecture Supabase dans un helper si le handler dépasse.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck 2>&1 | tail -15 && grep -q "assignVehicleAction" src/app/\(app\)/courses/actions/assignment.ts && grep -q "POST" src/app/api/optimizer/route.ts && ! grep -qE "patient_id|\\.from\\('patients'\\)|nir" src/app/api/optimizer/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/api/optimizer/route.ts` exporte `POST` ; le handler rejette en 401 si non authentifié et en 403 si le rôle n'est ni régulateur ni dirigeant.
    - Le `select` Supabase du Route Handler ne contient AUCUNE des sous-chaînes `patient_id`, `patients`, `nir`, `nom`, `prenom` (vérifiable par grep).
    - `assignVehicleAction` existe dans `assignment.ts`, écrit uniquement `vehicle_id` (pas `status`, pas `driver_id`), est gardée par `REGULATEUR_OR_DIRIGEANT`.
    - `assignVehicleAction` est exportée depuis `actions/index.ts`.
    - `pnpm typecheck` dans `apps/web` passe.
    - Le Route Handler n'importe pas `withIdempotency` et ne référence pas `idempotency_keys`.
    - Aucun fichier modifié ne dépasse 300 lignes.
  </acceptance_criteria>
  <done>Le Route Handler /api/optimizer lit les courses du jour sans aucune PII, appelle le solveur via `@tap/optimizer-client`, et retourne une proposition. La Server Action `assignVehicleAction` permet d'écrire le véhicule suggéré via le pattern audité existant.</done>
</task>

<task type="auto">
  <name>Task 2: Écran d'optimisation — page, hook, composants UI et bouton cockpit</name>
  <files>apps/web/src/app/(app)/cockpit/optimisation/page.tsx, apps/web/src/app/(app)/cockpit/optimisation/loading.tsx, apps/web/src/app/(app)/cockpit/optimisation/_lib/use-optimization.client.ts, apps/web/src/app/(app)/cockpit/optimisation/_lib/hauts-citycodes.ts, apps/web/src/app/(app)/cockpit/optimisation/_components/optimization-shell.client.tsx, apps/web/src/app/(app)/cockpit/optimisation/_components/comparative-view.tsx, apps/web/src/app/(app)/cockpit/optimisation/_components/proposed-group-card.tsx, apps/web/src/app/(app)/cockpit/optimisation/_components/adjust-sheet.client.tsx, apps/web/src/app/(app)/cockpit/optimisation/_components/indicators-band.tsx, apps/web/src/app/(app)/cockpit/optimisation/_components/excluded-rides-section.tsx, apps/web/src/app/(app)/cockpit/optimisation/_components/hauts-badge.tsx, apps/web/src/app/(app)/cockpit/optimisation/_components/apply-confirmation-dialog.client.tsx, apps/web/src/app/(app)/cockpit/_components/cockpit-content.client.tsx</files>
  <read_first>
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-UI-SPEC.md (§7-§17 intégral : surfaces, copywriting, états, composants, anti-patterns)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-PATTERNS.md (§"page.tsx" 394-432, §"loading.tsx" 434-460, §"optimization-shell" 464-493, §"comparative-view" 496-521, §"proposed-group-card" 524-558, §"adjust-sheet" 562-598, §"indicators-band" 601-628, §"excluded-rides-section" 632-644, §"hauts-badge" 648-664, §"apply-confirmation-dialog" 668-723, §"use-optimization" 727-755, §"cockpit/page.tsx modification" 759-778)
    - apps/web/src/app/(app)/cockpit/page.tsx (analog page Server Component)
    - apps/web/src/app/(app)/cockpit/_components/cockpit-content.client.tsx (fichier à modifier — ajout du bouton dans le header)
    - apps/web/src/app/(app)/tableau-de-bord/loading.tsx (analog skeleton)
    - apps/web/src/app/(app)/cockpit/_components/alert-card.client.tsx (analog carte avec ton conditionnel)
    - apps/web/src/app/(admin)/admin/tarifs/_components/tariff-edit-sheet.client.tsx (analog Sheet)
    - apps/web/src/app/(app)/tableau-de-bord/_components/kpi-card.tsx (analog affichage indicateur)
    - apps/web/src/app/(app)/courses/_components/ride-badges.tsx (analog badge sémantique)
    - apps/web/src/app/(admin)/admin/chauffeurs/_components/deactivate-confirm-dialog.client.tsx (analog dialog de confirmation)
    - apps/web/src/app/(app)/cockpit/_lib/use-cockpit-rides.ts (analog hook async)
    - packages/shared/src/constants/villes-974.ts (codes INSEE 974 — base pour hauts-citycodes.ts)
    - packages/optimizer-client/src/index.ts (types et fonctions consommés)
    - CLAUDE.md (§2 design system, §5 règles UI, §11 composant <= 150 LOC)
  </read_first>
  <action>
    Créer les 12 fichiers de l'écran + modifier le header du cockpit. Appliquer le contrat UI-SPEC §7-§16 (copywriting exact §6, spacing NFR-003, couleurs §5, icônes Lucide §16). Chaque composant <= 150 LOC.
    `_lib/hauts-citycodes.ts` : exporter `HAUTS_CITYCODES` (set de codes INSEE des Hauts) et un helper `groupTraverseHauts(rideCitycodes: string[]): boolean`. Inclure au minimum `97413` (Cilaos) et `97433` (Salazie) — codes confirmés dans `villes-974.ts`. Pour les codes `[ASSUMED]` de RESEARCH.md §"Zones de Hauts" (Sainte-Rose, Bourg-Murat, Entre-Deux), ajouter ceux présents dans `villes-974.ts` et un commentaire `# TODO UAT D-10` pour les autres. Aucun nom propre dans le code (NFR-001 — les commentaires peuvent nommer les communes, le code n'expose que des codes INSEE).
    `page.tsx` : Server Component. `metadata = { title: 'Optimisation' }`, `dynamic = 'force-dynamic'`. Lire `date` depuis `searchParams` (défaut : jour courant) ; `createClient` + `supabase.auth.getUser()` -> `redirect('/login')` si absent. Lire les courses du jour (`rides` — pour le panneau « Plan actuel », lecture seule ; ici le nom patient EST affiché car écran opérationnel réel, mais le payload solveur reste dé-identifié côté Route Handler). Passer `initialRides` et `date` à `<OptimizationShell>`.
    `loading.tsx` : skeleton deux colonnes (`grid sm:grid-cols-2`), blocs `Skeleton` de hauteur variable, aucun spinner (UI-SPEC §7).
    `_lib/use-optimization.client.ts` : hook `useOptimization(date)`. Sur déclenchement, `fetch('/api/optimizer', { method: 'POST', body: JSON.stringify({ date }) })`. Machine d'états `OptimizationState` (voir `<interfaces>`). Gérer les décisions par groupement dans une `Map<string, 'idle' | 'accepted' | 'rejected'>` + un état d'ajustements. Exposer `{ state, launch(), accept(id), reject(id), adjust(id, adjusted), acceptedGroupements }`. Aucune souscription Supabase (proposition éphémère, D-18). NE PAS envoyer de header `Idempotency-Key` dans le `fetch` de `/api/optimizer` : l'appel d'optimisation est un calcul pur sans idempotence (voir note du plan ligne 100). La section PATTERNS.md §"use-optimization" qui suggère ce header est explicitement caduque sur ce point.
    `optimization-shell.client.tsx` : orchestrateur `'use client'`. Reçoit `initialRides` et `date`. Appelle `useOptimization`. Rend selon l'état : skeleton + message « Calcul en cours… » (loading), `<ComparativeView>` (result), état vide (empty, copywriting §6), état erreur + bouton « Réessayer » (error). Bouton « Appliquer les groupements acceptés » en bas, ouvrant `<ApplyConfirmationDialog>`. Bouton « Fermer » variante `ghost`.
    `comparative-view.tsx` : Server Component (sans état). Grille `grid grid-cols-2 gap-24` (`grid-cols-1` < lg avec `aria-label` sur la colonne réordonnée, UI-SPEC §8). Colonne gauche `<ol>` plate des courses (lecture seule). Colonne droite : `<IndicatorsBand>` en haut, cartes `<ProposedGroupCard>`, courses non groupées, `<ExcludedRidesSection>`.
    `proposed-group-card.tsx` : `'use client'`. Carte `rounded-lg border bg-muted/40 p-16`, `hover:shadow-md` 150 ms. En-tête h3 « Groupement N », `<HautsBadge>` conditionnel. `<ol>` ordonnée des courses. Véhicule suggéré (icône `Car`). Motif + gain estimé. 3 boutons « Accepter » / « Rejeter » / « Ajuster » (min-h 44px, icônes `Check`/`X`/`SlidersHorizontal`). État local `'idle' | 'accepted' | 'rejected'` -> bordure gauche verte/rouge + badge. « Ajuster » ouvre `<AdjustSheet>`.
    `adjust-sheet.client.tsx` : `Sheet` latéral droit 480px. Checkboxes pour retirer des courses du groupement, `<select>` pour changer le véhicule. CTA « Valider l'ajustement » / « Abandonner les modifications ». Aucune Server Action — état local uniquement (D-18).
    `indicators-band.tsx` : Server Component. `bg-muted/60 rounded-lg border`, `grid grid-cols-2 gap-32 p-16`. Exactement 2 indicateurs (D-19) : « Taux de mutualisation estimé » + « Km à vide estimés ». Valeurs `text-2xl font-semibold tabular-nums`, libellés `text-xs text-muted-foreground`. Avertissement italique §6. Aucune couleur sémantique, aucun graphique.
    `excluded-rides-section.tsx` : Server Component. `bg-amber-50 border-amber-200 rounded-lg p-16`, icône `Info`. `<ul>` des courses exclues. Rendu UNIQUEMENT si `excludedRides.length > 0` (D-17). Au-delà de 5 items, bouton « Afficher toutes ».
    `hauts-badge.tsx` : Server Component. `Badge` variant `outline`, `text-amber-700 border-amber-300 bg-amber-50`, icône `AlertTriangle` 12px, texte « À vérifier », infobulle `title` (`tabIndex={0}`).
    `apply-confirmation-dialog.client.tsx` : `Dialog` de confirmation. `useTransition` pour appeler `assignVehicleAction` pour chaque course des groupements acceptés. Sur succès : `toast.success` Sonner « {N} groupement(s) enregistré(s). » + `router.refresh()`. CTA « Appliquer les groupements », retour « Revenir à la proposition ».
    `cockpit-content.client.tsx` : MODIFIER le header — ajouter `<Button asChild variant="default">` avec un `<Link href={\`/cockpit/optimisation?date=${today}\`}>` et l'icône `Sparkles` (`mr-8 h-16 w-16`, `aria-hidden`), `min-h-[44px]`, à côté du `RealtimeStatusBadge`. Ajouter `data-testid="optimize-day-btn"` pour le E2E.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck 2>&1 | tail -15 && pnpm lint 2>&1 | tail -10 && test -f src/app/\(app\)/cockpit/optimisation/page.tsx && grep -q "optimize-day-btn" src/app/\(app\)/cockpit/_components/cockpit-content.client.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 12 fichiers de l'écran existent sous `apps/web/src/app/(app)/cockpit/optimisation/`.
    - `cockpit-content.client.tsx` contient un bouton avec `data-testid="optimize-day-btn"` liant vers `/cockpit/optimisation`.
    - `indicators-band.tsx` n'affiche QUE 2 indicateurs et chaque libellé contient le mot « estimé » (D-19/D-20).
    - `excluded-rides-section.tsx` n'est pas rendu quand la liste est vide (D-17).
    - `proposed-group-card.tsx` a 3 boutons d'action ; aucun libellé « automatique » n'apparaît dans l'écran (D-14, anti-pattern UI-SPEC §17).
    - `pnpm typecheck` et `pnpm lint` passent dans `apps/web`.
    - Aucun composant ne dépasse 150 lignes, aucun fichier ne dépasse 300 lignes.
    - Aucune nouvelle dépendance npm ajoutée (composants shadcn existants suffisent — UI-SPEC §14).
  </acceptance_criteria>
  <done>L'écran `/cockpit/optimisation` est complet : vue comparative, 2 indicateurs estimés, courses exclues, cartes de groupement avec accept/reject/ajuster, dialog de confirmation, bouton d'entrée dans le cockpit. typecheck et lint verts.</done>
</task>

<task type="auto">
  <name>Task 3: Test E2E golden path et clôture documentaire de la phase</name>
  <files>apps/web/tests/e2e/optimizer-golden-path.spec.ts, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, docs/showcase/06.7-or-tools-optimisation-de-tournees/README.md</files>
  <read_first>
    - apps/web/tests/e2e/ (lister les specs E2E existantes pour copier le pattern de login régulateur et la configuration Playwright)
    - apps/web/tests/smoke/preview.spec.ts (pattern de connexion compte démo)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-RESEARCH.md (§"Playwright golden path" lignes 958-977)
    - .planning/ROADMAP.md (bloc « ### Phase 06.7 » lignes 378-383 ; ligne checklist 45)
    - .planning/REQUIREMENTS.md (table de traçabilité lignes 324-328 : OPTI-01..05 mappés à tort « Phase 10 »)
    - .planning/PROJECT.md (table des décisions — chercher DEC-079/080/081 en statut CANDIDATE ; format des entrées LOCKED)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-CONTEXT.md (§"Décisions candidates" lignes 119-128)
    - CLAUDE.md (§13.5 Visible Progress Mandate ; §9 politique de test E2E)
  </read_first>
  <action>
    `optimizer-golden-path.spec.ts` : un seul test E2E Playwright. Login régulateur (compte démo `regulateur@demo.tap` / `demo1234!` — pattern du smoke `preview.spec.ts`). Naviguer `/cockpit`. Cliquer `[data-testid="optimize-day-btn"]`. Vérifier que le skeleton/état chargement apparaît. Attendre la première carte `proposed-group-card` (timeout 10 s). Accepter le premier groupement (`data-testid="group-0-accept"`). Vérifier l'état accepté. Cliquer « Appliquer les groupements acceptés », confirmer dans le dialog, vérifier le toast Sonner contenant « enregistré ». Ajouter les `data-testid` correspondants dans les composants de la Task 2 si absents — coordonner les noms : `optimize-day-btn` (bouton cockpit), `optimization-skeleton` (bloc skeleton de `optimization-shell`, état chargement), `proposed-group-card` (conteneur de carte de groupement, portant aussi `data-state` idle/accepted/rejected — le E2E cible la première carte via `.first()`), `group-0-accept` (bouton Accepter de la première carte), `apply-groupements-btn` (bouton « Appliquer les groupements acceptés »), `confirm-apply-btn` (bouton de confirmation du dialog), `sonner-toast` (toast Sonner de succès). Ce test est le golden path unique de la phase (CLAUDE.md §9) — pas d'autres E2E.
    `.planning/ROADMAP.md` : (1) cocher la ligne checklist Phase 06.7 (ligne ~45) -> `[x]` avec une mention « Livré 2026-05-22 ». (2) Dans le bloc « ### Phase 06.7 » : corriger le `**Goal**` — remplacer « affectation courses ↔ chauffeurs » par le périmètre réel V1 (mutualisation + ordonnancement, véhicule suggéré, journée entière ; affectation chauffeur hors V1 -> Phase 06.7-bis) conformément à D-01/D-03. Remplacer `**Plans**: TBD` par la liste des 3 plans livrés. Ajouter une ligne `**Status**: Complete`.
    `.planning/REQUIREMENTS.md` : dans la table de traçabilité (lignes ~324-328), changer le mapping de OPTI-01, OPTI-02, OPTI-03, OPTI-04, OPTI-05 de « Phase 10 » vers « Phase 06.7 » et le statut de « Pending » vers « Complete ». Cocher aussi les 5 lignes `OPTI-0x` de la liste des exigences (lignes ~119-123) -> `[x]`.
    `.planning/PROJECT.md` : promouvoir les 3 décisions candidates en LOCKED dans la table des décisions, en suivant le format des entrées LOCKED existantes : DEC-079 (périmètre V1 = mutualisation + ordonnancement sur la journée entière ; affectation chauffeur et ré-optimisation hors V1), DEC-080 (fenêtres horaires dérivées par règle depuis `urgency`, sans migration BDD), DEC-081 (indicateurs = taux de mutualisation + km à vide, affichés « estimés »). Référencer CONTEXT.md D-01/D-02/D-03/D-11/D-19/D-20.
    `docs/showcase/06.7-or-tools-optimisation-de-tournees/README.md` : créer le dossier showcase et un `README.md` court (FR, ton sobre, sans nom propre, sans émoji) décrivant l'artefact attendu : capture(s) d'écran ou GIF de l'écran d'optimisation, walkthrough en 5-10 étapes pour la régulatrice (aller sur /cockpit, cliquer « Optimiser la journée », observer la proposition, accepter un groupement, appliquer). Indiquer que la capture sera produite depuis la preview Vercel après merge (CLAUDE.md §13.5).
    Note : s'il existe un fichier CONCERNS.md avec un item « optimisation », le résoudre — sinon ne rien faire (le brief le mentionne comme conditionnel).
  </action>
  <verify>
    <automated>test -f apps/web/tests/e2e/optimizer-golden-path.spec.ts && grep -q "optimizer golden path" apps/web/tests/e2e/optimizer-golden-path.spec.ts && grep -q "Phase 06.7" .planning/REQUIREMENTS.md && ! grep -E "OPTI-0[1-5].*Phase 10" .planning/REQUIREMENTS.md && grep -q "DEC-079" .planning/PROJECT.md && test -f docs/showcase/06.7-or-tools-optimisation-de-tournees/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/tests/e2e/optimizer-golden-path.spec.ts` existe et contient un test nommé « optimizer golden path » couvrant : login -> /cockpit -> Optimiser -> proposition -> accepter -> appliquer -> toast.
    - `.planning/REQUIREMENTS.md` : aucune ligne ne mappe `OPTI-0x` à « Phase 10 » ; les 5 sont mappées « Phase 06.7 » et « Complete ».
    - `.planning/ROADMAP.md` : la ligne Phase 06.7 est cochée `[x]` ; le `**Goal**` ne contient plus « affectation courses ↔ chauffeurs ».
    - `.planning/PROJECT.md` contient DEC-079, DEC-080, DEC-081 en statut LOCKED.
    - `docs/showcase/06.7-or-tools-optimisation-de-tournees/README.md` existe, en français, sans nom propre ni émoji.
  </acceptance_criteria>
  <done>Le golden path E2E couvre le parcours complet. ROADMAP, REQUIREMENTS et PROJECT sont à jour : Phase 06.7 livrée, OPTI-01..05 tracées sur la bonne phase, DEC-079/080/081 verrouillées. Le dossier showcase est prêt pour la capture preview.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vérification visuelle de l'écran d'optimisation sur la preview</name>
  <what-built>
    L'écran `/cockpit/optimisation` complet : bouton « Optimiser la journée » dans le cockpit, vue comparative plan actuel / plan proposé, bande de 2 indicateurs estimés, cartes de groupement avec accept/rejeter/ajuster, section courses exclues, dialog de confirmation. Le Route Handler `/api/optimizer` (dé-identifié) et la Server Action `assignVehicleAction`. Le service Python est déployé sur Vercel (Task 0, DEC-079/ADR-008) et `OPTIMIZER_SERVICE_URL` configuré côté `apps/web`.
  </what-built>
  <how-to-verify>
    1. Ouvrir la preview Vercel de la PR de phase, se connecter avec `regulateur@demo.tap` / `demo1234!`.
    2. Sur `/cockpit`, vérifier la présence du bouton « Optimiser la journée » (icône Sparkles) dans l'en-tête.
    3. Cliquer le bouton : un état de chargement (skeleton + « Calcul en cours… ») doit s'afficher, jamais de page blanche.
    4. La proposition doit s'afficher : 2 colonnes (Plan actuel / Plan proposé), bande d'indicateurs avec « Taux de mutualisation estimé » et « Km à vide estimés » (et le mot « estimé » visible), au moins une carte de groupement.
    5. Accepter un groupement : la carte passe en bordure verte + badge « Accepté ».
    6. Cliquer « Ajuster » sur un groupement : le Sheet latéral s'ouvre, permet de retirer une course / changer le véhicule.
    7. Cliquer « Appliquer les groupements acceptés », confirmer : un toast de succès apparaît.
    8. Vérifier qu'aucun libellé ne suggère une affectation automatique (D-14) et qu'aucun 3e indicateur n'est présent (D-19).
    9. Si des courses sans coordonnées existent dans le seed, vérifier la section « Courses non incluses dans le calcul ».
  </how-to-verify>
  <resume-signal>Taper « approuvé » si l'écran se comporte comme décrit, ou décrire les frictions constatées.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigateur régulatrice → Route Handler /api/optimizer | Entrée utilisateur (date). Frontière d'authentification et d'autorisation. |
| Route Handler → base Supabase (lecture rides) | Lecture de données de santé. La requête doit exclure toute PII destinée au solveur (D-08). |
| Route Handler → service Python (non-HDS) | Le payload traverse l'internet vers le service Python hébergé sur Vercel (non-HDS, DEC-079). La dé-identification est faite ici. |
| Dialog de confirmation → Server Action assignVehicleAction | Écriture en base. RLS + trigger audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-09 | Elevation of Privilege | `POST /api/optimizer` appelé par un utilisateur non autorisé | mitigate | Le Route Handler vérifie `supabase.auth.getUser()` (401 sinon) ET le rôle applicatif régulateur/dirigeant (403 sinon) avant toute lecture de `rides`. |
| T-06.7-10 | Information Disclosure | PII (nom, NIR, note médicale) envoyée au solveur non-HDS | mitigate | Le `select` Supabase du Route Handler liste explicitement les colonnes — aucune jointure `patients`, aucun `patient_id`/`nir`. `ridesToSolveRequest` (Plan 02) mappe champ par champ. Vérification grep en acceptance criteria. |
| T-06.7-11 | Tampering | Écriture du véhicule contournant l'audit | mitigate | `assignVehicleAction` est une Server Action ('use server') : zod + garde de rôle + `UPDATE rides` ; le trigger `rides_audit_trigger` journalise dans `audit_logs` (D-16). Aucune écriture directe depuis le client. |
| T-06.7-12 | Denial of Service | Service optimizer indisponible bloque la régulatrice | mitigate | `solve()` a un timeout 5 s ; le Route Handler rattrape `OptimizerError` et renvoie 503 ; `optimization-shell.client.tsx` affiche l'état d'erreur avec « Réessayer » sans bloquer le cockpit (D-14, UI-SPEC §13). |
| T-06.7-13 | Information Disclosure | Stack trace ou code HTTP affiché à l'utilisateur en cas d'erreur service | mitigate | Messages d'erreur FR sobres prédéfinis (copywriting UI-SPEC §6) ; aucune interpolation de statut HTTP ni de stack dans l'UI (CLAUDE.md §13). |
| T-06.7-SC | Tampering | Installs npm | mitigate | Aucune nouvelle dépendance : tous les composants UI sont des composants shadcn déjà présents (UI-SPEC §14) ; `@tap/optimizer-client` est un workspace interne. Pas de checkpoint requis. |
</threat_model>

<verification>
- `pnpm typecheck` et `pnpm lint` verts dans `apps/web`.
- `grep -E "patient_id|patients|nir" apps/web/src/app/api/optimizer/route.ts` : aucun résultat.
- Le Route Handler n'importe pas `withIdempotency`.
- `pnpm --filter web test:e2e -- --grep "optimizer golden path"` : vert sur la preview.
- Le smoke test preview existant (`preview.spec.ts`) ne régresse pas.
- `.planning/REQUIREMENTS.md` ne mappe plus OPTI-0x à Phase 10.
- Aucune migration ajoutée sous `supabase/migrations/`.
</verification>

<success_criteria>
- L'écran `/cockpit/optimisation` applique le contrat UI-SPEC : propose/dispose au grain fin, 2 indicateurs estimés, courses exclues visibles, aucun libellé « automatique ».
- Le Route Handler dé-identifie le payload (D-08) et est protégé par auth + rôle.
- L'écriture finale du véhicule passe par `assignVehicleAction` (RLS + audit trigger, D-16).
- Le golden path Playwright est vert (OPTI-01..05 prouvés de bout en bout).
- ROADMAP, REQUIREMENTS et PROJECT sont à jour ; DEC-079/080/081 verrouillées ; showcase prêt.
- Aucune migration BDD ; aucune idempotence ajoutée.
</success_criteria>

<output>
Créer `.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-03-SUMMARY.md` à la fin, incluant l'URL de preview Vercel et le walkthrough script (CLAUDE.md §13.5).
</output>

## Couverture des décisions CONTEXT.md

Décisions de `06.7-CONTEXT.md` implémentées par ce plan (traçabilité du gate de couverture) :

- D-01 — vue comparative, groupements ordonnés affichés à la régulatrice.
- D-02 — optimisation portant sur la journée entière (date du cockpit).
- D-03 — véhicule suggéré uniquement, aucune affectation chauffeur automatique en V1.
- D-05 — timeout du Route Handler aligné sur l'appel synchrone borné.
- D-07 — aucune idempotence sur `/api/optimizer`, calcul pur sans effet de bord.
- D-08 — dé-identification du payload appliquée et vérifiée dans le Route Handler.
- D-10 — badge « À vérifier » sur les groupements traversant les zones de Hauts.
- D-14 — propose/dispose, rien n'est écrit en base avant validation.
- D-15 — acceptation au grain fin, accepter / rejeter / ajuster chaque groupement.
- D-16 — écriture finale via la Server Action `assignVehicleAction`, audit automatique.
- D-17 — section « courses non incluses » visible.
- D-18 — proposition éphémère, aucune souscription, aucune persistance.
- D-19 — bande de deux indicateurs, tous deux libellés « estimés ».
- D-20 — aucun indicateur présenté comme une mesure terrain.
