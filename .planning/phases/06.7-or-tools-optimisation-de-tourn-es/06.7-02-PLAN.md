---
phase: 06.7-or-tools-optimisation-de-tourn-es
plan: 02
type: execute
wave: 2
depends_on: [06.7-01]
files_modified:
  - packages/optimizer-client/package.json
  - packages/optimizer-client/tsconfig.json
  - packages/optimizer-client/vitest.config.ts
  - packages/optimizer-client/src/index.ts
  - packages/optimizer-client/src/contract.ts
  - packages/optimizer-client/src/client.ts
  - packages/optimizer-client/src/transform.ts
  - packages/optimizer-client/src/__tests__/contract.test.ts
  - packages/optimizer-client/src/__tests__/transform.test.ts
autonomous: true
requirements: [OPTI-02]

must_haves:
  truths:
    - "Le package @tap/optimizer-client expose un contrat zod (SolveRequestSchema, SolveResponseSchema, GroupementSchema) miroir exact du contrat pydantic du service Python"
    - "Le contrat porte un CONTRACT_VERSION littéral ('1') validé dans les deux sens — un payload sans contract_version est rejeté"
    - "La fonction solve() appelle le service HTTP, valide la réponse par SolveResponseSchema.safeParse, et lève OptimizerError typée en cas de timeout / indisponibilité / dérive de contrat"
    - "transform.ts transforme les courses DB en SolveRequest dé-identifié (jamais de champ patient/nom/nir) et exclut les courses sans coordonnées"
    - "transform.ts transforme la SolveResponse en OptimizationProposal avec le taux de mutualisation calculé"
    - "Les tests Vitest passent (transform: exclusion coords, no PII, taux mutualisation ; contract: rejet version manquante, roundtrip)"
    - "Décisions CONTEXT.md honorées par ce plan, traçabilité du gate de couverture : D-04, D-05, D-07, D-08, D-17, D-18, D-21"
  artifacts:
    - path: "packages/optimizer-client/src/contract.ts"
      provides: "Schémas zod du contrat solveur + types inférés + CONTRACT_VERSION"
      contains: "SolveRequestSchema"
    - path: "packages/optimizer-client/src/client.ts"
      provides: "Client HTTP typé solve() + classe OptimizerError"
      contains: "OptimizerError"
    - path: "packages/optimizer-client/src/transform.ts"
      provides: "Transformations pures rides->SolveRequest et SolveResponse->OptimizationProposal"
      contains: "ridesToSolveRequest"
    - path: "packages/optimizer-client/package.json"
      provides: "Workspace pnpm @tap/optimizer-client"
      contains: "@tap/optimizer-client"
    - path: "packages/optimizer-client/src/__tests__/transform.test.ts"
      provides: "Tests Vitest des transformations (OPTI-02)"
      contains: "describe"
  key_links:
    - from: "packages/optimizer-client/src/client.ts"
      to: "packages/optimizer-client/src/contract.ts"
      via: "import des schémas zod pour valider requête et réponse"
      pattern: "from './contract'"
    - from: "packages/optimizer-client/src/transform.ts"
      to: "packages/optimizer-client/src/contract.ts"
      via: "import des types SolveRequest / SolveResponse"
      pattern: "from './contract'"
    - from: "packages/optimizer-client/src/index.ts"
      to: "packages/optimizer-client/src/contract.ts"
      via: "ré-export du contrat depuis le barrel"
      pattern: "export \\* from './contract'"
---

<objective>
Créer le package TypeScript `packages/optimizer-client` : le client typé qui isole l'application Next.js du service Python OR-Tools (OPTI-02). Le package porte le contrat partagé (schémas zod), le client HTTP `solve()` avec timeout et validation bidirectionnelle, et les transformations pures (courses DB → payload solveur dé-identifié ; réponse solveur → proposition affichable).

Purpose: C'est le chemin critique de cohérence solveur↔client. Le contrat zod défini ici est le miroir exact du contrat pydantic livré en Wave 1 (Plan 01). Tout écart casse l'écran d'optimisation — le `contract_version` littéral et le `safeParse` bidirectionnel empêchent toute dérive silencieuse. La Wave 3 (Plan 03) consomme ce package.
Output: Un workspace pnpm `@tap/optimizer-client` complet, testé par Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-CONTEXT.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-RESEARCH.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-PATTERNS.md
@.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-01-PLAN.md

<notes>
- Le contrat pydantic de référence est défini dans le bloc `<interfaces>` du Plan 01 (lignes 105-145) et dans `services/optimizer/models.py` (livré en Wave 1). Le contrat zod de CE plan en est le miroir EXACT. `CONTRACT_VERSION = '1'`.
- Aucune migration BDD dans cette phase. Aucune idempotence : l'appel d'optimisation est un calcul pur sans effet de bord en base (D-18, D-14) — naturellement idempotent. NE PAS importer `withIdempotency`, NE PAS utiliser la table `idempotency_keys`. La décision D-07 (idempotence via idempotency_keys) est explicitement remplacée par cet arbitrage du brief de phase.
- Le package est un workspace pnpm pur (analog exact : `packages/pricing`). `pnpm-workspace.yaml` contient déjà le glob `packages/*` — le `package.json` à créer suffit à l'enregistrer.
- `client.ts` est isomorphe (utilisé côté serveur depuis le Route Handler en Wave 3) : il utilise `fetch` global + `AbortController`. NE PAS y mettre `import 'server-only'` (le package doit rester importable hors du contexte Next.js, notamment pour Vitest).
</notes>

<interfaces>
<!-- Contrat zod miroir du contrat pydantic du Plan 01. Source de vérité partagée. -->

CONTRACT_VERSION = '1'   // string littérale — incrémenter si le schéma change

SolveRequestSchema (TS -> Python) :
  contract_version: z.literal('1')
  date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/)
  rides: z.array(RideNodeSchema).min(1).max(200)
  vehicles: z.array(VehicleNodeSchema).min(1)
  depot: z.tuple([z.number(), z.number()])
  correction_factor: z.number().min(1.0).max(2.0)
  avg_speed_kmh: z.number().min(10).max(120)
  time_limit_seconds: z.number().int().min(2).max(5)

RideNodeSchema :
  id: z.string().uuid()                       // UUID opaque — JAMAIS de nom patient (D-08)
  pickup: z.tuple([z.number(), z.number()])
  dropoff: z.tuple([z.number(), z.number()])
  scheduled_at: z.string().datetime({ offset: true })
  urgency: z.enum(['programmee', 'urgente', 'immediate'])
  transport_mode: z.enum(['taxi_conventionne', 'tpmr', 'vsl', 'ambulance'])

VehicleNodeSchema :
  id: z.string().uuid()
  type: z.enum(['taxi', 'tpmr', 'vsl', 'ambulance'])
  places_assises: z.number().int().min(1).max(20)
  places_tpmr: z.number().int().min(0).max(4)

SolveResponseSchema (Python -> TS) :
  contract_version: z.literal('1')
  groupements: z.array(GroupementSchema)
  rides_non_groupees_ids: z.array(z.string().uuid())
  rides_exclues_ids: z.array(z.string().uuid())
  km_a_vide_estimes: z.number().min(0)

GroupementSchema :
  vehicle_id: z.string().uuid()
  ride_ids: z.array(z.string().uuid()).min(2)
  order: z.array(z.string().uuid())
  motif: z.string().max(120)
  gain_km_a_vide: z.number().min(0)

OptimizationProposal (sortie de transform.ts — type local du package) :
  groupements: Groupement[]
  ridesNonGroupeesIds: string[]
  excludedRides: ExcludedRide[]          // courses sans coords (filtrées côté transform)
  tauxMutualisation: number              // pourcentage entier 0..100
  kmAVideEstimes: number

ExcludedRide :
  id: string
  reason: 'no_coordinates'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffolding du workspace et contrat zod partagé</name>
  <files>packages/optimizer-client/package.json, packages/optimizer-client/tsconfig.json, packages/optimizer-client/vitest.config.ts, packages/optimizer-client/src/index.ts, packages/optimizer-client/src/contract.ts, packages/optimizer-client/src/__tests__/contract.test.ts</files>
  <read_first>
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-PATTERNS.md (§"package.json" lignes 80-113, §"tsconfig.json" lignes 115-129, §"vitest.config.ts" lignes 131-162, §"index.ts" lignes 165-174, §"contract.ts" lignes 177-202)
    - packages/pricing/package.json (analog exact du package.json)
    - packages/pricing/tsconfig.json (analog exact du tsconfig)
    - packages/pricing/vitest.config.ts (analog exact de la config Vitest)
    - packages/shared/src/validators/ride.ts (pattern zod : z.object, z.enum, z.infer)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-01-PLAN.md (bloc <interfaces> lignes 105-145 : contrat pydantic de référence à mirorer)
    - CLAUDE.md (§7 conventions TS, kebab-case ; §11 fichiers <= 300 LOC)
  </read_first>
  <action>
    `package.json` : copier le pattern de `packages/pricing/package.json`. `"name": "@tap/optimizer-client"`, `"version": "0.0.0"`, `"private": true`, `"type": "module"`, `main`/`types` pointant `./src/index.ts`, `exports` `"."` -> `./src/index.ts`. Scripts `typecheck` (`tsc --noEmit`), `test` (`vitest run --coverage`), `clean`. Ajouter `"zod": "*"` sous `dependencies` (zod est utilisé au runtime par `contract.ts`). `devDependencies` : `@vitest/coverage-v8`, `typescript`, `vitest` aux mêmes versions que `packages/pricing`.
    `tsconfig.json` : `extends "../../tsconfig.base.json"`, `outDir "dist"`, `rootDir "src"`, `include ["src/**/*"]`.
    `vitest.config.ts` : copier `packages/pricing/vitest.config.ts`. `environment: 'node'`, `env: { TZ: 'Indian/Reunion' }`. Coverage v8, `include: ['src/**/*.ts']`, `exclude: ['src/index.ts', 'src/**/__tests__/**']`. Seuils de couverture : NE PAS exiger 100% (CLAUDE.md §9 — 100% réservé à pricing/recurrence ; ici cible raisonnable sur la logique non triviale). Mettre les seuils `branches`/`functions`/`lines`/`statements` à une valeur atteignable par les tests de la Task 3, ou retirer le bloc `thresholds` si l'exécuteur ne peut pas garantir un chiffre — documenter le choix en commentaire.
    `src/index.ts` : barrel — `export * from './contract'; export * from './client'; export * from './transform';`.
    `src/contract.ts` : définir les schémas zod EXACTEMENT selon le bloc `<interfaces>` du contexte — `RideNodeSchema`, `VehicleNodeSchema`, `SolveRequestSchema`, `GroupementSchema`, `SolveResponseSchema`. Exporter la constante `CONTRACT_VERSION = '1'` et l'utiliser dans `z.literal(CONTRACT_VERSION)` pour `contract_version`. Exporter les types inférés via `z.infer` : `SolveRequest`, `SolveResponse`, `Groupement`, `RideNode`, `VehicleNode`. Aucune logique, uniquement des schémas et types.
    `src/__tests__/contract.test.ts` : tests Vitest (`describe`/`it`/`expect`) — (1) `SolveRequestSchema.safeParse` d'un payload sans `contract_version` retourne `success: false` ; (2) un payload valide minimal passe `safeParse` avec `success: true` ; (3) roundtrip : un `SolveResponse` valide parse correctement et un objet avec `contract_version: '2'` est rejeté (dérive de version détectée).
  </action>
  <verify>
    <automated>cd packages/optimizer-client && pnpm install --filter @tap/optimizer-client 2>/dev/null; pnpm --filter @tap/optimizer-client test -- contract 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `packages/optimizer-client/package.json` a `"name": "@tap/optimizer-client"` et `"zod"` sous `dependencies`.
    - `packages/optimizer-client/src/contract.ts` exporte `SolveRequestSchema`, `SolveResponseSchema`, `GroupementSchema`, `RideNodeSchema`, `VehicleNodeSchema`, `CONTRACT_VERSION`, et les types `SolveRequest`/`SolveResponse`/`Groupement`.
    - `contract_version` est défini par `z.literal('1')` dans `SolveRequestSchema` ET `SolveResponseSchema`.
    - `pnpm --filter @tap/optimizer-client test -- contract` passe (rejet version manquante, payload valide accepté, rejet version '2').
    - Tous les fichiers <= 300 lignes.
  </acceptance_criteria>
  <done>Le workspace `@tap/optimizer-client` est enregistré dans pnpm, le contrat zod est défini et miroir exact du contrat pydantic, les tests de contrat passent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client HTTP typé solve() avec timeout et validation</name>
  <files>packages/optimizer-client/src/client.ts</files>
  <read_first>
    - packages/optimizer-client/src/contract.ts (créé en Task 1 — schémas à importer)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-RESEARCH.md (§"Pattern 5 : Client HTTP typé avec timeout" lignes 542-591 ; §"Common Pitfalls" Pitfall 3 et Pitfall 4)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-PATTERNS.md (§"client.ts" lignes 206-251)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-CONTEXT.md (D-05 : appel synchrone borné, timeout HTTP > time_limit solveur)
    - CLAUDE.md (§11 : fonctions <= 50 LOC ; §13 messages FR sans code technique)
  </read_first>
  <behavior>
    - Test : `solve()` avec un mock `fetch` retournant un `SolveResponse` valide retourne l'objet parsé.
    - Test : `solve()` avec un mock `fetch` qui ne répond pas avant `timeoutMs` lève `OptimizerError` de code `'TIMEOUT'`.
    - Test : `solve()` avec un mock `fetch` retournant un statut HTTP non-2xx lève `OptimizerError` de code `'UNAVAILABLE'`.
    - Test : `solve()` avec un mock `fetch` retournant un JSON invalide (champ manquant ou `contract_version: '2'`) lève `OptimizerError` de code `'CONTRACT_MISMATCH'`.
  </behavior>
  <action>
    `src/client.ts` : implémenter le client HTTP isomorphe (pas de `server-only`). Importer `SolveRequestSchema`, `SolveResponseSchema`, `SolveRequest`, `SolveResponse` depuis `./contract`.
    Définir la classe `OptimizerError extends Error` avec une propriété `readonly code: 'TIMEOUT' | 'UNAVAILABLE' | 'CONTRACT_MISMATCH' | 'VALIDATION'`.
    Définir et exporter la fonction `solve(request: SolveRequest, options: { baseUrl: string; timeoutMs: number }): Promise<SolveResponse>` :
    1. valider `request` via `SolveRequestSchema.parse` avant l'envoi (échec -> `OptimizerError('...', 'VALIDATION')`) ;
    2. `AbortController` + `setTimeout(timeoutMs)` -> sur abort, lever `OptimizerError` code `'TIMEOUT'` avec message FR sobre « Le service d'optimisation a dépassé le délai imparti. » ;
    3. `fetch(\`${baseUrl}/solve\`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(validated), signal })` ;
    4. statut non-2xx -> `OptimizerError` code `'UNAVAILABLE'` message FR « Le service d'optimisation n'est pas disponible. » ;
    5. `SolveResponseSchema.safeParse(await res.json())` -> si `!success` lever `OptimizerError` code `'CONTRACT_MISMATCH'` message FR « Réponse du service d'optimisation inattendue. » ;
    6. `clearTimeout` dans un `finally`.
    Aucun message d'erreur ne doit contenir de code HTTP brut ni de stack trace (CLAUDE.md §13). Fonction `solve()` <= 50 LOC (extraire un helper si besoin).
  </action>
  <verify>
    <automated>cd packages/optimizer-client && pnpm --filter @tap/optimizer-client test -- client 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `packages/optimizer-client/src/client.ts` exporte `solve` et `OptimizerError`.
    - `OptimizerError` a une propriété `code` du type union `'TIMEOUT' | 'UNAVAILABLE' | 'CONTRACT_MISMATCH' | 'VALIDATION'`.
    - `pnpm --filter @tap/optimizer-client test -- client` passe (4 cas : succès, timeout, indisponible, contrat divergent).
    - Aucun message d'`OptimizerError` ne contient un code HTTP numérique brut ni de stack trace.
    - `client.ts` ne contient pas `import 'server-only'`.
  </acceptance_criteria>
  <done>Le client `solve()` appelle le service, applique un timeout strict, valide la réponse par zod, et lève une `OptimizerError` typée et lisible en français pour chaque mode d'échec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Transformations pures rides->SolveRequest et SolveResponse->proposition</name>
  <files>packages/optimizer-client/src/transform.ts, packages/optimizer-client/src/__tests__/transform.test.ts</files>
  <read_first>
    - packages/optimizer-client/src/contract.ts (créé en Task 1 — types SolveRequest/SolveResponse/Groupement)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-RESEARCH.md (§"Pattern 6" lignes 595-653 : dé-identification ; §"Scénarios Vitest détaillés" lignes 925-954)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-PATTERNS.md (§"transform.ts" lignes 255-283 ; §"transform.test.ts" lignes 286-318)
    - packages/pricing/src/compute-cgss-short-trip.ts (analog : fonction de transformation pure typée)
    - packages/pricing/src/__tests__/compute-cgss-short-trip.test.ts (analog : pattern fixture factory + describe/it)
    - .planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-CONTEXT.md (D-08 dé-identification, D-17 exclusion courses sans coords, D-19 taux mutualisation)
    - CLAUDE.md (§11 fonctions <= 50 LOC ; NFR-001 aucun nom propre)
  </read_first>
  <behavior>
    - Test `ridesToSolveRequest` exclut les courses sans coordonnées : 1 course avec coords + 1 sans -> `payload.rides` longueur 1, `excluded` longueur 1 avec `reason: 'no_coordinates'`.
    - Test `ridesToSolveRequest` ne produit jamais de PII : `JSON.stringify(payload)` ne contient aucune des sous-chaînes `patient`, `nom`, `nir`.
    - Test `ridesToSolveRequest` mappe correctement `urgency` et `transport_mode` dans chaque `RideNode`.
    - Test `solveResponseToProposal` calcule le taux de mutualisation : réponse avec 1 groupement de 2 courses sur 4 courses totales -> `tauxMutualisation === 50`.
    - Test `solveResponseToProposal` avec 0 groupement -> `tauxMutualisation === 0`.
  </behavior>
  <action>
    `src/transform.ts` : définir et exporter deux fonctions PURES (aucun `fetch`, aucun accès Supabase, aucun effet de bord) et les types locaux `OptimizationProposal` et `ExcludedRide` (voir bloc `<interfaces>` du contexte).
    Définir un type d'entrée `RideRow` représentant une ligne de la table `rides` telle que lue par le Route Handler (Wave 3) : `id`, `scheduled_at`, `urgency`, `transport_mode`, `pickup_lat`, `pickup_lng`, `dropoff_lat`, `dropoff_lng` (les `*_lat`/`*_lng` sont `number | null`), `pickup_citycode`, `dropoff_citycode`. Définir aussi un type `VehicleRow` (`id`, `type`, `places_assises`, `places_tpmr`).
    1. `ridesToSolveRequest(rides: RideRow[], vehicles: VehicleRow[], opts: { date: string; depot: [number, number]; correctionFactor: number; avgSpeedKmh: number; timeLimitSeconds: number }): { payload: SolveRequest; excluded: ExcludedRide[] }` — filtrer les courses dont `pickup_lat`/`pickup_lng`/`dropoff_lat`/`dropoff_lng` est `null` -> `excluded` (`reason: 'no_coordinates'`) ; mapper les courses restantes en `RideNode` (uniquement `id` opaque + géométrie + horaires + contraintes — JAMAIS de champ patient, D-08) ; mapper `vehicles` en `VehicleNode` ; assembler le `SolveRequest` avec `contract_version: CONTRACT_VERSION` et les `opts`. Si toutes les courses sont exclues, `payload.rides` peut être vide — le Route Handler décidera de ne pas appeler le solveur (le schéma exige min 1 ; documenter ce cas en commentaire, la décision finale est dans le Route Handler du Plan 03).
    2. `solveResponseToProposal(response: SolveResponse, totalRides: number, excluded: ExcludedRide[]): OptimizationProposal` — calculer `tauxMutualisation` = round( (somme des `ride_ids.length` de tous les groupements) / `totalRides` × 100 ), 0 si `totalRides === 0` ; reporter `groupements`, `ridesNonGroupeesIds`, `kmAVideEstimes` ; intégrer `excluded`.
    Toutes les fonctions <= 50 LOC, max 3 niveaux d'imbrication.
    `src/__tests__/transform.test.ts` : implémenter les 5 tests du bloc `<behavior>`. Utiliser des factories `buildTestRide(overrides)` et `buildTestVehicle(overrides)` (pattern `baseInput` de `compute-cgss-short-trip.test.ts`). Données fictives sans nom propre (NFR-001) — utiliser des UUID littéraux et des coordonnées numériques.
  </action>
  <verify>
    <automated>cd packages/optimizer-client && pnpm --filter @tap/optimizer-client test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `packages/optimizer-client/src/transform.ts` exporte `ridesToSolveRequest`, `solveResponseToProposal`, et les types `OptimizationProposal`, `ExcludedRide`, `RideRow`, `VehicleRow`.
    - `pnpm --filter @tap/optimizer-client test` passe : tests de contrat + client + transform tous verts.
    - Le test « no PII » confirme que `JSON.stringify(payload)` ne contient ni `patient`, ni `nom`, ni `nir`.
    - `solveResponseToProposal` retourne `tauxMutualisation === 50` pour 1 groupement de 2 courses sur 4 totales.
    - `transform.ts` n'importe ni `fetch`, ni un client Supabase — fonctions pures.
    - Toutes les fonctions <= 50 lignes ; tous les fichiers <= 300 lignes.
  </acceptance_criteria>
  <done>Les transformations pures sont livrées et testées : courses DB -> payload dé-identifié sans PII avec exclusion des courses sans coordonnées ; réponse solveur -> proposition avec taux de mutualisation. La suite Vitest du package est entièrement verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application Next.js → package optimizer-client | Le package transforme des données métier (courses) en payload solveur. Frontière de dé-identification (D-08). |
| package optimizer-client → service Python (HTTP) | `solve()` envoie le payload sur le réseau vers un hébergeur non-HDS. La validation zod sortante est la dernière barrière côté TS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-05 | Information Disclosure | `ridesToSolveRequest` pourrait copier un champ patient dans le payload | mitigate | `RideNode` du contrat zod n'a aucun champ patient ; `ridesToSolveRequest` mappe explicitement champ par champ (jamais de spread de la ligne DB). Le test Vitest « no PII » échoue si `patient`/`nom`/`nir` apparaît dans le payload sérialisé. |
| T-06.7-06 | Tampering | Dérive silencieuse entre le contrat zod et le contrat pydantic du Plan 01 | mitigate | `CONTRACT_VERSION` littéral `'1'` partagé ; `SolveResponseSchema.safeParse` côté `client.ts` lève `OptimizerError('CONTRACT_MISMATCH')` si la réponse ne correspond pas — un champ ajouté côté Python sans bump de version est détecté. |
| T-06.7-07 | Denial of Service | Réponse solveur jamais reçue (service figé) bloque l'appelant | mitigate | `AbortController` + `timeoutMs` strict dans `solve()` ; au-delà du délai, `OptimizerError('TIMEOUT')` est levée — l'appelant (Wave 3) affiche l'état d'erreur sans bloquer (D-14). |
| T-06.7-08 | Information Disclosure | Message d'erreur exposant un code HTTP ou une stack trace | mitigate | Les messages d'`OptimizerError` sont des chaînes FR sobres prédéfinies, sans interpolation de statut HTTP ni de stack (CLAUDE.md §13). |
| T-06.7-SC | Tampering | Installs npm (`zod`, `@vitest/coverage-v8`, `vitest`, `typescript`) | mitigate | Aucune nouvelle dépendance : `zod`, `vitest`, `typescript`, `@vitest/coverage-v8` sont déjà dans le repo (réutilisés par `packages/pricing`). `package.json` référence `"zod": "*"` (version workspace existante). Aucun package `[ASSUMED]`/`[SUS]` — pas de checkpoint requis. |
</threat_model>

<verification>
- `pnpm --filter @tap/optimizer-client test` : suite Vitest complète verte.
- `pnpm --filter @tap/optimizer-client typecheck` : aucune erreur TypeScript.
- `grep -rE "patient|nir" packages/optimizer-client/src/contract.ts` : aucun résultat (le contrat n'a structurellement pas de PII).
- `packages/optimizer-client/src/client.ts` ne contient pas `import 'server-only'`.
- Aucun fichier `packages/optimizer-client/src/*.ts` ne dépasse 300 lignes.
</verification>

<success_criteria>
- `@tap/optimizer-client` est un workspace pnpm valide, typecheck propre.
- Le contrat zod est le miroir exact du contrat pydantic du Plan 01 (mêmes champs, mêmes bornes, même `contract_version`).
- `solve()` appelle le service, applique un timeout borné (D-05), valide la réponse, et lève une `OptimizerError` typée pour chaque échec.
- `transform.ts` dé-identifie le payload (D-08) et exclut les courses sans coordonnées (D-17) — testé.
- La suite Vitest du package est verte (OPTI-02).
</success_criteria>

<output>
Créer `.planning/phases/06.7-or-tools-optimisation-de-tourn-es/06.7-02-SUMMARY.md` à la fin.
</output>

## Couverture des décisions CONTEXT.md

Décisions de `06.7-CONTEXT.md` implémentées par ce plan (traçabilité du gate de couverture) :

- D-04 — client TS `packages/optimizer-client` isolant le calcul du solveur.
- D-05 — timeout HTTP borné, strictement supérieur au `time_limit` du solveur.
- D-07 — aucune idempotence, l'appel d'optimisation est un calcul pur sans effet de bord ; arbitrage du propriétaire produit remplaçant la décision initiale.
- D-08 — `transform.ts` dé-identifie le payload, aucune PII transmise au solveur.
- D-17 — courses sans coordonnées exclues du calcul et signalées séparément.
- D-18 — proposition éphémère, aucune persistance, aucun cache.
- D-21 — tests Vitest sur la logique non triviale du client (contrat, transform).
