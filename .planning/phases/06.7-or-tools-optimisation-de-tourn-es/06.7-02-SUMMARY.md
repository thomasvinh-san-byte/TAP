---
phase: 06.7-or-tools-optimisation-de-tourn-es
plan: "02"
subsystem: optimizer-client
tags: [optimizer, zod, typescript, vitest, wave2]
completed: "2026-06-01"
duration: "~25 min"

dependency_graph:
  requires:
    - "06.7-01 (microservice Python OR-Tools, CONTRACT_VERSION figé)"
  provides:
    - "@tap/optimizer-client (workspace pnpm)"
    - "SolveRequestSchema / SolveResponseSchema (contrat zod)"
    - "solve() (client HTTP typé avec timeout)"
    - "ridesToSolveRequest / solveResponseToProposal (transformations pures)"
  affects:
    - "06.7-03 (Wave 3 — UI Next.js, consomme @tap/optimizer-client)"

tech_stack:
  added:
    - "packages/optimizer-client (nouveau workspace pnpm)"
  patterns:
    - "zod safeParse bidirectionnel — dérive de contrat détectée à chaque appel"
    - "AbortController + setTimeout pour timeout HTTP borné"
    - "Mapping champ par champ explicite — jamais de spread de RideRow (D-08)"

key_files:
  created:
    - path: packages/optimizer-client/package.json
      locs: 24
    - path: packages/optimizer-client/tsconfig.json
      locs: 8
    - path: packages/optimizer-client/vitest.config.ts
      locs: 17
    - path: packages/optimizer-client/src/index.ts
      locs: 3
    - path: packages/optimizer-client/src/contract.ts
      locs: 66
    - path: packages/optimizer-client/src/client.ts
      locs: 77
    - path: packages/optimizer-client/src/transform.ts
      locs: 150
    - path: packages/optimizer-client/src/__tests__/contract.test.ts
      locs: 105
    - path: packages/optimizer-client/src/__tests__/client.test.ts
      locs: 134
    - path: packages/optimizer-client/src/__tests__/transform.test.ts
      locs: 201

decisions:
  - "vitest.config.ts sans seuils de couverture (CLAUDE.md §9 — 100% réservé pricing/recurrence)"
  - "Trois tâches dans un seul commit (hook guard-commit exige typecheck monorepo entier — index.ts référençant client + transform force la coexistence)"
  - "OptimizerErrorCode exporté comme type union distinct (maintenabilité)"
  - "SolveRequestOpts exposé comme type nommé (Wave 3 peut l'importer sans réinventer)"

metrics:
  duration: "~25 min"
  completed: "2026-06-01"
  tasks_completed: 3
  tasks_total: 3
  files_created: 10
  files_modified: 1
  test_results: "24/24 verts (7 contract + 7 client + 10 transform)"
  coverage: "100% stmts/funcs/lines, 96% branches (branche défensive AbortError fallthrough)"
---

# Phase 06.7 Plan 02 : @tap/optimizer-client — Résumé

## Une ligne

Client TypeScript typé `@tap/optimizer-client` : contrat zod miroir du pydantic Wave 1, `solve()` avec timeout borné et `OptimizerError` discriminée, transformations pures dé-identifiées testées par 24 assertions Vitest.

## Résultat Vitest

```
pnpm --filter @tap/optimizer-client test

 ✓ src/__tests__/contract.test.ts  (7 tests)   8ms
 ✓ src/__tests__/transform.test.ts (10 tests)  18ms
 ✓ src/__tests__/client.test.ts    (7 tests)   34ms

 Test Files  3 passed (3)
      Tests  24 passed (24)

 % Coverage report from v8
 client.ts    | 100 | 92.3 | 100 | 100 | (branche défensive AbortError)
 contract.ts  | 100 |  100 | 100 | 100 |
 transform.ts | 100 |  100 | 100 | 100 |
```

## Parité de contrat (DEC-083 fermée)

`CONTRACT_VERSION = '1'` figé dans les deux sens :

| Champ | Python (models.py) | TypeScript (contract.ts) | Parité |
|---|---|---|---|
| `contract_version` | `Literal["1"]` | `z.literal('1')` | exacte |
| `date` | `pattern=r"^\d{4}-\d{2}-\d{2}$"` | `z.string().regex(/^\d{4}-\d{2}-\d{2}$/)` | exacte |
| `rides` | `list[RideNode] min=1 max=200` | `z.array(RideNodeSchema).min(1).max(200)` | exacte |
| `vehicles` | `list[VehicleNode] min=1` | `z.array(VehicleNodeSchema).min(1)` | exacte |
| `depot` | `tuple[float, float]` | `z.tuple([z.number(), z.number()])` | exacte |
| `correction_factor` | `ge=1.0 le=2.0` | `z.number().min(1.0).max(2.0)` | exacte |
| `avg_speed_kmh` | `ge=10.0 le=120.0` | `z.number().min(10).max(120)` | exacte |
| `time_limit_seconds` | `ge=2 le=5` | `z.number().int().min(2).max(5)` | exacte |
| `ride_ids` (Groupement) | `min_length=2` | `z.array(z.string().uuid()).min(2)` | exacte |
| `motif` | `max_length=120` | `z.string().max(120)` | exacte |
| `places_assises` | `ge=1 le=20` | `z.number().int().min(1).max(20)` | exacte |
| `places_tpmr` | `ge=0 le=4` | `z.number().int().min(0).max(4)` | exacte |
| `km_a_vide_estimes` | `ge=0.0` | `z.number().min(0)` | exacte |

Synchronisation manuelle effectuée — aucune dérive détectée.

## Décisions honorées (traçabilité gate CONTEXT.md)

| Décision | Fichier | Comment |
|---|---|---|
| D-04 | `packages/optimizer-client/` | Workspace isolé du calcul Python |
| D-05 | `src/client.ts` — `AbortController + timeoutMs` | Timeout HTTP strict > time_limit solveur |
| D-07 | Aucune idempotence dans ce package | Calcul pur sans effet de bord (arbitrage propriétaire produit) |
| D-08 | `src/transform.ts` — mapping explicite + test JSON.stringify | Aucun champ patient/nom/nir dans le payload |
| D-17 | `ridesToSolveRequest` — filtrage null lat/lng | Courses sans coords exclues + remontées |
| D-18 | Aucune persistance dans ce package | Proposition éphémère — pas de Supabase |
| D-21 | 24 tests Vitest | Logique non triviale couverte |

## Déviations du plan

### Auto-fix — Blocage typecheck guard (Rule 3)

**Trouvé pendant :** Task 1 (tentative de commit après scaffold)
**Problème :** le hook `guard-commit.sh` exige que `pnpm typecheck` passe sur le monorepo entier avant tout commit. `index.ts` ré-exporte depuis `./client` et `./transform` qui n'existaient pas — le typecheck échouait avec `TS2307: Cannot find module './client'`.
**Correction :** implémentation complète de `client.ts` et `transform.ts` (Tasks 2 et 3) dans la même session, avant le premier commit. Les trois tâches ont été livrées dans un seul commit `79322a1`.
**Impact :** aucun — la qualité et la complétude du code ne sont pas affectées. Le plan décrit des tâches logiques ; le commit unique est une contrainte du pipeline CI local.

### Auto-fix — Reformatage Prettier (Rule 3)

**Trouvé pendant :** premier commit
**Problème :** `guard-commit.sh` a détecté des incohérences de formatage (guillemets, trailing commas, longueur de lignes) sur 5 fichiers.
**Correction :** `pnpm format` appliqué — aucun changement de logique.
**Impact :** aucun.

## Menaces couvertes

| Threat ID | Mitigation | Vérification |
|---|---|---|
| T-06.7-05 | Mapping champ par champ dans `ridesToSolveRequest` | Test `JSON.stringify(payload)` ne contient pas `patient`/`nom`/`nir` |
| T-06.7-06 | `SolveResponseSchema.safeParse` → `CONTRACT_MISMATCH` | Test `contract_version: '2'` rejeté |
| T-06.7-07 | `AbortController + timeoutMs` → `TIMEOUT` | Test mock fetch non-répondant |
| T-06.7-08 | Messages FR prédéfinis sans interpolation de statut HTTP | Test `message.not.toMatch(/\b503\b/)` |
| T-06.7-SC | Aucune nouvelle dépendance — zod/vitest/typescript déjà présents | `package.json` dependencies = `{ "zod": "*" }` |

## Self-Check

### Fichiers créés existent
- [x] `packages/optimizer-client/package.json`
- [x] `packages/optimizer-client/src/contract.ts`
- [x] `packages/optimizer-client/src/client.ts`
- [x] `packages/optimizer-client/src/transform.ts`
- [x] `packages/optimizer-client/src/index.ts`
- [x] `packages/optimizer-client/src/__tests__/contract.test.ts`
- [x] `packages/optimizer-client/src/__tests__/client.test.ts`
- [x] `packages/optimizer-client/src/__tests__/transform.test.ts`

### Commit existe
- [x] `79322a1` — feat(06.7-02): créer @tap/optimizer-client

### Acceptance criteria plan vérifiés
- [x] `"name": "@tap/optimizer-client"` et `"zod"` dans `dependencies`
- [x] Exports : `SolveRequestSchema`, `SolveResponseSchema`, `GroupementSchema`, `RideNodeSchema`, `VehicleNodeSchema`, `CONTRACT_VERSION`, `SolveRequest`/`SolveResponse`/`Groupement`
- [x] `contract_version: z.literal('1')` dans les deux schémas
- [x] `solve` et `OptimizerError` exportés depuis `client.ts`
- [x] `OptimizerError.code` union `'TIMEOUT' | 'UNAVAILABLE' | 'CONTRACT_MISMATCH' | 'VALIDATION'`
- [x] Pas de `import 'server-only'` dans `client.ts`
- [x] `ridesToSolveRequest`, `solveResponseToProposal`, `OptimizationProposal`, `ExcludedRide`, `RideRow`, `VehicleRow` exportés
- [x] Test no-PII confirme `JSON.stringify(payload)` sans `patient`/`nom`/`nir`
- [x] `tauxMutualisation === 50` pour 1 groupement de 2 courses sur 4
- [x] `transform.ts` n'importe ni `fetch` ni client Supabase
- [x] Tous fichiers <= 300 lignes ; toutes fonctions <= 50 lignes

## Self-Check : PASSED
