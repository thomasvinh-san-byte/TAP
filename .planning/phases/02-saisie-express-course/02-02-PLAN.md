---
phase: 02-saisie-express-course
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - supabase/migrations/20260509000001_rides.sql
  - supabase/tests/rides_rls.sql
  - supabase/tests/ride_draft_rls.sql
  - supabase/tests/rides_audit.sql
  - packages/database/src/types.gen.ts
  - packages/database/package.json
autonomous: true
requirements: [SAIS-06]

must_haves:
  truths:
    - "La table public.rides existe avec les 3 enums ride_transport_mode/ride_urgency/ride_status"
    - "La table public.ride_draft existe avec author_id + organization_id + payload jsonb"
    - "RLS forcée sur rides ET ride_draft (relrowsecurity ET relforcerowsecurity = true)"
    - "Un régulateur de l'org A ne voit JAMAIS les rides ni les ride_draft de l'org B (cross-tenant strict)"
    - "Deux régulateurs MÊME ORG ne voient PAS leurs brouillons réciproques (author_id scoping)"
    - "Un INSERT/UPDATE/DELETE sur rides écrit une ligne dans audit_logs avec action='ride.insert'/'ride.update'/'ride.delete'"
    - "Aucun trigger d'audit sur ride_draft (donnée transitoire — D-10)"
    - "Les types Database TypeScript sont régénérés et exposent rides + ride_draft + enums"
  artifacts:
    - path: supabase/migrations/20260509000001_rides.sql
      provides: "Migration 004 — schéma rides + ride_draft + RLS + triggers + indexes"
      contains: "create table public.rides"
    - path: supabase/tests/rides_rls.sql
      provides: "pgTAP RLS rides — isolation org + role check + chauffeur=0 access"
      contains: "select plan("
    - path: supabase/tests/ride_draft_rls.sql
      provides: "pgTAP RLS ride_draft — author_id scoping (2 users même org)"
      contains: "select plan("
    - path: supabase/tests/rides_audit.sql
      provides: "pgTAP audit trigger rides — INSERT/UPDATE/DELETE → audit_logs"
      contains: "select plan("
    - path: packages/database/src/types.gen.ts
      provides: "Types TS générés — Database['public']['Tables']['rides'] + ['ride_draft'] + Enums"
      contains: "rides:"
  key_links:
    - from: "supabase/migrations/20260509000001_rides.sql"
      to: "public.audit_logs"
      via: "trigger rides_audit_trigger after insert or update or delete on rides"
      pattern: "create trigger rides_audit_trigger"
    - from: "supabase/migrations/20260509000001_rides.sql"
      to: "public.current_organization_id() / public.has_role()"
      via: "RLS policies"
      pattern: "current_organization_id\\(\\)"
    - from: "packages/database/src/types.gen.ts"
      to: "@tap/database public types"
      via: "supabase gen types typescript"
      pattern: "rides:"
---

<objective>
Wave 1 — Schéma de données Phase 2. Crée la migration 004 (`rides` + `ride_draft` + 3 enums + RLS forcée + triggers updated_at + trigger d'audit + indexes), promeut les 3 scaffolds pgTAP de Wave 0 en assertions GREEN complètes (RLS isolation org, RLS author_id scoping pour brouillons, audit trigger), et régénère les types TypeScript Database.

Purpose: Fournir la couche persistante exigée par D-01 (schéma rides minimal V1, futurs champs prescription_id/driver_id en commentaire), D-02 (brouillons en DB pas localStorage — RGPD + survie refresh), D-10 (audit_logs sur rides UNIQUEMENT, pas sur ride_draft transitoire), avec garanties de sécurité pgTAP (T1 cross-tenant rides, T2 cross-author brouillons, T3 audit log bypass impossible).

Output: 1 migration, 3 fichiers pgTAP GREEN, types regen committés. Push migrations = automatique via CI cloud (cd.yml sur push main).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/TAP/CLAUDE.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md

<interfaces>
<!-- Helpers DB existants (Phase 0/1) à réutiliser -->

From supabase/migrations/20260506000001_foundations.sql + 20260506000002_rls_foundations.sql:
- public.organizations(id uuid pk)
- public.profiles(id uuid pk references auth.users, organization_id, role user_role)
- public.audit_logs(id, organization_id, actor_id, actor_role, action, entity_type, entity_id, metadata, created_at)
- public.current_organization_id() returns uuid (SECURITY DEFINER, retourne profile.organization_id de auth.uid())
- public.current_user_role() returns public.user_role
- public.has_role(role public.user_role) returns boolean
- public.user_role enum : 'dirigeant' | 'regulateur' | 'chauffeur'

From supabase/migrations/20260507000001_patients.sql (pattern audit trigger ligne 199-222 à dupliquer)

From supabase/tests/patients.sql (pattern fixtures ligne 23-57) :
- Org Alpha = '11111111-1111-1111-1111-111111111111'
- Org Bravo = '22222222-2222-2222-2222-222222222222'
- alpha-dir 'aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa'
- alpha-reg 'cccccccc-cccc-cccc-cccc-cccccccccccc'
- bravo-reg 'dddddddd-dddd-dddd-dddd-dddddddddddd'
- alpha-chauffeur (vérifier dans le fichier) — utilisé pour test « chauffeur ne voit RIEN »
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Migration 004 rides + ride_draft + RLS + audit trigger</name>
  <read_first>
    - /home/user/TAP/supabase/migrations/20260507000001_patients.sql (pattern complet : enums, table, RLS, trigger audit ligne 199-222)
    - /home/user/TAP/supabase/migrations/20260506000002_rls_foundations.sql (helpers current_organization_id, has_role, user_role enum)
    - /home/user/TAP/supabase/migrations/20260508000001_legal_compliance.sql (pattern RLS forcée + helpers org)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md §D-01 + §D-02 + §D-10
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`supabase/migrations/20260509000001_rides.sql`
  </read_first>
  <action>
    1. **Vérifier la fonction set_updated_at existante** : `grep -rn "create.*function.*set_updated_at\|set_updated_at()" supabase/migrations/`. Si elle existe Phase 0/1 — la réutiliser ; sinon dupliquer le pattern depuis patients.sql.

    2. **Créer** `supabase/migrations/20260509000001_rides.sql` (≤ 280 lignes) avec ce contenu structuré exact :

       **(a) Header documentaire** (~12 lignes) :
       ```sql
       -- =============================================================================
       -- Migration 004 — Courses (rides + ride_draft)
       -- =============================================================================
       -- Crée :
       --   - 3 enums : ride_transport_mode (4), ride_urgency (3), ride_status (8)
       --   - table rides : saisie express V1 (sans pricing/recurrence/driver — futurs)
       --   - table ride_draft : brouillons RGPD-compliant (author_id scoping, pas localStorage)
       --   - RLS forcée sur les deux tables + policies (regulateur+dirigeant ; brouillons author-only)
       --   - 2 triggers updated_at + 1 trigger d'audit (rides UNIQUEMENT — D-10)
       --   - 3 indexes (org+scheduled_at, patient_idx, partial status='validee')
       -- Refs : D-01 / D-02 / D-10 ; pattern dupliqué de 20260507000001_patients.sql
       -- =============================================================================
       ```

       **(b) Enums** (cf. CONTEXT D-01) :
       ```sql
       create type public.ride_transport_mode as enum
         ('taxi_conventionne', 'tpmr', 'vsl', 'ambulance');

       create type public.ride_urgency as enum
         ('programmee', 'urgente', 'immediate');

       create type public.ride_status as enum (
         'brouillon',
         'validee',
         'assignee',
         'en_cours',
         'terminee',
         'annulee_regulateur',
         'annulee_patient',
         'annulee_chauffeur'
       );
       ```

       **(c) Table rides** (cf. CONTEXT D-01 — 18 colonnes) :
       ```sql
       create table public.rides (
         id uuid primary key default gen_random_uuid(),
         organization_id uuid not null references public.organizations(id) on delete cascade,
         patient_id uuid not null references public.patients(id),
         scheduled_at timestamptz not null,
         pickup_address text not null,
         pickup_postal_code text,
         pickup_city text,
         dropoff_address text not null,
         dropoff_postal_code text,
         dropoff_city text,
         transport_mode public.ride_transport_mode not null default 'taxi_conventionne',
         urgency public.ride_urgency not null default 'programmee',
         status public.ride_status not null default 'validee',
         notes_regulateur text,
         archive boolean not null default false,
         created_at timestamptz not null default now(),
         updated_at timestamptz not null default now(),
         created_by uuid not null references auth.users(id) on delete restrict,
         updated_by uuid not null references auth.users(id) on delete restrict,
         constraint notes_regulateur_max_500 check (char_length(notes_regulateur) <= 500)
       );

       create index rides_org_scheduled_idx on public.rides (organization_id, scheduled_at desc);
       create index rides_patient_idx on public.rides (patient_id);
       create index rides_status_validee_idx on public.rides (organization_id, status)
         where status = 'validee';
       ```

       Adapter `gen_random_uuid()` → `extensions.uuid_generate_v4()` si c'est le pattern dans patients.sql (vérifier ligne ~70).

       **(d) RLS rides** :
       ```sql
       alter table public.rides enable row level security;
       alter table public.rides force row level security;

       create policy rides_select_same_org on public.rides
         for select to authenticated
         using (organization_id = public.current_organization_id());

       create policy rides_insert_regulateur_dirigeant on public.rides
         for insert to authenticated
         with check (
           organization_id = public.current_organization_id()
           and created_by = auth.uid()
           and (
             public.has_role('regulateur'::public.user_role)
             or public.has_role('dirigeant'::public.user_role)
           )
         );

       create policy rides_update_regulateur_dirigeant on public.rides
         for update to authenticated
         using (
           organization_id = public.current_organization_id()
           and (
             public.has_role('regulateur'::public.user_role)
             or public.has_role('dirigeant'::public.user_role)
           )
         )
         with check (organization_id = public.current_organization_id());
       -- Pas de DELETE policy — archivage logique via colonne `archive`.
       ```

       **(e) Trigger audit rides** (calque patients.sql 199-222, retirer le filtre `nir_encrypted` car non applicable) :
       ```sql
       create or replace function public.rides_audit_trigger()
       returns trigger
       language plpgsql
       security definer
       set search_path = public
       as $$
       declare
         action_name text;
       begin
         action_name := 'ride.' || lower(tg_op);
         insert into public.audit_logs (
           organization_id, actor_id, actor_role,
           action, entity_type, entity_id, metadata
         ) values (
           coalesce(new.organization_id, old.organization_id),
           auth.uid(),
           public.current_user_role(),
           action_name,
           'ride',
           coalesce(new.id, old.id),
           jsonb_build_object(
             'old', case when tg_op in ('UPDATE','DELETE') then to_jsonb(old) else null end,
             'new', case when tg_op in ('INSERT','UPDATE') then to_jsonb(new) else null end
           )
         );
         return coalesce(new, old);
       end;
       $$;

       create trigger rides_audit_trigger
         after insert or update or delete on public.rides
         for each row execute function public.rides_audit_trigger();

       create trigger rides_set_updated_at
         before update on public.rides
         for each row execute function public.set_updated_at();
       ```

       **(f) Table ride_draft + RLS author-scoped** :
       ```sql
       create table public.ride_draft (
         id uuid primary key default gen_random_uuid(),
         organization_id uuid not null references public.organizations(id) on delete cascade,
         author_id uuid not null references auth.users(id) on delete cascade,
         patient_id uuid references public.patients(id) on delete set null,
         payload jsonb not null,
         created_at timestamptz not null default now(),
         updated_at timestamptz not null default now()
       );

       create index ride_draft_author_idx on public.ride_draft (author_id, updated_at desc);

       alter table public.ride_draft enable row level security;
       alter table public.ride_draft force row level security;

       -- RLS critique (Pitfall 4 RESEARCH) : DEUX prédicats — author_id ET organization_id
       create policy ride_draft_owner_all on public.ride_draft
         for all to authenticated
         using (
           author_id = auth.uid()
           and organization_id = public.current_organization_id()
         )
         with check (
           author_id = auth.uid()
           and organization_id = public.current_organization_id()
         );

       create trigger ride_draft_set_updated_at
         before update on public.ride_draft
         for each row execute function public.set_updated_at();
       -- PAS de trigger d'audit sur ride_draft (D-10 — donnée transitoire)
       ```

       **(g) Revoke / Grant** :
       ```sql
       revoke all on public.rides from anon;
       grant select, insert, update on public.rides to authenticated;
       revoke all on public.ride_draft from anon;
       grant select, insert, update, delete on public.ride_draft to authenticated;
       ```

    3. **Self-check syntaxe** : relire le fichier après écriture pour vérifier que toutes les policies et triggers sont fermés, virgules cohérentes, semicolons en fin d'instruction. Pas de `console.log`. Pas de NIR clair.

    4. **Validation locale opportuniste** : si `pnpm db:diff` ou `pnpm supabase:diff` est disponible et que le projet local fonctionne — exécuter pour repérer les erreurs syntaxiques. Sinon laisser CI cloud valider.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "create table public.rides" supabase/migrations/20260509000001_rides.sql && grep -c "create table public.ride_draft" supabase/migrations/20260509000001_rides.sql && grep -c "force row level security" supabase/migrations/20260509000001_rides.sql && grep -c "rides_audit_trigger" supabase/migrations/20260509000001_rides.sql && grep -c "ride_draft_owner_all" supabase/migrations/20260509000001_rides.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create table public.rides" supabase/migrations/20260509000001_rides.sql` retourne 1
    - `grep -c "create table public.ride_draft" supabase/migrations/20260509000001_rides.sql` retourne 1
    - `grep -c "force row level security" supabase/migrations/20260509000001_rides.sql` retourne 2
    - `grep -c "rides_audit_trigger" supabase/migrations/20260509000001_rides.sql` retourne ≥ 3 (function + trigger + helper grants)
    - `grep -c "ride_draft" supabase/migrations/20260509000001_rides.sql` retourne ≥ 8 (table + index + RLS + trigger)
    - Aucun audit trigger sur ride_draft : `grep -c "ride_draft_audit" supabase/migrations/20260509000001_rides.sql` retourne 0
    - Pas de policy DELETE sur rides : `grep -E "policy.*delete.*on public.rides" supabase/migrations/20260509000001_rides.sql` retourne 0
  </acceptance_criteria>
  <done>
    - Migration committée avec FR : `feat(02): migration 004 rides + ride_draft + RLS + audit trigger (D-01/D-02/D-10)`
    - Self-check syntaxe OK
    - Push CI cloud appliquera la migration en preview Supabase staging
  </done>
</task>

<task type="auto">
  <name>Task 2.2: Promouvoir scaffolds pgTAP en assertions complètes (rides_rls + ride_draft_rls + rides_audit)</name>
  <read_first>
    - /home/user/TAP/supabase/tests/patients.sql (fixtures multi-tenant ligne 23-57 + assertions audit ligne 195-218)
    - /home/user/TAP/supabase/tests/check_breach_deadlines.sql (pattern audit_logs query)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`rides_rls.sql` + §`ride_draft_rls.sql` + §`rides_audit.sql`
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §Pitfall 4 (cross-author dans même org)
    - supabase/migrations/20260509000001_rides.sql (la migration à tester — créée Task 2.1)
  </read_first>
  <action>
    Réécrire les 3 scaffolds RED (plan(0)) en assertions complètes GREEN. Réutiliser **mot pour mot** les UUID fixtures existantes : Org Alpha `11111111...`, Org Bravo `22222222...`, alpha-dir `aaaaaaaa...`, alpha-reg `cccccccc...`, bravo-reg `dddddddd...`. Ajouter un 2e régulateur Alpha pour le test author_id scoping : alpha-reg2 `eeeeeeee-eeee-eeee-eeee-eeeeeeeeeeee` (créé en fixture si absent du seed pgTAP).

    **(A) `supabase/tests/rides_rls.sql`** (~120 lignes, plan(15)) — calquer fixture multi-tenant patients.sql lignes 23-57 puis ajouter ces blocs d'assertion :

    ```sql
    begin;
    select plan(15);

    -- (Fixtures duplicate de patients.sql lignes 23-57 si non déjà disponibles dans une fixture partagée)
    -- … insert auth.users + profiles pour alpha-dir, alpha-reg, bravo-reg, alpha-chauffeur, alpha-patient

    -- 1-2. RLS activée + forcée
    select ok((select relrowsecurity from pg_class where oid = 'public.rides'::regclass), 'RLS activée sur rides');
    select ok((select relforcerowsecurity from pg_class where oid = 'public.rides'::regclass), 'RLS forcée sur rides');

    -- 3-5. alpha-reg crée une course Alpha
    set local role authenticated;
    set local "request.jwt.claim.sub" = 'cccccccc-cccc-cccc-cccc-cccccccccccc';
    select lives_ok($$
      insert into public.rides (organization_id, patient_id, scheduled_at, pickup_address, dropoff_address, created_by, updated_by)
      values ('11111111-1111-1111-1111-111111111111',
              (select id from public.patients where organization_id = '11111111-1111-1111-1111-111111111111' limit 1),
              now() + interval '1 day', '12 rue Pasteur', 'CHU Bellepierre',
              'cccccccc-cccc-cccc-cccc-cccccccccccc', 'cccccccc-cccc-cccc-cccc-cccccccccccc')
    $$, 'alpha-reg INSERT rides OK');

    select is((select count(*)::int from public.rides), 1, 'alpha-reg voit sa course Alpha');

    -- 6. bravo-reg ne voit AUCUNE course Alpha
    set local "request.jwt.claim.sub" = 'dddddddd-dddd-dddd-dddd-dddddddddddd';
    select is((select count(*)::int from public.rides), 0, 'bravo-reg ne voit pas la course Alpha (cross-tenant)');

    -- 7. bravo-reg ne peut PAS insérer dans Alpha (RLS WITH CHECK)
    select throws_ok($$
      insert into public.rides (organization_id, patient_id, scheduled_at, pickup_address, dropoff_address, created_by, updated_by)
      values ('11111111-1111-1111-1111-111111111111', gen_random_uuid(), now() + interval '1 day', 'X', 'Y',
              'dddddddd-dddd-dddd-dddd-dddddddddddd', 'dddddddd-dddd-dddd-dddd-dddddddddddd')
    $$, 42501, null, 'bravo-reg refusé sur cross-org INSERT');

    -- 8. alpha-chauffeur ne voit AUCUNE course (V1 — chauffeur sans accès rides)
    set local "request.jwt.claim.sub" = '<alpha-chauffeur-uuid>';  -- depuis fixtures patients.sql
    select is((select count(*)::int from public.rides), 0, 'chauffeur ne voit pas les rides V1');

    -- 9. alpha-chauffeur ne peut PAS insérer (rôle non listé)
    select throws_ok($$
      insert into public.rides (organization_id, patient_id, scheduled_at, pickup_address, dropoff_address, created_by, updated_by)
      values ('11111111-1111-1111-1111-111111111111', gen_random_uuid(), now() + interval '1 day', 'X', 'Y',
              '<alpha-chauffeur-uuid>', '<alpha-chauffeur-uuid>')
    $$, 42501, null, 'chauffeur refusé INSERT rides');

    -- 10-12. Defaults appliqués (transport_mode, urgency, status)
    set local "request.jwt.claim.sub" = 'cccccccc-cccc-cccc-cccc-cccccccccccc';
    select is(
      (select transport_mode::text from public.rides limit 1),
      'taxi_conventionne', 'default transport_mode'
    );
    select is(
      (select urgency::text from public.rides limit 1),
      'programmee', 'default urgency'
    );
    select is(
      (select status::text from public.rides limit 1),
      'validee', 'default status'
    );

    -- 13. Constraint check_length notes_regulateur ≤ 500
    select throws_ok($$
      insert into public.rides (organization_id, patient_id, scheduled_at, pickup_address, dropoff_address, created_by, updated_by, notes_regulateur)
      values ('11111111-1111-1111-1111-111111111111',
              (select id from public.patients where organization_id = '11111111-1111-1111-1111-111111111111' limit 1),
              now() + interval '1 day', 'X', 'Y',
              'cccccccc-cccc-cccc-cccc-cccccccccccc', 'cccccccc-cccc-cccc-cccc-cccccccccccc',
              repeat('a', 501))
    $$, 23514, null, 'notes_regulateur > 500 chars rejetée');

    -- 14. Granted to authenticated
    select ok(
      has_table_privilege('authenticated', 'public.rides', 'INSERT, SELECT, UPDATE'),
      'authenticated a droits CRUD sauf DELETE'
    );

    -- 15. Anon refusé
    select ok(
      not has_table_privilege('anon', 'public.rides', 'SELECT'),
      'anon refusé sur rides'
    );

    select * from finish();
    rollback;
    ```

    **(B) `supabase/tests/ride_draft_rls.sql`** (~80 lignes, plan(8)) — pattern critique : 2 régulateurs **MÊME ORG** :

    ```sql
    begin;
    select plan(8);

    -- Fixtures + ajouter alpha-reg2 'eeeeeeee-...' dans org Alpha
    insert into auth.users (id, email, encrypted_password)
    values ('eeeeeeee-eeee-eeee-eeee-eeeeeeeeeeee', 'alpha-reg2@test.tap', crypt('test', gen_salt('bf')))
    on conflict do nothing;
    insert into public.profiles (id, organization_id, role, full_name)
    values ('eeeeeeee-eeee-eeee-eeee-eeeeeeeeeeee', '11111111-1111-1111-1111-111111111111', 'regulateur', 'Reg 2 Alpha')
    on conflict do nothing;

    -- 1-2. RLS activée + forcée
    select ok((select relrowsecurity from pg_class where oid = 'public.ride_draft'::regclass), 'RLS activée');
    select ok((select relforcerowsecurity from pg_class where oid = 'public.ride_draft'::regclass), 'RLS forcée');

    -- 3. alpha-reg1 crée un brouillon
    set local role authenticated;
    set local "request.jwt.claim.sub" = 'cccccccc-cccc-cccc-cccc-cccccccccccc';
    select lives_ok($$
      insert into public.ride_draft (organization_id, author_id, payload)
      values ('11111111-1111-1111-1111-111111111111',
              'cccccccc-cccc-cccc-cccc-cccccccccccc', '{}'::jsonb)
    $$, 'alpha-reg1 crée son brouillon');

    -- 4. alpha-reg1 voit son brouillon
    select is((select count(*)::int from public.ride_draft), 1, 'reg1 voit ses brouillons');

    -- 5. alpha-reg2 (MÊME ORG) ne voit PAS le brouillon de reg1 (author_id scoping)
    set local "request.jwt.claim.sub" = 'eeeeeeee-eeee-eeee-eeee-eeeeeeeeeeee';
    select is((select count(*)::int from public.ride_draft), 0,
      'reg2 même org ne voit aucun brouillon de reg1 (author_id scope)');

    -- 6. alpha-reg2 ne peut PAS insérer en se faisant passer pour reg1 (with check author_id)
    select throws_ok($$
      insert into public.ride_draft (organization_id, author_id, payload)
      values ('11111111-1111-1111-1111-111111111111',
              'cccccccc-cccc-cccc-cccc-cccccccccccc', '{}'::jsonb)
    $$, 42501, null, 'reg2 ne peut INSERT en spoofant author_id de reg1');

    -- 7. bravo-reg cross-tenant 0 access
    set local "request.jwt.claim.sub" = 'dddddddd-dddd-dddd-dddd-dddddddddddd';
    select is((select count(*)::int from public.ride_draft), 0,
      'bravo-reg ne voit aucun brouillon Alpha');

    -- 8. PAS de trigger audit sur ride_draft (D-10)
    set local "request.jwt.claim.sub" = 'cccccccc-cccc-cccc-cccc-cccccccccccc';
    select is(
      (select count(*)::int from public.audit_logs
        where action like 'ride_draft%' or entity_type = 'ride_draft'),
      0, 'aucun audit log généré pour ride_draft'
    );

    select * from finish();
    rollback;
    ```

    **(C) `supabase/tests/rides_audit.sql`** (~60 lignes, plan(6)) — calque patients.sql 195-218 :

    ```sql
    begin;
    select plan(6);

    -- (fixtures partagées avec rides_rls.sql)

    -- 1. INSERT ride → audit_logs ride.insert
    set local role authenticated;
    set local "request.jwt.claim.sub" = 'cccccccc-cccc-cccc-cccc-cccccccccccc';
    insert into public.rides (organization_id, patient_id, scheduled_at, pickup_address, dropoff_address, created_by, updated_by)
    values ('11111111-1111-1111-1111-111111111111',
            (select id from public.patients where organization_id = '11111111-1111-1111-1111-111111111111' limit 1),
            now() + interval '1 day', '12 rue Pasteur', 'CHU Bellepierre',
            'cccccccc-cccc-cccc-cccc-cccccccccccc', 'cccccccc-cccc-cccc-cccc-cccccccccccc');

    select ok(
      (select exists(
        select 1 from public.audit_logs
        where action = 'ride.insert' and entity_type = 'ride'
          and organization_id = '11111111-1111-1111-1111-111111111111'
      )),
      'audit_logs reçoit ride.insert'
    );

    -- 2. metadata->'new' contient patient_id
    select ok(
      (select (metadata->'new') ? 'patient_id' from public.audit_logs
        where action = 'ride.insert' order by created_at desc limit 1),
      'metadata.new contient patient_id'
    );

    -- 3. actor_role correctement renseigné
    select is(
      (select actor_role::text from public.audit_logs
        where action = 'ride.insert' order by created_at desc limit 1),
      'regulateur', 'actor_role = regulateur'
    );

    -- 4. UPDATE rides → audit_logs ride.update
    update public.rides set notes_regulateur = 'note de test'
      where id = (select id from public.rides limit 1);
    select ok(
      (select exists(select 1 from public.audit_logs where action = 'ride.update')),
      'audit_logs reçoit ride.update'
    );

    -- 5. metadata->'old' présent en UPDATE
    select ok(
      (select (metadata ? 'old') from public.audit_logs
        where action = 'ride.update' order by created_at desc limit 1),
      'metadata.old présent sur UPDATE'
    );

    -- 6. NO audit log pour ride_draft (déjà couvert ride_draft_rls test 8 — assertion redondante mais explicite ici)
    insert into public.ride_draft (organization_id, author_id, payload)
    values ('11111111-1111-1111-1111-111111111111',
            'cccccccc-cccc-cccc-cccc-cccccccccccc', '{}'::jsonb);
    select is(
      (select count(*)::int from public.audit_logs where entity_type = 'ride_draft'),
      0, 'aucun audit log sur ride_draft (D-10)'
    );

    select * from finish();
    rollback;
    ```

    Remplacer `<alpha-chauffeur-uuid>` par la valeur exacte trouvée dans `supabase/tests/patients.sql` (probablement `bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb` selon la convention). Vérifier avant écriture.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "select plan(" supabase/tests/rides_rls.sql && grep -c "select plan(" supabase/tests/ride_draft_rls.sql && grep -c "select plan(" supabase/tests/rides_audit.sql && grep -c "ride.insert" supabase/tests/rides_audit.sql && grep -c "author_id" supabase/tests/ride_draft_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    - Aucun fichier ne contient `plan(0)` (les 3 sont passés en assertions)
    - `grep -c "ride.insert" supabase/tests/rides_audit.sql` ≥ 1
    - `grep -c "eeeeeeee-eeee" supabase/tests/ride_draft_rls.sql` ≥ 1 (2e user même org)
    - `grep -c "cross-tenant\|cross-org" supabase/tests/rides_rls.sql` ≥ 1
    - Au push main, le job CI pgTAP cloud doit passer GREEN — sinon le checker Wave finalisation bloquera la phase
  </acceptance_criteria>
  <done>
    - 3 fichiers pgTAP avec assertions complètes (~plan(15) + plan(8) + plan(6) = 29 assertions)
    - Commit FR : `test(02): pgTAP rides_rls + ride_draft_rls + rides_audit GREEN (T1-T3)`
    - Couverture : T1 cross-tenant rides ✓, T2 cross-author brouillons ✓, T3 audit bypass impossible ✓
  </done>
</task>

<task type="auto">
  <name>Task 2.3: Régénérer types Database TypeScript (BLOCKING — pnpm db:types)</name>
  <read_first>
    - /home/user/TAP/packages/database/package.json (script supabase:gen-types)
    - /home/user/TAP/packages/database/src/types.gen.ts (ancien contenu — pour diff)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md §code_context (Database typing pattern)
  </read_first>
  <action>
    1. **Identifier la commande exacte** de régénération :
       ```bash
       grep -E "gen-types|db:types|supabase.*gen" /home/user/TAP/packages/database/package.json /home/user/TAP/package.json /home/user/TAP/turbo.json 2>/dev/null
       ```
       Il y a normalement un script `pnpm db:types` ou `pnpm --filter @tap/database supabase:gen-types`. Le mode autoritaire pour exécuter dépend du projet ID Supabase staging — utiliser celui de la CI (`SUPABASE_PROJECT_ID` env var) ou un projet local si pas connecté.

    2. **Cas A — Supabase staging accessible (CI cloud)** :
       ```bash
       cd /home/user/TAP && pnpm db:types
       ```
       Cela invoque `supabase gen types typescript --project-id $SUPABASE_PROJECT_ID --schema public > packages/database/src/types.gen.ts` (ou variant équivalent du projet).

    3. **Cas B — Régénération locale impossible** (sandbox Docker bloqué — déjà connu cf. STATE.md dette) :
       - Documenter dans le commit que la régénération sera faite par CI cloud.
       - Étendre **manuellement** `packages/database/src/types.gen.ts` avec les types attendus (à fusionner avec ce que CI produira) :
         ```ts
         // Ajouts attendus (régénération CI cloud confirmera les noms exacts) :
         // public.Tables.rides : { Row, Insert, Update, Relationships }
         // public.Tables.ride_draft : { Row, Insert, Update, Relationships }
         // public.Enums.ride_transport_mode : 'taxi_conventionne' | 'tpmr' | 'vsl' | 'ambulance'
         // public.Enums.ride_urgency : 'programmee' | 'urgente' | 'immediate'
         // public.Enums.ride_status : 'brouillon' | 'validee' | 'assignee' | 'en_cours' | 'terminee' | 'annulee_regulateur' | 'annulee_patient' | 'annulee_chauffeur'
         ```
       - Si la stratégie projet est de **toujours** regen via CI : créer une PR ouverte, laisser CI commit le types.gen.ts via bot, et continuer Wave 2 en utilisant les types `@tap/database/types` qui se résoudront après CI merge.

    4. **Vérifier** :
       - `grep -c "rides:" packages/database/src/types.gen.ts` ≥ 1
       - `grep -c "ride_draft:" packages/database/src/types.gen.ts` ≥ 1
       - `grep -c "ride_transport_mode" packages/database/src/types.gen.ts` ≥ 1
       - `pnpm --filter @tap/database typecheck` exit 0
       - `pnpm --filter @tap/web typecheck` exit 0 — confirme que toute la monorepo compile encore (les futures tasks dépendront de ces types)

    5. **Documenter** dans le commit message :
       - Méthode utilisée (CI cloud automatique vs ajout manuel)
       - Lien vers le run CI qui a réellement appliqué la migration en preview Supabase
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "rides:" packages/database/src/types.gen.ts && grep -c "ride_draft:" packages/database/src/types.gen.ts && grep -c "ride_transport_mode\|taxi_conventionne" packages/database/src/types.gen.ts && pnpm --filter @tap/database typecheck 2>&1 | tail -5 && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rides:" packages/database/src/types.gen.ts` ≥ 1
    - `grep -c "ride_draft:" packages/database/src/types.gen.ts` ≥ 1
    - `grep -c "taxi_conventionne" packages/database/src/types.gen.ts` ≥ 1 (enum exposé)
    - Typecheck monorepo OK (`pnpm typecheck` ou par package)
    - Aucun import `from '@tap/database/types'` n'est cassé suite à la régénération
  </acceptance_criteria>
  <done>
    - types.gen.ts contient rides + ride_draft + 3 enums
    - Typecheck OK
    - Commit FR : `chore(02): regen types Database (rides + ride_draft + enums)` [BLOCKING]
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Postgres (RLS) | Toute requête Supabase passe par RLS — rides + ride_draft sont la première frontière de sécurité |
| Régulateur A ↔ Régulateur B (même org) | Brouillons author-scoped ; le partage de brouillons entre régulateurs n'est PAS un cas d'usage V1 |
| Org A ↔ Org B (cross-tenant) | Isolation stricte via current_organization_id() — testé pgTAP T1 |
| auth.users ↔ rides.created_by/updated_by | FK on delete restrict — empêche purge orpheline d'un user créateur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T1 | Information Disclosure | rides cross-tenant via RLS bypass | mitigate | RLS forcée + policies using/with check sur (organization_id, role) — pgTAP rides_rls assertions 6-9 |
| T-02-T2 | Information Disclosure | ride_draft contient patient_id, leak via XSS payload jsonb | mitigate | RLS forcée author_id ET organization_id (Pitfall 4) ; pgTAP test cross-author MÊME ORG ; payload validé zod côté client + serveur (Wave 2) ; aucun audit log (D-10) donc pas de leak de payload via audit_logs |
| T-02-T3 | Tampering | Audit logs ride.created bypass via INSERT direct sans trigger | mitigate | Trigger PG `for each row execute function rides_audit_trigger` ne peut être contourné depuis l'API (security definer) ; pgTAP rides_audit assertions 1-3 |
| T-02-T4 | DoS / Race | Multi-instance auto-save concurrent → conflits ride_draft | mitigate | upsert idempotent `on conflict (id)` ; index ride_draft_author_idx supporte la fréquence d'updates (Wave 2) |
| T-02-T6 | DoS | Auto-save agressive 5s × 100 onglets = 1200 req/min | accept (low) | RLS rate limit Postgres + Vercel functions throttle ; pas de mitigation supplémentaire V1 ; documenté pour V2 monitoring Sentry |
</threat_model>

<verification>
- Migration `20260509000001_rides.sql` syntactiquement correcte (self-check + CI cloud apply OK)
- Les 3 fichiers pgTAP exécutés sur Supabase preview retournent **29/29 assertions PASS** (15 + 8 + 6)
- `pnpm --filter @tap/database typecheck && pnpm --filter @tap/web typecheck` exit 0
- Aucun audit trigger sur ride_draft (`grep -c "ride_draft.*audit\|audit.*ride_draft" supabase/migrations/20260509000001_rides.sql` = 0)
- Aucune policy DELETE sur rides
- Cohérence avec helpers Phase 0/1 : `current_organization_id()` et `has_role()` invoqués sans erreur
</verification>

<success_criteria>
- D-01 implémenté : rides table avec 3 enums, 18 colonnes, indexes, RLS forcée
- D-02 implémenté : ride_draft author-scoped, RLS forcée, pas de TTL V1 (deferred)
- D-10 implémenté : trigger d'audit sur rides UNIQUEMENT, pas sur ride_draft
- T1+T2+T3 du threat model couverts par pgTAP
- Types Database régénérés et exportés via @tap/database
- CI cloud pgTAP GREEN sur la branche Phase 2
</success_criteria>

<output>
À l'issue de Wave 1, créer `.planning/phases/02-saisie-express-course/02-02-SUMMARY.md` listant :
- Schéma livré (3 enums, 2 tables, 4 policies rides + 1 ride_draft, 1 trigger audit, 2 triggers updated_at, 3 indexes)
- 29 assertions pgTAP PASS (15 RLS rides + 8 RLS ride_draft + 6 audit)
- Types Database regen committés
- Lien run CI cloud (cd.yml) où la migration a été appliquée en Supabase staging
</output>
