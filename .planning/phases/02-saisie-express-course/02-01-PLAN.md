---
phase: 02-saisie-express-course
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/validators/ride.ts
  - packages/shared/src/index.ts
  - packages/shared/src/utils/parse-freeform-date.ts
  - packages/shared/src/utils/index.ts
  - packages/shared/src/validators/__tests__/ride.test.ts
  - packages/shared/src/utils/__tests__/parse-freeform-date.test.ts
  - packages/shared/package.json
  - apps/web/src/components/ui/dialog.tsx
  - apps/web/src/components/ui/dropdown-menu.tsx
  - apps/web/package.json
  - apps/web/tests/e2e/saisie-express.spec.ts
  - supabase/tests/rides_rls.sql
  - supabase/tests/ride_draft_rls.sql
  - supabase/tests/rides_audit.sql
  - docs/showcase/02-saisie-express-course/README.md
autonomous: true
requirements: [SAIS-01, SAIS-02, SAIS-03, SAIS-04, SAIS-05, SAIS-06]

must_haves:
  truths:
    - "Le schéma zod rideExpressInputSchema est aligné sur D-08 (pickup_address, dropoff_address, scheduled_at, transport_mode 4 valeurs, urgency 3 valeurs)"
    - "courseExpressSchema et typeTransportSchema sont supprimés et n'ont aucun consommateur"
    - "parseFreeformDate parse 15/05 14h30 / demain 8h / lundi 9h, rejette dates passées, retourne ISO + offset"
    - "chrono-node@2.9.1 est installé dans @tap/shared"
    - "Composants shadcn dialog + dropdown-menu sont disponibles dans apps/web"
    - "Tests Vitest ride + parse-freeform-date sont GREEN"
    - "Scaffolds pgTAP rides_rls / ride_draft_rls / rides_audit existent en RED (skip ou plan(0)) — ils passeront GREEN après Wave 1"
    - "Scaffold E2E saisie-express.spec.ts existe en mode test.skip (passera GREEN Wave 5)"
  artifacts:
    - path: packages/shared/src/validators/ride.ts
      provides: "rideExpressInputSchema + rideDraftSchema + rideTransportModeSchema + rideUrgencySchema"
      contains: "rideExpressInputSchema"
    - path: packages/shared/src/utils/parse-freeform-date.ts
      provides: "parseFreeformDate(input, ref) wrapper chrono-node FR"
      contains: "export function parseFreeformDate"
    - path: packages/shared/src/validators/__tests__/ride.test.ts
      provides: "Tests Vitest rideExpressInputSchema GREEN"
    - path: packages/shared/src/utils/__tests__/parse-freeform-date.test.ts
      provides: "Tests Vitest parser date GREEN avec TZ Indian/Reunion"
    - path: apps/web/src/components/ui/dialog.tsx
      provides: "Wrapper shadcn Radix Dialog"
    - path: apps/web/src/components/ui/dropdown-menu.tsx
      provides: "Wrapper shadcn dropdown-menu"
    - path: docs/showcase/02-saisie-express-course/README.md
      provides: "Convention nommage 6 captures Visible Progress (placeholders OK)"
  key_links:
    - from: "packages/shared/src/utils/parse-freeform-date.ts"
      to: "chrono-node"
      via: "import * as chrono from 'chrono-node'"
      pattern: "from 'chrono-node'"
    - from: "packages/shared/src/index.ts"
      to: "validators/ride.ts + utils/parse-freeform-date.ts"
      via: "ré-export public package"
      pattern: "export.*ride|parseFreeformDate"
---

<objective>
Wave 0 — Fondations Phase 2 : refonte du schéma zod `validators/ride.ts` (suppression `courseExpressSchema` stub Phase 0 incompatible D-08), ajout helper pur `parseFreeformDate` (chrono-node FR), installation des dépendances manquantes (`chrono-node@2.9.1` côté shared, `dialog` + `dropdown-menu` shadcn côté web), et création des scaffolds de tests RED qui passeront GREEN au fil des waves.

Purpose: TDD complet sur la couche pure (validators + parser) **AVANT** toute implémentation UI/DB. La Phase 0 contient un stub `courseExpressSchema` (champs `adresse_depart`, `aller_retour`, `prescription_id`...) totalement incompatible avec D-08 — refonte obligatoire (cf. RESEARCH §R3). chrono-node est l'unique nouvelle dépendance externe Phase 2 (cf. RESEARCH §C5).

Output: schémas zod alignés, helper date freeform testé, deps installées, 3 scaffolds pgTAP + 1 scaffold Playwright en RED, placeholder showcase Visible Progress prêt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/TAP/CLAUDE.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md

<interfaces>
<!-- Helpers existants à RÉUTILISER (extraits du codebase) -->

From packages/shared/src/validators/common.ts:
```typescript
export const codePostalReunionSchema: z.ZodString;  // /^974|^976/
export const adresseSchema: z.ZodObject<{ ligne: z.ZodString; code_postal; ville }>;
export const verifyLuhn: (siret: string) => boolean;
```

From packages/shared/src/validators/patient.ts (pattern à imiter):
```typescript
export const patientSchema = z.object({...});
export type PatientInput = z.infer<typeof patientSchema>;
```

À REMPLACER (legacy stub Phase 0 — packages/shared/src/validators/ride.ts):
- `typeTransportSchema` enum 2-valeurs (assis|tpmr) → SUPPRIMÉ, remplacé par `rideTransportModeSchema` 4-valeurs
- `courseExpressSchema` (adresse_depart/adresse_arrivee/heure_souhaitee/aller_retour/prescription_id) → SUPPRIMÉ, remplacé par `rideExpressInputSchema` (pickup_address/dropoff_address/scheduled_at/transport_mode/urgency)
- `CourseExpressInput` type → SUPPRIMÉ
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Refonte complète validators/ride.ts + tests Vitest GREEN</name>
  <read_first>
    - /home/user/TAP/packages/shared/src/validators/ride.ts (legacy à supprimer intégralement)
    - /home/user/TAP/packages/shared/src/validators/common.ts (helpers codePostalReunionSchema)
    - /home/user/TAP/packages/shared/src/validators/patient.ts (pattern composition + types)
    - /home/user/TAP/packages/shared/src/validators/__tests__/patient.test.ts (pattern Vitest cas paramétrés)
    - /home/user/TAP/packages/shared/src/index.ts (ré-exports publics)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md §D-08
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`packages/shared/src/validators/ride.ts (refonte complète)`
  </read_first>
  <behavior>
    - Test 1 : rideExpressInputSchema accepte une saisie minimale {patient_id UUID, scheduled_at ISO+offset, pickup_address, dropoff_address} et applique les defaults transport_mode='taxi_conventionne' + urgency='programmee'
    - Test 2 : refuse patient_id non-UUID → message « Patient requis »
    - Test 3 : refuse scheduled_at sans offset → message « Date/heure requise »
    - Test 4 : refuse pickup_address < 3 chars → message « Adresse de prise en charge requise »
    - Test 5 : refuse dropoff_address vide → message « Adresse de destination requise »
    - Test 6 : refuse pickup_postal_code hors Réunion (ex 75001) — réutilise codePostalReunionSchema
    - Test 7 : refuse notes_regulateur > 500 chars
    - Test 8 : rideTransportModeSchema accepte les 4 valeurs (taxi_conventionne, tpmr, vsl, ambulance) et refuse 'assis'/'tpmr_old'
    - Test 9 : rideUrgencySchema accepte (programmee, urgente, immediate)
    - Test 10 : rideDraftSchema (= rideExpressInputSchema.partial()) accepte un brouillon avec uniquement {patient_id} ou même {} vide
  </behavior>
  <action>
    1. **Vérifier zéro consommateur** des symboles legacy avant suppression :
       ```bash
       grep -rn "courseExpressSchema\|typeTransportSchema\|CourseExpressInput" /home/user/TAP --include="*.ts" --include="*.tsx" | grep -v "packages/shared/src/validators/ride.ts" | grep -v "node_modules"
       ```
       Doit retourner 0 ligne (déjà vérifié — RESEARCH A6). Si ≥ 1 résultat (hors le fichier lui-même) : STOP, escalade nécessaire.

    2. **Réécrire intégralement** `packages/shared/src/validators/ride.ts` (overwrite, ≤ 80 lignes) :
       ```ts
       import { z } from 'zod';
       import { codePostalReunionSchema } from './common';

       export const rideTransportModeSchema = z.enum([
         'taxi_conventionne',
         'tpmr',
         'vsl',
         'ambulance',
       ]);
       export type RideTransportMode = z.infer<typeof rideTransportModeSchema>;

       export const rideUrgencySchema = z.enum(['programmee', 'urgente', 'immediate']);
       export type RideUrgency = z.infer<typeof rideUrgencySchema>;

       /**
        * Saisie express d'une course (CDC v2 § 5.8, DEC-005, D-08).
        * Cible : < 30 secondes pour la régulatrice (mesuré E2E SAIS-01).
        */
       export const rideExpressInputSchema = z.object({
         patient_id: z.string().uuid('Patient requis'),
         scheduled_at: z
           .string()
           .datetime({ offset: true, message: 'Date/heure requise' }),
         pickup_address: z
           .string()
           .trim()
           .min(3, 'Adresse de prise en charge requise')
           .max(200),
         pickup_postal_code: codePostalReunionSchema.optional(),
         pickup_city: z.string().trim().max(80).optional(),
         dropoff_address: z
           .string()
           .trim()
           .min(3, 'Adresse de destination requise')
           .max(200),
         dropoff_postal_code: codePostalReunionSchema.optional(),
         dropoff_city: z.string().trim().max(80).optional(),
         transport_mode: rideTransportModeSchema.default('taxi_conventionne'),
         urgency: rideUrgencySchema.default('programmee'),
         notes_regulateur: z.string().trim().max(500).optional(),
       });
       export type RideExpressInput = z.infer<typeof rideExpressInputSchema>;

       /** Brouillon : tous champs optionnels (D-02). */
       export const rideDraftSchema = rideExpressInputSchema.partial();
       export type RideDraftInput = z.infer<typeof rideDraftSchema>;
       ```

    3. **Mettre à jour** `packages/shared/src/index.ts` — retirer les ré-exports legacy (`courseExpressSchema`, `typeTransportSchema`, `CourseExpressInput`) et ajouter :
       ```ts
       export {
         rideExpressInputSchema,
         rideDraftSchema,
         rideTransportModeSchema,
         rideUrgencySchema,
         type RideExpressInput,
         type RideDraftInput,
         type RideTransportMode,
         type RideUrgency,
       } from './validators/ride';
       ```

    4. **Créer** `packages/shared/src/validators/__tests__/ride.test.ts` avec les 10 cas listés en `<behavior>`. Pattern strictement calqué sur `__tests__/patient.test.ts`. Utiliser une `baseValide` factorisée et tester via `safeParse(...).success` + `.error.errors[0].message`.

    5. **Lancer** `pnpm --filter @tap/shared test -- ride` — les 10 tests DOIVENT passer GREEN avant commit.

    6. **Lancer** `pnpm --filter @tap/web typecheck` pour confirmer qu'aucun consommateur ne casse (filet de sécurité au-delà du grep).
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm --filter @tap/shared test -- ride 2>&1 | tail -20 && pnpm --filter @tap/shared typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "courseExpressSchema\|typeTransportSchema" /home/user/TAP/packages/shared/src/validators/ride.ts` retourne 0
    - `grep -c "rideExpressInputSchema" /home/user/TAP/packages/shared/src/validators/ride.ts` retourne ≥ 1
    - `grep -c "rideExpressInputSchema" /home/user/TAP/packages/shared/src/index.ts` retourne ≥ 1
    - `pnpm --filter @tap/shared test -- ride` retourne 10 tests passed
    - `pnpm --filter @tap/shared typecheck && pnpm --filter @tap/web typecheck` exit code 0
  </acceptance_criteria>
  <done>
    - Refonte committée avec message FR : `feat(02): refonte rideExpressInputSchema aligné D-08 + suppression stub Phase 0`
    - Tests Vitest 10/10 GREEN
    - Typecheck monorepo OK (filet de sécurité grep + tsc)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Helper parseFreeformDate (chrono-node FR) + tests TZ Indian/Reunion</name>
  <read_first>
    - /home/user/TAP/packages/shared/package.json (vérifier deps)
    - /home/user/TAP/packages/shared/src/utils/patient-note.ts (pattern helper pur)
    - /home/user/TAP/packages/shared/src/utils/index.ts (ré-exports)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §C5 + §Pitfall 5
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`parse-freeform-date.ts`
    - /home/user/TAP/apps/web/playwright.config.ts (vérifier ligne `timezoneId: 'Indian/Reunion'`)
  </read_first>
  <behavior>
    - Test 1 (TZ Indian/Reunion, ref 2026-05-07T08:00:00+04:00) : `parseFreeformDate('15/05 14h30')` retourne `{ ok: true, iso: '2026-05-15T10:30:00.000Z' }` (14h30 Réunion = 10h30 UTC)
    - Test 2 : `parseFreeformDate('demain 8h')` retourne `{ ok: true, iso: '2026-05-08T04:00:00.000Z' }`
    - Test 3 : `parseFreeformDate('lundi 9h')` retourne `{ ok: true, iso }` pointant vers prochain lundi 09:00 (forwardDate)
    - Test 4 : `parseFreeformDate('15 mai 14:30')` retourne `{ ok: true }` avec heure 14:30 locale
    - Test 5 : `parseFreeformDate('30/02')` retourne `{ ok: false, reason: /Format non reconnu/ }` OU `{ ok: false, reason: /passé/ }` (chrono peut convertir en date proche, accepter les deux)
    - Test 6 : `parseFreeformDate('01/05/2025')` (date passée) retourne `{ ok: false, reason: 'Date dans le passé' }`
    - Test 7 : `parseFreeformDate('')` retourne `{ ok: false, reason: 'Date requise' }`
    - Test 8 : `parseFreeformDate('   ')` (whitespace only) retourne `{ ok: false, reason: 'Date requise' }`
  </behavior>
  <action>
    1. **Installer chrono-node** dans le package shared :
       ```bash
       pnpm --filter @tap/shared add chrono-node@2.9.1
       ```
       Vérifier que `package.json` shared liste `"chrono-node": "2.9.1"` exactement (pas `^2.9.1`, on fige) ou cohérent avec stack.

    2. **Créer** `packages/shared/src/utils/parse-freeform-date.ts` (≤ 30 lignes) :
       ```ts
       import * as chrono from 'chrono-node';

       export type ParseFreeformDateResult =
         | { ok: true; iso: string }
         | { ok: false; reason: string };

       /**
        * Parse une saisie libre type "15/05 14h30" / "demain 8h" / "lundi 9h"
        * vers une ISO 8601 (UTC). Locale FR via chrono.fr.
        *
        * Règle métier (DEC-005) : les dates dans le passé sont refusées
        * (la régulatrice ne saisit jamais une course rétroactive Phase 2).
        *
        * IMPORTANT : appelée UNIQUEMENT côté client (TZ navigateur =
        * Indian/Reunion pour la régulatrice — voir RESEARCH §Pitfall 5).
        * Le serveur reçoit une ISO string déjà résolue.
        */
       export function parseFreeformDate(
         input: string,
         ref: Date = new Date(),
       ): ParseFreeformDateResult {
         if (!input || input.trim().length === 0) {
           return { ok: false, reason: 'Date requise' };
         }
         const parsed = chrono.fr.parseDate(input, ref, { forwardDate: true });
         if (!parsed) {
           return {
             ok: false,
             reason:
               'Format non reconnu — exemples : 15/05 14h30, demain 8h, lundi 9h',
           };
         }
         if (parsed.getTime() < ref.getTime()) {
           return { ok: false, reason: 'Date dans le passé' };
         }
         return { ok: true, iso: parsed.toISOString() };
       }
       ```

    3. **Ré-exporter** depuis `packages/shared/src/utils/index.ts` (ajouter `export * from './parse-freeform-date';`).

    4. **Créer** `packages/shared/src/utils/__tests__/parse-freeform-date.test.ts` avec les 8 cas listés. Pattern : utiliser une `ref = new Date('2026-05-07T08:00:00+04:00')` figée pour reproductibilité, et `vi.setSystemTime(ref)` dans `beforeEach`. Vérifier que la config Vitest hérite de `TZ=Indian/Reunion` ou utiliser `process.env.TZ === 'Indian/Reunion'` skip si non configuré (préférable : laisser passer en CI, le runtime navigateur de la régulatrice sera bon).

    5. **Lancer** `pnpm --filter @tap/shared test -- parse-freeform-date` — les 8 tests DOIVENT passer GREEN.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm --filter @tap/shared test -- parse-freeform-date 2>&1 | tail -20 && grep -c "chrono-node" packages/shared/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chrono-node" /home/user/TAP/packages/shared/package.json` retourne ≥ 1
    - `grep -c "parseFreeformDate" /home/user/TAP/packages/shared/src/utils/index.ts` retourne ≥ 1
    - `pnpm --filter @tap/shared test -- parse-freeform-date` 8 tests passed
    - Le test 1 vérifie strictement `iso === '2026-05-15T10:30:00.000Z'` (le décalage UTC est correct)
    - Aucun import de chrono-node hors `parse-freeform-date.ts` (`grep -r "from 'chrono-node'" packages apps | grep -v parse-freeform-date | grep -v node_modules` → 0)
  </acceptance_criteria>
  <done>
    - chrono-node@2.9.1 installé dans @tap/shared
    - Helper pur < 30 lignes, exporté
    - Tests Vitest 8/8 GREEN
    - Commit FR : `feat(02): helper parseFreeformDate chrono-node FR + tests TZ Réunion`
  </done>
</task>

<task type="auto">
  <name>Task 1.3: Install shadcn dialog + dropdown-menu + scaffolds tests RED + showcase placeholder</name>
  <read_first>
    - /home/user/TAP/apps/web/components.json (config shadcn)
    - /home/user/TAP/apps/web/src/components/ui/sheet.tsx (pattern Radix wrapper)
    - /home/user/TAP/apps/web/tests/smoke/preview.spec.ts (pattern login démo)
    - /home/user/TAP/supabase/tests/patients.sql (pattern fixtures pgTAP)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`rides_rls.sql` + §`ride_draft_rls.sql` + §`rides_audit.sql`
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md §D-12 (6 captures Visible Progress)
  </read_first>
  <action>
    1. **Installer composants shadcn** dans apps/web :
       ```bash
       cd /home/user/TAP/apps/web && npx --yes shadcn@latest add dialog dropdown-menu --yes
       ```
       Si le CLI shadcn pose des questions interactives → utiliser `--yes` ou copier-coller les wrappers minimaux depuis `ui/sheet.tsx` en remplaçant `@radix-ui/react-dialog` (déjà transitif) et `@radix-ui/react-dropdown-menu` (à installer si absent : `pnpm --filter @tap/web add @radix-ui/react-dropdown-menu`). Vérifier que `apps/web/src/components/ui/dialog.tsx` exporte `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`, `DialogTrigger`, `DialogClose` ; `dropdown-menu.tsx` exporte `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator`.

    2. **Créer scaffolds pgTAP RED** (squelettes minimaux qui passeront GREEN après Wave 1 — la table rides n'existe pas encore, donc plan(0) ou skip explicite) :

       `supabase/tests/rides_rls.sql` (≤ 30 lignes scaffold) :
       ```sql
       -- Wave 0 scaffold — passera GREEN après Wave 1 (migration rides).
       -- Couvre RLS rides : isolation org, regulateur+dirigeant peuvent
       -- INSERT/SELECT, chauffeur ne voit RIEN, cross-org refusé.
       begin;
       select plan(0);  -- TODO Wave 1 : passer à plan(15) avec assertions complètes
       select * from finish();
       rollback;
       ```

       `supabase/tests/ride_draft_rls.sql` (≤ 30 lignes scaffold) :
       ```sql
       -- Wave 0 scaffold — passera GREEN après Wave 1.
       -- Couvre RLS ride_draft : 2 régulateurs MÊME ORG ne se voient pas
       -- (author_id scoping strict, pas seulement organization_id).
       begin;
       select plan(0);
       select * from finish();
       rollback;
       ```

       `supabase/tests/rides_audit.sql` (≤ 30 lignes scaffold) :
       ```sql
       -- Wave 0 scaffold — passera GREEN après Wave 1.
       -- Couvre trigger rides_audit_trigger : INSERT → audit_logs.action='ride.insert'.
       begin;
       select plan(0);
       select * from finish();
       rollback;
       ```

    3. **Créer scaffold E2E RED** `apps/web/tests/e2e/saisie-express.spec.ts` (≤ 60 lignes) — tous les tests en `test.skip` jusqu'à Wave 5 :
       ```ts
       import { test, expect } from '@playwright/test';

       const REG_DEMO_EMAIL = 'regulateur@demo.tap';
       const REG_DEMO_PASSWORD = 'demo1234!';

       async function loginRegulateur(page) {
         await page.goto('/login');
         await page.getByLabel(/email/i).fill(REG_DEMO_EMAIL);
         await page.getByLabel(/mot de passe/i).fill(REG_DEMO_PASSWORD);
         await page.getByRole('button', { name: /se connecter/i }).click();
         await page.waitForURL((url) => !url.pathname.includes('/login'));
       }

       test.describe('Saisie express course (SAIS-01..06)', () => {
         test.beforeEach(loginRegulateur);

         test.skip('SAIS-01 saisie complète < 30 s', async ({ page }) => {});
         test.skip('SAIS-02 raccourci global Cmd/Ctrl+Shift+K', async ({ page }) => {});
         test.skip('SAIS-03 recherche patient 2 chars dans modal', async ({ page }) => {});
         test.skip('SAIS-04 brouillon reprise après pause', async ({ page }) => {});
         test.skip('SAIS-05 multi-saisies parallèles compteur badge', async ({ page }) => {});
         test.skip('SAIS-06 audit log ride.insert créé', async ({ page }) => {});
       });
       ```

    4. **Créer/étendre placeholder showcase** `docs/showcase/02-saisie-express-course/README.md` (≤ 40 lignes) :
       ```markdown
       # Phase 2 — Saisie express course

       Captures Visible Progress (CLAUDE.md § 13.5, D-12) à produire avant la finalisation Phase 2.

       Convention : numéro à 2 chiffres + slug en français.

       ## Captures attendues
       - `01-modal-express-vide.png` — modal après Cmd+Shift+K, focus champ patient
       - `02-modal-recherche-patient.png` — recherche fuzzy 2 chars « Ho » → 3 résultats
       - `03-modal-rempli-pret-submit.png` — tous champs remplis, bouton Créer prêt
       - `04-toast-creation-confirmee.gif` — submit + fade-out + toast Sonner ≤ 30 s perçu
       - `05-brouillons-dropdown-3-en-attente.png` — multi-saisies, badge compteur 3
       - `06-page-courses-liste-30j.png` — page /courses, ≥ 30 courses listées

       Walkthrough script (10 étapes) : voir `02-SUMMARY.md` après livraison.

       Status : à produire en Wave 5 (E2E + Visible Progress).
       ```

    5. **Vérifier** que `pnpm --filter @tap/web test:e2e -- saisie-express` retourne 0 test exécuté (6 skipped) sans erreur de compilation, et que `psql -f supabase/tests/rides_rls.sql` (ou la commande pgTAP standard du projet) ne casse rien (plan(0) = OK).

    6. **Vérifier** que les composants shadcn n'introduisent pas de console.log ni d'`@supabase/*` import — `grep -E "console\.(log|warn|error)" apps/web/src/components/ui/dialog.tsx apps/web/src/components/ui/dropdown-menu.tsx` doit retourner 0.
  </action>
  <verify>
    <automated>cd /home/user/TAP && ls apps/web/src/components/ui/dialog.tsx apps/web/src/components/ui/dropdown-menu.tsx supabase/tests/rides_rls.sql supabase/tests/ride_draft_rls.sql supabase/tests/rides_audit.sql apps/web/tests/e2e/saisie-express.spec.ts docs/showcase/02-saisie-express-course/README.md 2>&1 && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Les 7 fichiers listés existent (`ls` retourne tous)
    - `grep -c "DialogContent" /home/user/TAP/apps/web/src/components/ui/dialog.tsx` ≥ 1
    - `grep -c "DropdownMenuContent" /home/user/TAP/apps/web/src/components/ui/dropdown-menu.tsx` ≥ 1
    - `grep -c "test.skip" /home/user/TAP/apps/web/tests/e2e/saisie-express.spec.ts` retourne 6 (les 6 SAIS skipped)
    - Les 3 fichiers pgTAP contiennent `select plan(0)` (RED scaffold)
    - `pnpm --filter @tap/web typecheck` exit 0
    - `grep -v '^#' /home/user/TAP/apps/web/src/components/ui/dialog.tsx | grep -c "console\."` retourne 0
    - `grep -v '^#' /home/user/TAP/apps/web/src/components/ui/dialog.tsx | grep -c "@supabase/"` retourne 0
  </acceptance_criteria>
  <done>
    - dialog + dropdown-menu shadcn installés et compilent
    - 3 scaffolds pgTAP en plan(0)
    - 1 scaffold Playwright avec 6 test.skip
    - README showcase placeholder
    - Commit FR : `chore(02): scaffolds tests RED + shadcn dialog/dropdown-menu + showcase placeholder`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → npm registry | chrono-node@2.9.1 télécharge depuis registry public — supply chain |
| @tap/shared → @tap/web (typecheck) | rupture de contrat zod si export refonte mal propagé |

## STRIDE Threat Register (Wave 0 — fondations pures)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | chrono-node supply chain | mitigate | Pin version exacte 2.9.1 (pas de `^`), audit `pnpm audit --filter @tap/shared` après install |
| T-02-02 | Information Disclosure | parseFreeformDate sur navigateur | accept | Helper pur, pas de PII — input et output sont des dates publiques |
| T-02-03 | Denial of Service | parseFreeformDate sur input pathologique | mitigate | Wrap try/catch — fallback `{ ok: false, reason: 'Format non reconnu' }` ; chrono-node a déjà des limites de taille (cf. tests) |
| T-02-04 (R3 RESEARCH) | Tampering | refonte zod sans grep préalable | mitigate | Étape 1 du Task 1.1 : grep -r `courseExpressSchema` AVANT suppression ; bloquer si ≥ 1 consommateur |
| T-02-05 | Information Disclosure | placeholder Visible Progress contenant données patient réelles | mitigate | Captures produites Wave 5 utilisent UNIQUEMENT le seed démo 974 (Phase 0.7) — pas de patient réel ; documenté dans README showcase |
| T-02-06 | Repudiation | tests RED qui passent en CI sans avoir vraiment testé | accept | Wave 0 = scaffold volontaire ; le checker Wave 1+ vérifie que `plan(0)` devient `plan(N)` |
</threat_model>

<verification>
- `pnpm --filter @tap/shared test` : tous les tests Vitest GREEN (incluant ride + parse-freeform-date)
- `pnpm --filter @tap/shared typecheck && pnpm --filter @tap/web typecheck` exit 0
- `pnpm --filter @tap/web test:e2e -- saisie-express` : 6 tests skipped, 0 failed
- `grep -c "courseExpressSchema\|typeTransportSchema" packages/` retourne 0 (legacy purgé)
- `grep -c "rideExpressInputSchema" packages/shared/src/index.ts` ≥ 1 (nouveau symbole exporté)
- Commit final phase autonomy : aucun TODO bloquant pour Wave 1
</verification>

<success_criteria>
- D-08 implémenté à 100% (refonte zod alignée pickup/dropoff/scheduled_at + 4 transport modes + 3 urgency)
- D-05/D-09 outillage prêt (chrono-node FR + parseFreeformDate testé Indian/Reunion)
- Les 3 fichiers pgTAP scaffolds existent (plan(0)) — Wave 1 les remplira
- Le scaffold E2E saisie-express.spec.ts compile et présente 6 test.skip alignés sur SAIS-01..06
- Composants shadcn dialog + dropdown-menu installés et typés
- Aucun consommateur résiduel des symboles legacy supprimés
- Visible Progress README placeholder présent (les .png/.gif viendront Wave 5)
</success_criteria>

<output>
À l'issue de Wave 0, créer `.planning/phases/02-saisie-express-course/02-01-SUMMARY.md` listant :
- Symboles supprimés (courseExpressSchema, typeTransportSchema, CourseExpressInput, adresseSchema usage retiré du fichier ride)
- Symboles ajoutés (rideExpressInputSchema, rideDraftSchema, rideTransportModeSchema, rideUrgencySchema, parseFreeformDate, ParseFreeformDateResult)
- Dépendance ajoutée : chrono-node@2.9.1 (justifiée par DEC-005, gain 5–8 s/saisie)
- Composants shadcn ajoutés : dialog, dropdown-menu
- Scaffolds RED : 3 pgTAP plan(0) + 1 Playwright 6×skip
- Tests Vitest GREEN : 10 (ride) + 8 (parse-freeform-date) = 18
</output>
