---
phase: 02-saisie-express-course
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - apps/web/src/lib/keyboard-shortcuts.tsx
  - apps/web/src/app/(app)/courses/_components/ride-express-orchestrator.client.tsx
  - apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx
  - apps/web/src/app/(app)/courses/_components/ride-orchestrator-context.client.tsx
autonomous: true
requirements: [SAIS-02, SAIS-03, SAIS-05]

must_haves:
  truths:
    - "Le hook useGlobalShortcut écoute Cmd/Ctrl+Shift+K (PAS Cmd+N — bloqué browser cf. RESEARCH C1) et déclenche e.preventDefault() + callback"
    - "RideExpressOrchestrator monte un store useReducer (Draft[]) et expose un Context minimal (dispatch + drafts) consommé par DraftQueue/Header (Wave 4)"
    - "Quand l'utilisateur déclenche OPEN_NEW alors qu'un modal est en cours, le modal courant flushAutoSave() puis se MINIMIZE, le nouveau modal ouvre vide"
    - "Le 1er modal reste accessible via la file d'attente brouillons et peut être REPRIS par RESUME(draftId)"
    - "RideExpressModal contient un formulaire avec PatientSearch (réutilisé Phase 1), date freeform parsée par parseFreeformDate, pickup/dropoff (datalist HTML5 alimenté par listRecentPickupAddresses), transport_mode + urgency (defaults), notes ≤ 500 chars"
    - "Auto-save Server Action upsertRideDraft via debounce 5s + onBlur + onClose (D-05) ; indicator « Sauvegardé il y a Xs » visible"
    - "Submit utilise useOptimistic OU useTransition (fallback) — ferme le modal en < 100 ms + toast Sonner ; sur erreur réouvre modal"
    - "Esc ferme le modal MAIS déclenche d'abord flushAutoSave() pour ne pas perdre la saisie"
    - "Dialog Radix utilisé (focus trap + aria-modal + Esc auto)"
  artifacts:
    - path: apps/web/src/lib/keyboard-shortcuts.tsx
      provides: "useGlobalShortcut hook (DOM keydown listener avec cleanup)"
      contains: "useGlobalShortcut"
    - path: apps/web/src/app/(app)/courses/_components/ride-orchestrator-context.client.tsx
      provides: "Context (dispatch, drafts) — consommé par Header bouton + et DraftQueue (Wave 4)"
      contains: "createContext"
    - path: apps/web/src/app/(app)/courses/_components/ride-express-orchestrator.client.tsx
      provides: "Store useReducer multi-instance + render du modal visible + écoute Cmd+Shift+K"
      contains: "useReducer"
    - path: apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx
      provides: "Formulaire saisie express avec auto-save + optimistic submit"
      contains: "RideExpressModal"
  key_links:
    - from: "ride-express-orchestrator.client.tsx"
      to: "useGlobalShortcut('mod+shift+k')"
      via: "callback dispatch OPEN_NEW"
      pattern: "useGlobalShortcut"
    - from: "ride-express-modal.client.tsx"
      to: "courses/actions upsertRideDraft + createRideAction"
      via: "Server Action call"
      pattern: "upsertRideDraft|createRideAction"
    - from: "ride-express-modal.client.tsx"
      to: "patients/_components/patient-search.client.tsx"
      via: "import + usage interne"
      pattern: "PatientSearch"
    - from: "ride-express-modal.client.tsx"
      to: "@tap/shared parseFreeformDate"
      via: "transformation onBlur date freeform → ISO"
      pattern: "parseFreeformDate"
---

<objective>
Wave 3 — Couche UI client de la saisie express. Implémente le store multi-instance (useReducer + Context minimal partagé avec Wave 4 pour le bouton header et la DraftQueue), le hook raccourci global `Cmd/Ctrl+Shift+K` (PAS `Cmd+N` — bloqué navigateur, RESEARCH C1+R1), et le composant `RideExpressModal` qui contient le formulaire avec auto-save Server Action 5s + onBlur, submit optimistic UI fade-out < 100 ms + toast Sonner, gestion Esc avec flushAutoSave, et réutilisation du `<PatientSearch>` Phase 1.

Purpose: Honorer SAIS-02 (raccourci global), SAIS-03 (recherche patient 2 chars dans modal — réutilise Phase 1), SAIS-05 (multi-saisies parallèles via useReducer), D-03 (modal global pas page dédiée), D-04 (instances modales séparées, pas tabs), D-05 (auto-save), DEC-005 (feedback < 100 ms), Pilier 1 UX (animation 150 ms, skeleton jamais spinners, focus trap a11y gratuit via Radix Dialog).

Output: 4 fichiers client (≤ 600 lignes total), modal fonctionnel mais pas encore monté dans le layout (Wave 4 le fait + livre la DraftQueue + bouton header).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/TAP/CLAUDE.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md

<interfaces>
<!-- Patterns existants à RÉUTILISER -->

From apps/web/src/components/cookie-banner.client.tsx (lignes 36-45 — useEffect cleanup pattern):
```typescript
useEffect(() => {
  const handler = () => { ... };
  window.addEventListener('cookie-consent-reset', handler);
  return () => window.removeEventListener('cookie-consent-reset', handler);
}, []);
```

From apps/web/src/app/(app)/patients/_components/patient-search.client.tsx (à RÉUTILISER tel quel):
```tsx
export function PatientSearch({ onSelect }: { onSelect: (p: Patient) => void }): JSX.Element;
// aria-label="Rechercher un patient" sur le champ Input (ligne 28)
```

From apps/web/src/app/(app)/patients/_components/patient-form.client.tsx (pattern useFormState):
```tsx
const [state, formAction] = useFormState(action, {});
// SubmitButton avec useFormStatus
```

From apps/web/src/components/ui/dialog.tsx (Wave 0 — installé):
```tsx
export const Dialog: React.FC;
export const DialogContent: React.FC;  // role="dialog", focus trap
export const DialogHeader / DialogTitle / DialogDescription / DialogFooter / DialogClose;
```

From @tap/shared (Wave 0):
```typescript
parseFreeformDate(input, ref): { ok: true, iso } | { ok: false, reason };
rideExpressInputSchema  // pour client-side parse safe avant submit
RideExpressInput, RideDraftInput
```

From apps/web/src/app/(app)/courses/actions.ts (Wave 2):
```typescript
createRideAction({ input, fromDraftId? }): Promise<{ success?, id?, error? }>;
upsertRideDraft({ id?, payload, patient_id? }): Promise<{ id } | { error }>;
deleteRideDraft(id): Promise<{ success, error? }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Hook useGlobalShortcut (Cmd/Ctrl+Shift+K) avec cleanup</name>
  <read_first>
    - /home/user/TAP/apps/web/src/components/cookie-banner.client.tsx (lignes 36-45 — pattern useEffect cleanup)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §C1 (Code exemple 3) + §R1 (DEC-015 contradiction Cmd+N → Cmd+Shift+K choisi)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`keyboard-shortcuts.tsx`
  </read_first>
  <action>
    Créer `apps/web/src/lib/keyboard-shortcuts.tsx` (≤ 60 lignes). Contenu :

    ```tsx
    'use client';

    import { useEffect } from 'react';

    /**
     * Définition d'un raccourci clavier global.
     *
     * IMPORTANT (RESEARCH C1) : `Cmd+N` / `Ctrl+N` sont réservés navigateur
     * et NON-INTERCEPTABLES. Phase 2 utilise donc `Cmd/Ctrl+Shift+K` (pattern
     * Slack/Linear). DEC-015 mentionnait Cmd+N — amendement assumé par Claude
     * dans le scope de discrétion délégué (CONTEXT.md §Claude's Discretion).
     */
    export interface ShortcutDef {
      /** true → metaKey OU ctrlKey requis (cross-platform Mac/Win) */
      mod: boolean;
      /** true → shiftKey requis ; false (default) → shiftKey ne doit PAS être pressé */
      shift?: boolean;
      /** Touche en minuscule (ex: 'k', 'n') — comparée à e.key.toLowerCase() */
      key: string;
    }

    /**
     * Enregistre un listener keydown global qui appelle `cb` quand `def` matche.
     * Cleanup automatique au démontage. La référence à `cb` doit être stable
     * (useCallback côté appelant) sinon le listener est ré-attaché à chaque render.
     */
    export function useGlobalShortcut(
      def: ShortcutDef,
      cb: () => void,
    ): void {
      useEffect(() => {
        const handler = (e: KeyboardEvent) => {
          const modOk = def.mod ? e.metaKey || e.ctrlKey : true;
          const shiftRequired = def.shift === true;
          const shiftOk = shiftRequired ? e.shiftKey : !e.shiftKey;
          if (modOk && shiftOk && e.key.toLowerCase() === def.key) {
            e.preventDefault();
            cb();
          }
        };
        window.addEventListener('keydown', handler);
        return () => window.removeEventListener('keydown', handler);
      }, [def.mod, def.shift, def.key, cb]);
    }
    ```

    **Garde-fous** :
    - Pas de `console.log` (CLAUDE.md §11)
    - Cleanup obligatoire dans le return du `useEffect`
    - Type ShortcutDef exporté pour tests futurs
    - Pas de logique métier — pur DOM event hook
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "useGlobalShortcut" apps/web/src/lib/keyboard-shortcuts.tsx && grep -c "removeEventListener" apps/web/src/lib/keyboard-shortcuts.tsx && grep -v '^[[:space:]]*//\|^[[:space:]]*\*' apps/web/src/lib/keyboard-shortcuts.tsx | grep -c "console\." && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useGlobalShortcut" apps/web/src/lib/keyboard-shortcuts.tsx` ≥ 1 (export présent)
    - `grep -c "removeEventListener" apps/web/src/lib/keyboard-shortcuts.tsx` ≥ 1 (cleanup présent)
    - 0 `console.*` hors commentaires
    - `pnpm --filter @tap/web typecheck` exit 0
    - Fichier ≤ 60 lignes
    - `'use client';` présent
  </acceptance_criteria>
  <done>
    - keyboard-shortcuts.tsx livré
    - Commit FR : `feat(02): hook useGlobalShortcut Cmd/Ctrl+Shift+K (R1 DEC-015 amendement)`
  </done>
</task>

<task type="auto">
  <name>Task 4.2: Orchestrator multi-instance + Context partagé (useReducer + dispatch)</name>
  <read_first>
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §C4 (multi-instance store, spec exhaustive)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`ride-express-orchestrator.client.tsx`
    - apps/web/src/lib/keyboard-shortcuts.tsx (livré Task 4.1)
  </read_first>
  <action>
    Créer 2 fichiers complémentaires.

    **(A) `apps/web/src/app/(app)/courses/_components/ride-orchestrator-context.client.tsx`** (~35 lignes) — Context minimal partagé orchestrator ↔ header bouton + DraftQueue (Wave 4) :

    ```tsx
    'use client';

    import { createContext, useContext } from 'react';

    export interface DraftEntry {
      tempKey: string;
      draftId?: string;
      patientId?: string;
      minimized: boolean;
    }

    export type DraftAction =
      | { type: 'OPEN_NEW' }
      | { type: 'CLOSE'; tempKey: string }
      | { type: 'RESUME'; draftId: string; patientId?: string }
      | { type: 'MINIMIZE'; tempKey: string }
      | { type: 'SET_DRAFT_ID'; tempKey: string; draftId: string };

    export interface RideOrchestratorCtx {
      drafts: DraftEntry[];
      dispatch: (a: DraftAction) => void;
    }

    const Ctx = createContext<RideOrchestratorCtx | null>(null);

    export const RideOrchestratorProvider = Ctx.Provider;

    export function useRideOrchestrator(): RideOrchestratorCtx {
      const v = useContext(Ctx);
      if (!v) {
        throw new Error(
          'useRideOrchestrator doit être utilisé sous <RideExpressOrchestrator>.',
        );
      }
      return v;
    }
    ```

    **Justification du Context (vs RESEARCH §C4 qui dit « pas de Context »)** : RESEARCH dit pas de Context POUR LE STORE INTERNE de l'orchestrator (pour éviter re-renders en cascade). Mais pour PARTAGER le `dispatch` avec la `DraftQueue` du header (Wave 4), un Context très ciblé `{drafts, dispatch}` est nécessaire et acceptable (overhead minimal — il alimente uniquement 2-3 consumers, pas tout le sous-arbre layout). Cette décision s'inscrit dans le scope de discrétion Claude.

    **(B) `apps/web/src/app/(app)/courses/_components/ride-express-orchestrator.client.tsx`** (~120 lignes) :

    ```tsx
    'use client';

    import { useCallback, useReducer } from 'react';
    import { useGlobalShortcut } from '@/lib/keyboard-shortcuts';
    import { RideExpressModal } from './ride-express-modal.client';
    import {
      RideOrchestratorProvider,
      type DraftAction,
      type DraftEntry,
    } from './ride-orchestrator-context.client';

    function generateTempKey(): string {
      return globalThis.crypto?.randomUUID() ?? `tmp-${Date.now()}-${Math.random()}`;
    }

    function draftsReducer(state: DraftEntry[], a: DraftAction): DraftEntry[] {
      switch (a.type) {
        case 'OPEN_NEW': {
          // Auto-minimise tout modal visible avant d'en ouvrir un nouveau (D-04)
          const minimized = state.map((d) =>
            d.minimized ? d : { ...d, minimized: true },
          );
          return [
            ...minimized,
            { tempKey: generateTempKey(), minimized: false },
          ];
        }
        case 'CLOSE':
          return state.filter((d) => d.tempKey !== a.tempKey);
        case 'MINIMIZE':
          return state.map((d) =>
            d.tempKey === a.tempKey ? { ...d, minimized: true } : d,
          );
        case 'RESUME': {
          // Si déjà ouvert : démise. Sinon ajoute en visible et minimise les autres.
          const exists = state.find((d) => d.draftId === a.draftId);
          if (exists) {
            return state.map((d) =>
              d.draftId === a.draftId
                ? { ...d, minimized: false }
                : { ...d, minimized: true },
            );
          }
          const minimized = state.map((d) => ({ ...d, minimized: true }));
          return [
            ...minimized,
            {
              tempKey: generateTempKey(),
              draftId: a.draftId,
              patientId: a.patientId,
              minimized: false,
            },
          ];
        }
        case 'SET_DRAFT_ID':
          return state.map((d) =>
            d.tempKey === a.tempKey ? { ...d, draftId: a.draftId } : d,
          );
        default:
          return state;
      }
    }

    /**
     * Orchestrator multi-instance modale. Monté UNE fois dans (app)/layout.tsx.
     * Écoute le raccourci global Cmd/Ctrl+Shift+K et expose un dispatch via Context
     * pour le bouton header « + Nouvelle course » et la DraftQueue (Wave 4).
     */
    export function RideExpressOrchestrator({
      children,
    }: {
      children?: React.ReactNode;
    }): JSX.Element {
      const [drafts, dispatch] = useReducer(draftsReducer, []);
      const openNew = useCallback(() => dispatch({ type: 'OPEN_NEW' }), []);

      useGlobalShortcut({ mod: true, shift: true, key: 'k' }, openNew);

      const visible = drafts.find((d) => !d.minimized);

      return (
        <RideOrchestratorProvider value={{ drafts, dispatch }}>
          {children}
          {visible && (
            <RideExpressModal
              key={visible.tempKey}
              tempKey={visible.tempKey}
              draftId={visible.draftId}
              initialPatientId={visible.patientId}
              onClose={() =>
                dispatch({ type: 'CLOSE', tempKey: visible.tempKey })
              }
              onMinimize={() =>
                dispatch({ type: 'MINIMIZE', tempKey: visible.tempKey })
              }
              onDraftIdResolved={(id) =>
                dispatch({
                  type: 'SET_DRAFT_ID',
                  tempKey: visible.tempKey,
                  draftId: id,
                })
              }
            />
          )}
        </RideOrchestratorProvider>
      );
    }
    ```

    **Garde-fous** :
    - Pas de `console.log`
    - reducer ≤ 50 lignes (pure fonction, testable)
    - Aucune Server Action appelée directement ici (l'orchestrator ne sait rien du DB ; le modal child s'occupe d'auto-save)
    - Cleanup automatique via Context provider unmount
    - Le `children` permet de placer header/main entre le Provider racine — usage par layout Wave 4
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "useReducer" apps/web/src/app/\(app\)/courses/_components/ride-express-orchestrator.client.tsx && grep -c "useGlobalShortcut" apps/web/src/app/\(app\)/courses/_components/ride-express-orchestrator.client.tsx && grep -c "RideOrchestratorProvider\|useRideOrchestrator" apps/web/src/app/\(app\)/courses/_components/ride-orchestrator-context.client.tsx && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useReducer" ride-express-orchestrator.client.tsx` ≥ 1
    - `grep -c "OPEN_NEW\|CLOSE\|MINIMIZE\|RESUME\|SET_DRAFT_ID" ride-express-orchestrator.client.tsx` ≥ 5 (les 5 actions présentes)
    - `grep -c "useGlobalShortcut" ride-express-orchestrator.client.tsx` ≥ 1 (raccourci câblé)
    - `useRideOrchestrator` hook exporté du Context
    - 0 `console.*` hors commentaires
    - `pnpm --filter @tap/web typecheck` exit 0 (en parallèle de Task 4.3 qui livre RideExpressModal — typecheck final attendu après Task 4.3)
    - Fichiers respectivement ≤ 35 et ≤ 130 lignes
  </acceptance_criteria>
  <done>
    - 2 fichiers livrés, reducer testable
    - Commit FR : `feat(02): orchestrator multi-instance saisie express + Context dispatch`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.3: RideExpressModal (Dialog Radix + form + auto-save 5s + optimistic submit + Esc handling)</name>
  <read_first>
    - /home/user/TAP/apps/web/src/app/(app)/patients/_components/patient-search.client.tsx (réutilisé dans le modal — ne PAS réimplémenter)
    - /home/user/TAP/apps/web/src/app/(app)/patients/_components/patient-form.client.tsx (pattern useFormState + sections)
    - /home/user/TAP/apps/web/src/components/ui/dialog.tsx (Wave 0 — wrapper Radix)
    - /home/user/TAP/apps/web/src/components/ui/sonner.tsx (toast déjà installé)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §C2 + §C3 + §C5 + §C6 + §C7 + §Pitfall 2 + §Pitfall 3 + §Pitfall 5
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`ride-express-modal.client.tsx`
  </read_first>
  <behavior>
    - Test 1 (E2E Wave 5) : ouverture du modal autofocus le champ patient (Wave 5 — pas de Vitest UI ici)
    - Test 2 : remplir tous les champs + cliquer Créer → modal ferme < 100 ms + toast Sonner « Course créée pour {Patient} le {date} » apparaît
    - Test 3 : Esc → flushAutoSave (await upsertRideDraft) → modal ferme sans perte de données
    - Test 4 : taper « 15/05 14h30 » dans Date → onBlur affiche la valeur ISO en hint « 15 mai 2026 à 14h30 »
    - Test 5 : taper 80 chars en 4 s → 1 seule Server Action upsertRideDraft (debounce respecté — Pitfall 2)
    - Test 6 : si Server Action createRideAction throw → toast erreur + modal réouvre avec valeurs préservées (Pitfall 3)
    - Test 7 : indicator « Sauvegardé il y a Xs » présent et mis à jour
  </behavior>
  <action>
    Créer `apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx` (≤ 280 lignes — proche de la limite 300 ; respecter les sous-fonctions ≤ 50 lignes pour éviter le dépassement).

    **Structure squelette** (à remplir avec les détails RESEARCH C3+C5+C6) :

    ```tsx
    'use client';

    import {
      useCallback,
      useEffect,
      useRef,
      useState,
      useTransition,
    } from 'react';
    import { toast } from 'sonner';
    import {
      Dialog,
      DialogContent,
      DialogHeader,
      DialogTitle,
      DialogFooter,
    } from '@/components/ui/dialog';
    import { Input } from '@/components/ui/input';
    import { Label } from '@/components/ui/label';
    import { Button } from '@/components/ui/button';
    import { PatientSearch } from '../../patients/_components/patient-search.client';
    import { parseFreeformDate } from '@tap/shared';
    import {
      rideExpressInputSchema,
      type RideDraftInput,
    } from '@tap/shared';
    import {
      createRideAction,
      upsertRideDraft,
    } from '../actions';

    const TRANSPORT_OPTIONS = [
      { value: 'taxi_conventionne', label: 'Taxi conventionné' },
      { value: 'tpmr', label: 'TPMR (fauteuil)' },
      { value: 'vsl', label: 'VSL' },
      { value: 'ambulance', label: 'Ambulance' },
    ] as const;

    const URGENCY_OPTIONS = [
      { value: 'programmee', label: 'Programmée' },
      { value: 'urgente', label: 'Urgente' },
      { value: 'immediate', label: 'Immédiate' },
    ] as const;

    interface Props {
      tempKey: string;
      draftId?: string;
      initialPatientId?: string;
      onClose: () => void;
      onMinimize: () => void;
      onDraftIdResolved: (id: string) => void;
    }

    export function RideExpressModal(props: Props): JSX.Element {
      const [form, setForm] = useState<RideDraftInput & { dateInput?: string }>({
        patient_id: props.initialPatientId,
      });
      const [patientLabel, setPatientLabel] = useState<string>('');
      const [dateError, setDateError] = useState<string | null>(null);
      const [savingState, setSavingState] = useState<'idle' | 'saving' | 'saved' | 'error'>('idle');
      const [lastSavedAt, setLastSavedAt] = useState<number | null>(null);
      const [isPending, startTransition] = useTransition();
      const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);
      const draftIdRef = useRef<string | undefined>(props.draftId);

      // Debounce auto-save 5 s — implémenté inline (~15 lignes — pas de dep use-debounce)
      const scheduleSave = useCallback((next: RideDraftInput) => {
        if (debounceRef.current) clearTimeout(debounceRef.current);
        debounceRef.current = setTimeout(() => flushSave(next), 5000);
      }, []);

      const flushSave = useCallback(async (payload: RideDraftInput) => {
        if (debounceRef.current) {
          clearTimeout(debounceRef.current);
          debounceRef.current = null;
        }
        setSavingState('saving');
        const res = await upsertRideDraft({
          id: draftIdRef.current,
          payload,
          patient_id: payload.patient_id,
        });
        if ('error' in res) {
          setSavingState('error');
          toast.error('Sauvegarde impossible — réessai automatique');
          return;
        }
        if (!draftIdRef.current) {
          draftIdRef.current = res.id;
          props.onDraftIdResolved(res.id);
        }
        setSavingState('saved');
        setLastSavedAt(Date.now());
      }, [props]);

      // Update form helper
      const updateField = useCallback(<K extends keyof RideDraftInput>(
        key: K,
        value: RideDraftInput[K],
      ) => {
        setForm((prev) => {
          const next = { ...prev, [key]: value };
          scheduleSave(next);
          return next;
        });
      }, [scheduleSave]);

      // Cleanup debounce on unmount
      useEffect(() => {
        return () => {
          if (debounceRef.current) clearTimeout(debounceRef.current);
        };
      }, []);

      // Esc handling : Radix appelle onOpenChange(false) → flushSave puis close
      const handleOpenChange = useCallback((open: boolean) => {
        if (!open) {
          void flushSave(form).then(props.onClose);
        }
      }, [flushSave, form, props.onClose]);

      // Submit
      const handleSubmit = useCallback((e: React.FormEvent) => {
        e.preventDefault();
        // Date freeform parsing côté client (Pitfall 5 — TZ navigateur Réunion)
        if (form.dateInput) {
          const parsed = parseFreeformDate(form.dateInput);
          if (!parsed.ok) {
            setDateError(parsed.reason);
            return;
          }
          form.scheduled_at = parsed.iso;
        }
        const validation = rideExpressInputSchema.safeParse({
          ...form,
          dateInput: undefined,
        });
        if (!validation.success) {
          toast.error(validation.error.errors[0]?.message ?? 'Saisie invalide.');
          return;
        }
        // Optimistic close + toast
        const optimisticLabel = `Course créée pour ${patientLabel || 'le patient sélectionné'}`;
        toast.success(optimisticLabel);
        const snapshot = { ...form };
        startTransition(async () => {
          try {
            const res = await createRideAction({
              input: validation.data,
              fromDraftId: draftIdRef.current,
            });
            if (res.error) {
              toast.error(`Échec — saisie restaurée (${res.error})`);
              setForm(snapshot);
              // Modal reste/réouvre — l'orchestrator ne ferme pas tant que le toast erreur
              return;
            }
            props.onClose();
          } catch (err) {
            toast.error('Échec — saisie restaurée');
            setForm(snapshot);
          }
        });
      }, [form, patientLabel, props]);

      return (
        <Dialog open onOpenChange={handleOpenChange}>
          <DialogContent
            className="max-w-[600px]"
            aria-label="Saisie express d'une course"
          >
            <DialogHeader>
              <DialogTitle>Nouvelle course</DialogTitle>
            </DialogHeader>

            <form onSubmit={handleSubmit} className="space-y-16" noValidate>
              {/* Champ patient — réutilise PatientSearch Phase 1 */}
              <div>
                <Label htmlFor="patient">Rechercher un patient</Label>
                <PatientSearch
                  onSelect={(p) => {
                    updateField('patient_id', p.id);
                    setPatientLabel(`${p.prenom} ${p.nom}`);
                  }}
                />
              </div>

              {/* Date freeform */}
              <div>
                <Label htmlFor="date">Date et heure</Label>
                <Input
                  id="date"
                  aria-label="Date et heure"
                  placeholder="ex : 15/05 14h30 — demain 8h — lundi 9h"
                  value={form.dateInput ?? ''}
                  onChange={(e) =>
                    setForm((prev) => ({ ...prev, dateInput: e.target.value }))
                  }
                  onBlur={() => {
                    if (!form.dateInput) return;
                    const parsed = parseFreeformDate(form.dateInput);
                    if (parsed.ok) {
                      setDateError(null);
                      updateField('scheduled_at', parsed.iso);
                    } else {
                      setDateError(parsed.reason);
                    }
                    void flushSave(form);
                  }}
                  autoComplete="off"
                  tabIndex={2}
                />
                {dateError && (
                  <p className="text-xs text-destructive mt-4" role="alert">
                    {dateError}
                  </p>
                )}
              </div>

              {/* Pickup + Dropoff — datalist HTML5 alimenté côté future via prop pickupSuggestions */}
              <div>
                <Label htmlFor="pickup">Adresse de prise en charge</Label>
                <Input
                  id="pickup"
                  aria-label="Adresse de prise en charge"
                  value={form.pickup_address ?? ''}
                  onChange={(e) => updateField('pickup_address', e.target.value)}
                  onBlur={() => void flushSave(form)}
                  autoComplete="off"
                  tabIndex={3}
                  required
                />
              </div>
              <div>
                <Label htmlFor="dropoff">Adresse de destination</Label>
                <Input
                  id="dropoff"
                  aria-label="Adresse de destination"
                  value={form.dropoff_address ?? ''}
                  onChange={(e) => updateField('dropoff_address', e.target.value)}
                  onBlur={() => void flushSave(form)}
                  autoComplete="off"
                  tabIndex={4}
                  required
                />
              </div>

              {/* Mode + urgence — selects natifs simples */}
              <div className="grid grid-cols-2 gap-16">
                <div>
                  <Label htmlFor="mode">Mode de transport</Label>
                  <select
                    id="mode"
                    aria-label="Mode de transport"
                    className="w-full h-40 rounded-md border bg-background px-12"
                    value={form.transport_mode ?? 'taxi_conventionne'}
                    onChange={(e) =>
                      updateField('transport_mode', e.target.value as never)
                    }
                    tabIndex={5}
                  >
                    {TRANSPORT_OPTIONS.map((opt) => (
                      <option key={opt.value} value={opt.value}>
                        {opt.label}
                      </option>
                    ))}
                  </select>
                </div>
                <div>
                  <Label htmlFor="urgency">Urgence</Label>
                  <select
                    id="urgency"
                    aria-label="Urgence"
                    className="w-full h-40 rounded-md border bg-background px-12"
                    value={form.urgency ?? 'programmee'}
                    onChange={(e) => updateField('urgency', e.target.value as never)}
                    tabIndex={6}
                  >
                    {URGENCY_OPTIONS.map((opt) => (
                      <option key={opt.value} value={opt.value}>
                        {opt.label}
                      </option>
                    ))}
                  </select>
                </div>
              </div>

              {/* Notes */}
              <div>
                <Label htmlFor="notes">Notes (optionnel)</Label>
                <Input
                  id="notes"
                  aria-label="Notes"
                  value={form.notes_regulateur ?? ''}
                  onChange={(e) => updateField('notes_regulateur', e.target.value)}
                  onBlur={() => void flushSave(form)}
                  maxLength={500}
                  tabIndex={7}
                />
              </div>

              {/* Indicator auto-save (D-05 specifics §11.5) */}
              <div className="text-xs text-muted-foreground">
                <SavingIndicator state={savingState} lastSavedAt={lastSavedAt} />
              </div>

              <DialogFooter>
                <Button
                  type="button"
                  variant="ghost"
                  onClick={props.onMinimize}
                >
                  Mettre en pause
                </Button>
                <Button type="submit" disabled={isPending} tabIndex={8}>
                  {isPending ? 'Création…' : 'Créer la course'}
                </Button>
              </DialogFooter>
            </form>
          </DialogContent>
        </Dialog>
      );
    }

    function SavingIndicator({
      state,
      lastSavedAt,
    }: {
      state: 'idle' | 'saving' | 'saved' | 'error';
      lastSavedAt: number | null;
    }): JSX.Element {
      const [tick, setTick] = useState(0);
      useEffect(() => {
        const id = setInterval(() => setTick((t) => t + 1), 5000);
        return () => clearInterval(id);
      }, []);

      if (state === 'saving') return <span aria-live="polite">Sauvegarde…</span>;
      if (state === 'error')
        return (
          <span className="text-destructive" role="alert">
            Erreur de sauvegarde — réessai dans 5 s
          </span>
        );
      if (state === 'saved' && lastSavedAt) {
        const seconds = Math.max(0, Math.floor((Date.now() - lastSavedAt) / 1000));
        // tick force le re-render every 5s pour mettre à jour le compteur
        void tick;
        return <span aria-live="polite">Sauvegardé il y a {seconds} s</span>;
      }
      return <span>&nbsp;</span>;
    }
    ```

    **Garde-fous critiques** :
    - Fichier ≤ 300 lignes (proche de la limite — extraire SavingIndicator en sous-composant déjà fait pour rester sous la limite ; fonctions ≤ 50 lignes)
    - `'use client';` en haut
    - PAS de logique métier serveur — uniquement orchestration UI + appel actions Wave 2
    - PAS de `console.log`
    - PAS d'import `@supabase/*` direct
    - PatientSearch RÉUTILISÉ tel quel (pas de duplication)
    - parseFreeformDate appelé UNIQUEMENT côté client (Pitfall 5)
    - Tab order explicite : 1 (patient via PatientSearch interne) → 2 (date) → 3 (pickup) → 4 (dropoff) → 5 (mode) → 6 (urgency) → 7 (notes) → 8 (submit)
    - aria-label exacts pour E2E : « Rechercher un patient », « Date et heure », « Adresse de prise en charge », « Adresse de destination », « Mode de transport », « Urgence », « Notes »
    - Le bouton Créer affiche `Création…` pendant `isPending`
    - Esc déclenche flushSave AVANT close (D-05 — pas de perte)
    - L'erreur réseau réinjecte le snapshot et toast erreur (Pitfall 3)

    **Note sur useOptimistic** : la version livrée utilise `useTransition + setForm(snapshot)` (fallback recommandé R2 si useOptimistic indisponible en React 18.3 sans flag canary). Si useOptimistic est disponible et stable au runtime, un futur refacto V1.5 peut le réintégrer sans changement de UX visible.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "RideExpressModal" apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx && grep -c "PatientSearch" apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx && grep -c "parseFreeformDate" apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx && grep -c "upsertRideDraft\|createRideAction" apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx && grep -c "Rechercher un patient\|Date et heure\|Adresse de prise en charge\|Adresse de destination\|Mode de transport\|Urgence" apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx && grep -v '^[[:space:]]*//\|^[[:space:]]*\*' apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx | grep -c "console\." && wc -l apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PatientSearch" ride-express-modal.client.tsx` ≥ 1 (réutilisation Phase 1)
    - `grep -c "parseFreeformDate" ride-express-modal.client.tsx` ≥ 1 (date freeform)
    - `grep -c "upsertRideDraft\|createRideAction" ride-express-modal.client.tsx` ≥ 2
    - 6 aria-label distincts présents (Rechercher un patient, Date et heure, Adresse de prise en charge, Adresse de destination, Mode de transport, Urgence)
    - 0 `console.*` hors commentaires
    - 0 import `@supabase/*` direct
    - Fichier ≤ 300 lignes (`wc -l` ≤ 300)
    - `pnpm --filter @tap/web typecheck` exit 0
    - `pnpm --filter @tap/web test:e2e -- saisie-express` ne casse pas (les 6 tests sont toujours skipped — passeront GREEN Wave 5)
  </acceptance_criteria>
  <done>
    - RideExpressModal livré, typecheck OK
    - Aucun anti-pattern
    - Commit FR : `feat(02): RideExpressModal Dialog Radix + auto-save + optimistic submit`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → Server Action | Submit + auto-save passent par RPC Server Action (cookie session, RLS) |
| Browser → React state local | form state contient patient_id (PII faible) — XSS empêche par échappement React (pas d'innerHTML) |
| Browser keyboard listener (window) | useGlobalShortcut — pas de capture de touches sensibles |
| Modal mount/unmount → store | dispatch CLOSE/MINIMIZE est local au layout — pas de fuite cross-org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T2 | Information Disclosure | form state inclut patient_id, leak via console.log debug | mitigate | Aucun console.log autorisé (CLAUDE.md §11) ; gate grep dans verify ; pas de patient_id dans Sentry breadcrumbs (configurer en V1.5) |
| T-02-T3 | Tampering | client bypass de createRideAction via fetch direct sur l'endpoint Next | accept | Server Actions sont protégées par Next.js (CSRF token automatique) + auth.getUser() côté serveur ; un attaquant aurait besoin du cookie session valide |
| T-02-T4 | Race | Multi-instance : 2 onglets dispatch OPEN_NEW simultané → 2 modals visibles | mitigate | reducer pure : OPEN_NEW minimise tous les autres avant push ; collision impossible dans un seul render React |
| T-02-T5 | DoS | Cmd+Shift+K conflit avec extension navigateur (R1 RESEARCH) | accept (low) | Documenté dans Open Questions ; alternative Alt+N en V1.5 si remontée terrain |
| T-02-T6 | DoS | Auto-save spam sur frappe rapide (Pitfall 2) | mitigate | debounce 5s + clearTimeout au unmount + flush onBlur seulement (pas onChange direct) ; cleanup useEffect |
| T-02-PIT3 | Repudiation | Optimistic UI masque échec Server Action (Pitfall 3) | mitigate | try/catch handleSubmit + toast.error + setForm(snapshot) — utilisateur informé et données restaurées |
| T-02-PIT5 | Tampering | TZ Indian/Reunion mal résolue dans parseFreeformDate (Pitfall 5) | mitigate | parseFreeformDate appelée UNIQUEMENT côté client ; navigateur régulatrice = TZ Réunion ; Server Action stocke ISO directement, pas de re-parse |
</threat_model>

<verification>
- `pnpm --filter @tap/web typecheck` exit 0
- 4 fichiers livrés (≤ 60 + ≤ 35 + ≤ 130 + ≤ 300 = ≤ 525 lignes total)
- 0 `console.*` hors commentaires (`grep -rE "console\." apps/web/src/app/\(app\)/courses/_components apps/web/src/lib/keyboard-shortcuts.tsx | grep -v '^.*://'` = 0)
- 0 import `@supabase/*` direct hors lib wrappers
- aria-labels exacts pour les 6 champs principaux (test E2E Wave 5 dépendra de cette nomenclature)
- Esc handling câblé (handleOpenChange → flushSave → onClose)
- Tab order 1-8 respecté
- Le composant n'est PAS encore monté dans layout — c'est Wave 4 qui l'intègrera + livrera le bouton header + DraftQueue
</verification>

<success_criteria>
- SAIS-02 : raccourci Cmd/Ctrl+Shift+K opérationnel via useGlobalShortcut
- SAIS-03 : recherche patient 2 chars dans le modal via PatientSearch réutilisé
- SAIS-05 : multi-saisies parallèles via reducer (OPEN_NEW minimise les autres)
- D-04 : instances modales séparées (visible = 1 à la fois, dropdown header pour les minimisés Wave 4)
- D-05 : auto-save 5s + onBlur + onClose Esc (flushSave avant unmount)
- D-06 : optimistic UI close + toast Sonner ; sur erreur reopen + restore
- DEC-005 : feedback < 100 ms (close immédiat), feedback < 200 ms (toast)
- Pilier 1 UX : Dialog Radix focus trap a11y, 8 tabs ordonnés, aria-labels FR
</success_criteria>

<output>
À l'issue de Wave 3, créer `.planning/phases/02-saisie-express-course/02-04-SUMMARY.md` listant :
- 4 fichiers UI client livrés
- Réutilisation PatientSearch Phase 1 (zéro duplication)
- Reducer testable + Context minimal partagé pour Wave 4
- Flow auto-save + optimistic submit + Esc handling
- Note R1 : DEC-015 amendé Cmd+N → Cmd+Shift+K (non-interceptable Cmd+N)
</output>
