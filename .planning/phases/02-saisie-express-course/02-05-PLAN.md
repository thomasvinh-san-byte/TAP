---
phase: 02-saisie-express-course
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - apps/web/src/app/(app)/layout.tsx
  - apps/web/src/app/(app)/courses/page.tsx
  - apps/web/src/app/(app)/courses/_components/draft-queue.client.tsx
  - apps/web/src/app/(app)/courses/_components/rides-list.client.tsx
  - apps/web/src/app/(app)/courses/_components/header-new-ride-button.client.tsx
autonomous: true
requirements: [SAIS-04, SAIS-05]

must_haves:
  truths:
    - "Le layout (app)/layout.tsx monte <RideExpressOrchestrator> qui enveloppe le header et main, exposant le dispatch via Context aux enfants"
    - "Le header global affiche un bouton « + Nouvelle course » qui dispatch OPEN_NEW et ouvre le modal"
    - "Le header affiche le compteur DraftQueue (icône Inbox + badge) ; click ouvre dropdown avec liste des brouillons reprenables"
    - "Click sur un brouillon dans la dropdown dispatch RESUME(draftId) et réouvre le modal avec valeurs restaurées"
    - "La page /courses prefetch listRides via QueryClient + HydrationBoundary, et délègue à <RidesList> client (filtres status/transport_mode/urgency + recherche)"
    - "RidesList affiche skeleton pendant loading, empty state explicite « Aucune course », tableau avec scheduled_at/patient/adresses/mode/urgency/status"
    - "Le lien « Courses » apparaît dans le header de navigation"
    - "Aucune logique métier hors actions Wave 2 + queries Wave 2"
  artifacts:
    - path: apps/web/src/app/(app)/layout.tsx
      provides: "Layout étendu — Orchestrator + Header avec bouton + DraftQueue + nav Courses"
      contains: "RideExpressOrchestrator"
    - path: apps/web/src/app/(app)/courses/page.tsx
      provides: "RSC liste courses + prefetch listRides"
      contains: "HydrationBoundary"
    - path: apps/web/src/app/(app)/courses/_components/draft-queue.client.tsx
      provides: "Dropdown header brouillons (Inbox icon + badge + liste cliquable)"
      contains: "DraftQueue"
    - path: apps/web/src/app/(app)/courses/_components/rides-list.client.tsx
      provides: "Tableau client courses + filtres + skeleton + empty state"
      contains: "RidesList"
    - path: apps/web/src/app/(app)/courses/_components/header-new-ride-button.client.tsx
      provides: "Bouton + Nouvelle course consommant useRideOrchestrator"
      contains: "Nouvelle course"
  key_links:
    - from: "(app)/layout.tsx"
      to: "RideExpressOrchestrator (Wave 3)"
      via: "wrapping mount avec children"
      pattern: "RideExpressOrchestrator"
    - from: "header-new-ride-button.client.tsx"
      to: "useRideOrchestrator → dispatch OPEN_NEW"
      via: "Context dispatch"
      pattern: "useRideOrchestrator"
    - from: "draft-queue.client.tsx"
      to: "listDraftsAction (Wave 2) + dispatch RESUME"
      via: "useQuery + click handler"
      pattern: "listDraftsAction"
    - from: "courses/page.tsx"
      to: "listRides (Wave 2)"
      via: "RSC prefetch via QueryClient"
      pattern: "listRides|prefetchQuery"
---

<objective>
Wave 4 — Intégration UI : monter l'orchestrator dans le layout authentifié, ajouter le bouton « + Nouvelle course » et la `DraftQueue` (compteur Inbox + dropdown des brouillons reprenables) dans le header global, et créer la page `/courses` minimale (D-07) avec liste filtrée + skeleton + empty state. Aucune nouvelle dépendance, uniquement composition de Wave 2 (actions/queries) + Wave 3 (orchestrator/modal/Context).

Purpose: Compléter SAIS-04 (file d'attente brouillons reprenable) et SAIS-05 (multi-saisies — 2e modal sauvegarde le 1er en brouillon visible dans la file). Honorer D-03 (modal global, pas page /new), D-07 (page /courses minimale Phase 2).

Output: 5 fichiers (~1 modification + 4 créations), prêt pour Wave 5 E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/TAP/CLAUDE.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md

<interfaces>
<!-- Wave 3 livré -->

From Wave 3 (RideExpressOrchestrator, Context):
```tsx
export function RideExpressOrchestrator({ children }: { children?: React.ReactNode }): JSX.Element;
export function useRideOrchestrator(): { drafts: DraftEntry[]; dispatch: (a: DraftAction) => void };
```

From Wave 2 (queries + actions):
```typescript
listRides({ status?, transport_mode?, urgency? }): Promise<RideRow[]>;
listDraftsAction(): Promise<RideDraftRow[]>;  // wrapper Server Action client-callable
deleteRideDraft(id): Promise<{ success, error? }>;
```

From Phase 1 patterns:
- patients-list.client.tsx — useQuery + filtres + skeleton + empty state (réutilisable pattern)
- patients/page.tsx — HydrationBoundary + prefetch (réutilisable pattern)
- (app)/layout.tsx — Providers wrapper + nav header

Existing UI:
- @/components/ui/dropdown-menu (Wave 0 installé)
- @/components/ui/badge (existant)
- @/components/ui/skeleton (existant)
- lucide-react : Plus, Inbox, Loader2 (existants)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Layout intégration (Orchestrator + bouton header + DraftQueue + nav Courses)</name>
  <read_first>
    - /home/user/TAP/apps/web/src/app/(app)/layout.tsx (à modifier)
    - /home/user/TAP/apps/web/src/app/(app)/courses/_components/ride-express-orchestrator.client.tsx (Wave 3)
    - /home/user/TAP/apps/web/src/app/(app)/courses/_components/ride-orchestrator-context.client.tsx (Wave 3 — useRideOrchestrator)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`(app)/layout.tsx (modifié)`
  </read_first>
  <action>
    1. **Créer** `apps/web/src/app/(app)/courses/_components/header-new-ride-button.client.tsx` (≤ 30 lignes) :
       ```tsx
       'use client';

       import { Plus } from 'lucide-react';
       import { Button } from '@/components/ui/button';
       import { useRideOrchestrator } from './ride-orchestrator-context.client';

       export function HeaderNewRideButton(): JSX.Element {
         const { dispatch } = useRideOrchestrator();
         return (
           <Button
             type="button"
             variant="default"
             size="sm"
             onClick={() => dispatch({ type: 'OPEN_NEW' })}
             aria-label="Nouvelle course (Cmd/Ctrl+Shift+K)"
             className="gap-8"
           >
             <Plus className="h-16 w-16" aria-hidden />
             <span>Nouvelle course</span>
           </Button>
         );
       }
       ```

    2. **Modifier** `apps/web/src/app/(app)/layout.tsx` pour envelopper avec RideExpressOrchestrator et étendre le header (≤ 75 lignes au total) :
       ```tsx
       import Link from 'next/link';
       import { redirect } from 'next/navigation';
       import { createClient } from '@/lib/supabase/server';
       import { Providers } from './providers.client';
       import { RideExpressOrchestrator } from './courses/_components/ride-express-orchestrator.client';
       import { HeaderNewRideButton } from './courses/_components/header-new-ride-button.client';
       import { DraftQueue } from './courses/_components/draft-queue.client';

       export default async function AppLayout({
         children,
       }: {
         children: React.ReactNode;
       }) {
         const supabase = createClient();
         const { data: { user } } = await supabase.auth.getUser();
         if (!user) redirect('/login');

         return (
           <Providers>
             <RideExpressOrchestrator>
               <div className="min-h-screen flex flex-col">
                 <header className="border-b px-24 py-12 flex items-center justify-between gap-16">
                   <Link
                     href="/patients"
                     className="font-semibold text-foreground tracking-tight"
                   >
                     TAP Régulation
                   </Link>
                   <nav className="flex gap-16 text-sm text-muted-foreground items-center">
                     <Link href="/patients" className="hover:text-foreground transition-colors">
                       Patients
                     </Link>
                     <Link href="/courses" className="hover:text-foreground transition-colors">
                       Courses
                     </Link>
                     <DraftQueue />
                     <HeaderNewRideButton />
                   </nav>
                 </header>
                 <main className="flex-1 px-24 py-24 max-w-[1280px] w-full mx-auto">
                   {children}
                 </main>
               </div>
             </RideExpressOrchestrator>
           </Providers>
         );
       }
       ```

    3. **Vérifier compilation** : `pnpm --filter @tap/web typecheck` doit rester GREEN. Le bouton + DraftQueue n'existent pas encore — TypeScript le saura à la prochaine compilation après Tasks 5.2/5.3. Pour cette task isolément, créer les fichiers stubs avant de modifier layout.tsx, OU livrer Tasks 5.1/5.2/5.3 en un seul commit.

       Recommandation : commit unique « feat(02): layout intégré + DraftQueue + bouton header » couvrant Tasks 5.1-5.3. La séparation logique reste pour la lisibilité du plan, mais le commit est atomique.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "RideExpressOrchestrator" apps/web/src/app/\(app\)/layout.tsx && grep -c "HeaderNewRideButton\|DraftQueue" apps/web/src/app/\(app\)/layout.tsx && grep -c "href=\"/courses\"" apps/web/src/app/\(app\)/layout.tsx && grep -c "useRideOrchestrator" apps/web/src/app/\(app\)/courses/_components/header-new-ride-button.client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Layout import + monte `<RideExpressOrchestrator>`
    - Layout référence `<HeaderNewRideButton>` ET `<DraftQueue>`
    - Lien `/courses` présent dans la nav header
    - HeaderNewRideButton consomme useRideOrchestrator et dispatch OPEN_NEW
    - aria-label « Nouvelle course (Cmd/Ctrl+Shift+K) » présent (E2E s'appuiera dessus)
  </acceptance_criteria>
  <done>
    - Layout étendu, bouton header livré (commit atomique avec 5.2 + 5.3)
  </done>
</task>

<task type="auto">
  <name>Task 5.2: DraftQueue (dropdown brouillons header)</name>
  <read_first>
    - /home/user/TAP/apps/web/src/components/ui/dropdown-menu.tsx (Wave 0)
    - /home/user/TAP/apps/web/src/components/ui/badge.tsx
    - /home/user/TAP/apps/web/src/app/(app)/patients/_components/patients-list.client.tsx (pattern useQuery)
    - /home/user/TAP/apps/web/src/app/(app)/courses/actions.ts (Wave 2 — listDraftsAction, deleteRideDraft)
    - /home/user/TAP/apps/web/src/app/(app)/courses/_components/ride-orchestrator-context.client.tsx (Wave 3 — useRideOrchestrator)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`draft-queue.client.tsx`
  </read_first>
  <action>
    Créer `apps/web/src/app/(app)/courses/_components/draft-queue.client.tsx` (≤ 130 lignes) :

    ```tsx
    'use client';

    import { useQuery, useQueryClient } from '@tanstack/react-query';
    import { Inbox, Trash2 } from 'lucide-react';
    import { Badge } from '@/components/ui/badge';
    import { Button } from '@/components/ui/button';
    import {
      DropdownMenu,
      DropdownMenuContent,
      DropdownMenuItem,
      DropdownMenuLabel,
      DropdownMenuSeparator,
      DropdownMenuTrigger,
    } from '@/components/ui/dropdown-menu';
    import { listDraftsAction, deleteRideDraft } from '../actions';
    import { useRideOrchestrator } from './ride-orchestrator-context.client';

    interface DraftEntry {
      id: string;
      patient_id: string | null;
      payload: Record<string, unknown>;
      updated_at: string;
    }

    function formatPreview(d: DraftEntry): string {
      const p = d.payload as { pickup_address?: string; dropoff_address?: string };
      const pickup = p.pickup_address?.slice(0, 40) ?? 'Adresse à compléter';
      const dropoff = p.dropoff_address?.slice(0, 40) ?? '…';
      return `${pickup} → ${dropoff}`;
    }

    function formatRelative(updatedAt: string): string {
      const diff = Date.now() - new Date(updatedAt).getTime();
      const minutes = Math.floor(diff / 60_000);
      if (minutes < 1) return "à l'instant";
      if (minutes < 60) return `il y a ${minutes} min`;
      const hours = Math.floor(minutes / 60);
      if (hours < 24) return `il y a ${hours} h`;
      return `il y a ${Math.floor(hours / 24)} j`;
    }

    export function DraftQueue(): JSX.Element {
      const queryClient = useQueryClient();
      const { dispatch } = useRideOrchestrator();
      const { data, isPending } = useQuery({
        queryKey: ['ride-drafts'],
        queryFn: () => listDraftsAction(),
        staleTime: 5_000,
        refetchInterval: 10_000,
      });

      const drafts = (data ?? []) as DraftEntry[];
      const count = drafts.length;

      const handleResume = (draftId: string, patientId: string | null) => {
        dispatch({
          type: 'RESUME',
          draftId,
          patientId: patientId ?? undefined,
        });
      };

      const handleDelete = async (draftId: string) => {
        const res = await deleteRideDraft(draftId);
        if (res.success) {
          await queryClient.invalidateQueries({ queryKey: ['ride-drafts'] });
        }
      };

      return (
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Button
              type="button"
              variant="ghost"
              size="sm"
              aria-label={`Brouillons (${count})`}
              className="relative gap-8"
            >
              <Inbox className="h-16 w-16" aria-hidden />
              {count > 0 && (
                <Badge variant="secondary" className="ml-4">
                  {count}
                </Badge>
              )}
            </Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end" className="w-[360px]">
            <DropdownMenuLabel>
              {count === 0
                ? 'Aucun brouillon en cours'
                : `${count} brouillon${count > 1 ? 's' : ''} en cours`}
            </DropdownMenuLabel>
            <DropdownMenuSeparator />
            {isPending && (
              <DropdownMenuItem disabled>Chargement…</DropdownMenuItem>
            )}
            {!isPending && drafts.length === 0 && (
              <DropdownMenuItem disabled>
                Vos brouillons apparaîtront ici.
              </DropdownMenuItem>
            )}
            {drafts.map((d) => (
              <DropdownMenuItem
                key={d.id}
                onSelect={(e) => {
                  e.preventDefault();
                  handleResume(d.id, d.patient_id);
                }}
                className="flex flex-col items-start gap-4"
              >
                <span className="text-sm font-medium truncate w-full">
                  {formatPreview(d)}
                </span>
                <div className="flex items-center justify-between w-full text-xs text-muted-foreground">
                  <span>{formatRelative(d.updated_at)}</span>
                  <button
                    type="button"
                    aria-label="Supprimer ce brouillon"
                    onClick={(e) => {
                      e.stopPropagation();
                      void handleDelete(d.id);
                    }}
                    className="hover:text-destructive transition-colors"
                  >
                    <Trash2 className="h-12 w-12" aria-hidden />
                  </button>
                </div>
              </DropdownMenuItem>
            ))}
          </DropdownMenuContent>
        </DropdownMenu>
      );
    }
    ```

    **Garde-fous** :
    - Pas de `console.log`
    - Pas de `useEffect` pour fetch (useQuery → DEC-005 + RESEARCH §C7 anti-pattern)
    - Refetch interval 10 s (raisonnable, pas spam)
    - Click sur item → RESUME ; click sur Trash icon → delete sans propager (stopPropagation)
    - aria-label `Brouillons (N)` pour E2E SAIS-04
    - Format français (« il y a X min », « brouillon(s) »)
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "DraftQueue" apps/web/src/app/\(app\)/courses/_components/draft-queue.client.tsx && grep -c "useQuery" apps/web/src/app/\(app\)/courses/_components/draft-queue.client.tsx && grep -c "RESUME" apps/web/src/app/\(app\)/courses/_components/draft-queue.client.tsx && grep -c "deleteRideDraft" apps/web/src/app/\(app\)/courses/_components/draft-queue.client.tsx && grep -v '^[[:space:]]*//\|^[[:space:]]*\*' apps/web/src/app/\(app\)/courses/_components/draft-queue.client.tsx | grep -c "console\."</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useQuery" draft-queue.client.tsx` ≥ 1 (pas de useEffect-fetch)
    - `grep -c "RESUME" draft-queue.client.tsx` ≥ 1 (dispatch présent)
    - `grep -c "deleteRideDraft" draft-queue.client.tsx` ≥ 1
    - aria-label `Brouillons (` présent (pour E2E)
    - 0 console.* hors commentaires
    - Fichier ≤ 130 lignes
  </acceptance_criteria>
  <done>
    - DraftQueue livrée (commit atomique 5.1+5.2+5.3)
  </done>
</task>

<task type="auto">
  <name>Task 5.3: Page /courses RSC + RidesList client (filtres + skeleton + empty state)</name>
  <read_first>
    - /home/user/TAP/apps/web/src/app/(app)/patients/page.tsx (pattern HydrationBoundary + prefetch)
    - /home/user/TAP/apps/web/src/app/(app)/patients/_components/patients-list.client.tsx (pattern table + skeleton + empty state)
    - /home/user/TAP/apps/web/src/app/(app)/courses/_lib/queries.ts (Wave 2 — listRides + types)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`courses/page.tsx` + §`rides-list.client.tsx`
  </read_first>
  <action>
    1. **Créer** `apps/web/src/app/(app)/courses/page.tsx` (RSC, ≤ 50 lignes) — calque `patients/page.tsx` :
       ```tsx
       import {
         HydrationBoundary,
         dehydrate,
         QueryClient,
       } from '@tanstack/react-query';
       import { listRides } from './_lib/queries';
       import { RidesList } from './_components/rides-list.client';

       export const metadata = { title: 'Courses — TAP Régulation' };
       export const dynamic = 'force-dynamic';

       export default async function CoursesPage() {
         const queryClient = new QueryClient();
         await queryClient.prefetchQuery({
           queryKey: ['rides', { filter: 'all' }],
           queryFn: () => listRides({}),
         });
         return (
           <div className="space-y-24">
             <header className="flex items-center justify-between">
               <h1 className="text-2xl font-semibold tracking-tight">Courses</h1>
               <p className="text-sm text-muted-foreground">
                 Utilisez{' '}
                 <kbd className="rounded border px-4 py-2 text-xs font-mono">
                   Cmd/Ctrl+Shift+K
                 </kbd>{' '}
                 ou le bouton « Nouvelle course » pour saisir une course.
               </p>
             </header>
             <HydrationBoundary state={dehydrate(queryClient)}>
               <RidesList />
             </HydrationBoundary>
           </div>
         );
       }
       ```

    2. **Créer** `apps/web/src/app/(app)/courses/_components/rides-list.client.tsx` (≤ 200 lignes) — calque `patients-list.client.tsx` :
       ```tsx
       'use client';

       import { useDeferredValue, useState } from 'react';
       import { useQuery } from '@tanstack/react-query';
       import { listRides } from '../_lib/queries';
       import { Skeleton } from '@/components/ui/skeleton';
       import { Input } from '@/components/ui/input';

       const STATUS_FILTERS = [
         { value: 'all', label: 'Tous statuts' },
         { value: 'validee', label: 'Validées' },
         { value: 'assignee', label: 'Assignées' },
         { value: 'en_cours', label: 'En cours' },
         { value: 'terminee', label: 'Terminées' },
       ] as const;

       const MODE_FILTERS = [
         { value: 'all', label: 'Tous modes' },
         { value: 'taxi_conventionne', label: 'Taxi conventionné' },
         { value: 'tpmr', label: 'TPMR' },
         { value: 'vsl', label: 'VSL' },
         { value: 'ambulance', label: 'Ambulance' },
       ] as const;

       const URGENCY_LABEL: Record<string, string> = {
         programmee: 'Programmée',
         urgente: 'Urgente',
         immediate: 'Immédiate',
       };
       const STATUS_LABEL: Record<string, string> = {
         validee: 'Validée',
         assignee: 'Assignée',
         en_cours: 'En cours',
         terminee: 'Terminée',
         annulee_regulateur: 'Annulée (régulateur)',
         annulee_patient: 'Annulée (patient)',
         annulee_chauffeur: 'Annulée (chauffeur)',
         brouillon: 'Brouillon',
       };

       export function RidesList(): JSX.Element {
         const [q, setQ] = useState('');
         const [statusFilter, setStatusFilter] = useState<string>('all');
         const [modeFilter, setModeFilter] = useState<string>('all');
         const dq = useDeferredValue(q);

         const { data, isPending } = useQuery({
           queryKey: ['rides', { status: statusFilter, mode: modeFilter, q: dq }],
           queryFn: () =>
             listRides({
               status: statusFilter === 'all' ? undefined : (statusFilter as never),
               transport_mode: modeFilter === 'all' ? undefined : (modeFilter as never),
             }),
           placeholderData: (prev) => prev,
           staleTime: 5_000,
         });

         const filtered = (data ?? []).filter((r) => {
           if (!dq) return true;
           const lower = dq.toLowerCase();
           return (
             r.pickup_address.toLowerCase().includes(lower) ||
             r.dropoff_address.toLowerCase().includes(lower)
           );
         });

         return (
           <div className="space-y-16">
             <div className="flex flex-wrap gap-12 items-end">
               <div className="flex-1 min-w-[240px]">
                 <Input
                   aria-label="Rechercher dans les adresses"
                   placeholder="Rechercher dans les adresses…"
                   value={q}
                   onChange={(e) => setQ(e.target.value)}
                 />
               </div>
               <select
                 aria-label="Filtre statut"
                 className="h-40 rounded-md border bg-background px-12 text-sm"
                 value={statusFilter}
                 onChange={(e) => setStatusFilter(e.target.value)}
               >
                 {STATUS_FILTERS.map((opt) => (
                   <option key={opt.value} value={opt.value}>
                     {opt.label}
                   </option>
                 ))}
               </select>
               <select
                 aria-label="Filtre mode de transport"
                 className="h-40 rounded-md border bg-background px-12 text-sm"
                 value={modeFilter}
                 onChange={(e) => setModeFilter(e.target.value)}
               >
                 {MODE_FILTERS.map((opt) => (
                   <option key={opt.value} value={opt.value}>
                     {opt.label}
                   </option>
                 ))}
               </select>
             </div>

             {isPending && (
               <div className="space-y-8">
                 {[0, 1, 2, 3, 4].map((i) => (
                   <Skeleton key={i} className="h-48 w-full rounded-md" />
                 ))}
               </div>
             )}

             {!isPending && filtered.length === 0 && (
               <div className="border rounded-md p-32 text-center text-sm text-muted-foreground">
                 Aucune course ne correspond aux critères.
               </div>
             )}

             {!isPending && filtered.length > 0 && (
               <table className="w-full text-sm" aria-label="Liste des courses">
                 <thead className="text-left text-xs uppercase text-muted-foreground">
                   <tr>
                     <th className="py-8 pr-12">Date</th>
                     <th className="py-8 pr-12">Patient</th>
                     <th className="py-8 pr-12">Trajet</th>
                     <th className="py-8 pr-12">Mode</th>
                     <th className="py-8 pr-12">Urgence</th>
                     <th className="py-8 pr-12">Statut</th>
                   </tr>
                 </thead>
                 <tbody>
                   {filtered.map((r) => (
                     <tr key={r.id} className="border-t">
                       <td className="py-12 pr-12 tabular-nums">
                         {new Date(r.scheduled_at).toLocaleString('fr-FR', {
                           dateStyle: 'short',
                           timeStyle: 'short',
                         })}
                       </td>
                       <td className="py-12 pr-12">
                         <span className="text-xs text-muted-foreground">
                           {r.patient_id.slice(0, 8)}…
                         </span>
                       </td>
                       <td className="py-12 pr-12">
                         <span className="block truncate max-w-[280px]">
                           {r.pickup_address} → {r.dropoff_address}
                         </span>
                       </td>
                       <td className="py-12 pr-12">{r.transport_mode}</td>
                       <td className="py-12 pr-12">
                         {URGENCY_LABEL[r.urgency] ?? r.urgency}
                       </td>
                       <td className="py-12 pr-12">
                         {STATUS_LABEL[r.status] ?? r.status}
                       </td>
                     </tr>
                   ))}
                 </tbody>
               </table>
             )}
           </div>
         );
       }
       ```

    3. **Vérifier** `pnpm --filter @tap/web typecheck && pnpm --filter @tap/web build` (ou au moins typecheck) → exit 0.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "HydrationBoundary\|prefetchQuery" apps/web/src/app/\(app\)/courses/page.tsx && grep -c "RidesList" apps/web/src/app/\(app\)/courses/_components/rides-list.client.tsx && grep -c "useQuery\|useDeferredValue" apps/web/src/app/\(app\)/courses/_components/rides-list.client.tsx && grep -c "Aucune course" apps/web/src/app/\(app\)/courses/_components/rides-list.client.tsx && grep -c "Skeleton" apps/web/src/app/\(app\)/courses/_components/rides-list.client.tsx && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contient `HydrationBoundary` ET `prefetchQuery`
    - rides-list.client.tsx contient `useQuery`, `useDeferredValue`, `Skeleton`, `Aucune course` (empty state FR)
    - Filtre statut + filtre mode + recherche présents avec aria-labels FR
    - 0 console.*
    - `pnpm --filter @tap/web typecheck` exit 0
    - Fichiers respectivement ≤ 50 et ≤ 200 lignes
  </acceptance_criteria>
  <done>
    - page.tsx + rides-list.client.tsx livrés
    - Commit unique atomique pour Tasks 5.1 + 5.2 + 5.3 : `feat(02): page /courses + DraftQueue header + bouton Nouvelle course intégré`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /courses RSC | Server Component charge listRides — RLS s'applique côté Postgres |
| Browser → DraftQueue useQuery | listDraftsAction = Server Action ; auth + RLS appliqués |
| Browser → bouton header | Pure UI dispatch ; pas de RPC à ce niveau |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T1 | Information Disclosure | /courses page accessible sans auth | mitigate | Layout (app)/layout.tsx redirige vers /login si !user — déjà en place Phase 1 |
| T-02-T2 | Information Disclosure | DraftQueue affiche payload (truncated 40 chars) — risque XSS si payload contient JS | mitigate | React échappe automatiquement les strings au render ; payload validé zod (Wave 0) qui rejette les types non listés |
| T-02-T6 | DoS | refetchInterval 10s × 100 onglets = 600 req/min sur listDraftsAction | accept (low) | Rate limit Vercel + Supabase ; staleTime 5s + invalidateQueries explicit limitent les refetches inutiles |
| T-02-V5 | Input validation | Filtres mode/status acceptent valeur arbitraire (`as never` cast) | mitigate | Le serveur listRides re-valide implicitement (RLS + colonnes typées Postgres rejette les valeurs hors enum) ; côté client `STATUS_FILTERS`/`MODE_FILTERS` proposent uniquement les valeurs valides |
| T-02-T7 | Repudiation | Suppression brouillon sans confirmation | accept | Brouillons sont des données transitoires (D-10 : pas d'audit log) ; pas de besoin de confirmation pour V1 ; UX permettra Ctrl+Z V1.5 |
</threat_model>

<verification>
- `pnpm --filter @tap/web typecheck` exit 0
- `pnpm --filter @tap/web build` exit 0 (vérification finale build OK avant Wave 5)
- Layout intègre Orchestrator + bouton + DraftQueue + lien Courses
- Page /courses s'affiche avec table vide (pas encore de courses) ou la liste seedée
- Le smoke preview existant continue de passer (les modifications layout n'ont pas cassé /login ni /patients)
</verification>

<success_criteria>
- D-03 : modal global accessible depuis n'importe quelle route (app) via raccourci ou bouton
- D-07 : page /courses minimale livrée (filtres + skeleton + empty state + table)
- SAIS-04 : DraftQueue affiche les brouillons reprenables avec preview, click → RESUME
- SAIS-05 : multi-saisies parallèles (le 2e dispatch OPEN_NEW minimise le 1er, badge compteur reflète l'état)
- Pilier 1 UX : skeleton (jamais spinners), empty state FR explicite, kbd shortcut hint dans la page
- DEC-016 respecté : zéro logique métier dans ces composants
</success_criteria>

<output>
À l'issue de Wave 4, créer `.planning/phases/02-saisie-express-course/02-05-SUMMARY.md` listant :
- Layout étendu (Orchestrator + bouton + DraftQueue + nav)
- Page /courses RSC + RidesList client + 4 filtres + recherche
- Aucun nouveau pkg, aucune dette
- Prêt pour Wave 5 (E2E saisie-express GREEN + Visible Progress 6 captures + smoke étendu)
</output>
