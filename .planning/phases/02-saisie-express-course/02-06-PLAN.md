---
phase: 02-saisie-express-course
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - apps/web/tests/e2e/saisie-express.spec.ts
  - apps/web/tests/smoke/preview.spec.ts
  - docs/showcase/02-saisie-express-course/01-modal-express-vide.png
  - docs/showcase/02-saisie-express-course/02-modal-recherche-patient.png
  - docs/showcase/02-saisie-express-course/03-modal-rempli-pret-submit.png
  - docs/showcase/02-saisie-express-course/04-toast-creation-confirmee.gif
  - docs/showcase/02-saisie-express-course/05-brouillons-dropdown-3-en-attente.png
  - docs/showcase/02-saisie-express-course/06-page-courses-liste-30j.png
  - .planning/phases/02-saisie-express-course/02-SUMMARY.md
autonomous: false
requirements: [SAIS-01, SAIS-02, SAIS-03, SAIS-04, SAIS-05, SAIS-06]

must_haves:
  truths:
    - "Les 6 tests Playwright SAIS-01..06 passent GREEN (test.skip retirés)"
    - "SAIS-01 : timing Cmd+Shift+K → toast confirmation < 30 secondes mesuré"
    - "SAIS-02 : Cmd/Ctrl+Shift+K ouvre le modal depuis /patients ET /courses"
    - "SAIS-03 : taper « Ho » dans le modal retourne « Hoarau Patrick » (depuis seed démo)"
    - "SAIS-04 : minimize → DraftQueue badge++ → click brouillon → modal réouvre avec valeurs"
    - "SAIS-05 : 3 OPEN_NEW consécutifs → badge DraftQueue affiche 2 (le 3e visible)"
    - "SAIS-06 : après création course, audit_logs contient une ligne action='ride.insert' (vérifié via assertion route admin OU via un mode test fixture)"
    - "Le smoke test preview.spec.ts est étendu : route /courses accessible + bouton Nouvelle course visible + Cmd+Shift+K déclenche un dialog"
    - "6 captures docs/showcase/02-saisie-express-course/*.png|gif présentes (réelles, pas placeholders binaires vides)"
    - "02-SUMMARY.md contient walkthrough script 5-10 étapes + URL preview Vercel cliquable"
  artifacts:
    - path: apps/web/tests/e2e/saisie-express.spec.ts
      provides: "6 tests Playwright SAIS-01..06 GREEN"
      contains: "SAIS-01 saisie complète"
    - path: apps/web/tests/smoke/preview.spec.ts
      provides: "Smoke étendu : /courses + bouton + raccourci"
      contains: "/courses"
    - path: docs/showcase/02-saisie-express-course/01-modal-express-vide.png
      provides: "Capture 1/6 — modal vide focus patient"
    - path: docs/showcase/02-saisie-express-course/02-modal-recherche-patient.png
      provides: "Capture 2/6 — fuzzy 2 chars Ho → résultats"
    - path: docs/showcase/02-saisie-express-course/03-modal-rempli-pret-submit.png
      provides: "Capture 3/6 — formulaire complet"
    - path: docs/showcase/02-saisie-express-course/04-toast-creation-confirmee.gif
      provides: "Capture 4/6 — submit + fade-out + toast Sonner"
    - path: docs/showcase/02-saisie-express-course/05-brouillons-dropdown-3-en-attente.png
      provides: "Capture 5/6 — DraftQueue badge 3"
    - path: docs/showcase/02-saisie-express-course/06-page-courses-liste-30j.png
      provides: "Capture 6/6 — page /courses"
    - path: .planning/phases/02-saisie-express-course/02-SUMMARY.md
      provides: "Walkthrough Visible Progress + lien preview Vercel + métriques SAIS"
  key_links:
    - from: "apps/web/tests/e2e/saisie-express.spec.ts"
      to: "page /courses + modal + DraftQueue"
      via: "Playwright assertions getByRole('dialog'), getByLabel(...), getByRole('button', {name:'Nouvelle course'})"
      pattern: "getByRole|getByLabel"
    - from: "apps/web/tests/smoke/preview.spec.ts"
      to: "Vercel preview URL + Cmd+Shift+K shortcut"
      via: "page.keyboard.press('Control+Shift+K')"
      pattern: "Control\\+Shift\\+K"
    - from: "02-SUMMARY.md"
      to: "Vercel preview URL"
      via: "lien markdown vers déploiement preview"
      pattern: "https://.*vercel\\.app"
---

<objective>
Wave 5 — Validation E2E + Visible Progress Mandate. Promeut les 6 tests `test.skip` du scaffold Wave 0 en assertions GREEN couvrant SAIS-01..06, étend le smoke preview pour vérifier `/courses` + bouton header + raccourci global, produit les 6 captures dans `docs/showcase/02-saisie-express-course/`, et rédige le walkthrough script 5-10 étapes dans `02-SUMMARY.md` avec URL preview Vercel cliquable.

Purpose: Honorer DEC-005 (mesure timing < 30 s par test), CLAUDE.md §13.5 Visible Progress Mandate (URL preview + 6 captures + walkthrough), D-12, prouver que les 6 SAIS-01..06 sont fonctionnels en preview Vercel.

Output: 1 fichier E2E (6 tests GREEN), 1 smoke étendu, 6 captures + 02-SUMMARY.md. Comporte 1 checkpoint humain Visible Progress (production des captures depuis preview Vercel — manual screenshot de l'utilisateur).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/TAP/CLAUDE.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md

<interfaces>
<!-- Test infra existante -->

From apps/web/tests/smoke/preview.spec.ts (existant):
- REG_DEMO_EMAIL = 'regulateur@demo.tap'
- REG_DEMO_PASSWORD = 'demo1234!'
- Pattern login démo (lignes 14-26) déjà établi

From apps/web/tests/admin/breach-countdown.spec.ts:
- Pattern auth + Server Action UI flow

From apps/web/playwright.config.ts:
- timezoneId: 'Indian/Reunion' (ligne 38) — cohérent avec parseFreeformDate

From Wave 0 scaffold (apps/web/tests/e2e/saisie-express.spec.ts):
- Helper loginRegulateur(page)
- 6 test.skip placeholders SAIS-01..06

From seed démo 974 (Phase 0.7) :
- 30 patients seedés avec noms réunionnais (« Hoarau Patrick » présent ou équivalent)
- 200 courses passées
- Compte regulateur@demo.tap / demo1234!
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Promouvoir saisie-express.spec.ts en 6 tests GREEN (SAIS-01..06)</name>
  <read_first>
    - /home/user/TAP/apps/web/tests/e2e/saisie-express.spec.ts (Wave 0 scaffold)
    - /home/user/TAP/apps/web/tests/smoke/preview.spec.ts (pattern login + getByLabel)
    - /home/user/TAP/apps/web/tests/admin/breach-countdown.spec.ts (pattern Server Action UI flow)
    - /home/user/TAP/apps/web/playwright.config.ts (timezoneId, base URL)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §C7 (budget temporel) + Exemple 4 (timing test)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`saisie-express.spec.ts`
    - /home/user/TAP/supabase/seed.demo.sql (vérifier nom patient « Hoarau Patrick » ou adapter le test au nom réellement seedé)
  </read_first>
  <action>
    Réécrire `apps/web/tests/e2e/saisie-express.spec.ts` (≤ 220 lignes) en remplaçant les 6 `test.skip` par des tests fonctionnels.

    **Préambule** :
    1. **Vérifier le nom patient seedé** :
       ```bash
       grep -E "Hoarau|insert into.*patients" /home/user/TAP/supabase/seed.demo.sql /home/user/TAP/supabase/seed.sql 2>/dev/null | head -10
       ```
       Si « Hoarau Patrick » n'est pas dans le seed, utiliser le premier nom de patient réellement présent. Documenter le choix dans un commentaire en haut du fichier.

    **Structure complète** :
    ```ts
    import { test, expect, type Page } from '@playwright/test';

    const REG_DEMO_EMAIL = 'regulateur@demo.tap';
    const REG_DEMO_PASSWORD = 'demo1234!';
    // Patient seedé démo Phase 0.7 — adapter selon supabase/seed.demo.sql
    const PATIENT_QUERY = 'Ho';
    const PATIENT_FULL_NAME_RE = /Ho/i; // regex tolérante (Hoarau, Houareau, etc.)

    async function loginRegulateur(page: Page) {
      await page.goto('/login');
      await page.getByLabel(/email/i).fill(REG_DEMO_EMAIL);
      await page.getByLabel(/mot de passe/i).fill(REG_DEMO_PASSWORD);
      await page.getByRole('button', { name: /se connecter/i }).click();
      await page.waitForURL((url) => !url.pathname.includes('/login'), {
        timeout: 10_000,
      });
    }

    test.describe('Saisie express course (SAIS-01..06)', () => {
      test.beforeEach(async ({ page }) => {
        await loginRegulateur(page);
      });

      test('SAIS-01 saisie complète < 30 s (DEC-005)', async ({ page }) => {
        await page.goto('/patients');
        const t0 = Date.now();
        await page.keyboard.press('Control+Shift+K');
        await expect(page.getByRole('dialog')).toBeVisible({ timeout: 1000 });
        await page.getByLabel('Rechercher un patient').fill(PATIENT_QUERY);
        // Cliquer sur le 1er résultat patient (texte contient PATIENT_FULL_NAME_RE)
        await page.getByText(PATIENT_FULL_NAME_RE).first().click();
        await page.getByLabel('Date et heure').fill('15/05 14h30');
        await page.getByLabel('Date et heure').blur();
        await page.getByLabel('Adresse de prise en charge').fill('12 rue Pasteur, Saint-Denis');
        await page.getByLabel('Adresse de destination').fill('CHU Bellepierre');
        await page.getByRole('button', { name: /Créer la course/i }).click();
        await expect(page.getByText(/Course créée/i)).toBeVisible({ timeout: 2000 });
        const elapsed = Date.now() - t0;
        expect(elapsed).toBeLessThan(30_000);
      });

      test('SAIS-02 raccourci Cmd/Ctrl+Shift+K depuis /patients ET /courses', async ({
        page,
      }) => {
        // Depuis /patients
        await page.goto('/patients');
        await page.keyboard.press('Control+Shift+K');
        await expect(page.getByRole('dialog')).toBeVisible({ timeout: 1000 });
        await page.keyboard.press('Escape');
        await expect(page.getByRole('dialog')).toBeHidden({ timeout: 2000 });

        // Depuis /courses
        await page.goto('/courses');
        await page.keyboard.press('Control+Shift+K');
        await expect(page.getByRole('dialog')).toBeVisible({ timeout: 1000 });
      });

      test('SAIS-03 recherche patient 2 chars dans modal', async ({ page }) => {
        await page.goto('/patients');
        await page.keyboard.press('Control+Shift+K');
        await expect(page.getByRole('dialog')).toBeVisible({ timeout: 1000 });
        const searchField = page.getByLabel('Rechercher un patient');
        await searchField.fill(PATIENT_QUERY);
        // Le premier résultat doit apparaître quasi-instantanément (DEC-015 fuzzy 2 chars)
        await expect(page.getByText(PATIENT_FULL_NAME_RE).first()).toBeVisible({
          timeout: 1500,
        });
      });

      test('SAIS-04 brouillon reprise après pause', async ({ page }) => {
        await page.goto('/patients');
        await page.keyboard.press('Control+Shift+K');
        await expect(page.getByRole('dialog')).toBeVisible();
        await page.getByLabel('Rechercher un patient').fill(PATIENT_QUERY);
        await page.getByText(PATIENT_FULL_NAME_RE).first().click();
        await page.getByLabel('Adresse de prise en charge').fill('12 rue Pasteur');
        // Cliquer "Mettre en pause" → minimise le brouillon (auto-save flush)
        await page.getByRole('button', { name: /Mettre en pause/i }).click();
        await expect(page.getByRole('dialog')).toBeHidden({ timeout: 2000 });

        // DraftQueue badge ≥ 1 (compteur)
        const draftButton = page.getByRole('button', { name: /Brouillons \(/i });
        await expect(draftButton).toBeVisible();
        await draftButton.click();

        // Click sur le brouillon dans la dropdown → réouvre le modal avec valeur préservée
        const draftItem = page
          .getByRole('menuitem')
          .filter({ hasText: '12 rue Pasteur' })
          .first();
        await draftItem.click();
        await expect(page.getByRole('dialog')).toBeVisible({ timeout: 2000 });
        await expect(page.getByLabel('Adresse de prise en charge')).toHaveValue(
          /12 rue Pasteur/,
        );
      });

      test('SAIS-05 multi-saisies parallèles compteur badge', async ({ page }) => {
        await page.goto('/patients');
        // 3 OPEN_NEW consécutifs (un visible, deux minimisés)
        await page.keyboard.press('Control+Shift+K');
        await expect(page.getByRole('dialog')).toBeVisible();
        await page.getByLabel('Adresse de prise en charge').fill('Adresse A');
        await page.keyboard.press('Control+Shift+K'); // minimise A, ouvre B
        await expect(page.getByRole('dialog')).toBeVisible();
        await page.getByLabel('Adresse de prise en charge').fill('Adresse B');
        await page.keyboard.press('Control+Shift+K'); // minimise B, ouvre C

        // Le badge DraftQueue doit afficher au moins 2 brouillons en attente
        const draftButton = page.getByRole('button', { name: /Brouillons \(/i });
        await expect(draftButton).toBeVisible();
        // Vérifier le label "Brouillons (N)" avec N >= 2
        const ariaLabel = await draftButton.getAttribute('aria-label');
        expect(ariaLabel).toMatch(/Brouillons \(([2-9]|\d{2,})\)/);
      });

      test('SAIS-06 audit log ride.insert créé', async ({ page, request }) => {
        // Créer une course
        await page.goto('/patients');
        await page.keyboard.press('Control+Shift+K');
        await page.getByLabel('Rechercher un patient').fill(PATIENT_QUERY);
        await page.getByText(PATIENT_FULL_NAME_RE).first().click();
        await page.getByLabel('Date et heure').fill('demain 14h');
        await page.getByLabel('Date et heure').blur();
        await page.getByLabel('Adresse de prise en charge').fill('Audit Test Pickup');
        await page.getByLabel('Adresse de destination').fill('Audit Test Dropoff');
        await page.getByRole('button', { name: /Créer la course/i }).click();
        await expect(page.getByText(/Course créée/i)).toBeVisible({ timeout: 2000 });

        // Vérifier audit_logs : 2 chemins possibles selon la maturité du projet :
        // (a) Route admin /admin/audit-logs?action=ride.insert (Phase 1.5 admin/legal a un pattern proche)
        // (b) Aller sur /courses et vérifier que la course est listée (preuve indirecte
        //     que le trigger PG s'est exécuté — l'INSERT n'aurait pas eu lieu sinon vu RLS)
        // V1 : choix (b) — pragmatique, sans dépendre d'une UI admin pas encore livrée.
        await page.goto('/courses');
        await expect(page.getByText('Audit Test Pickup')).toBeVisible({ timeout: 5000 });
      });
    });
    ```

    **Note pragmatique sur SAIS-06** : la vérification idéale est d'interroger `audit_logs` directement via une route admin. Pas encore livrée Phase 2. La présence de la course dans /courses (qui passe par `listRides` → RLS → table populée par INSERT) atteste que le pipeline a fonctionné, ET le trigger `rides_audit_trigger` (testé pgTAP rides_audit.sql Wave 1) garantit qu'une ligne audit_logs a été créée. **La preuve forte est dans pgTAP**, le E2E est une preuve secondaire d'intégration de bout en bout.

    Si une route admin `/admin/audit-logs` existe (héritage Phase 1.5 — vérifier `ls apps/web/src/app/(admin)/`), préférer un fetch direct via `request.get('/api/admin/audit-logs?action=ride.insert')` pour une assertion forte. Sinon laisser la stratégie (b).

    **Tab order et timing optimal** : profitant de Wave 3 qui a câblé tabIndex 1-8, le test SAIS-01 peut être plus rapide en utilisant `page.keyboard.press('Tab')` entre les fills si nécessaire (mais `getByLabel` + `.fill()` est plus stable face aux refactos UI).

    Vérifier que les 6 tests passent **réellement** en lançant :
    ```bash
    pnpm --filter @tap/web test:e2e -- saisie-express
    ```
    Si Docker/sandbox empêche l'exécution locale (cf. STATE.md dette), pousser sur la branche et laisser CI cloud exécuter — documenter le résultat dans le commit.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -cE "^[[:space:]]*test\(" apps/web/tests/e2e/saisie-express.spec.ts && grep -c "test.skip" apps/web/tests/e2e/saisie-express.spec.ts && grep -c "Control\+Shift\+K" apps/web/tests/e2e/saisie-express.spec.ts && grep -c "Course créée" apps/web/tests/e2e/saisie-express.spec.ts && grep -c "lessThan(30_000)" apps/web/tests/e2e/saisie-express.spec.ts && pnpm --filter @tap/web typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^[[:space:]]*test\(" saisie-express.spec.ts` retourne 6 (les 6 SAIS-XX)
    - `grep -c "test.skip" saisie-express.spec.ts` retourne 0 (tous activés)
    - `grep -c "Control\+Shift\+K" saisie-express.spec.ts` ≥ 5 (les 5 tests qui ouvrent le modal via raccourci)
    - `grep -c "lessThan(30_000)" saisie-express.spec.ts` retourne 1 (timing SAIS-01)
    - Typecheck OK
    - CI cloud OU local : 6/6 tests GREEN sur la branche Phase 2 (à vérifier dans le run CI lié au commit)
  </acceptance_criteria>
  <done>
    - 6 tests E2E GREEN
    - Commit FR : `test(02): saisie-express SAIS-01..06 GREEN (timing < 30s + multi-saisies + audit)`
  </done>
</task>

<task type="auto">
  <name>Task 6.2: Smoke preview étendu (route /courses + bouton + raccourci)</name>
  <read_first>
    - /home/user/TAP/apps/web/tests/smoke/preview.spec.ts (existant — 4 tests)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`tests/smoke/preview.spec.ts (étendu)`
  </read_first>
  <action>
    Étendre `apps/web/tests/smoke/preview.spec.ts` en ajoutant 3 tests à la suite des 4 existants (≤ 30 lignes ajoutées) :

    ```ts
    test('Phase 2 — route /courses accessible après login', async ({ page }) => {
      await page.goto('/login');
      await page.getByLabel(/email/i).fill(REG_DEMO_EMAIL);
      await page.getByLabel(/mot de passe/i).fill(REG_DEMO_PASSWORD);
      await page.getByRole('button', { name: /se connecter/i }).click();
      await page.waitForURL((url) => !url.pathname.includes('/login'));
      await page.goto('/courses');
      await expect(page.getByRole('heading', { name: /^Courses$/ })).toBeVisible();
    });

    test('Phase 2 — bouton « Nouvelle course » visible dans header global', async ({
      page,
    }) => {
      await page.goto('/login');
      await page.getByLabel(/email/i).fill(REG_DEMO_EMAIL);
      await page.getByLabel(/mot de passe/i).fill(REG_DEMO_PASSWORD);
      await page.getByRole('button', { name: /se connecter/i }).click();
      await page.waitForURL((url) => !url.pathname.includes('/login'));
      await page.goto('/patients'); // depuis n'importe quelle route (app)
      await expect(
        page.getByRole('button', { name: /Nouvelle course/i }),
      ).toBeVisible();
    });

    test('Phase 2 — raccourci Cmd/Ctrl+Shift+K déclenche le modal global', async ({
      page,
    }) => {
      await page.goto('/login');
      await page.getByLabel(/email/i).fill(REG_DEMO_EMAIL);
      await page.getByLabel(/mot de passe/i).fill(REG_DEMO_PASSWORD);
      await page.getByRole('button', { name: /se connecter/i }).click();
      await page.waitForURL((url) => !url.pathname.includes('/login'));
      await page.goto('/patients');
      await page.keyboard.press('Control+Shift+K');
      await expect(page.getByRole('dialog')).toBeVisible({ timeout: 2000 });
    });
    ```

    Ne **PAS** dupliquer le helper login — préférer un helper local `loginRegulateur(page)` factorisé en haut du fichier, refactor mineur des tests existants pour cohérence (≤ 10 lignes diff).

    Vérifier que le smoke continue de passer pour les 4 tests existants ET les 3 nouveaux.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -cE "^[[:space:]]*test\(" apps/web/tests/smoke/preview.spec.ts && grep -c "Phase 2" apps/web/tests/smoke/preview.spec.ts && grep -c "Nouvelle course" apps/web/tests/smoke/preview.spec.ts && grep -c "Control\+Shift\+K" apps/web/tests/smoke/preview.spec.ts && pnpm --filter @tap/web typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^[[:space:]]*test\(" smoke/preview.spec.ts` ≥ 7 (4 existants + 3 ajoutés)
    - `grep -c "Phase 2" smoke/preview.spec.ts` retourne 3
    - `grep -c "/courses" smoke/preview.spec.ts` ≥ 1
    - `grep -c "Control\+Shift\+K" smoke/preview.spec.ts` ≥ 1
    - Typecheck OK
    - CI cloud : smoke GREEN sur preview Vercel
  </acceptance_criteria>
  <done>
    - Smoke étendu, 7+ tests GREEN
    - Commit FR : `test(02): smoke preview étendu — /courses + bouton + raccourci global`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.3: Visible Progress — produire 6 captures depuis preview Vercel</name>
  <what-built>
    Phase 2 complète : modal saisie express, file d'attente brouillons, page /courses, raccourci global. La preview Vercel de la PR Phase 2 doit afficher :
    1. Modal vide avec focus champ patient (après Cmd+Shift+K)
    2. Recherche patient « Ho » → résultats fuzzy
    3. Modal rempli (patient + date + adresses + mode + urgence)
    4. Toast Sonner « Course créée pour {Patient} » après submit
    5. Dropdown DraftQueue avec 3 brouillons en attente
    6. Page /courses avec liste de courses (seedées + nouvellement créées)
  </what-built>
  <how-to-verify>
    1. Ouvrir l'URL preview Vercel de la PR Phase 2 (lien dans les commentaires GitHub Actions)
    2. Se connecter avec `regulateur@demo.tap` / `demo1234!`
    3. Capture 1 : presser Cmd/Ctrl+Shift+K → screenshot du modal vide → enregistrer en
       `docs/showcase/02-saisie-express-course/01-modal-express-vide.png`
    4. Capture 2 : taper « Ho » dans le champ patient → screenshot des résultats fuzzy →
       `02-modal-recherche-patient.png`
    5. Capture 3 : remplir tout (patient sélectionné, date « demain 14h », adresses pickup/dropoff, mode TPMR ou taxi, urgence programmée) → screenshot →
       `03-modal-rempli-pret-submit.png`
    6. Capture 4 : enregistrer un GIF 2-3 secondes du clic « Créer la course » jusqu'à apparition du toast Sonner « Course créée pour … » →
       `04-toast-creation-confirmee.gif` (utiliser un outil système d'enregistrement, ou Playwright `page.video()` en mode preview manuel)
    7. Capture 5 : ouvrir 3 modaux successivement avec Cmd+Shift+K (sans submit), puis cliquer sur le bouton DraftQueue (icône Inbox) → screenshot avec badge `(3)` et dropdown ouvert listant les 3 brouillons →
       `05-brouillons-dropdown-3-en-attente.png`
    8. Capture 6 : naviguer sur /courses, attendre que la liste s'affiche (≥ 30 courses seedées + créées pendant le test) → screenshot →
       `06-page-courses-liste-30j.png`
    9. Vérifier que les 6 fichiers existent et font une taille raisonnable (≥ 50 ko, < 5 Mo chacun)
    10. Commit : `docs(02): 6 captures Visible Progress depuis preview Vercel`

    **Substitution acceptable si Claude peut exécuter Playwright en mode visible** : automatiser les captures via `page.screenshot({ path: ..., fullPage: false })` ajouté au scénario E2E SAIS-01..06 derrière un flag `PHASE_2_CAPTURES=1`. Si cette option est techniquement praticable, Claude peut produire les captures **automatiquement** et le checkpoint humain devient une simple validation visuelle « ces images sont représentatives ». Documenter le choix dans le commit.
  </how-to-verify>
  <resume-signal>Type "approved" si les 6 captures sont conformes, ou décrire ce qu'il faut refaire (capture floue, mauvais cadrage, etc.)</resume-signal>
</task>

<task type="auto">
  <name>Task 6.4: Rédiger 02-SUMMARY.md (walkthrough + URL preview + métriques)</name>
  <read_first>
    - /home/user/TAP/.planning/phases/01-referentiel-patients/01-SUMMARY.md (si existant — pattern SUMMARY de phase précédente)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-SUMMARY.md (pattern walkthrough Phase 1.5)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md §D-12
    - Tous les SUMMARYs intermédiaires (02-01-SUMMARY.md … 02-05-SUMMARY.md) pour agrégation
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Créer `.planning/phases/02-saisie-express-course/02-SUMMARY.md` (≤ 200 lignes) avec sections :

    ```markdown
    # Phase 2 — Saisie express course (SUMMARY)

    **Status:** Complete
    **Delivered:** {DATE}
    **Commits:** {N} (voir `git log --oneline phase-02..main` pour la liste)
    **Preview Vercel:** https://{deployment-url}.vercel.app  ← cliquable

    ## Goal
    La régulatrice peut saisir une course en mode express en moins de 30 secondes,
    avec brouillons en file d'attente et multi-saisies parallèles, sans jamais
    être bloquée par un appel entrant.

    ## What Was Built

    ### Migration & schéma
    - `20260509000001_rides.sql` : 3 enums + 2 tables (rides + ride_draft) + RLS forcée + 1 trigger d'audit + indexes
    - 29 assertions pgTAP GREEN (15 RLS rides + 8 RLS ride_draft + 6 audit trigger)

    ### Couche serveur
    - 4 Server Actions : `createRideAction`, `upsertRideDraft`, `deleteRideDraft`, `listDraftsAction`
    - 3 queries RSC : `listRides`, `listDrafts`, `listRecentPickupAddresses`
    - Validation zod côté serveur (defense in depth) ; RLS-protégé via auth.getUser
    - revalidatePath après mutation, pas de redirect (D-06)

    ### Couche UI
    - `<RideExpressOrchestrator>` monté dans `(app)/layout.tsx` — store useReducer multi-instance + raccourci global Cmd/Ctrl+Shift+K
    - `<RideExpressModal>` Dialog Radix + auto-save 5s + onBlur + Esc avec flushSave + optimistic submit fade-out + toast Sonner
    - `<DraftQueue>` dropdown header avec badge compteur, preview brouillon, click = RESUME
    - `<HeaderNewRideButton>` dans le header global
    - Page `/courses` RSC avec prefetch + `<RidesList>` filtres status/mode/recherche + skeleton + empty state FR

    ### Tests
    - 18 tests Vitest GREEN (10 ride zod + 8 parseFreeformDate)
    - 29 assertions pgTAP GREEN (RLS + audit)
    - 6 tests Playwright E2E GREEN (SAIS-01..06)
    - 7 tests smoke preview GREEN (4 existants + 3 ajoutés Phase 2)

    ## Visible Progress (CLAUDE.md §13.5)

    ### Captures showcase
    Voir `docs/showcase/02-saisie-express-course/` :
    - `01-modal-express-vide.png` — modal après Cmd+Shift+K
    - `02-modal-recherche-patient.png` — fuzzy 2 chars « Ho »
    - `03-modal-rempli-pret-submit.png` — formulaire complet
    - `04-toast-creation-confirmee.gif` — submit + toast
    - `05-brouillons-dropdown-3-en-attente.png` — multi-saisies
    - `06-page-courses-liste-30j.png` — liste /courses

    ### Walkthrough script (5-10 étapes reproductibles)

    **Prérequis** : ouvrir la preview Vercel, se connecter en `regulateur@demo.tap` / `demo1234!`.

    1. Depuis n'importe quel écran (par exemple `/patients`), presser **Cmd/Ctrl+Shift+K** → un modal s'ouvre, le focus est dans le champ « Rechercher un patient ».
    2. Taper **« Ho »** → la liste fuzzy retourne « Hoarau Patrick » (et autres patients seedés). Cliquer sur le résultat.
    3. Tab → **Date et heure** : taper « demain 14h ». Au blur, le hint affiche la date résolue.
    4. Tab → **Adresse de prise en charge** : taper « 12 rue Pasteur, Saint-Denis ».
    5. Tab → **Adresse de destination** : taper « CHU Bellepierre ».
    6. Tab → **Mode de transport** : laisser « Taxi conventionné » par défaut.
    7. Tab → **Urgence** : laisser « Programmée ».
    8. Tab → **Notes** : optionnel, ne rien renseigner.
    9. Cliquer **Créer la course** → le modal se ferme en moins de 100 ms, un toast Sonner confirme « Course créée pour Hoarau Patrick … ».
    10. Naviguer sur `/courses` → la course apparaît en haut de la liste.

    Test multi-saisies : presser Cmd+Shift+K une 2e fois pendant la saisie → le 1er modal se minimise, un nouveau modal vide s'ouvre. Le compteur DraftQueue (icône Inbox dans le header) affiche `(1)`. Click sur le brouillon = réouverture avec valeurs préservées.

    ## Metrics (DEC-005 / SAIS-01)

    | Métrique | Cible | Mesuré |
    |---|---|---|
    | Saisie complète Cmd+Shift+K → toast | < 30 s | {X} s (ci. test SAIS-01) |
    | Feedback visuel sur action | < 100 ms | optimistic close + toast |
    | Recherche patient 2 chars | < 200 ms | RPC pg_trgm Phase 1 |
    | Auto-save → DB | 5 s debounce | confirmé |

    ## Decisions Honored

    | Decision | Status |
    |---|---|
    | D-01 schéma rides v1 minimal | ✓ |
    | D-02 brouillons en DB pas localStorage | ✓ |
    | D-03 modal global, pas page /new | ✓ |
    | D-04 instances modales séparées | ✓ |
    | D-05 auto-save 5s + onBlur + onClose | ✓ |
    | D-06 Server Action + revalidatePath, pas de redirect | ✓ |
    | D-07 page /courses minimale | ✓ |
    | D-08 zod rideExpressInputSchema | ✓ |
    | D-09 mesure SAIS-01 < 30 s par Playwright | ✓ |
    | D-10 audit_logs sur rides, pas sur ride_draft | ✓ |
    | D-11 tests pgTAP + Vitest + Playwright | ✓ |
    | D-12 Visible Progress 6 captures | ✓ |

    ## Decisions Amendments

    - **DEC-015 Cmd/Ctrl+N → Cmd/Ctrl+Shift+K** (R1 RESEARCH) : `Cmd+N` est non-interceptable navigateur. Amendement assumé sous l'autorité « Claude's Discretion » du mode autonome — `Cmd/Ctrl+Shift+K` retenu (pattern Slack/Linear). À documenter dans `docs/adr/` si Guillaume confirme.

    ## Tech Stack Δ

    - **Ajout** : `chrono-node@2.9.1` (parser date freeform FR) — justifié par DEC-005 (gain 5–8 s/saisie)
    - **Ajout** : `@radix-ui/react-dialog` + `@radix-ui/react-dropdown-menu` (déjà transitifs ou ajoutés via shadcn)
    - **Suppression** : stub `courseExpressSchema` + `typeTransportSchema` Phase 0 (refonte D-08 — R3 RESEARCH)

    ## Patterns Established

    - **Server Action pattern** Phase 2 : `getAuthContext` helper interne + zod re-parse + INSERT/UPDATE + revalidatePath (réutilisable Phases 3+)
    - **Multi-instance modal** : useReducer + Context minimal + key={tempKey} sur le composant rendu (pattern réutilisable pour Phase 6 Gantt drag-and-drop, Phase 7 imprévus)
    - **useGlobalShortcut** : hook réutilisable pour Phase 5 cockpit (Cmd+/ open command palette future)

    ## Security (ASVS L1)

    Tous les threats T1–T6 (cross-tenant, cross-author, audit bypass, race, raccourci conflit, DoS auto-save) traités, mitigés ou explicitement acceptés (cf. threat models 02-01..02-05).

    ## Carry-over / Deferred

    - Suggestion d'adresses datalist HTML5 : implémentée côté query (`listRecentPickupAddresses`), pas encore branchée côté UI modal (effet visible après quelques semaines de courses — V1.5)
    - Undo création course : non livré V1 (CONTEXT deferred)
    - Édition course créée : Phase 6 (Gantt)
    - Fallback offline auto-save : V2

    ## Next Phase

    Phase 3 — Moteur tarification CGSS. Le schéma `rides` Phase 2 sert de point d'attache : `ride_billing` arrivera en migration 005 avec FK `ride_id`, et le calcul tarifaire ne touche pas aux composants UI Phase 2 (DEC-016 — packages/pricing isolé).
    ```

    Remplacer `{DATE}` par la date du jour, `{N}` par le nombre de commits, `{X}` par la mesure de SAIS-01 (à extraire du run CI le plus récent), et `{deployment-url}` par l'URL preview Vercel réelle.
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "https://.*vercel\\.app" .planning/phases/02-saisie-express-course/02-SUMMARY.md && grep -c "Walkthrough script" .planning/phases/02-saisie-express-course/02-SUMMARY.md && grep -cE "^[0-9]+\." .planning/phases/02-saisie-express-course/02-SUMMARY.md && ls docs/showcase/02-saisie-express-course/0[1-6]-*.{png,gif} 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vercel\\.app" 02-SUMMARY.md` ≥ 1 (URL preview cliquable)
    - `grep -c "Walkthrough" 02-SUMMARY.md` ≥ 1
    - Walkthrough script contient ≥ 5 étapes numérotées
    - 6 fichiers showcase présents (5 .png + 1 .gif)
    - Tableau des décisions honorées (D-01..D-12) présent
    - Mention amendement DEC-015 présente
    - 02-SUMMARY.md ≤ 200 lignes
  </acceptance_criteria>
  <done>
    - 02-SUMMARY.md livré avec walkthrough + URL preview + métriques
    - Commit FR : `docs(02): SUMMARY Phase 2 + walkthrough Visible Progress`
    - Phase 2 prête pour `/gsd-verify-work` puis merge
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests E2E → Vercel preview | Cookie session démo (`regulateur@demo.tap`) — données seedées 974, pas de patient réel |
| Captures showcase → Git public | Données affichées = seed démo uniquement (3 sociétés fictives Phase 0.7) |
| 02-SUMMARY.md → public | Pas d'info sensible (pas de credentials, pas de patient réel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T2 | Information Disclosure | Captures contiennent des « patients réels » par erreur | mitigate | Préview Vercel utilise UNIQUEMENT le seed démo Phase 0.7 (3 sociétés fictives, NIRs valides Luhn mais inventés, adresses 974) ; aucune capture sur env production |
| T-02-T7 | Repudiation | Test E2E qui passe sans réellement valider le timing < 30 s | mitigate | `expect(elapsed).toBeLessThan(30_000)` est une assertion forte ; le test échoue si dépassement |
| T-02-T8 | Tampering | Test E2E qui ne lance pas vraiment dans CI cloud (false GREEN local) | mitigate | Le smoke preview tourne sur CHAQUE PR Vercel ; le E2E saisie-express tourne en CI cloud (configuré playwright-ci.yml) ; les 2 doivent être GREEN avant merge |
| T-02-T9 | Repudiation | 02-SUMMARY.md mentionne URL preview qui expire | accept | Vercel préserve les previews 30+ jours pour les PRs mergées ; lien remplacé par production URL après merge final commercial (V2) |
</threat_model>

<verification>
- 6 tests E2E saisie-express GREEN en CI cloud (run lié au commit final)
- 7+ tests smoke preview GREEN
- 6 captures docs/showcase/02-saisie-express-course/ présentes (vérifiable `ls`)
- 02-SUMMARY.md contient URL preview Vercel + walkthrough 5+ étapes + tableau décisions D-01..D-12
- ROADMAP.md Phase 2 statut peut passer à `[x]` (la finalisation se fait par le commit qui marque la phase complete)
</verification>

<success_criteria>
- SAIS-01 : timing < 30 s mesuré et validé E2E
- SAIS-02 : raccourci global testé depuis 2 routes différentes
- SAIS-03 : recherche patient 2 chars validée dans le contexte modal
- SAIS-04 : pause/reprise brouillon validée bout-en-bout
- SAIS-05 : multi-saisies validées avec assertion sur le compteur badge ≥ 2
- SAIS-06 : audit log ride.insert validé indirectement via /courses + directement par pgTAP rides_audit
- Visible Progress : 6 captures + walkthrough + URL preview
- Smoke preview Vercel étendu et GREEN
- Phase 2 mergée → STATE.md sera mis à jour par le orchestrator final
</success_criteria>

<output>
À l'issue de Wave 5, le commit final marque Phase 2 complete. Mettre à jour :
- ROADMAP.md : `- [x] Phase 2: Saisie express course — livrée…` avec date et nombre de commits
- STATE.md : `current_focus` passe à Phase 3 ; `completed_phases` += 1
- CLAUDE.md §14 : ajouter ligne « Phase 2 livrée » sous PHASES LIVRÉES
- Mise à jour faite par l'orchestrator `/gsd-execute-phase` ou `/gsd-verify-work`, pas par cette task.
</output>
