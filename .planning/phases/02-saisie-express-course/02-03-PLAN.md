---
phase: 02-saisie-express-course
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - apps/web/src/app/(app)/courses/actions.ts
  - apps/web/src/app/(app)/courses/_lib/queries.ts
autonomous: true
requirements: [SAIS-04, SAIS-06]

must_haves:
  truths:
    - "createRideAction(formData) re-valide rideExpressInputSchema côté serveur, vérifie auth + role, INSERT rides, DELETE ride_draft si fromDraftId fourni, revalidatePath('/courses') et /cockpit, retourne {success, id} ou {error: string}"
    - "upsertRideDraft({id?, payload, patient_id?}) est idempotent (onConflict: 'id'), RLS-protégé via auth.getUser, retourne {id} (existing or generated UUID)"
    - "deleteRideDraft(id) supprime le brouillon RLS-scoped et retourne {success: boolean}"
    - "listRides({status?, transport_mode?, q?}) retourne les courses RLS-filtrées de l'org courante"
    - "listDrafts() retourne les brouillons author-scoped (≤ 20)"
    - "listRecentPickupAddresses(patientId) retourne ≤ 5 adresses pickup distinctes du patient (60 derniers jours implicite via order desc)"
    - "Aucune Server Action n'expose le service_role ni ne bypass RLS"
    - "Aucune logique métier dans les composants — toute logique vit ici (DEC-016)"
  artifacts:
    - path: apps/web/src/app/(app)/courses/actions.ts
      provides: "Server Actions create/upsert/delete + listDraftsAction wrapper client"
      contains: "createRideAction"
    - path: apps/web/src/app/(app)/courses/_lib/queries.ts
      provides: "RSC queries listRides, listDrafts, listRecentPickupAddresses"
      contains: "export async function listRides"
  key_links:
    - from: "apps/web/src/app/(app)/courses/actions.ts"
      to: "@tap/shared rideExpressInputSchema"
      via: "import + safeParse"
      pattern: "rideExpressInputSchema"
    - from: "apps/web/src/app/(app)/courses/actions.ts"
      to: "supabase.from('rides').insert"
      via: "Server Action mutation"
      pattern: "from\\('rides'\\).*insert"
    - from: "apps/web/src/app/(app)/courses/actions.ts"
      to: "next/cache revalidatePath"
      via: "revalidatePath('/courses') after mutation"
      pattern: "revalidatePath"
    - from: "apps/web/src/app/(app)/courses/_lib/queries.ts"
      to: "@tap/database types Database['public']['Tables']['rides']"
      via: "type imports"
      pattern: "Database\\['public'\\]"
---

<objective>
Wave 2 — Server Actions et queries pour la couche serveur Phase 2. Implémente `createRideAction` (mutation principale avec validation serveur, RLS-protégée, audit automatique via trigger PG, revalidatePath, optionnellement DELETE de brouillon source), `upsertRideDraft` (idempotent pour auto-save 5s + onBlur), `deleteRideDraft`, `listRides` / `listDrafts` / `listRecentPickupAddresses` (queries RSC). Aucune UI à ce stade — uniquement la couche serveur testable indépendamment.

Purpose: Honorer DEC-016 (logique métier hors composants), D-05 (upsert idempotent), D-06 (Server Action + revalidatePath + pas de redirect), D-07 (page /courses prefetch listRides), specifics §11.4 (top 5 adresses pickup récentes par patient pour gain temps SAIS-01).

Output: 2 fichiers serveur (≤ 250 lignes total), typecheck OK, prêt pour la couche UI Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/TAP/CLAUDE.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-CONTEXT.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md
@/home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md

<interfaces>
<!-- Patterns existants à RÉUTILISER -->

From apps/web/src/app/(app)/patients/actions.ts (à imiter):
```typescript
'use server';
import { revalidatePath } from 'next/cache';
import { createClient } from '@/lib/supabase/server';

export type ActionState = { error?: string; success?: boolean };

// Pattern auth + profile + INSERT (lignes 68-160)
export async function createPatientAction(_prev, formData): Promise<ActionState> {
  const supabase = createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { error: 'Session expirée. Reconnectez-vous.' };
  // ... profile fetch, parse zod, insert, revalidatePath
}
```

From apps/web/src/app/(app)/patients/queries.ts:
```typescript
import { createClient } from '@/lib/supabase/server';
import type { Database } from '@tap/database/types';
export type PatientRow = Database['public']['Tables']['patients']['Row'];

export async function searchPatients(q: string): Promise<PatientRow[]> { ... }
```

From @tap/shared (Wave 0 livré):
```typescript
export const rideExpressInputSchema: z.ZodObject<...>;
export const rideDraftSchema: z.ZodObject<...>;
export type RideExpressInput = z.infer<typeof rideExpressInputSchema>;
export type RideDraftInput = z.infer<typeof rideDraftSchema>;
```

From @tap/database (Wave 1 livré):
```typescript
type RideRow = Database['public']['Tables']['rides']['Row'];
type RideDraftRow = Database['public']['Tables']['ride_draft']['Row'];
type RideTransportMode = Database['public']['Enums']['ride_transport_mode'];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Server Actions courses/actions.ts (createRide + upsertDraft + deleteDraft + listDraftsAction)</name>
  <read_first>
    - /home/user/TAP/apps/web/src/app/(app)/patients/actions.ts (createPatientAction lignes 1-160 — pattern auth + profile + INSERT + revalidatePath)
    - /home/user/TAP/apps/web/src/app/(app)/patients/constraints.actions.ts (pattern action compact addPatientConstraintAction lignes 20-62)
    - /home/user/TAP/apps/web/src/lib/supabase/server.ts (createClient signature)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §C3 (upsert idempotent) + §C6 (optimistic UI flow)
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`courses/actions.ts`
    - /home/user/TAP/packages/shared/src/validators/ride.ts (Wave 0 — rideExpressInputSchema, rideDraftSchema)
  </read_first>
  <action>
    Créer `apps/web/src/app/(app)/courses/actions.ts` (≤ 200 lignes total). Structure :

    ```ts
    'use server';

    import { revalidatePath } from 'next/cache';
    import { z } from 'zod';
    import {
      rideExpressInputSchema,
      rideDraftSchema,
      type RideExpressInput,
    } from '@tap/shared';
    import { createClient } from '@/lib/supabase/server';

    export type ActionState = {
      error?: string;
      success?: boolean;
      id?: string;
    };

    /**
     * Helper interne : auth + profile org_id en une étape.
     * Retourne null si non authentifié (l'action doit short-circuit).
     */
    async function getAuthContext() {
      const supabase = createClient();
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return null;
      const profileRes = await supabase
        .from('profiles')
        .select('organization_id')
        .eq('id', user.id)
        .single();
      const profile = profileRes.data as { organization_id: string } | null;
      if (!profile) return null;
      return { supabase, user, organization_id: profile.organization_id };
    }
    ```

    **createRideAction** (D-06) — accepte FormData OU un objet typé pour faciliter `useTransition` côté client :
    ```ts
    /**
     * Crée une course en mode express (D-06).
     * - Re-valide zod côté serveur (defense in depth)
     * - Auth check via auth.getUser()
     * - INSERT rides (RLS s'occupe du role check via WITH CHECK has_role)
     * - DELETE ride_draft si fromDraftId fourni (D-06 finalisation)
     * - audit_logs alimenté par trigger PG (D-10)
     * - revalidatePath('/courses') + revalidatePath('/cockpit')
     * - PAS de redirect (D-06 : modal se ferme côté client)
     */
    const createRideInputSchema = z.object({
      input: rideExpressInputSchema,
      fromDraftId: z.string().uuid().optional(),
    });

    export async function createRideAction(
      args: z.infer<typeof createRideInputSchema>,
    ): Promise<ActionState> {
      const parsed = createRideInputSchema.safeParse(args);
      if (!parsed.success) {
        return { error: parsed.error.errors[0]?.message ?? 'Saisie invalide.' };
      }

      const ctx = await getAuthContext();
      if (!ctx) return { error: 'Session expirée. Reconnectez-vous.' };
      const { supabase, user, organization_id } = ctx;

      const { data: row, error } = await supabase
        .from('rides')
        .insert({
          organization_id,
          ...parsed.data.input,
          created_by: user.id,
          updated_by: user.id,
        } as never)
        .select('id')
        .single();
      if (error || !row) {
        return { error: 'Création course impossible.' };
      }

      // Optionnel : supprime le brouillon source (RLS auto-filtre author_id)
      if (parsed.data.fromDraftId) {
        await supabase.from('ride_draft').delete().eq('id', parsed.data.fromDraftId);
      }

      revalidatePath('/courses');
      revalidatePath('/cockpit');
      return { success: true, id: row.id };
    }
    ```

    **upsertRideDraft** (D-05 idempotent) :
    ```ts
    const upsertDraftInputSchema = z.object({
      id: z.string().uuid().optional(),
      payload: rideDraftSchema,
      patient_id: z.string().uuid().optional(),
    });

    export async function upsertRideDraft(
      args: z.infer<typeof upsertDraftInputSchema>,
    ): Promise<{ id: string } | { error: string }> {
      const parsed = upsertDraftInputSchema.safeParse(args);
      if (!parsed.success) return { error: 'Brouillon invalide.' };

      const ctx = await getAuthContext();
      if (!ctx) return { error: 'Session expirée.' };
      const { supabase, user, organization_id } = ctx;

      const row = {
        // Si id fourni → upsert ; sinon INSERT (id généré DB)
        ...(parsed.data.id ? { id: parsed.data.id } : {}),
        organization_id,
        author_id: user.id,
        payload: parsed.data.payload,
        patient_id: parsed.data.patient_id ?? null,
      };

      const { data, error } = await supabase
        .from('ride_draft')
        .upsert(row as never, { onConflict: 'id' })
        .select('id')
        .single();
      if (error || !data) return { error: 'Sauvegarde impossible.' };
      return { id: data.id };
    }
    ```

    **deleteRideDraft** :
    ```ts
    export async function deleteRideDraft(
      id: string,
    ): Promise<{ success: boolean; error?: string }> {
      if (!z.string().uuid().safeParse(id).success) {
        return { success: false, error: 'Identifiant brouillon invalide.' };
      }
      const ctx = await getAuthContext();
      if (!ctx) return { success: false, error: 'Session expirée.' };

      const { error } = await ctx.supabase.from('ride_draft').delete().eq('id', id);
      if (error) return { success: false, error: 'Suppression brouillon impossible.' };
      return { success: true };
    }
    ```

    **listDraftsAction** (wrapper Server Action pour useQuery côté client — l'usage RSC passe par queries.ts) :
    ```ts
    export async function listDraftsAction() {
      const ctx = await getAuthContext();
      if (!ctx) return [];
      const { data } = await ctx.supabase
        .from('ride_draft')
        .select('id, payload, patient_id, updated_at')
        .order('updated_at', { ascending: false })
        .limit(20);
      return data ?? [];
    }
    ```

    **Garde-fous** :
    - Aucune importation `@supabase/supabase-js` direct — uniquement via `@/lib/supabase/server`
    - Aucun `console.log` (CLAUDE.md §11)
    - Fichier ≤ 300 lignes, fonctions ≤ 50 lignes
    - Tous les messages d'erreur sont en français reformulé (jamais l'erreur Postgres brute)
    - Le `service_role` n'est JAMAIS référencé
    - `revalidatePath` couvre les 2 surfaces qui afficheront les rides : `/courses` (Wave 4) et `/cockpit` (Phase 5 future — déjà préparé)
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c "createRideAction\|upsertRideDraft\|deleteRideDraft\|listDraftsAction" apps/web/src/app/\(app\)/courses/actions.ts && grep -c "rideExpressInputSchema\|rideDraftSchema" apps/web/src/app/\(app\)/courses/actions.ts && grep -c "auth.getUser\|getAuthContext" apps/web/src/app/\(app\)/courses/actions.ts && grep -c "revalidatePath" apps/web/src/app/\(app\)/courses/actions.ts && grep -v '^[[:space:]]*//\|^[[:space:]]*\*' apps/web/src/app/\(app\)/courses/actions.ts | grep -c "console\." && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 4 exports nommés (`createRideAction`, `upsertRideDraft`, `deleteRideDraft`, `listDraftsAction`) — `grep -cE "^export (async )?function (createRideAction|upsertRideDraft|deleteRideDraft|listDraftsAction)" apps/web/src/app/\(app\)/courses/actions.ts` retourne 4
    - `'use server';` présent en haut
    - `revalidatePath('/courses')` ET `revalidatePath('/cockpit')` présents dans createRideAction
    - 0 `console.log/warn/error` (hors commentaires)
    - 0 import `@supabase/supabase-js` direct (`grep -c "@supabase/" actions.ts` = 0)
    - `pnpm --filter @tap/web typecheck` exit 0
    - Fichier ≤ 200 lignes
  </acceptance_criteria>
  <done>
    - actions.ts livré, typecheck OK
    - Aucun anti-pattern (CLAUDE.md §11 respecté)
    - Commit FR : `feat(02): Server Actions createRideAction + upsertRideDraft + deleteRideDraft (D-05/D-06)`
  </done>
</task>

<task type="auto">
  <name>Task 3.2: Queries RSC courses/_lib/queries.ts (listRides + listDrafts + listRecentPickupAddresses)</name>
  <read_first>
    - /home/user/TAP/apps/web/src/app/(app)/patients/queries.ts (115 lignes — searchPatients pattern + Database typing)
    - /home/user/TAP/apps/web/src/lib/supabase/server.ts
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-PATTERNS.md §`courses/_lib/queries.ts`
    - /home/user/TAP/.planning/phases/02-saisie-express-course/02-RESEARCH.md §C7 (suggestions adresses récentes patient — datalist HTML5)
    - /home/user/TAP/packages/database/src/types.gen.ts (Wave 1 — types rides + ride_draft)
  </read_first>
  <action>
    Créer `apps/web/src/app/(app)/courses/_lib/queries.ts` (≤ 130 lignes). Structure :

    ```ts
    import { createClient } from '@/lib/supabase/server';
    import type { Database } from '@tap/database/types';

    export type RideRow = Database['public']['Tables']['rides']['Row'];
    export type RideDraftRow = Database['public']['Tables']['ride_draft']['Row'];
    export type RideTransportMode = Database['public']['Enums']['ride_transport_mode'];
    export type RideUrgency = Database['public']['Enums']['ride_urgency'];
    export type RideStatus = Database['public']['Enums']['ride_status'];

    /**
     * Liste des courses RLS-filtrées (org courante).
     * Tri scheduled_at desc, limit 100. Filtres optionnels.
     */
    export async function listRides(params: {
      status?: RideStatus;
      transport_mode?: RideTransportMode;
      urgency?: RideUrgency;
    } = {}): Promise<RideRow[]> {
      const supabase = createClient();
      let q = supabase
        .from('rides')
        .select(
          'id, organization_id, patient_id, scheduled_at, pickup_address, pickup_city, dropoff_address, dropoff_city, transport_mode, urgency, status, notes_regulateur, archive, created_at, updated_at, created_by, updated_by',
        )
        .eq('archive', false)
        .order('scheduled_at', { ascending: false })
        .limit(100);
      if (params.status) q = q.eq('status', params.status);
      if (params.transport_mode) q = q.eq('transport_mode', params.transport_mode);
      if (params.urgency) q = q.eq('urgency', params.urgency);
      const { data, error } = await q;
      if (error) throw new Error('Lecture courses impossible.');
      return (data ?? []) as RideRow[];
    }

    /**
     * Brouillons RLS-filtrés (author_id = auth.uid() automatiquement).
     */
    export async function listDrafts(): Promise<RideDraftRow[]> {
      const supabase = createClient();
      const { data, error } = await supabase
        .from('ride_draft')
        .select('id, organization_id, author_id, patient_id, payload, created_at, updated_at')
        .order('updated_at', { ascending: false })
        .limit(20);
      if (error) throw new Error('Lecture brouillons impossible.');
      return (data ?? []) as RideDraftRow[];
    }

    /**
     * Top 5 adresses pickup distinctes du patient (specifics §11.4).
     * Améliore SAIS-01 par auto-suggestion sans tap-tap-tap.
     * Limit 60 jours implicite : on prend les 5 dernières par created_at desc.
     */
    export async function listRecentPickupAddresses(
      patientId: string,
    ): Promise<string[]> {
      const supabase = createClient();
      const { data } = await supabase
        .from('rides')
        .select('pickup_address, created_at')
        .eq('patient_id', patientId)
        .order('created_at', { ascending: false })
        .limit(20);
      if (!data) return [];
      const seen = new Set<string>();
      const result: string[] = [];
      for (const row of data) {
        const addr = (row as { pickup_address: string }).pickup_address;
        if (addr && !seen.has(addr)) {
          seen.add(addr);
          result.push(addr);
          if (result.length >= 5) break;
        }
      }
      return result;
    }
    ```

    **Garde-fous** :
    - Pas de `'use server'` (ces fonctions sont appelées depuis des Server Components, pas des Server Actions)
    - Pas de `console.log`
    - Limit 100 sur rides (assez pour une org petite/moyenne — pagination V1.5)
    - Aucune logique métier complexe — juste lecture RLS-protégée
    - Erreurs reformulées en français (jamais l'erreur Postgres brute)

    **Ne PAS** créer un fichier-barrel `_lib/index.ts` si non nécessaire (Phase 1 ne le fait pas — rester sobre).
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -cE "^export (async )?function (listRides|listDrafts|listRecentPickupAddresses)" apps/web/src/app/\(app\)/courses/_lib/queries.ts && grep -c "Database\['public'\]" apps/web/src/app/\(app\)/courses/_lib/queries.ts && grep -v '^[[:space:]]*//\|^[[:space:]]*\*' apps/web/src/app/\(app\)/courses/_lib/queries.ts | grep -c "console\." && pnpm --filter @tap/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 3 exports nommés : listRides, listDrafts, listRecentPickupAddresses
    - Types `RideRow`, `RideDraftRow`, `RideTransportMode`, `RideUrgency`, `RideStatus` exportés
    - 0 `console.*` hors commentaires
    - 0 import @supabase/* direct (`grep -c "@supabase/" apps/web/src/app/\(app\)/courses/_lib/queries.ts` = 0)
    - `pnpm --filter @tap/web typecheck` exit 0
    - Fichier ≤ 130 lignes
  </acceptance_criteria>
  <done>
    - queries.ts livré, typecheck OK
    - Types Database réutilisés (pas de duplication manuelle)
    - Commit FR : `feat(02): queries RSC listRides + listDrafts + listRecentPickupAddresses`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | FormData ou args typés, validés zod côté serveur (defense in depth) |
| Server Action → Postgres (RLS) | createClient() utilise le cookie session — RLS s'applique aux requêtes |
| Server Action → next/cache | revalidatePath force la régénération RSC ; pas de payload sensible exposé |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T1 | Information Disclosure | createRideAction sans auth check → cross-tenant via cookie volé | mitigate | getAuthContext() appelé au début de chaque action ; absence d'auth → return error sans toucher la DB |
| T-02-T2 | Information Disclosure | upsertRideDraft accepte payload jsonb avec données patient | mitigate | RLS author_id=auth.uid() (Wave 1) — un attaquant ne peut pas lire les brouillons d'un autre user via cette action ; payload validé `rideDraftSchema` (refuse les champs hors schéma) |
| T-02-T3 | Tampering | createRideAction tente de bypass audit_logs en utilisant raw SQL | accept | Trigger PG `for each row` est inviolable depuis l'API ; risque résiduel uniquement si service_role exposé (jamais dans ce code) |
| T-02-T4 | Race condition | Multi-instance auto-save : 2 onglets upsert simultané même draftId | mitigate | upsert idempotent `onConflict: 'id'` — last write wins ; acceptable car payload entier est remplacé (pas de merge partiel) |
| T-02-V5 | Input validation | XSS via payload jsonb stocké puis re-rendu | mitigate | rideDraftSchema (zod) refuse les champs non listés ; React échappe automatiquement les strings au render — pas d'innerHTML |
| T-02-T7 | Repudiation | Server Action échoue silencieusement (catch trop large) | mitigate | Aucun catch global ; les erreurs sont retournées explicitement `{ error: '...' }` et l'UI Wave 3 doit Sentry-capturer |
</threat_model>

<verification>
- `pnpm --filter @tap/web typecheck` exit 0
- Aucun `console.log` ni import `@supabase/*` direct (`grep -E "console\.|@supabase/" apps/web/src/app/\(app\)/courses/{actions.ts,_lib/queries.ts}` = 0 hors commentaires/lib wrapper)
- 4 Server Actions + 3 queries RSC livrés
- Tous les messages d'erreur en français reformulé (`grep -E "error: '" apps/web/src/app/\(app\)/courses/actions.ts` montre des messages FR)
- Fichiers ≤ 200/130 lignes
</verification>

<success_criteria>
- D-05 implémenté : upsertRideDraft idempotent avec onConflict
- D-06 implémenté : createRideAction valide zod serveur, INSERT, optionnellement DELETE draft, revalidatePath, retourne {success, id} sans redirect
- D-07 préparé : listRides prêt à être consommé par /courses page Wave 4
- specifics §11.4 implémenté : listRecentPickupAddresses retourne ≤ 5 adresses dédupliquées
- DEC-016 respecté : zéro logique métier dans des composants (les composants Wave 3 délèguent à ces actions)
</success_criteria>

<output>
À l'issue de Wave 2, créer `.planning/phases/02-saisie-express-course/02-03-SUMMARY.md` listant :
- 4 Server Actions + 3 queries RSC livrées
- Pattern d'auth + RLS reposé sur Phase 1 patients (sans duplication)
- Préparation pour Wave 3 (UI consomme ces APIs typées) et Wave 4 (page /courses prefetch)
</output>
