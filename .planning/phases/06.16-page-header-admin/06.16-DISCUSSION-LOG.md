---
phase: 06.16-page-header-admin
type: discussion-log
status: closed
date: 2026-06-04
---

# Phase 06.16 — DISCUSSION LOG

## Arbitrage périmètre (décision dirigeant 2026-06-04)

Deux options évaluées :

- **Option large** — refonte complète des pages admin (header + toolbar
  recherche/filtres + densité revue, esprit Linear). Dépend de l'API
  tri/pagination du `<DataTable>` 06.15 laissée en V2 (pour les filtres
  dynamiques) — sinon on duplique la couche données. Effort élevé
  (15-25 h).

- **Strict** (✓) — `<PageHeader>` commun (titre + description + slot
  actions) appliqué aux 16 pages admin. Chrome globale inchangée.
  Toolbar recherche/filtres **différée**, pas abandonnée. Effort
  modéré (5-8 h).

**Décision** : Strict. Rationale : referme la cohérence visuelle des
en-têtes sans introduire de neuf qui toucherait la couche données.
La toolbar (filtres) sera cadrée séparément si le besoin terrain est
confirmé — elle suppose de cadrer aussi le tri généralisé du
`<DataTable>` (déjà inscrit V2).

## Vérifications terrain (2026-06-04)

- 16 fichiers `page.tsx` sous `apps/web/src/app/(admin)/admin/`
  (commande `find … -name page.tsx`).
- Pattern actuel uniforme : `<header>` avec `<h1
  className="text-2xl font-semibold tracking-tight">` + `<p
  className="text-muted-foreground text-sm">` (souvent suivi de `mt-4`).
- Une seule page a des actions header : `legal/registre/page.tsx`
  (`<ExportPdfButton />` + bouton « Nouvelle entrée » sous
  `<header className="flex items-center justify-between">`).
- Aucun composant `PageHeader` partagé n'existe (`grep -r PageHeader
  apps/web/src` → 0 résultat avant cette PR).
- `(admin)/layout.tsx` rend `AdminTopBar` + `NavTabs` (selon rôle) +
  `LegalNavMenu` — chrome déjà cohérente, **hors périmètre**.

## Décisions LOCKED

- **D-01** Composant `<PageHeader>` (titre + description + slot actions),
  pattern dérivé verbatim de l'existant.
- **D-02** Application aux 16 pages, texte titre/description repris à
  l'identique.
- **D-03** Préserver les actions de `legal/registre` via le slot.
- **D-04** Chrome (`(admin)/layout.tsx`, `NavTabs`, `LegalNavMenu`)
  inchangée.

## Garde-fous transverses (rappel)

- Tokens 06.14 uniquement, 0 hex.
- 0 nouvelle dépendance, 0 migration BDD.
- 100 % présentationnel — logique métier intacte.
- NFR-001, RGAA, jour+nuit, ≤ 300 LOC/fichier.

## Suite

Cadrage + exécution dans une seule PR (`feat/06.16-page-header`). Pas
de RESEARCH (doctrine déjà posée 06.13 § patterns émergents). PLAN
implicite via le découpage T1→T4 inscrit en CONTEXT § Découpage.
