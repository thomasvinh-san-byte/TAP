# Phase 06.14: migration-tokens-tailwind - Context

**Gathered:** 2026-06-04 (assumptions mode)
**Status:** Ready for planning

<domain>

## Phase Boundary

Brancher le design system documenté en Phase 06.13 sur le code applicatif,
sans créer de divergence entre sources de vérité. La cible :
`apps/web/src/styles/tokens.json` (format DTCG 2025.10) devient la **seule
source éditable à la main**. `apps/web/src/app/globals.css` (CSS vars light
+ bloc `[data-theme='dark']`) et un export JS pour les contextes hors-DOM
(PDF, PWA themeColor) sont **générés** depuis les tokens via Style Dictionary.

Périmètre chiffré vérifié sur le code (2026-06-04) :

- 57 tokens feuilles dans `tokens.json` ; 12 couleurs chromatiques avec
  variante sombre, 45 invariants light/dark.
- 16 occurrences `hsl(var(--x))` à corriger en `hsl(var(--x) / <alpha-value>)`
  dans `tailwind.config.ts` uniquement.
- 3 sites de couleurs hex en dur (élargi par l'analyse vs RESEARCH §0) :
  2 PDF `@react-pdf/renderer` + 1 `themeColor` PWA dans `app/layout.tsx`.

Estimation indicative 5-8 h, à raffiner en plan.

</domain>

<decisions>

## Implementation Decisions

### A — Architecture DTCG du dark dans tokens.json

- **D-01 :** Architecture **Token Sets** : `apps/web/src/styles/tokens.json`
  reste la base light. Nouveau `apps/web/src/styles/tokens.dark.json`
  override **uniquement les 12 couleurs chromatiques**. Les 45 invariants
  (spacing, typo, radius, shadow, motion, touchTarget) ne sont JAMAIS
  dupliqués. Style Dictionary compose les deux sources pour produire le bloc
  `:root` (light) et `[data-theme='dark']` dans `globals.css`. Rationale :
  pattern standard DTCG 2025.10 + Style Dictionary, multi-thème-friendly
  (extensible vers un futur `tokens.high-contrast.json` pour PWA chauffeur
  plein soleil mentionné CLAUDE.md §5).

- **D-02 :** Les **3 couleurs sans dark documenté** reçoivent une valeur dark
  **explicite** dans `tokens.dark.json` (pas d'héritage implicite cross-theme,
  règle « jamais d'invisible-on-dark silencieux ») :
  - `color.text.onAction` dark : `hsl(222 47% 11%)` (suit `--primary-foreground`
    dark, `globals.css:42`).
  - `color.action.focusRing` dark : `hsl(217 91% 60%)` (= `--primary` dark).
  - `color.border.input` dark : `hsl(217 33% 22%)` (= `--border` dark,
    cohérent avec `globals.css:50` actuel).

### B — Pipeline de génération Style Dictionary

- **D-03 :** `style-dictionary` v4 ajouté en **devDependency racine** du
  monorepo (pas dans `apps/web` seul) pour permettre le partage futur avec
  `apps/mobile`/`apps/admin`/`packages/ui`. Aucune dépendance runtime —
  Style Dictionary n'est consommé qu'au build.

- **D-04 :** Script `tokens:build` dans `apps/web/package.json`, branché en
  **`prebuild`** (et exposé en commande manuelle). Garantit que toute
  préview Vercel compile les tokens sans intervention humaine. Ne pas
  brancher en `postinstall` (ralentit CI cold-start) ni en `prepare` Husky
  (alourdit chaque commit, complique les rebases).

- **D-05 :** Style Dictionary génère **2 outputs** :
  - `apps/web/src/app/globals.css` (CSS vars light + bloc `[data-theme='dark']`)
    avec le format `css/variables` (ou `customFormat` minimal si le built-in
    v4 ne supporte pas les deux sélecteurs nativement — à confirmer cf.
    `<deferred>` D-08).
  - `apps/web/src/styles/tokens.generated.ts` (format `javascript/es6`) pour
    les contextes hors-DOM (PDF, `themeColor` PWA). Export TypeScript typé.

- **D-06 :** `tailwind.config.ts` **n'est PAS régénéré** par Style Dictionary.
  Il reste consommateur statique des CSS vars produites. Régénérer la
  config Tailwind apporterait zéro valeur et casserait le pattern alpha
  qu'on introduit en D-07.

### C — Correction du pattern alpha Tailwind

- **D-07 :** Migrer les **16 occurrences** de `hsl(var(--x))` en
  `hsl(var(--x) / <alpha-value>)` **uniquement dans
  `apps/web/tailwind.config.ts:17-42`**. Le placeholder `<alpha-value>` est
  interprété par le compilateur Tailwind exclusivement. **Ne pas toucher**
  `globals.css` (overrides react-datepicker, font-feature-settings)
  ni `(driver)/layout.tsx:54` (CSS inline) — l'ajout du placeholder y
  produirait une couleur invalide côté navigateur.

### D — Migration des couleurs en dur (périmètre élargi)

- **D-08 :** Le périmètre RESEARCH §0 « 2 fichiers `.tsx` » est élargi à
  **3 sites** après vérification code :
  - `apps/web/src/lib/pdf/pdf-template.tsx:15-19` (5 hex).
  - `apps/web/src/app/api/admin/facturation/pdf/_components/facture-cgss-pdf.tsx:21-46`
    (5 hex).
  - `apps/web/src/app/layout.tsx:23` (`themeColor: '#0944a0'`, manifest PWA
    — non comptabilisé par RESEARCH).
  Les 3 sites importent **`tokens.generated.ts`** (produit par D-05) au
  lieu de hex en dur. Dérive vérifiée : `#1466DB` actuel dans pdf-template
  vs `hsl(217 92% 32%) ≈ #0944A0` dans tokens — la dérive est **déjà
  présente** avant 06.14. Migrer en 06.14 referme la promesse « tokens.json
  = source unique ».

### E — Périmètre de génération et bug latent (ajouts plan-phase)

- **D-09 :** Style Dictionary génère un **fragment dédié**
  `apps/web/src/styles/tokens.generated.css`, **PAS** `globals.css` entier
  (qui contient ~200 lignes de non-tokens : `@tailwind`, `@layer base`,
  `@keyframes`, font-feature-settings, ~70 lignes d'overrides
  `react-datepicker`). Le fragment contient uniquement `:root { --… }`
  (light) + `[data-theme='dark'] { --… }` (dark). `globals.css` est édité
  **une seule fois** pour : (a) ajouter `@import '../styles/tokens.generated.css';`
  tout en haut, AVANT les `@tailwind` (PostCSS `postcss-import` gère
  l'ordre en Tailwind v3) ; (b) retirer les déclarations de vars
  désormais générées (`:root` lignes ~6-33 + `[data-theme='dark']`
  lignes ~36-62). Tout le reste de `globals.css` est PRÉSERVÉ (couches,
  keyframes, overrides datepicker, font-feature-settings). Format SD :
  `css/variables` si built-in v4 sait émettre deux sélecteurs depuis
  deux token sets ; sinon `customFormat` minimal (≤ 30 lignes) qui
  concatène les deux blocs. Résout le point `<deferred>` built-in-vs-
  customFormat.

- **D-10 :** **Bug latent corrigé** : `globals.css:124-125` consomme
  `hsl(var(--popover))` et `hsl(var(--popover-foreground))` (overrides
  `react-datepicker`), mais ces vars **ne sont définies nulle part dans
  le repo**. Le date-picker tombe sur une couleur invalide. Ajout aux
  tokens (referme « source unique » + corrige le bug) :
  - `color.surface.popover` : light `hsl(0 0% 100%)` / dark `hsl(222 47% 8%)`
    (mapping shadcn standard `--popover = --background`).
  - `color.text.onPopover` : light `hsl(222 47% 11%)` / dark `hsl(210 40% 96%)`
    (mapping shadcn standard `--popover-foreground = --foreground`).
  Les overrides datepicker eux-mêmes ne sont **pas** touchés ; seules
  les vars qu'ils consomment deviennent valides. Impact : 12 → 14
  couleurs chromatiques avec dark ; 57 → 59 tokens.

- **D-11 :** **Génération câblée dans la commande `build`**, **pas** via
  hook `prebuild` npm (Turbo n'exécute pas les hooks `pre*`/`post*`, donc
  D-04 hook seul ne se déclencherait pas sur Vercel). Câblage effectif :
  - `apps/web/package.json` :
    `"tokens:build": "style-dictionary build --config style-dictionary.config.mjs"`
    et `"build": "pnpm tokens:build && next build"`.
  - `turbo.json` : task `tokens:build` avec
    `outputs: ["src/styles/tokens.generated.css", "src/styles/tokens.generated.ts"]`
    pour cache. Si friction, l'enchaînement `&&` suffit.
  - **Fichiers générés commités** (`tokens.generated.css` + `tokens.generated.ts`)
    avec en-tête `/* GÉNÉRÉ par tokens:build — ne pas éditer */`. Présents
    au repo pour éviter tout import cassé en dev/IDE ; régénérés au build.
  - Config Style Dictionary en **`.mjs`** (ESM cohérent Node 20). Résout
    le point `<deferred>` ESM/Node 20.

  Cette décision **remplace effectivement** la mécanique `prebuild` de
  D-04 (D-04 reste valide en intention « génération automatique au build »,
  mais le mécanisme exact est `npm script build = tokens:build && next build`
  + cache Turbo, pas un hook npm `prebuild`).

### Claude's Discretion

- Architecture Token Sets (D-01) tranchée après lecture spec DTCG 2025.10 +
  doc Style Dictionary theming. Inscriptible en ADR si jugé structurant en
  plan-phase (à arbitrer : 1 décision design qui contraint toute migration
  multi-thème future).

</decisions>

<canonical_refs>

## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `.planning/phases/06.14-migration-tokens-tailwind/06.14-RESEARCH.md`
  (matière sourcée — 7 sections + 12 sources datées).
- `apps/web/src/styles/tokens.json` (source DTCG livrée Phase 06.13).
- `apps/web/src/app/globals.css` (cible de génération, lignes 5-90 pour
  les CSS vars actuelles).
- `apps/web/tailwind.config.ts` (consommateur — pattern alpha à corriger
  lignes 17-42).
- `docs/design-system/01-foundations.md` (doctrine Phase 06.13, traçabilité
  des valeurs).
- `docs/design-system/02-patterns-emergents.md` (5 patterns réutilisables
  qui consomment les CSS vars).
- DTCG Format Module 2025.10 — `designtokens.org/tr/drafts/format`.
- Style Dictionary 4.x docs — `styledictionary.com` (theming + `css/variables`
  + `javascript/es6`).

</canonical_refs>

<code_context>

## Existing Code Insights

### Reusable Assets

- `apps/web/src/styles/tokens.json` (Phase 06.13) — source DTCG 2025.10
  validée `jq empty`. Structure 7 groupes (color/spacing/radius/typography/
  shadow/motion/touchTarget). Aucune valeur dark exploitable (uniquement
  en prose dans `$description`) — c'est précisément ce que D-01/D-02
  corrigent.
- `apps/web/tailwind.config.ts` (lignes 10, 17-42) — déjà configuré
  `darkMode: ['class', '[data-theme="dark"]']` ; la cible Token Sets
  s'y mappe trivialement.
- `apps/web/src/app/globals.css:36` — sélecteur `[data-theme='dark']`
  déjà utilisé. Aucun refactor de selector.

### Established Patterns

- **NFR-003** (couleur jamais seule) : tous les patterns 06.11 (`KpiCard`,
  `EmptyState`, `RideBadge`, `SlaBadgesCard`, `HautsBadge`) doublent la
  couleur avec un texte/icône. La migration tokens ne touche pas à ce
  niveau — elle déplace la source des valeurs, pas la sémantique.
- **HSL components sans wrapper** : `globals.css` utilise déjà la syntaxe
  moderne `--primary: 217 92% 32%` (composantes HSL espacées, sans
  `hsl()`). C'est le prérequis pour `hsl(var(--x) / <alpha-value>)`
  (D-07).
- **Mode jour + nuit à parité** (CLAUDE.md § 1 pilier 2) — D-01 préserve.

### Integration Points

- **Vercel build** : `pnpm build` côté `apps/web` doit déclencher
  `tokens:build` avant Next.js compile. Vérifier que `cd.yml` n'override
  pas la chaîne. D-04 pré-suppose que `prebuild` npm est respecté par
  Turbo (à confirmer lecture `turbo.json`).
- **react-datepicker overrides** (`globals.css:115-193`) : référencent
  `--popover`, `--popover-foreground` — ces CSS vars n'existent pas dans
  `tokens.json` (Phase 06.13). Décider en plan-phase si elles sont
  ajoutées aux tokens ou laissées comme overrides locaux.
- **PWA chauffeur tint** (`--driver-surface`) : déjà dans `tokens.json`
  et `globals.css`, mais consommé en CSS inline dans
  `(driver)/layout.tsx:54`. Pas à migrer (consommation directe).

</code_context>

<specifics>

## Specific Ideas

- Token Sets DTCG : pattern standard documenté `designtokens.org` +
  Style Dictionary 4.x. Préféré à `$value` structuré pour extensibilité
  multi-thème (high-contrast PWA chauffeur futur).
- Export JS pour PDF : `format: javascript/es6` de Style Dictionary
  permet aux fichiers `@react-pdf/renderer` de consommer la même source
  que les CSS vars sans dépendre du runtime navigateur.
- `themeColor` PWA dans `layout.tsx:23` : doit importer
  `tokens.generated.ts` pour rester synchronisé avec `--primary`.

</specifics>

<deferred>

## Deferred Ideas

### Hors périmètre 06.14 (notes pour phases futures)

- **Bascule Tailwind v4** : couplée éventuellement à Phase 06.9
  (Modernisation Next.js 15). v4 a un modèle CSS-first (`@theme`) qui
  converge avec « tokens → CSS vars » mais demande validation cibles
  navigateur (PWA chauffeur iOS Safari ancien). **NE PAS** mélanger
  dans la PR 06.14.

- **Refonte data tables Carbon-inspired** (Phase 06.15) — consommera
  les tokens migrés.

- **Refonte settings/admin Linear+Stripe** (Phase 06.16) — idem.

- **Thème high-contrast PWA chauffeur** : déjà mentionné CLAUDE.md §5
  comme évolution possible. L'architecture Token Sets (D-01) le rend
  trivial (ajouter `tokens.high-contrast.json`).

### Reviewed Todos (not folded)

Aucun todo cross-référencé pour 06.14 (`gsd-sdk query todo.match-phase`
retourne `todo_count: 0`).

</deferred>
