# Phase 04.7 — Pricing mockup + Caisse + Migration géocoding — Context (placeholder)

**Status:** TBD via `/gsd-discuss-phase 04.7`
**Inserted:** 2026-05-13 (refonte E2E god-phase 04 → 4 sous-phases — DEC-023)

---

## Goal (esquisse depuis ROADMAP)

La chaîne pricing UI est en place sans calcul réel. `packages/pricing` exporte un **stub** `computeCgssShortTrip` retournant des valeurs hardcodées clairement « DEMO ». `PricingBreakdown` UI réel à la clôture. `OverrideTarifModal` fonctionnel et tracé `audit_logs`. Page `/courses/caisse` avec totaux et export CSV. Migration BDD préparée pour `lat/lng/citycode` mais **non consommée** par le pricing (le calcul réel arrive Phase 05.5 — DEC-021).

`PricingBreakdown` lisible avec mention visible « DEMO — tarif non contractuel ». `OverrideTarifModal` avec raison obligatoire. `/courses/caisse` table dense type Stripe Balance, groupes expandables.

## Périmètre — dans

- `packages/pricing` workspace avec stub function et **100 % Vitest sur le contrat** (DEC-013 — la couverture porte sur le contrat de la fonction pure, pas la grille tarifaire réelle qui arrive 05.5)
- Migration BDD : `rides.pickup_lat / lng / citycode` + `dropoff_*` nullable (lift D-ADDR-06)
- Threading lat/lng depuis `AddressPickerField` BAN (Phase 03.2.7) vers `createRideAction` / `editRideAction`
- `PricingBreakdown` composant avec **badge « DEMO »** visible (UI lock pour éviter usage facturation)
- `OverrideTarifModal` régulateur depuis `ride-drawer` + inline override chauffeur dans `end-ride-modal`
- Page `/courses/caisse?date=YYYY-MM-DD` Server Component
- `_lib/queries-caisse.ts` (miroir du pattern `queries-enriched.ts`)
- Export CSV `utf-8-sig` + `;` + format date FR `jj/mm/aaaa` (ouverture Excel FR sans encoding cassé)
- Permissions régulateur + dirigeant uniquement sur `/courses/caisse` (RLS query)

## Périmètre — hors (reporté Phase 05.5)

- Calcul tarif **réel** CGSS court trajet 974
- Décision distance (OSRM auto-hébergé / Mapbox / OpenRoute paid / Haversine + override Hauts)
- Validation grille tarifaire dirigeant avec 5+ cas de référence

## Success Criteria (à valider en discuss)

1. Régulateur ouvre course clôturée, voit `PricingBreakdown` avec mention « DEMO » + valeurs hardcodées cohérentes (forfait + km factice + total)
2. `OverrideTarifModal` accessible depuis `ride-drawer`, raison obligatoire, trace `audit_logs` avec ancien montant + nouveau + raison
3. `/courses/caisse` affiche récap journée par chauffeur, totaux pied `<tfoot>`
4. Export CSV s'ouvre dans Excel FR sans encoding cassé (`utf-8-sig` + `;`)
5. Migration géocoding appliquée, `lat/lng/citycode` threadés depuis BAN

## Captures Visible Progress

3 : `PricingBreakdown` DEMO, `OverrideTarifModal`, `/courses/caisse`

## Estimation

6-9 h
