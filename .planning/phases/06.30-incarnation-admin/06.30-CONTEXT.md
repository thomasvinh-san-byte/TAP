# Phase 06.30 — CONTEXT

> Incarnation de la direction (DEC-101) sur la famille Admin (16 écrans).
> Famille DÉJÀ la plus cohérente (PageHeader partout, légal homogène) :
> lot ciblé = terracotta moments-clés + skeletons + hiérarchie typo.

## Constat d'audit

**Déjà bon — ne pas casser** :
- 16 écrans tous en `PageHeader` (grammaire d'en-tête unifiée).
- Sous-domaine légal (8 pages) homogène (même pattern descriptions
  pédagogiques + actions).
- 6 écrans avec titres de section.

**Écarts à combler** :

1. **Terracotta** : 0 usage dans `(admin)` avant ce lot. Moments-clés en
   bleu standard.
2. **`loading.tsx`** : 0 sur des écrans qui chargent (chauffeurs ~8
   appels Supabase, registre/requests ~5, véhicules/tarifs ~5,
   facturation ~3).
3. **Hiérarchie typo écrasée** : 77 `text-sm` / 25 `text-xs` / **4
   `text-base`** au départ. Corps de lecture en petit.

## Décisions fermées

### D-01 — Terracotta sur les moments-clés admin (liste fermée)

11 occurrences de `variant="accent"` ajoutées dans `(admin)` :

**Page-level CTAs (primary action de l'écran)** :

| Fichier | CTA |
|---|---|
| `chauffeurs/_components/drivers-list.client.tsx` | « Nouveau chauffeur » (page) |
| `chauffeurs/_components/drivers-list.client.tsx` | EmptyState action « Inviter un chauffeur » (via `variant: 'accent'` exposé par `<EmptyState>`) |
| `vehicules/_components/vehicles-list.client.tsx` | « Nouveau véhicule » (page) |
| `vehicules/_components/vehicles-list.client.tsx` | EmptyState action « Ajouter un véhicule » |
| `legal/dpa/_components/dpa-list.client.tsx` | « Nouveau DPA » |
| `legal/registre/page.tsx` | « Nouvelle entrée » (PageHeader actions) |
| `legal/requests/_components/requests-list.client.tsx` | « Nouvelle demande » |
| `legal/dpia/_components/dpia-list.client.tsx` | « Créer une trame DPIA » (asChild Link) |

**Sheet/drawer submits (moment-clé creation)** :

| Fichier | CTA |
|---|---|
| `tarifs/_components/tariff-edit-sheet.client.tsx` | « Créer la version » (submit) |
| `legal/registre/_components/registre-drawer.client.tsx` | « Enregistrer » (submit) |
| `legal/requests/_components/request-drawer.client.tsx` | « Créer » (submit) |
| `legal/dpa/_components/dpa-drawer.client.tsx` | « Enregistrer » (submit) |
| `chauffeurs/_components/drivers-list.client.tsx` | « Envoyer l'invitation » (submit) |

**NE PAS mettre en terracotta** : « Confirmer en saisissant ARCHIVER »
(action destructive → reste `destructive` — sémantique vigilance, pas
moment-clé d'avancement), « Modifier la grille » (action Modifier
neutre), « Pré-remplir » (assistant, pas l'acte final), « Télécharger
le PDF » (Export = neutre), « Annuler ».

**Extension légère de `EmptyState`** : ajout d'une prop optionnelle
`variant?: 'default' | 'accent'` sur les actions de l'EmptyState. Permet
aux moments-clés des écrans vides d'hériter du terracotta sans casser
les autres EmptyStates (default = bleu primaire conservé).

### D-02 — `loading.tsx` sur les 6 écrans à fetch

Skeletons épousant le layout réel de chaque écran (modèle = les 5 de
régulation lot 3). Aucun spinner générique.

| Écran | Structure skeleton |
|---|---|
| `chauffeurs/loading.tsx` | En-tête + CTA, table 8 lignes |
| `vehicules/loading.tsx` | En-tête + CTA, table 6 lignes |
| `tarifs/loading.tsx` | En-tête, card grille (titre + 5 lignes clé/valeur), historique 3 lignes |
| `facturation/loading.tsx` | En-tête + description, sélecteur mois, section aperçu (totaux 3 cards + lignes + CTA PDF) |
| `legal/registre/loading.tsx` | En-tête + 2 CTA, table 6 lignes |
| `legal/requests/loading.tsx` | En-tête + CTA, table 5 lignes |

`prefers-reduced-motion` couvert par la règle globale `globals.css`
(animation-duration 0.01ms) — RGAA OK.

### D-03 — Hiérarchie typographique (gradation, pas grossir)

Promotion ciblée des **corps de lecture réels** (paragraphes
explicatifs, pas légendes) :

| Fichier | Avant | Après |
|---|---|---|
| `legal/dpa/pre-remplir/_components/dpa-prefill-card.client.tsx` (help text) | `text-xs` | `text-sm` |
| `legal/dpa/pre-remplir/_components/dpa-prefill-review.client.tsx` (paragraphe légal) | `text-sm` | **`text-base`** |
| `legal/registre/pre-remplir/_components/registre-prefill-review.client.tsx` (paragraphe légal) | `text-sm` | **`text-base`** |
| `legal/dpia/pre-remplir/_components/dpia-prefill-confirm.client.tsx` (paragraphe légal) | `text-sm` | **`text-base`** |
| `legal/page.tsx` titres de cards | `text-sm font-semibold` | `text-base font-semibold` |
| `maintenance/page.tsx` titres section + descriptions | `text-sm` titres + `text-xs` descriptions | `text-base` titres + `text-sm` descriptions |
| `chauffeurs/_components/archive-driver-modal.client.tsx` help text | `text-xs` | `text-sm` |

**Tables/listes denses préservées** (`text-sm`) — densité admin assumée.

**Résultats mesurables** :

| Classe | Avant | Après | Δ |
|---|---|---|---|
| `text-base` | 4 | **10** | **+150 %** |
| `text-sm` | 77 | 75 | -2 |
| `text-xs` | 25 | **21** | **-16 %** |

### D-04 — Cohérence légale (vérif légère)

Les 8 pages légales étaient déjà homogènes (PageHeader unifié, ton des
descriptions cohérent depuis 06.6). Vérifié :
- Terracotta appliqué de façon cohérente (DPA, DPIA, registre, requests
  tous traités sur le même pattern : button page-level + submit drawer).
- Ton des descriptions : déjà aligné §4 (06.6 + 06.29 ont harmonisé).
- Pas de refonte.

### D-05 — Périmètre

Admin uniquement. Pas de nouvelle feature, pas de refactor logique.

## Garde-fous

- DEC-032 (branche + PR) ✓
- guard-commit (typecheck + format + tests verts) ✓
- ≤ 300 LOC / fichier ✓ (6 nouveaux `loading.tsx` ≤ 60 LOC)
- WCAG AA : terracotta hsl(14 78% 46%) + blanc = 4.61:1 ✓ (validé lot 2)
- Jour + nuit ✓
- `prefers-reduced-motion` couvert globalement
- NFR-001 ✓
- RGAA : skeleton suit le gabarit Suspense Next.js
- Direction DEC-101 §3 (couleur) + §5 (premium) + §5bis (structure)
- 0 hex ajouté, 0 nouvelle dépendance, 0 migration BDD

## Trace DEC-110

Inscrite dans `.planning/STATE.md` + entrée `.planning/journal.md` :
« Incarnation direction sur Admin (16 écrans) : terracotta sur 13 CTA
moments-clés (8 page-level + 5 submits drawer ; ARCHIVER reste
destructive ; Modifier/Pré-remplir/Exporter neutres) ; loading.tsx sur
6 écrans à fetch (chauffeurs, vehicules, tarifs, facturation,
legal/registre, legal/requests) ; hiérarchie typo travaillée
(text-base 4→10 +150 %, text-xs 25→21 -16 %) ; cohérence légale
vérifiée. PageHeader déjà unifié, préservé. »

## Pas d'ADR

Activation et déclinaison de patterns existants (variant accent,
loading.tsx, Skeleton). Extension légère de `EmptyState` (variant prop
optionnel, rétro-compatible). Aucun choix structurel nouveau.
