# Phase 04.5 — Robustesse régulateur + Qualité saisie + Workflow chauffeur — Discussion Log (Assumptions Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-05-14
**Phase:** 04.5-robustesse-regulateur
**Mode:** assumptions (scope-locked par dirigeant)
**Trigger:** UAT mini du 2026-05-14 a remonté 16 frictions (4 majeures) + 1 dette seed
**Areas analyzed:** Workflow chauffeur (T1), Qualité saisie patient (T2), Recherche adresse + POI (T3), Filtrage permis/véhicule (T4), Dette technique (T5), Découpes + refactor visuel (T6)

---

## Source du scope

Le dirigeant a fourni le scope intégral en input (prompt HOTFIX DEC-034 + Phase 04.5). Pas de gray area de scope — uniquement des ambiguïtés d'implémentation à valider.

Méthode assumptions appliquée : codebase scout d'abord, assumptions ensuite, validation utilisateur pour les 4 ambiguïtés résiduelles uniquement.

---

## Assumptions Présentées

### TRONC 1 — Workflow chauffeur

| Assumption | Confidence | Evidence |
|---|---|---|
| Pattern logging défensif `/conduite/page.tsx` miroir PR #63 admin chauffeurs | Confident | `apps/web/src/app/(admin)/admin/chauffeurs/page.tsx` (post PR #63) |
| Seed `seed.demo.sql` utilise déjà dates relatives — cause root = `ON CONFLICT DO NOTHING` | Confident | `supabase/seed.demo.sql:240-256` |
| Fix idempotent via `DO UPDATE SET scheduled_at, started_at, ended_at, created_at` sur ids démo `44444444-%` | Likely | Pattern courant Supabase upsert |

### TRONC 2 — Qualité saisie patient

| Assumption | Confidence | Evidence |
|---|---|---|
| `telephoneReunionSchema` existe — schéma `0(262\|263\|692\|693)…` plus précis que brief « 06/07/02 » | Confident | `packages/shared/src/validators/common.ts` |
| `nirFormatSchema` valide format 15 chiffres mais PAS la clé contrôle | Confident | `packages/shared/src/validators/common.ts` |
| `codePostalReunionSchema` valide `^974[0-9]{2}$` | Confident | idem |
| Ville actuellement `z.string().min(2)` — à transformer en `z.enum(VILLES_974)` | Likely | `packages/shared/src/validators/common.ts:adresseSchema` |
| `react-datepicker` réutilisable (déjà Phase 03.2 DateTimeFields) | Confident | dépendance package.json + CONCERNS.md hotfix Phase 03.2 |
| Regex nom/prénom et range âge à AJOUTER | Likely | `patient.ts` schéma actuel min/max seulement |

### TRONC 3 — Recherche adresse + POI métier

| Assumption | Confidence | Evidence |
|---|---|---|
| `address-picker-field.client.tsx` existe — à refondre en `AddressOrPOIPicker` | Confident | `find apps/web/src -name "*ddress*"` |
| Nouvelle table `pois_metier` via migration `supabase db push` côté CD (DEC-032) | Confident | Pattern verrouillé DEC-032 |
| Seed initial ~30 POI dans `seed.demo.sql` | Likely | Pattern existant (3 chauffeurs + 12 rides) |

### TRONC 4 — Filtrage permis/véhicule

| Assumption | Confidence | Evidence |
|---|---|---|
| Modal assignation ligne 45 « Pas de couplage type_permis ↔ vehicle.type V1 » | Confident | `assign-modal.client.tsx:45` |
| Logique : véhicule TPMR exige `tpmr ∈ type_permis[]`, VSL accepte taxi (taxi conventionné peut faire VSL), taxi exige taxi | Likely | Métier taxi conventionné 974 |
| Filtre par défaut compatibles + toggle « Afficher tous » | Likely | UX cohérent avec sémantique 4 actions DEC-029 |

### TRONC 5 — Dette technique

| Assumption | Confidence | Evidence |
|---|---|---|
| Join `audit_logs.actor_id → profiles.id` pour nom acteur | Confident | Schéma BDD vu via Supabase MCP |
| 5 `TODO(types)` confirmés présents `apps/web/src` | Confident | `grep -rn "TODO(types)" apps/web/src` |
| Audit permissions T5.3 = grep + documentation, pas tout corriger dans 04.5 | Likely | Anti-pattern « tout absorber dans une phase » |

### TRONC 6 — Découpes + refactor visuel

| Assumption | Confidence | Evidence |
|---|---|---|
| `ride-express-modal.client.tsx` est à 291 L (déjà sous 300) — T6.1 peut-être NO-OP | Confident | `wc -l` |
| `ride-drawer.client.tsx` à 337 L > 300 L | Confident | `wc -l` |
| T6.3 refactor `/admin/chauffeurs` short (~30 min) car application directe DEC-034 | Likely | UI-PATTERNS.md déjà codifié |

---

## Corrections Made

À renseigner par le dirigeant après revue des 4 assumptions présentées dans CONTEXT.md `<assumptions_to_validate>` :

### A-01 — Validation clé contrôle NIR (T2.1)

- **Original assumption** : ajouter validation clé contrôle au schéma `nirFormatSchema`
- **User correction** : _en attente_
- **Reason** : _en attente_

### A-02 — Liste fermée 26 communes 974 (T2.1)

- **Original assumption** : `z.enum(VILLES_974)` avec auto-complétion `code_postal → ville`
- **User correction** : _en attente_
- **Reason** : _en attente_

### A-03 — Préfixe téléphone (T2.1)

- **Original assumption** : garder schéma existant `0262/0263/0692/0693` (vs spec brief 06/07/02)
- **User correction** : _en attente_
- **Reason** : _en attente_

### A-04 — Refactor T6.3 dans Phase 04.5 (UI-PATTERNS.md)

- **Original assumption** : garder T6.3 dans 04.5 (court, application directe)
- **User correction** : _en attente_
- **Reason** : _en attente_

---

## DEC anticipées (à inscrire en PROJECT.md après planning)

- DEC-035 : Table `pois_metier` seedée Réunion (~30 POI)
- DEC-036 : Masques saisie patient stricts (DatePicker + NIR + téléphone + code postal + ville liste fermée)
- DEC-037 : `AddressOrPOIPicker` mixe POI métier + BAN
- DEC-038 : Modal assignation filtre par défaut compatibles permis/véhicule
- DEC-039 : Seed démo idempotent + glissant (`DO UPDATE` au lieu de `DO NOTHING` sur rides démo)
- DEC-040 (optionnelle) : Pattern audit permissions modules admin si trous récurrents

---

## External Research

Aucune recherche externe lancée — le scope brief du dirigeant + le scout codebase ont couvert l'ensemble des assumptions. Les 4 ambiguïtés A-01..A-04 sont des choix utilisateur, pas des questions techniques nécessitant recherche.

Si la validation utilisateur sur A-01 (clé contrôle NIR) demande un calcul précis, prévoir un mini-spike côté planning pour récupérer l'algorithme officiel INSEE (Wikipedia ou source primaire). Estimation : 30 min, à intégrer au Plan 2 (Qualité saisie patient).

---

## Méthodologie

Pas de `.planning/METHODOLOGY.md` actif. Mode assumptions appliqué selon `discuss-phase-assumptions.md` v1.41.2.

---

*Discussion log clos : 2026-05-14 — assumptions surfacées (33 au total dont 4 à valider), CONTEXT.md écrit, ready for `/gsd-ui-phase 04.5` ou `/gsd-plan-phase 04.5` après réponses A-01..A-04.*
