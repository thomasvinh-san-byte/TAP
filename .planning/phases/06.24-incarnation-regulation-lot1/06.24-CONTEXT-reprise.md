# Phase 06.24 (reprise) — CONTEXT

> Reprise du lot 1 d'incarnation Régulation. Finit l'en-tête (3 écrans oubliés) + hiérarchie typo (vrai travail mesurable).

## Constat précis (post-merge #248)

- **3 écrans régulation SANS PageHeader** (h1 manuel résiduel) : `patients/new/page.tsx`, `patients/[id]/edit/page.tsx`, `cockpit/optimisation/_components/optimization-shell.client.tsx`.
- **Hiérarchie typo** : la gradation existait par la GRAISSE et les CAPITALES (titres section en `text-xs font-semibold uppercase tracking-wide`) mais PAS par la TAILLE — tout restait en 12-14 px. Hiérarchie faible : perceptible de près, peu au coup d'œil.

## Décisions fermées

### D-01 — Finir l'en-tête sur les 3 écrans oubliés

| Écran | Migration |
|---|---|
| `patients/new/page.tsx` | `<h1>Nouveau patient</h1>` → `<PageHeader title="Nouveau patient" />` |
| `patients/[id]/edit/page.tsx` | `<h1>Modifier — {nom} {prenom}</h1>` → `<PageHeader title={\`Modifier — ${p.nom} ${p.prenom}\`} />` |
| `cockpit/optimisation/optimization-shell.client.tsx` | `<header>` manuel avec titre dynamique « Optimisation du DD/MM/YYYY » + boutons « ↻ Re-calculer » + « Fermer » → `<PageHeader title=… actions={...}>` |

Objectif atteint : **0 h1 manuel `text-2xl`** dans `(app)`, **9 fichiers** importent `PageHeader`.

### D-02 — Hiérarchie typo : décision CONSCIENTE, pas grossir aveuglément

Promotion ciblée des **textes de lecture courante** (descriptions, aides de saisie, paragraphes explicatifs) de `text-xs` → `text-sm` ou `text-base` :

**Fiche patient `[id]/page.tsx`** (vrai texte de lecture) :
- `Né(e) le …` : `text-sm` → `text-base`
- Téléphone, adresse, préférences (paragraphes sans classe par défaut) : ajout explicite `text-base`

**Help text de formulaire** (corps de lecture pour l'utilisateur, pas légende) :
- `patient-form-fields` : 5 occurrences `text-xs` → `text-sm` (help nom, NIR, NIR descriptif, téléphone, code postal, commune)
- `patient-form-sections` : 1 occurrence (`nom-help`) → `text-sm`
- `override-tarif-modal` : 2 occurrences (`new-tarif-help`, `motif-help`) → `text-sm`
- `ride-patient-picker` : 2 occurrences (tip de recherche, empty state) → `text-sm`
- `address-picker-field` : 2 occurrences (tip de recherche, empty state) → `text-sm`

**Descriptions de panneau** (texte explicatif réel) :
- `driver-positions-panel` : description « Dernière position connue… » `text-xs` → `text-sm` ; empty state `text-sm` → `text-base`

**Liens d'action** (un lien CTA ne doit pas être xs) :
- `optimization-shell` : lien `/admin/maintenance` `text-xs` → `text-sm`

**Titres de section / panneau préservés** :
- Kicker `text-xs font-semibold uppercase tracking-wide` (Linear/Stripe convention) — pattern majoritaire conservé.
- Titres panneau `text-base font-semibold` (« Carte des chauffeurs », « Patient absent ») conservés.
- `excluded-rides-section` : `text-sm font-semibold text-amber-800` → `text-base font-semibold text-amber-800` (aligné sur les autres titres de panneau).

**Tables denses NON touchées** (cockpit courses-table, caisse) : `text-sm` préservé — densité régulatrice assumée (direction §5bis).

### D-03 — Harmoniser la convention titre-de-section

Aujourd'hui le pattern majoritaire était `text-xs uppercase font-semibold tracking-wide` ; `tableau-de-bord` utilisait `text-sm font-semibold uppercase` (sans `tracking-wide`).

**Action** : `tableau-de-bord` aligné sur le pattern majoritaire — 2 titres (`bloc-action`, `bloc-sante`) passés à `text-xs font-semibold uppercase tracking-wide`. La famille Régulation a maintenant **UNE seule convention** de kicker section.

### D-04 — Rien d'autre

Pas de couleur (lot 3), pas de skeletons (lot 4), pas de refactor (lot 5), pas de rangement (lot 6).

## Résultats mesurables (avant → après)

| Classe | Avant (post-#248) | Après (cette PR) | Δ |
|---|---|---|---|
| `text-2xl` | 7 | 4 | -3 (h1 manuels remplacés par PageHeader) |
| `text-base` | 8 | **15** | **+88 %** ← vrai texte de lecture promu |
| `text-lg` | 1 | 1 | — |
| `text-sm` | 115 | 126 | +9 (ex-xs montés) |
| `text-xs` | 87 | **73** | **-16 %** ← corps de texte mal employé en xs réduit |

**Gradation de taille désormais visible** entre corps de lecture (`text-base`/`text-sm`) et légende (`text-xs`), pas seulement la graisse/casse.

## Garde-fous

- DEC-032 (branche + PR)
- guard-commit (typecheck + format + tests verts) ✓
- ≤ 300 LOC / fichier ✓
- NFR-001 ✓
- RGAA : hiérarchie headings logique (h1 PageHeader > h2 kicker > h3 sous-section)
- Jour + nuit : 0 hex ajouté ✓
- Tables denses (cockpit courses-table, caisse) **non touchées** (densité assumée, direction §5bis) ✓
- Direction DEC-101 §5bis respectée (CONTRASTE par propriété taille + graisse + uppercase + tracking)

## Trace DEC-103

Inscrite dans `.planning/STATE.md` + entrée `.planning/journal.md` :
« Reprise lot 1 : PageHeader étendu aux 3 écrans restants (patients/new, patients/[id]/edit, cockpit/optimisation) → en-tête unifié sur TOUTE la régulation (0 h1 manuel restant) ; hiérarchie typo travaillée (corps de lecture en `text-sm`/`text-base`, `text-base` +88 %, `text-xs` -16 %, convention titre-de-section harmonisée, tables denses préservées). Pas d'ADR. »

## Pas d'ADR

Activation et complétude de pattern acté (DEC-101 §5bis levier 2). Aucun choix structurel nouveau.
