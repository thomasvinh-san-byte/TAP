# Phase 1.5 : DPA + RGPD compliance — Pattern Map

**Mapped:** 2026-05-07
**Files analyzed:** 23 fichiers à créer + 1 fichier à modifier
**Analogs found:** 22 / 24 (2 sans analog direct : `legal-token.ts`, `cookie-banner.client.tsx`)

---

## File Classification

| Fichier à créer / modifier | Rôle | Data Flow | Analog le plus proche | Qualité du match |
|----------------------------|------|-----------|------------------------|------------------|
| `supabase/migrations/2026050X_legal_compliance.sql` | migration | DDL + RLS + audit triggers | `supabase/migrations/20260507000001_patients.sql` | exact |
| `supabase/migrations/2026050Y_breach_72h_alert.sql` | migration | DDL + pg_cron job | `supabase/migrations/20260507000001_patients.sql` (Section 11 trigger) | role-match (pas de cron analog) |
| `supabase/tests/legal_compliance.sql` | test pgTAP | RLS isolation + audit | `supabase/tests/patients.sql` | exact |
| `packages/shared/src/utils/patient-data-export.ts` | shared (helper pur) | request-response (read-only export) | `packages/shared/src/utils/patient-note.ts` | role-match |
| `packages/shared/src/utils/patient-anonymize.ts` | shared (helper pur) | transform/RPC | `packages/shared/src/utils/patient-note.ts` | role-match |
| `packages/shared/src/utils/legal-token.ts` | shared (helper pur) | sign/verify JWT | `packages/shared/src/validators/common.ts` (helper pur Luhn) | role-match faible |
| `apps/web/src/app/(public)/layout.tsx` | layout RSC public | request-response | `apps/web/src/app/layout.tsx` (root) + `apps/web/src/app/(app)/layout.tsx` (auth) | partial — composer les 2 |
| `apps/web/src/app/(public)/legal/{cgu,cgv,confidentialite,cookies,dpo}/page.tsx` | page SSG | static render Markdown | `apps/web/src/app/(app)/patients/page.tsx` (RSC + metadata) | role-match (pas de SSG MDX existant) |
| `apps/web/src/app/(public)/legal/request/[token]/page.tsx` | page RSC dynamique | request-response | `apps/web/src/app/(app)/patients/[id]/page.tsx` (à inférer) ou `patients/page.tsx` | role-match |
| `apps/web/src/app/(public)/legal/request/[token]/actions.ts` | server actions | mutate + audit | `apps/web/src/app/(app)/patients/actions.ts` | exact |
| `apps/web/src/app/(admin)/legal/registre/page.tsx` (et 5 autres pages admin) | page RSC admin | CRUD + RLS | `apps/web/src/app/(app)/patients/page.tsx` | exact |
| `apps/web/src/app/(admin)/legal/_components/*.client.tsx` (drawers CRUD) | client component | UI Sheet drawer | `apps/web/src/app/(app)/patients/_components/patient-drawer.client.tsx` | exact |
| `apps/web/src/app/(admin)/legal/breaches/breach-timer.client.tsx` | client component | realtime subscribe + tick | `apps/web/src/app/(app)/patients/_components/patient-drawer.client.tsx` (useQuery) | role-match (timer = setInterval custom) |
| `apps/web/src/app/api/admin/legal/registre/pdf/route.ts` | route handler | streaming PDF | aucun route handler existant | **no analog** |
| `apps/web/src/components/cookie-banner.client.tsx` | client component | localStorage + audit log | aucun banner existant | **no analog** |
| `apps/web/src/content/legal/*.md` | content statique | — | aucun MDX existant | **no analog** |
| `apps/web/src/middleware.ts` | middleware (modif) | request-response routing | déjà existant — extension du matcher | exact (édit mineur) |

---

## Pattern Assignments

### `supabase/migrations/2026050X_legal_compliance.sql` (migration, DDL + RLS + audit)

**Analog :** `/home/user/TAP/supabase/migrations/20260507000001_patients.sql`

**Pattern table + organization_id + check enums** (lignes 35-68) :
```sql
create table public.patients (
  id uuid primary key default extensions.uuid_generate_v4(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  prenom text not null check (length(trim(prenom)) between 1 and 80),
  ...
  archive boolean not null default false,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  created_by uuid references auth.users(id) on delete set null,
  updated_by uuid references auth.users(id) on delete set null,
  constraint patients_consentement_sms_horodatage check (...)
);
```

**Pattern RLS forcée + 3 policies (select/insert/update, pas de DELETE)** (lignes 114-138) :
```sql
alter table public.patients enable row level security;
alter table public.patients force row level security;

create policy patients_select_same_org on public.patients
  for select to authenticated
  using (organization_id = public.current_organization_id());

create policy patients_insert_regulateur on public.patients
  for insert to authenticated
  with check (
    organization_id = public.current_organization_id()
    and (public.has_role('regulateur'::public.user_role)
         or public.has_role('dirigeant'::public.user_role))
  );
-- Pas de policy DELETE : archivage logique via colonne archive.
```

**Application Phase 1.5 :** dupliquer pour les 5 tables `data_processing_register`, `dpa_record`, `dpia_record`, `data_breach_incident`, `patient_data_request`. Pour D-16 / D-18 : remplacer `regulateur` par `dirigeant` (rôle requis par CONTEXT.md). Pour `patient_data_request` : également policy `update_dirigeant` (workflow réponse).

**Pattern audit trigger filtre colonnes sensibles** (lignes 199-222) :
```sql
create or replace function public.patients_audit_trigger()
returns trigger language plpgsql security definer set search_path = public as $$
declare action_name text;
begin
  action_name := 'patient.' || lower(tg_op);
  insert into public.audit_logs
    (organization_id, actor_id, actor_role, action, entity_type, entity_id, metadata)
  values (
    coalesce(new.organization_id, old.organization_id),
    auth.uid(), public.current_user_role(),
    action_name, 'patient', coalesce(new.id, old.id),
    jsonb_build_object(
      'old', case when tg_op in ('UPDATE','DELETE')
                  then to_jsonb(old) - 'nir_encrypted' - 'nir_search_hash' else null end,
      'new', case when tg_op in ('INSERT','UPDATE')
                  then to_jsonb(new) - 'nir_encrypted' - 'nir_search_hash' else null end
    )
  );
  return coalesce(new, old);
end; $$;
create trigger patients_audit_trigger
  after insert or update or delete on public.patients
  for each row execute function public.patients_audit_trigger();
```

**Application Phase 1.5 :** dupliquer pour les 5 tables. Pour `patient_data_request` : exclure `request_token` et `requester_proof_of_identity_url` (Pitfall 2 RESEARCH). Pour `data_breach_incident` : exclure `cnil_notification_reference` si jugé sensible (numéro ARS-CNIL ; selon décision planner).

**Pattern revoke/grant** (lignes 273-278) :
```sql
revoke all on public.patients from anon;
grant select, insert, update on public.patients to authenticated;
```

**Notes différences :**
- Pas de colonne générée `search_text` à reproduire (pas de fuzzy search sur les tables RGPD V1).
- Pas de vue `*_safe` à reproduire — pas de ciphertext sur ces 5 tables (sauf éventuel `requester_proof_of_identity_url` storage signé).
- Pour `data_processing_register` (D-05 versioning par lignes) : ne pas exposer policy UPDATE — uniquement INSERT (nouvelle ligne par modification).

---

### `supabase/migrations/2026050Y_breach_72h_alert.sql` (migration, pg_cron)

**Analog :** aucun cron existant. Pattern à composer depuis :
- Pattern `security definer` + `set search_path` : `patients_audit_trigger` (lignes 199-218)
- Pattern `insert into audit_logs` : idem

**Squelette à utiliser (depuis RESEARCH.md §5)** :
```sql
create extension if not exists pg_cron with schema cron;

create or replace function public.check_breach_deadlines()
returns void language plpgsql security definer set search_path = public as $$
declare r record;
begin
  for r in select id, organization_id, detected_at from public.data_breach_incident
    where cnil_notification_required = true and cnil_notification_at is null
      and closed_at is null
      and now() > detected_at + interval '66 hours'
  loop
    insert into public.audit_logs (organization_id, actor_id, actor_role, action,
                                   entity_type, entity_id, metadata)
    values (r.organization_id, null, null, 'breach.deadline_warning',
      'data_breach_incident', r.id,
      jsonb_build_object('hours_remaining',
        extract(epoch from (r.detected_at + interval '72 hours' - now()))/3600));
  end loop;
end; $$;

select cron.schedule('breach-72h-watchdog', '0 * * * *',
  $$ select public.check_breach_deadlines(); $$);
```

**Notes :** `actor_id`/`actor_role` peuvent être null (action système). À confirmer planner si `audit_logs.actor_id` est NOT NULL (vérifier migration 001 foundations).

---

### `supabase/tests/legal_compliance.sql` (test pgTAP)

**Analog :** `/home/user/TAP/supabase/tests/patients.sql`

**Pattern fixture multi-tenant Alpha + Bravo** (lignes 28-57) :
```sql
insert into public.organizations (id, nom, ville, code_postal)
values
  ('11111111-1111-1111-1111-111111111111', 'Org Alpha', 'Saint-Denis', '97400'),
  ('22222222-2222-2222-2222-222222222222', 'Org Bravo', 'Saint-Pierre', '97410');

insert into auth.users (id, instance_id, aud, role, email, encrypted_password,
  email_confirmed_at, created_at, updated_at, raw_app_meta_data, raw_user_meta_data)
values
  ('aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa', '00000000-0000-0000-0000-000000000000',
   'authenticated', 'authenticated', 'alpha-dir@test.tap',
   crypt('test1234!', gen_salt('bf')), now(), now(), now(),
   '{}'::jsonb, '{}'::jsonb),
  ...
insert into public.profiles (id, organization_id, role, prenom, nom, email)
values
  ('aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa', '11111111-1111-1111-1111-111111111111',
   'dirigeant', 'Alpha', 'Dirigeant', 'alpha-dir@test.tap'),
  ...
```

**Pattern RLS activée + forcée assertions** (lignes 62-69) :
```sql
select ok(
  (select relrowsecurity from pg_class where oid = 'public.patients'::regclass),
  'RLS activée sur patients'
);
select ok(
  (select relforcerowsecurity from pg_class where oid = 'public.patients'::regclass),
  'RLS forcée sur patients (force row level security)'
);
```

**Pattern isolation tenant `set local "request.jwt.claim.sub"`** (lignes 127-167) :
```sql
set local role authenticated;
set local "request.jwt.claim.sub" = 'cccccccc-cccc-cccc-cccc-cccccccccccc';

select lives_ok($$ insert into public.patients (...) values (...) $$,
  'alpha-reg insère un patient minimal valide');

set local "request.jwt.claim.sub" = 'dddddddd-dddd-dddd-dddd-dddddddddddd';
select is((select count(*)::int from public.patients), 0,
  'bravo-reg ne voit aucun patient Alpha (isolation tenant)');

select throws_ok(
  $$ insert into public.patients (organization_id, ...) values
       ('11111111-1111-1111-1111-111111111111', ...) $$,
  '42501', null, 'bravo-reg ne peut pas créer un patient dans Alpha');
```

**Pattern audit_logs ne contient PAS la colonne sensible** (lignes 204-218) :
```sql
select ok(
  (select not (metadata->'new') ? 'nir_encrypted'
     from public.audit_logs where action = 'patient.insert' limit 1),
  'audit_logs.metadata->new ne contient PAS nir_encrypted'
);
```

**Application Phase 1.5 :** 5 tests pgTAP — un par table RGPD. Pour `patient_data_request` : assert que `metadata->'new'` ne contient PAS `request_token` ni `requester_proof_of_identity_url`. Pour `dpa_record`, `dpia_record` : assert que rôle régulateur INSERT échoue avec `42501` (D-16 dirigeant only). Test dédié pour `check_breach_deadlines()` : insérer un breach `detected_at = now() - 67h`, exécuter la fonction, assert ligne `audit_logs.action = 'breach.deadline_warning'` créée.

---

### `packages/shared/src/utils/patient-data-export.ts` (shared, helper pur read)

**Analog :** `/home/user/TAP/packages/shared/src/utils/patient-note.ts`

**Pattern signature SupabaseClient<Database> + JSDoc + ≤ 50 lignes** (lignes 1-22) :
```ts
import type { SupabaseClient } from '@supabase/supabase-js';
import type { Database } from '@tap/database/types';

/**
 * Remplace la note opérationnelle active d'un patient (pattern
 * `replaced_by_id`, D-18). Modèle historique en chaîne :
 *  ...
 * Helper pur (≤ 50 lignes) — réutilisable côté Server Action et tests.
 */
export async function replacePatientNote(
  supabase: SupabaseClient<Database>,
  patientId: string,
  newContent: string,
  authorId: string,
): Promise<void> {
  const { data: active } = await supabase
    .from('patient_operational_note')
    .select('id, content, organization_id')
    .eq('patient_id', patientId)
    ...
```

**Application Phase 1.5 :** signature `generatePatientDataExport(supabase, patientId): Promise<PatientDataExport>`. Lit la **vue `patients_safe`** (PAS la table — Pitfall 2 RESEARCH + comment ligne 297 migration), `patient_operational_note`, `patient_constraint`, `audit_logs` filtrés par `entity_id`. Pour le NIR clair : appel à un wrapper qui passe par l'Edge Function `nir` (audit log automatique). Format de retour structuré JSON `{ format_version: '1.0', exported_at, patient: {...}, ... }`.

**Test analog :** `packages/shared/src/utils/__tests__/patient-note.test.ts` — mock fluent SupabaseClient minimal (lignes 13-40).

---

### `packages/shared/src/utils/patient-anonymize.ts` (shared, helper RPC)

**Analog :** `/home/user/TAP/packages/shared/src/utils/patient-note.ts`

**Pattern :** identique signature SupabaseClient<Database>, doc en français, ≤ 50 lignes. **Différence clé :** appelle une RPC SQL `rgpd_anonymize_patient(p_patient_id, p_request_id, p_salt)` (à créer dans la migration legal_compliance ou une migration RPC séparée). Le helper TS ne fait que valider l'appel et propager l'erreur traduite. Voir RESEARCH.md ligne 314-329 pour le snippet cible.

```ts
// signature cible
export async function anonymizePatient(
  supabase: SupabaseClient<Database>,
  patientId: string,
  requestId: string,
): Promise<void> {
  const salt = process.env.APP_ANONYMIZATION_SALT!;
  const { error } = await supabase.rpc('rgpd_anonymize_patient', {
    p_patient_id: patientId, p_request_id: requestId, p_salt: salt,
  });
  if (error) throw new Error(`Anonymisation échouée : ${error.message}`);
}
```

---

### `packages/shared/src/utils/legal-token.ts` (shared, JWT)

**Analog :** **aucun JWT helper existant**. Pattern proche : `packages/shared/src/validators/common.ts` (helper pur Luhn lignes 39-51) — fonction utilitaire isolée avec doc française.

**Pattern à composer (RESEARCH.md §1) :**
```ts
import { SignJWT, jwtVerify } from 'jose';

const secret = () => new TextEncoder().encode(process.env.APP_LEGAL_TOKEN_SECRET!);

export async function signRequestToken(claims: { sub: string; org: string; type: string }) {
  return new SignJWT(claims)
    .setProtectedHeader({ alg: 'HS256' })
    .setIssuedAt()
    .setExpirationTime('30d')
    .sign(secret());
}

export async function verifyRequestToken(token: string) {
  const { payload } = await jwtVerify(token, secret(), { algorithms: ['HS256'] });
  return payload as { sub: string; org: string; type: string };
}
```

**Notes :** placer dans `packages/shared/src/utils/` pour réutilisation Server Action publique + tests Vitest. Ne JAMAIS inclure `patient_id`, `email`, ou `nir_last4` dans les claims (Pitfall 1 RESEARCH).

---

### `apps/web/src/app/(public)/layout.tsx` (layout RSC public)

**Analogs combinés :**
- `apps/web/src/app/layout.tsx` (root, lignes 30-43) — pattern `<html lang="fr">` + bootstrap thème
- `apps/web/src/app/(app)/layout.tsx` (lignes 13-52) — pattern layout group avec children

**Pattern à inverser (RETIRER l'auth check) :**
```tsx
// Source : (app)/layout.tsx lignes 13-26 — version AVEC auth
export default async function AppLayout({ children }: { children: React.ReactNode }) {
  const supabase = createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) { redirect('/login'); }
  return (<Providers>...</Providers>);
}
```

**Application Phase 1.5 :** layout sans `getUser()` ni `redirect('/login')`. Header minimal (logo + lien "Connexion") + main + footer avec liens permanents `/legal/cgu`, `/legal/cgv`, `/legal/confidentialite`, `/legal/cookies`, `/legal/dpo`, "Gérer mes cookies" (D-14). Ne PAS envelopper de `<Providers>` (React Query inutile pour pages SSG). Conserver `<html lang="fr">` du root layout.

---

### Pages `/legal/{cgu,cgv,confidentialite,cookies,dpo}/page.tsx` (page SSG MDX)

**Analog le plus proche :** `apps/web/src/app/(app)/patients/page.tsx`

**Pattern metadata + RSC async** (lignes 12-22) :
```tsx
export const metadata = { title: 'Patients — TAP Régulation' };

export default async function PatientsPage() {
  const queryClient = new QueryClient();
  ...
  return (
    <div className="space-y-24">
      <header className="flex items-center justify-between">
        <h1 className="text-2xl font-semibold tracking-tight">Patients</h1>
        ...
      </header>
      ...
    </div>
  );
}
```

**Application Phase 1.5 :** chaque page lit son fichier `apps/web/src/content/legal/<slug>.md` via `fs.readFile` + `gray-matter` pour le frontmatter + `next-mdx-remote/rsc` `compileMDX` pour le rendu. Spacing tokens CLAUDE.md (`space-y-24`, `text-2xl font-semibold tracking-tight`). Métadonnées Next : titre depuis frontmatter `title:`. Ajouter `<LastUpdated date={frontmatter.version} />` composant custom rendu via mapping MDX.

---

### `apps/web/src/app/(public)/legal/request/[token]/page.tsx` + `actions.ts`

**Analog page :** pattern `apps/web/src/app/(app)/patients/page.tsx` (RSC + metadata + composant client form)
**Analog actions :** `apps/web/src/app/(app)/patients/actions.ts`

**Pattern Server Action zod → user → org → mutation → audit → revalidate** (lignes 68-160) :
```ts
'use server';
// Pattern (CLAUDE.md § 10) : Validation zod → Authz (RLS Postgres) →
// Transaction → Audit log (trigger Postgres) → Réponse.

export async function createPatientAction(
  _prev: ActionState,
  formData: FormData,
): Promise<ActionState> {
  const parsed = parsePatientForm(formData);
  if (!parsed.success) {
    return { error: parsed.error.errors[0]?.message ?? 'Saisie invalide.' };
  }
  const supabase = createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { error: 'Session expirée. Reconnectez-vous.' };

  const profileRes = await supabase.from('profiles').select('organization_id')
    .eq('id', user.id).single();
  ...
  const { data: row, error } = await supabase.from('patients').insert({...}).select('id').single();
  if (error || !insertedRow) {
    return { error: 'Création impossible (un patient avec ce NIR existe peut-être déjà).' };
  }
  revalidatePath('/patients');
  redirect(`/patients/${insertedRow.id}`);
}
```

**Application Phase 1.5 différences :**
- Pas d'`auth.getUser()` (portail public). À la place : `verifyRequestToken(token)` → `payload.sub = requestId` → load `patient_data_request` avec **service_role** (D-20). Voir RESEARCH.md §pattern Server Action workflow droit accès (lignes 555-573).
- Rate limiting AVANT vérification (RESEARCH.md §1 + §rate-limit) : table `legal_request_attempts` 5/h.
- Validation identité par formulaire (NIR + nom + DDN) avec `nirFormatSchema` de `common.ts`.
- Audit log via INSERT explicite dans Server Action (pas seulement trigger DB) : action `patient.data_request.fulfilled`.
- Messages d'erreur français : « Trop de tentatives. Réessayez dans 1 heure. », « Lien expiré ou invalide. ».

---

### Pages admin `/admin/legal/{registre,dpa,dpia,breaches,requests,dpo}/page.tsx`

**Analog :** `apps/web/src/app/(app)/patients/page.tsx` + `apps/web/src/app/(app)/layout.tsx` (auth check)

**Pattern guard rôle dirigeant (à composer) :** réutiliser `current_user_role()` côté DB via RLS (D-18) + ajouter check serveur dans le layout `(admin)` ou en tête de page :
```tsx
const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single();
if (profile?.role !== 'dirigeant') { redirect('/'); }
```

**Pattern preload + HydrationBoundary** (patients/page.tsx lignes 22-44) :
```tsx
export default async function PatientsPage() {
  const queryClient = new QueryClient();
  await queryClient.prefetchQuery({
    queryKey: ['patients', { q: '' }],
    queryFn: () => searchPatients(''),
  });

  return (
    <div className="space-y-24">
      <header className="flex items-center justify-between">
        <h1 className="text-2xl font-semibold tracking-tight">Patients</h1>
        <Button asChild><Link href="/patients/new">...</Link></Button>
      </header>
      <HydrationBoundary state={dehydrate(queryClient)}>
        <PatientsList />
      </HydrationBoundary>
    </div>
  );
}
```

**Application Phase 1.5 :** chaque page admin = liste table + bouton "Nouveau" → Drawer édition. Structure header h1 « Registre des traitements » / « DPA » / etc. Spacing `space-y-24`, ` text-2xl font-semibold tracking-tight`.

---

### `apps/web/src/app/(admin)/legal/_components/*-drawer.client.tsx`

**Analog :** `apps/web/src/app/(app)/patients/_components/patient-drawer.client.tsx`

**Pattern Sheet shadcn 400px + useQuery + Skeleton** (lignes 36-57) :
```tsx
'use client';
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription } from '@/components/ui/sheet';
import { Skeleton } from '@/components/ui/skeleton';
import { useQuery } from '@tanstack/react-query';

export function PatientDrawer({ patientId, open, onOpenChange }: Props) {
  const { data, isPending } = useQuery({
    queryKey: ['patient', patientId],
    queryFn: () => getPatientByIdAction(patientId!),
    enabled: open && patientId !== null,
  });
  return (
    <Sheet open={open} onOpenChange={onOpenChange}>
      <SheetContent side="right"
        className="w-[400px] sm:w-[400px] sm:max-w-[400px] overflow-y-auto">
        {isPending || !data ? <DrawerSkeleton /> : <DrawerBody data={...} />}
      </SheetContent>
    </Sheet>
  );
}
```

**Application Phase 1.5 :** un drawer par entité (`registre-drawer`, `dpa-drawer`, `dpia-drawer`, `breach-drawer`, `request-drawer`). Pour DPIA : éditeur Markdown `<textarea>` long + preview server-side via Server Action de rendu. Respect strict 150 lignes (CLAUDE.md § 11) — extraire les sections de formulaire en sous-composants comme `patient-drawer-sections.client.tsx`.

**Pattern formulaire client + react-dom Server Actions** (`patient-form.client.tsx` lignes 39-60) :
```tsx
import { useFormState, useFormStatus } from 'react-dom';
function SubmitButton({ children }) {
  const { pending } = useFormStatus();
  return <Button type="submit" disabled={pending} className="h-48">
    {pending ? 'Enregistrement…' : children}</Button>;
}
export function PatientForm({ action, defaultValues = {}, submitLabel }: Props) {
  const [state, formAction] = useFormState<ActionState, FormData>(action, {});
  return (
    <form action={formAction} className="max-w-[640px] space-y-24" noValidate>
      ...
      {state.error && <p role="alert" className="text-sm text-destructive">{state.error}</p>}
      <SubmitButton>{submitLabel}</SubmitButton>
    </form>
  );
}
```

---

### `apps/web/src/app/(admin)/legal/breaches/breach-timer.client.tsx`

**Analog faible :** `patient-drawer.client.tsx` (useQuery pattern)

**Pattern à composer :** `'use client'` + `useEffect` + `setInterval` (1s) recalculant `hours_remaining = (detected_at + 72h) - now()`. Code couleur Tailwind sémantique :
```tsx
'use client';
const remaining = ...;
const tone = remaining > 24 ? 'text-success'
           : remaining > 6 ? 'text-warning'
           : 'text-destructive';
return <div className={cn('font-mono tabular-nums', tone)}>J-{Math.floor(remaining/24)} • {Math.floor(remaining)}h</div>;
```

**Anti-pattern à éviter (CLAUDE.md § 5) :** pas d'animation rebondissante, pas d'emoji. Chiffres tabulaires obligatoires (`tabular-nums`).

**Realtime optionnel :** `supabase.channel('breach-incidents').on('postgres_changes', {...}).subscribe()` pour mise à jour fade-in subtile à l'ouverture/fermeture d'un incident.

---

### `apps/web/src/app/api/admin/legal/registre/pdf/route.ts`

**Analog :** **aucun** route handler existant dans le projet.

**Pattern à composer (RESEARCH.md §2) :**
```ts
export const runtime = 'nodejs';  // OBLIGATOIRE — @react-pdf/renderer N'EST PAS Edge

import { renderToStream } from '@react-pdf/renderer';
import { createClient } from '@/lib/supabase/server';
import { RegistrePdf } from './_components/registre-pdf';

export async function GET() {
  const supabase = createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return new Response('Unauthorized', { status: 401 });
  // Check rôle dirigeant (RLS le bloquera de toute façon, mais 401 propre)
  const { data: entries } = await supabase.from('data_processing_register').select('*');
  const stream = await renderToStream(<RegistrePdf entries={entries ?? []} />);
  return new Response(stream as unknown as ReadableStream, {
    headers: { 'Content-Type': 'application/pdf' },
  });
}
```

**Notes critique :** `runtime = 'nodejs'` requis (Pitfall RESEARCH §2). Composant PDF dans `_components/registre-pdf.tsx` séparé (≤ 150 lignes). Audit log explicite avant retour : action `legal.registre.exported_pdf`.

---

### `apps/web/src/components/cookie-banner.client.tsx`

**Analog :** **aucun** banner existant. Composant maison (RESEARCH.md « Don't Hand-Roll » → exception : <150 lignes).

**Pattern à composer (RESEARCH.md §4 + CLAUDE.md § 5) :**
```tsx
'use client';
import { useState, useEffect } from 'react';
import { Button } from '@/components/ui/button';

export function CookieBanner() {
  const [open, setOpen] = useState(false);
  useEffect(() => { setOpen(!readConsent()); }, []);
  if (!open) return null;
  return (
    <div role="dialog" aria-label="Préférences cookies"
         className="fixed bottom-0 inset-x-0 border-t bg-background p-16 shadow-lg z-50">
      <p className="text-sm mb-12">Nous utilisons des cookies pour ...</p>
      <div className="flex gap-12">
        {/* 3 boutons strictement équivalents — taille, couleur, padding */}
        <Button variant="outline" className="flex-1 h-44" onClick={...}>Tout refuser</Button>
        <Button variant="outline" className="flex-1 h-44" onClick={...}>Personnaliser</Button>
        <Button variant="outline" className="flex-1 h-44" onClick={...}>Tout accepter</Button>
      </div>
    </div>
  );
}
```

**Règles strictes (Pitfall 4 RESEARCH) :**
- **Symétrie absolue** des 3 boutons : `variant="outline"` identique, `flex-1`, `h-44` (≥ 44px CNIL), même `padding`. Snapshot Playwright en test E2E.
- Pas de modal pleine page (anti-pattern CNIL).
- `localStorage.cookieConsent = { version, choices, expires_at }` (6 mois TTL).
- POST `/api/legal/cookie-consent` (route à créer ou Server Action) → INSERT `cookie_consent_log` (table dédiée, voir RESEARCH ligne 240 + Open Question 1).

---

### `apps/web/src/content/legal/*.md`

**Analog :** **aucun** contenu MDX existant. Frontmatter standard (RESEARCH.md §3) :
```yaml
---
title: Conditions Générales d'Utilisation
version: 2026-05-07
effective_at: 2026-05-15
---

# Conditions Générales d'Utilisation

(corps Markdown français — pas d'emoji, ton professionnel sobre, niveau Linear/Stripe)
```

5 fichiers : `cgu.md`, `cgv.md`, `confidentialite.md`, `cookies.md`, `dpo.md`. Templates de base avec mentions légales obligatoires CNIL (Identité responsable traitement, finalités, droits art. 15-21, contact DPO, durée conservation, recours CNIL).

---

### `apps/web/src/middleware.ts` (modification)

**Fichier existant** — édition mineure :

**Pattern actuel** (lignes 22-32) :
```ts
const { pathname } = request.nextUrl;
const isAuthRoute = pathname.startsWith('/login');

if (!user && !isAuthRoute) {
  const loginUrl = request.nextUrl.clone();
  loginUrl.pathname = '/login';
  if (pathname !== '/') {
    loginUrl.searchParams.set('next', pathname);
  }
  return NextResponse.redirect(loginUrl);
}
```

**Modification Phase 1.5 :** ajouter `/legal/*` à la liste des routes publiques (D-21) :
```ts
const isAuthRoute = pathname.startsWith('/login');
const isPublicLegal = pathname.startsWith('/legal');

if (!user && !isAuthRoute && !isPublicLegal) {
  // ... redirect login
}
```

**Notes :** ne PAS modifier le matcher (ligne 45) — il continue de couvrir `/legal/*` mais le code skip le redirect. Conserve `getUser()` qui est utile pour afficher CTA "Connexion" si l'utilisateur est déjà connecté sur le footer public.

---

## Shared Patterns

### Authentification + autorisation
**Source :** `apps/web/src/app/(app)/layout.tsx` lignes 18-25 + RLS Postgres `current_organization_id()` / `has_role()`
**Apply to :** toutes les pages admin `/admin/legal/*`, toutes les Server Actions
```tsx
const supabase = createClient();
const { data: { user } } = await supabase.auth.getUser();
if (!user) { redirect('/login'); }
// pour admin : check rôle dirigeant
const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single();
if (profile?.role !== 'dirigeant') redirect('/');
```
**EXCLU :** layout `(public)` et pages `/legal/*` (D-21).

### Validation zod française
**Source :** `packages/shared/src/validators/common.ts` lignes 11-20 + `patient.ts` lignes 51-76
**Apply to :** tous les schémas zod des nouveaux formulaires admin (registre, DPIA, breach, request)
```ts
export const codePostalReunionSchema = z
  .string().trim()
  .regex(/^974[0-9]{2}$/, { message: 'Code postal invalide (974xx attendu).' });

export const patientSchema = z.object({
  prenom: z.string().trim().min(1, 'Prénom requis').max(80),
  ...
}).refine(/* contraintes croisées */, { message: '...', path: ['...'] });
```
**Application Phase 1.5 :** schémas `dataProcessingRegisterSchema`, `dpiaSchema`, `breachIncidentSchema`, `dataRequestSchema` dans `packages/shared/src/validators/legal.ts` (nouveau fichier).

### Server Action workflow
**Source :** `apps/web/src/app/(app)/patients/actions.ts` lignes 68-160
**Apply to :** toutes les Server Actions admin + portail patient
```ts
'use server';
// CLAUDE.md § 10 : Validation zod → Authz (RLS) → Transaction → Audit log → Réponse
export async function actionName(_prev: ActionState, formData: FormData): Promise<ActionState> {
  const parsed = schema.safeParse(...);
  if (!parsed.success) return { error: parsed.error.errors[0]?.message ?? 'Saisie invalide.' };
  const supabase = createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { error: 'Session expirée.' };
  // ... mutation
  if (error) return { error: 'Message FR safe.' };
  revalidatePath('/admin/legal/...');
  redirect(...);
}
```

### Audit logs
**Source :** trigger Postgres `patients_audit_trigger()` (migration patients lignes 199-218)
**Apply to :** toutes les 5 tables RGPD via triggers DB (D-19)
**Filtre colonnes sensibles obligatoire** sur `patient_data_request` : `to_jsonb(...) - 'request_token' - 'requester_proof_of_identity_url'`.

### Composants shadcn/ui
**Source :** Sheet `apps/web/src/components/ui/sheet.tsx` (existant), Skeleton, Badge, Button
**Apply to :** tous les drawers admin + bandeau cookies

### Tests pgTAP fixtures
**Source :** `supabase/tests/patients.sql` lignes 21-57
**Apply to :** tous les nouveaux fichiers de tests RLS — réutiliser les UUIDs Alpha (1...) / Bravo (2...) et les rôles `dirigeant` (a...) / `regulateur` (c...) déjà installés.

---

## No Analog Found

| Fichier | Rôle | Data Flow | Raison |
|---------|------|-----------|--------|
| `apps/web/src/app/api/admin/legal/registre/pdf/route.ts` | route handler | streaming PDF | Aucun route handler existant. Composer depuis RESEARCH.md §2 + runtime nodejs. |
| `apps/web/src/components/cookie-banner.client.tsx` | client component | localStorage + audit log | Aucun banner. Composer depuis RESEARCH.md §4 (≤150 lignes). |
| `apps/web/src/content/legal/*.md` | content statique | — | Aucun MDX. Templates Markdown CNIL à rédiger. |
| `packages/shared/src/utils/legal-token.ts` | shared (JWT) | sign/verify | Aucun JWT helper. Utiliser `jose` (RESEARCH.md §1). |
| `supabase/migrations/2026050Y_breach_72h_alert.sql` (cron) | migration | pg_cron | Aucun cron existant. Squelette RESEARCH.md §5. |

---

## Metadata

**Analog search scope :**
- `/home/user/TAP/supabase/migrations/`
- `/home/user/TAP/supabase/tests/`
- `/home/user/TAP/apps/web/src/`
- `/home/user/TAP/packages/shared/src/`

**Files scanned :** ~35 fichiers
**Pattern extraction date :** 2026-05-07

---

## PATTERN MAPPING COMPLETE

**Phase :** 1.5 — DPA + RGPD compliance
**Files classified :** 23 nouveaux + 1 modif
**Analogs found :** 22 / 24

### Coverage
- Files with exact analog : 9 (migration, tests pgTAP, server actions, drawer Sheet, layout admin, page admin, validators, helpers shared, audit triggers)
- Files with role-match analog : 10 (helper anonymize, helper export, layout public, pages SSG MDX, page portail patient, page admin /admin/legal/*, breach timer, JWT helper)
- Files with no analog : 5 (PDF route handler, cookie banner, content MDX, JWT helper composé, migration pg_cron)

### Key Patterns Identified
- **Multi-tenant strict** : 5 nouvelles tables = `organization_id` + RLS forcée + helpers `current_organization_id()` / `has_role('dirigeant')` (D-18)
- **Audit trigger filtre colonnes sensibles** : reproduire pattern `to_jsonb(NEW) - 'col1' - 'col2'` de `patients_audit_trigger` — Pitfall 2 critique pour `patient_data_request` (token + proof_of_identity)
- **Server Actions** : zod → auth → mutation → audit → revalidate → redirect, messages FR safe, jamais de stack trace en UI
- **shadcn/ui Sheet drawer 400px** : pattern `patient-drawer.client.tsx` directement réutilisable pour les 5 drawers RGPD
- **Tests pgTAP** : fixtures Alpha/Bravo + `set local "request.jwt.claim.sub"` + assert audit_logs ne contient PAS la colonne sensible
- **Helpers purs `packages/shared/src/utils/`** : signature `SupabaseClient<Database>`, ≤ 50 lignes, JSDoc français
- **Pas d'analog pour cron / PDF / banner / MDX / JWT** — composition depuis RESEARCH.md justifiée

### Ready for Planning
Le planner dispose des patterns concrets (avec lignes précises) à recopier pour 22 des 24 fichiers, et des squelettes RESEARCH.md pour les 5 fichiers sans analog. Les 3 piliers CLAUDE.md (UX, design system, sécurité) sont matérialisés via les patterns Tailwind existants, les helpers RLS/audit, et les anti-patterns explicites (Pitfall 2 trigger, Pitfall 4 cookie banner).
