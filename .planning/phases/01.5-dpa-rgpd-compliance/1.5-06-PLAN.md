---
phase: 01.5-dpa-rgpd-compliance
plan: 06
type: execute
wave: 3
depends_on: ["1.5-04"]
files_modified:
  - apps/web/src/app/(admin)/admin/legal/registre/page.tsx
  - apps/web/src/app/(admin)/admin/legal/registre/actions.ts
  - apps/web/src/app/(admin)/admin/legal/registre/_components/registre-drawer.client.tsx
  - apps/web/src/app/(admin)/admin/legal/dpa/page.tsx
  - apps/web/src/app/(admin)/admin/legal/dpa/actions.ts
  - apps/web/src/app/(admin)/admin/legal/dpa/_components/dpa-drawer.client.tsx
  - apps/web/src/app/(admin)/admin/legal/dpia/page.tsx
  - apps/web/src/app/(admin)/admin/legal/dpia/actions.ts
  - apps/web/src/app/(admin)/admin/legal/dpia/_components/dpia-form.client.tsx
  - apps/web/src/app/(admin)/admin/legal/breaches/page.tsx
  - apps/web/src/app/(admin)/admin/legal/breaches/actions.ts
  - apps/web/src/app/(admin)/admin/legal/breaches/_components/breach-drawer.client.tsx
  - apps/web/src/app/(admin)/admin/legal/breaches/_components/breach-timer.client.tsx
  - apps/web/src/app/(admin)/admin/legal/requests/page.tsx
  - apps/web/src/app/(admin)/admin/legal/requests/actions.ts
  - apps/web/src/app/(admin)/admin/legal/dpo/page.tsx
  - apps/web/src/app/(admin)/admin/legal/dpo/actions.ts
  - apps/web/src/app/(admin)/admin/legal/_components/cgu-acceptance-banner.client.tsx
  - apps/web/src/app/api/admin/legal/registre/pdf/route.ts
  - apps/web/src/app/api/admin/legal/registre/pdf/_components/registre-pdf.tsx
autonomous: true
requirements: [DPA-01, DPA-02, DPA-03, DPA-05, DPA-06, DPA-07]

must_haves:
  truths:
    - "/admin/legal/registre liste les data_processing_register avec filtre + drawer édition (D-16)"
    - "/admin/legal/registre/pdf retourne un PDF (Content-Type: application/pdf) via @react-pdf/renderer (runtime nodejs)"
    - "/admin/legal/dpa liste les dpa_record"
    - "/admin/legal/dpia CRUD avec status workflow (brouillon → validee → archivee), Markdown long"
    - "/admin/legal/breaches CRUD avec compteur 72h temps réel (vert > 24h, orange 6-24h, rouge < 6h)"
    - "/admin/legal/requests CRUD demandes patients + workflow réponse + génération request_token JWT"
    - "/admin/legal/dpo édite organizations.dpo_*"
    - "Banner CGU si profiles.cgu_version_accepted < version courante"
    - "Toutes les Server Actions admin : guard rôle dirigeant + revalidate + audit log via trigger DB"
    - "Tests E2E registre-pdf + dpia + breach-countdown GREEN"
  artifacts:
    - path: "apps/web/src/app/api/admin/legal/registre/pdf/route.ts"
      provides: "Route PDF runtime nodejs"
      contains: "runtime = 'nodejs'"
      max_lines: 60
    - path: "apps/web/src/app/(admin)/admin/legal/breaches/_components/breach-timer.client.tsx"
      provides: "Compteur 72h temps réel + code couleur sémantique"
      max_lines: 80
    - path: "apps/web/src/app/(admin)/admin/legal/dpia/_components/dpia-form.client.tsx"
      provides: "Form DPIA Markdown long + status enum"
      max_lines: 150
  key_links:
    - from: "registre/pdf/route.ts"
      to: "@react-pdf/renderer"
      via: "renderToStream(<RegistrePdf />)"
      pattern: "renderToStream"
    - from: "breach-timer.client.tsx"
      to: "data_breach_incident"
      via: "supabase.channel realtime postgres_changes"
      pattern: "postgres_changes"
    - from: "requests/actions.ts"
      to: "signRequestToken"
      via: "import @tap/shared"
      pattern: "signRequestToken"
---

<objective>
**Wave 3B — UI admin /admin/legal/* (6 pages CRUD + route PDF + banner CGU)**. Toutes les pages admin pour les 5 ressources RGPD + édition DPO + compteur 72h temps réel + export PDF du registre + banner CGU acceptance. Parallèle au Plan 05 (file ownership disjoint : `/admin/legal/*` vs `/legal/*`).

Purpose: UI admin productrice de la conformité légale opérationnelle. Toutes les Server Actions consomment les helpers @tap/shared et les schémas zod legal de Wave 1B.
Output: 21 fichiers. Tests E2E registre-pdf + dpia + breach-countdown GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md
@CLAUDE.md
@apps/web/src/app/(app)/patients/page.tsx
@apps/web/src/app/(app)/patients/actions.ts
@apps/web/src/app/(app)/patients/_components/patient-drawer.client.tsx
@packages/shared/src/index.ts

<interfaces>
<!-- Wave 1B livré : -->
```typescript
import {
  signRequestToken,
  dataProcessingRegisterSchema, dpaRecordSchema, dpiaSchema, breachIncidentSchema,
  dataRequestSchema, dpoContactSchema,
  type DataProcessingRegisterInput, type DpiaInput, type BreachIncidentInput,
  type DpoContactInput,
} from '@tap/shared';
```

<!-- Wave 2 livré : layout (admin) check rôle dirigeant en SSR -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1 : Pages admin registre/dpa/dpo + Server Actions + drawers</name>
  <files>
    apps/web/src/app/(admin)/admin/legal/registre/page.tsx,
    apps/web/src/app/(admin)/admin/legal/registre/actions.ts,
    apps/web/src/app/(admin)/admin/legal/registre/_components/registre-drawer.client.tsx,
    apps/web/src/app/(admin)/admin/legal/dpa/page.tsx,
    apps/web/src/app/(admin)/admin/legal/dpa/actions.ts,
    apps/web/src/app/(admin)/admin/legal/dpa/_components/dpa-drawer.client.tsx,
    apps/web/src/app/(admin)/admin/legal/dpo/page.tsx,
    apps/web/src/app/(admin)/admin/legal/dpo/actions.ts
  </files>
  <read_first>
    - /home/user/TAP/apps/web/src/app/\(app\)/patients/page.tsx (intégral — pattern RSC + HydrationBoundary + Sheet drawer)
    - /home/user/TAP/apps/web/src/app/\(app\)/patients/actions.ts (intégral — pattern Server Action zod → auth → mutation → revalidate)
    - /home/user/TAP/apps/web/src/app/\(app\)/patients/_components/patient-drawer.client.tsx (pattern Sheet 400px + useQuery + Skeleton)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 401-485
  </read_first>
  <action>
    Pour chaque resource (registre, dpa, dpo) :

    **page.tsx** : pattern `patients/page.tsx` exact — header h1 + bouton « Nouveau »/« Éditer » + table → drawer édition.

    **actions.ts** : pattern `patients/actions.ts` exact :
    - `'use server'`
    - Import schema zod legal
    - Server Action `createXAction(_prev, formData)` : safeParse → user check → org → INSERT → revalidatePath → redirect
    - Server Action `updateXAction(id, _prev, formData)` (sauf registre — D-05 versioning par lignes : INSERT-only)

    **_components/X-drawer.client.tsx** : pattern `patient-drawer.client.tsx` Sheet 400px + useQuery + form sections.

    **Spécifique registre** :
    - Pas de policy UPDATE en DB (D-05) → Server Action `createDataProcessingRegisterAction` uniquement (chaque édition = nouvelle ligne avec `created_at` plus récent). Le drawer présente les versions historiques en lecture seule + bouton « Créer une nouvelle version pré-remplie ».
    - Bouton « Exporter PDF » dans la page → lien vers `/api/admin/legal/registre/pdf` (Task 6.3).

    **Spécifique dpa** :
    - Liste simple : pas de drawer édition complexe — drawer = formulaire `dpaRecordSchema` (subprocessor_name, dpa_version, signed_at, expires_at, notes).

    **Spécifique dpo** :
    - Page = formulaire édition `organizations.dpo_*` (single record per org).
    - Server Action `updateDpoContactAction` valide via `dpoContactSchema` puis `update organizations`.

    Tous les drawers : Sheet 400px (`w-[400px]`), Skeleton pendant loading, react-hook-form + zodResolver, messages erreurs FR sous champs.

    Code limits : chaque fichier ≤ 150 lignes (composant) ; éclater en sous-composants si nécessaire (`registre-form-sections.client.tsx`).
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck --filter @tap/web && grep -rc "dataProcessingRegisterSchema\|dpaRecordSchema\|dpoContactSchema" apps/web/src/app/\(admin\)/admin/legal/ | grep -v ':0$' | wc -l | grep -qE '^[3-9]$|^1[0-9]$'</automated>
  </verify>
  <acceptance_criteria>
    - 8 fichiers créés
    - Aucun `console.log` (`grep -rc 'console\.log' apps/web/src/app/\(admin\)/admin/legal/registre apps/web/src/app/\(admin\)/admin/legal/dpa apps/web/src/app/\(admin\)/admin/legal/dpo` = 0)
    - Aucun `any` non-justifié (`grep -rc ': any' apps/web/src/app/\(admin\)/admin/legal/` ≤ 0 sauf justifications)
    - registre/actions.ts exporte uniquement `createDataProcessingRegisterAction` (PAS d'`updateAction` — D-05)
    - dpo/actions.ts importe `dpoContactSchema` depuis `@tap/shared`
    - `wc -l` chaque fichier ≤ 150
    - `pnpm typecheck --filter @tap/web` exit 0
  </acceptance_criteria>
  <done>3 ressources admin opérationnelles (registre + dpa + dpo) avec drawers shadcn + Server Actions zod-validated.</done>
</task>

<task type="auto">
  <name>Task 6.2 : Pages admin dpia/breaches/requests + compteur 72h + banner CGU</name>
  <files>
    apps/web/src/app/(admin)/admin/legal/dpia/page.tsx,
    apps/web/src/app/(admin)/admin/legal/dpia/actions.ts,
    apps/web/src/app/(admin)/admin/legal/dpia/_components/dpia-form.client.tsx,
    apps/web/src/app/(admin)/admin/legal/breaches/page.tsx,
    apps/web/src/app/(admin)/admin/legal/breaches/actions.ts,
    apps/web/src/app/(admin)/admin/legal/breaches/_components/breach-drawer.client.tsx,
    apps/web/src/app/(admin)/admin/legal/breaches/_components/breach-timer.client.tsx,
    apps/web/src/app/(admin)/admin/legal/requests/page.tsx,
    apps/web/src/app/(admin)/admin/legal/requests/actions.ts,
    apps/web/src/app/(admin)/admin/legal/_components/cgu-acceptance-banner.client.tsx
  </files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 488-505 (breach timer)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Trigger 72h breach » (lignes 250-278)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md § specifics « CGU versioning » (lignes 302-310)
  </read_first>
  <action>
    **DPIA** :
    - `page.tsx` : liste DPIA par status (brouillon/validee/archivee) avec onglets ou filtre.
    - `dpia-form.client.tsx` (≤ 150 lignes) : formulaire long avec `<textarea>` Markdown pour `data_flow_diagram`, JSON editor simple pour `risks_identified` (textarea avec validation JSON.parse), date pickers pour `reviewed_at`/`next_review_at`, select status. Bouton « Enregistrer » → Server Action.
    - `actions.ts` : `createDpiaAction`, `updateDpiaAction` ; valide `dpiaSchema` ; INSERT/UPDATE avec `organization_id = current_organization_id()` (RLS auto).

    **Breaches** :
    - `page.tsx` : liste breaches avec compteur 72h en temps réel pour les `cnil_notification_required = true` et `cnil_notification_at IS NULL`.
    - `breach-timer.client.tsx` (≤ 80 lignes) :
      ```tsx
      'use client';
      import { useEffect, useState } from 'react';
      import { cn } from '@/lib/utils';

      export function BreachTimer({ detectedAt }: { detectedAt: string }) {
        const [now, setNow] = useState(() => Date.now());
        useEffect(() => {
          const id = setInterval(() => setNow(Date.now()), 1000);
          return () => clearInterval(id);
        }, []);
        const deadline = new Date(detectedAt).getTime() + 72 * 3600_000;
        const remainingMs = deadline - now;
        const remainingHours = remainingMs / 3600_000;
        const days = Math.floor(remainingHours / 24);
        const hours = Math.floor(remainingHours % 24);
        const minutes = Math.floor((remainingMs % 3600_000) / 60_000);
        const tone = remainingHours > 24 ? 'text-success'
                   : remainingHours > 6 ? 'text-warning'
                   : 'text-destructive';
        if (remainingMs < 0) {
          return <span className="font-mono tabular-nums text-destructive">Délai dépassé</span>;
        }
        return (
          <span className={cn('font-mono tabular-nums', tone)}>
            J-{days} • {hours}h {minutes.toString().padStart(2, '0')}m
          </span>
        );
      }
      ```
    - Realtime optionnel : `supabase.channel('breach-incidents').on('postgres_changes', ...)` pour fade-in subtil à l'ouverture/fermeture (CLAUDE.md § 1).
    - `breach-drawer.client.tsx` : form `breachIncidentSchema` (dates, severity, nature enums via Select shadcn).
    - `actions.ts` : `createBreachAction`, `updateBreachAction` (workflow status), `closeBreachAction` (set `closed_at`, `closed_by`).

    **Requests** :
    - `page.tsx` : liste demandes avec filtre status, date deadline, nom requérant.
    - `actions.ts` :
      - `createDataRequestAction` : la régulatrice initie une demande pour un patient → INSERT `patient_data_request` + génère `request_token` via `signRequestToken({ sub: requestId, org, type })` + UPDATE `request_token` en DB + envoie email simulé (V1 : log uniquement, intégration SMS Twilio en Phase 8).
      - `updateRequestStatusAction` : workflow régulatrice/dirigeant marque la demande comme `en_cours`/`satisfaite`/`rejetee` avec response.

    Snippet création de token :
    ```ts
    const { data: inserted } = await sb.from('patient_data_request').insert({
      organization_id, patient_id, request_type, requester_email,
      requested_at: new Date().toISOString(),
      // request_token et request_token_expires_at remplis ci-dessous
      request_token: 'placeholder', // remplacé après signature
      request_token_expires_at: new Date(Date.now() + 30 * 24 * 3600_000).toISOString(),
      status: 'recue',
    }).select('id, organization_id, request_type').single();
    if (!inserted) return { error: 'Création demande impossible.' };
    const token = await signRequestToken({
      sub: inserted.id,
      org: inserted.organization_id,
      type: inserted.request_type,
    });
    await sb.from('patient_data_request').update({ request_token: token }).eq('id', inserted.id);
    // TODO Phase 8 : envoyer email avec lien https://app/legal/request/${token}
    ```

    **Banner CGU** (`_components/cgu-acceptance-banner.client.tsx`, ≤ 60 lignes) :
    - Lit `profiles.cgu_version_accepted` côté layout admin (server) → passe en prop au banner client
    - Compare avec `CGU_CURRENT_VERSION` (constant tirée du frontmatter `cgu.md` Wave 3A — lecture build-time)
    - Si `accepted < current` → affiche banner non-bloquant « Nos CGU ont été mises à jour » + bouton « Lire et accepter »
    - Bouton → Server Action `acceptCguAction` qui INSERT `cgu_acceptance(profile_id, version, document_type='cgu')` + UPDATE `profiles.cgu_version_accepted`

    Tous les composants : aucun emoji, `tabular-nums` pour les chiffres temps, transitions 150ms ease-out, focus visible CLAUDE.md § 1.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck --filter @tap/web && grep -c 'tabular-nums' apps/web/src/app/\(admin\)/admin/legal/breaches/_components/breach-timer.client.tsx && grep -c 'signRequestToken' apps/web/src/app/\(admin\)/admin/legal/requests/actions.ts && pnpm exec playwright test tests/admin/dpia.spec.ts tests/admin/breach-countdown.spec.ts --reporter=line 2>&1 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - 10 fichiers créés
    - `grep -c "text-success\|text-warning\|text-destructive" apps/web/src/app/\(admin\)/admin/legal/breaches/_components/breach-timer.client.tsx` ≥ 3 (3 niveaux couleur sémantiques)
    - `grep -c "tabular-nums" apps/web/src/app/\(admin\)/admin/legal/breaches/_components/breach-timer.client.tsx` ≥ 1 (anti-shift digits)
    - `grep -c "signRequestToken" apps/web/src/app/\(admin\)/admin/legal/requests/actions.ts` ≥ 1
    - `grep -c "30 \* 24 \* 3600_000\|30 jours\|expirationTime.*30d" apps/web/src/app/\(admin\)/admin/legal/requests/actions.ts` ≥ 1 (D-10 expiry 30j)
    - `wc -l apps/web/src/app/\(admin\)/admin/legal/dpia/_components/dpia-form.client.tsx` ≤ 150
    - `pnpm exec playwright test tests/admin/dpia.spec.ts tests/admin/breach-countdown.spec.ts` GREEN
  </acceptance_criteria>
  <done>DPIA + breaches + requests + banner CGU opérationnels. Compteur 72h sémantique vert/orange/rouge.</done>
</task>

<task type="auto">
  <name>Task 6.3 : Route export PDF /api/admin/legal/registre/pdf (runtime nodejs + @react-pdf/renderer)</name>
  <files>
    apps/web/src/app/api/admin/legal/registre/pdf/route.ts,
    apps/web/src/app/api/admin/legal/registre/pdf/_components/registre-pdf.tsx
  </files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « PDF generation » (lignes 162-198)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 509-535
  </read_first>
  <action>
    **`registre-pdf.tsx`** (≤ 100 lignes) — composant PDF avec `@react-pdf/renderer` :
    ```tsx
    import { Document, Page, Text, View, StyleSheet } from '@react-pdf/renderer';

    type Entry = {
      purpose: string;
      legal_basis: string;
      data_categories: string[];
      data_subjects: string[];
      recipients: string[];
      retention_period_days: number;
      security_measures: string;
      international_transfer: boolean;
      created_at: string;
    };

    const styles = StyleSheet.create({
      page: { padding: 32, fontFamily: 'Helvetica' },
      h1: { fontSize: 18, marginBottom: 16, fontWeight: 700 },
      h2: { fontSize: 12, marginBottom: 8, fontWeight: 700 },
      label: { fontSize: 9, color: '#666' },
      value: { fontSize: 10, marginBottom: 6 },
      entry: { marginBottom: 16, paddingBottom: 12, borderBottom: '1pt solid #ccc' },
      meta: { fontSize: 8, color: '#999', marginTop: 24 },
    });

    export function RegistrePdf({ entries, organizationName, generatedAt }: {
      entries: Entry[]; organizationName: string; generatedAt: string;
    }) {
      return (
        <Document>
          <Page size="A4" style={styles.page}>
            <Text style={styles.h1}>Registre des activités de traitement</Text>
            <Text style={styles.value}>Société : {organizationName}</Text>
            <Text style={styles.value}>Généré le : {generatedAt}</Text>
            <Text style={styles.value}>Article 30 RGPD</Text>
            {entries.map((e, idx) => (
              <View key={idx} style={styles.entry}>
                <Text style={styles.h2}>{e.purpose}</Text>
                <Text style={styles.label}>Base légale</Text>
                <Text style={styles.value}>{e.legal_basis}</Text>
                <Text style={styles.label}>Catégories de données</Text>
                <Text style={styles.value}>{e.data_categories.join(', ')}</Text>
                <Text style={styles.label}>Personnes concernées</Text>
                <Text style={styles.value}>{e.data_subjects.join(', ')}</Text>
                <Text style={styles.label}>Destinataires</Text>
                <Text style={styles.value}>{e.recipients.join(', ')}</Text>
                <Text style={styles.label}>Durée de conservation</Text>
                <Text style={styles.value}>{e.retention_period_days} jours</Text>
                <Text style={styles.label}>Mesures de sécurité</Text>
                <Text style={styles.value}>{e.security_measures}</Text>
                <Text style={styles.label}>Transfert international</Text>
                <Text style={styles.value}>{e.international_transfer ? 'Oui' : 'Non'}</Text>
              </View>
            ))}
            <Text style={styles.meta}>Document généré automatiquement — TAP Régulation</Text>
          </Page>
        </Document>
      );
    }
    ```

    **`route.ts`** (≤ 60 lignes) :
    ```ts
    export const runtime = 'nodejs'; // OBLIGATOIRE — @react-pdf/renderer non-Edge
    export const dynamic = 'force-dynamic';

    import { renderToStream } from '@react-pdf/renderer';
    import { NextResponse } from 'next/server';
    import { createClient } from '@/lib/supabase/server';
    import { RegistrePdf } from './_components/registre-pdf';

    export async function GET() {
      const supabase = createClient();
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) {
        return NextResponse.json({ error: 'Non authentifié.' }, { status: 401 });
      }

      const { data: profile } = await supabase
        .from('profiles').select('role, organization_id').eq('id', user.id).single();
      if (profile?.role !== 'dirigeant') {
        return NextResponse.json({ error: 'Accès refusé.' }, { status: 403 });
      }

      const { data: org } = await supabase.from('organizations')
        .select('nom').eq('id', profile.organization_id).single();
      const { data: entries } = await supabase
        .from('data_processing_register').select('*')
        .order('created_at', { ascending: false });

      // Audit log explicite
      await supabase.from('audit_logs').insert({
        organization_id: profile.organization_id,
        actor_id: user.id, actor_role: 'dirigeant',
        action: 'legal.registre.exported_pdf',
        entity_type: 'data_processing_register',
        entity_id: null,
        metadata: { entries_count: entries?.length ?? 0 },
      });

      const stream = await renderToStream(
        <RegistrePdf
          entries={entries ?? []}
          organizationName={org?.nom ?? 'Société'}
          generatedAt={new Date().toLocaleString('fr-FR')}
        />,
      );
      return new Response(stream as unknown as ReadableStream, {
        headers: {
          'Content-Type': 'application/pdf',
          'Content-Disposition': `attachment; filename="registre-traitements-${new Date().toISOString().slice(0,10)}.pdf"`,
        },
      });
    }
    ```

    Tester runtime nodejs : `pnpm build` confirme la route est build en `λ` (lambda Node) pas en `ƒ` (Edge).

    **Note W-3 (DRY admin client)** : Cette route consomme `createClient` depuis `@/lib/supabase/server` (client authentifié de l'utilisateur dirigeant) — RLS s'applique automatiquement, pas de `service_role` ici. Aucun import direct `@supabase/supabase-js`. Le wrapper `@/lib/supabase/admin` (créé Plan 04) n'est PAS utilisé sur cette route car l'usage est strictement contextuel à l'utilisateur authentifié. Si une route admin future devait écrire avec `service_role` (ex. job cron), elle DOIT importer `createAdminClient` depuis `@/lib/supabase/admin` et JAMAIS recréer une factory.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm build --filter @tap/web 2>&1 | grep "api/admin/legal/registre/pdf" | grep -qE 'λ|nodejs' && pnpm exec playwright test tests/admin/registre-pdf.spec.ts --reporter=line 2>&1 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - 2 fichiers créés
    - `grep -c "runtime = 'nodejs'" apps/web/src/app/api/admin/legal/registre/pdf/route.ts` ≥ 1 (CRITIQUE — Pitfall RESEARCH)
    - `grep -c "renderToStream" apps/web/src/app/api/admin/legal/registre/pdf/route.ts` ≥ 1
    - `grep -c "Content-Type.*application/pdf" apps/web/src/app/api/admin/legal/registre/pdf/route.ts` ≥ 1
    - `grep -c "legal.registre.exported_pdf" apps/web/src/app/api/admin/legal/registre/pdf/route.ts` ≥ 1 (audit log)
    - `grep -c "role !== 'dirigeant'\|role.*'dirigeant'" apps/web/src/app/api/admin/legal/registre/pdf/route.ts` ≥ 1 (guard)
    - `pnpm build --filter @tap/web` génère la route en runtime nodejs (pas Edge)
    - `! grep -E "from '@supabase/supabase-js'" apps/web/src/app/api/admin/legal/registre/pdf/route.ts` (zéro import direct — la route utilise `@/lib/supabase/server` authentifié, pas de factory `service_role` recréée localement ; W-3)
    - `pnpm exec playwright test tests/admin/registre-pdf.spec.ts` GREEN
  </acceptance_criteria>
  <done>Route PDF runtime nodejs avec @react-pdf/renderer, guard dirigeant, audit log, Content-Type application/pdf.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser dirigeant → /admin/legal/* | Auth Supabase + RLS dirigeant en SSR + DB |
| Server Action → DB | Auth check user + RLS dirigeant policy applique |
| /api/admin/legal/registre/pdf → @react-pdf/renderer | Runtime Node (pas Edge) |
| breach-timer realtime → DB | Read-only subscription scopée à org via RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-23 (T3 HIGH) | Elevation of Privilege | régulateur accède /admin/legal/* | mitigate | Layout (admin)/layout.tsx (Plan 04) check `profile.role !== 'dirigeant'` SSR + RLS DB D-16 INSERT policy `has_role('dirigeant')`. Double rideau. |
| T-1.5-24 | Information Disclosure | route PDF leak data autre tenant | mitigate | Query `data_processing_register` consomme RLS authenticated → filtre auto par org. Re-vérification `profile.organization_id` côté route. |
| T-1.5-25 (T1 HIGH) | Tampering | request_token JWT signé prévisible | mitigate | `signRequestToken` consomme `APP_LEGAL_TOKEN_SECRET` (32 bytes random). Claims minimaux `{sub, org, type}`. Test type-level Wave 1B. |
| T-1.5-26 | Repudiation | export PDF sans audit | mitigate | Audit log explicite `legal.registre.exported_pdf` AVANT renderToStream. |
| T-1.5-27 | Denial of Service | spam /api/admin/legal/registre/pdf | accept | Limité aux dirigeants (≤ 5 par société). Cold-start @react-pdf monitored Sentry. |
| T-1.5-28 (T6 MEDIUM) | Tampering | banner CGU dark pattern | mitigate | Banner non-bloquant (D-13 + Open Question 3). Bouton « Lire et accepter » ouvre la page CGU complète, pas de auto-accept. |
</threat_model>

<verification>
- `pnpm typecheck --filter @tap/web` exit 0
- `pnpm build --filter @tap/web` génère 6 routes admin/legal/* + 1 route api/admin/legal/registre/pdf en runtime nodejs
- `pnpm exec playwright test tests/admin/registre-pdf.spec.ts tests/admin/dpia.spec.ts tests/admin/breach-countdown.spec.ts` : 3/3 GREEN
- Manuel : login dirigeant → /admin/legal/registre → bouton Exporter PDF → fichier .pdf téléchargé
- Manuel : login régulateur → /admin/legal/registre → redirect /
- Manuel : créer breach `detected_at = now() - 60h` → compteur affiche « 12h » avec text-warning
</verification>

<success_criteria>
- 21 fichiers créés
- 3 tests E2E admin GREEN
- Aucun emoji, code limits respectés
- Toutes les Server Actions appliquent zod → guard dirigeant → mutation → audit (via trigger DB) → revalidate
- Commit français : `feat(1.5): UI admin /admin/legal/* + export PDF + compteur 72h temps réel`
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-dpa-rgpd-compliance/1.5-06-SUMMARY.md`.
</output>

## Revision Log

- **2026-05-06 — W-3 fix (iteration 2/3)** : Wrapper `apps/web/src/lib/supabase/admin.ts` créé en Plan 04. Plans 05 et 06 importent depuis `@/lib/supabase/admin` au lieu de `@supabase/supabase-js` direct. Élimine 3 factories dupliquées et respecte ADR-001 (apps consomme `@tap/*` + ses propres wrappers, pas `@supabase/*` direct). Plan 06 — la route `/api/admin/legal/registre/pdf` utilise `createClient` authentifié depuis `@/lib/supabase/server` (RLS s'applique), donc aucun usage de `service_role` ; pas d'import direct `@supabase/supabase-js`. Note explicite ajoutée dans l'action de Task 6.3.
