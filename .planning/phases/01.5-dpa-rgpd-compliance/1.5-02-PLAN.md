---
phase: 01.5-dpa-rgpd-compliance
plan: 02
type: execute
wave: 1
depends_on: ["1.5-01"]
files_modified:
  - supabase/migrations/20260508000001_legal_compliance.sql
  - supabase/migrations/20260508000002_legal_additional_tables.sql
  - supabase/migrations/20260508000003_breach_72h_alert.sql
  - supabase/migrations/20260508000004_organizations_dpo_fields.sql
  - supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql
  - packages/database/src/types.gen.ts
autonomous: true
requirements: [DPA-01, DPA-02, DPA-03, DPA-04, DPA-05, DPA-06, DPA-08]

must_haves:
  truths:
    - "Les 5 tables RGPD (data_processing_register, dpa_record, dpia_record, data_breach_incident, patient_data_request) existent avec RLS forcée + organization_id NOT NULL"
    - "Les 3 tables additionnelles (cgu_acceptance, cookie_consent_log, legal_request_attempts) existent"
    - "organizations a 5 nouveaux champs DPO + profiles a 2 champs CGU"
    - "Les 5 audit triggers filtrent les colonnes sensibles (request_token, requester_proof_of_identity_url, cnil_notification_reference)"
    - "pg_cron job 'breach-72h-watchdog' planifié horaire"
    - "RPC rgpd_anonymize_patient(uuid, uuid, text) existe et est SECURITY DEFINER"
    - "Tous les pgTAP de Plan 01 deviennent GREEN"
  artifacts:
    - path: "supabase/migrations/20260508000001_legal_compliance.sql"
      provides: "5 tables RGPD + RLS + 5 audit triggers + indexes"
      min_lines: 250
    - path: "supabase/migrations/20260508000002_legal_additional_tables.sql"
      provides: "cgu_acceptance, cookie_consent_log, legal_request_attempts + RLS"
      min_lines: 60
    - path: "supabase/migrations/20260508000003_breach_72h_alert.sql"
      provides: "pg_cron extension + check_breach_deadlines() + schedule"
      min_lines: 40
    - path: "supabase/migrations/20260508000004_organizations_dpo_fields.sql"
      provides: "5 champs DPO sur organizations + 2 champs CGU sur profiles"
      min_lines: 15
    - path: "supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql"
      provides: "RPC rgpd_anonymize_patient SECURITY DEFINER + advisory lock"
      min_lines: 50
    - path: "packages/database/src/types.gen.ts"
      provides: "Types regenerés"
      contains: "data_processing_register"
  key_links:
    - from: "audit triggers patient_data_request"
      to: "audit_logs.metadata"
      via: "to_jsonb(NEW) - 'request_token' - 'requester_proof_of_identity_url'"
      pattern: "to_jsonb\\([^)]+\\)\\s*-\\s*'request_token'\\s*-\\s*'requester_proof_of_identity_url'"
    - from: "pg_cron job"
      to: "check_breach_deadlines()"
      via: "cron.schedule('breach-72h-watchdog', '0 * * * *', ...)"
      pattern: "cron\\.schedule\\('breach-72h-watchdog'"
    - from: "rgpd_anonymize_patient RPC"
      to: "rides table"
      via: "ne supprime PAS rides (UPDATE patients only)"
      pattern: "update public\\.patients"
---

<objective>
**Wave 1A — Migration RGPD complète**. Créer les 5 tables verrouillées (D-05..D-09), 3 tables additionnelles (RESEARCH §A2), 5 champs DPO sur `organizations` + 2 CGU sur `profiles`, RLS forcée + audit triggers reproduisant fidèlement le pattern Phase 1, watchdog 72h via pg_cron, et RPC `rgpd_anonymize_patient` sécurisée. Tests pgTAP de Plan 01 → GREEN après ce plan.

Purpose: Schéma RGPD posé en 1 wave atomique pour permettre le développement parallèle des helpers (Plan 03) et des UI (Wave 2-3).
Output: 5 fichiers de migration + types.gen.ts régénérés. Aucune modification UI/code applicatif dans ce plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md
@CLAUDE.md
@supabase/migrations/20260507000001_patients.sql
@supabase/migrations/20260506000001_foundations.sql
@supabase/migrations/20260506000002_rls_foundations.sql

<interfaces>
<!-- Helpers RLS Postgres existants à réutiliser (migration 20260506000001_foundations) : -->
```sql
public.current_organization_id() returns uuid;       -- claim JWT
public.current_user_role() returns user_role;        -- enum 'dirigeant' | 'regulateur' | 'chauffeur'
public.has_role(role public.user_role) returns boolean;
public.audit_logs (id, organization_id, actor_id, actor_role, action, entity_type, entity_id, metadata, created_at);
```

<!-- Pattern audit trigger existant (lignes 199-218 patients.sql) à dupliquer fidèlement. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1 : Migration legal_compliance — 5 tables RGPD verrouillées + RLS + audit triggers</name>
  <files>supabase/migrations/20260508000001_legal_compliance.sql</files>
  <read_first>
    - /home/user/TAP/supabase/migrations/20260507000001_patients.sql (intégral — pattern table+RLS+trigger)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md § decisions D-05..D-09 (schémas SQL verbatim)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 35-114 (pattern dupliqué)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § Pitfall 2 (filtre audit colonnes sensibles)
  </read_first>
  <behavior>
    - Test 1 : RLS activée + forcée sur les 5 tables (pgTAP `relrowsecurity` + `relforcerowsecurity`)
    - Test 2 : régulateur Alpha INSERT dans `data_processing_register` → 42501 (D-16 dirigeant only)
    - Test 3 : dirigeant Alpha INSERT lives_ok dans les 5 tables
    - Test 4 : dirigeant Bravo SELECT n'a aucune ligne Alpha (isolation tenant)
    - Test 5 : `data_processing_register` n'a pas de policy UPDATE (D-05 versioning par lignes)
    - Test 6 : INSERT dans `patient_data_request` avec `request_token = 'jwt-eyXXX'` → audit_logs.metadata->'new' ne contient PAS `request_token` ni `requester_proof_of_identity_url`
    - Test 7 : audit trigger `data_breach_incident` exclut `cnil_notification_reference` (numéro ARS-CNIL sensible)
  </behavior>
  <action>
    Créer la migration en 5 sections, en dupliquant FIDÈLEMENT le pattern de `20260507000001_patients.sql`.

    **Section 1 — Tables (verbatim CONTEXT.md D-05..D-09)** :
    - `data_processing_register` (D-05) — schéma exact incluant `legal_basis` CHECK, `data_categories text[]`, `retention_period_days int`, etc.
    - `dpa_record` (D-06)
    - `dpia_record` (D-07) — `risks_identified jsonb default '[]'::jsonb`, `mitigations jsonb`, `status text check`
    - `data_breach_incident` (D-08)
    - `patient_data_request` (D-09) — `request_type` CHECK enum 6 valeurs, `deadline_at` calculé en trigger BEFORE INSERT (`NEW.deadline_at = NEW.requested_at + interval '30 days'` si NULL)

    Tous : `organization_id uuid not null references public.organizations(id) on delete cascade`, `created_at`, `updated_at` (sauf D-05 versioning par lignes — pas d'updated_at), `created_by uuid references auth.users(id)`.

    **Section 2 — Indexes** (RESEARCH ligne 382-388) :
    ```sql
    create index data_processing_register_org_idx on public.data_processing_register (organization_id);
    create index dpa_record_org_idx on public.dpa_record (organization_id);
    create index dpia_record_org_status_idx on public.dpia_record (organization_id, status);
    create index data_breach_incident_org_open_idx on public.data_breach_incident (organization_id) where closed_at is null;
    create unique index patient_data_request_token_idx on public.patient_data_request (request_token);
    create index patient_data_request_org_status_idx on public.patient_data_request (organization_id, status, deadline_at);
    ```

    **Section 3 — RLS forcée + policies** (pattern patients.sql lignes 114-138 dupliqué × 5) :
    - `enable row level security` + `force row level security`
    - Policy SELECT : `organization_id = public.current_organization_id()`
    - Policy INSERT : `with check (organization_id = public.current_organization_id() and public.has_role('dirigeant'::public.user_role))` — D-16 dirigeant only
    - Policy UPDATE pour `dpa_record`, `dpia_record`, `data_breach_incident`, `patient_data_request` : same org + dirigeant. **PAS de policy UPDATE pour `data_processing_register`** (D-05 versioning par lignes).
    - Pas de policy DELETE (archivage logique).

    **Section 4 — Audit triggers** (pattern lignes 199-222 patients.sql dupliqué × 5) :
    Pour chaque table, fonction `<table>_audit_trigger()` SECURITY DEFINER set search_path = public.

    **CRITIQUE — Filtres colonnes sensibles** :
    - `patient_data_request` : `to_jsonb(old/new) - 'request_token' - 'requester_proof_of_identity_url'`
    - `data_breach_incident` : `to_jsonb(old/new) - 'cnil_notification_reference'`
    - 3 autres tables : pas de filtre nécessaire (pas de secret).

    Action name : `'data_processing_register.' || lower(tg_op)`, etc. Entity type : `'data_processing_register'`, etc.

    **Section 5 — Trigger BEFORE INSERT pour `patient_data_request.deadline_at`** :
    ```sql
    create or replace function public.patient_data_request_set_deadline()
    returns trigger language plpgsql as $$
    begin
      if new.deadline_at is null then
        new.deadline_at := new.requested_at + interval '30 days';
      end if;
      return new;
    end; $$;
    create trigger patient_data_request_set_deadline_trigger
      before insert on public.patient_data_request
      for each row execute function public.patient_data_request_set_deadline();
    ```

    **Section 6 — Revoke/Grant** (pattern patients.sql lignes 273-278) :
    ```sql
    revoke all on public.data_processing_register from anon;
    grant select, insert on public.data_processing_register to authenticated;
    -- (pas d'UPDATE pour data_processing_register — versioning)
    revoke all on public.dpa_record from anon;
    grant select, insert, update on public.dpa_record to authenticated;
    -- ... (3 autres)
    ```

    En tête : commentaire FR « Migration Phase 1.5 — Conformité RGPD : registre traitements (art. 30), DPA (art. 28), DPIA (art. 35), violations (art. 33), demandes droits (art. 15-21). Verrouille D-05..D-09. ».

    Code limit 300 lignes (CLAUDE.md § 11) — fichier proche de la limite, accepter ; ne pas découper artificiellement.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm db:reset && pnpm db:test --file data_processing_register_rls.sql --file dpa_record_rls.sql --file dpia_record_rls.sql --file data_breach_incident_rls.sql --file patient_data_request_rls.sql 2>&1 | grep -E '(ok|not ok)' | grep -c '^not ok' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'create table public\.\(data_processing_register\|dpa_record\|dpia_record\|data_breach_incident\|patient_data_request\)' supabase/migrations/20260508000001_legal_compliance.sql` ≥ 5
    - `grep -v '^--' supabase/migrations/20260508000001_legal_compliance.sql | grep -c 'force row level security'` ≥ 5
    - `grep -c "to_jsonb(.*) - 'request_token' - 'requester_proof_of_identity_url'" supabase/migrations/20260508000001_legal_compliance.sql` ≥ 1
    - `grep -c "to_jsonb(.*) - 'cnil_notification_reference'" supabase/migrations/20260508000001_legal_compliance.sql` ≥ 1
    - `grep -v '^--' supabase/migrations/20260508000001_legal_compliance.sql | grep -E "create policy.*data_processing_register.*for update" | wc -l | grep -q '^0$'` (PAS de policy UPDATE D-05)
    - 5 pgTAP RLS GREEN après `pnpm db:reset && pnpm db:test`
  </acceptance_criteria>
  <done>5 tables RGPD live avec RLS + audit triggers filtrants ; 5 tests pgTAP RLS GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2 : Migrations additionnelles — tables CGU/cookies/rate-limit + champs DPO/CGU + watchdog 72h + RPC anonymize</name>
  <files>
    supabase/migrations/20260508000002_legal_additional_tables.sql,
    supabase/migrations/20260508000003_breach_72h_alert.sql,
    supabase/migrations/20260508000004_organizations_dpo_fields.sql,
    supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql,
    packages/database/src/types.gen.ts
  </files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Data Schema Recommendations » (lignes 374-446) + Pitfall 5/6 (pg_cron, rate-limit purge)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § « breach_72h_alert.sql » (lignes 118-148) et « rgpd_anonymize_patient » (RESEARCH lignes 280-329)
    - /home/user/TAP/supabase/migrations/20260507000001_patients.sql (lignes complètes — colonnes patients à anonymiser)
  </read_first>
  <behavior>
    - Test 1 : `cgu_acceptance(profile_id, version, accepted_at)` créée avec RLS forcée
    - Test 2 : `cookie_consent_log` créée SANS `organization_id` (RLS = service_role insert only)
    - Test 3 : `legal_request_attempts(token_hash, attempted_at, success)` + index `(token_hash, attempted_at)`
    - Test 4 : `organizations.dpo_contact_email`, `dpo_contact_phone`, `dpo_contact_address`, `dpo_external boolean default false`, `dpo_updated_at` ajoutés
    - Test 5 : `profiles.cgu_version_accepted text`, `cgu_accepted_at timestamptz` ajoutés
    - Test 6 : `pg_cron` extension activée + job `breach-72h-watchdog` planifié `'0 * * * *'`
    - Test 7 : `check_breach_deadlines()` insère `audit_logs` action `breach.deadline_warning` avec `metadata.hours_remaining` quand breach détecté > 66h
    - Test 8 : `rgpd_anonymize_patient(uuid, uuid, text)` UPDATE patients (NIR null, identité null, archive=true), insert audit `patient.anonymized`, ne touche PAS `rides`
    - Test 9 : tests pgTAP `check_breach_deadlines.sql` + `rgpd_anonymize_patient.sql` GREEN
  </behavior>
  <action>
    **Migration `20260508000002_legal_additional_tables.sql`** :
    ```sql
    -- cgu_acceptance : trace acceptance CGU/CGV par utilisateur (DPA-07)
    create table public.cgu_acceptance (
      id uuid primary key default extensions.uuid_generate_v4(),
      profile_id uuid not null references public.profiles(id) on delete cascade,
      version text not null,                     -- ex: '2026-05-07' depuis frontmatter MDX
      document_type text not null check (document_type in ('cgu', 'cgv', 'confidentialite')),
      accepted_at timestamptz not null default now(),
      ip_address inet,
      user_agent text
    );
    create index cgu_acceptance_profile_idx on public.cgu_acceptance (profile_id);
    alter table public.cgu_acceptance enable row level security;
    alter table public.cgu_acceptance force row level security;
    create policy cgu_acceptance_select_own on public.cgu_acceptance
      for select to authenticated using (profile_id = auth.uid());
    create policy cgu_acceptance_insert_own on public.cgu_acceptance
      for insert to authenticated with check (profile_id = auth.uid());
    grant select, insert on public.cgu_acceptance to authenticated;

    -- cookie_consent_log : pas d'organization_id (visiteur anon possible)
    create table public.cookie_consent_log (
      id uuid primary key default extensions.uuid_generate_v4(),
      session_token_hash text not null,
      choices jsonb not null,
      user_agent_hash text,
      created_at timestamptz not null default now()
    );
    create index cookie_consent_log_created_at_idx on public.cookie_consent_log (created_at desc);
    alter table public.cookie_consent_log enable row level security;
    alter table public.cookie_consent_log force row level security;
    -- service_role insert only (Server Action)
    create policy cookie_consent_log_insert_service on public.cookie_consent_log
      for insert to service_role with check (true);
    create policy cookie_consent_log_select_service on public.cookie_consent_log
      for select to service_role using (true);
    revoke all on public.cookie_consent_log from authenticated, anon;

    -- legal_request_attempts : rate limit 5/h portail patient
    create table public.legal_request_attempts (
      id uuid primary key default extensions.uuid_generate_v4(),
      token_hash text not null,
      attempted_at timestamptz not null default now(),
      success boolean not null default false
    );
    create index legal_request_attempts_token_time_idx
      on public.legal_request_attempts (token_hash, attempted_at desc);
    alter table public.legal_request_attempts enable row level security;
    alter table public.legal_request_attempts force row level security;
    create policy legal_request_attempts_service on public.legal_request_attempts
      for all to service_role using (true) with check (true);
    revoke all on public.legal_request_attempts from authenticated, anon;
    ```

    **Migration `20260508000003_breach_72h_alert.sql`** (RESEARCH ligne 268-276 + PATTERNS ligne 124-148) :
    ```sql
    create extension if not exists pg_cron with schema cron;

    create or replace function public.check_breach_deadlines()
    returns void language plpgsql security definer set search_path = public as $$
    declare r record;
    begin
      for r in select id, organization_id, detected_at from public.data_breach_incident
        where cnil_notification_required = true
          and cnil_notification_at is null
          and closed_at is null
          and now() > detected_at + interval '66 hours'
      loop
        insert into public.audit_logs (organization_id, actor_id, actor_role, action,
                                       entity_type, entity_id, metadata)
        values (r.organization_id, null, null, 'breach.deadline_warning',
          'data_breach_incident', r.id,
          jsonb_build_object(
            'hours_remaining',
            extract(epoch from (r.detected_at + interval '72 hours' - now()))/3600
          ));
      end loop;
    end; $$;

    -- Purge rate limit > 7 jours
    create or replace function public.purge_legal_request_attempts()
    returns void language plpgsql security definer set search_path = public as $$
    begin
      delete from public.legal_request_attempts
      where attempted_at < now() - interval '7 days';
    end; $$;

    select cron.schedule('breach-72h-watchdog', '0 * * * *',
      $$ select public.check_breach_deadlines(); $$);
    select cron.schedule('legal-request-attempts-purge', '0 3 * * *',
      $$ select public.purge_legal_request_attempts(); $$);
    ```

    **Migration `20260508000004_organizations_dpo_fields.sql`** (D-15 + RESEARCH ligne 432-446) :
    ```sql
    alter table public.organizations
      add column if not exists dpo_contact_email text,
      add column if not exists dpo_contact_phone text,
      add column if not exists dpo_contact_address text,
      add column if not exists dpo_external boolean not null default false,
      add column if not exists dpo_updated_at timestamptz;

    alter table public.profiles
      add column if not exists cgu_version_accepted text,
      add column if not exists cgu_accepted_at timestamptz;
    ```

    **Migration `20260508000005_rgpd_anonymize_rpc.sql`** (RESEARCH lignes 280-329, lignes patients à anonymiser) :
    ```sql
    create or replace function public.rgpd_anonymize_patient(
      p_patient_id uuid,
      p_request_id uuid,
      p_salt text
    ) returns void
    language plpgsql security definer set search_path = public, extensions as $$
    declare
      v_org_id uuid;
      v_old_search_hash text;
      v_lock_key bigint;
    begin
      -- Advisory lock : empêche anonymisations parallèles du même patient
      v_lock_key := ('x' || substr(md5(p_patient_id::text), 1, 16))::bit(64)::bigint;
      perform pg_advisory_xact_lock(v_lock_key);

      select organization_id, nir_search_hash into v_org_id, v_old_search_hash
        from public.patients where id = p_patient_id for update;
      if v_org_id is null then
        raise exception 'Patient introuvable : %', p_patient_id;
      end if;

      -- Anonymisation : NULL identifiants directs, hash NIR rotated
      update public.patients set
        prenom = null,
        nom = null,
        date_naissance = null,
        telephone = null,
        telephone_normalized = null,
        adresse_ligne_1 = null,
        adresse_ligne_2 = null,
        ville = null,
        contact_urgence_nom = null,
        contact_urgence_telephone = null,
        nir_encrypted = null,
        nir_last4 = null,
        nir_search_hash = encode(extensions.digest(coalesce(v_old_search_hash, '') || p_salt, 'sha256'), 'hex'),
        consentement_sms_actif = false,
        consentement_sms_horodatage = null,
        archive = true,
        archive_at = now(),
        archive_reason = 'rgpd.art17.anonymisation',
        updated_at = now()
      where id = p_patient_id;

      -- Suppression PII opérationnelles (pas de durée légale)
      delete from public.patient_constraint where patient_id = p_patient_id;
      delete from public.patient_operational_note where patient_id = p_patient_id;

      -- Audit log
      insert into public.audit_logs (organization_id, actor_id, actor_role, action,
                                     entity_type, entity_id, metadata)
      values (v_org_id, auth.uid(), public.current_user_role(), 'patient.anonymized',
        'patient', p_patient_id,
        jsonb_build_object(
          'request_id', p_request_id,
          'retention_basis', 'css_l114_19',
          'anonymized_at', now()
        ));
    end; $$;

    revoke execute on function public.rgpd_anonymize_patient(uuid, uuid, text) from public, anon;
    grant execute on function public.rgpd_anonymize_patient(uuid, uuid, text) to authenticated;
    ```

    **RPC `nir_match_patient_for_legal_request`** (FIX B-1 itération 2/3 — requise par Plan 05 task 5.2 `verifyIdentityAction`) :
    Ajouter dans `supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql` (même fichier, en fin) :
    ```sql
    -- RPC nir_match_patient_for_legal_request : vérifie identité d'un demandeur RGPD
    -- Compare le NIR fourni (déchiffré côté Edge Function NIR puis hashé) au patient cible.
    -- Retourne patient_id si match exact (NIR + nom + date naissance), sinon NULL.
    -- SECURITY DEFINER pour traverser RLS (le portail patient n'a pas de session Supabase).

    create or replace function public.nir_match_patient_for_legal_request(
      p_request_id uuid,
      p_nir_search_hash bytea,
      p_nom text,
      p_date_naissance date
    )
    returns uuid
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_patient_id uuid;
      v_org_id uuid;
    begin
      -- Récupérer organization_id de la requête (sans exposer plus)
      select organization_id into v_org_id
      from public.patient_data_request
      where id = p_request_id;

      if v_org_id is null then
        return null;
      end if;

      -- Match exact NIR + nom (insensible casse + accents) + date naissance
      select id into v_patient_id
      from public.patients
      where organization_id = v_org_id
        and nir_search_hash = p_nir_search_hash
        and lower(unaccent(nom)) = lower(unaccent(p_nom))
        and date_naissance = p_date_naissance
        and archive = false
      limit 1;

      return v_patient_id;
    end;
    $$;

    revoke all on function public.nir_match_patient_for_legal_request(uuid, bytea, text, date) from public;
    grant execute on function public.nir_match_patient_for_legal_request(uuid, bytea, text, date) to authenticated, service_role;
    ```

    **Régénérer types** :
    ```bash
    pnpm --filter @tap/database db:types
    ```
    (commande standard du repo — vérifier dans `packages/database/package.json`)

    Tous commentaires français.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm db:reset && pnpm db:test --file check_breach_deadlines.sql --file rgpd_anonymize_patient.sql 2>&1 | grep -c '^not ok' | grep -q '^0$' && grep -c 'data_processing_register' packages/database/src/types.gen.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4 fichiers .sql créés
    - `grep -c 'cron.schedule' supabase/migrations/20260508000003_breach_72h_alert.sql` ≥ 2 (watchdog + purge)
    - `grep -c 'breach-72h-watchdog' supabase/migrations/20260508000003_breach_72h_alert.sql` ≥ 1
    - `grep -c 'rgpd_anonymize_patient' supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql` ≥ 1
    - `grep -c 'pg_advisory_xact_lock' supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql` ≥ 1
    - `grep -v '^\s*--' supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql | grep -c 'delete from public\.patients\|delete from public\.rides'` = 0 (jamais DELETE patients ni rides — R5 critique)
    - `grep -c 'archive_reason = .rgpd.art17.anonymisation.' supabase/migrations/20260508000005_rgpd_anonymize_rpc.sql` ≥ 1
    - `grep -c 'data_processing_register' packages/database/src/types.gen.ts` ≥ 1 (types regen)
    - `pnpm db:test` : `check_breach_deadlines.sql` GREEN ET `rgpd_anonymize_patient.sql` GREEN
    - `psql -c "\df nir_match_patient_for_legal_request" | grep -c "nir_match_patient_for_legal_request"` ≥ 1 (FIX B-1 — RPC requise par Plan 05)
    - pgTAP test : `nir_match_patient_for_legal_request` appel avec données correctes retourne UUID, avec NIR incorrect retourne NULL
  </acceptance_criteria>
  <done>4 migrations + types regen ; 7 tests pgTAP GREEN après `pnpm db:reset && pnpm db:test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres SECURITY DEFINER → caller | RPC `rgpd_anonymize_patient` exécute en privilèges owner ; restreint via GRANT authenticated + advisory lock |
| pg_cron → audit_logs | Job s'exécute en service_role implicite |
| Audit triggers → audit_logs.metadata | NEW/OLD JSONB potentiellement avec PII/secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-04 (T2 HIGH) | Information Disclosure | audit triggers `patient_data_request` | mitigate | Filtre obligatoire `to_jsonb(...) - 'request_token' - 'requester_proof_of_identity_url'` reproduit verbatim Phase 1. Test pgTAP dédié assert no leak. |
| T-1.5-05 (T4 MEDIUM) | Tampering | RPC anonymize | mitigate | UPDATE only, JAMAIS DELETE patients/rides. Advisory lock empêche double-call concurrent. Test pgTAP assert rides preservation. |
| T-1.5-06 (T5 MEDIUM) | Repudiation | pg_cron not running prod | mitigate | Migration explicite `create extension if not exists pg_cron` + test pgTAP assert `select count(*) from cron.job where jobname = 'breach-72h-watchdog'` ≥ 1. |
| T-1.5-07 | Elevation | RPC `rgpd_anonymize_patient` accessible role anonyme | mitigate | `revoke execute ... from public, anon;` + `grant ... to authenticated;` + RLS sur patients (dirigeant only INSERT, pas DELETE). |
| T-1.5-08 | Information Disclosure | rate limit table croît sans purge | mitigate | pg_cron `legal-request-attempts-purge` quotidien (Pitfall 6 RESEARCH). |
</threat_model>

<verification>
- `pnpm db:reset` exit 0 (toutes migrations appliquent)
- `pnpm db:test` : 7 tests pgTAP de Plan 01 GREEN
- `psql -c "select jobname from cron.job where jobname like '%breach%' or jobname like '%legal%'"` retourne 2 lignes
- `psql -c "select pg_get_functiondef('public.rgpd_anonymize_patient'::regproc)"` ne contient PAS `DELETE FROM public.patients`
- `pnpm typecheck --filter @tap/database` : types.gen.ts compile
</verification>

<success_criteria>
- 5 migrations créées et appliquées sans erreur
- 7 tests pgTAP de Plan 01 deviennent GREEN
- Types Supabase régénérés et exportent les 8 nouvelles tables
- Aucun code applicatif modifié (strictement DB)
- Commit français : `feat(1.5): migration RGPD — 5 tables + watchdog 72h + RPC anonymize`
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-dpa-rgpd-compliance/1.5-02-SUMMARY.md`.
</output>

## Revision Log

- B-1 fix (iteration 2/3): RPC `nir_match_patient_for_legal_request` ajoutée comme function SECURITY DEFINER. Match exact NIR (via Edge Function hash) + nom unaccent + date naissance dans la même org. Sans cette RPC, Plan 05 verifyIdentityAction throw → DPA-04 non livré.
- Date: 2026-05-06
