---
phase: 01.5-dpa-rgpd-compliance
plan: 07
type: execute
wave: 4
depends_on: ["1.5-05", "1.5-06"]
files_modified:
  - .env.local.example
  - apps/web/playwright.config.ts
autonomous: true
requirements: [DPA-01, DPA-02, DPA-03, DPA-04, DPA-05, DPA-06, DPA-07, DPA-08]

must_haves:
  truths:
    - "pnpm db:reset applique les 5 nouvelles migrations (legal_compliance + additional + breach_72h_alert + dpo_fields + anonymize_rpc) sans erreur"
    - "pnpm db:test : 7 tests pgTAP RGPD GREEN (5 RLS + watchdog + anonymize)"
    - "pnpm test --filter @tap/shared : 14+ tests Vitest helpers GREEN"
    - "pnpm exec playwright test tests/(admin|public|portail) : 6 specs E2E GREEN"
    - "Toutes les variables d'environnement requises documentées dans .env.local.example"
    - "Aucune fuite : grep audit_logs ne contient JAMAIS request_token, requester_proof_of_identity_url, ni nir_encrypted"
    - "/legal/cgu accessible 200 sans auth ; /admin/legal/registre nécessite rôle dirigeant"
  artifacts:
    - path: ".env.local.example"
      provides: "Documentation des nouvelles env vars (APP_LEGAL_TOKEN_SECRET, APP_ANONYMIZATION_SALT)"
      contains: "APP_LEGAL_TOKEN_SECRET"
    - path: "apps/web/playwright.config.ts"
      provides: "Config Playwright incluant les 6 nouveaux specs"
      contains: "tests"
  key_links:
    - from: ".env.local.example"
      to: "APP_LEGAL_TOKEN_SECRET (32 bytes)"
      via: "openssl rand -base64 32"
      pattern: "APP_LEGAL_TOKEN_SECRET"
    - from: "phase verification"
      to: "all green"
      via: "pnpm db:reset && pnpm db:test && pnpm test && pnpm exec playwright test"
      pattern: "passed"

user_setup:
  - service: env-vars
    why: "Secrets RGPD à générer localement et en prod (Vercel/Supabase)"
    env_vars:
      - name: APP_LEGAL_TOKEN_SECRET
        source: "Générer via 'openssl rand -base64 32' (32 bytes minimum). Stocker dans .env.local (dev) et dans Vercel project env (prod). Distinct de APP_NIR_ENCRYPTION_KEY."
      - name: APP_ANONYMIZATION_SALT
        source: "Générer via 'openssl rand -base64 32'. Stocker dans .env.local (dev) et Supabase secrets (prod) — utilisé par RPC rgpd_anonymize_patient. ROTATION JAMAIS sans plan migration (les hash NIR anonymisés deviendraient incohérents)."
---

<objective>
**Wave 4 — Schema push BLOCKING + verification gate complète**. Ce plan ferme la phase :
1. Documente les 2 nouvelles env vars (`APP_LEGAL_TOKEN_SECRET`, `APP_ANONYMIZATION_SALT`)
2. **[BLOCKING]** Exécute `pnpm db:reset` (local) qui applique TOUTES les migrations Plan 02 dans l'ordre
3. Exécute la suite de tests complète : pgTAP + Vitest + Playwright
4. Vérifie absences de fuite (grep audit_logs)
5. Met à jour CLAUDE.md § 14 (état d'avancement)

Purpose: Phase 1.5 strictement no-go en prod sans cette gate. Schema push obligatoire (BLOCKING task) pour que les types Supabase soient cohérents et que les pgTAP tournent.
Output: 2 fichiers modifiés + verification rapport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md
@CLAUDE.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-01-PLAN.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-02-PLAN.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-05-PLAN.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 7.1 : Documenter env vars + générer secrets locaux</name>
  <files>.env.local.example, apps/web/.env.local.example</files>
  <read_first>
    - /home/user/TAP/.env.local.example (s'il existe — patterns env existants)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Environment Availability » (lignes 832-844)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md § « Claude's Discretion » (token signature)
  </read_first>
  <action>
    Vérifier l'existence de `.env.local.example` à la racine et/ou dans `apps/web/`. Y ajouter (sans écraser les existants Phase 0/1) :

    ```bash
    # ─── Phase 1.5 — RGPD compliance ────────────────────────────────

    # JWT portail patient (D-10) — 32 bytes minimum
    # Générer : openssl rand -base64 32
    APP_LEGAL_TOKEN_SECRET=changeme-32-bytes-minimum-required-pour-jwt-hs256

    # Salt anonymisation art. 17 (Plan 02 RPC rgpd_anonymize_patient)
    # Générer : openssl rand -base64 32
    # JAMAIS rotation sans plan migration (hash NIR anonymisés deviendraient incohérents)
    APP_ANONYMIZATION_SALT=changeme-32-bytes-minimum-required-pour-anonymisation-nir
    ```

    Documenter en commentaire en haut de fichier que ces vars sont requises côté server (Server Actions, Edge Functions, RPC) et JAMAIS côté client.

    Si nécessaire, ajouter aussi en local `.env.local` (non-commité — vérifier `.gitignore`) :
    ```bash
    APP_LEGAL_TOKEN_SECRET=$(openssl rand -base64 32)
    APP_ANONYMIZATION_SALT=$(openssl rand -base64 32)
    ```

    Vérifier que `.env.local` est listé dans `.gitignore` (Phase 0 a dû le faire — confirmer).

    CI : ajouter ces vars aux secrets GitHub Actions si la CI exécute des tests qui en dépendent (Plan 01 Vitest legal-token utilise `'a'.repeat(32)` donc OK ; mais E2E Playwright pourra avoir besoin du vrai secret pour signer un token de test).
  </action>
  <verify>
    <automated>cd /home/user/TAP && grep -c 'APP_LEGAL_TOKEN_SECRET' .env.local.example && grep -c 'APP_ANONYMIZATION_SALT' .env.local.example && grep -c '^\.env\.local$' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "APP_LEGAL_TOKEN_SECRET" .env.local.example` ≥ 1
    - `grep -c "APP_ANONYMIZATION_SALT" .env.local.example` ≥ 1
    - `grep -c "openssl rand" .env.local.example` ≥ 1 (instructions de génération)
    - `grep -c '^\.env\.local$' .gitignore` ≥ 1 (.env.local pas commité)
    - `grep -rE "APP_LEGAL_TOKEN_SECRET\s*=\s*[A-Za-z0-9+/=]{32,}" apps/web/src/ packages/` = 0 (aucun secret commité)
  </acceptance_criteria>
  <done>Env vars documentées, secrets locaux générés (mais pas commités), .gitignore vérifié.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Task 7.2 : Schema push local — pnpm db:reset applique 5 migrations RGPD</name>
  <files>(aucun fichier modifié — exécution DB)</files>
  <read_first>
    - /home/user/TAP/package.json (vérifier scripts `db:reset`, `db:test`)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-02-PLAN.md (5 migrations à appliquer)
    - Schema push requirement du planning_context : `pnpm db:reset` (local Supabase)
  </read_first>
  <action>
    **[BLOCKING] Cette tâche DOIT être exécutée AVANT toute verification E2E** :

    1. Vérifier que Supabase local tourne :
       ```bash
       cd /home/user/TAP
       supabase status 2>&1 | grep -q 'API URL' || pnpm dlx supabase start
       ```

    2. Reset DB locale + appliquer toutes les migrations dans l'ordre :
       ```bash
       pnpm db:reset
       ```
       Ce script doit (selon `package.json` Phase 0) :
       - Drop schema
       - Réappliquer les 4 migrations Phase 0/1 (foundations, rls_foundations, patients, search_rpc)
       - Appliquer les 5 nouvelles migrations Phase 1.5 :
         - `20260508000001_legal_compliance.sql`
         - `20260508000002_legal_additional_tables.sql`
         - `20260508000003_breach_72h_alert.sql`
         - `20260508000004_organizations_dpo_fields.sql`
         - `20260508000005_rgpd_anonymize_rpc.sql`

    3. Régénérer les types TypeScript :
       ```bash
       pnpm --filter @tap/database db:types
       ```
       (régénère `packages/database/src/types.gen.ts` avec les 8 nouvelles tables + colonnes DPO/CGU)

    4. Vérifier qu'aucune migration n'a échoué :
       ```bash
       supabase migration list 2>&1 | grep -E '(20260508)' | wc -l
       # Doit retourner 5
       ```

    5. Vérifier que pg_cron a bien planifié les jobs :
       ```bash
       psql "$DATABASE_URL" -c "SELECT jobname FROM cron.job;" | grep -E '(breach-72h-watchdog|legal-request-attempts-purge)' | wc -l
       # Doit retourner 2
       ```

    Si toute étape échoue, **STOP** et debug avant de continuer (Pitfall 5 RESEARCH — pg_cron doit être activé).

    Cette tâche est BLOCKING : pas d'E2E ni de Vitest ne peuvent passer si le schema n'est pas pushé.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm db:reset 2>&1 | tail -20 && supabase migration list 2>&1 | grep -E '20260508' | wc -l | grep -q '^5$' && psql "${DATABASE_URL:-postgresql://postgres:postgres@localhost:54322/postgres}" -c "SELECT count(*) FROM cron.job WHERE jobname IN ('breach-72h-watchdog', 'legal-request-attempts-purge');" 2>&1 | grep -q '2'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:reset` exit 0 sans erreur SQL
    - 5 migrations Phase 1.5 appliquées (`supabase migration list | grep 20260508` = 5 lignes)
    - 2 cron jobs actifs (`breach-72h-watchdog`, `legal-request-attempts-purge`)
    - Types régénérés (`grep -c "data_processing_register" packages/database/src/types.gen.ts` ≥ 1, idem pour les 7 autres tables)
    - `psql -c "select pg_get_functiondef('public.rgpd_anonymize_patient'::regproc)"` retourne le code de la RPC
  </acceptance_criteria>
  <done>Schema RGPD complet pushé en local. Types Supabase à jour. Jobs pg_cron actifs.</done>
</task>

<task type="auto">
  <name>Task 7.3 : Verification gate — pgTAP + Vitest + Playwright + grep no-leak</name>
  <files>(aucun fichier modifié — exécution tests)</files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Validation Architecture (Nyquist) — Phase Requirements → Test Map » (lignes 730-755)
  </read_first>
  <action>
    Exécuter dans l'ordre, **stop au premier échec** :

    1. **pgTAP** (7 tests) :
       ```bash
       pnpm db:test --file data_processing_register_rls.sql \
                    --file dpa_record_rls.sql \
                    --file dpia_record_rls.sql \
                    --file data_breach_incident_rls.sql \
                    --file patient_data_request_rls.sql \
                    --file check_breach_deadlines.sql \
                    --file rgpd_anonymize_patient.sql
       ```
       Attendu : 7/7 GREEN.

    2. **Vitest helpers** (3 fichiers) :
       ```bash
       APP_LEGAL_TOKEN_SECRET="$(openssl rand -base64 32)" \
       APP_ANONYMIZATION_SALT="$(openssl rand -base64 32)" \
       pnpm test --filter @tap/shared -- legal-token patient-data-export patient-anonymize legal
       ```
       Attendu : 14+ tests GREEN, couverture `packages/shared/src/utils/{legal-token,patient-data-export,patient-anonymize}.ts` + `validators/legal.ts` ≥ 80 %.

    3. **Playwright E2E** (6 specs) :
       ```bash
       pnpm exec playwright test tests/public/legal-pages.spec.ts \
                                  tests/public/cookie-banner.spec.ts \
                                  tests/portail/access-flow.spec.ts \
                                  tests/admin/registre-pdf.spec.ts \
                                  tests/admin/dpia.spec.ts \
                                  tests/admin/breach-countdown.spec.ts
       ```
       Attendu : 6/6 GREEN.

    4. **No-leak grep audit_logs** (CRITIQUE Pitfall 2 + R3) :
       Insérer une `patient_data_request` avec un token `'jwt-MARKER123'` puis vérifier :
       ```bash
       psql "$DATABASE_URL" -c "
         INSERT INTO public.patient_data_request (organization_id, patient_id, request_type,
           request_token, request_token_expires_at, requested_at, status,
           requester_proof_of_identity_url)
         SELECT id, NULL, 'acces', 'jwt-MARKER123', now() + interval '30 days', now(), 'recue',
                'https://supabase.test/proof-MARKER456.pdf'
         FROM public.organizations LIMIT 1;
       "
       psql "$DATABASE_URL" -c "
         SELECT count(*) FROM public.audit_logs
         WHERE metadata::text ILIKE '%MARKER123%' OR metadata::text ILIKE '%MARKER456%';
       " | grep -q '^[[:space:]]*0$'
       ```
       Attendu : `0` (les markers du token et de l'URL proof_of_identity NE DOIVENT PAS apparaître dans audit_logs.metadata).

       Idem vérification : `cnil_notification_reference` ne fuit pas dans audit_logs des breaches.

    5. **Build production** :
       ```bash
       pnpm build --filter @tap/web
       ```
       Attendu : exit 0, routes /legal/* en SSG, /api/admin/legal/registre/pdf en runtime nodejs (`λ`).

    6. **Mettre à jour CLAUDE.md § 14** (état d'avancement) :
       ```markdown
       LOT EN COURS : Lot 1 — Référentiels patients + saisie express
       LOT 0 : Fondations multi-tenant, RLS, migrations, CI/CD — TERMINÉ
       LOT 1.5 : DPA + RGPD compliance — TERMINÉ (tests pgTAP + Vitest + E2E green, schema pushé)
       DERNIER COMMIT MAJEUR : feat(1.5): phase 1.5 verifiée — RGPD compliance complète
       ```

    Si ANY étape échoue, ne pas marquer la phase comme terminée — créer un VERIFICATION.md avec les gaps.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm db:test 2>&1 | grep -c '^not ok' | grep -q '^0$' && APP_LEGAL_TOKEN_SECRET="$(openssl rand -base64 32)" APP_ANONYMIZATION_SALT="$(openssl rand -base64 32)" pnpm test --filter @tap/shared 2>&1 | grep -E 'Tests:.*failed' | wc -l | grep -q '^0$' && pnpm exec playwright test tests/public tests/portail tests/admin --reporter=line 2>&1 | grep -E 'passed' | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:test` : 0 ligne `not ok` (7 tests pgTAP RGPD GREEN + tests existants Phase 0/1 toujours GREEN)
    - `pnpm test --filter @tap/shared` : 0 failed
    - `pnpm exec playwright test tests/public tests/portail tests/admin` : 0 failed
    - `pnpm build --filter @tap/web` : exit 0
    - SQL grep no-leak : 0 occurrence des markers dans audit_logs
    - Routes /legal/{cgu,cgv,confidentialite,cookies,dpo} buildées en SSG (statut `○ Static`)
    - Route /api/admin/legal/registre/pdf buildée en `λ Dynamic` (Node runtime)
    - CLAUDE.md § 14 mis à jour avec état Lot 1.5 TERMINÉ
  </acceptance_criteria>
  <done>Phase 1.5 entièrement verte : 7 pgTAP + 14+ Vitest + 6 E2E + build prod + audit no-leak vérifié.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local DB → CI DB | Migrations doivent appliquer dans les deux environnements |
| .env.local → Vercel env | Secrets RGPD JAMAIS commités, doivent être propagés manuellement en prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-29 (T2 HIGH) | Information Disclosure | audit_logs leak token/proof | mitigate | Test grep automatisé Task 7.3 step 4 — assertion zéro occurrence des markers en metadata. Si fail → fix audit trigger Plan 02 (Pitfall 2 RESEARCH). |
| T-1.5-30 (T5 MEDIUM) | Repudiation | pg_cron pas activé en prod | mitigate | `select count(*) from cron.job where jobname = 'breach-72h-watchdog'` ≥ 1 vérifié post-deploy. À documenter en runbook Vercel/Supabase. |
| T-1.5-31 | Information Disclosure | secret env commité par accident | mitigate | `.env.local` dans `.gitignore` (Phase 0). Grep CI `[A-Za-z0-9+/=]{32,}` patterns suspects. Pre-commit hook (Phase 0). |
| T-1.5-32 | Tampering | rotation `APP_ANONYMIZATION_SALT` casse hash NIR | mitigate | Documentation en `.env.local.example` : « JAMAIS rotation sans plan migration ». ADR placeholder à créer V1.5 si rotation envisagée. |
</threat_model>

<verification>
- Toutes les acceptance_criteria des 3 tasks atteintes
- `git status` : aucun fichier non-committé inattendu
- `git log --oneline | head -7` : 6 commits Phase 1.5 (1 par plan)
- ROADMAP.md Phase 1.5 status : « Complete » + plans tous `[x]`
- STATE.md mis à jour : `progress.completed_phases: 2`
</verification>

<success_criteria>
- 5 migrations RGPD appliquées localement
- 7 + 14+ + 6 = 27+ tests GREEN
- 0 fuite audit_logs (grep markers)
- Build production OK
- 2 nouvelles env vars documentées
- CLAUDE.md § 14 mis à jour
- Commit français : `chore(1.5): verification gate — schema push + tests green + no-leak audit`
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-dpa-rgpd-compliance/1.5-07-SUMMARY.md` + global `1.5-VERIFICATION.md` listant tous les success criteria DPA-01..DPA-08 et leur status.
</output>
