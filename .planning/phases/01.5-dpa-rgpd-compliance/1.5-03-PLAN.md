---
phase: 01.5-dpa-rgpd-compliance
plan: 03
type: execute
wave: 1
# Wave 1B — runs after Plan 02 in same wave to consume regenerated types.gen.ts
depends_on: ["1.5-01", "1.5-02"]
files_modified:
  - packages/shared/src/utils/legal-token.ts
  - packages/shared/src/utils/patient-data-export.ts
  - packages/shared/src/utils/patient-anonymize.ts
  - packages/shared/src/validators/legal.ts
  - packages/shared/src/index.ts
autonomous: true
requirements: [DPA-04]

must_haves:
  truths:
    - "signRequestToken/verifyRequestToken signent et vérifient un JWT HS256 avec claims minimaux {sub, org, type}"
    - "verifyRequestToken rejette tokens expirés, malformés, signature invalide, algo non-HS256"
    - "generatePatientDataExport retourne format_version 1.0 + données patient (vue patients_safe) + audit_log entity_id"
    - "anonymizePatient appelle RPC rgpd_anonymize_patient et propage erreur en français"
    - "Les 3 helpers font ≤ 50 lignes chacun (CLAUDE.md § 11)"
    - "Tous les Vitest helpers de Plan 01 deviennent GREEN"
  artifacts:
    - path: "packages/shared/src/utils/legal-token.ts"
      provides: "JWT sign/verify avec jose, secret env APP_LEGAL_TOKEN_SECRET"
      max_lines: 50
      exports: ["signRequestToken", "verifyRequestToken"]
    - path: "packages/shared/src/utils/patient-data-export.ts"
      provides: "Helper portabilité art. 15+20"
      max_lines: 50
      exports: ["generatePatientDataExport", "PatientDataExport"]
    - path: "packages/shared/src/utils/patient-anonymize.ts"
      provides: "Helper effacement art. 17 via RPC"
      max_lines: 50
      exports: ["anonymizePatient"]
    - path: "packages/shared/src/validators/legal.ts"
      provides: "zod schemas registre/dpa/dpia/breach/request"
      exports: ["dataProcessingRegisterSchema", "dpiaSchema", "breachIncidentSchema", "dataRequestSchema", "dpaRecordSchema"]
  key_links:
    - from: "legal-token.ts"
      to: "process.env.APP_LEGAL_TOKEN_SECRET"
      via: "TextEncoder().encode()"
      pattern: "APP_LEGAL_TOKEN_SECRET"
    - from: "patient-data-export.ts"
      to: "supabase.from('patients_safe')"
      via: "vue Phase 1 — pas la table directe"
      pattern: "patients_safe"
    - from: "anonymizePatient"
      to: "rgpd_anonymize_patient RPC"
      via: "supabase.rpc('rgpd_anonymize_patient', ...)"
      pattern: "rpc\\('rgpd_anonymize_patient'"
---

<objective>
**Wave 1B — Helpers purs RGPD**. Créer 3 helpers (`legal-token`, `patient-data-export`, `patient-anonymize`) dans `packages/shared/src/utils/` + validators zod legal dans `packages/shared/src/validators/legal.ts`. Strictement pure, ≤ 50 lignes par fichier. Tests Vitest de Plan 01 → GREEN.

Purpose: Briques réutilisables consommées par Server Actions admin (Plan 06) et portail patient (Plan 05). Aucune dépendance UI.
Output: 5 fichiers + index update. **Wave 1B** — séquentiel après Plan 02 (DB) dans la même wave : Plan 03 consomme `types.gen.ts` régénéré par Plan 02 (RPC `rgpd_anonymize_patient` + 5 nouvelles tables).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md
@CLAUDE.md
@packages/shared/src/utils/patient-note.ts
@packages/shared/src/validators/common.ts
@packages/shared/src/validators/patient.ts
@packages/shared/src/index.ts

<interfaces>
<!-- Symboles déjà exportés par packages/shared : -->
```typescript
// packages/shared/src/validators/common.ts
export const codePostalReunionSchema: z.ZodSchema;
export const nirFormatSchema: z.ZodSchema;
export const phoneNumberSchema: z.ZodSchema;
export function luhnCheck(s: string): boolean;
```

<!-- Types Supabase (regénérés Plan 02) : -->
```typescript
// packages/database/src/types.gen.ts (après Plan 02)
import type { SupabaseClient } from '@supabase/supabase-js';
import type { Database } from '@tap/database/types';
// Tables disponibles : data_processing_register, dpa_record, dpia_record,
//                      data_breach_incident, patient_data_request, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1 : legal-token.ts (JWT sign/verify, jose HS256)</name>
  <files>packages/shared/src/utils/legal-token.ts, packages/shared/src/index.ts</files>
  <read_first>
    - /home/user/TAP/packages/shared/src/validators/common.ts (lignes 1-51 — pattern helper pur français + JSDoc)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « JWT signature pour portail patient » (lignes 130-160) + Pitfall 1 (claims minimaux)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § « legal-token.ts » (lignes 280-305)
    - /home/user/TAP/packages/shared/src/utils/__tests__/legal-token.test.ts (cibles à faire passer GREEN)
  </read_first>
  <behavior>
    - Test 1 : `signRequestToken({ sub, org, type })` retourne string avec 3 segments séparés par `.`
    - Test 2 : `verifyRequestToken(signed)` retourne payload `{ sub, org, type }` (autres champs filtrés)
    - Test 3 : token signé avec `setExpirationTime('-1d')` → throw error avec message contenant `expir`
    - Test 4 : token avec dernière char modifié → throw error signature
    - Test 5 : token random `'abc.def.ghi'` → throw error
    - Test 6 : token signé HS512 → throw error (algorithm not in `algorithms: ['HS256']`)
    - Test 7 : claims sans `sub` ou `org` ou `type` → TypeScript empêche compile (test type-level)
  </behavior>
  <action>
    Créer `packages/shared/src/utils/legal-token.ts` (≤ 50 lignes) :

    ```ts
    import { SignJWT, jwtVerify } from 'jose';

    /**
     * Helpers JWT HS256 pour le portail patient (D-10, D-20).
     * Token signé identifie une `patient_data_request` ; le serveur résout
     * les autres champs en DB. JAMAIS de PII (email, patient_id, NIR) dans
     * les claims (Pitfall 1 RESEARCH).
     *
     * Secret : `APP_LEGAL_TOKEN_SECRET` (32 bytes, distinct des clés NIR).
     */
    export type RequestTokenClaims = {
      sub: string;       // patient_data_request.id (UUID)
      org: string;       // organization_id
      type: string;      // request_type
    };

    function getSecret(): Uint8Array {
      const secret = process.env.APP_LEGAL_TOKEN_SECRET;
      if (!secret || secret.length < 32) {
        throw new Error('APP_LEGAL_TOKEN_SECRET manquant ou trop court (≥ 32 bytes requis).');
      }
      return new TextEncoder().encode(secret);
    }

    /** Signe un token de portail patient valide 30 jours (D-10, art. 12). */
    export async function signRequestToken(claims: RequestTokenClaims): Promise<string> {
      return new SignJWT(claims as unknown as Record<string, unknown>)
        .setProtectedHeader({ alg: 'HS256' })
        .setIssuedAt()
        .setExpirationTime('30d')
        .sign(getSecret());
    }

    /** Vérifie un token et retourne les claims typés. Throw si invalide/expiré. */
    export async function verifyRequestToken(token: string): Promise<RequestTokenClaims> {
      const { payload } = await jwtVerify(token, getSecret(), { algorithms: ['HS256'] });
      const { sub, org, type } = payload as Record<string, unknown>;
      if (typeof sub !== 'string' || typeof org !== 'string' || typeof type !== 'string') {
        throw new Error('Claims du token incomplets.');
      }
      return { sub, org, type };
    }
    ```

    Ajouter export dans `packages/shared/src/index.ts` :
    ```ts
    export * from './utils/legal-token';
    ```

    Toutes les exceptions en français. Pas de `console.log`. Pas de PII dans les claims (Pitfall 1).
  </action>
  <verify>
    <automated>cd /home/user/TAP && APP_LEGAL_TOKEN_SECRET="$(node -e "console.log('a'.repeat(32))")" pnpm test --filter @tap/shared -- legal-token 2>&1 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - Fichier ≤ 50 lignes (`wc -l < packages/shared/src/utils/legal-token.ts` ≤ 50)
    - Exporte `signRequestToken`, `verifyRequestToken`, `RequestTokenClaims`
    - Aucune PII dans le type `RequestTokenClaims` (pas `email`, `patient_id`, `nir`)
    - `grep -c "algorithms: \['HS256'\]" packages/shared/src/utils/legal-token.ts` ≥ 1
    - `pnpm test --filter @tap/shared -- legal-token` : 6+ tests GREEN
    - Re-export depuis `packages/shared/src/index.ts`
  </acceptance_criteria>
  <done>JWT sign/verify GREEN, claims minimaux, secret validé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.2 : patient-data-export.ts + patient-anonymize.ts (helpers art. 15+20 et art. 17)</name>
  <files>packages/shared/src/utils/patient-data-export.ts, packages/shared/src/utils/patient-anonymize.ts, packages/shared/src/index.ts</files>
  <read_first>
    - /home/user/TAP/packages/shared/src/utils/patient-note.ts (intégral — pattern signature + JSDoc + ≤ 50 lignes)
    - /home/user/TAP/packages/shared/src/utils/__tests__/patient-note.test.ts (pattern mock SupabaseClient)
    - /home/user/TAP/supabase/migrations/20260507000001_patients.sql (vue `patients_safe` — confirmer colonnes exposées)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Code Examples » (lignes 657-678) + § Anonymisation (lignes 280-329)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 222-276
  </read_first>
  <behavior>
    - Test 1 (export) : signature `(supabase, patientId): Promise<PatientDataExport>` ; mock retourne `format_version: '1.0'`, `exported_at` ISO 8601, `patient` depuis `patients_safe`, `operational_notes`, `constraintes`, `audit_log` filtrés `entity_id = patientId`
    - Test 2 (export) : si `patients_safe.has_nir = false`, le retour `patient.nir === null` SANS appel à Edge Function
    - Test 3 (export) : si patient introuvable → throw `'Patient introuvable.'`
    - Test 4 (anonymize) : appel `supabase.rpc('rgpd_anonymize_patient', { p_patient_id, p_request_id, p_salt: process.env.APP_ANONYMIZATION_SALT })`
    - Test 5 (anonymize) : si rpc retourne error → throw `'Anonymisation échouée : <message>'`
    - Test 6 (anonymize) : APP_ANONYMIZATION_SALT manquant → throw `'APP_ANONYMIZATION_SALT manquant'`
  </behavior>
  <action>
    **`packages/shared/src/utils/patient-data-export.ts`** (≤ 50 lignes) :

    ```ts
    import type { SupabaseClient } from '@supabase/supabase-js';
    import type { Database } from '@tap/database/types';

    /**
     * Helper portabilité (art. 15 + art. 20 RGPD) — D-10 / DPA-04.
     * Lit la vue `patients_safe` (jamais la table directe) + tables liées
     * + audit_logs filtrés par entity_id. Le NIR clair n'est PAS récupéré
     * ici : un appel séparé à l'Edge Function `nir` (avec audit log
     * automatique) est requis si décryptage demandé.
     */
    export type PatientDataExport = {
      format_version: '1.0';
      exported_at: string;
      patient: Record<string, unknown> & { nir: string | null };
      operational_notes: unknown[];
      constraintes: unknown[];
      audit_log: unknown[];
    };

    export async function generatePatientDataExport(
      supabase: SupabaseClient<Database>,
      patientId: string,
    ): Promise<PatientDataExport> {
      const [{ data: patient, error: e1 }, { data: notes }, { data: constraintes }, { data: audits }] =
        await Promise.all([
          supabase.from('patients_safe').select('*').eq('id', patientId).single(),
          supabase.from('patient_operational_note').select('*').eq('patient_id', patientId),
          supabase.from('patient_constraint').select('*').eq('patient_id', patientId),
          supabase.from('audit_logs').select('*').eq('entity_id', patientId),
        ]);
      if (e1 || !patient) throw new Error('Patient introuvable.');
      return {
        format_version: '1.0',
        exported_at: new Date().toISOString(),
        patient: { ...patient, nir: null },
        operational_notes: notes ?? [],
        constraintes: constraintes ?? [],
        audit_log: audits ?? [],
      };
    }
    ```

    **`packages/shared/src/utils/patient-anonymize.ts`** (≤ 50 lignes) :

    ```ts
    import type { SupabaseClient } from '@supabase/supabase-js';
    import type { Database } from '@tap/database/types';

    /**
     * Helper effacement (art. 17 RGPD) — D-10 / DPA-04.
     * Délègue à la RPC SQL `rgpd_anonymize_patient` qui exécute
     * UPDATE patients (NULL identité, NIR rotated hash) + DELETE notes/contraintes
     * + INSERT audit_logs, dans une transaction unique avec advisory lock.
     * NE SUPPRIME JAMAIS les rides (durée légale CGSS L114-19, R5 RESEARCH).
     */
    export async function anonymizePatient(
      supabase: SupabaseClient<Database>,
      patientId: string,
      requestId: string,
    ): Promise<void> {
      const salt = process.env.APP_ANONYMIZATION_SALT;
      if (!salt || salt.length < 16) {
        throw new Error('APP_ANONYMIZATION_SALT manquant ou trop court (≥ 16 bytes requis).');
      }
      const { error } = await supabase.rpc('rgpd_anonymize_patient', {
        p_patient_id: patientId,
        p_request_id: requestId,
        p_salt: salt,
      });
      if (error) {
        throw new Error(`Anonymisation échouée : ${error.message}`);
      }
    }
    ```

    Ajouter exports dans `packages/shared/src/index.ts`.

    Aucun `console.log`. Aucun `any`. Messages d'erreur français.
  </action>
  <verify>
    <automated>cd /home/user/TAP && APP_ANONYMIZATION_SALT="$(node -e "console.log('s'.repeat(32))")" pnpm test --filter @tap/shared -- "patient-(data-export|anonymize)" 2>&1 | grep -E '(passed|failed)' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 2 fichiers ≤ 50 lignes chacun (`wc -l`)
    - `grep -c "patients_safe" packages/shared/src/utils/patient-data-export.ts` ≥ 1 (jamais `from('patients')`)
    - `grep -c "from\('patients'\)" packages/shared/src/utils/patient-data-export.ts` = 0
    - `grep -c "rpc\('rgpd_anonymize_patient'" packages/shared/src/utils/patient-anonymize.ts` ≥ 1
    - `grep -ci 'delete' packages/shared/src/utils/patient-anonymize.ts` = 0 (jamais DELETE côté TS)
    - `pnpm test --filter @tap/shared -- patient-data-export patient-anonymize` : 5+ tests GREEN
    - Tous les exports re-exportés depuis `packages/shared/src/index.ts`
  </acceptance_criteria>
  <done>2 helpers GREEN, vue patients_safe respectée, RPC anonymize wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.3 : validators zod legal (registre, dpia, breach, request, dpa)</name>
  <files>packages/shared/src/validators/legal.ts, packages/shared/src/index.ts</files>
  <read_first>
    - /home/user/TAP/packages/shared/src/validators/patient.ts (intégral — pattern composition zod + messages français + refine cross-field)
    - /home/user/TAP/packages/shared/src/validators/common.ts (helpers réutilisables)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md § decisions D-05..D-09 (champs + enums verbatim)
  </read_first>
  <behavior>
    - Test 1 : `dataProcessingRegisterSchema.safeParse({purpose:'transport patients', legal_basis:'contrat', data_categories:['identite','sante'], data_subjects:['patient'], recipients:['CGSS'], retention_period_days:1825, security_measures:'AES-256-GCM + RLS', international_transfer:false})` → success
    - Test 2 : `legal_basis: 'invalide'` → fail avec message FR « Base légale invalide »
    - Test 3 : `data_categories: []` → fail « Au moins une catégorie de données requise »
    - Test 4 : `dpiaSchema` : `status: 'brouillon'` valide ; `next_review_at < reviewed_at` → refine error
    - Test 5 : `breachIncidentSchema` : `cnil_notification_required: true` + `cnil_notification_at: null` → success (timer 72h non encore notifié) ; `severity: 'invalid'` fail
    - Test 6 : `dataRequestSchema` : `request_type: 'effacement'` valide ; `request_type: 'invalid'` fail
  </behavior>
  <action>
    Créer `packages/shared/src/validators/legal.ts` :

    ```ts
    import { z } from 'zod';

    // ─── data_processing_register (D-05) ─────────────────────────────
    export const legalBasisEnum = z.enum([
      'consentement', 'contrat', 'obligation_legale',
      'mission_interet_public', 'interet_legitime', 'sauvegarde_vie',
    ], { errorMap: () => ({ message: 'Base légale invalide.' }) });

    export const dataProcessingRegisterSchema = z.object({
      purpose: z.string().trim().min(1, 'Finalité requise').max(500),
      legal_basis: legalBasisEnum,
      data_categories: z.array(z.string().trim().min(1)).min(1, 'Au moins une catégorie de données requise.'),
      data_subjects: z.array(z.string().trim().min(1)).min(1, 'Au moins une catégorie de personnes concernées requise.'),
      recipients: z.array(z.string().trim().min(1)).min(1, 'Au moins un destinataire requis.'),
      retention_period_days: z.number().int().min(1, 'Durée de conservation requise.').max(36500),
      security_measures: z.string().trim().min(1, 'Mesures de sécurité requises.').max(2000),
      international_transfer: z.boolean(),
      international_transfer_safeguards: z.string().trim().max(2000).optional().nullable(),
    });

    // ─── dpa_record (D-06) ───────────────────────────────────────────
    export const dpaRecordSchema = z.object({
      subprocessor_name: z.string().trim().min(1, 'Nom du sous-traitant requis.').max(200),
      subprocessor_role: z.string().trim().min(1).max(100),
      dpa_version: z.string().trim().min(1).max(100),
      dpa_document_url: z.string().url('URL invalide.').optional().nullable(),
      signed_at: z.coerce.date(),
      expires_at: z.coerce.date().optional().nullable(),
      notes: z.string().trim().max(2000).optional().nullable(),
    });

    // ─── dpia_record (D-07) ──────────────────────────────────────────
    export const dpiaStatusEnum = z.enum(['brouillon', 'validee', 'archivee']);
    export const dpiaResidualRiskEnum = z.enum(['faible', 'moyen', 'eleve']);

    export const dpiaSchema = z.object({
      title: z.string().trim().min(1, 'Titre requis.').max(200),
      scope: z.string().trim().min(1, 'Périmètre requis.').max(2000),
      data_flow_diagram: z.string().max(10000).optional().nullable(),
      risks_identified: z.array(z.object({ id: z.string(), label: z.string(), severity: z.string() })).default([]),
      mitigations: z.array(z.object({ risk_id: z.string(), measure: z.string() })).default([]),
      residual_risk_level: dpiaResidualRiskEnum,
      cnil_consultation_required: z.boolean(),
      cnil_consultation_date: z.coerce.date().optional().nullable(),
      reviewed_at: z.coerce.date(),
      next_review_at: z.coerce.date(),
      status: dpiaStatusEnum,
    }).refine((d) => d.next_review_at > d.reviewed_at, {
      message: 'La prochaine revue doit être postérieure à la date de revue.',
      path: ['next_review_at'],
    });

    // ─── data_breach_incident (D-08) ─────────────────────────────────
    export const breachSeverityEnum = z.enum(['faible', 'moyen', 'eleve', 'critique']);
    export const breachNatureEnum = z.enum(['confidentialite', 'integrite', 'disponibilite']);

    export const breachIncidentSchema = z.object({
      detected_at: z.coerce.date(),
      severity: breachSeverityEnum,
      nature: breachNatureEnum,
      affected_data_categories: z.array(z.string().trim().min(1)).min(1),
      affected_subjects_count: z.number().int().min(0).optional().nullable(),
      description: z.string().trim().min(1, 'Description requise.').max(5000),
      immediate_measures: z.string().trim().min(1, 'Mesures immédiates requises.').max(5000),
      cnil_notification_required: z.boolean(),
      cnil_notification_at: z.coerce.date().optional().nullable(),
      cnil_notification_reference: z.string().trim().max(100).optional().nullable(),
      subjects_notification_required: z.boolean(),
      subjects_notified_at: z.coerce.date().optional().nullable(),
    });

    // ─── patient_data_request (D-09) ────────────────────────────────
    export const dataRequestTypeEnum = z.enum([
      'acces', 'rectification', 'effacement', 'limitation', 'portabilite', 'opposition',
    ]);
    export const dataRequestStatusEnum = z.enum([
      'recue', 'en_cours', 'satisfaite', 'rejetee', 'partiellement_satisfaite',
    ]);

    export const dataRequestSchema = z.object({
      patient_id: z.string().uuid().optional().nullable(),
      request_type: dataRequestTypeEnum,
      requester_email: z.string().email('Email invalide.').optional().nullable(),
      requested_at: z.coerce.date().default(() => new Date()),
    });

    // ─── DPO contact (D-15) ──────────────────────────────────────────
    export const dpoContactSchema = z.object({
      dpo_contact_email: z.string().email('Email DPO invalide.').optional().nullable(),
      dpo_contact_phone: z.string().trim().max(40).optional().nullable(),
      dpo_contact_address: z.string().trim().max(500).optional().nullable(),
      dpo_external: z.boolean().default(false),
    });

    export type DataProcessingRegisterInput = z.infer<typeof dataProcessingRegisterSchema>;
    export type DpaRecordInput = z.infer<typeof dpaRecordSchema>;
    export type DpiaInput = z.infer<typeof dpiaSchema>;
    export type BreachIncidentInput = z.infer<typeof breachIncidentSchema>;
    export type DataRequestInput = z.infer<typeof dataRequestSchema>;
    export type DpoContactInput = z.infer<typeof dpoContactSchema>;
    ```

    Re-export depuis `packages/shared/src/index.ts`.

    Code limit fichier : visé ≤ 200 lignes (zod déclaratif). Si dépasse 300 → splitter par concept (registre/dpia/breach/request) — pas nécessaire ici.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck --filter @tap/shared && pnpm test --filter @tap/shared -- legal 2>&1 | grep -E '(passed|failed)' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Fichier ≤ 220 lignes (zod déclaratif)
    - Exporte 6 schemas + 6 types : `dataProcessingRegisterSchema`, `dpaRecordSchema`, `dpiaSchema`, `breachIncidentSchema`, `dataRequestSchema`, `dpoContactSchema`
    - Tous les messages d'erreur en français (`grep -c "message: '" packages/shared/src/validators/legal.ts` ≥ 8)
    - `grep -c "z.enum" packages/shared/src/validators/legal.ts` ≥ 6 (enums alignés CHECK constraints DB)
    - `pnpm typecheck --filter @tap/shared` exit 0
  </acceptance_criteria>
  <done>Validators zod legal exports, types inferés, messages français, alignés DB constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.env` → helpers | `APP_LEGAL_TOKEN_SECRET` et `APP_ANONYMIZATION_SALT` lus runtime — disponibles serveur uniquement |
| Helper TS → Supabase | Le helper `patient-data-export` lit `patients_safe` (vue safe), jamais la table directe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-09 (T1 HIGH) | Tampering | JWT signature | mitigate | `algorithms: ['HS256']` strict (rejette HS512/none/RS256). Secret ≥ 32 bytes (validation runtime). Tests dédiés algos non-autorisés. |
| T-1.5-10 (T1 HIGH) | Information Disclosure | JWT claims contenant PII | mitigate | Type `RequestTokenClaims` figé à `{sub, org, type}`. Aucun email/patient_id/NIR — Pitfall 1 RESEARCH. Test type-level. |
| T-1.5-11 | Information Disclosure | Helper export NIR clair sans audit | mitigate | `patient-data-export` retourne `nir: null`. Décryptage NIR uniquement via Edge Function dédiée Phase 1 (audit log auto). |
| T-1.5-12 (T4 MEDIUM) | Tampering | anonymizePatient bypass RPC | mitigate | Helper TS ne fait QUE `supabase.rpc()` — pas de DELETE/UPDATE direct. RPC SECURITY DEFINER côté DB applique les garde-fous. |
</threat_model>

<verification>
- `pnpm test --filter @tap/shared` : tous les tests Vitest du Plan 01 GREEN (3 fichiers, 14+ assertions)
- `pnpm typecheck --filter @tap/shared` : 0 erreur
- `pnpm typecheck` exit 0 (assumes Plan 02 has regenerated types BEFORE Plan 03 starts — Wave 1B sequencing)
- `grep -E "Database\['public'\]\['Functions'\]\['rgpd_anonymize_patient'\]" packages/shared/src/utils/patient-anonymize.ts` should match (vérifie que le RPC type est consommé)
- `wc -l packages/shared/src/utils/{legal-token,patient-data-export,patient-anonymize}.ts` : tous ≤ 50
- Aucun `any` ni `console.log`
</verification>

<success_criteria>
- 4 fichiers (`legal-token.ts`, `patient-data-export.ts`, `patient-anonymize.ts`, `validators/legal.ts`) créés
- 14+ tests Vitest GREEN
- Tous les exports disponibles depuis `@tap/shared`
- Commit français : `feat(1.5): helpers RGPD purs (jwt, export, anonymize, validators)`
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-dpa-rgpd-compliance/1.5-03-SUMMARY.md`.
</output>

## Revision Log

- **B-3 fix (iteration 2/3)** — 2026-05-06 : `depends_on` étendu à `["1.5-01", "1.5-02"]` pour garantir que `types.gen.ts` régénéré par Plan 02 (RPC `rgpd_anonymize_patient` + 5 nouvelles tables) est disponible avant le typecheck Plan 03. Plan 03 reste en wave 1 mais consomme Plan 02 séquentiellement (pattern intra-wave dependency déclarée). Plan 03 est désormais étiqueté **Wave 1B**.
