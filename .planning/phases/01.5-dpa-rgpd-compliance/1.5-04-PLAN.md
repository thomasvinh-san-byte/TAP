---
phase: 01.5-dpa-rgpd-compliance
plan: 04
type: execute
wave: 2
depends_on: ["1.5-02", "1.5-03"]
files_modified:
  - apps/web/src/middleware.ts
  - apps/web/src/app/(public)/layout.tsx
  - apps/web/src/app/(admin)/layout.tsx
  - apps/web/src/components/cookie-banner.client.tsx
  - apps/web/src/components/footer.tsx
  - apps/web/src/app/api/legal/cookie-consent/route.ts
  - apps/web/src/lib/cookie-consent.ts
  - apps/web/src/lib/supabase/admin.ts
autonomous: true
requirements: [DPA-06, DPA-07, DPA-08]

must_haves:
  truths:
    - "Le middleware Auth EXCLUT /legal/* du redirect login (D-21)"
    - "Le middleware admin redirige vers / si profile.role !== 'dirigeant'"
    - "Le bandeau cookies a 3 boutons strictement équivalents (taille/couleur/padding)"
    - "Aucun cookie analytics/marketing déclenché tant que pas accepté (sauf strictement nécessaires)"
    - "Le consent est persisté en localStorage + audit log via /api/legal/cookie-consent"
    - "Le footer public expose 5 liens legal + lien permanent 'Gérer mes cookies'"
    - "Le test E2E cookie-banner.spec.ts (Plan 01) GREEN"
  artifacts:
    - path: "apps/web/src/middleware.ts"
      provides: "Routes /legal/* publiques + /admin/* protégé dirigeant"
      contains: "/legal"
    - path: "apps/web/src/app/(public)/layout.tsx"
      provides: "Layout public sans auth, avec footer legal"
      max_lines: 80
    - path: "apps/web/src/app/(admin)/layout.tsx"
      provides: "Guard rôle dirigeant"
      max_lines: 60
    - path: "apps/web/src/components/cookie-banner.client.tsx"
      provides: "Banner CNIL-conforme symétrique"
      max_lines: 150
    - path: "apps/web/src/app/api/legal/cookie-consent/route.ts"
      provides: "POST consent → cookie_consent_log"
      max_lines: 60
  key_links:
    - from: "middleware.ts"
      to: "isPublicLegal check"
      via: "pathname.startsWith('/legal')"
      pattern: "isPublicLegal"
    - from: "cookie-banner.client.tsx"
      to: "/api/legal/cookie-consent"
      via: "fetch POST"
      pattern: "/api/legal/cookie-consent"
    - from: "(admin)/layout.tsx"
      to: "profiles.role"
      via: "if (profile.role !== 'dirigeant') redirect('/')"
      pattern: "role !== 'dirigeant'"
---

<objective>
**Wave 2 — Layouts publics/admin + bandeau cookies CNIL-conforme**. Ce plan pose la structure routing :
- Modifier `middleware.ts` pour exclure `/legal/*` du redirect login (D-21)
- Créer le layout `(public)` (sans auth, footer legal, banner)
- Créer le layout `(admin)` (guard rôle dirigeant)
- Créer le bandeau cookies (3 boutons strictement symétriques, anti-dark-pattern CNIL)
- Créer la route API consent

Purpose: Wave 2 unique (file ownership middleware.ts impose sequential vis-à-vis Wave 3 qui dépend des layouts). Aucune page legal ni admin créée ici (Wave 3).
Output: 7 fichiers. Test cookie-banner GREEN. /legal/* accessibles SANS auth (mais 404 jusqu'à Wave 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md
@CLAUDE.md
@apps/web/src/middleware.ts
@apps/web/src/app/(app)/layout.tsx
@apps/web/src/app/layout.tsx

<interfaces>
<!-- Existant Phase 1 : -->
```typescript
// apps/web/src/middleware.ts (existant) — EXTEND
export async function middleware(request: NextRequest);
export const config = { matcher: ['/((?!_next/static|_next/image|favicon.ico|api/health).*)'] };

// apps/web/src/app/(app)/layout.tsx (existant) — pattern Auth + Providers
// apps/web/src/components/ui/button.tsx (existant shadcn)
```

<!-- Wave 1 livré : -->
```typescript
// packages/database/src/types.gen.ts contient maintenant : cookie_consent_log
// packages/shared exporte : DpoContactInput
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1 : Middleware — exclure /legal/* + créer layouts (public)/(admin)</name>
  <files>
    apps/web/src/middleware.ts,
    apps/web/src/app/(public)/layout.tsx,
    apps/web/src/app/(admin)/layout.tsx,
    apps/web/src/components/footer.tsx
  </files>
  <read_first>
    - /home/user/TAP/apps/web/src/middleware.ts (intégral)
    - /home/user/TAP/apps/web/src/app/(app)/layout.tsx (intégral — pattern auth + Providers)
    - /home/user/TAP/apps/web/src/app/layout.tsx (root)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 308-326 (layout public) et 401-431 (layout admin)
  </read_first>
  <action>
    **`apps/web/src/middleware.ts`** — édition (D-21) :
    Modifier la condition de redirect dans le middleware existant :

    ```ts
    const { pathname } = request.nextUrl;
    const isAuthRoute = pathname.startsWith('/login');
    const isPublicLegal = pathname.startsWith('/legal');
    const isLegalApi = pathname.startsWith('/api/legal/cookie-consent');

    if (!user && !isAuthRoute && !isPublicLegal && !isLegalApi) {
      // ... redirect login (existant inchangé)
    }
    ```

    Ne PAS modifier le `matcher` (les pages /legal/* et l'API consent sont matchées mais skippées en code).

    Le user déjà connecté qui visite `/legal/*` continue normalement (pas de redirect).

    **`apps/web/src/app/(public)/layout.tsx`** (≤ 80 lignes) :
    ```tsx
    import type { Metadata } from 'next';
    import { Footer } from '@/components/footer';
    import { CookieBanner } from '@/components/cookie-banner.client';

    export const metadata: Metadata = {
      title: { default: 'TAP Régulation', template: '%s — TAP Régulation' },
    };

    export default function PublicLayout({ children }: { children: React.ReactNode }) {
      return (
        <div className="min-h-screen flex flex-col">
          <header className="border-b">
            <div className="max-w-[1200px] mx-auto px-24 py-16 flex items-center justify-between">
              <a href="/" className="font-semibold tracking-tight">TAP Régulation</a>
              <a href="/login" className="text-sm text-muted-foreground hover:text-foreground">Connexion</a>
            </div>
          </header>
          <main className="flex-1 max-w-[840px] mx-auto px-24 py-48 w-full">{children}</main>
          <Footer />
          <CookieBanner />
        </div>
      );
    }
    ```

    **`apps/web/src/app/(admin)/layout.tsx`** (≤ 60 lignes) — guard rôle dirigeant (D-16) :
    ```tsx
    import { redirect } from 'next/navigation';
    import { createClient } from '@/lib/supabase/server';
    import { Providers } from '@/app/providers.client';

    export default async function AdminLayout({ children }: { children: React.ReactNode }) {
      const supabase = createClient();
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) redirect('/login');

      const { data: profile } = await supabase
        .from('profiles')
        .select('role')
        .eq('id', user.id)
        .single();

      if (profile?.role !== 'dirigeant') {
        redirect('/');
      }

      return (
        <Providers>
          <div className="min-h-screen bg-background">
            <aside className="...">{/* nav admin minimale */}</aside>
            <main className="max-w-[1200px] mx-auto px-24 py-32">{children}</main>
          </div>
        </Providers>
      );
    }
    ```

    **`apps/web/src/components/footer.tsx`** (≤ 50 lignes) :
    ```tsx
    'use client';
    import { resetCookieConsent } from '@/lib/cookie-consent';

    export function Footer() {
      return (
        <footer className="border-t bg-muted/30">
          <div className="max-w-[1200px] mx-auto px-24 py-32 flex flex-wrap gap-24 text-sm text-muted-foreground">
            <a href="/legal/cgu" className="hover:text-foreground">CGU</a>
            <a href="/legal/cgv" className="hover:text-foreground">CGV</a>
            <a href="/legal/confidentialite" className="hover:text-foreground">Politique de confidentialité</a>
            <a href="/legal/cookies" className="hover:text-foreground">Cookies</a>
            <a href="/legal/dpo" className="hover:text-foreground">Contact DPO</a>
            <button type="button" onClick={resetCookieConsent} className="hover:text-foreground">
              Gérer mes cookies
            </button>
          </div>
        </footer>
      );
    }
    ```

    Tous les composants sans emoji (anti-pattern UI). Spacing strict 8/12/16/24/32/48 (CLAUDE.md § 1).
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck --filter @tap/web && grep -c "isPublicLegal" apps/web/src/middleware.ts && grep -c "role !== 'dirigeant'" apps/web/src/app/\(admin\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isPublicLegal" apps/web/src/middleware.ts` ≥ 1
    - `grep -c "/legal/cgu\|/legal/cgv\|/legal/confidentialite\|/legal/cookies\|/legal/dpo" apps/web/src/components/footer.tsx` ≥ 5
    - `grep -c "Gérer mes cookies" apps/web/src/components/footer.tsx` ≥ 1
    - `wc -l apps/web/src/app/\(public\)/layout.tsx` ≤ 80
    - `wc -l apps/web/src/app/\(admin\)/layout.tsx` ≤ 60
    - `grep -c "role !== 'dirigeant'" apps/web/src/app/\(admin\)/layout.tsx` ≥ 1
    - `pnpm typecheck --filter @tap/web` exit 0
    - `pnpm dev` démarre sans erreur ; `curl -I http://localhost:3000/legal/cgu` → 404 (pas de redirect /login — middleware exclut)
  </acceptance_criteria>
  <done>Middleware /legal/* publics ; layouts (public)/(admin) prêts ; footer legal opérationnel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2 : Bandeau cookies CNIL-conforme + helper localStorage + route API consent</name>
  <files>
    apps/web/src/components/cookie-banner.client.tsx,
    apps/web/src/lib/cookie-consent.ts,
    apps/web/src/lib/supabase/admin.ts,
    apps/web/src/app/api/legal/cookie-consent/route.ts
  </files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Bandeau cookies CNIL-conforme 2026 » (lignes 228-247) + Pitfall 4
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 538-572
    - /home/user/TAP/apps/web/tests/public/cookie-banner.spec.ts (cible Plan 01 — assertions à satisfaire)
    - /home/user/TAP/apps/web/src/components/ui/button.tsx (variants disponibles)
  </read_first>
  <behavior>
    - Test 1 : Visiteur sans consent → banner visible avec 3 boutons
    - Test 2 : Les 3 boutons ont **boundingBox.height identique** (à ±2px) — assertion E2E Playwright
    - Test 3 : Les 3 boutons ont la même classe Tailwind variant + `flex-1` (snapshot)
    - Test 4 : Click « Tout refuser » → `localStorage.cookieConsent.choices = { technique: true, analytics: false, marketing: false }` + POST `/api/legal/cookie-consent`
    - Test 5 : Click « Tout accepter » → `analytics: true, marketing: false` (vide V1)
    - Test 6 : Click « Personnaliser » → ouvre panel avec checkboxes (technique forcé pré-coché disabled)
    - Test 7 : Après consent, banner disparaît (return null si readConsent valide)
    - Test 8 : Consent expire 6 mois → banner réapparaît
    - Test 9 : Footer « Gérer mes cookies » → resetCookieConsent → banner réapparaît
    - Test 10 : POST `/api/legal/cookie-consent` insère ligne dans `cookie_consent_log` (service_role) avec `choices` JSON + user_agent_hash
  </behavior>
  <action>
    **`apps/web/src/lib/cookie-consent.ts`** (≤ 80 lignes) — pure helper :
    ```ts
    'use client';

    export type CookieChoices = { technique: true; analytics: boolean; marketing: boolean };

    type StoredConsent = {
      version: '1';
      choices: CookieChoices;
      expires_at: string; // ISO
    };

    const STORAGE_KEY = 'cookieConsent';
    const CONSENT_VERSION = '1';
    const CONSENT_TTL_MS = 6 * 30 * 24 * 60 * 60 * 1000; // 6 mois

    export function readConsent(): StoredConsent | null {
      if (typeof window === 'undefined') return null;
      try {
        const raw = window.localStorage.getItem(STORAGE_KEY);
        if (!raw) return null;
        const parsed = JSON.parse(raw) as StoredConsent;
        if (parsed.version !== CONSENT_VERSION) return null;
        if (new Date(parsed.expires_at).getTime() < Date.now()) return null;
        return parsed;
      } catch {
        return null;
      }
    }

    export function writeConsent(choices: CookieChoices): void {
      const stored: StoredConsent = {
        version: CONSENT_VERSION,
        choices,
        expires_at: new Date(Date.now() + CONSENT_TTL_MS).toISOString(),
      };
      window.localStorage.setItem(STORAGE_KEY, JSON.stringify(stored));
    }

    export function resetCookieConsent(): void {
      window.localStorage.removeItem(STORAGE_KEY);
      // Recharger pour réafficher le banner
      window.dispatchEvent(new CustomEvent('cookie-consent-reset'));
    }
    ```

    **`apps/web/src/components/cookie-banner.client.tsx`** (≤ 150 lignes) :
    ```tsx
    'use client';
    import { useEffect, useState } from 'react';
    import { Button } from '@/components/ui/button';
    import { readConsent, writeConsent, type CookieChoices } from '@/lib/cookie-consent';

    const ALL_REFUSED: CookieChoices = { technique: true, analytics: false, marketing: false };
    const ALL_ACCEPTED: CookieChoices = { technique: true, analytics: true, marketing: false }; // marketing vide V1

    export function CookieBanner() {
      const [open, setOpen] = useState(false);
      const [customizing, setCustomizing] = useState(false);
      const [choices, setChoices] = useState<CookieChoices>(ALL_REFUSED);

      useEffect(() => {
        setOpen(!readConsent());
        const handler = () => setOpen(true);
        window.addEventListener('cookie-consent-reset', handler);
        return () => window.removeEventListener('cookie-consent-reset', handler);
      }, []);

      if (!open) return null;

      const submit = async (final: CookieChoices) => {
        writeConsent(final);
        try {
          await fetch('/api/legal/cookie-consent', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ choices: final }),
          });
        } catch {
          // Silencieux — consent stocké localStorage prime UX, log côté serveur best-effort
        }
        setOpen(false);
      };

      return (
        <div
          role="dialog"
          aria-label="Préférences cookies"
          aria-describedby="cookie-banner-desc"
          className="fixed bottom-0 inset-x-0 z-50 border-t bg-background shadow-lg"
        >
          <div className="max-w-[1200px] mx-auto px-24 py-24">
            <p id="cookie-banner-desc" className="text-sm mb-16">
              Nous utilisons des cookies pour assurer le fonctionnement de l'application
              et mesurer son usage de manière agrégée. Vous pouvez accepter, refuser ou
              personnaliser votre choix. Vos préférences sont conservées 6 mois.
            </p>
            {customizing ? (
              <CustomizePanel choices={choices} onChange={setChoices} onValidate={() => submit(choices)} />
            ) : (
              // 3 boutons strictement équivalents — taille, couleur, padding
              <div className="flex gap-12">
                <Button
                  variant="outline"
                  className="flex-1 h-44"
                  onClick={() => submit(ALL_REFUSED)}
                  data-testid="cookie-refuse"
                >
                  Tout refuser
                </Button>
                <Button
                  variant="outline"
                  className="flex-1 h-44"
                  onClick={() => setCustomizing(true)}
                  data-testid="cookie-customize"
                >
                  Personnaliser
                </Button>
                <Button
                  variant="outline"
                  className="flex-1 h-44"
                  onClick={() => submit(ALL_ACCEPTED)}
                  data-testid="cookie-accept"
                >
                  Tout accepter
                </Button>
              </div>
            )}
          </div>
        </div>
      );
    }

    function CustomizePanel({ choices, onChange, onValidate }: {
      choices: CookieChoices;
      onChange: (c: CookieChoices) => void;
      onValidate: () => void;
    }) {
      return (
        <div className="space-y-12">
          <label className="flex items-start gap-12 text-sm">
            <input type="checkbox" checked disabled aria-label="Cookies techniques (obligatoires)" />
            <span><strong>Techniques</strong> — session, authentification (obligatoires).</span>
          </label>
          <label className="flex items-start gap-12 text-sm">
            <input
              type="checkbox"
              checked={choices.analytics}
              onChange={(e) => onChange({ ...choices, analytics: e.target.checked })}
            />
            <span><strong>Mesure d'audience</strong> — Sentry (anonymisé).</span>
          </label>
          <label className="flex items-start gap-12 text-sm text-muted-foreground">
            <input type="checkbox" checked={false} disabled aria-label="Marketing — non utilisé en V1" />
            <span><strong>Marketing</strong> — non utilisé dans cette version.</span>
          </label>
          <Button variant="outline" className="h-44" onClick={onValidate}>Valider mes choix</Button>
        </div>
      );
    }
    ```

    **`apps/web/src/lib/supabase/admin.ts`** (≤ 30 lignes) — wrapper centralisé service_role consommé par toutes les routes/actions admin (W-3 DRY) :
    ```ts
    import { createClient } from '@supabase/supabase-js';
    import type { Database } from '@tap/database';

    /**
     * Admin client (service_role) — bypasses RLS.
     * Use ONLY in trusted server contexts: cron, system actions, public routes
     * that need to write logs (cookie consent, breach watchdog, legal request creation).
     *
     * NEVER expose service_role to the browser or include in any response.
     */
    export function createAdminClient() {
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL!;
      const key = process.env.SUPABASE_SERVICE_ROLE_KEY!;
      if (!url || !key) {
        throw new Error('Supabase admin credentials missing');
      }
      return createClient<Database>(url, key, {
        auth: { persistSession: false, autoRefreshToken: false },
      });
    }
    ```

    Ce wrapper sera consommé en Plan 05 (rate-limit, actions portail patient) et tout autre code futur ayant besoin de service_role. Respecte ADR-001 (apps consomme `@tap/*` + ses propres wrappers, pas `@supabase/*` direct).

    **`apps/web/src/app/api/legal/cookie-consent/route.ts`** (≤ 60 lignes) :
    ```ts
    export const runtime = 'nodejs'; // accès service_role
    import { NextResponse } from 'next/server';
    import { createAdminClient } from '@/lib/supabase/admin';
    import crypto from 'node:crypto';
    import { z } from 'zod';

    const bodySchema = z.object({
      choices: z.object({
        technique: z.literal(true),
        analytics: z.boolean(),
        marketing: z.boolean(),
      }),
    });

    export async function POST(req: Request) {
      const json = await req.json().catch(() => null);
      const parsed = bodySchema.safeParse(json);
      if (!parsed.success) {
        return NextResponse.json({ error: 'Choix de cookies invalide.' }, { status: 400 });
      }
      const userAgent = req.headers.get('user-agent') ?? 'unknown';
      const userAgentHash = crypto.createHash('sha256').update(userAgent).digest('hex');
      const sessionToken = crypto.randomBytes(16).toString('hex');
      const sessionTokenHash = crypto.createHash('sha256').update(sessionToken).digest('hex');

      const supabase = createAdminClient();
      const { error } = await supabase.from('cookie_consent_log').insert({
        session_token_hash: sessionTokenHash,
        choices: parsed.data.choices,
        user_agent_hash: userAgentHash,
      });
      if (error) {
        // Pas de stack trace au client (CLAUDE.md § 1)
        return NextResponse.json({ error: "Enregistrement du consentement impossible." }, { status: 500 });
      }
      return NextResponse.json({ ok: true });
    }
    ```

    Aucun emoji, messages français, anti-dark-pattern strict (CNIL 2024-2025).
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck --filter @tap/web && grep -c 'flex-1 h-44' apps/web/src/components/cookie-banner.client.tsx | grep -q '^3$' && pnpm exec playwright test tests/public/cookie-banner.spec.ts --reporter=line 2>&1 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'flex-1 h-44' apps/web/src/components/cookie-banner.client.tsx` = 3 (3 boutons strictement équivalents)
    - `grep -c 'variant="outline"' apps/web/src/components/cookie-banner.client.tsx` ≥ 3 (même variant pour les 3)
    - `wc -l apps/web/src/components/cookie-banner.client.tsx` ≤ 150
    - `wc -l apps/web/src/lib/cookie-consent.ts` ≤ 80
    - `wc -l apps/web/src/app/api/legal/cookie-consent/route.ts` ≤ 60
    - `grep -c 'export const runtime' apps/web/src/app/api/legal/cookie-consent/route.ts` ≥ 1
    - `test -f apps/web/src/lib/supabase/admin.ts`
    - `grep -c "createAdminClient" apps/web/src/lib/supabase/admin.ts` ≥ 1
    - `wc -l apps/web/src/lib/supabase/admin.ts` ≤ 30
    - `grep -c "createAdminClient" apps/web/src/app/api/legal/cookie-consent/route.ts` ≥ 1 (route consomme le wrapper, pas `@supabase/supabase-js` direct)
    - `! grep -E "from '@supabase/supabase-js'" apps/web/src/app/api/legal/cookie-consent/route.ts` (zéro import direct)
    - `grep -ic 'emoji\|🎉\|✅\|❌' apps/web/src/components/cookie-banner.client.tsx` = 0 (pas d'emoji)
    - Aucun `console.log` (`grep -c 'console\.log' apps/web/src/components/cookie-banner.client.tsx apps/web/src/lib/cookie-consent.ts apps/web/src/app/api/legal/cookie-consent/route.ts` = 0)
    - `pnpm exec playwright test tests/public/cookie-banner.spec.ts` GREEN (assertion symétrie boutons + flow consent)
  </acceptance_criteria>
  <done>Banner CNIL-conforme avec 3 boutons symétriques, helper localStorage, route API consent → cookie_consent_log. Test E2E GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/legal/cookie-consent | Visiteur anonyme POST consent — pas d'auth Supabase |
| Server route → cookie_consent_log | service_role bypass RLS |
| middleware.ts → /legal/* | bypass auth check (D-21) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-13 (T3 HIGH) | Elevation of Privilege | bypass middleware admin | mitigate | Layout `(admin)/layout.tsx` SSR check `profile.role !== 'dirigeant'` → redirect('/'). RLS Postgres D-16 dirigeant only en double rideau. |
| T-1.5-14 (T6 MEDIUM) | Tampering | dark pattern cookie banner | mitigate | 3 boutons même `variant="outline"`, `flex-1`, `h-44`. Snapshot Playwright assert boundingBox.height égales. CNIL 2024-2025 enforcement. |
| T-1.5-15 | Information Disclosure | user-agent / IP en clair dans cookie_consent_log | mitigate | Hash SHA-256 du user-agent. Pas d'IP stockée. Session token random côté serveur. |
| T-1.5-16 | Denial of Service | spam POST /api/legal/cookie-consent | accept | Insert simple, table append-only, purge à prévoir si volume — V1.5. Best-effort logging (le client garde localStorage indépendamment). |
| T-1.5-17 | Tampering | bypass middleware via /legal-XXX/foo | mitigate | `pathname.startsWith('/legal')` strict. Tester `/legalX` redirect normalement vers /login. |
</threat_model>

<verification>
- `pnpm typecheck --filter @tap/web` exit 0
- `pnpm dev` démarre — `/legal/cgu` retourne 404 (page absente Wave 3) mais SANS redirect login
- `pnpm exec playwright test tests/public/cookie-banner.spec.ts` GREEN
- `curl -X POST http://localhost:3000/api/legal/cookie-consent -H 'Content-Type: application/json' -d '{"choices":{"technique":true,"analytics":false,"marketing":false}}'` retourne `{"ok":true}` ; ligne insérée dans `cookie_consent_log`
- Visite `/admin/legal/registre` avec compte régulateur (pas dirigeant) → redirect vers `/`
</verification>

<success_criteria>
- 7 fichiers créés/modifiés
- E2E cookie-banner.spec.ts GREEN
- Layouts (public)/(admin) opérationnels
- Aucun emoji, code limits respectés
- Commit français : `feat(1.5): layouts public/admin + bandeau cookies CNIL-conforme`
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-dpa-rgpd-compliance/1.5-04-SUMMARY.md`.
</output>

## Revision Log

- **2026-05-06 — W-3 fix (iteration 2/3)** : Wrapper `apps/web/src/lib/supabase/admin.ts` créé en Plan 04. Plans 05 et 06 importent depuis `@/lib/supabase/admin` au lieu de `@supabase/supabase-js` direct. Élimine 3 factories dupliquées et respecte ADR-001 (apps consomme `@tap/*` + ses propres wrappers, pas `@supabase/*` direct).
