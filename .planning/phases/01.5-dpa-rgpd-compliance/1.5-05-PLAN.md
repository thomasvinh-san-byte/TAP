---
phase: 01.5-dpa-rgpd-compliance
plan: 05
type: execute
wave: 3
depends_on: ["1.5-04"]
files_modified:
  - apps/web/src/content/legal/cgu.md
  - apps/web/src/content/legal/cgv.md
  - apps/web/src/content/legal/confidentialite.md
  - apps/web/src/content/legal/cookies.md
  - apps/web/src/content/legal/dpo.md
  - apps/web/src/app/(public)/legal/cgu/page.tsx
  - apps/web/src/app/(public)/legal/cgv/page.tsx
  - apps/web/src/app/(public)/legal/confidentialite/page.tsx
  - apps/web/src/app/(public)/legal/cookies/page.tsx
  - apps/web/src/app/(public)/legal/dpo/page.tsx
  - apps/web/src/app/(public)/legal/_lib/load-legal.ts
  - apps/web/src/app/(public)/legal/_components/last-updated.tsx
  - apps/web/src/app/(public)/legal/request/[token]/page.tsx
  - apps/web/src/app/(public)/legal/request/[token]/actions.ts
  - apps/web/src/app/(public)/legal/request/[token]/_lib/rate-limit.ts
  - apps/web/src/app/(public)/legal/request/[token]/_components/identity-form.client.tsx
  - apps/web/src/app/(public)/legal/request/[token]/access/page.tsx
  - apps/web/src/app/(public)/legal/request/[token]/erasure/page.tsx
autonomous: true
requirements: [DPA-04, DPA-05, DPA-06, DPA-07]

must_haves:
  truths:
    - "Pages /legal/cgu, /cgv, /confidentialite, /cookies, /dpo accessibles sans auth (200 OK), rendues SSG depuis Markdown frontmatter"
    - "Page /legal/dpo affiche organizations.dpo_contact_email + procédure droits"
    - "/legal/request/[token] vérifie JWT (signature + expiry + algo HS256), incrémente legal_request_attempts, rejette > 5/h"
    - "Identity check : NIR + nom + DDN comparés au patient lié à la request_id"
    - "Page /access fournit JSON portable (helper generatePatientDataExport) + status 'satisfaite' + audit log"
    - "Page /erasure invoque anonymizePatient (UPDATE only, jamais DELETE)"
    - "Tests E2E legal-pages.spec.ts + access-flow.spec.ts (Plan 01) GREEN"
  artifacts:
    - path: "apps/web/src/app/(public)/legal/_lib/load-legal.ts"
      provides: "Helper SSG load Markdown + frontmatter"
      max_lines: 60
    - path: "apps/web/src/app/(public)/legal/request/[token]/page.tsx"
      provides: "Verify token → identity check form"
      max_lines: 150
    - path: "apps/web/src/app/(public)/legal/request/[token]/actions.ts"
      provides: "Server Actions service_role : verify identity, fulfill access, fulfill erasure"
      max_lines: 200
    - path: "apps/web/src/app/(public)/legal/request/[token]/_lib/rate-limit.ts"
      provides: "Check 5 attempts/h via legal_request_attempts"
      max_lines: 50
    - path: "apps/web/src/content/legal/cgu.md"
      provides: "Template CGU avec frontmatter version"
      contains: "version:"
  key_links:
    - from: "page [token]"
      to: "verifyRequestToken (legal-token.ts)"
      via: "import @tap/shared"
      pattern: "verifyRequestToken"
    - from: "actions.ts erasure"
      to: "anonymizePatient (patient-anonymize.ts)"
      via: "import @tap/shared"
      pattern: "anonymizePatient"
    - from: "actions.ts access"
      to: "generatePatientDataExport"
      via: "import @tap/shared"
      pattern: "generatePatientDataExport"
    - from: "rate-limit.ts"
      to: "legal_request_attempts"
      via: "supabase.from('legal_request_attempts').select count + insert"
      pattern: "legal_request_attempts"
---

<objective>
**Wave 3A — Pages publiques /legal/* + portail patient**. Rendre les 5 pages légales SSG depuis Markdown (D-13) + le flow complet du portail patient (D-10) : verify token → identity check → exercer droit accès (art. 15+20) ou effacement (art. 17). Tous les helpers Wave 1B sont consommés ici.

Purpose: Plan parallèle au Plan 06 (admin CRUD) — file ownership disjoint sous `/legal/*` vs `/admin/legal/*`.
Output: 18 fichiers (5 markdown + 13 TS/TSX). Tests E2E legal-pages + access-flow GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md
@CLAUDE.md
@apps/web/src/app/(app)/patients/page.tsx
@apps/web/src/app/(app)/patients/actions.ts
@packages/shared/src/index.ts

<interfaces>
<!-- Wave 1B livré : -->
```typescript
import {
  signRequestToken, verifyRequestToken, type RequestTokenClaims,
  generatePatientDataExport, type PatientDataExport,
  anonymizePatient,
  dataRequestSchema, dataRequestTypeEnum,
  nirFormatSchema, // commun.ts existant
} from '@tap/shared';
```

<!-- Wave 1A livré : tables data_breach_incident, patient_data_request, legal_request_attempts existent + RPC rgpd_anonymize_patient -->

<!-- Wave 2 livré : layout (public) wraps automatiquement, middleware exclut /legal/* -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 5.1 : Pages SSG /legal/{cgu,cgv,confidentialite,cookies,dpo} depuis Markdown</name>
  <files>
    apps/web/src/content/legal/cgu.md,
    apps/web/src/content/legal/cgv.md,
    apps/web/src/content/legal/confidentialite.md,
    apps/web/src/content/legal/cookies.md,
    apps/web/src/content/legal/dpo.md,
    apps/web/src/app/(public)/legal/cgu/page.tsx,
    apps/web/src/app/(public)/legal/cgv/page.tsx,
    apps/web/src/app/(public)/legal/confidentialite/page.tsx,
    apps/web/src/app/(public)/legal/cookies/page.tsx,
    apps/web/src/app/(public)/legal/dpo/page.tsx,
    apps/web/src/app/(public)/legal/_lib/load-legal.ts,
    apps/web/src/app/(public)/legal/_components/last-updated.tsx
  </files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Markdown rendering » (lignes 200-227) + frontmatter format
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 329-352
    - /home/user/TAP/apps/web/src/app/\(app\)/patients/page.tsx (pattern RSC + metadata)
  </read_first>
  <action>
    **5 fichiers Markdown** (`apps/web/src/content/legal/*.md`) — frontmatter standard + corps français professionnel sobre :

    Exemple `cgu.md` (templates similaires pour les 4 autres) :
    ```markdown
    ---
    title: Conditions Générales d'Utilisation
    version: 2026-05-08
    effective_at: 2026-05-15
    ---

    # Conditions Générales d'Utilisation

    Dernière mise à jour : <LastUpdated date="2026-05-08" />

    ## 1. Objet

    Les présentes CGU régissent l'utilisation de la plateforme TAP Régulation
    (ci-après « le Service ») éditée par … pour les sociétés de transport
    sanitaire et taxis conventionnés CGSS exerçant à La Réunion.

    ## 2. Identité du responsable de traitement

    [À compléter par chaque société cliente].

    ## 3. Acceptation

    L'utilisation du Service emporte acceptation pleine et entière des présentes
    CGU. En cas de mise à jour, l'utilisateur est notifié et invité à accepter
    la nouvelle version.

    ## 4. Données personnelles

    Voir notre [Politique de confidentialité](/legal/confidentialite) pour les
    modalités de traitement des données personnelles, conformément au RGPD et à
    la Loi Informatique et Libertés.

    ## 5. Droits des personnes

    Conformément aux articles 15 à 21 du RGPD, vous disposez de droits d'accès,
    de rectification, d'effacement, de limitation, de portabilité et d'opposition.
    Pour les exercer, contactez le DPO via la page [Contact DPO](/legal/dpo) ou
    initiez une demande directement depuis le [portail droits des personnes](/legal/request/new).

    ## 6. Recours

    En cas de désaccord, vous pouvez introduire une réclamation auprès de la CNIL
    (cnil.fr).
    ```

    Templates similaires pour `cgv.md`, `confidentialite.md`, `cookies.md`, `dpo.md`. Chaque markdown a frontmatter `title`/`version`/`effective_at` minimum.

    Aucune emoji. Sobre. Pour `confidentialite.md` : inclure obligations CNIL santé (référentiel 2024) — finalités, base légale, durées, droits, DPO contact, recours CNIL. Pour `dpo.md` : inclure placeholder `<DpoContact organizationId="…" />` (composant à fournir Plan 06 si dynamique, sinon mention statique « consultez votre établissement »).

    **`apps/web/src/app/(public)/legal/_lib/load-legal.ts`** (≤ 60 lignes) — helper SSG :
    ```ts
    import { promises as fs } from 'node:fs';
    import path from 'node:path';
    import matter from 'gray-matter';
    import { compileMDX } from 'next-mdx-remote/rsc';
    import { LastUpdated } from '../_components/last-updated';

    type LegalSlug = 'cgu' | 'cgv' | 'confidentialite' | 'cookies' | 'dpo';

    type LegalFrontmatter = {
      title: string;
      version: string;
      effective_at: string;
    };

    export async function loadLegalDoc(slug: LegalSlug) {
      const filePath = path.join(process.cwd(), 'src/content/legal', `${slug}.md`);
      const raw = await fs.readFile(filePath, 'utf-8');
      const { data, content } = matter(raw);
      const fm = data as LegalFrontmatter;
      const { content: rendered } = await compileMDX({
        source: content,
        components: { LastUpdated },
        options: { parseFrontmatter: false },
      });
      return { frontmatter: fm, rendered };
    }
    ```

    **`apps/web/src/app/(public)/legal/_components/last-updated.tsx`** (≤ 20 lignes) :
    ```tsx
    export function LastUpdated({ date }: { date: string }) {
      return (
        <span className="text-sm text-muted-foreground">
          Dernière mise à jour : {new Date(date).toLocaleDateString('fr-FR', {
            year: 'numeric', month: 'long', day: 'numeric',
          })}
        </span>
      );
    }
    ```

    **5 pages `apps/web/src/app/(public)/legal/<slug>/page.tsx`** (chacune ≤ 40 lignes, RSC) :
    ```tsx
    import type { Metadata } from 'next';
    import { loadLegalDoc } from '../_lib/load-legal';

    export const dynamic = 'force-static'; // SSG

    export async function generateMetadata(): Promise<Metadata> {
      const { frontmatter } = await loadLegalDoc('cgu');
      return { title: frontmatter.title };
    }

    export default async function CguPage() {
      const { frontmatter, rendered } = await loadLegalDoc('cgu');
      return (
        <article className="prose prose-sm max-w-none">
          <h1 className="text-2xl font-semibold tracking-tight mb-12">{frontmatter.title}</h1>
          <p className="text-sm text-muted-foreground mb-32">Version : {frontmatter.version}</p>
          {rendered}
        </article>
      );
    }
    ```

    Idem pour cgv, confidentialite, cookies, dpo (changer le slug).

    Note : le composant `<DpoContact />` (dynamique avec organisation_id) est V1.5 si jugé nécessaire — V1 statique « voir votre établissement » suffit.

    Aucun emoji. Spacing 12/16/24/32 strict.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm build --filter @tap/web 2>&1 | grep -E "(legal/cgu|legal/cgv|legal/confidentialite|legal/cookies|legal/dpo)" | wc -l | grep -q '^[5-9]$\|^1[0-9]$' && pnpm exec playwright test tests/public/legal-pages.spec.ts --reporter=line 2>&1 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - 5 fichiers .md créés, chacun avec frontmatter `title:`, `version:`, `effective_at:`
    - 5 pages page.tsx créées sous `apps/web/src/app/(public)/legal/<slug>/`
    - `grep -c "force-static" apps/web/src/app/\(public\)/legal/cgu/page.tsx` ≥ 1 (SSG)
    - `pnpm build --filter @tap/web` génère 5 routes statiques /legal/*
    - `curl -I http://localhost:3000/legal/cgu` → 200 (sans login)
    - `pnpm exec playwright test tests/public/legal-pages.spec.ts` GREEN
    - Aucune emoji dans les .md ou les pages
  </acceptance_criteria>
  <done>5 pages legal SSG accessibles publiquement, rendues depuis Markdown.</done>
</task>

<task type="auto">
  <name>Task 5.2 : Portail patient — verify token + rate limit + identity check</name>
  <files>
    apps/web/src/app/(public)/legal/request/[token]/page.tsx,
    apps/web/src/app/(public)/legal/request/[token]/actions.ts,
    apps/web/src/app/(public)/legal/request/[token]/_lib/rate-limit.ts,
    apps/web/src/app/(public)/legal/request/[token]/_components/identity-form.client.tsx
  </files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Server Action workflow droit accès » (lignes 555-573) + § rate-limit (lignes 707-717)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 356-396
    - /home/user/TAP/apps/web/src/app/\(app\)/patients/actions.ts (pattern Server Action zod → auth → mutation → audit → revalidate)
    - /home/user/TAP/packages/shared/src/utils/legal-token.ts (Wave 1B)
  </read_first>
  <action>
    **`_lib/rate-limit.ts`** (≤ 50 lignes) :
    ```ts
    import crypto from 'node:crypto';
    import { createAdminClient } from '@/lib/supabase/admin';

    function tokenHash(token: string): string {
      return crypto.createHash('sha256').update(token).digest('hex');
    }

    /**
     * Vérifie le quota 5 tentatives/heure pour un token portail patient (D-20).
     * Throw avec message FR si dépassé.
     */
    export async function checkRateLimit(token: string): Promise<void> {
      const sb = createAdminClient();
      const hash = tokenHash(token);
      const { count } = await sb
        .from('legal_request_attempts')
        .select('*', { count: 'exact', head: true })
        .eq('token_hash', hash)
        .gte('attempted_at', new Date(Date.now() - 3600_000).toISOString());
      if ((count ?? 0) >= 5) {
        throw new Error('Trop de tentatives. Réessayez dans 1 heure.');
      }
      await sb.from('legal_request_attempts').insert({ token_hash: hash, success: false });
    }

    export async function markAttemptSuccess(token: string): Promise<void> {
      const sb = createAdminClient();
      await sb.from('legal_request_attempts').insert({
        token_hash: tokenHash(token),
        success: true,
      });
    }
    ```

    **`page.tsx`** (≤ 150 lignes) :
    ```tsx
    import { notFound } from 'next/navigation';
    import { verifyRequestToken } from '@tap/shared';
    import { checkRateLimit } from './_lib/rate-limit';
    import { IdentityForm } from './_components/identity-form.client';
    import { verifyIdentityAction } from './actions';

    export const dynamic = 'force-dynamic';
    export const runtime = 'nodejs';

    export default async function RequestTokenPage({ params }: { params: { token: string } }) {
      try {
        await checkRateLimit(params.token);
        const claims = await verifyRequestToken(params.token);
        // claims = { sub: requestId, org, type }
        return (
          <div className="space-y-32">
            <header>
              <h1 className="text-2xl font-semibold tracking-tight mb-12">Vérification d'identité</h1>
              <p className="text-sm text-muted-foreground">
                Pour exercer votre droit ({claims.type}), nous devons vérifier votre identité.
                Veuillez saisir votre numéro de sécurité sociale (NIR), votre nom et votre date
                de naissance.
              </p>
            </header>
            <IdentityForm
              token={params.token}
              requestType={claims.type}
              action={verifyIdentityAction}
            />
          </div>
        );
      } catch (e) {
        // Token invalide / expiré / rate-limit
        return (
          <div className="space-y-16">
            <h1 className="text-2xl font-semibold">Lien invalide ou expiré</h1>
            <p className="text-sm text-muted-foreground">
              {e instanceof Error ? e.message : 'Lien invalide ou expiré.'}
            </p>
            <p className="text-sm">
              Pour formuler une nouvelle demande, contactez le service client de votre transporteur.
            </p>
          </div>
        );
      }
    }
    ```

    **`actions.ts`** (≤ 200 lignes) — 3 Server Actions : `verifyIdentityAction`, `fulfillAccessAction`, `fulfillErasureAction` :
    ```ts
    'use server';
    import { createAdminClient } from '@/lib/supabase/admin';
    import { redirect } from 'next/navigation';
    import { z } from 'zod';
    import {
      verifyRequestToken, generatePatientDataExport, anonymizePatient, nirFormatSchema,
    } from '@tap/shared';
    import { checkRateLimit, markAttemptSuccess } from './_lib/rate-limit';

    type ActionState = { error?: string };

    const identitySchema = z.object({
      nir: nirFormatSchema,
      nom: z.string().trim().min(1, 'Nom requis.').max(80),
      date_naissance: z.coerce.date({ errorMap: () => ({ message: 'Date de naissance invalide.' }) }),
    });

    export async function verifyIdentityAction(
      _prev: ActionState,
      formData: FormData,
    ): Promise<ActionState> {
      const token = String(formData.get('token') ?? '');
      try {
        await checkRateLimit(token);
        const claims = await verifyRequestToken(token);

        const parsed = identitySchema.safeParse({
          nir: formData.get('nir'),
          nom: formData.get('nom'),
          date_naissance: formData.get('date_naissance'),
        });
        if (!parsed.success) {
          return { error: parsed.error.errors[0]?.message ?? 'Saisie invalide.' };
        }

        const sb = createAdminClient();
        const { data: request } = await sb
          .from('patient_data_request')
          .select('id, patient_id, request_type, organization_id, status')
          .eq('id', claims.sub)
          .single();
        if (!request || request.status !== 'recue') {
          return { error: 'Demande introuvable ou déjà traitée.' };
        }

        // Vérification identité : appel Edge Function nir.search-by-fields ou comparaison directe
        // (V1 simple : comparer nom + date_naissance + nir_search_hash)
        // Note : nir_search_hash calculé via la même Edge Function NIR Phase 1.
        const { data: matchedPatient } = await sb.rpc('nir_match_patient_for_legal_request', {
          p_request_id: request.id,
          p_nir: parsed.data.nir,
          p_nom: parsed.data.nom,
          p_date_naissance: parsed.data.date_naissance.toISOString().slice(0, 10),
        });
        if (!matchedPatient) {
          return { error: 'Identité non reconnue. Vérifiez vos informations ou contactez le DPO.' };
        }

        await markAttemptSuccess(token);
        // Redirect vers la page spécifique au type de demande
        const dest = request.request_type === 'effacement' ? 'erasure' : 'access';
        redirect(`/legal/request/${token}/${dest}`);
      } catch (e) {
        return { error: e instanceof Error ? e.message : 'Erreur inattendue.' };
      }
    }

    export async function fulfillAccessAction(token: string): Promise<{ data?: unknown; error?: string }> {
      try {
        const claims = await verifyRequestToken(token);
        const sb = createAdminClient();
        const { data: request } = await sb
          .from('patient_data_request')
          .select('id, patient_id, organization_id, status')
          .eq('id', claims.sub).single();
        if (!request || !request.patient_id) return { error: 'Demande introuvable.' };
        if (request.status !== 'recue' && request.status !== 'en_cours') {
          return { error: 'Demande déjà traitée.' };
        }
        const data = await generatePatientDataExport(sb, request.patient_id);
        await sb.from('audit_logs').insert({
          organization_id: request.organization_id,
          actor_id: null, actor_role: null,
          action: 'patient.data_request.fulfilled',
          entity_type: 'patient_data_request',
          entity_id: request.id,
          metadata: { type: 'acces' },
        });
        await sb.from('patient_data_request').update({
          status: 'satisfaite',
          response_at: new Date().toISOString(),
        }).eq('id', request.id);
        return { data };
      } catch (e) {
        return { error: e instanceof Error ? e.message : 'Erreur inattendue.' };
      }
    }

    export async function fulfillErasureAction(token: string): Promise<{ ok?: true; error?: string }> {
      try {
        const claims = await verifyRequestToken(token);
        const sb = createAdminClient();
        const { data: request } = await sb
          .from('patient_data_request')
          .select('id, patient_id, organization_id, status')
          .eq('id', claims.sub).single();
        if (!request || !request.patient_id) return { error: 'Demande introuvable.' };
        await anonymizePatient(sb, request.patient_id, request.id);
        await sb.from('patient_data_request').update({
          status: 'satisfaite',
          response_at: new Date().toISOString(),
          response: 'Anonymisation effectuée. Les courses sont conservées 5 ans (CSS L114-19).',
        }).eq('id', request.id);
        return { ok: true };
      } catch (e) {
        return { error: e instanceof Error ? e.message : 'Erreur inattendue.' };
      }
    }
    ```

    **`identity-form.client.tsx`** (≤ 100 lignes) — pattern `patient-form.client.tsx` Phase 1 (`useFormState` + react-dom + champs zod-validated NIR/nom/DDN).

    Note : la RPC `nir_match_patient_for_legal_request` peut être créée en migration additionnelle si nécessaire ; sinon V1 utilise une vérif simple via `nir_search_hash` recalculé par Edge Function NIR Phase 1.

    Tous les messages français. Aucun emoji. Pas de stack trace UI.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck --filter @tap/web && grep -c "verifyRequestToken\|checkRateLimit" apps/web/src/app/\(public\)/legal/request/\[token\]/page.tsx && grep -c "anonymizePatient\|generatePatientDataExport" apps/web/src/app/\(public\)/legal/request/\[token\]/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4 fichiers créés
    - `grep -c 'verifyRequestToken' apps/web/src/app/\(public\)/legal/request/\[token\]/page.tsx` ≥ 1
    - `grep -c 'checkRateLimit' apps/web/src/app/\(public\)/legal/request/\[token\]/page.tsx` ≥ 1
    - `grep -c 'anonymizePatient' apps/web/src/app/\(public\)/legal/request/\[token\]/actions.ts` ≥ 1
    - `grep -c 'generatePatientDataExport' apps/web/src/app/\(public\)/legal/request/\[token\]/actions.ts` ≥ 1
    - `wc -l apps/web/src/app/\(public\)/legal/request/\[token\]/_lib/rate-limit.ts` ≤ 50
    - `grep -rc "from '@/lib/supabase/admin'" apps/web/src/app/\(public\)/legal/request/` ≥ 1 (utilisation du wrapper Plan 04)
    - `! grep -rE "createClient.*supabase-js" apps/web/src/app/\(public\)/legal/` (zéro import direct `@supabase/supabase-js` — doit passer par le wrapper)
    - `pnpm typecheck --filter @tap/web` exit 0
  </acceptance_criteria>
  <done>Portail patient verify token + rate limit + identity form. Sécurité D-10 + D-20 respectée.</done>
</task>

<task type="auto">
  <name>Task 5.3 : Pages /access et /erasure du portail patient (consommer Server Actions)</name>
  <files>
    apps/web/src/app/(public)/legal/request/[token]/access/page.tsx,
    apps/web/src/app/(public)/legal/request/[token]/erasure/page.tsx
  </files>
  <read_first>
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Server Action workflow droit accès » (lignes 555-573)
    - apps/web/tests/portail/access-flow.spec.ts (cible Plan 01)
  </read_first>
  <action>
    **`access/page.tsx`** (≤ 100 lignes) :
    - Server Component qui invoque `fulfillAccessAction(params.token)` au load
    - Si `data` retourné : afficher résumé patient + bouton « Télécharger JSON » (Blob client-side) + bouton « Télécharger PDF » (lien vers route Plan 06 si livré)
    - Si `error` : afficher message FR sobre

    Snippet :
    ```tsx
    import { fulfillAccessAction } from '../actions';
    import { DownloadJsonButton } from './_components/download-json.client';

    export const runtime = 'nodejs';
    export const dynamic = 'force-dynamic';

    export default async function AccessPage({ params }: { params: { token: string } }) {
      const result = await fulfillAccessAction(params.token);
      if (result.error) {
        return <p className="text-sm text-destructive">{result.error}</p>;
      }
      return (
        <div className="space-y-24">
          <h1 className="text-2xl font-semibold tracking-tight">Vos données</h1>
          <p className="text-sm text-muted-foreground">
            Conformément aux articles 15 et 20 du RGPD, vous trouverez ci-dessous l'ensemble
            des données vous concernant. Vous pouvez les télécharger au format JSON portable.
          </p>
          <DownloadJsonButton data={result.data} />
        </div>
      );
    }
    ```

    Le `DownloadJsonButton` est un client component minimal (≤ 30 lignes) qui crée un Blob et déclenche le download.

    **`erasure/page.tsx`** (≤ 100 lignes) :
    - Affiche d'abord un avertissement : « L'effacement de vos données identifiantes est irréversible. Les courses passées seront conservées 5 ans pour les obligations CGSS (CSS L114-19) sous forme anonymisée. »
    - Bouton « Confirmer l'anonymisation » → form action invoquant `fulfillErasureAction`
    - Confirmation modale shadcn/ui (CLAUDE.md § 5 — action destructive)
    - Après succès : page de confirmation avec lien recours CNIL

    ```tsx
    'use client';
    import { useState } from 'react';
    import { Button } from '@/components/ui/button';
    import { fulfillErasureAction } from '../actions';

    export default function ErasureClient({ token }: { token: string }) {
      const [confirming, setConfirming] = useState(false);
      const [result, setResult] = useState<{ ok?: true; error?: string } | null>(null);

      const onConfirm = async () => {
        const r = await fulfillErasureAction(token);
        setResult(r);
      };

      if (result?.ok) {
        return (
          <div className="space-y-12">
            <h2 className="text-xl font-semibold">Effacement effectué</h2>
            <p className="text-sm text-muted-foreground">
              Vos données identifiantes ont été anonymisées. Conformément à la durée
              légale CGSS (5 ans), les courses passées sont conservées sans lien
              identitaire. En cas de désaccord, vous pouvez saisir la CNIL.
            </p>
          </div>
        );
      }
      // ...
    }
    ```

    Note : page séparée wrapper RSC + client component pour confirmation modale.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck --filter @tap/web && pnpm exec playwright test tests/portail/access-flow.spec.ts --reporter=line 2>&1 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - 2 pages créées (`access/page.tsx`, `erasure/page.tsx`)
    - `grep -c 'fulfillAccessAction' apps/web/src/app/\(public\)/legal/request/\[token\]/access/page.tsx` ≥ 1
    - `grep -c 'fulfillErasureAction' apps/web/src/app/\(public\)/legal/request/\[token\]/erasure/page.tsx` ≥ 1
    - `grep -ic 'irréversible\|anonymis' apps/web/src/app/\(public\)/legal/request/\[token\]/erasure/page.tsx` ≥ 2 (avertissement)
    - `pnpm exec playwright test tests/portail/access-flow.spec.ts` GREEN
  </acceptance_criteria>
  <done>Pages /access et /erasure opérationnelles ; flow complet portail patient GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Patient (anonymous) → /legal/request/[token] | Bypass auth Supabase (D-21), authentification via JWT custom |
| Server Action → service_role | Bypass RLS contrôlé, scoped au requestId de la token |
| Helpers @tap/shared → DB | RPC `rgpd_anonymize_patient` SECURITY DEFINER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-18 (T1 HIGH) | Tampering | JWT replay | mitigate | `expirationTime: 30d` + `legal_request_attempts` 5/h. Token stocké en DB (`request_token`) — révocable en marquant `request_token_expires_at = now()` (post-MVP). |
| T-1.5-19 (T7 LOW) | Open Redirect | redirect après identity check | mitigate | redirect strict vers `/legal/request/${token}/(access|erasure)` — pas de `searchParams.return_url` accepté. |
| T-1.5-20 | Information Disclosure | NIR clair en réponse art. 15 sans audit | mitigate | `generatePatientDataExport` retourne `nir: null`. Décryptage NIR explicite via Edge Function dédiée Phase 1 (audit log auto). |
| T-1.5-21 (T4 MEDIUM) | Tampering | erasure DELETE physique | mitigate | `fulfillErasureAction` invoque `anonymizePatient` (UPDATE only). RPC SECURITY DEFINER côté DB applique le garde-fou (Plan 02). Avertissement explicite « irréversible » côté UI. |
| T-1.5-22 | Spoofing | identity check faible (NIR seul) | mitigate | Triple check : NIR + nom + DDN. Comparé via `nir_match_patient_for_legal_request` (NIR hashé + égalité nom + DDN). Rate limit 5/h. |
</threat_model>

<verification>
- `pnpm build --filter @tap/web` génère pages SSG /legal/{cgu,cgv,confidentialite,cookies,dpo}
- `pnpm exec playwright test tests/public/legal-pages.spec.ts` GREEN (5 pages 200 OK sans auth)
- `pnpm exec playwright test tests/portail/access-flow.spec.ts` GREEN (token → identity → export JSON)
- Manual : token JWT signé valide → /legal/request/{token} affiche identity form ; mauvais NIR → message FR
- `curl -I http://localhost:3000/legal/request/invalid-token` → page « Lien invalide ou expiré »
</verification>

<success_criteria>
- 18 fichiers créés (5 .md + 13 TS/TSX)
- Tests E2E legal-pages + access-flow GREEN
- Helpers Wave 1B consommés correctement
- Aucun emoji, code limits respectés (300/150/50)
- Commit français : `feat(1.5): pages legal SSG + portail patient (verify token, identity, access, erasure)`
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-dpa-rgpd-compliance/1.5-05-SUMMARY.md`.
</output>

## Revision Log

- **2026-05-06 — W-3 fix (iteration 2/3)** : Wrapper `apps/web/src/lib/supabase/admin.ts` créé en Plan 04. Plans 05 et 06 importent depuis `@/lib/supabase/admin` au lieu de `@supabase/supabase-js` direct. Élimine 3 factories dupliquées et respecte ADR-001 (apps consomme `@tap/*` + ses propres wrappers, pas `@supabase/*` direct).
