---
phase: 01.5-dpa-rgpd-compliance
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/package.json
  - packages/shared/package.json
  - supabase/tests/data_processing_register_rls.sql
  - supabase/tests/dpa_record_rls.sql
  - supabase/tests/dpia_record_rls.sql
  - supabase/tests/data_breach_incident_rls.sql
  - supabase/tests/patient_data_request_rls.sql
  - supabase/tests/check_breach_deadlines.sql
  - supabase/tests/rgpd_anonymize_patient.sql
  - packages/shared/src/utils/__tests__/patient-data-export.test.ts
  - packages/shared/src/utils/__tests__/patient-anonymize.test.ts
  - packages/shared/src/utils/__tests__/legal-token.test.ts
  - apps/web/tests/admin/registre-pdf.spec.ts
  - apps/web/tests/admin/dpia.spec.ts
  - apps/web/tests/admin/breach-countdown.spec.ts
  - apps/web/tests/public/legal-pages.spec.ts
  - apps/web/tests/public/cookie-banner.spec.ts
  - apps/web/tests/portail/access-flow.spec.ts
autonomous: true
requirements: [DPA-01, DPA-02, DPA-03, DPA-04, DPA-05, DPA-06, DPA-07, DPA-08]

must_haves:
  truths:
    - "Toutes les nouvelles deps (@react-pdf/renderer, jose, next-mdx-remote, gray-matter) installées et résolvables"
    - "Tous les fichiers de tests (pgTAP, Vitest, Playwright) existent en RED — appel direct aux symboles à venir"
    - "Aucune nouvelle dépendance n'apparaît hors de apps/web et packages/shared"
  artifacts:
    - path: "apps/web/package.json"
      provides: "deps @react-pdf/renderer@4.5.1, next-mdx-remote@6.0.0, jose@6.2.3, gray-matter@4.x"
      contains: "@react-pdf/renderer"
    - path: "supabase/tests/data_processing_register_rls.sql"
      provides: "RLS isolation tenant + dirigeant-only INSERT pgTAP"
      min_lines: 40
    - path: "supabase/tests/check_breach_deadlines.sql"
      provides: "Watchdog 72h test (mock now())"
      min_lines: 30
    - path: "packages/shared/src/utils/__tests__/legal-token.test.ts"
      provides: "Tests JWT sign/verify, expired, malformed"
      min_lines: 25
    - path: "apps/web/tests/portail/access-flow.spec.ts"
      provides: "E2E portail patient — token → identité → export"
      min_lines: 30
  key_links:
    - from: "apps/web/package.json"
      to: "node_modules/@react-pdf/renderer"
      via: "pnpm install"
      pattern: '"@react-pdf/renderer":\s*"4'
    - from: "supabase/tests/*.sql"
      to: "supabase/tests/patients.sql"
      via: "fixtures Alpha/Bravo réutilisées"
      pattern: '11111111-1111-1111-1111-111111111111'
---

<objective>
**Wave 0 — Scaffold tests + deps**. Installer les 4 nouvelles dépendances justifiées (D-13, D-17, RESEARCH §1) dans `apps/web` et créer TOUS les fichiers de tests (pgTAP RLS × 5 + watchdog + RPC anonymize + Vitest helpers × 3 + Playwright × 6) en état RED. Les tests écrivent contre les symboles à venir aux Waves 1-3 — aucune compilation requise pour qu'ils existent.

Purpose: Nyquist gate — chaque task downstream peut référencer une commande `<automated>` qui pointe vers un test existant. RED ⇒ implementation forces tests vert.
Output: 1 commit `chore(1.5): scaffold tests RGPD + 4 deps RGPD justifiées`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md
@.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md
@CLAUDE.md
@supabase/tests/patients.sql
@packages/shared/src/utils

<interfaces>
<!-- Symboles cibles que les tests vont importer (créés Waves 1-3) -->

From packages/shared/src/utils/legal-token.ts (Wave 1, à créer) :
```typescript
export function signRequestToken(claims: { sub: string; org: string; type: string }): Promise<string>;
export function verifyRequestToken(token: string): Promise<{ sub: string; org: string; type: string }>;
```

From packages/shared/src/utils/patient-data-export.ts (Wave 1, à créer) :
```typescript
export type PatientDataExport = {
  format_version: '1.0';
  exported_at: string;
  patient: Record<string, unknown>;
  operational_notes: unknown[];
  constraintes: unknown[];
  audit_log: unknown[];
};
export function generatePatientDataExport(
  supabase: SupabaseClient<Database>,
  patientId: string
): Promise<PatientDataExport>;
```

From packages/shared/src/utils/patient-anonymize.ts (Wave 1, à créer) :
```typescript
export function anonymizePatient(
  supabase: SupabaseClient<Database>,
  patientId: string,
  requestId: string
): Promise<void>;
```

From Supabase RPC (Wave 1, à créer en migration) :
```sql
public.rgpd_anonymize_patient(p_patient_id uuid, p_request_id uuid, p_salt text) returns void;
public.check_breach_deadlines() returns void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1 : Installer 4 deps RGPD justifiées (apps/web + packages/shared)</name>
  <files>apps/web/package.json, packages/shared/package.json</files>
  <read_first>
    - /home/user/TAP/apps/web/package.json (lire dependencies/devDependencies actuels)
    - /home/user/TAP/packages/shared/package.json
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Standard Stack — Nouvelles dépendances » (lignes 348-369)
  </read_first>
  <action>
    Installer dans `apps/web` (workspace pnpm) :
    - `@react-pdf/renderer@4.5.1` (D-17 — export PDF registre, runtime Node)
    - `next-mdx-remote@6.0.0` (D-13 — pages legal Markdown SSG, RSC compat)
    - `gray-matter@^4.0.3` (frontmatter parser, peer next-mdx-remote)
    - `jose@^6.2.3` (D-10 — JWT HS256 portail patient)

    Commande exacte :
    ```bash
    pnpm add @react-pdf/renderer@4.5.1 next-mdx-remote@6.0.0 gray-matter@^4.0.3 jose@^6.2.3 --filter @tap/web
    ```

    Puis ajouter `jose` aussi à `packages/shared` (les helpers `legal-token.ts` y vivent) :
    ```bash
    pnpm add jose@^6.2.3 --filter @tap/shared
    ```

    Vérifier `pnpm why jose` : si déjà transitive (Supabase), conserver l'ajout explicite (pin version locale).

    Justification commit (DEC-011 — commits français) :
    ```
    chore(1.5): scaffold tests RGPD + 4 deps justifiées

    - @react-pdf/renderer 4.5.1 — export PDF registre art. 30 (D-17, RESEARCH §2)
    - next-mdx-remote 6.0.0 — rendu pages legal Markdown SSG (D-13, RESEARCH §3)
    - gray-matter 4.0.3 — frontmatter parsing (peer next-mdx-remote)
    - jose 6.2.3 — JWT HS256 portail patient (D-10, RESEARCH §1)

    Aucune autre dep majeure ajoutée. Stack frozen respecté (CLAUDE.md § 3).
    ```
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm why @react-pdf/renderer next-mdx-remote jose gray-matter --filter @tap/web | grep -E '^(@react-pdf|next-mdx|jose|gray-matter)' | wc -l | grep -q '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@react-pdf/renderer"' apps/web/package.json` ≥ 1
    - `grep -c '"next-mdx-remote"' apps/web/package.json` ≥ 1
    - `grep -c '"jose"' apps/web/package.json` ≥ 1 ET `grep -c '"jose"' packages/shared/package.json` ≥ 1
    - `pnpm install` exit 0
    - `node -e "require.resolve('@react-pdf/renderer')"` exit 0 depuis apps/web
  </acceptance_criteria>
  <done>4 deps installées, lockfile mis à jour, aucune autre lib ajoutée silencieusement.</done>
</task>

<task type="auto">
  <name>Task 1.2 : Scaffold 7 tests pgTAP RLS + watchdog + RPC anonymize</name>
  <files>
    supabase/tests/data_processing_register_rls.sql,
    supabase/tests/dpa_record_rls.sql,
    supabase/tests/dpia_record_rls.sql,
    supabase/tests/data_breach_incident_rls.sql,
    supabase/tests/patient_data_request_rls.sql,
    supabase/tests/check_breach_deadlines.sql,
    supabase/tests/rgpd_anonymize_patient.sql
  </files>
  <read_first>
    - /home/user/TAP/supabase/tests/patients.sql (intégral — pattern fixtures, pattern RLS isolation, pattern audit_logs no leak)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § « supabase/tests/legal_compliance.sql » (lignes 154-219)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-CONTEXT.md § decisions D-05..D-09 (schémas verbatim)
  </read_first>
  <action>
    Créer 7 fichiers pgTAP. Chacun suit le squelette de `supabase/tests/patients.sql` :
    1. `BEGIN; SELECT plan(N);`
    2. Réutiliser les fixtures Alpha (UUID `11111111...`) / Bravo (UUID `22222222...`) + dirigeant Alpha (`aaaaaaaa...`) / dirigeant Bravo (`bbbbbbbb...`) / régulateur Alpha (`cccccccc...`) — voir `patients.sql` lignes 21-57.
    3. Asserts par table :
       - `relrowsecurity` + `relforcerowsecurity` (pattern lignes 62-69 patients.sql)
       - Isolation tenant : Bravo ne voit PAS les lignes Alpha (`set local "request.jwt.claim.sub"`)
       - **Spécifique D-16 dirigeant only** : régulateur Alpha INSERT échoue avec `42501` sur `data_processing_register`, `dpa_record`, `dpia_record`, `data_breach_incident`, `patient_data_request`
       - **Spécifique D-05 versioning** : pas de policy UPDATE sur `data_processing_register` (assert `select count(*) from pg_policies where tablename = 'data_processing_register' and cmd = 'UPDATE'` = 0)
    4. Pour `patient_data_request_rls.sql` (CRITIQUE — Pitfall 2) : assert que `audit_logs.metadata->'new'` ne contient PAS `request_token` ni `requester_proof_of_identity_url` après INSERT (pattern lignes 204-218 patients.sql).
    5. `check_breach_deadlines.sql` :
       - Insérer un `data_breach_incident` avec `detected_at = now() - interval '67 hours'`, `cnil_notification_required = true`, `cnil_notification_at = NULL`
       - `select public.check_breach_deadlines();`
       - Assert `select count(*) from audit_logs where action = 'breach.deadline_warning'` ≥ 1
       - Assert metadata contient `hours_remaining` (numeric > 0 et < 6)
    6. `rgpd_anonymize_patient.sql` :
       - Insérer un patient Alpha avec NIR + adresse complète, puis insérer un `patient_data_request` lié
       - `select public.rgpd_anonymize_patient(patient_id, request_id, 'salt-test');`
       - Assert `nir_encrypted IS NULL`, `nir_search_hash != original` (rehashed), `prenom IS NULL`, `archive = true`, `archive_reason = 'rgpd.art17.anonymisation'`
       - Assert ligne `audit_logs.action = 'patient.anonymized'` créée
       - Assert que les `rides` du patient (s'il y en avait) sont CONSERVÉES (R5 RESEARCH critique)

    En tête de chaque fichier : commentaire FR « Tests RLS table {nom} — Phase 1.5 DPA + RGPD compliance — D-XX ».

    Format d'assertion : `select ok(...)`, `select is(...)`, `select throws_ok(...)`, `select lives_ok(...)`.

    Chaque fichier : `select * from finish(); rollback;`.

    Les tests sont en RED jusqu'à Wave 1 (les tables n'existent pas encore) — c'est attendu.
  </action>
  <verify>
    <automated>cd /home/user/TAP && for f in data_processing_register_rls dpa_record_rls dpia_record_rls data_breach_incident_rls patient_data_request_rls check_breach_deadlines rgpd_anonymize_patient; do test -s "supabase/tests/$f.sql" || { echo "MISSING $f"; exit 1; }; done && grep -l 'select plan' supabase/tests/data_processing_register_rls.sql supabase/tests/check_breach_deadlines.sql supabase/tests/rgpd_anonymize_patient.sql | wc -l | grep -q '^3$'</automated>
  </verify>
  <acceptance_criteria>
    - 7 fichiers .sql existent dans `supabase/tests/` avec ≥ 30 lignes chacun
    - Chacun contient `BEGIN;`, `select plan(`, `select * from finish();`, `rollback;`
    - `grep -c "11111111-1111-1111-1111-111111111111" supabase/tests/data_processing_register_rls.sql` ≥ 1 (réuse fixture Alpha)
    - `patient_data_request_rls.sql` contient assert `not (metadata->'new') ? 'request_token'`
    - `check_breach_deadlines.sql` contient `interval '67 hours'` ET `breach.deadline_warning`
    - `rgpd_anonymize_patient.sql` contient `'patient.anonymized'` ET `rides` (assert preservation)
    - `pnpm db:test` AVANT migration : tous ces tests RED (attendu — tables absentes)
  </acceptance_criteria>
  <done>7 fichiers pgTAP scaffoldés, RED comme attendu, prêts à virer GREEN après Wave 1.</done>
</task>

<task type="auto">
  <name>Task 1.3 : Scaffold 3 tests Vitest helpers + 6 specs Playwright E2E</name>
  <files>
    packages/shared/src/utils/__tests__/patient-data-export.test.ts,
    packages/shared/src/utils/__tests__/patient-anonymize.test.ts,
    packages/shared/src/utils/__tests__/legal-token.test.ts,
    apps/web/tests/admin/registre-pdf.spec.ts,
    apps/web/tests/admin/dpia.spec.ts,
    apps/web/tests/admin/breach-countdown.spec.ts,
    apps/web/tests/public/legal-pages.spec.ts,
    apps/web/tests/public/cookie-banner.spec.ts,
    apps/web/tests/portail/access-flow.spec.ts
  </files>
  <read_first>
    - /home/user/TAP/packages/shared/src/utils/__tests__/patient-note.test.ts (pattern Vitest mock SupabaseClient, lignes 1-40)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-RESEARCH.md § « Validation Architecture (Nyquist) — Phase Requirements → Test Map » (lignes 730-755)
    - /home/user/TAP/.planning/phases/01.5-dpa-rgpd-compliance/1.5-PATTERNS.md § lignes 252-303 (helpers signatures cibles)
  </read_first>
  <action>
    **Vitest (packages/shared)** — 3 fichiers, mock SupabaseClient fluent (pattern `patient-note.test.ts`) :

    1. `patient-data-export.test.ts` (≥ 25 lignes) :
       - `it('exporte format_version 1.0 + exported_at ISO + patient sans NIR clair sans appel Edge')` — assert `data.patient.nir === null` quand mock `has_nir: false`
       - `it('inclut operational_notes, constraintes, audit_log dans la réponse')`
       - `it('échoue avec message FR si patient_id introuvable')`

    2. `patient-anonymize.test.ts` (≥ 20 lignes) :
       - `it('appelle rpc rgpd_anonymize_patient avec p_salt depuis env')` — mock `supabase.rpc`, assert args
       - `it('throw avec message FR si rpc retourne error')`

    3. `legal-token.test.ts` (≥ 30 lignes) :
       - `beforeAll` : `process.env.APP_LEGAL_TOKEN_SECRET = 'a'.repeat(32)`
       - `it('signe puis vérifie un token valide avec sub/org/type')`
       - `it('rejette un token expiré')` (token signé avec `setExpirationTime('-1d')`)
       - `it('rejette un token avec signature invalide')` (modifier dernier caractère)
       - `it('rejette un token malformé')` (string aléatoire)
       - `it('rejette un token avec algo HS512 (algorithme non autorisé)')`

    **Playwright (apps/web/tests)** — 6 fichiers, scaffolds avec `test.fixme` ou `test.skip` quand impl pas prête (RED OK) :

    4. `tests/admin/registre-pdf.spec.ts` :
       - Login dirigeant → /admin/legal/registre → bouton « Exporter PDF » → assert response Content-Type=application/pdf

    5. `tests/admin/dpia.spec.ts` :
       - Login dirigeant → créer DPIA brouillon → editer Markdown → save → status `validee` → re-edit → audit_log

    6. `tests/admin/breach-countdown.spec.ts` :
       - Créer breach via Server Action → assert compteur affiche « J-2 » avec classe `text-success` (> 24h)

    7. `tests/public/legal-pages.spec.ts` :
       - Sans auth, GET /legal/cgu → 200 (pas de redirect login)
       - Idem cgv, confidentialite, cookies, dpo
       - Footer contient 5 liens legal

    8. `tests/public/cookie-banner.spec.ts` :
       - Sans auth, GET / → banner visible
       - **Visual snapshot** : 3 boutons même hauteur (assert `boundingBox.height` égales à ±2px), même couleur de fond (assert même classe Tailwind)
       - Click « Tout refuser » → localStorage.cookieConsent.choices.analytics === false
       - POST `/api/legal/cookie-consent` appelé

    9. `tests/portail/access-flow.spec.ts` :
       - Régulatrice crée patient_data_request type=acces → email simulé contient lien
       - Suivre lien `/legal/request/{token}` → formulaire identité → soumettre NIR+nom+DDN → page export → bouton « Télécharger JSON »
       - Assert JSON contient `format_version: '1.0'` et pas de NIR clair sans appel Edge

    Tous les fichiers en français (`describe('helper export patient', ...)`). RED OK : aucun symbole cible n'existe encore.
  </action>
  <verify>
    <automated>cd /home/user/TAP && for f in patient-data-export patient-anonymize legal-token; do test -s "packages/shared/src/utils/__tests__/$f.test.ts" || exit 1; done && for f in tests/admin/registre-pdf.spec.ts tests/admin/dpia.spec.ts tests/admin/breach-countdown.spec.ts tests/public/legal-pages.spec.ts tests/public/cookie-banner.spec.ts tests/portail/access-flow.spec.ts; do test -s "apps/web/$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 9 fichiers de tests existent, tous ≥ 20 lignes
    - `legal-token.test.ts` contient ≥ 5 cas de test (`it('...')` ≥ 5)
    - `cookie-banner.spec.ts` contient assertion sur `boundingBox` ou hauteur explicite des 3 boutons
    - `access-flow.spec.ts` contient assertion `format_version: '1.0'` ET `nir`
    - `pnpm test --filter @tap/shared` lancé : tests RED (helpers absents) — attendu
    - `pnpm exec playwright test --list | grep -c 'legal\|portail\|admin/registre' ≥ 6
  </acceptance_criteria>
  <done>3 Vitest + 6 Playwright spec scaffoldés en RED, prêts à virer GREEN aux Waves 1-3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 → Waves 1-3 | Tests scaffoldés référencent des symboles à venir — pas de boundary runtime à ce stade |
| pnpm registry | Téléchargement nouvelles deps — confiance npm + lockfile pinning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-01 | Tampering | pnpm install | mitigate | Lockfile pinned (`pnpm-lock.yaml`), versions exactes (4.5.1, 6.0.0, 6.2.3, 4.0.3). PR review obligatoire pour toute modif lockfile. |
| T-1.5-02 | Information Disclosure | secret de test JWT | mitigate | `legal-token.test.ts` utilise secret littéral `'a'.repeat(32)` — JAMAIS le vrai `APP_LEGAL_TOKEN_SECRET`. Test scope `process.env` réinitialisé en `afterAll`. |
| T-1.5-03 | Denial of Service | dep malveillante (npm supply chain) | accept | Versions vérifiées RESEARCH (publish dates 2026-04-15, 2025+) — confiance HIGH. Sentry monitoring runtime ajouté en V1.5. |
</threat_model>

<verification>
- `pnpm install` exit 0 sans warning bloquant
- `pnpm test --filter @tap/shared` : tests RED attendus (3 helpers absents)
- `pnpm db:test` (sans la migration legal — tests RED attendus, tables absentes)
- `pnpm exec playwright test --list` liste les 6 nouveaux specs
</verification>

<success_criteria>
- 4 deps installées et résolvables (`pnpm why` ≥ 4)
- 7 tests pgTAP + 3 Vitest + 6 Playwright créés (16 fichiers tests)
- Aucun symbole d'implémentation créé (Wave 0 strictement scaffolds)
- Commit français unique : `chore(1.5): scaffold tests RGPD + 4 deps justifiées`
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-dpa-rgpd-compliance/1.5-01-SUMMARY.md`.
</output>
