---
phase: 06.10-dettes-techniques-phase-06.7
type: context
status: cadrage
created: 2026-06-01
---

# Phase 06.10 — Dettes techniques Phase 06.7 — CONTEXT

## Phase

**06.10 Dettes techniques Phase 06.7** — phase next après clôture de Phase 06.7 (Wave 4 livrée + walkthrough validé 2026-06-01).

## Goal

Rendre l'optimiseur démontrable sur de vraies données en traitant les dettes D1 (hébergement Python solveur) et D2 (pipeline geocoding au moment création course) identifiées à la clôture de Phase 06.7. Préparer le terrain pour qu'un prospect puisse, lors d'un walkthrough, voir l'optimiseur tourner sur ses propres adresses (post-D2) avec un solveur réel (post-D1).

## Périmètre IN

- **D1 — Hébergement Python solveur OR-Tools** (Wave 1).
  - Wave 1 : tentative Vercel Python `/py/` hors `/api/`. Pattern documenté (issue Vercel #6598, template `vercel/examples/nextjs-flask-starter`) non encore essayé après 5 PR de fix infructueuses. Critère d'abandon ferme : 4 h de travail sans succès.
  - Wave 1bis (conditionnel) : bascule Render Starter Docker (~7 $/mois, container warm). Active uniquement si Wave 1 échoue. ADR-008 sera amendée à ce moment-là pour acter la bascule.
- **D2 — Pipeline geocoding au moment création course** (Wave 2).
  - Audit `AddressPickerField` + fix `createRide` Server Action pour persister `pickup_lat/lng/citycode` + `dropoff_lat/lng/citycode`.
  - Effort estimé selon cas (audit avant engagement) : A=15 min, B=15-30 min, C=2-4 h.
  - Mise à jour seed démo pour éliminer le besoin du seed ad hoc `seed-courses-pour-optimiseur.sql`.

## Périmètre OUT (différé explicitement)

- **D3 — Passe UX complète écran optimisation (lecture B)** : structure visuelle « Plan actuel », polish design system, micro-interactions. Différée car nécessite données réelles (post-D2) et retour terrain régulatrice. Reste tracée dans `docs/dette-technique/2026-06-01-phase-06.7-cloture.md`.
- **D4 — Audit casts `as TYPE[]` (lot 3 audit D+A)** : non urgent depuis lot 2 (PR #202) qui a propagé le destructuring `{ data, error }` sur 16 fichiers. Reste tracée dans `docs/dette-technique/2026-06-01-phase-06.7-cloture.md`.

## Decisions LOCKED héritées

| Decision | Source | Portée |
|---|---|---|
| **DEC-079** Hébergement Vercel Python serverless | ADR-008 + révisions 2026-06-01 | **Conditionnelle au succès Wave 1** — sera rééexaminée si Wave 1bis Render déclenchée. |
| **DEC-080** Commits Wave-by-Wave | DEC-080 LOCKED | S'applique aux deux waves de Phase 06.10. |
| **DEC-081** Indicateurs estimés non contractuels | DEC-081 LOCKED | Préservé tel quel — pas touché par Phase 06.10. |
| **DEC-044** Geocoding BAN gouv.fr ou POI métier | DEC-044 documentée | Note : l'enquête 2026-06-01 a confirmé la migration BAN → Géoplateforme IGN en janvier 2026, endpoint `data.geopf.fr/geocodage/search`. |

## Decisions LOCKED nouvelles

- **ADR-009** Pattern « container long-running pour algorithmes coûteux ». Critères : binaires > 30 MB, cold start > 2 s, burst sans scale, calcul > 1 s/req. Voir `docs/adr/ADR-009-pattern-hebergement-services-couteux.md`.

## Doctrines applicables

- **D-08** Dé-identification solveur — le payload envoyé au solveur reste géométrique + horaires + IDs opaques uniquement. L'enrichissement UI reste côté Route Handler authentifié (Wave 4 acquis).
- **D-19** Indicateurs métier — taux mutualisation + km à vide estimés, mention « estimé » obligatoire, Haversine non contractuel.
- **NFR-001** Aucun nom propre dans les logs, commits, code, doc générés par Phase 06.10.

## Architecture watch — 3 patterns transversaux à respecter

Issus de l'enquête open-source 2026-06-01 (`docs/dette-technique/2026-06-01-enquete-open-source-dettes.md`) :

1. **Pas de serverless pour algorithmes coûteux** (ADR-009). Si Wave 1 échoue, Wave 1bis est légitime sans hésitation.
2. **Geocoding au moment de la saisie UI**, pas du persist. Ne pas faire d'appel HTTP au persist en Wave 2 — l'autocomplete BAN/Géoplateforme retourne déjà les coords dans le `onChange`.
3. **BFF / Route Handler enrichit la réponse** du microservice avant UI. Pattern déjà appliqué en Wave 4 (`enrichProposal()` dans `apps/web/src/app/api/optimizer/route.ts`) — à préserver lors d'éventuels refactors.

## Pré-requis

- Phase 06.7 close (validation walkthrough Wave 4 OK 2026-06-01).
- PR #205 (synchro STATE) mergée.
- ADR-009 acté.
- Mock optimizer reste actif en production tant que Wave 1 ou 1bis n'est pas validée par walkthrough complet.

## Hors scope (boundary)

- Pas de modification du contrat zod (`CONTRACT_VERSION='1'` reste source de vérité).
- Pas de modification du code Python métier (`solver.py`, `models.py`, `haversine.py`, `_extract.py`).
- Pas de modification du contrat front-end OR-Tools optimizer screen.
- Pas de refactor du composant `AddressPickerField` si cas A ou B suffit (cas C uniquement si nécessaire).
- Pas de migration des courses historiques sans coords (script séparé, hors Phase 06.10).
