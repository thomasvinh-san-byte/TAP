# Phase 06.23 — CONTEXT

> Durcissements moyens groupés : audit DEC-041 complet + tests métier ciblés.
> Clôt la dette DEC-041 reportée + ferme les angles morts mesurés.

## Volet A — Audit complet du pattern DEC-041 (24 SA avec mutations)

### Pourquoi

DEC-041 : après UPDATE/DELETE sur table RLS, faire `.select('id')` + vérifier `data.length === 0` → sinon RLS rejette SILENCIEUSEMENT (0 ligne, pas d'erreur SQL) et l'app affiche un faux succès. Incident réel 15/05 (startRide/endRide). Le pattern n'a été appliqué qu'à un échantillon ; audit exhaustif reporté ici.

### Tableau récap

| Action | Mutation | Table | Pattern requis ? | Avant 06.23 | Après 06.23 |
|---|---|---|---|---|---|
| `(auth)/accept-invite` | UPDATE driver_invitations | RLS | OUI | ❌ | ✅ |
| `(auth)/accept-invite` | UPDATE drivers (rattachement) | RLS | OUI | ❌ | ✅ |
| `(app)/courses/assignment::assign` | UPDATE rides | RLS | OUI | ❌ | ✅ |
| `(app)/courses/assignment::assignVehicle` | UPDATE rides | RLS | OUI | ❌ | ✅ |
| `(app)/courses/assignment::unassign` | UPDATE rides | RLS | OUI | ❌ | ✅ |
| `(app)/courses/payment` | UPDATE rides | RLS | OUI | ❌ | ✅ |
| `(admin)/admin/legal/dpia::update` | UPDATE dpia_record | RLS | OUI | ❌ | ✅ |
| `(admin)/admin/legal/breaches::close` | UPDATE data_breach_incident | RLS | OUI | ❌ | ✅ |
| `(admin)/admin/legal/dpo::save` | UPDATE organizations | RLS | OUI | ❌ | ✅ |
| `(admin)/admin/legal/requests::create token` | UPDATE patient_data_request | RLS | OUI | ❌ | ✅ |
| `(admin)/admin/legal/requests::updateStatus` | UPDATE patient_data_request | RLS | OUI | ❌ | ✅ |
| `(admin)/admin/legal/_actions/cgu-accept` | UPDATE profiles | RLS | OUI | ❌ | ✅ |
| `(admin)/admin/sms-templates::update` | UPDATE sms_templates | RLS | OUI | ✅ (déjà fait) | ✅ |
| `setup/actions` | `url.searchParams.delete` | N/A | N/A | — | — |
| `(public)/legal/request/[token]::*` | UPDATE patient_data_request | RLS bypass | N/A (service_role) | — | — |

**Bilan** : **11 vrais trous comblés** + 1 confirmation false-positive (sms-templates avait déjà le pattern) + 2 N/A documentés.

### Décisions

#### D-A1 — Auditer, pas appliquer aveuglément
Lecture de chaque mutation avant correction. INSERT seul → non concerné (l'INSERT échoue franchement sur RLS). UPDATE sur service-role → N/A (bypass RLS légitime). Seuls les UPDATE/DELETE sur table RLS via client utilisateur sont corrigés.

#### D-A2 — Ne pas changer le comportement métier
Le check ajoute une branche d'erreur sur droits insuffisants ; ne pas toucher à la logique existante. Message standard : « Modification refusée — droits insuffisants ou [entité] absente. »

## Volet B — Tests unitaires logique métier (angles morts)

### Pourquoi

DEC-013 : pricing 100%, recurrence 100% (déjà tenus). Reste à fermer les angles morts mesurés de la logique à risque.

### Mesure (vitest --coverage)

| Module | Branches AVANT | Branches APRÈS | Δ |
|---|---|---|---|
| `@tap/pricing` | 100 % | 100 % | maintien |
| `@tap/recurrence` | 100 % | 100 % | maintien |
| `lib/optimizer/solve-local.ts` | 90.74 % | **92.42 %** | +1.68 |
| `lib/geocoding/geocode-safety-net.ts` | 84.61 % | **100 %** | +15.39 |
| `lib/sentry/scrub.ts` | 61.29 % | **77.14 %** | +15.85 |

### Tests ajoutés (3 fichiers, 11 tests)

#### `solve-local.edge-cases.test.ts` (4 tests)
- **1 course seule** → preFilterRides early return (rides ≤ 1).
- **Extension n=3** quand places_assises ≥ 3 et fenêtres compatibles.
- **Extension bloquée** si capacity dépassée (places_assises=3 + 4 courses).
- **Course TPMR rejetée** pendant l'extension sur véhicule taxi (compat type).

#### `geocode-safety-net.edge-cases.test.ts` (2 tests)
- Coords fournies SANS citycode → `citycode = null`.
- BAN renvoie un résultat avec `citycode = ''` → null normalisé.

#### `scrub.edge-cases.test.ts` (5 tests)
- Scrub tableau d'objets sensibles (`Array.isArray` branch).
- `request.query_string` filtré.
- `request.data` scrubbé récursif.
- Récursion bornée à 6 niveaux (cutoff sans crash).
- Préservation primitives non-string (number, boolean, null).

### Décisions

#### D-B1 — Mesurer puis cibler
`pnpm exec vitest run --coverage` → identification des branches non couvertes des modules à risque. Tests CIBLÉS, pas de gonflage cosmétique.

#### D-B2 — Priorité au calcul critique
Cibles : pricing (donnée financière — déjà 100%), solve-local (cœur optimisation), build-rides-payload (récurrences — déjà 100%), geocode-safety-net (filet géocodage), scrub (anti-PII santé Sentry).

#### D-B3 — Ne pas réécrire l'existant
Les tests présents restent. On ajoute sur les trous mesurés.

## Garde-fous

- DEC-032 (branche + PR)
- guard-commit (typecheck + format verts)
- ≤ 300 LOC / fichier
- DEC-013 (couverture) renforcé
- DEC-041 audit clos (dette reportée Phase 06 fermée)
- `supabase test db` validé en CI (aucune migration ni policy touchée)
- `pnpm test` 129/129 verts
- 0 migration BDD, 0 policy modifiée, 1 nouvelle devDep (`@vitest/coverage-v8`)

## Trace DEC-100

Inscrite dans `.planning/STATE.md` + entrée `.planning/journal.md` :
« Audit complet DEC-041 sur 24 Server Actions à mutations (11 vrais trous comblés ; 2 N/A documentés : setup/actions string ops + `(public)/legal/request/[token]` service_role bypass). Tests ciblés angles morts métier : solve-local (1.68 pp), geocode-safety-net (15.39 pp → 100% branches), scrub (15.85 pp). Pricing + recurrence 100% maintenus. Clôt la dette DEC-041 reportée Phase 06. Pas d'ADR. »

## Pas d'ADR

Activation et complétude de patterns/qualité actés (DEC-041 + DEC-013). Aucun choix structurel nouveau.
