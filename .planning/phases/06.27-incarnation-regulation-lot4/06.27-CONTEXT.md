# Phase 06.27 — CONTEXT

> Lot 4 d'incarnation de la direction artistique (DEC-101 §5bis « un
> écran bien structuré naît d'un code bien structuré »). Découpe le
> fichier hors-limite + revue de cohérence des modales courses.

## Pourquoi

- `ride-express-form-fields.client.tsx` = **493 lignes** (> limite
  CON-008 de 300). 4 responsabilités mélangées (constantes/types,
  helpers date/heure, masked inputs, champs composés).
- Direction §5bis : structure du code = structure visuelle. Découper sert
  la maintenabilité ET la cohérence.
- Revue légère de cohérence des modales courses (Dialog vs Sheet,
  footers, terracotta action primaire).

## État vérifié avant action

| Fichier | LOC avant |
|---|---|
| `ride-express-form-fields.client.tsx` | 493 (> 300) |
| `address-or-poi-picker.client.tsx` | 408 (hors scope D-03) |
| `assign-modal.client.tsx` | 377 (hors scope D-03) |
| `rides-list.client.tsx` | 354 (hors scope D-03) |
| `ride-drawer.client.tsx` | 343 (hors scope D-03) |

Consommateurs de `ride-express-form-fields` : **2 seulement** —
`ride-express-modal.client.tsx` + `use-ride-submit.client.ts` → refactor sûr.

## Décisions fermées

### D-01 — Découpage de `ride-express-form-fields.client.tsx`

Éclaté en 5 modules sous `courses/_components/ride-fields/` :

| Fichier | LOC | Responsabilité |
|---|---|---|
| `types.ts` | 15 | `TransportMode`, `Urgency`, `TRANSPORT_OPTIONS`, `URGENCY_OPTIONS` — pas de React, importable depuis hooks. |
| `datetime-helpers.ts` | 79 | Helpers purs (`projectFromIso`, `sameDate`, `sameTime`, `combineDateTime`, `digitsOnly`, `formatDateMask`, `formatTimeMask`) + constantes service. Pas de React, testable. |
| `masked-inputs.client.tsx` | 110 | `DateMaskedInput`, `TimeMaskedInput`, `MaskedFieldProps`, `MASKED_FIELD_CLASS`. |
| `datetime-fields.client.tsx` | 175 | `DateTimeFields` (composant react-datepicker complet). |
| `field-groups.client.tsx` | 157 | `AddressField`, `ModeUrgencyFields`, `NotesField`, `SavingIndicator`. |
| `index.ts` | 25 | **Barrel** — préserve l'API publique (`TransportMode`, `Urgency`, `DateTimeFields`, `AddressField`, `ModeUrgencyFields`, `NotesField`, `SavingIndicator`). |

Tous ≤ 175 LOC. Ancien fichier supprimé. **2 imports consommateurs mis à
jour** (`'./ride-express-form-fields.client'` → `'./ride-fields'`).
**API publique préservée** : mêmes noms d'exports, comportement strictement
inchangé.

### D-02 — Cohérence modales courses

**Documenter le choix Dialog vs Sheet** (1 ligne en en-tête de chaque
modale) — pas de refonte, le choix existant est CONSCIENT :

| Modale | Pattern | Commentaire ajouté |
|---|---|---|
| `ride-express-modal.client.tsx` | Dialog centré | « création focalisée, l'utilisateur ne consulte rien d'autre pendant la saisie » |
| `assign-modal.client.tsx` | Dialog centré | « action focalisée, l'assignation engage la régulatrice sur un choix » |
| `override-tarif-modal.client.tsx` | Sheet latéral | « ajustement contextuel d'une course existante (consultation/correction), distinct de la création focalisée » |
| `ride-drawer.client.tsx` | Sheet latéral | « consultation contextuelle d'une course (à droite, n'efface pas la liste). Distinct du Dialog centré utilisé pour la création focalisée. » |

**Footers — primaire à droite** : tous les `DialogFooter`/`SheetFooter`
respectent déjà ce placement. `override-tarif-modal` footer ligne 177 :
`flex flex-row items-center justify-end gap-12` — OK (les `justify-between`
des lignes 120/126 sont dans la card d'info clé/valeur, pas le footer).

**Terracotta sur action « moment-clé »** (cohérence lot 2 DEC-104) :

| Modale | CTA primaire | Avant | Après |
|---|---|---|---|
| `ride-express-modal` | « Créer la course » / « Enregistrer les modifications » | default | **accent** (créer = moment-clé fondamental) |
| `assign-modal` | « Assigner » | default | **accent** (fait avancer le travail régulatrice) |
| `override-tarif-modal` | « Confirmer » | default | inchangé (Confirmer-dialogue = neutral, lot 2) |
| `ride-payment-popover` | « Confirmer » | default | inchangé (Confirmer-dialogue = neutral, lot 2) |

Total `variant="accent"` dans `(app)` : 7 — toujours « max 1 par écran »
(chaque accent est dans un contexte/modale distinct).

### D-03 — Périmètre fermé

Refactor + cohérence modales UNIQUEMENT. **Pas de nouvelle feature, pas
de changement de comportement**, pas les autres gros fichiers
(address-or-poi-picker, rides-list, etc. — rangement = lot 5).

## Garde-fous

- DEC-032 (branche + PR)
- guard-commit (typecheck + format + tests verts) ✓
- ≤ 300 LOC / fichier ✓ (objectif du lot atteint pour ride-fields/)
- NFR-001 ✓
- **Comportement INCHANGÉ** — refactor pur, les 129 tests existants
  passent SANS modification
- Direction DEC-101 §5bis (structure code = structure visuelle) ✓
- 0 migration BDD, 0 changement de wording, 0 nouvelle dépendance

## Trace DEC-106

Inscrite dans `.planning/STATE.md` + entrée `.planning/journal.md` :
« Refactor structurel : `ride-express-form-fields` (493 l.) scindé en
modules `ride-fields/` (types / helpers purs / masked-inputs /
datetime-fields / field-groups), API préservée, tous ≤ 175 l. Revue
cohérence modales courses (Dialog=focalisé / Sheet=contextuel documenté,
footers primaire-à-droite, terracotta action-clé sur "Créer la course"
+ "Assigner"). Incarne direction §5bis. »

## Pas d'ADR

Refactor pur + activation de pattern existant (variant accent posé en
lot 2). Aucun choix structurel nouveau, aucune dépendance ajoutée.
