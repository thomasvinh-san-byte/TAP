---
phase: 03.1
plan: 02
type: execute
wave: 1
depends_on:
  - 03.1-00
files_modified:
  - apps/web/src/app/(app)/courses/_components/ride-express-form-fields.client.tsx
autonomous: true
requirements:
  - SAIS-08
  - NFR-002
  - NFR-003
  - NFR-004
tags:
  - chips
  - date-input
  - quick-actions
must_haves:
  truths:
    - "4 chips date (`demain 8h`, `demain 14h`, `lundi 9h`, `dans 30 minutes`) apparaissent inline sous l'input date dans la modal saisie express."
    - "Cliquer une chip remplit l'input ET déclenche le parsing chrono.fr (onChange + onParsed synchrones)."
    - "Les chips sont masquées en mode édition (`rideId` présent) et en mode disabled."
    - "Les chips utilisent les classes Tailwind locked D-A3-3 verbatim, AUCUN composant shadcn Button."
  artifacts:
    - path: "apps/web/src/app/(app)/courses/_components/ride-express-form-fields.client.tsx"
      provides: "DateFreeformField étendu avec 4 chips date inline"
      contains: "demain 8h"
  key_links:
    - from: "DateFreeformField (chip onClick)"
      to: "props.onChange + props.onParsed"
      via: "handleChipClick(label) → onChange(label) puis onParsed(parsed)"
      pattern: "onChange\\(.*\\).*onParsed"
---

<objective>
Implémenter A3 — 4 chips date rapides inline dans `DateFreeformField` du modal
saisie express course. Référence Uber Health (chips sous datepicker) +
Linear (style chip plat, discret).

Purpose : permettre à la régulatrice de saisir une date courante en 1 clic
au lieu de taper « demain 8h » manuellement. Pilier 1 (UX 8h/jour) +
objectif chiffré CLAUDE.md (saisie express < 30 s).

Output : extension inline du composant existant `DateFreeformField` (≤ 80 L
pour la section concernée, fichier reste ≤ 300 L).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/regle-neutralite-et-ton.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03.1-efficience-saisie-modal-course/03.1-CONTEXT.md
@.planning/phases/03.1-efficience-saisie-modal-course/03.1-UI-SPEC.md
@apps/web/src/app/(app)/courses/_components/ride-express-form-fields.client.tsx
@packages/shared/src/parse-freeform-date.ts

<interfaces>
**Pré-requis Wave 0 (Plan 03.1-00 LIVRÉ)** :
- `ride-express-modal.client.tsx` passe désormais explicitement
  `rideId={props.rideId}` à `<DateFreeformField />` (Task 3 Wave 0).
- Si `DateFreeformField` n'a pas encore la prop `rideId?: string` dans sa
  signature, Wave 0 l'a ajoutée à la signature pour permettre le passage.

  **Conséquence pour Plan 02** : le wiring `rideId` modal→DateFreeformField
  est **déjà fait**. Plan 02 n'a plus à toucher le modal. `files_modified`
  contient UNIQUEMENT `ride-express-form-fields.client.tsx` (résout
  checker B3 Option A : scope cohérent avec le commit).

**Décisions locked (D-A3-1..5) + UI-SPEC Element 1** :

- D-A3-1 : chips **inline** dans DateFreeformField, PAS sous-composant séparé.
- D-A3-2 : 4 valeurs figées (ordre exact) : `demain 8h`, `demain 14h`, `lundi 9h`, `dans 30 minutes`.
- D-A3-3 : classes Tailwind locked verbatim :
  `px-12 py-8 rounded-full text-xs border border-input bg-background hover:bg-muted text-muted-foreground transition-colors`
- D-A3-4 : click → `onChange(label)` puis `onParsed(iso)` synchrones (pas de side-effect masqué).
- D-A3-5 : masqués si `props.disabled === true` OU `props.rideId` présent.

Position UI : après `<Input>`, avant `<p>` d'erreur. Conteneur :
`<div className="flex flex-wrap gap-8 mt-8">`.

Markup chip canonique (UI-SPEC) :
```tsx
<button
  type="button"
  onClick={() => handleChipClick('demain 8h')}
  className="px-12 py-8 rounded-full text-xs border border-input bg-background hover:bg-muted text-muted-foreground transition-colors"
>
  demain 8h
</button>
```

Parsing : réutiliser `parseFreeformDate` de `packages/shared` (chrono.fr
avec `forwardDate: true`, déjà câblé). Les 4 valeurs sont chrono.fr-compatibles.

Aucun toast, aucune animation, aucune icône Lucide.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre DateFreeformField avec 4 chips date inline</name>
  <files>apps/web/src/app/(app)/courses/_components/ride-express-form-fields.client.tsx</files>
  <action>
**Pré-requis** : Wave 0 a propagé `rideId={props.rideId}` du modal à
`DateFreeformField`. Vérifier que la prop `rideId?: string` existe dans la
signature de `DateFreeformField` ; si elle existe déjà, ne pas la redéfinir.

Dans `DateFreeformField`, ajouter le rendu conditionnel des 4 chips
APRÈS le `<Input>` et AVANT le `<p>` d'erreur.

1) **Définir une constante** (top du composant ou en module-level
   avant l'export) :
   ```typescript
   const DATE_CHIPS = ['demain 8h', 'demain 14h', 'lundi 9h', 'dans 30 minutes'] as const;
   ```

2) **Définir `handleChipClick(label: string)`** qui :
   - appelle `props.onChange(label)` (ou équivalent — propager au state parent comme une saisie manuelle).
   - parse via `parseFreeformDate(label)` (chrono.fr `forwardDate: true`).
   - appelle `props.onParsed(parsed.iso)` si le parse réussit.
   - les deux appels sont synchrones (D-A3-4).

   Le contrat exact (`onChange`, `onParsed`) doit suivre les props
   déjà exposées par `DateFreeformField`. Si les noms diffèrent,
   adapter — mais le comportement « tape + parse » doit être identique
   à une saisie manuelle dans l'input.

3) **Rendre conditionnellement** :
   ```tsx
   {!props.disabled && !props.rideId ? (
     <div className="flex flex-wrap gap-8 mt-8">
       {DATE_CHIPS.map((label) => (
         <button
           key={label}
           type="button"
           onClick={() => handleChipClick(label)}
           className="px-12 py-8 rounded-full text-xs border border-input bg-background hover:bg-muted text-muted-foreground transition-colors"
         >
           {label}
         </button>
       ))}
     </div>
   ) : null}
   ```

   **Coller les classes Tailwind verbatim** (D-A3-3). Ne pas reformuler,
   ne pas remplacer par `outline` Button shadcn (anti-pattern explicite
   D-A3-3 et UI-SPEC).

4) **`type="button"` obligatoire** pour ne pas submit le form parent.

5) **Câblage `rideId`** : la prop `rideId?: string` est déjà câblée par
   Wave 0 (Plan 03.1-00 Task 3) — si elle n'apparaît PAS dans la
   signature du composant, ajouter `rideId?: string` aux props localement
   et signaler dans le SUMMARY (anomalie Wave 0). **Ne PAS** modifier
   `ride-express-modal.client.tsx` depuis ce plan (checker B3 — scope hors
   `files_modified`).

Aucun nouveau composant `components/ui/`, aucune icône, aucune
animation. Pas de Tooltip. Pas de toast.

Per D-A3-1..5 + UI-SPEC Element 1, checker B3 Option A.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck && pnpm lint && [ "$(wc -l < apps/web/src/app/\(app\)/courses/_components/ride-express-form-fields.client.tsx)" -le 300 ]</automated>
  </verify>
  <done>
- 4 chips rendues inline sous l'Input quand `!disabled && !rideId`.
- Classes Tailwind D-A3-3 collées verbatim.
- Click chip remplit l'input et déclenche le parse (onChange + onParsed synchrones).
- Mode édition (`rideId`) : chips totalement non-rendues.
- `pnpm typecheck` + `pnpm lint` GREEN.
- Fichier `ride-express-form-fields.client.tsx` reste ≤ 300 L.
- Section `DateFreeformField` reste ≤ 80 L (D-A3-1).
- **Aucune modification** à `ride-express-modal.client.tsx` (checker B3 : scope cohérent avec `files_modified`).
  </done>
</task>

</tasks>

<verification>
Vérification fonctionnelle manuelle sur preview Vercel :

1. Login `regulateur@demo.tap`, ouvrir saisie express.
2. Vérifier la présence des 4 chips sous l'input date (ordre exact :
   `demain 8h`, `demain 14h`, `lundi 9h`, `dans 30 minutes`).
3. Cliquer `demain 8h` : l'input se remplit avec « demain 8h » et l'heure
   parsée s'affiche sous l'input (date résolue ISO).
4. Cliquer `lundi 9h` : idem, parsing OK.
5. Cliquer `dans 30 minutes` : idem, parsing OK.
6. Ouvrir une course existante en édition (rideId présent) : les chips
   doivent être ABSENTES (pas grisées, totalement non-rendues).
7. Mode mobile (375 px iPhone SE) : les chips wrappent proprement
   (`flex-wrap gap-8`).
8. Mode sombre : contraste correct (hérité des tokens existants).
</verification>

<success_criteria>
- 4 chips inline visibles en mode création, absentes en mode édition.
- Click chip remplit + parse l'input comme une saisie manuelle.
- Aucun `Button` shadcn utilisé pour les chips (D-A3-3).
- Aucun toast, aucune animation, aucune icône.
- `pnpm typecheck` + `pnpm lint` GREEN.
- Aucun fichier > 300 L.
- Aucun nom propre ajouté.
- Aucun test Playwright nouveau.
- `files_modified` contient UNIQUEMENT `ride-express-form-fields.client.tsx` (checker B3).
</success_criteria>

<anti_patterns>
- ❌ Modifier `ride-express-modal.client.tsx` depuis ce plan (Wave 0 a déjà câblé `rideId` — checker B3 Option A).
- ❌ Utiliser `<Button variant="outline" size="sm">` shadcn (D-A3-3 explicite : chips Linear plates, PAS Button shadcn).
- ❌ Ajouter une icône Lucide dans la chip.
- ❌ Toast de confirmation au click.
- ❌ Animation/transition autre que `transition-colors` 150 ms hérité.
- ❌ Modifier `parseFreeformDate` (chrono.fr suffit — anti-pattern explicite CONTEXT).
- ❌ Extraire les chips dans un sous-composant séparé (D-A3-1 : inline).
- ❌ Afficher les chips en mode édition.
- ❌ Nouveau composant `components/ui/` ou dépendance npm.
- ❌ Test Playwright nouveau.
</anti_patterns>

<output>
Après complétion, créer `.planning/phases/03.1-efficience-saisie-modal-course/03.1-02-SUMMARY.md`.

Commit atomique (FR) :
```
feat(courses): chips date rapides dans saisie express (A3)
```
</output>
