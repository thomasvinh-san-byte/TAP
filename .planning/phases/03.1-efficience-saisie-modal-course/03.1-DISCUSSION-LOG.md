# Phase 03.1 — Discussion Log

**Date** : 2026-05-12
**Mode** : assumptions (workflow.discuss_mode = "assumptions")
**Skill injection** : tap-neutralite (neutralité absolue + ton sobre)

---

## Source des assumptions

Le prompt utilisateur a fourni les 14 décisions D-XX directement, basées sur :
- 3 patterns externes identifiés (Doctolib quick booking, Uber Health NEMT,
  Onfleet task validation, Linear warning banner)
- Étude des frictions de saisie observées en Phase 03 (validation walkthrough)
- Décision Q3 questionnaire (conservation des noms patients démo)

L'agent `gsd-assumptions-analyzer` n'a PAS été spawné (les inputs étaient
pré-validés et détaillés au niveau du code). Économie : 1 spawn + ~30s analyse.

---

## Assumptions présentées

| Area | Confidence | Statement |
|---|---|---|
| Smart defaults (A2) | Confident | Pré-remplissage silencieux mode + urgence depuis dernière course |
| Chips date (A3) | Confident | 4 chips inline `px-12 py-8 rounded-full text-xs` |
| Détection doublon (B3) | Confident | Banner amber non-bloquant + bouton « Créer quand même » |
| Re-seed patients (SEED) | Confident | UPDATE idempotent + 4 sources seed patchées |

---

## Réponse utilisateur

**Q : Toutes les assumptions ci-dessus sont confirmées ?**
**A : Yes, proceed (Recommandé)**

Aucune correction demandée. Les 14 décisions D-XX sont locked dans CONTEXT.md.

---

## Codebase scout (lecture .planning/codebase/)

Le scan automatique a confirmé que les conventions repo sont compatibles
avec les inputs :

- `useFormState` + `useFormStatus` : pattern unique repo (PAS react-hook-form).
  → Cohérent avec D-A2 et D-B3 (pas de form lib supplémentaire).
- Spacing scale 4/8/12/16/24/32/48/64 : strict (NFR-003).
  → D-A3-3 (`px-12 py-8`) et D-B3-4 (`px-16 py-12`) sont within scale.
- Drawer widths fixes 400 (patient) / 480 (course).
  → Pas d'impact (Phase 03.1 ne touche pas aux drawers).
- Reformulation FR systématique des erreurs Supabase.
  → `checkDuplicateRideAction` devra renvoyer des messages FR (cf.
    `cancelRideAction` comme modèle).

---

## Risques identifiés (non-bloquants)

1. **Modal RideExpressModal déjà à 384 lignes** (CONCERNS.md). Les
   intégrations A2 + A3 wiring + B3 banner peuvent pousser à 450L+.
   Mitigation : le planner devra décider extraction d'un sous-composant
   `<DuplicateBanner>` ou refacto du wiring smart defaults dans un hook
   `useSmartDefaults` séparé.

2. **A2 court-circuit en mode édition** : `props.rideId` présent → skip A2.
   Vérifier dans la planification que cette branche est testable (Test 1
   du UAT mentionne « patient avec course existante » → ne fonctionne que
   si A2 est en mode création).

3. **B3 fenêtre ±30 min hardcodée V1** : si la régulatrice a des doublons
   intentionnels (transfert dialyse), elle devra cliquer « Créer quand
   même ». Acceptable V1 — taxonomie viendra de l'observation usage réel.

---

## Suite immédiate

1. `/gsd-ui-phase 03.1` — lock design contract chips date + banner amber.
2. `/gsd-plan-phase 03.1` — production de 4 plans atomiques (A2, A3, B3, SEED).
3. `/gsd-execute-phase 03.1` — exécution sequentielle (les 4 plans ont des
   dépendances : SEED autonome, A2 indépendant, A3 indépendant, B3 dépend
   de A2 pour le `handleSubmit` modifié).
