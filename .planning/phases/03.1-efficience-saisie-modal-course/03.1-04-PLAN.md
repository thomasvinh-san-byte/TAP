---
phase: 03.1
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260513000003_reseed_patients_fictifs.sql
  - supabase/seed.sql
  - supabase/seed.demo.sql
  - supabase/setup-all.sql
  - apps/web/src/lib/setup-sql.ts
autonomous: true
requirements:
  - SAIS-10
  - NFR-001
tags:
  - seed
  - migration
  - patients-demo
  - defense-in-depth
must_haves:
  truths:
    - "Les 10 patients démo affichent un téléphone au pattern `02 62 99 90 XX` ou `06 92 99 90 XX`."
    - "Les 10 patients démo affichent une adresse au pattern `9XXX rue|chemin|allée` (numéro civique commençant par 9)."
    - "Les noms des patients sont conservés inchangés (Bègue, Boyer, Dijoux, Grondin, Hoarau, Lebon, Maillot, Payet, Robert, Vergoz) — exception NFR-001 explicite pour seed.demo.sql (D-SEED-4)."
    - "La migration est idempotente : un re-run UPDATE ne crée pas de duplicats, ne casse pas les FK rides.patient_id existantes."
    - "Les 4 sources seed (seed.sql, seed.demo.sql, setup-all.sql, setup-sql.ts) sont alignées sur les MÊMES valeurs (10 occurrences `99 90 XX` exactement par fichier) pour résister à une réinit DB."
  artifacts:
    - path: "supabase/migrations/20260513000003_reseed_patients_fictifs.sql"
      provides: "Migration UPDATE idempotent ciblé sur (nom, prenom) clé naturelle"
      contains: "9001 chemin du Vacoa"
      min_lines: 40
    - path: "supabase/seed.sql"
      provides: "Source seed alignée (10 occurrences `99 90`)"
      contains: "99 90"
    - path: "supabase/seed.demo.sql"
      provides: "Source seed démo alignée (10 occurrences `99 90`)"
      contains: "99 90"
    - path: "supabase/setup-all.sql"
      provides: "Source setup-all.sql alignée OBLIGATOIRE (10 occurrences `99 90`) — checker W5"
      contains: "99 90"
    - path: "apps/web/src/lib/setup-sql.ts"
      provides: "Setup TS aligné (10 occurrences `99 90`)"
      contains: "99 90"
  key_links:
    - from: "Migration 20260513000003"
      to: "patients table (existing rows)"
      via: "UPDATE WHERE (nom, prenom) IN (10 paires)"
      pattern: "UPDATE patients"
    - from: "supabase/seed.demo.sql"
      to: "Migration values"
      via: "Mêmes 10 lignes pré-injectées au INSERT initial"
      pattern: "99 90"
---

<objective>
Re-seed défensif des 10 patients fictifs avec pattern reconnaissable
(téléphone `02 62 99 90 XX` / `06 92 99 90 XX`, adresse `9XXX ...`).
Couche d'anonymisation défense en profondeur conforme à NFR-001 (signal
visuel évident que ce sont des données fictives, repérable par un grep CI
futur).

Purpose : éliminer tout risque de confusion entre les patients seedés et
des personnes réelles dans les screenshots, démos commerciales, captures
vidéo et docs. Les noms réunionnais sont conservés (D-SEED-4, décision
Q3 questionnaire) — la fiction se signale par les numéros.

Output : 1 migration SQL idempotente + 4 sources seed patchées pour
cohérence à la réinit DB (checker W5 : `setup-all.sql` **obligatoire**, le
fichier existe — vérifié 2433 L).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/regle-neutralite-et-ton.md
@.planning/phases/03.1-efficience-saisie-modal-course/03.1-CONTEXT.md
@supabase/migrations/20260513000002_anonymize_seed_profiles.sql
@supabase/seed.sql
@supabase/seed.demo.sql
@supabase/setup-all.sql
@apps/web/src/lib/setup-sql.ts

<interfaces>
Décisions locked (D-SEED-1..4) + checker W4 (regex strict) + checker W5
(`setup-all.sql` obligatoire).

**Migration cible** : `supabase/migrations/20260513000003_reseed_patients_fictifs.sql`.

**Header obligatoire (commentaire SQL)** :
```sql
-- 20260513000003 — Re-seed patients fictifs (défense en profondeur NFR-001)
--
-- Données fictives — pattern numéro 9XXX + tél 99-90-XX,
-- aucune correspondance volontaire avec personnes réelles.
--
-- Migration idempotente : UPDATE ciblé sur clé naturelle (nom, prenom).
-- Re-runnable sans casser les FK rides.patient_id existantes.
-- Les noms réunionnais sont conservés (exception NFR-001 explicite pour
-- les données de démo, décision Q3 questionnaire phase 03.1).
```

**Pattern UPDATE idempotent** (template `20260513000002_anonymize_seed_profiles.sql`) :

Pour chaque patient (10 au total), une instruction UPDATE :
```sql
UPDATE patients
SET telephone = '02 62 99 90 01',
    adresse = '9001 chemin du Vacoa, 97400 Saint-Denis'
WHERE nom = 'Bègue' AND prenom = 'Christiane';
```

(Adapter aux noms de colonnes réels du schéma `patients`. Vérifier en
ouvrant `20260513000002_anonymize_seed_profiles.sql` qui sert de référence.)

**Table de valeurs canonique (D-SEED-3)** — coller verbatim :

| Patient                  | Téléphone        | Adresse                                                       |
|--------------------------|------------------|---------------------------------------------------------------|
| Bègue Christiane         | 02 62 99 90 01   | 9001 chemin du Vacoa, 97400 Saint-Denis                       |
| Boyer Suzanne            | 02 62 99 90 02   | 9002 rue des Lataniers, 97410 Saint-Pierre                    |
| Dijoux André             | 06 92 99 90 03   | 9003 allée des Songes, 97410 Saint-Pierre                     |
| Grondin Jean-Bernard     | 06 92 99 90 04   | 9004 chemin du Piton, 97490 Sainte-Clotilde                   |
| Hoarau Patrick           | 06 92 99 90 05   | 9005 rue des Bambous, 97400 Saint-Denis                       |
| Lebon Bernard            | 06 92 99 90 06   | 9006 chemin de la Ravine, 97410 Saint-Pierre                  |
| Maillot Marlène          | 02 62 99 90 07   | 9007 rue des Cyclones, 97432 Ravine des Cabris                |
| Payet Marie-Ange         | 02 62 99 90 08   | 9008 allée du Volcan, 97400 Saint-Denis                       |
| Robert Anne-Sophie       | 02 62 99 90 09   | 9009 chemin du Lagon, 97430 Le Tampon                         |
| Vergoz Yves              | 06 92 99 90 10   | 9010 rue des Galets, 97418 La Plaine des Cafres               |

**Sources seed à aligner (D-SEED-2 + checker W5)** :
- `supabase/seed.sql` — **OBLIGATOIRE**, 10 occurrences `99 90 XX` après patch.
- `supabase/seed.demo.sql` — **OBLIGATOIRE**, 10 occurrences `99 90 XX` après patch.
- `supabase/setup-all.sql` — **OBLIGATOIRE** (checker W5 : fichier existe à 2433 L), 10 occurrences `99 90 XX` après patch.
- `apps/web/src/lib/setup-sql.ts` — **OBLIGATOIRE**, 10 occurrences `99 90 XX` après patch.

**Anti-pattern** : pas de `DROP + INSERT` (casserait les FK
`rides.patient_id` → `patients.id`). Pattern UPDATE-only pour la migration,
remplacement de valeurs in-place pour les sources seed.

**Apostrophes SQL** : strings SQL en quotes simples. Caractères accentués
(è, é, à) : valides en UTF-8, fichier doit être sauvegardé en UTF-8 sans BOM.

Per D-SEED-1..4, checker W4 (regex exact 10), checker W5 (setup-all
obligatoire).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer la migration 20260513000003_reseed_patients_fictifs.sql</name>
  <files>supabase/migrations/20260513000003_reseed_patients_fictifs.sql</files>
  <action>
Créer le nouveau fichier de migration `supabase/migrations/20260513000003_reseed_patients_fictifs.sql`.

1. **Ouvrir d'abord** `supabase/migrations/20260513000002_anonymize_seed_profiles.sql`
   pour confirmer :
   - Les noms exacts des colonnes (`telephone` vs `phone`, `adresse` vs `address`)
   - Le pattern UPDATE idempotent par clé naturelle
   - La convention d'en-tête de commentaire

2. **Rédiger l'en-tête commentaire FR** (cf. section `<interfaces>`).

3. **Émettre 10 instructions UPDATE successives**, une par patient, dans
   l'ordre de la table D-SEED-3 :
   ```sql
   UPDATE patients
   SET telephone = '02 62 99 90 01',
       adresse = '9001 chemin du Vacoa, 97400 Saint-Denis'
   WHERE nom = 'Bègue' AND prenom = 'Christiane';

   UPDATE patients
   SET telephone = '02 62 99 90 02',
       adresse = '9002 rue des Lataniers, 97410 Saint-Pierre'
   WHERE nom = 'Boyer' AND prenom = 'Suzanne';

   -- ... etc pour les 10 patients
   ```

   **Adapter les noms de colonnes aux noms réels** vus dans la migration de
   référence `20260513000002`. Ne pas inventer.

4. Pas de `BEGIN` / `COMMIT` explicites (Supabase wrapper gère la
   transaction par fichier).

5. Total attendu : ~40-50 L. Reste ≤ 300 L.

Per D-SEED-1, D-SEED-3, D-SEED-4.
  </action>
  <verify>
    <automated>test -f /home/user/TAP/supabase/migrations/20260513000003_reseed_patients_fictifs.sql && [ "$(grep -c "^UPDATE patients" /home/user/TAP/supabase/migrations/20260513000003_reseed_patients_fictifs.sql)" -eq 10 ] && [ "$(grep -cE "99 90 [0-9]{2}" /home/user/TAP/supabase/migrations/20260513000003_reseed_patients_fictifs.sql)" -eq 10 ] && echo OK</automated>
  </verify>
  <done>
- Fichier `20260513000003_reseed_patients_fictifs.sql` existe.
- Contient **exactement 10** instructions `UPDATE patients` (regex strict checker W4).
- Contient **exactement 10** occurrences `99 90 XX` (regex strict checker W4).
- En-tête commentaire FR présent avec mention "Données fictives".
- Caractères accentués (è, é, à) préservés (UTF-8 sans BOM).
  </done>
</task>

<task type="auto">
  <name>Task 2: Aligner les 4 sources seed OBLIGATOIRES (regex strict 10 occurrences chacune)</name>
  <files>
    supabase/seed.sql,
    supabase/seed.demo.sql,
    supabase/setup-all.sql,
    apps/web/src/lib/setup-sql.ts
  </files>
  <action>
**Checker W5** : `supabase/setup-all.sql` **existe** (vérifié 2433 L, pas
optionnel — supprimé tout langage « skip si absent »). Les 4 fichiers sont
**tous obligatoires** à patcher.

**Checker W4** : la verify exige **exactement 10 occurrences** du pattern
`99 90 XX` par fichier (regex strict `99 90 [0-9]{2}`). Si un patient
référencé sur 2 lignes (par exemple, mention dans un INSERT puis dans un
commentaire), compter exactement 10 — ajuster si nécessaire en supprimant
les références redondantes en commentaire.

Pour chacun des 4 fichiers, **lire d'abord** pour identifier les INSERTs
des 10 patients (ou template literals dans le `.ts`). Ensuite, remplacer
les valeurs `telephone` et `adresse` (ou équivalents `phone`, `address`)
par les valeurs canoniques de la table D-SEED-3 (mêmes valeurs que dans
la migration 20260513000003).

**`supabase/seed.sql`** : patcher chaque INSERT des 10 patients :
- `Bègue Christiane` → tel `02 62 99 90 01`, adresse `9001 chemin du Vacoa, 97400 Saint-Denis`.
- ... etc pour les 10.
- Résultat attendu : exactement 10 occurrences `99 90 XX`.

**`supabase/seed.demo.sql`** : idem, c'est le seed démo principal exécuté
par `cd.yml` après chaque push sur main.
- Résultat attendu : exactement 10 occurrences `99 90 XX`.

**`supabase/setup-all.sql`** : **OBLIGATOIRE** (2433 L, vérifié). Localiser
les INSERTs des 10 patients (peut être au milieu du fichier) et appliquer
les mêmes valeurs.
- Résultat attendu : exactement 10 occurrences `99 90 XX`.

**`apps/web/src/lib/setup-sql.ts`** : probablement des template literals
JavaScript wrappant du SQL. Patcher de manière équivalente. Attention aux
backticks et à l'échappement.
- Résultat attendu : exactement 10 occurrences `99 90 XX`.

**Règles invariantes** :
- Ne modifier QUE `telephone`/`phone` et `adresse`/`address` des 10
  patients listés. Ne pas toucher `nom`, `prenom`, `date_naissance`,
  `nir`, autres champs.
- Conserver les autres patients du seed (s'il y en a) sans modification.
- Les noms réunionnais sont conservés (D-SEED-4).

**Cohérence à la réinit DB** : après ces patches, un `supabase db reset`
suivi de `seed.sql` + `seed.demo.sql` + `setup-all.sql` produit directement
les bonnes valeurs (la migration `20260513000003` est alors un no-op
idempotent sur ces données — c'est volontaire).
  </action>
  <verify>
    <automated>cd /home/user/TAP && [ "$(grep -cE "99 90 [0-9]{2}" supabase/seed.sql)" -eq 10 ] && [ "$(grep -cE "99 90 [0-9]{2}" supabase/seed.demo.sql)" -eq 10 ] && [ "$(grep -cE "99 90 [0-9]{2}" supabase/setup-all.sql)" -eq 10 ] && [ "$(grep -cE "99 90 [0-9]{2}" apps/web/src/lib/setup-sql.ts)" -eq 10 ] && pnpm typecheck && echo OK</automated>
  </verify>
  <done>
- `supabase/seed.sql` patché : `grep -cE "99 90 [0-9]{2}"` = **10 exactement** (checker W4).
- `supabase/seed.demo.sql` patché : `grep -cE "99 90 [0-9]{2}"` = **10 exactement**.
- `supabase/setup-all.sql` patché : `grep -cE "99 90 [0-9]{2}"` = **10 exactement** (checker W5 : OBLIGATOIRE, pas de skip).
- `apps/web/src/lib/setup-sql.ts` patché : `grep -cE "99 90 [0-9]{2}"` = **10 exactement**.
- Aucun nom réunionnais (Bègue, Boyer, ...) renommé (D-SEED-4).
- `pnpm typecheck` GREEN (pour le fichier `.ts`).
  </done>
</task>

</tasks>

<verification>
**Vérification locale (sandbox)** :

1. `pnpm typecheck` GREEN (Task 2 modifie un `.ts`).
2. **Regex strict** (checker W4) — chaque grep retourne **exactement 10** :
   ```bash
   grep -cE "99 90 [0-9]{2}" supabase/migrations/20260513000003_reseed_patients_fictifs.sql
   grep -cE "99 90 [0-9]{2}" supabase/seed.sql
   grep -cE "99 90 [0-9]{2}" supabase/seed.demo.sql
   grep -cE "99 90 [0-9]{2}" supabase/setup-all.sql
   grep -cE "99 90 [0-9]{2}" apps/web/src/lib/setup-sql.ts
   # Chaque ligne doit afficher exactement : 10
   ```

**Vérification preview Vercel (après merge sur main)** :

3. Le workflow `cd.yml` applique la migration + re-applique
   `seed.sql` + `seed.demo.sql` + `setup-all.sql`. Vérifier dans Supabase
   Studio que les 10 patients ont bien :
   - téléphone au pattern `0[26] [69]2 99 90 XX`
   - adresse au pattern `9XXX [rue|chemin|allée] ...`

4. Login `regulateur@demo.tap` sur preview, page `/patients` :
   - Les 10 patients listés avec les nouveaux numéros visibles.

5. Ouvrir la fiche `Hoarau Patrick` : tél `06 92 99 90 05`, adresse
   `9005 rue des Bambous, 97400 Saint-Denis`.

**Vérification idempotence** :

6. Ré-exécuter la migration 20260513000003 (en local : `supabase db reset`
   puis `supabase db push`) — aucune erreur, aucun changement de données
   par rapport au seed déjà aligné.

**Vérification non-régression FK** :

7. Vérifier que les courses seedées (`rides`) référencent toujours les
   patients par leurs UUID (FK `rides.patient_id` = `patients.id`
   inchangée — l'UPDATE ne touche pas `id`).
</verification>

<success_criteria>
- Migration `20260513000003_reseed_patients_fictifs.sql` créée, **exactement 10** UPDATEs idempotents (checker W4).
- 4 sources seed alignées : chacune `grep -cE "99 90 [0-9]{2}"` = **10 exactement** (checker W4, W5).
- `setup-all.sql` OBLIGATOIRE traité (pas de skip — checker W5).
- Tous les 10 patients ont tél `0[26] [69]2 99 90 XX` et adresse `9XXX ...`.
- Aucun nom modifié (D-SEED-4 — exception NFR-001 explicite pour seed démo).
- Aucune FK cassée (UPDATE-only, pas DROP + INSERT).
- `pnpm typecheck` GREEN.
- Idempotente : re-run sans effet de bord.
- Aucune nouvelle dépendance, aucun composant UI touché.
- Aucun test Playwright nouveau (politique tests allégée).
</success_criteria>

<anti_patterns>
Repris depuis CONTEXT.md (Anti-Patterns) + D-SEED + checker :
- ❌ Skipper `supabase/setup-all.sql` (checker W5 : le fichier existe, c'est OBLIGATOIRE).
- ❌ Accepter < 10 ou > 10 occurrences `99 90 XX` dans n'importe quel fichier (checker W4 : exactement 10).
- ❌ DROP + INSERT (casse FK `rides.patient_id`).
- ❌ Renommage des patients (Bègue reste Bègue, Boyer reste Boyer, etc. — D-SEED-4).
- ❌ Migration DB autre que ce re-seed pour la phase 03.1.
- ❌ Modification des colonnes `nom`, `prenom`, `date_naissance`, `nir`, autres.
- ❌ Seed d'autres tables que `patients` dans cette migration.
- ❌ Introduction d'apostrophes typographiques (`'` vs `'`) dans le SQL.
- ❌ Nouveau fichier seed (réutiliser les 4 sources existantes).
- ❌ Test Playwright pour vérifier les valeurs seedées (over-engineering V1).
</anti_patterns>

<risk_hints>
- **Noms de colonnes** : selon le schéma du repo, les colonnes peuvent
  être `telephone` (FR) ou `phone` (EN), `adresse` (FR) ou `address` (EN).
  Vérifier dans `20260513000002_anonymize_seed_profiles.sql` (référence)
  AVANT d'écrire les UPDATEs. Ne pas inventer.
- **Caractères accentués** : sauvegarder le fichier en UTF-8 sans BOM
  (vérifier avec `file supabase/migrations/20260513000003_reseed_patients_fictifs.sql`
  → doit afficher `UTF-8 Unicode text`). Postgres lit nativement UTF-8.
- **`setup-all.sql` à 2433 L** : grand fichier, localiser les INSERTs
  patients via `grep -n "INSERT INTO patients" supabase/setup-all.sql` puis
  patcher chirurgicalement. Ne pas modifier autre chose.
- **`setup-sql.ts` template literal** : si les valeurs sont injectées
  dynamiquement (loop sur un array), il suffit de patcher l'array source.
  Si elles sont en dur dans une string, patcher la string. Lire d'abord.
- **Comptage exact 10** : si un patient apparaît sur 2 lignes (ex : INSERT
  + commentaire de référence), le grep comptera 2 → dépassera 10. Supprimer
  les références redondantes (commentaires) pour atterrir à exactement 10.
- **Ordre d'exécution** : cette migration est tagguée Wave 0 (standalone,
  aucune dépendance app). Peut être mergée et déployée AVANT les Plans
  00/01/02/03. Aucun impact sur le code applicatif. Wave 0 contient donc
  **2 plans parallèles** : `03.1-00-PLAN.md` (modal decompression) et
  `03.1-04-PLAN.md` (SEED) — indépendants, mergeables dans n'importe quel
  ordre.
- **Données préservées** : les courses (`rides`) seedées en Phase 03
  pointent vers `patients.id` (UUID). L'UPDATE ne touche pas `id`, donc
  les courses restent valides. Si pour une raison quelconque les
  patients seedés ont été supprimés (table vide), la migration est un
  no-op (UPDATE WHERE sans match) — pas d'erreur.
</risk_hints>

<output>
Après complétion, créer `.planning/phases/03.1-efficience-saisie-modal-course/03.1-04-SUMMARY.md`.

Commit atomique (FR, format `type(scope): description`) :
```
chore(seed): re-seed patients fictifs avec tél/adresse 9XXX-99-90-XX (NFR-001 défense en profondeur)
```
</output>
