---
phase: 03.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx
  - apps/web/src/app/(app)/courses/_components/use-smart-defaults.client.ts
  - apps/web/src/app/(app)/courses/_components/duplicate-banner.client.tsx
  - apps/web/src/app/(app)/courses/_components/ride-status-fr.ts
  - apps/web/src/app/(app)/patients/actions/index.ts
  - apps/web/src/app/(app)/patients/actions/_existing.ts
autonomous: true
requirements:
  - SAIS-07
  - SAIS-09
  - NFR-002
tags:
  - refactor
  - decompression
  - barrel
must_haves:
  truths:
    - "`apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx` est ramené à ≤ 280 L (depuis 384 L) pour absorber les +50 L des Plans 01 et 03 sans dépasser 300 L (CLAUDE.md § 11)."
    - "`useSmartDefaults` hook existe en fichier dédié, prêt à câbler `getPatientRideDefaultsAction` (Plan 01) sans toucher au modal."
    - "`DuplicateBanner` composant existe en fichier dédié, prêt à recevoir `duplicates`, `onCancel`, `onConfirm`, `isPending` (Plan 03)."
    - "`ride-status-fr.ts` exporte le mapping FR `{validee → validée, assignee → assignée à un chauffeur, en_cours → en cours, terminee → terminée}` réutilisé par DuplicateBanner."
    - "`apps/web/src/app/(app)/patients/actions.ts` est remplacé par le barrel `apps/web/src/app/(app)/patients/actions/index.ts` + `_existing.ts`, chaque fichier ≤ 300 L. Plan 01 pourra ajouter `get-ride-defaults.ts` sans breach."
    - "Aucune régression fonctionnelle : le modal se comporte comme avant (chips date V0 absentes, banner doublon absent, smart defaults absents — ils arrivent en Wave 1+)."
  artifacts:
    - path: "apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx"
      provides: "Modal décompressé ≤ 280 L, props rideId propagé à DateFreeformField"
      contains: "DuplicateBanner"
    - path: "apps/web/src/app/(app)/courses/_components/use-smart-defaults.client.ts"
      provides: "Hook useSmartDefaults (scaffold prêt pour Plan 01)"
      contains: "useSmartDefaults"
      min_lines: 20
    - path: "apps/web/src/app/(app)/courses/_components/duplicate-banner.client.tsx"
      provides: "Composant DuplicateBanner (scaffold prêt pour Plan 03)"
      contains: "DuplicateBanner"
      min_lines: 30
    - path: "apps/web/src/app/(app)/courses/_components/ride-status-fr.ts"
      provides: "Mapping FR humanisé des statuts course"
      contains: "STATUS_LABELS_FR"
    - path: "apps/web/src/app/(app)/patients/actions/index.ts"
      provides: "Barrel re-export des actions patients (≤ 100 L)"
      contains: "_existing"
    - path: "apps/web/src/app/(app)/patients/actions/_existing.ts"
      provides: "Contenu actuel de patients/actions.ts déplacé sans modification"
      contains: "use server"
  key_links:
    - from: "ride-express-modal.client.tsx"
      to: "use-smart-defaults.client.ts"
      via: "import { useSmartDefaults }"
      pattern: "useSmartDefaults"
    - from: "ride-express-modal.client.tsx"
      to: "duplicate-banner.client.tsx"
      via: "import { DuplicateBanner }"
      pattern: "DuplicateBanner"
    - from: "duplicate-banner.client.tsx"
      to: "ride-status-fr.ts"
      via: "import { STATUS_LABELS_FR }"
      pattern: "STATUS_LABELS_FR"
    - from: "patients/actions/index.ts"
      to: "patients/actions/_existing.ts"
      via: "export * from './_existing'"
      pattern: "_existing"
---

<objective>
Wave 0 — Refacto de décompression OBLIGATOIRE avant Wave 1 (Plans 01/02) et
Wave 2 (Plan 03). Le but est strictement préparatoire : extraire les sous-composants
et helpers du modal saisie express (actuellement 384 L, déjà au-dessus de la
limite CLAUDE.md § 11 de 300 L) ET transformer `patients/actions.ts` (293 L)
en barrel pour permettre l'ajout de `getPatientRideDefaultsAction` (Plan 01)
sans dépasser 300 L.

Aucun changement de comportement utilisateur visible. Le modal continue de
fonctionner exactement comme avant cette phase. Les hooks/composants extraits
sont des **scaffolds** : leur logique sera câblée par Plans 01 et 03.

Purpose : sans cette Wave 0, les Plans 01 (+15-25 L modal) et 03 (+40-50 L
modal) feraient passer le fichier à 432-449 L → breach CLAUDE.md § 11 §
codebase/CONVENTIONS.md § fichiers ≤ 300 L. Cette Wave 0 ramène le modal à
≤ 280 L pour offrir une marge.

Output : 4 nouveaux fichiers `_components/*` (scaffolds), 1 barrel
`patients/actions/index.ts`, 1 fichier déplacé `patients/actions/_existing.ts`.
Fichier modal réduit à ≤ 280 L. Fichier `patients/actions.ts` **supprimé**
(remplacé par le barrel).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/regle-neutralite-et-ton.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03.1-efficience-saisie-modal-course/03.1-CONTEXT.md
@.planning/phases/03.1-efficience-saisie-modal-course/03.1-UI-SPEC.md
@apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx
@apps/web/src/app/(app)/courses/_components/ride-express-form-fields.client.tsx
@apps/web/src/app/(app)/courses/actions/index.ts
@apps/web/src/app/(app)/patients/actions.ts

<interfaces>
**État actuel constaté** :
- `ride-express-modal.client.tsx` = 384 L (CLAUDE.md § 11 = limite 300 L → breach existante).
- `patients/actions.ts` = 293 L (déjà proche limite, +10-15 L Plan 01 = breach).
- Pattern barrel `actions/index.ts` déjà utilisé dans `courses/actions/` (référence).

**Scaffolds à créer (logique vide, contrats prêts)** :

1. `use-smart-defaults.client.ts` (hook, ~20-30 L) :
   ```typescript
   'use client';
   import { useTransition } from 'react';

   type SmartDefaultsInput = {
     rideId: string | undefined;
     currentMode: string;
     currentUrgency: string;
     onApply: (mode: string, urgency: string) => void;
   };

   // Scaffold : la logique d'appel `getPatientRideDefaultsAction` sera câblée
   // par Plan 03.1-01. Pour l'instant, expose une fonction `triggerForPatient`
   // qui est un no-op (return early).
   export function useSmartDefaults(input: SmartDefaultsInput) {
     const [, startTransition] = useTransition();
     async function triggerForPatient(patientId: string) {
       // Scaffold no-op — Plan 01 ajoutera l'appel Server Action.
       if (input.rideId) return;
       if (input.currentMode !== 'taxi_conventionne') return;
       if (input.currentUrgency !== 'programmee') return;
       // TODO Plan 01 : appel getPatientRideDefaultsAction + input.onApply(...)
     }
     return { triggerForPatient };
   }
   ```

2. `ride-status-fr.ts` (helper pur, ~8 L) :
   ```typescript
   export const STATUS_LABELS_FR: Record<string, string> = {
     validee: 'validée',
     assignee: 'assignée à un chauffeur',
     en_cours: 'en cours',
     terminee: 'terminée',
   };

   export function formatHourFR(iso: string): string {
     return new Date(iso).toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit' });
   }
   ```

3. `duplicate-banner.client.tsx` (composant React, ~40-50 L) — scaffold avec
   props mais conteneur vide pour l'instant (le composant accepte les props,
   mais la Wave 0 ne le rend nulle part dans le modal — c'est Plan 03 qui
   l'instanciera) :
   ```typescript
   'use client';
   import { Button } from '@/components/ui/button';
   import { STATUS_LABELS_FR, formatHourFR } from './ride-status-fr';

   export type DuplicateBannerProps = {
     duplicates: Array<{ id: string; scheduled_at: string; status: string }>;
     isPending: boolean;
     onCancel: () => void;
     onConfirm: () => void;
   };

   export function DuplicateBanner(props: DuplicateBannerProps) {
     if (props.duplicates.length === 0) return null;
     return (
       <div
         role="alert"
         aria-live="polite"
         className="rounded-md border border-amber-500/50 bg-amber-50 dark:bg-amber-950/30 px-16 py-12 space-y-8"
       >
         <p className="text-sm font-medium text-amber-900 dark:text-amber-100">
           Course en doublon détectée
         </p>
         <ul className="text-sm text-amber-900/80 dark:text-amber-100/80 space-y-4">
           {props.duplicates.slice(0, 3).map((d) => (
             <li key={d.id}>
               {formatHourFR(d.scheduled_at)} — {STATUS_LABELS_FR[d.status] ?? d.status}
             </li>
           ))}
         </ul>
         {/* `pt-4` (16 px Tailwind default — sur scale 4/8/12/16/24/32/48/64) */}
         <div className="flex gap-8 justify-end pt-4">
           <Button type="button" variant="outline" size="sm" disabled={props.isPending} onClick={props.onCancel}>
             Annuler
           </Button>
           <Button type="button" variant="destructive" size="sm" disabled={props.isPending} onClick={props.onConfirm}>
             Créer quand même
           </Button>
         </div>
       </div>
     );
   }
   ```

**Barrel patients/actions** (Option A — recommandé checker B2) :
- Renommer `apps/web/src/app/(app)/patients/actions.ts` → `apps/web/src/app/(app)/patients/actions/_existing.ts` (contenu inchangé).
- Créer `apps/web/src/app/(app)/patients/actions/index.ts` (~5 L) :
  ```typescript
  export * from './_existing';
  ```
- Vérifier que tous les imports `from '@/app/(app)/patients/actions'` continuent de résoudre (Next.js résout `actions/index.ts` automatiquement).

**Modal — extractions à effectuer** :
- Remplacer le mapping FR statut inline (s'il existe déjà via Phase 03) par import depuis `ride-status-fr`.
- Propager explicitement `rideId={props.rideId}` à `DateFreeformField` (préparation Plan 02 — chips masquées en édition).
- Importer (sans utiliser activement encore) `useSmartDefaults` et `DuplicateBanner` — l'instanciation viendra en Wave 1+.
  *Note* : si tsc/lint signale `unused-import`, garder les imports en tant que `// eslint-disable-next-line @typescript-eslint/no-unused-vars` temporaire OU instancier le hook avec un appel inerte (`const _smart = useSmartDefaults({ rideId: props.rideId, currentMode: state.transport_mode, currentUrgency: state.urgency, onApply: () => {} });`).
- Cibler une réduction du modal à ≤ 280 L. Si nécessaire, extraire aussi des sous-blocs JSX répétés en sous-composants locaux dans le même fichier ou en `_components/*`.

**Anti-patterns** :
- ❌ Ne pas modifier le comportement utilisateur. Aucune nouvelle feature.
- ❌ Ne pas ajouter d'icône Lucide dans le banner (cf. UI-SPEC anti-patterns).
- ❌ Ne pas changer les classes Tailwind D-A3-3 ou D-B3-4 (verbatim).
- ❌ Ne pas créer de nouveau composant `components/ui/` (CONTEXT.md anti-pattern).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraire ride-status-fr + use-smart-defaults + DuplicateBanner (scaffolds)</name>
  <files>
    apps/web/src/app/(app)/courses/_components/ride-status-fr.ts,
    apps/web/src/app/(app)/courses/_components/use-smart-defaults.client.ts,
    apps/web/src/app/(app)/courses/_components/duplicate-banner.client.tsx
  </files>
  <action>
Créer les 3 fichiers scaffolds dans `apps/web/src/app/(app)/courses/_components/`.

1. **`ride-status-fr.ts`** : Coller le contenu canonique de la section `<interfaces>` (mapping + helper `formatHourFR`). Aucune logique métier. Fichier ≤ 20 L.

2. **`use-smart-defaults.client.ts`** : Coller le scaffold canonique de `<interfaces>`. Le hook retourne `{ triggerForPatient }` avec une logique no-op qui respecte déjà les gardes D-A2-4 (`currentMode === 'taxi_conventionne'`) et D-A2-5 (`!rideId`). Plan 03.1-01 viendra ajouter l'appel à `getPatientRideDefaultsAction` et `input.onApply(mode, urgency)`. Fichier ≤ 40 L.

3. **`duplicate-banner.client.tsx`** : Coller le composant canonique de `<interfaces>`. Le composant est complet (markup amber, accessibilité role="alert", boutons disabled={isPending}, mapping FR statut via import). Il est juste **pas encore instancié** par le modal — Plan 03 le câblera. Fichier ≤ 60 L.

Tous les libellés en FR sobre (NFR-002). Aucun nom propre (NFR-001). Aucun émoji. Classes Tailwind D-B3-4 collées verbatim. Commentaire `pt-4` documenté : « 16 px Tailwind default, sur scale 4/8/12/16/24/32/48/64 ».
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck</automated>
  </verify>
  <done>
- Les 3 fichiers existent dans `apps/web/src/app/(app)/courses/_components/`
- `pnpm typecheck` GREEN
- Aucun fichier > 60 L
- `DuplicateBanner` accepte props `{ duplicates, isPending, onCancel, onConfirm }`
- `useSmartDefaults` retourne `{ triggerForPatient }` (no-op câblé Plan 01)
- `STATUS_LABELS_FR` exporté avec 4 clés (`validee`, `assignee`, `en_cours`, `terminee`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Convertir patients/actions.ts en barrel actions/index.ts (Option A checker B2)</name>
  <files>
    apps/web/src/app/(app)/patients/actions/index.ts,
    apps/web/src/app/(app)/patients/actions/_existing.ts
  </files>
  <action>
Migration du fichier `apps/web/src/app/(app)/patients/actions.ts` (293 L) vers un barrel pour préparer l'ajout de `getPatientRideDefaultsAction` (Plan 01) sans breach 300 L.

1. **Renommer / déplacer** : `git mv apps/web/src/app/(app)/patients/actions.ts apps/web/src/app/(app)/patients/actions/_existing.ts`.
   Le contenu de `_existing.ts` est strictement identique à l'ancien `actions.ts` (zéro modification du code applicatif — on déplace seulement). Garder l'en-tête `'use server'` en première ligne.

2. **Créer le barrel** `apps/web/src/app/(app)/patients/actions/index.ts` (~5 L) :
   ```typescript
   export * from './_existing';
   ```
   (Si `_existing.ts` exporte des types ou des constantes via `export *`, c'est résolu automatiquement. Si le code utilise des `export default`, ajouter explicitement `export { default } from './_existing';` — vérifier en lisant le fichier.)

3. **Vérification d'import-resolution** : tous les imports `from '@/app/(app)/patients/actions'` (ou chemin relatif équivalent) doivent continuer à résoudre. Next.js et tsc résolvent `actions/index.ts` quand on importe le chemin `actions` (dossier). Aucun import à modifier ailleurs dans le repo.

4. **Vérification de taille** : `_existing.ts` ≤ 300 L (il était à 293 L, identique au déplacement). `index.ts` ≤ 100 L (en fait ~5 L). Plan 01 ajoutera `apps/web/src/app/(app)/patients/actions/get-ride-defaults.ts` (~30 L self-contained) + mettra à jour le barrel : `export * from './get-ride-defaults';`.

5. **Anti-pattern explicite** : ne pas modifier la logique de `_existing.ts`. Ne pas y ajouter d'imports. Ne pas restructurer. C'est un déplacement byte-pour-byte (à l'exception du nom de fichier).
  </action>
  <verify>
    <automated>cd /home/user/TAP && test ! -f apps/web/src/app/\(app\)/patients/actions.ts && test -f apps/web/src/app/\(app\)/patients/actions/index.ts && test -f apps/web/src/app/\(app\)/patients/actions/_existing.ts && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
- `apps/web/src/app/(app)/patients/actions.ts` **n'existe plus** (remplacé par le barrel).
- `actions/index.ts` existe (≤ 100 L, contient `export * from './_existing';`).
- `actions/_existing.ts` existe (contenu identique à l'ancien `actions.ts`, ≤ 300 L).
- `pnpm typecheck` + `pnpm lint` GREEN — tous les imports continuent de résoudre.
- Aucune modification fonctionnelle du code des actions patients existantes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Décompresser ride-express-modal.client.tsx à ≤ 280 L</name>
  <files>apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx</files>
  <action>
**Objectif chiffré** : ramener `ride-express-modal.client.tsx` de **384 L à ≤ 280 L**, dégageant ~100 L de marge pour absorber Plan 01 (+15-25 L `handlePatientSelect` smart defaults) et Plan 03 (+40-50 L state doublons + appel checkDuplicate + instanciation `<DuplicateBanner>`).

**Étapes de décompression** :

1. **Importer les scaffolds** créés en Task 1 :
   ```typescript
   import { useSmartDefaults } from './use-smart-defaults.client';
   import { DuplicateBanner } from './duplicate-banner.client';
   import { STATUS_LABELS_FR, formatHourFR } from './ride-status-fr';
   ```
   (Garder ces imports même s'ils ne sont pas encore utilisés activement. Si le linter exige usage, instancier `useSmartDefaults` avec un `onApply` no-op pour activer le hook — la Wave 1 le rendra fonctionnel.)

2. **Si un mapping FR statut existait inline dans le modal** (par exemple un `STATUS_LABELS` local), **le supprimer** et utiliser l'import depuis `./ride-status-fr`. Économie estimée : 5-10 L.

3. **Propager `rideId` à `DateFreeformField`** : trouver le `<DateFreeformField ... />` dans le JSX et ajouter `rideId={props.rideId}` aux props (préparation Plan 02 — chips masquées en mode édition). Si `DateFreeformField` n'accepte pas encore `rideId`, ajouter `rideId?: string` à sa signature côté `ride-express-form-fields.client.tsx` ET wire `props.rideId` dans le markup (Plan 02 utilisera). Économie nette : 0 L mais évite un commit cross-fichiers en Plan 02.

4. **Extraire 2-3 blocs JSX répétitifs en sous-composants locaux** (dans le même fichier ou dans `_components/`) — cibles probables :
   - Bloc « patient picker + tags » (~25-35 L) → fonction locale `<PatientPickerSection ... />`
   - Bloc « champs date + heure » (~20-30 L) → fonction locale `<DateTimeSection ... />`
   - Bloc « actions footer » (~15-25 L) → fonction locale `<ModalActions ... />`

   Les extractions doivent rester **dans le même fichier** (functions locales React) sauf si elles dépassent 30 L, auquel cas créer un fichier `_components/ride-express-{nom}.client.tsx`.

   **Alternative** : si le modal contient une logique d'effets / hooks lourde (effets de fermeture, gestion clavier, focus management), extraire ces effets dans un hook custom `use-ride-modal-effects.client.ts` (~20-30 L).

5. **Mesurer après chaque extraction** :
   ```bash
   wc -l apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx
   ```
   Cible : **≤ 280 L**. Si à 290 L, continuer à extraire. Si à 250 L, s'arrêter (marge suffisante).

6. **Aucun changement comportemental visible** :
   - Le modal s'ouvre et se ferme exactement comme avant.
   - Les champs se remplissent exactement comme avant.
   - La soumission appelle exactement la même Server Action avec les mêmes paramètres.
   - **Aucune** instanciation de `<DuplicateBanner>` dans le JSX (Plan 03 le fera).
   - **Aucun** appel à `useSmartDefaults().triggerForPatient(...)` (Plan 01 le fera). Si le hook est instancié pour satisfaire le linter, son retour n'est pas utilisé en Wave 0.

7. **Vérification typecheck + lint** :
   ```bash
   pnpm typecheck && pnpm lint
   ```

8. **Smoke manuel local** (optionnel mais recommandé) : `pnpm dev`, ouvrir la modal saisie express, créer une course, vérifier qu'elle apparaît dans la liste. Aucune régression attendue.

**Anti-patterns** :
- ❌ Ne pas modifier le comportement utilisateur (zéro nouvelle feature en Wave 0).
- ❌ Ne pas créer de nouveau composant `components/ui/` (CONTEXT.md).
- ❌ Ne pas extraire dans `packages/` (ces composants restent app-local).
- ❌ Ne pas changer les Server Actions importées (`createRideAction`, `updateRideAction`, etc.).
- ❌ Ne pas ajouter de toast, animation, icône Lucide nouvelle.

Per CLAUDE.md § 11 (≤ 300 L par fichier), CONTEXT.md (anti-pattern refacto opportuniste limité aux hooks d'intégration), checker B1.
  </action>
  <verify>
    <automated>cd /home/user/TAP && [ "$(wc -l < apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx)" -le 280 ] && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
- `ride-express-modal.client.tsx` **≤ 280 L** (mesuré par `wc -l`).
- Les 3 imports scaffolds (`useSmartDefaults`, `DuplicateBanner`, `STATUS_LABELS_FR`) sont présents dans le fichier.
- `DateFreeformField` reçoit `rideId={props.rideId}` dans le JSX.
- Aucun mapping FR statut inline restant (consolidé dans `./ride-status-fr`).
- `pnpm typecheck` + `pnpm lint` GREEN.
- Aucun changement comportemental utilisateur visible (la modal fonctionne exactement comme avant).
  </done>
</task>

</tasks>

<verification>
**Vérification automatisée (CI)** :

1. `pnpm typecheck` GREEN.
2. `pnpm lint` GREEN.
3. Tailles de fichiers :
   ```bash
   wc -l apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx   # ≤ 280
   wc -l apps/web/src/app/\(app\)/courses/_components/use-smart-defaults.client.ts    # ≤ 40
   wc -l apps/web/src/app/\(app\)/courses/_components/duplicate-banner.client.tsx     # ≤ 60
   wc -l apps/web/src/app/\(app\)/courses/_components/ride-status-fr.ts               # ≤ 20
   wc -l apps/web/src/app/\(app\)/patients/actions/index.ts                           # ≤ 100
   wc -l apps/web/src/app/\(app\)/patients/actions/_existing.ts                       # ≤ 300
   ```
4. Absence de l'ancien fichier : `test ! -f apps/web/src/app/\(app\)/patients/actions.ts`.

**Vérification manuelle preview Vercel** (CLAUDE.md § 13.5) :

5. Login `regulateur@demo.tap`, ouvrir la modal saisie express : aucune régression visuelle, aucun nouveau banner, aucune nouvelle chip.
6. Créer une course : la course apparaît dans la liste comme avant.
7. Ouvrir une course en édition : les champs se remplissent comme avant.
8. Fermer la modal sans soumettre : aucun warning console.

**Vérification non-régression imports** :

9. Tous les fichiers qui importaient depuis `@/app/(app)/patients/actions` continuent de fonctionner (vérifié par typecheck).
</verification>

<success_criteria>
- `ride-express-modal.client.tsx` ≤ 280 L, débloquant Plans 01 et 03 sans breach 300 L.
- Scaffolds `useSmartDefaults`, `DuplicateBanner`, `ride-status-fr` créés et prêts à être câblés en Wave 1+.
- Barrel `patients/actions/index.ts` + `_existing.ts` en place, Plan 01 pourra ajouter `get-ride-defaults.ts` sans breach.
- Aucun comportement utilisateur modifié (zero feature en Wave 0).
- `pnpm typecheck` + `pnpm lint` GREEN.
- Aucun nom propre ajouté (NFR-001).
- Aucun toast, émoji, animation nouvelle (NFR-002).
- Aucun test Playwright nouveau (politique tests allégée Passe 1).
</success_criteria>

<anti_patterns>
- ❌ Ajouter une feature visible utilisateur en Wave 0 (réservé Wave 1+).
- ❌ Modifier la signature des Server Actions existantes (`createRideAction`, etc.).
- ❌ Câbler activement `useSmartDefaults` ou `DuplicateBanner` dans le modal (Plan 01/03).
- ❌ Modifier les classes Tailwind D-A3-3 ou D-B3-4 (verbatim CONTEXT.md).
- ❌ Créer un composant dans `components/ui/` (CONTEXT.md).
- ❌ Ajouter une dépendance npm.
- ❌ Ajouter un test Playwright.
- ❌ Toucher `_existing.ts` autrement que par déplacement byte-pour-byte.
</anti_patterns>

<output>
Après complétion, créer `.planning/phases/03.1-efficience-saisie-modal-course/03.1-00-SUMMARY.md`.

Commit atomique (FR, format `type(scope): description`) :
```
refactor(courses, patients): décompresser modal saisie express + barrel actions patients (Wave 0 phase 03.1)
```
</output>
