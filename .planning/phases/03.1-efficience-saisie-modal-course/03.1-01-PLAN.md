---
phase: 03.1
plan: 01
type: execute
wave: 1
depends_on:
  - 03.1-00
files_modified:
  - apps/web/src/app/(app)/patients/queries.ts
  - apps/web/src/app/(app)/patients/actions/get-ride-defaults.ts
  - apps/web/src/app/(app)/patients/actions/index.ts
  - apps/web/src/app/(app)/courses/_components/use-smart-defaults.client.ts
  - apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx
autonomous: true
requirements:
  - SAIS-07
  - NFR-001
  - NFR-002
tags:
  - smart-defaults
  - patient-picker
  - ride-modal
must_haves:
  truths:
    - "Sélectionner un patient ayant déjà une course validée/assignée/en cours/terminée pré-remplit silencieusement transport_mode et urgency dans la modal saisie express."
    - "Le pré-remplissage n'a lieu QUE si le formulaire est encore aux défauts V1 (`transport_mode === 'taxi_conventionne'` ET `urgency === 'programmee'`)."
    - "Le pré-remplissage n'a JAMAIS lieu en mode édition (`props.rideId` présent)."
    - "Aucun toast, aucune animation focus, AUCUN état `isPending` visible n'est émis lors du pré-remplissage (silent prefill D-A2-1)."
  artifacts:
    - path: "apps/web/src/app/(app)/patients/queries.ts"
      provides: "Fonction getPatientRideDefaults (lecture last ride)"
      contains: "getPatientRideDefaults"
    - path: "apps/web/src/app/(app)/patients/actions/get-ride-defaults.ts"
      provides: "Server Action getPatientRideDefaultsAction (self-contained ~30 L)"
      contains: "getPatientRideDefaultsAction"
      min_lines: 20
    - path: "apps/web/src/app/(app)/patients/actions/index.ts"
      provides: "Barrel mis à jour, re-export get-ride-defaults"
      contains: "get-ride-defaults"
    - path: "apps/web/src/app/(app)/courses/_components/use-smart-defaults.client.ts"
      provides: "Hook useSmartDefaults câblé sur getPatientRideDefaultsAction"
      contains: "getPatientRideDefaultsAction"
    - path: "apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx"
      provides: "Callback patient enrichi via useSmartDefaults().triggerForPatient(id)"
      contains: "triggerForPatient"
  key_links:
    - from: "ride-express-modal.client.tsx (handlePatientSelect)"
      to: "useSmartDefaults().triggerForPatient"
      via: "appel après updateField('patient_id', id)"
      pattern: "triggerForPatient"
    - from: "use-smart-defaults.client.ts"
      to: "getPatientRideDefaultsAction"
      via: "import + appel `await` (PAS startTransition — silent D-A2-1)"
      pattern: "getPatientRideDefaultsAction"
    - from: "actions/get-ride-defaults.ts (getPatientRideDefaultsAction)"
      to: "queries.ts (getPatientRideDefaults)"
      via: "import + wrapping getAuthContext + REGULATEUR_OR_DIRIGEANT"
      pattern: "getPatientRideDefaults\\("
---

<objective>
Implémenter A2 — Smart defaults `transport_mode` + `urgency` pré-remplis
silencieusement depuis la dernière course du patient lorsque celui-ci est
sélectionné dans la modal saisie express course. Référence Doctolib quick
booking.

Purpose : réduire d'une étape la saisie pour les patients récurrents
(dialyse, chimio, contrôles), conformément au Pilier 1 (UX qui donne
envie, 8h/jour).

Output : 1 query DB, 1 Server Action **self-contained** dans le barrel
patients/actions/ (Option A checker B2), câblage du hook `useSmartDefaults`
(scaffold créé Wave 0) sur l'action, hook activé dans le modal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/regle-neutralite-et-ton.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/03.1-efficience-saisie-modal-course/03.1-CONTEXT.md
@apps/web/src/app/(app)/patients/queries.ts
@apps/web/src/app/(app)/patients/actions/_existing.ts
@apps/web/src/app/(app)/patients/actions/index.ts
@apps/web/src/app/(app)/courses/_components/use-smart-defaults.client.ts
@apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx
@apps/web/src/app/(app)/courses/_components/use-ride-prefill.client.ts
@apps/web/src/app/(app)/courses/actions/list.ts

<interfaces>
**Pré-requis Wave 0 (Plan 03.1-00 LIVRÉ)** :
- Barrel `patients/actions/index.ts` + `_existing.ts` existent (ancien `actions.ts` supprimé).
- Scaffold `_components/use-smart-defaults.client.ts` existe avec signature :
  ```typescript
  export function useSmartDefaults(input: {
    rideId: string | undefined;
    currentMode: string;
    currentUrgency: string;
    onApply: (mode: string, urgency: string) => void;
  }): { triggerForPatient: (patientId: string) => Promise<void> };
  ```
- Le modal importe déjà ce hook (mais ne l'appelle pas encore activement).

**Décisions locked (D-A2-1..5)** :

- D-A2-1 : silent prefill (aucun toast, aucune animation focus, **AUCUN `isPending` visible** — donc PAS de `startTransition` wrap).
- D-A2-2 : `getPatientRideDefaults(patientId, organizationId)` dans `queries.ts`. Filtre status ∈ {validee, assignee, en_cours, terminee}, `archive=false`, `order by scheduled_at desc limit 1`. Retourne `{ transport_mode, urgency } | null`.
- D-A2-3 : Server Action `getPatientRideDefaultsAction(patientId)` self-contained (~30 L) dans **NOUVEAU** fichier `actions/get-ride-defaults.ts` (Option A barrel — checker B2). Re-exporté via `actions/index.ts`.
- D-A2-4 : N'écrase QUE si défauts V1 actifs (`mode === 'taxi_conventionne'` ET `urgency === 'programmee'`).
- D-A2-5 : Désactivé si `props.rideId` présent (mode édition).

**Note sur `startTransition`** (checker W6) : D-A2-1 mandate silent prefill, donc **AUCUN état de chargement visible**. Le côté chargement « modal bloque un tick » est toléré (await simple, aucune UI ne reflète `isPending` pour cet appel). Ne PAS wrapper l'appel dans `startTransition`. Si la latence devient gênante en pratique (> 200 ms), Phase 04+ pourra introduire un loader silencieux, mais V1 = simple `await`.

**Pattern Server Action self-contained dans le barrel (~30 L)** :
```typescript
// apps/web/src/app/(app)/patients/actions/get-ride-defaults.ts
'use server';

import { z } from 'zod';
import { getAuthContext } from '@/lib/auth-context';
import { REGULATEUR_OR_DIRIGEANT } from '@/lib/roles';
import { getPatientRideDefaults } from '../queries';

const schema = z.object({ patientId: z.string().uuid() });

export type PatientRideDefaultsState = {
  data?: { transport_mode: string; urgency: string } | null;
  error?: string;
};

export async function getPatientRideDefaultsAction(
  patientId: string,
): Promise<PatientRideDefaultsState> {
  const parsed = schema.safeParse({ patientId });
  if (!parsed.success) {
    return { error: 'Identifiant patient invalide.' };
  }
  try {
    const { user, organization } = await getAuthContext();
    if (!REGULATEUR_OR_DIRIGEANT.includes(user.role)) {
      return { error: 'Accès non autorisé.' };
    }
    const data = await getPatientRideDefaults(parsed.data.patientId, organization.id);
    return { data };
  } catch {
    return { error: 'Pré-remplissage indisponible.' };
  }
}
```

(Adapter les chemins d'import (`@/lib/auth-context`, `@/lib/roles`, etc.)
aux conventions réelles du repo — lire `_existing.ts` pour identifier les
imports déjà utilisés.)

**Barrel update** (`apps/web/src/app/(app)/patients/actions/index.ts`) :
```typescript
export * from './_existing';
export * from './get-ride-defaults';
```

**Câblage `useSmartDefaults`** (remplacer le scaffold no-op de Wave 0) :
```typescript
'use client';
import { getPatientRideDefaultsAction } from '@/app/(app)/patients/actions';

type SmartDefaultsInput = {
  rideId: string | undefined;
  currentMode: string;
  currentUrgency: string;
  onApply: (mode: string, urgency: string) => void;
};

export function useSmartDefaults(input: SmartDefaultsInput) {
  async function triggerForPatient(patientId: string) {
    if (input.rideId) return;                                    // D-A2-5
    if (input.currentMode !== 'taxi_conventionne') return;       // D-A2-4
    if (input.currentUrgency !== 'programmee') return;           // D-A2-4
    const result = await getPatientRideDefaultsAction(patientId); // D-A2-1 (no startTransition)
    if (!result?.data) return;
    input.onApply(result.data.transport_mode, result.data.urgency); // D-A2-3
  }
  return { triggerForPatient };
}
```

**Câblage modal `handlePatientSelect`** :
```typescript
const smartDefaults = useSmartDefaults({
  rideId: props.rideId,
  currentMode: state.transport_mode,
  currentUrgency: state.urgency,
  onApply: (mode, urgency) => {
    updateField('transport_mode', mode);
    updateField('urgency', urgency);
  },
});

async function handlePatientSelect(id: string) {
  updateField('patient_id', id);
  await smartDefaults.triggerForPatient(id);
}
```

Reformulation FR sur erreur Supabase : silencieuse (return null côté query,
return `{ error: '...' }` côté action — le hook ignore les erreurs et ne
montre rien). Aucun toast.

Per CLAUDE.md § 11, CONVENTIONS.md (Server Actions pattern), D-A2-1..5,
checker W6, checker B2 (Option A barrel).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter query getPatientRideDefaults + Server Action self-contained dans le barrel</name>
  <files>
    apps/web/src/app/(app)/patients/queries.ts,
    apps/web/src/app/(app)/patients/actions/get-ride-defaults.ts,
    apps/web/src/app/(app)/patients/actions/index.ts
  </files>
  <action>
**Pré-requis** : Wave 0 (Plan 03.1-00) a converti `patients/actions.ts` en
barrel `patients/actions/index.ts` + `_existing.ts` (Option A checker B2).
**Vérifier** : `test -f apps/web/src/app/\(app\)/patients/actions/index.ts && test ! -f apps/web/src/app/\(app\)/patients/actions.ts`.

1. **`apps/web/src/app/(app)/patients/queries.ts`** (+~20 L, fichier final ≤ 300 L) :
   ajouter `getPatientRideDefaults(patientId: string, organizationId: string)`.

   Implémentation : SELECT `transport_mode, urgency` FROM `rides` WHERE
   `patient_id = $1` AND `organization_id = $2` AND `archive = false` AND
   `status IN ('validee','assignee','en_cours','terminee')` ORDER BY
   `scheduled_at DESC` LIMIT 1.

   Utiliser le client Supabase serveur conforme au pattern des autres
   queries du fichier (typage via `types.gen.ts`). En cas d'erreur Supabase
   ou résultat vide : `return null` (silent fail — D-A2-1).

   Retour : `{ transport_mode: TransportMode; urgency: Urgency } | null`.

2. **NOUVEAU fichier** `apps/web/src/app/(app)/patients/actions/get-ride-defaults.ts`
   (self-contained ~30 L, NE PAS toucher `_existing.ts`) :

   Coller le pattern canonique de la section `<interfaces>` :
   - `'use server'` en tête.
   - Schéma zod `z.object({ patientId: z.string().uuid() })`.
   - Export `getPatientRideDefaultsAction(patientId: string)` :
     1. `safeParse` ; si échec : `return { error: 'Identifiant patient invalide.' }`.
     2. `getAuthContext()` + assertion rôle REGULATEUR_OR_DIRIGEANT (reformulation FR).
     3. Appel `getPatientRideDefaults(parsed.data.patientId, organization.id)`.
     4. `return { data }`.
     5. Sur exception : `return { error: 'Pré-remplissage indisponible.' }`.

   Aucun `revalidatePath` (lecture seule, non mutative). Adapter les chemins
   d'import (`@/lib/auth-context`, `@/lib/roles` ou équivalents) aux imports
   utilisés dans `_existing.ts`.

3. **Mettre à jour le barrel** `apps/web/src/app/(app)/patients/actions/index.ts` (+1 L) :
   ```typescript
   export * from './_existing';
   export * from './get-ride-defaults';
   ```

   Si `_existing.ts` exporte un identifiant en conflit (peu probable), utiliser
   `export { getPatientRideDefaultsAction } from './get-ride-defaults';` à la place.

Per D-A2-2, D-A2-3, checker B2 Option A.

**Note de design (checker W6)** : la Server Action retourne `null | { transport_mode, urgency }`
côté `data`, ne wrap PAS dans `startTransition` côté hook (Plan 03.1-01 Task 2).
Le `await` simple est suffisant et conforme à D-A2-1 (silent prefill).
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck && [ "$(wc -l < apps/web/src/app/\(app\)/patients/queries.ts)" -le 300 ] && [ "$(wc -l < apps/web/src/app/\(app\)/patients/actions/get-ride-defaults.ts)" -le 100 ] && [ "$(wc -l < apps/web/src/app/\(app\)/patients/actions/_existing.ts)" -le 300 ]</automated>
  </verify>
  <done>
- `getPatientRideDefaults` exporté depuis `patients/queries.ts`, signature `(patientId, organizationId) → { transport_mode, urgency } | null`.
- Fichier NOUVEAU `actions/get-ride-defaults.ts` créé (≤ 100 L, ~30 L), exportant `getPatientRideDefaultsAction`.
- Barrel `actions/index.ts` met à jour avec `export * from './get-ride-defaults'`.
- `_existing.ts` **NON modifié** (≤ 300 L identique à Wave 0).
- `pnpm typecheck` GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Câbler useSmartDefaults sur getPatientRideDefaultsAction + activer dans le modal</name>
  <files>
    apps/web/src/app/(app)/courses/_components/use-smart-defaults.client.ts,
    apps/web/src/app/(app)/courses/_components/ride-express-modal.client.tsx
  </files>
  <action>
**Pré-requis** : Wave 0 a créé `use-smart-defaults.client.ts` (scaffold no-op)
et le modal l'importe déjà (peut-être inerte). Modal ≤ 280 L après Wave 0.

1. **Remplacer le scaffold** de `use-smart-defaults.client.ts` par
   l'implémentation câblée sur l'action (~25 L total).

   Coller le pattern canonique de `<interfaces>` :
   ```typescript
   'use client';
   import { getPatientRideDefaultsAction } from '@/app/(app)/patients/actions';

   type SmartDefaultsInput = {
     rideId: string | undefined;
     currentMode: string;
     currentUrgency: string;
     onApply: (mode: string, urgency: string) => void;
   };

   export function useSmartDefaults(input: SmartDefaultsInput) {
     async function triggerForPatient(patientId: string) {
       if (input.rideId) return;                                       // D-A2-5
       if (input.currentMode !== 'taxi_conventionne') return;          // D-A2-4
       if (input.currentUrgency !== 'programmee') return;              // D-A2-4
       const result = await getPatientRideDefaultsAction(patientId);
       if (!result?.data) return;
       input.onApply(result.data.transport_mode, result.data.urgency); // D-A2-3
     }
     return { triggerForPatient };
   }
   ```

   **NOTE checker W6** : PAS de `startTransition`, PAS de state `isPending`. Le
   prefill est silencieux par décision D-A2-1. Le côté chargement (modal bloque
   un tick le temps de l'await) est toléré et aucune UI ne le reflète. La modal
   peut être interagissable pendant l'await mais c'est attendu et invisible.

2. **Activer le hook dans `ride-express-modal.client.tsx`** :

   En haut du composant, instancier le hook (remplacer l'éventuelle
   instanciation inerte de Wave 0) :
   ```typescript
   const smartDefaults = useSmartDefaults({
     rideId: props.rideId,
     currentMode: state.transport_mode,
     currentUrgency: state.urgency,
     onApply: (mode, urgency) => {
       updateField('transport_mode', mode);
       updateField('urgency', urgency);
     },
   });
   ```

   (Adapter `state.transport_mode`, `state.urgency`, `updateField` aux noms réels
   du state/updater du modal — lire les ~20 premières lignes du composant pour
   identifier les hooks d'état réels.)

3. **Modifier `handlePatientSelect`** (callback existant ou créer si absent) :
   ```typescript
   async function handlePatientSelect(id: string) {
     updateField('patient_id', id);
     await smartDefaults.triggerForPatient(id);
   }
   ```

   Le hook se charge des gardes (D-A2-4, D-A2-5) — pas besoin de les répéter
   dans le callback.

4. **Aucune autre modification** au modal. Pas de refacto opportuniste.

5. **Anti-refacto** : si le modal dépasse 295 L après cette modification,
   c'est anormal (Wave 0 devait laisser ≥ 20 L de marge). Investigation
   requise avant commit.

Per D-A2-1, D-A2-3, D-A2-4, D-A2-5, checker W6.
  </action>
  <verify>
    <automated>cd /home/user/TAP && pnpm typecheck && pnpm lint && [ "$(wc -l < apps/web/src/app/\(app\)/courses/_components/ride-express-modal.client.tsx)" -le 300 ]</automated>
  </verify>
  <done>
- `use-smart-defaults.client.ts` câblé sur `getPatientRideDefaultsAction`, gardes D-A2-4 et D-A2-5 respectées, **aucun `startTransition`** (checker W6).
- Modal instancie le hook et appelle `triggerForPatient(id)` après `updateField('patient_id', id)`.
- Aucun toast, aucune animation, aucun état isPending visible.
- `pnpm typecheck` + `pnpm lint` GREEN.
- Modal ≤ 300 L (cible ≤ 295 L après marge Wave 0).
  </done>
</task>

</tasks>

<verification>
Vérification fonctionnelle manuelle sur preview Vercel (cf. CLAUDE.md § 13.5) :

1. Login `regulateur@demo.tap`.
2. Ouvrir saisie express (Cmd+Shift+K ou bouton dédié).
3. Sélectionner un patient ayant ≥ 1 course passée non-annulée.
4. Vérifier que les champs `transport_mode` et `urgency` se remplissent
   silencieusement avec les valeurs de la dernière course.
5. Vérifier qu'aucun toast n'apparaît, aucun spinner, aucune animation focus.
6. Changer manuellement `urgency` puis re-sélectionner un autre patient :
   `urgency` ne doit PAS être écrasé (D-A2-4 garde défauts V1).
7. Ouvrir une course existante en édition (`rideId` présent) : le
   pré-remplissage smart defaults ne se déclenche pas (D-A2-5).
</verification>

<success_criteria>
- Une régulatrice sélectionnant un patient récurrent voit instantanément
  mode + urgence pré-remplis sans rétroaction visuelle intrusive.
- `pnpm typecheck` + `pnpm lint` GREEN sur preview CI.
- Aucun fichier > 300 L (queries.ts, _existing.ts, get-ride-defaults.ts, modal).
- Aucun nom propre ajouté (NFR-001).
- Aucun toast ni animation ajouté (D-A2-1).
- Aucun `startTransition` autour de l'appel `getPatientRideDefaultsAction` (checker W6).
- Aucun test Playwright nouveau (politique tests allégée Passe 1, CLAUDE.md § 9).
</success_criteria>

<anti_patterns>
- ❌ Toast, animation focus, ou état isPending visible sur le prefill (D-A2-1 silent).
- ❌ Wrap `startTransition` autour de `getPatientRideDefaultsAction` (checker W6).
- ❌ Écraser un choix manuel (D-A2-4 : garde défauts V1 obligatoire).
- ❌ Déclencher en mode édition (D-A2-5 : garde !rideId obligatoire).
- ❌ Utiliser `useActionState` (CONTEXT anti-pattern : garder useState + useTransition).
- ❌ Modifier `_existing.ts` (Wave 0 = byte-pour-byte).
- ❌ Refacto opportuniste du modal au-delà de l'instanciation du hook + appel callback.
- ❌ Nouveau composant `components/ui/` ou dépendance npm.
- ❌ Test Playwright nouveau.
</anti_patterns>

<output>
Après complétion, créer `.planning/phases/03.1-efficience-saisie-modal-course/03.1-01-SUMMARY.md`.

Commit atomique (FR, format `type(scope): description`) :
```
feat(courses): smart defaults mode + urgence depuis dernière course patient (A2)
```
</output>
