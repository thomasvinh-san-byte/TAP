'use client';

/**
 * Stub temporaire — implémentation complète livrée en Task 4.2.
 * Présent ici pour permettre l'import dans `(public)/layout.tsx`
 * sans casser le typecheck à mi-plan.
 */
export function CookieBanner() {
  return null;
}
